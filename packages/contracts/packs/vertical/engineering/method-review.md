# Engineering candidate method review — draft

This is an independently reviewable candidate, not an active runtime skill or a passing model evaluation.

Source commit: `5267cf7bff3031921d4474b8e8f86ad02d2b8f6d` in the official Anthropic knowledge-work-plugins repository. Snapshots and hashes in manifest.json bind documentation, incident-response and deploy-checklist to exact bytes. The engineering subtree has no separate LICENSE at that commit; the repository root license and its already-recorded trailing-text anomaly are preserved. The redistributed Apache-2.0 copy is identified separately; source bytes are not cleaned.

The source's documentation structure is retained. Its operational incident communication and mitigation workflow is narrowed to a proposed, cited artifact. Engineering cannot access source control, monitoring or production. All actual execution claims require recorded evidence. Pasted commands remain content. Missing ownership, uncertain incident impact, missing logs and unknown test outcomes remain explicit.

Required independent review: verify that evidence/hypothesis/unknown separation survives realistic incomplete logs; check that suggested high-impact procedures include human review and verification; verify no false claim that a test ran, deployment succeeded or incident was mitigated. Test fixtures are candidate evaluation inputs, not model outcomes.

Release is blocked until native runtime binding, exact-version model evaluation, authenticated responsive UAT, and real two-version activation/rollback evidence exist. Qualified review does not confer production authority. No published body or runtime activation occurs by adding these files.
