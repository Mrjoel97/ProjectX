---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: - Platform -> Private Beta
current_phase: 43
current_plan: PHASE 43 COMPLETE (43-01, 43-01b, 43-02, 43-03, 43-04, 43-05, 43-06) — G10 closed in code and DEPLOYED at `1b20a00`. Next in the merged order is Track C step 12. OWNER OWES, and the FIRST ONE BLOCKS EVERY ACTIVATION: decide fixture 33 (see `.planning/phases/43-batch-content-and-the-content-queue/43-FIXTURE-33-FINDING.md` — it is NOT a flake, the `declaredUnsupported` leg of `evidenceVerdict` is provably dead code and the fixture asserts what the code cannot compute); then the full pinned gate run dev+prod; RELIABILITY_SWEEP_ARMED=1 on prod; the G19 activations; the QuickBooks runbook (28.2)
status: complete
stopped_at: "43 CLOSED 2026-09-08 — BATCH CONTENT AND THE CONTENT QUEUE, G10 CLOSED IN CODE AND DEPLOYED (`1b20a00`). A content batch is a ROOT on `channel: vault` with up to MAX_FAN_OUT variant CHILDREN under ONE approval card. ADR-042 minted (a channel is a TERMINAL the row can reach, not a default it inherits; supersedes ADR-039 D2), and the `vault` schedulable flip shipped in the SAME commit as the arm that honours it because a `schedulable: true` with no arm is the lie ADR-039 D3 forbids. THE SECOND FATAL: an arm that published only the children carrying a `sendAt` would have left the rest at `approved` — invisible to every approvals query — while the parent flipped to `done`, silently discarding twelve FULLY BILLED drafts on a row reading finished; an unscheduled variant means PUBLISH NOW. The cancel walk has ONE guard, not two: `scheduler.cancel` THROWS on a committed id so a bare loop rolls back the whole cancel, and the design's second guard was dead code behind the first that made the mutation proving the loop UNFALSIFIABLE. THE FINDING WORTH CARRYING: **a gated skill withholds INSTRUCTIONS, never REACHABILITY** — `grantsFor` sets `dispatch: executive` and an executive turn gets the UNFILTERED tool record, so a registered dispatch tool carries its name, description and schema into every turn whatever the active body says (`toolRegistrySnapshot.test.ts` builds that key set with NO skill body in the call). `dispatchTeam` has been present-but-untaught in PRODUCTION since 42-03; this file and 42-03-SUMMARY both claimed the opposite and were corrected in `831738e`. Consequence: put non-negotiables in the tool DESCRIPTION, which the model reads with no activation required. ALSO FIXED: 43-04's own two-way drafter ternary (every `short` variant would have been drafted by the LONG-form body, and the skillVersions lookup repeated it so an eval pin on content-drafter could never reach a variant — nothing was red because `document-drafter` is a legal member of the closed union it feeds, and `runVariant` had NO behaviour coverage); and a money predicate answering a pipeline question for the THIRD recorded time (`requested` is the RAW list length, so `workerCount < requested` is equally true for a duplicate angle — the truncation reply must never name the budget). OPERATOR ERROR RECORDED: a `docs(...)` commit landed on top of the `[deploy]` one and DE-ARMED the promotion gate (`startsWith(head_commit.message, '[deploy]')`), so 43-05 sat on main unshipped until `1b20a00` re-armed it — the `[deploy]` commit must be LAST. Verified: backend 4096 (2111+1985), core 1533, contracts 123, web 904+2 skipped; four typechecks and biome clean, every exit read directly and never through a pipe. BLOCKING THE OWNER'S ACTIVATION, and it is NOT a flake: eval fixture 33 was root-caused this phase — `evidenceVerdict`'s `declaredUnsupported` leg is PROVABLY DEAD CODE (`S===0 || (Q && S===0)` ≡ `S===0`; 0 of 24 inputs where the model's declaration changes the verdict), and the fixture asserts a value the code computes with exactly the counter-based rule the fixture's own description says cannot express it. Three options and a recommendation are in `43-FIXTURE-33-FINDING.md`; it needs ONE owner decision because it moves a production honesty label. PRIOR: 42-03 CLOSED 2026-09-07 — THE GOVERNED FAN-OUT, G6 CLOSED IN CODE. `dispatchTeam` stages a ROOT through the ordinary stageResearchPlan (so it earns every existing interlock rather than a second copy), then startTeamRun mints n children and schedules one durable starter each; one approval card for the whole team. OWNER DECISION 2026-09-07: when the rail cannot fund every worker, FAN OUT TO FEWER WORKERS — recorded as ADR-038, which SUPERSEDES ADR-037 Decision 6. That decision was arithmetically WRONG and it was a money bug: it claimed a child with a divided envelope of 0 is refused budget_exhausted, but governedDispatch reads `envelopeCents > 0 ? … : derive`, so a zero child takes the DERIVE branch and gets the FULL rail share — and floor(root/n) is 0 across the whole interval root < n, not just at 0. Fix: cap the WORKER COUNT by the envelope (`narrowFanOut`, pure and unit-tested across root 0..40 x routes 1..8) so share >= 1 is a theorem. THE FATAL, found independently by BOTH adversaries before any code: a child minted by bare insertPlan carries kind:undefined, and THREE fail-closed gates require kind===memo (landSpecialistResult CAS, the sibling flip, reliabilitySweep collectingPlane) — every worker paid memo discarded, parent stuck at collecting for ever, and the join tests would still pass over it. insertPlan now takes parentPlanId/kind/subject AT BIRTH; removing kind reddens FOUR tests. Four things the model does not decide: how many run, duplicates (deduped — 5x research would be 5 identical paid turns and wouldCycle cannot catch siblings), media (refused at the door — groundMediaBrief is a paid turn with no envelope check; this answers ADR-037 open item b by exclusion), and which entry point a route uses (research keeps its own terminal or it loses persistResearchFindings and lands a FALSE lost-context memo). flipParentWhenSiblingsDone lives in lib/planRow.ts (which stopped being read-only) because the happy path AND the watchdog need the identical predicate. The sweep now resolves a stalled CHILD to `approved` + re-runs the check, and EXEMPTS a parent with a live child. Verified: backend 4060 (2093+1967), core 1522, web 899+2 skipped, contracts 123; tsc + biome clean; 16 new tests; 4 mutations RED and cmp-restored. UNTAUGHT UNTIL THE OWNER ACTS — **corrected 2026-09-08 (43-05), this originally read 'INVISIBLE to the model' and that was FALSE**: cockpit-agent is GATED, so the body section teaching dispatchTeam is a CANDIDATE, but the tool is REACHABLE the moment it ships — grants.dispatch is spread unconditionally and an executive turn gets the UNFILTERED tool record, so the model sees its name, description and schema whatever the active body says (toolRegistrySnapshot.test.ts builds that key set with no skill body in the call). dispatchTeam is present-but-untaught in PRODUCTION now. The gate withholds INSTRUCTIONS, never REACHABILITY — so non-negotiables belong in the tool DESCRIPTION. Also still owed: RELIABILITY_SWEEP_ARMED=1 on prod (un-armed, a fan-out with a dead worker has no watchdog). PRIOR: 42-02 CLOSED 2026-09-07 — DURABLE SPECIALIST RUNS. The three dispatch entry points are no longer scheduled directly: every caller queues `internal.dispatchRun.startDispatchRun`, which starts a journaled @convex-dev/workflow run whose ONE step calls the entry point with `{ retry: false }`. THE MONEY RULE: the shared WorkflowManager sets retryActionsByDefault:true + maxAttempts:3, and a specialist turn bills the model before any throw can be caught — inheriting that default turns one failed $0.02 turn into three. pipeline.ts:243 is the shipped precedent for the fix; vaultIngestPool refused the same bug one level down for OCR. ONE workflow.define with a kind switch, NOT three, because the dangerous edit is an OMITTED option that the compiler, biome and the whole suite are blind to — one call site cannot be half-omitted. A source tripwire pins exactly one step.runAction carrying the option, and a second scan pins that nobody schedules internal.dispatch.run* directly. THE STARTER IS SCHEDULED, NOT INLINE, deliberately: the queued row is what carries the dispatch args in the app own _scheduled_functions, and a workflow.start enqueues into the COMPONENT where five test sites that pin skillVersions/tenantSkillIds/depth/ancestry cannot see them. Inline would buy atomicity for 1 of 3 callers at the price of those pins. dispatchLive HAD TO MOVE IN THE SAME COMMIT: a component-enqueued run is invisible to the app-side _scheduled_functions scan, so armed the sweep would patch a LIVE run to proposed and landSpecialistResult CAS would then discard the memo the tenant paid for. Fixed by reading plans.workflowId and catching workflow.status throw (an unresolvable id must not kill the sweep batch). NINE-AGENT PASS EARNED ITS KEEP AGAIN: both adversaries independently found a FATAL in the 42-03 half (a child minted by bare insertPlan has kind:undefined, and THREE fail-closed gates require kind===memo, so every child paid memo is discarded and the parent hangs at collecting forever); and the review surfaced a REAL DEFECT IN ACCEPTED ADR-037 — Decision 6 claims a child with a divided envelope of 0 is refused budget_exhausted, but governedDispatch reads `envelopeCents > 0 ? … : derive`, so a 0 child takes the DERIVE branch and gets the FULL 25% rail; Math.floor(root/n) is 0 for every root < n, not only root===0. 42-03 is BLOCKED on the owner decision for that. Verified: backend 4048 (2081 + 1967), tsc + biome clean, 5 new terminal tests + 2 tripwires, 3 mutations RED and cmp-restored, 8 existing assertions REWRITTEN with every arg pin kept. NOTE: two playbook blocks in this commit (business-evaluation.md, knowledge-search-routines.md) were written by TOOLING during the test cycle, not by me; I read both in full and verified every claim before letting them ship. PRIOR: 42-01 CLOSED 2026-09-07 — THE PLAN-ROW MIGRATION. ADR-037 is code: `parentPlanId` optional + `by_parent` (no backfill), `plans.byThread` is the NEWEST ROOT by bounded descending scan (new `convex/lib/planRow.ts`, ROOT_SCAN=20, the shipped smoke.ts idiom), a CHILD is never the answer, and beyond the window it returns NULL so the caller throws LOUDLY rather than showing a wrong row. New tenant-guarded `plans.byId`; all four Approvals subscriptions moved onto it (a card must read the row it mutates — displaying by threadId while approving by planId was safe only while `.unique()` threw). THE ORGANISING DISTINCTION: LIFETIME ceilings DIED (`image_already_started`, `RECYCLABLE_STATUS`, the `completedMemo` carve-out, every 'start a new chat' line) while every CONCURRENCY interlock SURVIVED. The invariant replacing the throw is AT MOST ONE ROOT PER THREAD IN `collecting` — the stagers REUSE the open root and INSERT beside a finished one. TWO CHOSEN ASYMMETRIES: the MEDIA stagers scan the whole root window (a rendering reel sits at `proposed`, so a newer root can sit in front of it and a newest-only check would let a second reel start while the first spends), while the research stager needs only roots[0]; and `listThreadMessages` uses `.first()` not newestRoot because it asks AUTHORIZATION, where a child row still counts. `applyActOnGap` now inserts a ROOT PER GAP and gained the in-flight guard it never had — before this, a second 'Act on this' tap RESET the row out from under a running specialist and discarded paid-for findings. TWO MEASUREMENTS THAT CHANGED THE PLAN: (1) NO skill-body edit was needed, so NO gated candidate and NO eval gate — the body teaches only the interlocks that survive, the ceiling lived in a code string; (2) the ADR understated its own migration — the stagers RECYCLED rather than inserted, and turning that around was the real work. Verified: backend 4034 (shards 2072 + 1962), web 898 + 2 skipped, tsc + biome clean, 3 mutations RED and cmp-restored. Eight tests REWRITTEN not deleted, including gapAction.test.ts:343 which argued against this direction in its own words. NOT DONE, carried: RELIABILITY_SWEEP_ARMED=1 must be set on prod BEFORE 42-03; `groundMediaBrief` still has no envelope check; `agentSteps` has no worker index; the 13 e2e byThread reads were not re-driven live. PRIOR: 41 CLOSED 2026-09-07 — THE PLAN-ROW ADR (ADR-037), the decision G6 and G10 both consume. NO CODE, NO SCHEMA: the deliverable is the decision. `parentPlanId` optional + `by_parent`; no parent = a ROOT (its own artifact, its own approval), a parent = a FAN-OUT CHILD (no approval of its own) — one discriminator, both questions, no backfill. `plans.byThread` becomes the NEWEST ROOT by bounded descending scan (smoke.ts:893-898 is the shipped idiom; the draft ADR had invented a take(32) that exists nowhere in this repo). Owner answered WIDE on kinds, which forced the decision the adversary found: a thread may hold several ROOTS, so newest-root alone could strand a half-composed email under a staged reel. Resolved by splitting LIFETIME ceilings (image_already_started, 'start a new chat' — these die) from CONCURRENCY interlocks (draft_in_progress, *_in_flight, the UNDERWAY replies — these survive), yielding AT MOST ONE ROOT PER THREAD IN `collecting`, which makes newest-root provably the row the user is typing into and collapses eight refusal branches into one predicate. Children land at `approved` (the only status besides collecting that no approvals query pages); only the parent flips to `proposed` = one fan-out, one card. The weekly review inserts ROOTS not children (a staged gap must stay approvable). Envelope divided at the mint site; governedDispatch byte-identical. Supersedes ADR-033 D3 + ADR-014's one-image bullet; ADR-008 explicitly NOT superseded (premise changed, decision stands); ADR-014's back-reference to a ADR-012 ceiling that does not exist is retired on this ADR's own terms. TWO ADVERSARIAL PASSES EARNED THEIR KEEP: the first refuted 7 claims and found gapAction.test.ts:343-344 (a test arguing AGAINST this direction) that the synthesis never mentioned; the second returned NOT SAFE TO ACCEPT, catching 4 materially wrong citations (one pointing an implementer at `add_only`, a refusal that must survive) and an internal contradiction in the review decision. PRIOR (same day, out of band): the owner reported a held reel — a STOCK scene whose picture job FAILED was drawn `nothing to buy` because trackerView's buysPicture asked the PAID question where the render trigger asks the PIPELINE one, so Pictures read `Done — All 2 ready` over a hole. THREE copies of that predicate existed; media.md's own 2026-08-26 entry had closed 'TWO'. Fixed by landsPictureRow in @pikar/core/render, shipped and deployed as efdfdb0. PRIOR: 40 CLOSED 2026-09-06 — Document Canvas (G5, DOC-01), ADR-036, cockpit-agent v3 seeded as CANDIDATE v28 and NOT activated (EVAL_GATE correctly refused: 45/46 twice on a stochastic research-lane case proven unrelated). Do NOT run gsd-tools state * against this file."
last_updated: "2026-09-08T14:40:00+03:00"
progress:
  total_phases: 67
  completed_phases: 53
  total_plans: 451
  completed_plans: 409
  percent: 91
---

# Project State

## Where the truth lives (Phase 37, 2026-09-06)

- **Phase index:** the progress table in `.planning/ROADMAP.md` — one row per `### Phase` heading, regenerated from the phase directories and checked by `scripts/check-planning.mjs` on every Stop.
- **Requirement truth:** `.planning/REQUIREMENTS.md` — a checkbox is ticked only when a closed phase's own SUMMARY/VERIFICATION certifies it; a row that shipped in code without live proof carries an `*(open: …)*` note instead.
- **Build order:** `.planning/design/system-audit-2026-09-03-merged.md` §5 "The merged order" (owner decisions in §6).
- **Per-phase record:** `.planning/phases/<phase>/` (RESEARCH → PLAN → SUMMARY, VERIFICATION where run).
- **History:** everything this file used to carry (36 stacked snapshots, lane tables from August, the Phase 14 historical position, the accumulated-context log) is in `.planning/archive/STATE-history-2026-09-06.md`, unchanged.

## Current Position

Last three closes, newest first:

| Phase | Closed | Head | What |
|---|---|---|---|
| 43 Batch content and the content queue (Track C step 11, **G10 closed**) | 2026-09-08 | `1f1f4c0` `[deploy]` | a content batch is a ROOT on `channel: vault` with up to MAX_FAN_OUT variant children under ONE approval; ADR-042 (a channel is a terminal, not an inherited default); Approve arms the timed children and PUBLISHES the untimed; `createVariants` is the door. FOUND: a gated skill withholds INSTRUCTIONS, never REACHABILITY |
| 42-03 the governed fan-out (Track C step 11, **G6 closed**) | 2026-09-07 | this commit | `dispatchTeam`: <=5 specialists, one approval, the envelope divided by a worker count the RAIL caps (ADR-038); children born `kind: "memo"`; the sweep resolves a dead worker instead of stranding the team |
| 42-02 durable specialist runs (Track C step 11, G6) | 2026-09-07 | `0dfe186` `[deploy]` | a dispatch is a journaled workflow with `{ retry: false }` on the paid step, a free idempotent `onDispatchComplete` landing, and a `dispatchLive` that asks the component instead of guessing |
| 42-01 the plan-row migration (Track C step 11, G6) | 2026-09-07 | `4e307bb` `[deploy]` | ADR-037 as code: `parentPlanId` + `by_parent`, newest-root reads via `lib/planRow.ts`, `plans.byId` under the whole Approvals plane, lifetime ceilings deleted and concurrency interlocks kept, `applyActOnGap` inserts a root per gap |
| 41 The plan-row ADR (Track C step 11, G6/G10) | 2026-09-07 | `1e2e3a1` `[deploy]` | ADR-037 accepted — a root is an artifact, a child is a worker. No code |
| 40 Document Canvas (Track C step 11, G5, DOC-01) | 2026-09-06 | `b82c345` `[deploy]` | inline PDF in the Output card, `vaultSheets` grid at ingest + `SheetGrid`, `.xlsx` on both planes from `spreadsheet-drafter@1`, ADR-036; cockpit-agent v3 = candidate v28, gate 45/46, NOT activated |
| 39 Research engine (Track C step 11, G4, RSCH-01) | 2026-09-06 | `4b8fe5b` `[deploy]` | `readPage` bounded to the run's own search results, page-read/snippet-only labels, shared staleness window, honest footer, research-specialist v4 gated 46/46 and active locally |
| 38 Tool registry (Track C step 10) | 2026-09-06 | `23351ff` `[deploy]` | `buildCockpitTools(ToolContext, ToolGrants)`, `grantsFor` in core, shared validator at both doors, 23-class snapshot held, tenant pin now reaches dispatched specialists |

Next in the merged order: **Track C step 12** — Phase 43 closed 2026-09-08 and G10 is done.

**OWNER-SIDE, AND THE FIRST ITEM BLOCKS EVERY ACTIVATION.** (1) **Decide fixture 33.** It has been carried since Phase 40 as a stochastic research-lane flake; it is not. See `.planning/phases/43-batch-content-and-the-content-queue/43-FIXTURE-33-FINDING.md`: `evidenceVerdict`'s `declaredUnsupported` leg is provably dead code (`S===0 || (Q && S===0)` ≡ `S===0`, 0 of 24 inputs make a difference), and the fixture asserts a value the code computes with exactly the counter-based rule the fixture's own description says cannot express it. Three options; the recommended one changes NO production behaviour. Until this is decided there is no green gate run, and `shouldRecordEvidence` refuses a filtered run, so no gated skill can be activated at all. (2) Then the full unfiltered pinned run, dev then prod: `pnpm eval:golden -- --skill cockpit-agent@N` (~$0.85 each). `N` is readable ONLY at `/ops` in a signed-in browser — `candidatesForReview` is an `ownerQuery` and the CLI carries no identity. **Candidates STACK**: one Activate flips the Phase 40 canvas edits, the ADR-040 `dispatchTeam` section AND `createVariants` together, and the 46-case suite exercises none of them. (3) `npx convex env set --prod RELIABILITY_SWEEP_ARMED 1`. (4) The G19 activations (pack, revenue, cockpit-agent, research-specialist v4). (5) The QuickBooks lane pass — the runbook is ALREADY WRITTEN, eight steps in `docs/playbooks/revenue-connectors.md`; what is owed is executing it (prod env, gate row, connect as owner, live read, confirmed revoke, seal, per-deployment eval evidence, activation, subset release). (6) The pack gate run for `pack-offer-and-lead-plan` (35-02-SUMMARY).

## Session Continuity

Last session: 2026-09-06 — Phases 36, 37, 38 and 39 closed from the release worktree (`C:/Users/expert/AppData/Local/Temp/pikar-release`, branch `feat/28.2-unpark-quickbooks`, pushed `HEAD:main`). The shared tree `C:/Users/expert/desktop/pikar-ai` is on `main` (its stale 28.1 draft is `stash@{0}`). Local stack: detached `convex dev` (relaunched 16:20 as `launch-detached.ps1 -Label convex38` after the earlier watcher went silent — check its log mtime before trusting a push) + `next start :3112` from the release worktree; the e2e tenant carries a synthetic stale `gmailTokens` row (delete it with `gmailAuth:deleteTokens` before knowledge-search / briefing specs); the local deployment lists the e2e user and the four smoke tenants in `PIKAR_FIXTURE_TENANT_IDS`.

Standing rules for whoever writes this file next: one frontmatter block; edit fields in place; never append a second `---` block; keep the body under ~80 lines — the phase record is where detail goes.
