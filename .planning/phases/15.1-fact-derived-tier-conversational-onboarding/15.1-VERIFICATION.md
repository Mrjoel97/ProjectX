---
phase: 15.1-fact-derived-tier-conversational-onboarding
verified: 2026-07-26T15:51:02Z
status: passed
score: 6/6 success criteria verified
---

# Phase 15.1: Fact-Derived Tier & Conversational Onboarding Verification Report

**Phase Goal:** The business tier stops being something a user can assign themselves and becomes a
fact-derived, auditable property of the tenant — because tier now selects agent voice and which
Growth OS specialists get offered, making a self-writable tier a behavioral control rather than a
preference.

**Verified:** 2026-07-26T15:51:02Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC#1 | `deriveTier(facts)` is the ONLY writer of the tier; no UI control and no mutation argument accepts a caller-supplied tier; enterprise stays admin-granted only | ✓ VERIFIED | `deriveTier` (`packages/core/src/businessProfile.ts:314`) returns `DerivedTier` (structurally excludes `"enterprise"`, pinned by `@ts-expect-error` at test). `vProfile` in `onboarding.ts:69-77` has no `persona`/`tier` field — an extra key is a hard Convex validator error. `saveFacts` (`tenantProfile.ts:163`) has no `tier` arg. Both `profile/page.tsx` and `onboarding/page.tsx` source-scanned (`businessProfile.test.ts` `describe("no tier control")`, 14/14 green, positively mutation-checked in 15.1-03 and re-armed/mutation-checked again in 15.1-07 against the rewritten pages). `grantEnterprise` is `internalMutation` with no public surface (`tenantProfile.ts:258`). |
| SC#2 | The tier lives in a queryable, indexed tenant-profile table with `tierSource` + `derivedAt`; a malformed doc can no longer silently reclassify a tenant | ✓ VERIFIED | `tenantProfiles` table with `by_tenant` index exists in `schema.ts:727` (`tier`, `tierSource`, `derivedAt` all required). `evaluations.ts` reads `internal.tenantProfile.forTenant` (line 201) and picks the framework off `TIER_FRAMEWORK[tp?.tier ?? "solopreneur"]` (line 318); `personaHint` no longer exists anywhere in `evaluations.ts` (grep returns nothing). `evaluations.test.ts` proves two different table tiers over ONE identical malformed `- **Persona:** wizard` doc yield two different frameworks (swot vs bmc) — confirmed passing in isolation (23/23). |
| SC#3 | Onboarding asks the determining facts instead of letting an LLM guess the persona; a required slot cannot be left empty | ✓ VERIFIED | `profileSchema` (the extraction contract) has no persona property. `converse` (`onboarding.ts`) is a `generateObject` slot-filler whose `done` is `canComplete(slots)` — computed in code, never read off the model reply — and `nextSlot` is `missingSlots(slots)[0]` in `REQUIRED_SLOTS` order. `commitProfile` throws `INCOMPLETE_ONBOARDING` while any slot is missing (`onboarding.ts:571-573`). `onboarding.test.ts` 24/24 green in isolation, including the "model cannot declare completion" and "fails closed unseeded" cases. |
| SC#4 | The hardcoded `personaConfirmed: true` is gone; the audit row reflects what actually happened | ✓ VERIFIED | `grep -rn "personaConfirmed" packages/backend/convex/*.ts` returns only comments/test descriptions naming the deletion — zero occurrences as an actual object key. `commitProfile`/`updateProfile` payloads are `{vaultDocId, fieldCount, tierSource}` / `{..., reembed}` (`onboarding.ts:583-587`, `639-644`). `onboarding.test.ts` asserts the exact sorted key sets. |
| SC#5 | Tier visibly changes treatment (agent voice/framing, ADR-009-scoped as prompt-shaping); a tier change is surfaced as an event | ✓ VERIFIED (scope per ADR-009) | `buildSpecialistPrompt` (`dispatch.ts:166-182`) reads `internal.tenantProfile.forTenant` and prepends `tierBriefing({tier, agentName, styleDirective})` (pure, `specialists.ts:168`) on both return paths; `dispatch.test.ts` proves two tiers produce two different prompts. Style directive read is fail-open (unseeded overlay degrades voice, not dispatch); specialist body loader stays fail-closed. `llm.ts` and `diagnose.ts` are byte-unchanged since before Phase 15.1 (confirmed via `git log`/`git diff --stat`), and `SPECIALISTS`/the offer set are unchanged — consistent with ADR-009's accepted scope-down. `saveFacts` returns `{tier, tierSource, changed}`; `profile/page.tsx` surfaces a tier move as a `role="status"` acknowledgement driven off `changed`, and `tenantProfile.ts` emits exactly one `tenant.tier_changed` audit row (payload `{from, to, tierSource, factsChanged}`, no fact values) on a real tier move, none on an unchanged re-derivation — confirmed in `tenantProfile.test.ts` (19/19). |
| SC#6 | Existing tenants backfill as `tierSource: "legacy"` with no forced re-onboarding | ✓ VERIFIED (code present, tested; live run correctly deferred — no `CONVEX_DEPLOYMENT` in this worktree) | `backfillLegacyTier`/`runBackfillLegacyTier` (`tenantProfile.ts:295,327`) use `@convex-dev/migrations`, are idempotent (`if (existing) return`), never downgrade a `derived` row, and are exercised under `convex-test` (backfill-tagged tests pass in isolation). `onboarding.status` returns `needsOnboarding: false` for a legacy tenant and `updateProfile` succeeds with no slot gate (SC#6c, tested). The live `npx convex run tenantProfile:runBackfillLegacyTier` invocation is correctly recorded as deferred-to-integration debt in `deferred-items.md` §2, consistent with the phase's documented environment constraint (no `CONVEX_DEPLOYMENT`). |

**Score:** 6/6 success criteria verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/core/src/businessProfile.ts` | `deriveTier`, `DerivedTier`/`Tier`/`TIERS`, `TIER_REASON`, `missingSlots`/`canComplete`, `sanitizeAgentName`, `ProfileInput`, `isTier` | ✓ VERIFIED | All exports present and match plan interfaces exactly (grep-confirmed at correct line numbers). `PERSONAS` still a 3-member union (Phase-11 invariant held). |
| `packages/backend/convex/schema.ts` | `tenantProfiles` table + `by_tenant` index | ✓ VERIFIED | Present at line 727, `tier`/`tierSource`/`derivedAt` required, facts all optional (legacy-row-forever design). |
| `packages/backend/convex/tenantProfile.ts` | `forTenant`, `get`, `saveFacts`, `grantEnterprise`, `backfillLegacyTier`, `runBackfillLegacyTier` | ✓ VERIFIED | All 6 exports present (327 lines); `saveFacts` has no `tier` arg; `grantEnterprise` is `internalMutation` with no public surface. |
| `packages/backend/convex/onboarding.ts` | `vProfile` persona-free, tier splice, truthful audit, slot gate, `converse` | ✓ VERIFIED | `vProfile` (69-77) has no persona field; `currentTierRow` splices `row.tier` into `writeProfileDoc` on both write paths; `converse` (24/24 tests) is a stateless `generateObject` turn with code-owned state machine. |
| `packages/backend/convex/evaluations.ts` | `TIER_FRAMEWORK` from the table, `personaHint` deleted | ✓ VERIFIED | `TIER_FRAMEWORK` (line 62) `satisfies Record<Tier, Framework>`; `personaHint` absent (grep confirms); `financialsPresent` override intact (Q3). |
| `packages/backend/convex/dispatch.ts` | `buildSpecialistPrompt` reads tier + style, prepends `tierBriefing` | ✓ VERIFIED | Confirmed at lines 166-182; `llm.ts` byte-unchanged (`git diff --exit-code`). |
| `packages/core/src/specialists.ts` | `tierBriefing`, `PRESET_SKILL` | ✓ VERIFIED | Both exported and total over `TIERS`/`BEHAVIOR_PRESETS` by compiler bind. |
| `packages/contracts/skills/{style-*,onboarding-agent}.md` | 4 new UNGATED registry skills through the 5-file mirror | ✓ VERIFIED | Confirmed absent from `GATED_SKILLS`; drift tests green (`skillBodies.test.ts`). |
| `apps/web/.../profile/page.tsx` | editable facts, read-only tier + `TIER_REASON`, no tier control | ✓ VERIFIED | `saveFacts`, `TIER_REASON`, `tierRow` present; no `PERSONAS` import; source scan green. |
| `apps/web/.../onboarding/page.tsx` | conversational flow over `converse`, agent identity, closing beat, no tier control | ✓ VERIFIED | `converse`, `BEHAVIOR_PRESETS`, `pikar:onboarding-draft-v2` present; source scan green. |
| `docs/decisions/009-...md` (ADR-009) | Q2 deferral, scopes SC#5 to prompt-shaping | ✓ VERIFIED | Accepted, cites `diagnose.ts`/`evaluations.ts` line ranges, carries a 15.1-05 implementation note. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `onboarding.ts` (`commitProfile`/`updateProfile`) | `tenantProfiles` | tier splice into `writeProfileDoc` | ✓ WIRED | Confirmed at lines 576, 630. |
| `onboarding.ts` (`commitProfile`) | `@pikar/core missingSlots` | completion gate | ✓ WIRED | Line 564; throws `INCOMPLETE_ONBOARDING`. |
| `tenantProfile.ts` (`saveFacts`) | `@pikar/core deriveTier` | the only persistence call site | ✓ WIRED | Confirmed; no other call site of `deriveTier` writes a tier. |
| `tenantProfile.ts` | `internal.audit.log` | `tenant.tier_changed` event | ✓ WIRED | Confirmed emitted only on an actual tier move; payload key set exact. |
| `evaluations.ts` (`runEvaluation`) | `internal.tenantProfile.forTenant` | rubric authority | ✓ WIRED | Line 201; `personaHint` deleted. |
| `dispatch.ts` (`buildSpecialistPrompt`) | `internal.tenantProfile.forTenant` + `internal.skills.getActiveSkill` | tier/style briefing | ✓ WIRED | Lines 166-172; fail-open on style, briefing on both return paths. |
| `apps/web/profile/page.tsx` | `api.tenantProfile.saveFacts` | facts write that re-derives the tier | ✓ WIRED | No tier argument constructed or sent. |
| `apps/web/onboarding/page.tsx` | `api.onboarding.converse` | fact-gathering loop | ✓ WIRED | Confirmed. |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| ONBD-01 | 01, 02, 03, 06, 07 | Guided first-run onboarding identifies determining facts (persona derivation) | ✓ SATISFIED | Already marked Complete under Phase 11 in REQUIREMENTS.md; Phase 15.1 extends/hardens it per design doc. No orphaned mapping — this phase's own contract is the design doc decisions D1-D7, not a fresh REQUIREMENTS.md row (per phase description: "consumes .planning/design/tier-and-conversational-onboarding.md"). |
| ONBD-02 | 03, 07 | Structured business profile stored/indexed in vault | ✓ SATISFIED | `writeProfileDoc`/vault indexing untouched in substance; markdown is now a verified projection of the table. |
| design:tier-and-conversational-onboarding D1-D7 | all 7 plans | Locked owner decisions | ✓ SATISFIED | D2 (fact-derived, no self-assignment), D4 (agent identity), D5 (tier shapes treatment, ADR-009-scoped), D6 (enterprise admin-only), D7 (sequencing) all traced to code above. |
| ADR-009 | 05 | Q2 deferral scoping SC#5 | ✓ SATISFIED | Accepted ADR present and honored (offer set unchanged, `diagnose()` unwidened). |

No orphaned requirements found: the phase's REQUIREMENTS.md entry is the design doc itself (per ROADMAP.md's phase description), not a set of fresh IDs, and `requirements mark-complete ONBD-01/ONBD-02` returning `not_found` is documented, known tool noise (already Complete under Phase 11).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | A full grep sweep for `personaConfirmed`, `TODO`/`FIXME`/`PLACEHOLDER`, and stub returns in the 7 plans' touched files turned up nothing beyond documented comments explaining deliberate deletions. |

### Human Verification Required

None blocking. The four items that were deferred when this verifier ran were subsequently executed
against the live deployment and are recorded in `deferred-items.md` §2:

1. **Live backfill run** (SC#6) — paid 2026-07-26; a legacy row was read back with
   `tierSource:"legacy"` and the profile remained usable.
2. **A real conversational turn** (SC#3) — paid; the live model asked for the missing facts and the
   completion gate remained server-enforced.
3. **Profile page click-through** (SC#1c) — paid; the tier was read-only and a fact edit drove the
   derived tier path.
4. **Tier prompt/output observation** (SC#5) — prompts differed as specified, but the two sampled
   `money-model-designer` outputs did not visibly differ. That is preserved as an evidence caveat:
   the locked contract is prompt-shaping, not guaranteed output divergence.

None is a gap in the delivered contract. The output-similarity observation is not promoted into a
stronger claim than the evidence supports.

### Gaps Summary

No gaps found. All 6 success criteria are verified against the actual codebase (not merely against
SUMMARY claims): every artifact named in every plan's `must_haves` exists, is substantive (no stubs),
and is wired to its stated caller. Independent test runs confirm the numbers claimed in the SUMMARY
files:

- `@pikar/core` full suite: 282/282 (includes the 14/14 `"no tier control"` source scan)
- Backend isolation run of `tenantProfile.test.ts` + `onboarding.test.ts` + `evaluations.test.ts` +
  `dispatch.test.ts` + `profileRedaction.test.ts`: 103/103
- Backend `tsc --noEmit`: 55 errors, all in `*.test.ts`, 0 in any non-test file (documented baseline,
  unchanged)
- `apps/web` `tsc --noEmit`: exit 0
- `node scripts/check-playbooks.mjs check`: exit 0
- `git diff --exit-code` on `llm.ts` and `diagnose.ts`: both clean since before Phase 15.1
- `GATED_SKILLS` does not include any of the 4 new UNGATED skills
- `personaConfirmed` does not appear as an actual object key anywhere in `packages/backend/convex/*.ts`

One minor documentation-hygiene note (non-blocking, does not affect goal achievement): `ROADMAP.md`'s
per-plan checkboxes for 15.1-02, 15.1-03, and 15.1-07 are still `[ ]` (unchecked) even though their
SUMMARY.md files exist, their commits are present, and their code verifies green — this appears to be
stale roadmap bookkeeping rather than an execution gap, since the phase-level checkbox (line 57) is
already `[x]` and all 7 SUMMARY files and their commits are present and verified.

---

_Verified: 2026-07-26T15:51:02Z_
_Verifier: Claude (gsd-verifier)_
