---
phase: 10-vault-agent-grounding
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [VGND-01]

must_haves:
  truths:
    - "During a grounded turn the LATEST TRACE shows 'Searching your knowledge vault…' (running) / 'Grounded in the vault' (done) — never the 'Working…' FALLBACK."
    - "After a grounded turn a source card '📚 Grounded in N documents' renders on the workspace canvas, listing the doc titles the answer drew on."
    - "Each listed title is clickable and navigates to /dashboard/vault (the lazy, context-sanctioned click-through)."
    - "The card uses globals.css BRAND tokens (opaque --card sheet, --rule hairline, tracked-caps label) — no hardcoded hex a token covers, no new component library."
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "VERB['searchVault'] verb pair + a SourceCard rendered in CardList reading the vaultSources reader"
      contains: "searchVault"
    - path: "docs/playbooks/cockpit.md"
      provides: "Phase-10 source-card/verb note appended to the (Plan-02-authored) Phase-10 section + Last verified bump"
      contains: "searchVault"
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx (SourceCard)"
      to: "api.vaultSources (Plan 02's tenantQuery reader)"
      via: "useQuery keyed on threadId"
      pattern: "useQuery\\(api\\.vaultSources"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx (VERB map)"
      to: "stepText / ActivityCard render"
      via: "VERB['searchVault'] lookup keyed on the closed agentSteps.tool union member"
      pattern: "searchVault"
---

<objective>
Surface vault grounding to the USER (VGND-01 SC1 — what the user SEES). Two pieces, both in the
workspace right-pane canvas: (1) the activity-step verb so the "Searching your knowledge vault…"
row reads a human label instead of the FALLBACK; (2) a source card "📚 Grounded in N documents"
listing the doc titles the answer drew on, each title clickable through to the vault.

Purpose: the grounding is invisible without this — the SDK already emits the step row and Plan 02
already writes the titles to a content-plane row; this plan is the read-side render.
Output: a `VERB["searchVault"]` entry + a `SourceCard` in `cards.tsx`, and an augmented cockpit playbook.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vault-agent-grounding/10-RESEARCH.md
@.planning/phases/10-vault-agent-grounding/10-CONTEXT.md
@.planning/phases/10-vault-agent-grounding/10-VALIDATION.md
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@apps/web/app/(app)/dashboard/vault/PreviewModal.tsx

<constraints>
CLAUDE.md §8 (ponytail): reuse the existing card scaffolding + the lazy fallback link. NO new
dependency, NO PreviewModal wiring, NO new query. §10 (BRAND): docs/design/BRAND.md is binding for
any user-facing UI — use the globals.css CSS variables, never hardcode a hex a token covers, match
the existing card patterns, do NOT add a component library. Per the graphify rule, run
`graphify explain "cards.tsx"` / `graphify query "workspace source card render"` BEFORE Read/Grep.

FILE-OWNERSHIP BOUNDARY (three sibling planners run concurrently — stay strictly inside these):
- YOU own ONLY: `apps/web/app/(app)/dashboard/workspace/cards.tsx` and `docs/playbooks/cockpit.md`.
- Do NOT touch: `vaultGround.ts` (01); `llm.ts`/`schema.ts`/`vaultSources.ts`/`cockpitTools.test.ts`
  (02 — you CONSUME the `vaultSources` reader, you do NOT create it); `contracts/skills/*`/`eval-cases/*`
  (04); `PreviewModal.tsx`/`vault.ts`; `docs/playbooks/vault.md|audit-dead-letter.md|skill-registry.md|agent-runtime.md`.
</constraints>

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration. -->

The code-owned VERB map + FALLBACK (cards.tsx L1075). Keyed off the CLOSED `agentSteps.tool` union
(Plan 02 adds "searchVault" to that union in schema.ts). Adding a VERB key is compile-safe on its own
— an unknown key just falls back; the STEP only paints once Plan 02's union member exists (that is the
depends_on [02] / wave 3 gate):
```typescript
// cards.tsx L1075
const VERB: Record<string, [running: string, done: string]> = {
  thinking: ["Thinking…", "Thought it through"],
  // …
  listInbox: ["Checking your inbox…", "Checked your inbox"],
  briefInbox: ["Reading and summarizing your inbox…", "Briefed your inbox"],
};
const FALLBACK: [string, string] = ["Working…", "Done"];
// stepText(step) does: const [running, done] = VERB[step.tool] ?? FALLBACK;
```

The briefing precedent for a content-plane reader + card (COPY this shape, do NOT re-derive it):
```typescript
// cards.tsx — type derivation + read + render
type Briefing = NonNullable<FunctionReturnType<typeof api.briefings.byThread>>;
const briefing = useQuery(api.briefings.byThread, threadId ? { threadId } : "skip");
// CardList returns: <div style={{display:"grid",gap:"1rem"}}> {trace} {rest()} </div>
```

Plan 02's `vaultSources` reader (created in `packages/backend/convex/vaultSources.ts` by wave 2 — it
EXISTS on disk when you run in wave 3). Research §Architecture models it on `briefings`: a tenantQuery
returning titles+ids+count for a thread, written by `internal.vaultSources.insert({ tenantId, threadId,
docIds, titles, count })`. Expected reader shape (mirrors `briefings.byThread`):
```typescript
// api.vaultSources.byThread({ threadId }) -> { titles: string[], docIds: string[], count: number } | null
```
CONFIRM the ACTUAL exported query name + return field names by reading the top of the real
`packages/backend/convex/vaultSources.ts` before typing the `useQuery` line (do NOT edit that file).
Derive the type via `FunctionReturnType<typeof api.vaultSources.byThread>` exactly like `Briefing` —
never a hand-written interface.

BRAND tokens for the card (globals.css §2/§5): opaque `--card` background + `--rule` hairline border +
soft shadow (the briefing REJECTED the transparent border-only `box` style for legibility — do NOT use
`box`). Text `--ink`/`--ink-soft`. Tracked-caps section label (BRAND §3 signature — e.g. `capsTeal`
`var(--teal-600)`). NO amber (`--held` is the approval gate's alone). Clickable titles use `--teal-600`.

PreviewModal contract (for the DEFERRED upgrade note only — do NOT wire it):
`PreviewModal({ doc: VaultDoc; onClose })` needs a FULL `VaultDoc` (an `api.vault.listVaultDocs` row).
There is NO `getVaultDoc(byId)` query today (`listVaultDocs` is by-category) — the inline modal would
need a new `getVaultDoc(byId)` tenant query + touching vault.ts, which is out of your boundary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: searchVault VERB entry + SourceCard rendered in CardList</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
Run `graphify explain "cards.tsx"` / `graphify query "workspace source card render VERB"` first, then
read only the spans you need (VERB map ~L1075, CardList ~L1165, BriefingCard scaffolding ~L943).

Two edits, one file:

1. VERB entry (~L1090, after the `briefInbox` line, before the closing brace):
   `searchVault: ["Searching your knowledge vault…", "Grounded in the vault"],`
   This is the ONLY human string in the feature and it is code-owned (schema §4 keeps the step text
   off the wire). Verb copy matches CONTEXT's locked idiom. No other change to the map.

2. A `SourceCard` component (Claude's discretion on shape — the LAZY choice is a small dedicated
   component, NOT reusing the transparent `box`). It self-queries and returns null when there is
   nothing to show, exactly like the trace/briefing precedent:
   - `type VaultSources = NonNullable<FunctionReturnType<typeof api.vaultSources.byThread>>;` (derive
     from the api — CONFIRM the real query name/fields in packages/backend/convex/vaultSources.ts first;
     do NOT edit that file).
   - `const sources = useQuery(api.vaultSources.byThread, threadId ? { threadId } : "skip");`
   - `if (!sources || sources.count === 0) return null;` (fails silent — an ungrounded turn shows no card).
   - Render an OPAQUE BRAND sheet: `background: var(--card)`, `border: 1px solid var(--rule)`,
     `borderRadius: ~1rem`, soft shadow; a tracked-caps label (BRAND §3 signature, reuse the `capsTeal`
     idiom / `var(--teal-600)`) reading `📚 Grounded in {count} document{count===1?"":"s"}`; then the
     titles as a list. Each title is a `<Link href="/dashboard/vault">` (add
     `import Link from "next/link";` — cards.tsx does NOT import it yet) styled `color: var(--teal-600)`,
     `overflowWrap:"anywhere"` (reuse `traceText`) so a long title wraps in the resizable pane. Give the
     card `data-testid="source-card"` and each title `data-testid="source-title"` for later UAT/E2E.
   - `ponytail:` comment on the Link naming the ceiling + upgrade path, e.g.
     `// ponytail: doc-level link to /dashboard/vault (context-sanctioned fallback, no new query). Inline
     //  PreviewModal upgrade = add a getVaultDoc(byId) tenant query + import PreviewModal — deferred (Pitfall 5).`

3. Wire it into `CardList` (~L1198 return): render `<SourceCard threadId={threadId} />` inside the
   returned grid alongside `{trace}` and `{rest()}` (e.g. between them) so a grounded turn shows the card
   whether or not a PLAN row exists (grounding happens on pure advice turns with no plan — mirror how the
   trace renders above every plan-status branch). SourceCard reads its own data, so pass only `threadId`.

Honor BRAND §6: don't encode meaning in color alone (the 📚 + text label carry it), keep a visible
focus state on the links (native anchor focus suffices — do not strip outline).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    Manual UAT (per 10-VALIDATION Manual-Only — SDK-loop + cross-surface React render, not covered by
    backend vitest; run at /gsd:verify-work with a connected user): run a grounded cockpit turn in the
    app and confirm (a) the LATEST TRACE shows "Searching your knowledge vault…" then "Grounded in the
    vault" (NOT "Working…"); (b) the "📚 Grounded in N documents" source card renders with the doc
    titles; (c) a title click lands on /dashboard/vault; (d) the card matches the brand cards (opaque
    white sheet, tracked-caps label, no amber).
  </verify>
  <done>
`pnpm --filter @pikar/web typecheck` exits 0 (JSX + the `api.vaultSources.byThread` consume + the
`FunctionReturnType` derivation compile against Plan 02's reader). `VERB` contains a `searchVault` pair;
`cards.tsx` contains a `SourceCard` that reads `api.vaultSources.byThread` and is rendered in `CardList`;
titles are `next/link` anchors to `/dashboard/vault`; the ponytail ceiling + inline-modal upgrade path is
commented. No hardcoded hex a token covers; no new dependency; no component library.
  </done>
</task>

<task type="auto">
  <name>Task 2: Augment the cockpit playbook with the source-card + verb note</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
§9 definition-of-done: this plan changed a cockpit.md-watched path (`workspace/cards.tsx`), so the
playbook updates in-phase.

IMPORTANT ordering — Plan 02 (wave 2) writes the Phase-10 section of cockpit.md FIRST (the `searchVault`
tool + closed-union member). You run in wave 3: APPEND to that existing Phase-10 section, do NOT replace
or duplicate it. Read the current top of cockpit.md first to find the Phase-10 `> Last verified` block.

Add a concise note (2-4 sentences) recording the UI read-side, e.g.: the `searchVault` activity step is
FREE once the tool joins the closed `agentSteps.tool` union — `cards.tsx` `VERB["searchVault"]` gives it
the "Searching your knowledge vault…" / "Grounded in the vault" label (unknown keys fall back to
"Working…", so a tool added later never crashes the card); the `SourceCard` in `CardList` is a DUMB
renderer over Plan 02's `vaultSources` content-plane reader (titles+ids+count, refs-only §4 — no query
string, no chunk text ever reaches the card), and each title links to `/dashboard/vault` (the lazy
click-through; the inline `PreviewModal` is a deferred upgrade needing a `getVaultDoc(byId)` query).
Bump the section's `Last verified` line to today (2026-07-24) with the plan id (10-03).

Do NOT touch vault.md / audit-dead-letter.md / skill-registry.md / agent-runtime.md — sibling-owned.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    (The §9 Stop hook: passes when the cockpit.md-watched change under workspace/ is accompanied by a
    cockpit.md edit. If the script path differs, the equivalent is: the turn's Stop hook does not block on
    an untouched playbook for the changed path.)
  </verify>
  <done>
cockpit.md's Phase-10 section carries an appended (not replaced) note covering the `searchVault` VERB
entry + the `SourceCard`/`vaultSources` read-side, `Last verified` bumped to 2026-07-24 (10-03), and the
§9 playbook check does not block on `workspace/cards.tsx`.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` exits 0 (proves the SourceCard consumes Plan 02's
  `api.vaultSources.byThread` reader and the whole file compiles).
- `node scripts/check-playbooks.mjs` (or the Stop hook) does not block on the cockpit.md-watched change.
- Everything else in the 10-03 row is MANUAL UAT (10-VALIDATION Manual-Only): the verb-visible trace,
  the rendered source card, the clickable title, and the brand match are confirmed by a human running a
  grounded turn during /gsd:verify-work. Be honest: no backend vitest covers this cross-surface render.
</verification>

<success_criteria>
- `VERB["searchVault"] = ["Searching your knowledge vault…", "Grounded in the vault"]` present.
- A `SourceCard` reads `api.vaultSources.byThread` (type-derived, not hand-written), renders
  "📚 Grounded in N documents" with the doc titles, is wired into `CardList`, returns null when
  ungrounded, and each title links to `/dashboard/vault`.
- Card uses BRAND globals.css tokens only (opaque `--card` sheet, `--rule` border, tracked-caps label,
  `--teal-600` links, no amber); no new dependency; no component library.
- The inline `PreviewModal` click-through is a MARKED deferred upgrade (`ponytail:` comment), not built.
- cockpit.md's Phase-10 section is AUGMENTED (not replaced) with the read-side note + `Last verified` bump.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vault-agent-grounding/10-03-SUMMARY.md`.
</output>
