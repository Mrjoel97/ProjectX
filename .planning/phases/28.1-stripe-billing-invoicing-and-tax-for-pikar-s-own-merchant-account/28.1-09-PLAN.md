---
phase: 28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account
plan: 09
type: execute
wave: 9
depends_on: ["28.1-07"]
files_modified:
  - apps/web/app/(app)/dashboard/settings/BillingPanel.tsx
  - apps/web/app/(app)/dashboard/settings/billingPanel.test.ts
  - apps/web/app/(app)/dashboard/settings/page.tsx
  - docs/playbooks/billing.md
autonomous: true
requirements: [BILL-03, BILL-05]

# EXECUTION ORDER, and why the number lies about it: this plan is authored after 28.1-08 but MUST
# RUN BEFORE IT. 28.1-08 seals the phase, and a seal written over a surface nobody can see is a seal
# over a known gap. The wave field gates nothing here — the phase is being executed plan-by-plan by
# the main session, not by `execute-phase`.

must_haves:
  truths:
    - "A tenant can SEE their subscription state, and 'unknown' renders as unknown — never as $0 and never as a free tier"
    - "Unapplied bank-transfer funds are visible WITH their age, because the 75/90-day clock is the whole point of recording them"
    - "The honest tax posture is rendered as words, never as a bare 0.00"
    - "Returning from Stripe Checkout lands on a page that ACKNOWLEDGES the return instead of ignoring ?checkout="
    - "Nothing on this surface fabricates a number, and no amber is spent outside the approval gate"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/settings/BillingPanel.tsx"
      provides: "The one tenant-facing billing surface: subscription state, portal link, unapplied funds, tax posture"
    - path: "apps/web/app/(app)/dashboard/settings/billingPanel.test.ts"
      provides: "Pure presentation tests over the panel's derivations — no rendering framework needed"
  key_links:
    - from: "apps/web/app/(app)/dashboard/settings/BillingPanel.tsx"
      to: "packages/backend/convex/billing.ts"
      via: "api.billing.billingStatus / unappliedFunds / portalLink"
      pattern: "api.billing"
    - from: "apps/web/app/(app)/dashboard/settings/BillingPanel.tsx"
      to: "apps/web/app/(app)/dashboard/finance/CashView.tsx"
      via: "FigureTile — the four-state figure renderer, reused not reimplemented"
      pattern: "FigureTile"
---

<objective>
Three things this phase built are invisible, and a capability nobody can reach is not shipped.

- `unappliedFunds` (28.1-06) — zero callers in `apps/web`. The 75/90-day unapplied-funds clock is
  visible only to a query nobody calls.
- `renderTaxPosture` (28.1-03) — zero renderers. The function whose entire job is to make a bare
  `0.00` unreachable has nothing to render into.
- `?checkout=` — 28.1-04's `success_url` / `cancel_url` point at `/dashboard/settings?checkout=…`
  and the page **ignores the parameter**. A tenant returning from a completed Stripe Checkout
  currently lands on a page about data export.

Purpose: close the gap before 28.1-08 seals the phase, so the seal is honest rather than a record
of a known hole.
Output: ONE `BillingPanel` on `/dashboard/settings`, beside the existing `DataControls`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/settings/page.tsx
@apps/web/app/(app)/dashboard/settings/DataControls.tsx
@apps/web/app/(app)/dashboard/finance/CashView.tsx
@packages/backend/convex/billing.ts
@packages/billing/src/tax.ts
@CLAUDE.md

<interfaces>
Reuse, do not reimplement (CLAUDE.md §8 rung 2):

```tsx
// apps/web/app/(app)/dashboard/finance/CashView.tsx — ALREADY EXPORTED
export function FigureTile({ label, figure, note }: { label: string; figure: CashFigure; note?: string })
```
`FigureTile` already implements the four-branch discipline this panel needs: `unknown` renders an
em-dash and the prompt — **not a zero**, because a zero is indistinguishable from a measured
nothing. Do not write a second figure renderer.

```ts
// packages/backend/convex/billing.ts
billingStatus   // tenantQuery -> { state: "unknown" | "not_subscribed" | "subscribed" }
unappliedFunds  // tenantQuery -> unapplied balances with their observed age
portalLink      // tenantAction -> { url } | refusal; NEVER creates a Stripe customer
```

```ts
// packages/billing/src/tax.ts (28.1-03)
renderTaxPosture(...)  // words, never a bare 0.00 — no zero arm emits an amount at all
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the panel's derivations, as pure functions with tests</name>
  <files>apps/web/app/(app)/dashboard/settings/BillingPanel.tsx, apps/web/app/(app)/dashboard/settings/billingPanel.test.ts</files>
  <behavior>
    Write these RED first. They are PURE — no React rendering, no jsdom, matching the sibling
    `cashView.test.ts` / `financeView.test.ts` idiom:
    - `checkoutNotice("success")` / `("cancel")` / `(null)` / `("garbage")` — a known token yields a
      message, an unknown token yields NOTHING. An unrecognised query parameter must never render
      as a blank or half-built banner.
    - `subscriptionCopy("unknown")` returns copy that says the state is not known, and **contains
      neither "$0" nor "free"**. Assert on the STRING, with written-out literals.
    - `subscriptionCopy("not_subscribed")` and `("subscribed")` are distinct from each other and
      from `unknown` — three states, three messages, no two collapsing.
    - Unapplied funds: a balance whose age crosses 75 days is described as returnable, one past 90
      as sweepable, one below both as merely waiting. Assert the **boundaries against written-out
      literals `75` and `90`**, not against imported constants — a constant the test imports moves
      the oracle with the subject.
    - An EMPTY unapplied list renders the honest empty state, and that state is NOT "$0".
  </behavior>
  <action>
Create `BillingPanel.tsx` exporting the pure helpers alongside the component, and test the helpers
directly — the sibling finance views do exactly this, and it keeps the suite free of a rendering
framework the repo does not have.

**Voice (BRAND §1):** executive, outcome-first, second person, plain. "You are not subscribed." not
"Subscription status: none". Honest about limits — if the state is unknown, say it is unknown and
say what would resolve it.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run app/\(app\)/dashboard/settings</automated>
  </verify>
  <done>Every branch of every helper is pinned by a test; the 75/90 boundaries are asserted against literals; no helper can return a partial or blank string.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the panel itself, on the settings page</name>
  <files>apps/web/app/(app)/dashboard/settings/BillingPanel.tsx, apps/web/app/(app)/dashboard/settings/page.tsx, docs/playbooks/billing.md</files>
  <behavior>
    - The panel renders four sections: subscription state, the manage-subscription action, unapplied
      funds, and the tax posture
    - `?checkout=success` renders an acknowledgement; `?checkout=cancel` renders a neutral one;
      neither claims a subscription is active — **only `billingStatus` may say that**, because a
      redirect back from Stripe is not evidence the webhook has landed yet
    - With no Stripe customer, the manage-subscription control is absent or disabled with a reason
      — it must never call `portalLink` in a way that could provoke customer creation
    - A source assertion in the test file: this directory contains no `--held` / amber token
  </behavior>
  <action>
Add `<BillingPanel />` to `page.tsx` beside `<DataControls />`. Update the page header copy: the
page is no longer only about data export.

**BRAND.md is binding (CLAUDE.md §10). The traps specific to this surface:**

1. **AMBER IS THE APPROVAL GATE'S ALONE (§2).** Unapplied funds *feel* like a warning and amber is
   the obvious choice. It is forbidden. "Spend amber in exactly one place." Use `--ink-soft` and
   weight for emphasis, or `--released` for a settled/cleared state. A source assertion enforces it.
2. **`--teal-600` on white is ~2.9:1 (§6)** — button fills with white text, never small teal body
   text.
3. **Tracked-caps section labels (§3)** are the signature pattern — UPPERCASE, letter-spaced, small,
   `--ink-soft`. Use them to title each section.
4. **White cards, ~16–20px radius, soft shadow, generous padding (§4).** Never a dense table.
5. **Honest zeros (§5)** — and this surface goes further: *unknown is not even zero*. `FigureTile`
   already encodes that; use it rather than printing a number.
6. **No component library (§8.3).** Hand-rolled, matching `DataControls.tsx` and `CashView.tsx`.

Update `docs/playbooks/billing.md`: the surface exists, what it renders, and the amber prohibition.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run app/\(app\)/dashboard/settings &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>The panel is on the page; `?checkout=` is acknowledged without claiming a subscription; no amber; `tsc --noEmit` exits 0; the playbook records the surface.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx vitest run app/\(app\)/dashboard/settings` then `npx tsc --noEmit` STANDALONE
2. `grep -rn "unappliedFunds\|renderTaxPosture" apps/web/` returns HITS — the gap this plan exists to close
3. `grep -rniE "\-\-held|#f0a22e" "apps/web/app/(app)/dashboard/settings/"` returns NOTHING
4. `grep -rn "checkout" "apps/web/app/(app)/dashboard/settings/"` returns hits — the parameter is read
5. Mutation: make `checkoutNotice` return a message for an UNKNOWN token; confirm RED; restore
6. Mutation: make `subscriptionCopy("unknown")` return "$0"; confirm RED; restore
7. `node scripts/check-playbooks.mjs < /dev/null` — read STDOUT
</verification>

<success_criteria>
- `unappliedFunds` and `renderTaxPosture` both have a caller in `apps/web`
- A tenant returning from Stripe Checkout sees an acknowledgement, not a data-export page
- Unknown renders as unknown — never `$0`, never "free tier"
- The 75/90-day clock is visible to a human, with the age
- No amber outside the approval gate, proven by a source assertion in the suite
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-09-SUMMARY.md`
</output>
