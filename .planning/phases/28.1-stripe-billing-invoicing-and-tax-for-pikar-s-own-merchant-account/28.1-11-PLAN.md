---
phase: 28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account
plan: 11
type: execute
wave: 11
depends_on: ["28.1-09"]
files_modified:
  - packages/billing/src/reconcile.ts
  - packages/billing/src/reconcile.test.ts
  - packages/billing/src/events.ts
  - packages/billing/src/events.test.ts
  - packages/backend/convex/billingWebhook.ts
  - packages/backend/convex/billingWebhook.test.ts
  - packages/backend/convex/billingRollup.ts
  - packages/backend/convex/billingRollup.test.ts
  - packages/backend/convex/billingLedger.test.ts
  - packages/backend/convex/billing.ts
  - packages/backend/convex/billing.test.ts
  - packages/backend/convex/schema.ts
  - docs/playbooks/billing.md
autonomous: true
requirements: [BILL-01, BILL-03]

# RUNS BEFORE 28.1-10 AND 28.1-08. Sealing a phase over a confirmed double-billing path is worse
# than the delay. Source: .planning/audits/28.1-billing-audit-2026-08-30.md — 37 candidates, 27
# survived a 3-skeptic refutation panel, 15 distinct defects.

must_haves:
  truths:
    - "A customer's SECOND bank transfer is booked — correlation is per-transaction, never per-customer"
    - "A subscription that goes unpaid stops reading as subscribed — a repeatable event type is not suppressed by the by-object dedupe"
    - "A rollup that fails AFTER creating a Stripe invoice never mints a second one"
    - "A second partial refund and a second credit note are each recorded"
    - "The held-funds surface stops reporting money we no longer hold"
    - "A billing dead letter is the OWNER's signal — the billed customer cannot read or resolve it"
    - "Every source scan in this subsystem is proven non-vacuous against the file it actually reads"
  artifacts:
    - path: "packages/billing/src/reconcile.ts"
      provides: "per-movement correlation on every arm"
    - path: "packages/backend/convex/billingWebhook.ts"
      provides: "one-shot vs repeatable dedupe, unattributed dead letters, self-clearing unapplied funds"
  key_links:
    - from: "packages/backend/convex/billingWebhook.ts"
      to: "packages/billing/src/events.ts"
      via: "ONE_SHOT_EVENT_TYPES — the closed set the by-object key may suppress"
      pattern: "ONE_SHOT"
---

<objective>
Close all 15 defects the adversarial audit confirmed, each with a regression test that fails before
the fix.

Source of truth: `.planning/audits/28.1-billing-audit-2026-08-30.md`. **Read it first** — it carries
the failure scenario and the prescribed smallest fix for every finding. This plan orders the work and
adds the constraints; it does not restate the analysis.

Two of these were re-verified by the main session against the live code before the plan was written
(#1 and #10), so treat them as established, not as claims to re-litigate.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/audits/28.1-billing-audit-2026-08-30.md
@packages/billing/src/reconcile.ts
@packages/backend/convex/billingWebhook.ts
@packages/backend/convex/billingRollup.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the correlation defects — one movement, one correlation (#1, #6, #7)</name>
  <files>packages/billing/src/reconcile.ts, packages/billing/src/reconcile.test.ts, packages/billing/src/events.ts, packages/billing/src/events.test.ts</files>
  <behavior>
    RED first, and each of these fails on today's code:
    - TWO `funded` events for ONE customer, different `ccsbtxn_` ids, different amounts => TWO
      reserved movements with DIFFERENT correlations. Today the second is swallowed.
    - TWO `funding_reversed` for one customer => two refunded movements.
    - TWO credit notes against ONE invoice => two movements.
    - A second PARTIAL refund records the DELTA, not a cumulative total, and is not swallowed.
  </behavior>
  <action>
Findings #1, #6, #7 are ONE root cause: a correlation built from an entity that repeats instead of
from the movement that is unique. Fix them together and say so in the commit.

#1: pass `txn.id` as the correlation source in the `funded` and `funding_reversed` arms and change
both `?? txn.customer` fallbacks to `?? txn.id`. **Also correct the comment at `:215`** — it argues
"the customer is the only thing to correlate on", which is what made this look deliberate. It is not
true: `txn.id` is unique and is already passed one argument later.

#7: use `object.id` (the credit note) as the correlation source; keep `object.invoice` as the
tie-back in `stripeObjectId`.

#6: prefer the audit's primary fix — map `refund.created` (whose `object.amount` is the DELTA and
whose `re_…` id is unique) and drop `charge.refunded` from the handled set in `events.ts`. If you
keep `charge.refunded` for any reason, it must NOT book a cumulative `amount_refunded` as a
movement. Whichever you choose, `HANDLED_EVENT_TYPES` and the playbook's event list must agree with
the code — and remember the docs list in `28.1-CONTEXT.md` is a record of the original decision, not
a constraint to preserve.

**Also fix #14 here:** `reconcile.test.ts`'s invoice fixture sets `total` and `amount_paid` to the
same number, so the assertion that we book `amount_paid` proves nothing. Make them differ
(`total: 1050, amount_paid: 900`) and assert `900`.
  </action>
  <verify>
    <automated>cd packages/billing &amp;&amp; npx vitest run &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Four repeat-movement tests were RED before the change and are green after; no correlation in this file is built from an entity that can repeat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the dedupe that suppresses effects (#2), and dead-letter attribution (#9)</name>
  <files>packages/backend/convex/billingWebhook.ts, packages/backend/convex/billingWebhook.test.ts, packages/billing/src/events.ts</files>
  <behavior>
    RED first:
    - TWO `customer.subscription.updated` for ONE `sub_`, the second carrying `status: "unpaid"` and
      a later `statusAt` => the mapping ends `unpaid` and `billingStatus` stops saying subscribed.
      Today the second is `duplicate_object` and never reaches the effect.
    - The `statusAt` freshness guard still refuses an OLDER repeated `updated` — prove it with two
      `updated` events, not with the `deleted`-then-`updated` pair that currently stands in for it.
    - A billing dead letter's `tenantId` is the unattributed sentinel, and the billed tenant's id
      survives as a REF inside the payload. Assert a tenant-scoped read cannot see the row.
  </behavior>
  <action>
#2: introduce a closed `ONE_SHOT_EVENT_TYPES` set (`checkout.session.completed`, `invoice.finalized`,
`invoice.paid`, `customer.subscription.created`, `customer.subscription.deleted`) and let the
by-object key suppress ONLY those. For a repeatable type, still record the delivery row — the row is
the audit trail — but let `applyEffect` run. Put the set in `packages/billing/src/events.ts` beside
`HANDLED_EVENT_TYPES` so the two are read together, and write down WHY each member is one-shot.

This is the guard that has been absorbing the `statusAt` freshness check. Once it stops, that check
carries real load — so its test must exercise two events of the SAME type.

#9: pass `UNATTRIBUTED_TENANT` as the dead letter's `tenantId` at all four sites and keep the tenant
id as a ref inside the payload (an id, §4-safe). A billing refusal is the OWNER's signal; filing it
under the billed customer lets them read it and mark it resolved.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billingWebhook.test.ts convex/billing.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>A subscription can go unpaid; the freshness guard is proven with same-type events; no billing dead letter is readable by the tenant it bills.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the double-invoice path (#3), card identification (#4), re-subscribe mapping (#5)</name>
  <files>packages/backend/convex/billingRollup.ts, packages/backend/convex/billingRollup.test.ts, packages/backend/convex/billing.ts, packages/backend/convex/billing.test.ts, packages/backend/convex/schema.ts</files>
  <behavior>
    RED first:
    - A period that FAILED after `/v1/invoices` succeeded carries the created `stripeInvoiceId`, and
      `claimable()` refuses to re-claim it. Assert NO second `POST /v1/invoices` is ever sent.
    - An `invoice.paid` carrying ONLY `payment_settings.payment_method_types: ["card"]` — no
      `charge`, no `payment_intent` — books `actual`. Today no probe can see it.
    - `startCheckout` for a tenant WITH an existing mapping sends `customer=<cus_…>`.
  </behavior>
  <action>
#3 is the only finding where money leaves a customer's account twice. `auto_advance: true` means the
abandoned invoice still collects, `crons.daily` guarantees the retry lands past Stripe's ~24h key
pruning, and `settleOutcome`'s failure variant has nowhere to put the invoice id. Add
`stripeInvoiceId: v.optional(v.string())` to that variant, thread the created id into `fail()`, and
make `claimable()` refuse a `failed` row that carries one. **Fail closed** — a period whose invoice
already exists at Stripe is an operator problem, not a second charge. Record the new terminal state
in the playbook.

#4: add `"payment_settings[payment_method_types][0]": "card"` to `postInvoice`'s POST body so probe
(c) is live for the only invoices this deployment creates. Do NOT weaken the fail-closed logic in
`invoicePaymentMethods` — an undeterminable method must still book no `actual`.

#5: in `startCheckout`, look up `stripeCustomerFor(tenantId)` and pass `customer` when a mapping
exists, so a re-subscribing tenant is not minted a second Stripe customer that nothing can map.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billingRollup.test.ts convex/billing.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>A failed-after-create period can never mint a second invoice; a card-only invoice books actual; a re-subscribe reuses the customer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: held funds that clear (#8, #13)</name>
  <files>packages/backend/convex/billingWebhook.ts, packages/backend/convex/billingWebhook.test.ts, packages/backend/convex/schema.ts</files>
  <behavior>
    RED first:
    - A `cash_balance.funds_available` of ZERO clears the `billingUnapplied` row. Today zero is
      skipped and the stale amount is reported and aged forever.
    - An OUT-OF-ORDER redelivery carrying an older `created` does NOT overwrite a newer amount, and
      **`observedAt` does not restart** — the 75/90-day sweep clock must not be reset by a
      redelivery.
  </behavior>
  <action>
Both live in one flow, so do them in one edit. Stop skipping zero in `fundsAvailable` (emit a zero
`unapplied-funds` observation) and delete the row in `observeUnapplied` when the amount is zero.
Add an `amountAt` field and only patch when the delivery is newer — freezing `observedAt` so the
clock keeps measuring how long the money has actually sat.

If you find that `cash_balance.funds_available` does not fire at zero, say so plainly in the summary
and implement the fallback the audit names (clear on `applied_to_payment` for that
customer/currency) rather than leaving the surface reporting money we do not hold.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billingWebhook.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>A cleared balance disappears from the surface; a stale redelivery neither lowers nor re-ages a newer figure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: the guards that prove nothing (#10, #11, #12) and the playbook (#15)</name>
  <files>packages/backend/convex/billing.test.ts, packages/backend/convex/billingWebhook.test.ts, packages/backend/convex/billingLedger.test.ts, docs/playbooks/billing.md</files>
  <behavior>
    - `codeOf` preserves a `https://` inside a STRING literal. Tripwire:
      `expect(codeOf('const u = "https://a/b"; //x')).toContain("https://a/b")`
    - Every source scan in the subsystem asserts, against the file it ACTUALLY reads, that the
      stripped text retained a token that does NOT survive gutting, and that stripped length is
      within a stated fraction of raw length
    - `expect(src).not.toMatch(/db\.patch\(/)` matches how Convex code is actually written
  </behavior>
  <action>
**#12 first, because #10 depends on it.** Put a string-literal alternative FIRST in the alternation
so a string is consumed before a comment can open — the audit gives the exact regex. Today `codeOf`
truncates every line at `https://`, so the two scans hunting a hardcoded Stripe origin are blind.

**#10:** `billingWebhook.test.ts:737` still runs the UNREPAIRED four-line stripper that 28.1-07 fixed
only in `billing.test.ts`. The main session measured it: **556 non-blank lines → 203**, and the
test's own non-vacuity guard passes because the two tokens it checks happen to survive. Replace it
with the repaired `codeOf` and add a **length** tripwire — a token check alone is what failed here.

**#11:** `expect(src).not.toMatch(/patch\(\s*["']billingEvents["']/)` can never match Convex code,
which is `ctx.db.patch(id, {...})` with no table name. Match `spendLedger.test.ts:193` instead. Then
add the scan that does not exist at all: **nothing checks `billingWebhook.ts` — the only writer of
`billingStripeEvents` — for a mutating call.**

**#15:** rewrite the playbook's three false "Known gaps" bullets. Verify each against the code as it
stands AFTER tasks 1-4, not against the audit's snapshot.

Every scan you touch or add must carry BOTH a positive control (the pattern matches a string you
know it should) and a length tripwire. A guard that only proves a token survived is the exact shape
that failed twice in this phase.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billing.test.ts convex/billingWebhook.test.ts convex/billingLedger.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Every scan is proven non-vacuous by length AND by a token that gutting would remove; the append-only guard can actually match; the playbook's gap list is true.</done>
</task>

</tasks>

<verification>
1. Full suites, each from inside its package: `packages/billing`, `packages/backend`, `packages/core`, `apps/web`. Judge by FAILURE COUNT.
2. `npx tsc --noEmit` STANDALONE in every package touched. Never chained, never through a pipe.
3. `git diff --stat origin/main..HEAD -- packages/core/src/spend.ts packages/backend/convex/spendLedger.ts packages/backend/convex/finance.ts` is EMPTY.
4. `node scripts/check-playbooks.mjs < /dev/null` — read STDOUT.
5. For EACH of the 15 findings, state in the summary: the regression test that now covers it, and whether you saw it RED before the fix.
</verification>

<success_criteria>
- All 15 confirmed findings closed, each with a test that was RED first
- No new failure in any suite; the spend plane still diffs empty
- Every source scan in the subsystem carries a positive control AND a length tripwire
- The playbook describes the code as it now is
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-11-SUMMARY.md`
</output>
