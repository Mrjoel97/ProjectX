---
phase: 28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account
plan: 03
type: tdd
wave: 3
depends_on: ["28.1-02"]
files_modified:
  - packages/billing/src/tax.ts
  - packages/billing/src/tax.test.ts
  - packages/billing/src/reconcile.ts
  - packages/billing/src/reconcile.test.ts
  - packages/billing/src/index.ts
  - docs/playbooks/billing.md
autonomous: true
requirements: [BILL-03, BILL-05]

must_haves:
  truths:
    - "`not_collecting` plus a real product tax code reads as 'no tax owed — not registered', never as a calculated zero"
    - "`not_collecting` plus txcd_00000000 reads as a DIFFERENT claim: 'we declared this product nontaxable'"
    - "`zero_rated` / `not_subject_to_tax` / `reverse_charge` read as calculated-zero, which is not the same statement"
    - "A rendered zero-tax line never reads '$0.00' alone — the reason travels with the number"
    - "A bank-transfer `invoice.paid` maps to NO `actual` movement; only `applied_to_payment` does"
    - "`funding_reversed` produces a reversing movement rather than being silently dropped"
    - "Stripe's lowercase `usd` and its native minor units survive the boundary without a double conversion or a float"
  artifacts:
    - path: "packages/billing/src/tax.ts"
      provides: "The honest tax posture — both halves required"
      exports: ["taxPosture", "renderTaxPosture", "NONTAXABLE_TAX_CODE", "TaxPosture"]
    - path: "packages/billing/src/reconcile.ts"
      provides: "Stripe event -> ledger movement, pure, no ctx"
      exports: ["reconcileEvent", "BillingMovement", "PAYMENT_METHOD_BANK_TRANSFER"]
  key_links:
    - from: "packages/billing/src/reconcile.ts"
      to: "packages/revenue/src/money.ts"
      via: "moneyFromMinor / parseMoney — the ONLY sanctioned way to make a money value"
      pattern: "moneyFromMinor|parseMoney"
    - from: "packages/billing/src/tax.ts"
      to: "packages/billing/src/config.ts"
      via: "PRODUCT_TAX_CODE is the second half of the not_collecting discrimination"
      pattern: "PRODUCT_TAX_CODE"
---

<objective>
The pure domain half of the honest ledger: which Stripe event means which money movement, and what a
zero on a tax line actually claims.

Purpose: this is where BILL-03's sharpest law and BILL-05's whole code surface live, and both are
fully provable offline with fabricated event JSON. A handler cannot tell a fabricated
`customer_cash_balance_transaction` from a real one — which is exactly why this is testable at $0.
Output: `packages/billing/src/tax.ts` and `src/reconcile.ts`, pure, no `ctx`, no `fetch`, no DB.

**This is a TDD plan.** RED → GREEN → REFACTOR, one commit per phase.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-RESEARCH.md
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-02-SUMMARY.md
@packages/revenue/src/money.ts
@packages/core/src/spend.ts
@packages/billing/src/config.ts
@CLAUDE.md

<interfaces>
From packages/core/src/spend.ts:25 — reuse this vocabulary VERBATIM. Do not invent a phase name:
```ts
export const SPEND_PHASES = ["estimated","reserved","actual","refunded","adjustment"] as const;
// `adjustment` = a correction UPWARD; `refunded` = money back. BOTH POSITIVE —
// direction lives in the phase, NEVER in the sign.
```

From packages/revenue/src/money.ts — the only sanctioned money constructors:
```ts
export function normalizeCurrency(input: string): ...;   // case-insensitive: "Stripe sends `usd`"
export function minorDigits(currency: string): number;   // EXPONENT_EXCEPTIONS covers JPY/BHD/...
export function moneyFromMinor(minor: number, currency: string): Result<Money, string>;
export function parseMoney(text: string, currency: string): Result<Money, string>;
export function addMoney / subMoney / negateMoney / sumMoney : Result<Money,string>;  // refuse a currency mismatch
```

From packages/billing/src/config.ts (plan 02):
```ts
export const PRODUCT_TAX_CODE: string;       // the REAL SaaS code the owner selected
export const NONTAXABLE_TAX_CODE = "txcd_00000000";
export const BANK_TRANSFER_ENABLED: boolean; // the owner's Dashboard answer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: taxPosture — the not_collecting ambiguity, both halves</name>
  <files>packages/billing/src/tax.ts, packages/billing/src/tax.test.ts, packages/billing/src/index.ts</files>
  <behavior>
    Write these RED first (tax.test.ts):
    - `taxPosture("not_collecting", PRODUCT_TAX_CODE, 0)` → `{ state: "not-owed", because: "unregistered" }`
    - `taxPosture("not_collecting", "txcd_00000000", 0)` → `{ state: "not-owed", because: "declared-nontaxable" }`
      — the WRITTEN-OUT literal, so renaming the constant cannot fake this pass
    - `taxPosture("zero_rated", PRODUCT_TAX_CODE, 0)` → `{ state: "calculated-zero", reason: "zero_rated" }`
    - Same for `not_subject_to_tax`, `reverse_charge`, `customer_exempt`, `product_exempt`, `not_supported`
    - `taxPosture("standard_rated", PRODUCT_TAX_CODE, 1250)` → `{ state: "collected", minor: 1250 }`
    - A positive tax amount wins over ANY reason — a non-zero number is never "not owed"
    - An unknown/absent `taxability_reason` with a zero amount is NOT silently treated as not-owed;
      it returns an explicit unknown state (missing history is unknown, never zero)
    - **Render assertions, not enum assertions.** `renderTaxPosture(not-owed/unregistered)` contains
      the words "not owed" and does NOT contain "0.00"; `renderTaxPosture(calculated-zero)` says the
      tax was calculated as zero and names the reason. A backend fix that never reaches the rendered
      string is the defect class this test exists to catch.
  </behavior>
  <action>
Implement `packages/billing/src/tax.ts` from RESEARCH.md §Code Examples 4 — the shape is already
worked out, use it. Add `renderTaxPosture(posture): string` beside it, because BILL-05's promise is
about what a human reads, not about an enum value.

The load-bearing comment on `taxPosture` must say, in Stripe's own terms: **`not_collecting` is
ambiguous.** It means EITHER no active registration in the customer's jurisdiction OR a Nontaxable
product tax code on the product. The product code is the second half, which is why this function
takes two arguments and refuses to be written with one.

Add an explicit `{ state: "unknown"; reason: string }` arm for a zero with an unrecognised or
missing reason. `@pikar/core`'s `CashFigure` establishes the law (unknown ≠ zero); borrow the
discipline, not the type — `cash.ts` is USD dollars about the TENANT's business and must never carry
Pikar's billing cents (`cash.ts:15-17`).

**Do not write a threshold monitor, a threshold constant, or a `tax.threshold.*` subscription.**
There is no such webhook event; monitoring is live-mode only and gated at $10k prior-year revenue.
That reality is already recorded in `docs/playbooks/billing.md` by plan 02.
  </action>
  <verify>
    <automated>cd packages/billing &amp;&amp; npx vitest run src/tax.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Every `taxability_reason` from Stripe's table has an asserted posture; the rendered not-owed string never contains "0.00"; `grep -rn "tax.threshold" packages/` returns nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcileEvent — the Stripe signal to ledger-phase mapping</name>
  <files>packages/billing/src/reconcile.ts, packages/billing/src/reconcile.test.ts, packages/billing/src/index.ts, docs/playbooks/billing.md</files>
  <behavior>
    Write these RED first (reconcile.test.ts), each against a fabricated Stripe event object literal:
    - `invoice.finalized` → one `estimated` movement for the invoice total, correlated on the invoice id
    - `invoice.paid` where the invoice was paid by CARD → one `actual` movement
    - **`invoice.paid` where the invoice used a bank transfer → NO `actual` movement.** This is the
      single most important assertion in the phase. Assert the returned movement list contains no
      `phase: "actual"` entry.
    - `customer_cash_balance_transaction.created` `type: "funded"` → `reserved` (money exists, it is
      not ours yet)
    - `customer_cash_balance_transaction.created` `type: "applied_to_payment"` → `actual`, correlated
      to the PaymentIntent in `applied_to_payment.payment_intent`
    - `customer_cash_balance_transaction.created` `type: "funding_reversed"` → a `refunded` movement.
      It is NEVER dropped.
    - `type: "unapplied_from_payment"` → `refunded`
    - `cash_balance.funds_available` → an `unapplied-funds` observation, NOT a money movement, and
      NOT an arrival signal. Assert it produces zero ledger movements and one visibility record.
      (Under Stripe's default automatic reconciliation this event fires only when money is LEFT
      OVER — getting this backwards is the trap named in the research.)
    - `charge.refunded` and `credit_note.created` → `refunded`, POSITIVE amount (direction lives in
      the phase, never in the sign — `spend.ts:25`)
    - `invoice.payment_failed` → zero movements
    - An event type outside the handled union → zero movements, no throw
    - **Order independence:** feeding the events in reverse order produces the same movement SET.
      Stripe does not guarantee delivery order.
    - **Money boundary, at this boundary and not by editing `packages/revenue`:** Stripe's lowercase
      `"usd"` normalizes; an amount of `1050` minor units stays `1050` (no double conversion, no
      float); a JPY amount has 0 minor digits; a movement that cannot be built returns an error
      rather than a zero.
  </behavior>
  <action>
Implement `packages/billing/src/reconcile.ts`. `reconcileEvent(event)` is PURE — no `ctx`, no DB, no
`fetch` — returning `{ movements: BillingMovement[]; observations: BillingObservation[] }` where a
`BillingMovement` is `{ phase: SpendPhase; amount: Money; correlationId: string; kind: string;
stripeObjectId: string }`.

The mapping table is settled in RESEARCH.md §Finding 6 — implement it, do not re-derive it:

| Stripe signal | phase |
|---|---|
| invoice finalized / open | `estimated` |
| cash-balance `funded` | `reserved` |
| cash-balance `applied_to_payment` | `actual` |
| `invoice.paid` for a CARD payment | `actual` |
| `charge.refunded` / `credit_note.created` / `refunded_from_payment` | `refunded` |
| `funding_reversed` / `unapplied_from_payment` | `refunded` |
| `cash_balance.funds_available` | no movement — an unapplied-funds OBSERVATION |

Detect bank transfer from the invoice's payment method / collection fields rather than assuming;
export `PAYMENT_METHOD_BANK_TRANSFER` as a named constant so the discriminator is one symbol, not a
string scattered through branches.

`correlationId` must satisfy the repo's ref-safe token rule `/^[A-Za-z0-9._:@/-]+$/`
(`spend.ts:42`) — Stripe ids already do, but build it from ids only. **Never put a Stripe object, an
email, a name or any prose into a movement, an observation or anything bound for audit or a dead
letter** (CLAUDE.md §4): refs, hashes, ids and counts ONLY.

Money is `@pikar/revenue`: `moneyFromMinor(event.amount, event.currency)`. Stripe is natively minor
units — **do not multiply, do not divide, never a float**.

If plan 02 reported `BANK_TRANSFER_ENABLED === false`, still implement and test the cash-balance
arm, and record in `docs/playbooks/billing.md` that it cannot fire for this merchant today. The
tests prove the LAW, which is what makes it safe to keep rather than delete — and safe to enable the
day the country changes.

Update `docs/playbooks/billing.md`: the event→phase table above under **Invariants**, with the
enforcing test file named beside each row, plus the sentence that `invoice.paid` is not cash in hand
for a bank transfer and `funded` is arrival, not collection.
  </action>
  <verify>
    <automated>cd packages/billing &amp;&amp; npx vitest run &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Every row of the mapping table has an asserted test; the bank-transfer `invoice.paid` negative case is green; `funding_reversed` and `cash_balance.funds_available` are each explicitly covered; `tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
1. `cd packages/billing && npx vitest run && npx tsc --noEmit`
2. `grep -rn "tax.threshold\|thresholdMonitor" packages/ apps/` returns NOTHING
3. `grep -rn "parseFloat\|Number(.*amount.*) *[*/]" packages/billing/src` returns NOTHING — no float money
4. Mutation check: change `applied_to_payment` to `funded` in `reconcile.ts` and confirm tests go RED, then restore. A rename-class mutation, not a deletion — deletion-only mutation is blind to loose matching.
5. `node scripts/check-playbooks.mjs < /dev/null` — read STDOUT
</verification>

<success_criteria>
- `invoice.paid` on a bank-transfer invoice produces no `actual` movement, proven by test
- `funding_reversed` reverses; nothing is silently dropped
- `cash_balance.funds_available` is modelled as unapplied-funds visibility, not as arrival
- A zero tax line always carries its reason and never renders as a bare "$0.00"
- All amounts are integer minor units through `@pikar/revenue`; no float touches money
- Zero lines of threshold-monitoring code exist
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-03-SUMMARY.md`
</output>
