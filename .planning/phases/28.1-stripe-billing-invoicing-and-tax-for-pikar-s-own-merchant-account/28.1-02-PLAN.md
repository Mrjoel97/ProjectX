---
phase: 28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account
plan: 02
type: execute
wave: 2
depends_on: ["28.1-01"]
files_modified:
  - packages/billing/src/config.ts
  - packages/billing/src/config.test.ts
  - packages/billing/src/index.ts
  - packages/backend/convex/lib/env.ts
  - docs/billing/stripe-dashboard-setup.md
  - docs/playbooks/billing.md
autonomous: false
requirements: [BILL-04, BILL-05, BILL-06]

user_setup:
  - service: stripe-billing
    why: "Pikar's OWN merchant account (acct_1U9DJHV05ajSTq7I). Products, prices, tax category, head-office address, Customer Portal, Smart Retries and bank transfer are Dashboard-only — no API surface is being built for them (locked decision)."
    env_vars:
      - name: BILLING_STRIPE_SECRET_KEY
        source: "Stripe Dashboard (TEST MODE first) -> Developers -> API keys -> Secret key"
      - name: BILLING_STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks -> the /billing/stripe/webhook endpoint -> Signing secret (whsec_...)"
      - name: BILLING_STRIPE_PRICE_ID
        source: "Stripe Dashboard -> Product catalogue -> the Pikar plan -> the recurring price id (price_...)"
    dashboard_config:
      - task: "Create the product + one flat-rate recurring price"
        location: "Dashboard -> Product catalogue"
      - task: "Set the product tax category to a REAL SaaS code — never txcd_00000000"
        location: "Dashboard -> Product -> Tax category"
      - task: "Set the head office address"
        location: "Dashboard -> Tax -> Settings -> Origin address"
      - task: "Enable Stripe Tax in monitoring mode with NO registrations"
        location: "Dashboard -> Tax"
      - task: "Configure the Customer Portal (upgrade/downgrade/cancel/payment method)"
        location: "Dashboard -> Settings -> Billing -> Customer portal"
      - task: "Enable Smart Retries + automated failed-payment emails"
        location: "Dashboard -> Settings -> Billing -> Revenue recovery"
      - task: "Invoice branding: logo, colour, footer"
        location: "Dashboard -> Settings -> Branding"
      - task: "Attempt to enable bank transfer, and REPORT WHETHER IT IS AVAILABLE for this merchant's country"
        location: "Dashboard -> Settings -> Billing -> Invoices -> Payment methods -> Bank transfer"
      - task: "Register the webhook endpoint at {CONVEX_SITE_URL}/billing/stripe/webhook and note its API version"
        location: "Dashboard -> Developers -> Webhooks"

must_haves:
  truths:
    - "The product tax code Pikar configured is recorded in code and is NOT txcd_00000000"
    - "The Stripe API version is pinned by us, not inherited from a Dashboard default"
    - "Whether this merchant can accept bank transfer at all is a KNOWN answer, recorded in code"
    - "Every BILLING_* name consumed in source has an ENV_MANIFEST row and refuses loudly when unset"
    - "The threshold-monitoring reality (live mode only, email/Dashboard only, $10k prior-year gate) is written down as an owner duty, not built as code"
  artifacts:
    - path: "packages/billing/src/config.ts"
      provides: "Code-owned Stripe configuration constants that mirror the Dashboard"
      exports: ["STRIPE_API_BASE", "STRIPE_API_VERSION", "PRODUCT_TAX_CODE", "HEAD_OFFICE_COUNTRY", "BANK_TRANSFER_ENABLED", "TRIAL_DAYS", "NONTAXABLE_TAX_CODE"]
    - path: "docs/billing/stripe-dashboard-setup.md"
      provides: "The exact Dashboard checklist and what each setting silently breaks"
  key_links:
    - from: "packages/billing/src/config.ts"
      to: "packages/billing/src/config.test.ts"
      via: "a guard test that PRODUCT_TAX_CODE !== NONTAXABLE_TAX_CODE"
      pattern: "txcd_00000000"
    - from: "packages/backend/convex/lib/env.ts"
      to: "process.env.BILLING_STRIPE_SECRET_KEY"
      via: "ENV_MANIFEST literal classification scanned by env.test.ts"
      pattern: "BILLING_STRIPE_SECRET_KEY"
---

<objective>
Turn the Dashboard-only prerequisites into KNOWN, code-owned facts, and get the credentials into the
Convex deployment env.

Purpose: three of the research's open questions are unanswerable from the repo — this merchant's
country and therefore bank-transfer eligibility, the exact SaaS product tax code, and which
`Stripe-Version` to pin. Every downstream plan encodes one of those answers. Guessing them ships a
`not_collecting` that is permanently unreadable, or a bank-transfer arm that is dead code.
Output: `packages/billing/src/config.ts` with the real values, `ENV_MANIFEST` rows, and
`docs/billing/stripe-dashboard-setup.md`.

**This plan is `autonomous: false` and BLOCKS waves 3+. It deliberately does NOT block wave 1** —
plan 01 needs no key.

**No threshold monitor is built here or anywhere.** Verified: there is no `tax.threshold.*` webhook
event, monitoring is live-mode only, and notifications require $10,000 of prior-year revenue.
Threshold monitoring is an owner/Dashboard duty and belongs in the playbook with its preconditions
stated. Code that watched for it could never fire.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-CONTEXT.md
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-RESEARCH.md
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-01-SUMMARY.md
@packages/backend/convex/lib/env.ts
@docs/playbooks/billing.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Dashboard checklist and the config module with the values still UNSET</name>
  <files>docs/billing/stripe-dashboard-setup.md, packages/billing/src/config.ts, packages/billing/src/config.test.ts, packages/billing/src/index.ts</files>
  <action>
`docs/billing/stripe-dashboard-setup.md` — an ordered checklist the owner can execute without
reading anything else. For each item: where it lives in the Dashboard, the exact value to choose,
and **what breaks silently if it is wrong**. Cover, in this order:

1. Confirm the account: `acct_1U9DJHV05ajSTq7I` ("Pikar-Ai"), TEST MODE first. Zero products
   existed at plan time — a clean slate, nothing to migrate.
2. Invoice branding: logo, colour, footer only. No rendering templates (locked decision).
3. One product + ONE flat-rate recurring price. Record the `price_…` id.
4. **Product tax category.** It must be a real SaaS code. It MUST NOT be `txcd_00000000`
   (Nontaxable). Why: `not_collecting` is ambiguous between "we are unregistered" and "we declared
   this product nontaxable", and the product code is the only thing that disambiguates it. Choose
   `txcd_00000000` and the honest posture is permanently unreadable, and threshold monitoring —
   which "assumes all sales are conducted with your preset tax code" — is poisoned too.
   Research proposes `txcd_10103001` (Software as a service) at LOW confidence: **verify the exact
   id in the Dashboard tax-code selector and report what you actually picked.** The RULE (not
   `txcd_00000000`) is HIGH confidence regardless of which id it turns out to be.
5. Head office / origin address. This is what EXCLUDES the home jurisdiction from monitoring, not
   just what includes others.
6. Stripe Tax enabled in monitoring mode with NO registrations. Zero tax is the correct answer today
   and must be reported as *not owed*, never as *calculated*.
7. Customer Portal configuration (upgrade / downgrade / cancel / payment-method update).
8. Smart Retries + automated failed-payment emails. Dashboard only, zero code.
9. Bank transfer under Invoices → Payment methods. **If the toggle is unavailable for this
   merchant's country, say so — that is the answer, not a failure.**
10. Webhook endpoint at `{CONVEX_SITE_URL}/billing/stripe/webhook` subscribed to the locked event
    list, and note the API version the endpoint reports.
11. The `npx convex env set` commands, run **from `packages/backend`**, never Vercel, never `.env`.

Then write `packages/billing/src/config.ts` with every value as an explicit constant and each one
carrying a `// UNCONFIRMED — set in 28.1-02 Task 3` marker where the owner's answer is still
pending:
`STRIPE_API_BASE = "https://api.stripe.com"`, `STRIPE_API_VERSION`, `PRODUCT_TAX_CODE`,
`NONTAXABLE_TAX_CODE = "txcd_00000000"`, `HEAD_OFFICE_COUNTRY`, `BANK_TRANSFER_ENABLED: boolean`,
`TRIAL_DAYS`.

`config.test.ts` — the guard that outlives this plan:
- `PRODUCT_TAX_CODE !== NONTAXABLE_TAX_CODE`, asserted against the written-out literal
  `"txcd_00000000"` (not just the imported constant — a rename of the constant must not silently
  pass the test).
- `STRIPE_API_VERSION` matches `/^\d{4}-\d{2}-\d{2}(\.[a-z]+)?$/` and is non-empty.
- `HEAD_OFFICE_COUNTRY` is a two-letter ISO code.
These are expected to FAIL until Task 3 lands the real values. That is the point: the red test is
the reminder.
  </action>
  <verify>
    <automated>cd packages/billing &amp;&amp; npx vitest run src/config.test.ts; test $? -le 1</automated>
  </verify>
  <done>`docs/billing/stripe-dashboard-setup.md` exists with all 11 items; `config.ts` exports every constant; `config.test.ts` runs and its failures name exactly the values the checkpoint must supply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CHECKPOINT — owner performs the Dashboard configuration and sets the Convex env</name>
  <what-built>`docs/billing/stripe-dashboard-setup.md`, an ordered checklist with the exact value and the silent failure mode for each setting, and a `config.ts` whose guard test is red until the answers land.</what-built>
  <how-to-verify>
Work through `docs/billing/stripe-dashboard-setup.md` top to bottom in **TEST MODE** on
`acct_1U9DJHV05ajSTq7I`, then report these FIVE answers back. Everything downstream is blocked on
them and none of them can be read from the repo:

  1. **Merchant country** (the account's country) and **is the bank-transfer toggle available?**
     yes / no. If no, BILL-03's bank-transfer arm becomes recorded dead code rather than shipped
     code — that is a legitimate outcome and the phase will say so.
  2. **The exact product tax code id you selected** (e.g. `txcd_10103001`). If the Dashboard offers
     a different SaaS code, that one wins — the rule is only that it is not `txcd_00000000`.
  3. **The `Stripe-Version` your webhook endpoint reports** (e.g. `2025-03-31.basil`). This decides
     whether the invoice tax field is `total_tax_amounts` or `total_taxes`.
  4. **The `price_…` id** of the flat-rate plan, and the **trial length in days**.
  5. **Head office country** as configured in Tax → Settings.

Then, **from `packages/backend`** (the Convex CLI only works from there):

```
npx convex env set BILLING_STRIPE_SECRET_KEY      sk_test_...
npx convex env set BILLING_STRIPE_WEBHOOK_SECRET  whsec_...
npx convex env set BILLING_STRIPE_PRICE_ID        price_...
```

Secrets go to the **Convex deployment env only** — never Vercel, never `.env`, never a commit.
Quoting on Windows is fragile: if `env set` misbehaves, verify with `npx convex env list` and
re-set rather than assuming it took.

**Do NOT attempt threshold monitoring here.** It does not run in test mode at all, and its
notifications need $10,000 of prior-year revenue. Nothing about it is verifiable today.
  </how-to-verify>
  <resume-signal>Reply with the five answers above, or describe what the Dashboard would not let you do</resume-signal>
  <files>(none — Stripe Dashboard and `npx convex env set`; no repo file changes)</files>
  <action>Human-only. Claude cannot log into the Stripe Dashboard, cannot read this merchant's
country, and must not invent a product tax code or an API version. Every automatable part of this
phase is already automated — this checkpoint verifies AFTER that automation, it does not replace it.
Do NOT proceed past this task on assumed values.</action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx convex env list | grep -c "^BILLING_STRIPE_" | grep -qx 3</automated>
  </verify>
  <done>The five answers are reported, and `npx convex env list` shows all three `BILLING_STRIPE_*` names set in the Convex deployment env (values never printed, never in `.env`, never in Vercel).</done>
</task>

<task type="auto">
  <name>Task 3: Land the owner's answers as code-owned constants and classify the env names</name>
  <files>packages/billing/src/config.ts, packages/billing/src/config.test.ts, packages/backend/convex/lib/env.ts, docs/billing/stripe-dashboard-setup.md, docs/playbooks/billing.md</files>
  <action>
Replace every `UNCONFIRMED` marker in `config.ts` with the owner's reported value, and delete the
markers. Set `BANK_TRANSFER_ENABLED` from the owner's yes/no. If it is **false**, add a comment on
the constant naming the merchant country and the date, and record in
`docs/playbooks/billing.md` that the cash-balance handlers exist but can never fire for this
merchant — the offline tests still prove the LAW (money in a cash balance is not collected until it
is applied), which is what makes the code safe to keep rather than delete.

Add the remaining `ENV_MANIFEST` rows in the Phase 28.1 block created by plan 01, all `tier:
"feature"` (with none set the app runs and billing refuses loudly — never degrades):
- `BILLING_STRIPE_SECRET_KEY` — whatBreaks: "Every outbound Stripe call. No Checkout session, no
  portal link and no invoice can be created; billing refuses rather than degrading."
- `BILLING_STRIPE_PRICE_ID` — whatBreaks: "Checkout cannot name a plan, so no tenant can subscribe."
Read each through a **literal** `process.env.BILLING_…` — `env.test.ts` scans for literals and a
computed `process.env[name]` is invisible to it (that is why `CONNECTOR_CREDENTIAL_KEY_V1` is
deliberately absent from the manifest).

Update `docs/billing/stripe-dashboard-setup.md` with an "As configured on YYYY-MM-DD" section
recording exactly what the owner selected, so a Dashboard drift is detectable by diffing it against
`config.ts`.

Update `docs/playbooks/billing.md` with an **Operations** section stating, in these terms:
threshold monitoring is a Dashboard/email duty of the account owner; it does **not** run in test
mode; there is **no** `tax.threshold.*` webhook to subscribe to; notifications require opting into
Stripe Tax, no active live-mode registration for the location, no notification in the past 7 days,
and **$10,000 USD of revenue in the previous year** — which this merchant does not have, so no
notification will fire today. State that the owner must check Dashboard → Tax → Monitoring manually,
and name the cadence.
  </action>
  <verify>
    <automated>cd packages/billing &amp;&amp; npx vitest run src/config.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../backend &amp;&amp; npx vitest run convex/env.test.ts</automated>
  </verify>
  <done>`config.test.ts` is GREEN (the tax code is real, the API version is pinned, the country is set); `env.test.ts` is green with all three `BILLING_*` names classified; the playbook's Operations section states the threshold reality including the $10k gate.</done>
</task>

</tasks>

<verification>
1. `cd packages/billing && npx vitest run && npx tsc --noEmit`
2. `cd packages/backend && npx vitest run convex/env.test.ts && npx tsc --noEmit`
3. `cd packages/backend && npx convex env list` — the three `BILLING_*` names are present (values not printed)
4. `grep -rn "txcd_00000000" packages/billing/src/config.ts` — appears ONLY as `NONTAXABLE_TAX_CODE`, never as `PRODUCT_TAX_CODE`
5. `grep -rn "tax.threshold" packages/ apps/` returns NOTHING — no monitor was written
6. `node scripts/check-playbooks.mjs < /dev/null` — read STDOUT
</verification>

<success_criteria>
- Merchant country and bank-transfer eligibility are a recorded, code-visible answer — not an assumption
- `PRODUCT_TAX_CODE` is a real SaaS code and a test kills any change to `txcd_00000000`
- `STRIPE_API_VERSION` is pinned by us and sent on every request from plan 05 onward
- All three `BILLING_*` names are in `ENV_MANIFEST` as `feature` with no development fallback
- Threshold monitoring is documented as an owner duty with its $10k / live-mode preconditions, and ZERO lines of monitor code exist
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-02-SUMMARY.md`
</output>
