---
phase: 28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account
plan: 05
type: execute
wave: 5
depends_on: ["28.1-04"]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/core/src/tenantData.ts
  - packages/backend/convex/deadLetter.ts
  - packages/backend/convex/deadLetter.test.ts
  - packages/backend/convex/deadLetters.ts
  - packages/backend/convex/billingWebhook.ts
  - packages/backend/convex/billingWebhook.test.ts
  - packages/backend/convex/billing.ts
  - docs/playbooks/billing.md
autonomous: true
requirements: [BILL-01]

must_haves:
  truths:
    - "A checkout.session.completed carrying a known tenant stores the mapping on BOTH sides — tenant row knows the Stripe customer, and the Stripe customer resolves back to exactly one tenant"
    - "A Stripe customer with no matching tenant is dead-lettered by ref and NO tenant is created"
    - "The dead-letter payload contains no email, no name and no Stripe object — refs, ids and counts only"
    - "Subscription created/updated/deleted keep the tenant's status current without requiring delivery order"
    - "The same checkout.session.completed delivered twice produces exactly one mapping row"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "billingCustomers table; deadLetters.workflowId relaxed to optional with a source discriminator"
      contains: "billingCustomers"
    - path: "packages/backend/convex/billingWebhook.ts"
      provides: "checkout + subscription arms of the effect switch, including the refusal to auto-provision"
      exports: ["receiveAndApply"]
  key_links:
    - from: "packages/backend/convex/billingWebhook.ts"
      to: "billingCustomers"
      via: "insert-if-absent keyed on (tenantId, stripeCustomerId)"
      pattern: "billingCustomers"
    - from: "packages/backend/convex/billingWebhook.ts"
      to: "deadLetters"
      via: "unmatched Stripe customer, by ref only"
      pattern: "deadLetters"
---

<objective>
The tenant↔Stripe-customer mapping, and the refusal that keeps it honest: an unmatched customer is
dead-lettered by ref, never auto-provisioned.

Purpose: BILL-01's second half. A `checkout.session.completed` can arrive for a customer who paid
before onboarding finished, or whose tenant was already deleted. Creating a tenant from a webhook is
how a billing system invents users. The locked decision is: never.
Output: `billingCustomers`, a relaxed `deadLetters.workflowId`, and the checkout/subscription arms of
the ONE effect switch plan 01 created.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-RESEARCH.md
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-04-SUMMARY.md
@packages/backend/convex/deadLetter.ts
@packages/backend/convex/deadLetters.ts
@packages/backend/convex/audit.ts
@packages/core/src/tenantData.ts
@CLAUDE.md
@packages/backend/.claude/skills/convex-migration-helper/SKILL.md

<interfaces>
From packages/backend/convex/schema.ts:216-229 — the table to relax. `workflowId` is REQUIRED today
and a Stripe webhook has no workflow:
```ts
deadLetters: defineTable({
  tenantId: v.string(),
  correlationId: v.string(),
  workflowId: v.string(),      // <- becomes v.optional(v.string())
  payload: v.any(),
  error: v.string(),
  status: v.union(v.literal("new"), v.literal("replayed"), v.literal("resolved")),
  createdAt: v.number(),
}).index("by_tenant", ["tenantId"]).index("by_status", ["status"]).index("by_tenant_status", ["tenantId","status"]),
```

From packages/core/src/tenantData.ts:32 — `deadLetters` is `audit_immutable`. Two obligations ride
with that category: the writer is INSERT-ONLY, and **nothing there may ever become personal data.**
It is excluded from BOTH the deletion walk and the export walk by construction.

From packages/backend/convex/deadLetter.ts:17 — the ONLY writer today, an `internalMutation` wired
as a workflow `onComplete`. It inserts the row, then writes a `deadletter.written` audit row, then
notifies.

From packages/backend/convex/audit.ts:16 — `audit.log` is the SOLE insert surface:
`{ tenantId, correlationId, eventType, actor, payload }`. `payload` is `v.any()` at the boundary but
the contract is refs / hashes / ids / counts (CLAUDE.md §4).
Cross-module call idiom, from tenantDelete.ts:31-40:
```ts
const auditLog = makeFunctionReference<"mutation", {...}>("audit:log");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relax deadLetters.workflowId and add a source discriminator</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/deadLetter.ts, packages/backend/convex/deadLetter.test.ts, packages/backend/convex/deadLetters.ts</files>
  <behavior>
    - An existing pipeline dead letter (with a `workflowId`) still writes, still audits, still
      notifies — the existing `deadLetter.test.ts` cases stay green unchanged
    - A row written with NO `workflowId` and `source: "billing"` inserts successfully
    - `newCount`, `listNew`, `listAll` and `markResolved` all still work over a row with no
      `workflowId` — no reader may assume the field is present
    - A row with no `source` (every existing row) is treated as `"workflow"` by readers, not as a
      missing value
  </behavior>
  <action>
Change `deadLetters.workflowId` from `v.string()` to `v.optional(v.string())` and add
`source: v.optional(v.union(v.literal("workflow"), v.literal("billing")))`.

**Relaxing a constraint needs no backfill and no migration** (`convex-migration-helper`: widen →
migrate → narrow; this is the widen step and it terminates here — nothing narrows). Existing rows
stay valid. Adding an optional field likewise needs no backfill.

Why relax rather than synthesize: writing a fake `workflowId` like `billing:evt_…` would lie about
what the field means to every existing reader and to the compliance surface. The field is optional
because the fact is optional.

Update `deadLetter.ts` to write `source: "workflow"` explicitly on its own path, so the
discriminator is set at the write site rather than inferred. Update the readers in `deadLetters.ts`
for the now-optional field. Do NOT add any mutating audit function — the audit module is insert-only
(CLAUDE.md §3), and `deadLetters` is `audit_immutable`, which means insert-only too.

Say out loud in a comment on the table: **this table is `audit_immutable` and is excluded from both
the tenant deletion walk and the export walk. Nothing written here may ever become personal data.**
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/deadLetter.test.ts convex/deadLetters.test.ts convex/dashboardSchema.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Existing dead-letter tests green unchanged; a workflow-less billing row inserts and reads; no reader assumes `workflowId` exists; no mutating audit or dead-letter function was added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: billingCustomers and the two-directional mapping</name>
  <files>packages/backend/convex/schema.ts, packages/core/src/tenantData.ts, packages/backend/convex/billingWebhook.ts, packages/backend/convex/billingWebhook.test.ts, packages/backend/convex/billing.ts</files>
  <behavior>
    Driven through the real route with `convexTest(schema, modules).fetch(...)`, fabricated events,
    fabricated `whsec_`:
    - `checkout.session.completed` with `client_reference_id` = a known tenant → one
      `billingCustomers` row; `by_tenant` resolves the Stripe customer and `by_customer` resolves
      back to exactly that tenant
    - The tenant is resolvable from `metadata.tenantId` when `client_reference_id` is absent
    - The SAME event delivered twice → still exactly ONE `billingCustomers` row
    - A `customer.subscription.created` arriving BEFORE its `checkout.session.completed` does not
      crash and does not create a second row — order is not guaranteed
    - `customer.subscription.updated` moves the status; `customer.subscription.deleted` marks it
      terminated without deleting the mapping row
    - `billingStatus` (plan 04) now returns the real state for a mapped tenant, and still returns
      "unknown" — never "free" and never zero — for an unmapped one
    - A second Stripe customer id arriving for an already-mapped tenant does NOT silently overwrite
      the mapping; assert the conflict is recorded rather than absorbed
  </behavior>
  <action>
Add `billingCustomers` to `schema.ts`:
`{ tenantId: v.string(), stripeCustomerId: v.string(), subscriptionId: v.optional(v.string()), status: v.string(), priceId: v.optional(v.string()), trialEndsAt: v.optional(v.number()), createdAt: v.number(), updatedAt: v.number() }`
with `.index("by_tenant", ["tenantId"])` and `.index("by_customer", ["stripeCustomerId"])`. Two
indexes because the mapping must resolve in BOTH directions — that is the requirement, not a
convenience.

Classify it in `packages/core/src/tenantData.ts` as `tenant_owned`: it holds no secret, and it MUST
be deleted by tenant erasure. That is precisely why plan 08's billing arm has to run BEFORE the page
loop — the loop deletes the row holding `stripeCustomerId`.

Add the `checkout.session.completed` and `customer.subscription.*` arms to the effect switch inside
`billingWebhook.receiveAndApply` — the SAME mutation plan 01 created, so the mapping write and the
dedupe insert commit or fail together. No new mutation, no `runAction`, no `fetch`.

Write every arm **order-independently.** A `customer.subscription.created` may arrive before its
checkout session; a handler that requires an ordering is a handler that breaks in production.
Resolve the tenant by `stripeCustomerId` via `by_customer` when `client_reference_id` is absent.

Fill the branch plan 04 left in `billing.ts::billingStatus`. Never fabricate a tier.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billing convex/dashboardSchema.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../core &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Mapping resolves both ways; a duplicate delivery leaves one row; out-of-order subscription events are handled; the tenantData drift test is green with `billingCustomers` classified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The refusal — an unmatched customer is dead-lettered by ref, never invented</name>
  <files>packages/backend/convex/billingWebhook.ts, packages/backend/convex/billingWebhook.test.ts, docs/playbooks/billing.md</files>
  <behavior>
    - `checkout.session.completed` whose `client_reference_id` matches NO tenant → exactly one
      `deadLetters` row with `source: "billing"`, no `workflowId`, and **zero new users, zero new
      tenants, zero new billingCustomers rows**. Assert the counts of all three before and after.
    - The dead-letter `payload` contains ONLY refs, ids and counts. Assert explicitly that the
      fabricated event's customer **email** and **name** appear NOWHERE in the stored row —
      serialize the whole row and assert the strings are absent. `deadLetters` is `audit_immutable`
      and must never become a PII honeypot (CLAUDE.md §4).
    - The `error` string names the reason by code, not by prose containing customer data
    - A `deadletter.written`-style audit row is written and its payload is equally clean
    - The same unmatched event delivered twice → still exactly ONE dead-letter row (the
      `billingStripeEvents` dedupe covers it)
    - `customer.subscription.updated` for an unknown customer takes the same refusal path
  </behavior>
  <action>
Add the unmatched-tenant arm to the effect switch. On no match:

1. Insert a `deadLetters` row: `tenantId` = a code-owned sentinel for "unattributable" (do NOT
   invent a tenant id, and do NOT reuse a real one), `correlationId` = the Stripe event id,
   `source: "billing"`, no `workflowId`, `error` = a code-owned reason constant, `payload` =
   `{ stripeCustomerId, stripeEventId, eventType, sessionId }` — **ids only.**
2. Write the audit row through the `audit:log` function reference idiom
   (`tenantDelete.ts:31-40`), with the same refs-only payload.
3. Return without creating anything.

**Never auto-provision.** No `users` insert, no tenant insert, no `billingCustomers` insert on this
path. The customer stays orphaned and VISIBLE — that is the point. The existing
`deadLetters.newCount` / `listNew` surfaces already make it visible; no new UI is needed.

Redact-then-write is a step-ordering contract: build the refs-only payload first, and never pass the
parsed Stripe object anywhere near the insert. The simplest way to make that true is to destructure
the ids you need at the top of the arm and let the event object go out of scope.

Update `docs/playbooks/billing.md`: the mapping data flow, and under **Invariants** — "a Stripe
customer without a tenant is dead-lettered by ref and never auto-provisioned", "no email, name or
Stripe object may enter a dead-letter or audit payload", each with the enforcing test named. Also
record the operational recovery: how an owner reconciles an orphaned customer by hand (match in the
Dashboard, then have the tenant re-run Checkout, or refund).
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billing convex/deadLetter.test.ts convex/audit.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>The unmatched path creates zero users/tenants/mappings and exactly one clean dead letter; the serialized row provably contains no email or name; the duplicate delivery still produces one row.</done>
</task>

</tasks>

<verification>
1. `cd packages/backend && npx vitest run convex/billing convex/deadLetter.test.ts convex/deadLetters.test.ts convex/audit.test.ts convex/env.test.ts && npx tsc --noEmit`
2. `cd packages/core && npx vitest run && npx tsc --noEmit`
3. `grep -rn "db.insert(\"users\"\|db.insert(\"tenant" packages/backend/convex/billingWebhook.ts` returns NOTHING
4. Mutation check: remove the tenant-match guard so the handler provisions on miss; the refusal tests must go RED. Restore.
5. `git diff --stat HEAD -- "*.ts"` after committing — every file in `files_modified` must appear, or the plan claimed work the commit does not contain
6. `node scripts/check-playbooks.mjs < /dev/null` — read STDOUT
</verification>

<success_criteria>
- The tenant↔Stripe-customer mapping resolves in both directions and cannot be double-created
- An unmatched Stripe customer produces exactly one dead letter and zero invented records
- No email, name or Stripe object reaches a dead-letter or audit payload, proven by a whole-row string assertion
- `deadLetters.workflowId` is optional with a `source` discriminator; no backfill, no mutating writer
- Subscription lifecycle events are order-independent
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-05-SUMMARY.md`
</output>
