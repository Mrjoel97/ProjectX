---
phase: 28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account
plan: 06
type: execute
wave: 6
depends_on: ["28.1-05"]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/core/src/tenantData.ts
  - packages/backend/convex/billingLedger.ts
  - packages/backend/convex/billingLedger.test.ts
  - packages/backend/convex/billingWebhook.ts
  - packages/backend/convex/billingWebhook.test.ts
  - packages/backend/convex/billing.ts
  - packages/backend/convex/billing.test.ts
  - docs/playbooks/billing.md
autonomous: true
requirements: [BILL-03]

must_haves:
  truths:
    - "Every billing outcome lands in OUR append-only ledger, which stays the book of record — Stripe is a processor, not the source of truth"
    - "An invoice.paid on a bank-transfer invoice writes NO actual movement; only applied_to_payment does"
    - "Bank-transfer money arriving is reserved, not collected; funding_reversed reverses it"
    - "Unapplied funds are visible with their AGE, not assumed collected"
    - "A tenant with no billing history reads as unknown coverage, never as zero revenue"
    - "aggregateSpend, spendEvents and every Phase 26 Finance total are byte-identical — no revenue rail was added"
  artifacts:
    - path: "packages/backend/convex/billingLedger.ts"
      provides: "recordBillingMovement (exactly-once), coverage, bounded reads — mirrors spendLedger.ts"
      exports: ["recordBillingMovement", "ensureBillingCoverage", "billingCoverageFor", "listBillingEventsFor", "BILLING_EVENT_PAGE_LIMIT"]
    - path: "packages/backend/convex/schema.ts"
      provides: "billingEvents, billingCoverage, billingUnapplied — separate from spendEvents by decision"
      contains: "billingEvents"
  key_links:
    - from: "packages/backend/convex/billingWebhook.ts"
      to: "packages/backend/convex/billingLedger.ts"
      via: "recordBillingMovement called INSIDE receiveAndApply's transaction"
      pattern: "recordBillingMovement"
    - from: "packages/backend/convex/billingLedger.ts"
      to: "packages/billing/src/reconcile.ts"
      via: "reconcileEvent supplies the phase and the Money; the adapter only writes"
      pattern: "reconcileEvent"
---

<objective>
The book of record. A separate `billingEvents` ledger that reuses the spend plane's law verbatim and
touches none of its rails.

Purpose: BILL-03. Money-in and money-out must not share a `Record<SpendRail, SpendTotals>`.
Output: `billingEvents` / `billingCoverage` / `billingUnapplied`, `convex/billingLedger.ts`, and the
money arms of the ONE effect switch.

**Owner decision, settled 2026-08-28 — do not reopen it.** A separate table, NOT a fourth `revenue`
rail on `spendEvents`. `SPEND_RAILS` is a closed cost union (`spend.ts:16`: "A fourth is a deliberate
schema edit, not a string"), `aggregateSpend` hard-codes all three rails in five places, and Phase 26
Finance renders those totals as *what Pikar spends*, today. **`aggregateSpend` must need NO change,
and a task in this plan proves it.**
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-RESEARCH.md
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-03-SUMMARY.md
@.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-05-SUMMARY.md
@packages/backend/convex/spendLedger.ts
@packages/core/src/spend.ts
@packages/billing/src/reconcile.ts
@CLAUDE.md

<interfaces>
From packages/backend/convex/spendLedger.ts — the file to MIRROR, symbol for symbol. Copy the shape,
not the table:
```ts
export async function ensureCoverage(ctx, tenantId, nowMs): Promise<number>;  // :50  insert-if-absent, returns the EXISTING start
export const startCoverage = internalMutation(...);                            // :68
export async function recordMovement(ctx, args): Promise<Id<"spendEvents">>;   // :107 identity = (tenantId, correlationId, phase)
export const record = internalMutation(...);                                   // :135
export async function coverageFor(ctx, tenantId): Promise<number | null>;      // :147 null = UNKNOWN, never zero
export async function listEventsFor(ctx, args);                                // :177 bounded half-open [since, until)
export const SPEND_EVENT_PAGE_LIMIT = 500;                                     // :174
```
The identity comment at `spendLedger.ts:92-99` is load-bearing and applies here unchanged:
identity is `(tenantId, correlationId, phase)` — NOT `correlationId` alone, because a reservation
and its later actual charge SHARE one correlation on purpose. A replay returns the stored id and
IGNORES the replayed amount.

From packages/core/src/spend.ts:
```ts
export const SPEND_PHASES = ["estimated","reserved","actual","refunded","adjustment"] as const;
export const REF_SAFE = /^[A-Za-z0-9._:@/-]+$/;         // :42 — correlationId/model/kind must match
export function validateSpendMovement(input): void;      // :55 throws on non-positive-safe-integer amounts
export function aggregateSpend(input);                   // :133 — DO NOT TOUCH
```

From packages/billing/src/reconcile.ts (plan 03):
```ts
export function reconcileEvent(event): { movements: BillingMovement[]; observations: BillingObservation[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The billingEvents ledger, mirroring spendLedger's law</name>
  <files>packages/backend/convex/schema.ts, packages/core/src/tenantData.ts, packages/backend/convex/billingLedger.ts, packages/backend/convex/billingLedger.test.ts</files>
  <behavior>
    - `recordBillingMovement` called twice with the same `(tenantId, correlationId, phase)` inserts
      ONCE and returns the same id — and the second call's differing amount is IGNORED, not written
    - A reservation and its later `actual` SHARE a correlationId and both persist — a
      correlation-only guard would swallow the actual, which is the exact bug the phase key prevents
    - A non-positive or non-safe-integer amount is REFUSED
    - A `correlationId` outside `/^[A-Za-z0-9._:@/-]+$/` is REFUSED
    - A currency mismatch inside one correlation is REFUSED rather than summed
    - `billingCoverageFor` returns `null` for a tenant with no coverage — **unknown, never zero**,
      and a test asserts the caller cannot mistake it for 0
    - `ensureBillingCoverage` is insert-if-absent and returns the EXISTING start on a second call
    - `listBillingEventsFor` is a bounded half-open `[since, until)` read capped at
      `BILLING_EVENT_PAGE_LIMIT`; a request for more returns the cap, never everything
  </behavior>
  <action>
Add three tables to `schema.ts`, additive only:

- `billingEvents`: `{ tenantId, phase (the SPEND_PHASES union, written out verbatim), amountMinor: v.number(), currency: v.string(), correlationId: v.string(), kind: v.string(), stripeObjectId: v.optional(v.string()), taxabilityReason: v.optional(v.string()), createdAt: v.number() }`
  with `.index("by_tenant_createdAt", ["tenantId","createdAt"])` and `.index("by_correlation", ["correlationId"])`.
  **Reuse `SPEND_PHASES` verbatim** — `estimated | reserved | actual | refunded | adjustment`. All
  amounts POSITIVE; direction lives in the phase, never in the sign (`spend.ts:25`).
  Carry `currency` explicitly: unlike `spendEvents` (USD cents), Stripe can send any currency and an
  implicit currency is a fabricated one.
- `billingCoverage`: `{ tenantId, coverageStartedAt }` with `.index("by_tenant", ["tenantId"])`.
- `billingUnapplied`: `{ tenantId, stripeObjectId, amountMinor, currency, observedAt }` with
  `.index("by_tenant_observedAt", ["tenantId","observedAt"])`.

Classify all three in `packages/core/src/tenantData.ts`. `billingEvents` and `billingCoverage` are
**`audit_immutable`**: they are the financial book for Pikar's OWN merchant account, they are
insert-only, and a tenant erasure must not rewrite recorded revenue. That category carries two
obligations — insert-only writers, and **nothing there may ever become personal data**, which holds
because these rows are ids, a currency code and integer minor units. `billingUnapplied` is
`tenant_owned`. Write the reasoning in the comment; a future reader must not have to re-derive it.

Create `convex/billingLedger.ts` mirroring `spendLedger.ts` symbol for symbol. Validate BEFORE
opening coverage, so a rejected movement leaves nothing behind (`spendLedger.ts` does exactly this).
Provide the plain-function half for callers already inside a mutation — `receiveAndApply` is one,
and the whole point is that the dedupe row and the ledger row commit or fail together.

**Do not import `validateSpendMovement`'s rail argument, do not add a rail, do not touch
`packages/core/src/spend.ts`.** Reuse `REF_SAFE` and the phase union; write billing's own amount
validation beside them.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billingLedger.test.ts convex/dashboardSchema.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../core &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Replay is a no-op that ignores the replayed amount; reserve and actual coexist on one correlation; coverage is `null` for an uninstrumented tenant; the tenantData drift test is green with all three tables classified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the money events through the ONE effect switch</name>
  <files>packages/backend/convex/billingWebhook.ts, packages/backend/convex/billingWebhook.test.ts</files>
  <behavior>
    All driven end to end through `convexTest(schema, modules).fetch("/billing/stripe/webhook", ...)`
    with fabricated, correctly-signed events. A handler cannot tell a fabricated
    `customer_cash_balance_transaction` from a real one — that is what makes this $0 and complete:
    - `invoice.finalized` → one `estimated` row
    - `invoice.paid` for a CARD invoice → one `actual` row
    - **`invoice.paid` for a BANK-TRANSFER invoice → NO `actual` row.** Assert the absence.
    - `customer_cash_balance_transaction.created` `funded` → `reserved`
    - `customer_cash_balance_transaction.created` `applied_to_payment` → `actual`
    - `funding_reversed` after an `applied_to_payment` → a `refunded` row; the net position returns
      to nothing collected
    - `cash_balance.funds_available` → one `billingUnapplied` row and ZERO `billingEvents` rows
    - `charge.refunded` and `credit_note.created` → `refunded`, positive amount
    - `invoice.paid` delivered TWICE → exactly one `actual` row (both the event dedupe and the
      movement identity hold)
    - The full bank-transfer sequence delivered in REVERSE order produces the same final ledger state
    - `taxabilityReason` from the invoice is persisted on the row it belongs to
    - An event for an unknown Stripe customer takes plan 05's dead-letter path and writes NO ledger row
  </behavior>
  <action>
Add the money arms to the effect switch inside `billingWebhook.receiveAndApply` — still ONE
`internalMutation`, still one transaction. Call `reconcileEvent` (pure, plan 03) to get the
movements and observations, then `recordBillingMovement` for each movement and a `billingUnapplied`
insert for each observation. No `runAction`, no `fetch`, no awaited I/O inside the mutation.

Resolve the tenant through plan 05's `billingCustomers.by_customer`. No mapping → plan 05's refusal
path, and **no ledger row**.

Read the invoice tax breakdown against the API version pinned in plan 02: the field is
`total_tax_amounts[]` before `2025-03-31.basil` and `total_taxes[]` from Basil onward. Write the
reader against the pinned version and say which one in a comment, so a version bump is a visible
edit rather than a silent null.

Every handler must be **order-independent**. Stripe does not guarantee delivery order: a
`customer.subscription.created` can arrive after its `invoice.paid`, and a `funded` can arrive after
its `applied_to_payment`. Do not build a state machine that requires a sequence — derive the state
from the movements present.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billing convex/dashboardSchema.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Every row of the event→phase table is proven end to end through the real HTTP route; the bank-transfer `invoice.paid` negative case is green; reverse-order delivery reaches the same state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unapplied funds with their age, and the proof the spend plane is untouched</name>
  <files>packages/backend/convex/billing.ts, packages/backend/convex/billing.test.ts, docs/playbooks/billing.md</files>
  <behavior>
    - `unappliedFunds` (a `tenantQuery`) returns each unapplied amount WITH its age in days —
      amount alone is not the answer, because Stripe emails reminders, attempts to return
      unreconciled funds to the customer's bank at 75 days, and sweeps unreturnable funds to the
      account balance by 90
    - A tenant with no coverage gets an explicit unknown, never an empty list read as "nothing owed"
    - The result is bounded by a page limit; a huge backlog cannot return everything
    - **The spend plane is unchanged:** a test asserts `SPEND_RAILS` is exactly
      `["reasoning","media","ingest"]` against a written-out literal, and that
      `aggregateSpend` over a tenant that ALSO has `billingEvents` rows returns byte-identical
      totals to the same tenant without them
    - Every existing Phase 26 Finance test (`convex/finance.test.ts`, `packages/core` spend tests)
      is green UNCHANGED — not adapted, not re-baselined
  </behavior>
  <action>
Add `unappliedFunds` to `convex/billing.ts` as a `tenantQuery` over `billingUnapplied.by_tenant_observedAt`,
bounded, returning `{ amountMinor, currency, stripeObjectId, ageDays }` per row plus the coverage
state. Under Stripe's DEFAULT automatic reconciliation, `cash_balance.funds_available` fires only
when money is LEFT OVER after reconciliation — so this surface is "money we hold that is not
attached to anything", not "money arrived". Write that sentence as the doc comment; getting it
backwards is the exact trap the research flagged.

Then write the guard that protects the owner's ledger decision: a test in `billing.test.ts` that
seeds a tenant with BOTH `spendEvents` and `billingEvents` rows and asserts `aggregateSpend`'s output
is identical to the spend-only case. Assert `SPEND_RAILS` against the written-out literal array so a
future `revenue` rail is a RED test, not a silent corruption of Phase 26 Finance.

Update `docs/playbooks/billing.md`: a **Ledger** section stating that `billingEvents` is a separate
book from `spendEvents` and why (money-in must never be summed into money-out; `aggregateSpend`
hard-codes three rails in five places; Finance renders those totals today), the phase vocabulary
table, the `audit_immutable` classification and the two obligations it carries, the unapplied-funds
75/90-day clock, and the invariant list with each enforcing test named.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/billing convex/finance.test.ts convex/spendLedger.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../core &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Unapplied funds surface with age and a bounded read; `aggregateSpend` output is provably identical with and without billing rows; `finance.test.ts` and `spendLedger.test.ts` pass UNCHANGED.</done>
</task>

</tasks>

<verification>
1. `cd packages/backend && npx vitest run convex/billing convex/finance.test.ts convex/spendLedger.test.ts convex/env.test.ts && npx tsc --noEmit`
2. `cd packages/core && npx vitest run && npx tsc --noEmit`
3. `git diff HEAD -- packages/core/src/spend.ts packages/backend/convex/spendLedger.ts packages/backend/convex/finance.ts` must be EMPTY — the spend plane was not touched
4. `grep -n "revenue" packages/core/src/spend.ts` returns NOTHING — no fourth rail
5. Mutation check (rename class, not deletion): change the `applied_to_payment` literal in the webhook arm to `funded` and confirm the bank-transfer tests go RED, then restore
6. `node scripts/check-playbooks.mjs < /dev/null` — read STDOUT
</verification>

<success_criteria>
- Our ledger, not Stripe, holds the recorded outcome of every billing event
- `invoice.paid` never writes `actual` for a bank transfer; `applied_to_payment` is the collection signal
- `funding_reversed` reverses a recorded collection instead of being dropped
- Unapplied funds are visible with their age
- Missing history reads as unknown coverage, never as zero
- `aggregateSpend`, `spendEvents` and Phase 26 Finance are byte-identical and their tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-stripe-billing-invoicing-and-tax-for-pikar-s-own-merchant-account/28.1-06-SUMMARY.md`
</output>
