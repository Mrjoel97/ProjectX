---
phase: 02-thin-end-to-end-slice
plan: 09
type: execute
wave: 5
depends_on: ["02-07"]
files_modified:
  - apps/web/app/(app)/review/page.tsx
  - apps/web/app/(app)/review/[id]/page.tsx
  - apps/web/app/(app)/ops/page.tsx
  - apps/web/app/(app)/connect-gmail/page.tsx
  - apps/web/app/(app)/_components/ReconnectBanner.tsx
autonomous: false
requirements: [REVW-01, AGNT-02, OPSG-07, DLVR-03]
must_haves:
  truths:
    - "The review queue is a live list of requests awaiting review, not a single-request takeover"
    - "The gate shows route + read-only step plan + recipient + drafted email together, and offers Approve / Edit / Ask for changes / Reject"
    - "Approve delivers; Edit sends the edited text verbatim; Ask-for-changes regenerates (capped) and re-arms; Reject terminates with an optional reason"
    - "The ops page lists the tenant's new dead letters and can mark one resolved; a Reconnect-Gmail banner appears when a token is expiring or a request is awaiting_reauth"
  artifacts:
    - path: "apps/web/app/(app)/review/[id]/page.tsx"
      provides: "the collapsed review gate (route+plan+recipient+draft + 4 actions)"
      contains: "sendDecision"
    - path: "apps/web/app/(app)/ops/page.tsx"
      provides: "tenant dead-letter list + Mark resolved"
      contains: "markResolved"
    - path: "apps/web/app/(app)/connect-gmail/page.tsx"
      provides: "gmail.modify consent link + reconnect"
      contains: "gmail"
  key_links:
    - from: "apps/web/app/(app)/review/[id]/page.tsx"
      to: "review.sendDecision"
      via: "the 4 gate actions send the typed decision"
      pattern: "sendDecision"
    - from: "apps/web/app/(app)/ops/page.tsx"
      to: "deadLetters.listNew + markResolved"
      via: "useQuery list + mutation resolve"
      pattern: "listNew"
---

<objective>
Build the human-review and operator surface: the live review queue and the collapsed
review gate (route + read-only plan + recipient + draft, with Approve/Edit/Ask-for-changes/
Reject), the ops page (dead-letter list + Mark resolved), the /connect-gmail consent page,
and the reconnect banner that closes DLVR-03's loop.

Purpose: REVW-01's approve/edit/reject at the durable gate (AGNT-02 shows the plan
read-only before anything runs), OPSG-07's operator page, and the reactive half of DLVR-03
(awaiting_reauth → "Reconnect Gmail to send this", resuming the already-approved draft).

Output: review queue + gate, ops page, connect-gmail page, reconnect banner.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-thin-end-to-end-slice/02-CONTEXT.md
@.planning/phases/02-thin-end-to-end-slice/02-RESEARCH.md

<interfaces>
<!-- Backend from Waves 2-3: -->
requests.list (tenantQuery) — filter to status "awaiting_review" for the queue.
requests.get (tenantQuery) — the single request (route, steps[], recipient, draft) for the gate.
review.sendDecision (internalMutation) — decision union approve|edit_text|regenerate|reject +
  optional editedText/instruction/reason + `attempt`. NOTE: sendDecision is internal (it resumes a
  workflow); expose a THIN tenantMutation wrapper (in requests.ts or a small review-facing module) that
  validates tenant ownership of the correlationId, then calls it — do NOT call an internal mutation
  directly from the client. If that wrapper does not yet exist, add it here in a tenant-scoped module.
MAX_REGENERATE = 3 — the UI stops offering "Ask for changes" once the cap is hit (approve/edit/reject remain).
deadLetters.listNew + markResolved (tenantQuery/Mutation, plan 02-06).
gmailAuth.gmailStatus (tenantQuery) → { connected, expiresAt }; buildAuthorizeUrl link (plan 02-05).
notifications.list (tenantQuery) — the reconnect notice may surface here too.
The step plan is READ-ONLY at the gate (AGNT-02 requires the user sees it; editing is meaningless
with one actionable step). Recipient is re-displayed at the gate before approval (CONTEXT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review queue + collapsed review gate</name>
  <files>apps/web/app/(app)/review/page.tsx, apps/web/app/(app)/review/[id]/page.tsx</files>
  <action>
    `review/page.tsx`: a live queue — `useQuery(api.requests.list, { status: "awaiting_review" })`
    rendered as a real LIST (not a takeover screen — CONTEXT), each row linking to the gate.
    `review/[id]/page.tsx`: the collapsed gate showing route + the READ-ONLY step plan + recipient +
    the drafted email TOGETHER, with four distinct actions: `[Approve] [Edit] [Ask for changes]
    [Reject]`. Edit = inline subject/body editing sent verbatim (no second LLM call) via decision
    `edit_text` + editedText. Ask for changes = a free-text instruction → decision `regenerate`
    (the only looping action; hidden once MAX_REGENERATE reached). Reject = decision `reject` with an
    OPTIONAL free-text reason. Approve = decision `approve`. All four call the tenant-scoped
    sendDecision wrapper with the current `attempt`. Re-display the recipient prominently before
    approval (a hallucinated address is the phase's highest-severity failure — CONTEXT).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck && pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Queue is a live list; the gate shows route+plan(read-only)+recipient+draft and wires all four typed decisions with the attempt counter; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Ops page + connect-gmail + reconnect banner</name>
  <files>apps/web/app/(app)/ops/page.tsx, apps/web/app/(app)/connect-gmail/page.tsx, apps/web/app/(app)/_components/ReconnectBanner.tsx</files>
  <action>
    `ops/page.tsx`: `useQuery(api.deadLetters.listNew)` rendering the tenant's new dead letters with a
    "Mark resolved" button (`markResolved`). Tenant-scoped, not owner-gated (CONTEXT). Ships resolve
    ONLY — no replay control (deferred).
    `connect-gmail/page.tsx`: read `gmailStatus`; if not connected (or reconnecting), link to the
    gmail.modify authorize URL (buildAuthorizeUrl). Show connected + expiresAt when present.
    `ReconnectBanner.tsx`: a shell-level banner shown when `gmailStatus` is expiring soon OR any request
    is `awaiting_reauth`, reading "Reconnect Gmail to send this" and linking to /connect-gmail. On
    reconnect, the already-approved draft's delivery resumes (backend re-fires the delivery step) — the
    UI does NOT re-prompt for approval (CONTEXT). Render the banner in the app shell.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck && pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Ops page lists+resolves new dead letters; connect-gmail links the consent; reconnect banner appears on expiry/awaiting_reauth; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify approve → real Gmail delivery + reconnect (SC-3, DLVR-03)</name>
  <files>none — verification only</files>
  <action>Human verification of the end-to-end delivery + reconnect flows built across this phase. No code is written; follow the how-to-verify steps against a running dev deployment with a real Google test account.</action>
  <what-built>The review gate wired to sendDecision, the /connect-gmail consent flow, and the reconnect banner.</what-built>
  <how-to-verify>
    1. With convex dev + next dev running and signed in, open /connect-gmail and complete the
       gmail.modify consent (Testing mode — expect the "unverified app" warning; ≤100 test users).
    2. Submit a request, open its gate, confirm route + read-only plan + recipient + draft are shown,
       then Approve. Confirm the email ARRIVES in the target inbox and the request reaches `sent`.
    3. Exercise the loop: on another request use "Ask for changes" (regenerate) once, confirm the draft
       updates and the gate re-arms; then Reject one with a reason and confirm it terminates (no send).
    4. Revoke the token in the Google account console; confirm the request goes to `awaiting_reauth` and
       the Reconnect-Gmail banner appears (rather than a silent failure); reconnect and confirm delivery
       resumes without re-approving.
    These require a human at a Google consent screen and a real inbox (02-VALIDATION.md manual-only).
  </how-to-verify>
  <verify>Manual — human confirms consent, inbox receipt, the loop, and reconnect resume.</verify>
  <done>Consent completes; an approved draft arrives in the inbox and reaches `sent`; regenerate/reject behave; a revoked token surfaces awaiting_reauth + the banner and resumes on reconnect.</done>
  <resume-signal>Type "approved" or describe what failed (consent, delivery, loop, or reconnect).</resume-signal>
</task>

</tasks>

<verification>
- Live review queue + collapsed gate with 4 typed decisions; plan read-only (REVW-01, AGNT-02).
- Ops page lists/resolves new dead letters (OPSG-07).
- Reconnect banner + connect-gmail close DLVR-03's reactive loop; real delivery confirmed (SC-3).
</verification>

<success_criteria>
- `pnpm --filter @pikar/web typecheck && build` green.
- Human confirms consent → approve → inbox receipt, the regenerate/reject paths, and reconnect resume.
</success_criteria>

<output>
After completion, create `.planning/phases/02-thin-end-to-end-slice/02-09-SUMMARY.md`.
</output>
