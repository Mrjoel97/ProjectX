---
phase: 02-thin-end-to-end-slice
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/backend/convex/review.ts
  - packages/backend/convex/smoke.ts
  - packages/backend/scripts/run-smoke-reviewgate.mjs
  - packages/core/src/buildTelemetry.ts
  - packages/core/src/buildTelemetry.test.ts
  - packages/backend/convex/telemetry.ts
autonomous: true
requirements: [REVW-01, OPSG-01]
must_haves:
  truths:
    - "The review gate accepts a typed decision (approve | edit_text | regenerate | reject), not an untyped string"
    - "A regenerate decision re-arms the gate under a fresh attempt-suffixed event so a late event from a prior attempt cannot resume the next one"
    - "A real decision cancels the scheduled timeout via an indexed lookup, not a full-table scan"
    - "Exactly one telemetry row is built at a terminal state with every OPSG-01 field present or explicitly null"
  artifacts:
    - path: "packages/backend/convex/review.ts"
      provides: "4-member decision union + attempt-suffixed event + by_correlation indexed cancel"
      contains: "edit_text"
    - path: "packages/core/src/buildTelemetry.ts"
      provides: "pure terminal telemetry-row builder"
      contains: "reviewOutcome"
    - path: "packages/backend/convex/telemetry.ts"
      provides: "write-once internalMutation persisting the built row"
      contains: "internalMutation"
  key_links:
    - from: "packages/backend/convex/review.ts"
      to: "pendingTimeouts by_correlation index"
      via: "sendDecision withIndex lookup + scheduler.cancel"
      pattern: "by_correlation"
    - from: "packages/backend/convex/telemetry.ts"
      to: "buildTelemetry"
      via: "internalMutation inserts buildTelemetry(outcome)"
      pattern: "buildTelemetry"
---

<objective>
Extend the existing awaitEvent-timeout review gate for the real Phase-2 decision
model (REVW-01) and add write-once per-request telemetry (OPSG-01). Reuse
`convex/review.ts` — it already solves the hard issue-#177 race; make exactly the
three edits the CONTEXT/RESEARCH call for, and update the gate's own smoke so the
repo stays green.

Purpose: REVW-01's approve/edit/regenerate/reject at a durable gate, with the
regenerate loop made safe by attempt-suffixed event names; and a single telemetry
row written from the terminal outcome so it can never drift from it.

Output: extended `review.ts` (+ updated smoke), `buildTelemetry` (pure + tested),
`telemetry.ts`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-thin-end-to-end-slice/02-CONTEXT.md
@.planning/phases/02-thin-end-to-end-slice/02-RESEARCH.md
@packages/backend/convex/review.ts
@packages/backend/convex/smoke.ts
@packages/backend/scripts/run-smoke-reviewgate.mjs
@packages/core/src/index.ts

<interfaces>
<!-- review.ts today (reuse — do NOT rebuild): -->
reviewEventValidator = union({kind:"decision", decision: v.string()}, {kind:"timeout"})
armTimeout({ workflowId, correlationId, timeoutMs }) — inserts a pendingTimeouts row, schedules fireTimeout
fireTimeout({ workflowId, correlationId }) — sends timeout variant to `review:${correlationId}`
sendDecision({ correlationId, decision }) — full-scans pendingTimeouts, sends decision, cancels schedule
smoke.ts reviewGate + smoke:reviewgate script drive the OLD `review:${correlationId}` name.
pendingTimeouts now has a `by_correlation` index (added in plan 02-01).
MAX_REGENERATE = 3 (locked). The gate stops offering regenerate on breach; approve/edit/reject remain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the decision union + attempt-suffix the event + indexed cancel</name>
  <files>packages/backend/convex/review.ts, packages/backend/convex/smoke.ts, packages/backend/scripts/run-smoke-reviewgate.mjs</files>
  <action>
    Three edits to `review.ts` (RESEARCH Pattern 4), keeping BOTH defensive rules intact:
    1. Replace `reviewEventValidator`'s `decision: v.string()` with the 4-member union
       `v.union(v.literal("approve"), v.literal("edit_text"), v.literal("regenerate"), v.literal("reject"))`,
       plus optional `editedText`, `instruction`, `reason` fields on the decision variant.
       Update `sendDecision` args to match (decision union + optional fields).
    2. Add an `attempt: v.number()` param to `armTimeout`, `fireTimeout`, and `sendDecision`, and
       change every event name from `review:${correlationId}` to `review:${correlationId}:${attempt}`.
       Because the gate now loops on `regenerate`, a late event from attempt N must not be consumed
       by attempt N+1 (re-introduces the issue-#177 race). Persist `attempt` on the pendingTimeouts
       row so fireTimeout targets the right suffix.
    3. In `sendDecision`, replace the full `.collect()` scan with a `by_correlation` indexed lookup
       (`withIndex("by_correlation", q => q.eq("correlationId", correlationId))`) before
       `scheduler.cancel` — unbounded concurrent requests make the full scan a read-limit risk.
       Remove the now-obsolete `ponytail:` full-scan comment.
    Update `smoke.ts` `reviewGate` + `armTimeout` call and `run-smoke-reviewgate.mjs` to pass
    `attempt: 0` and await/send the suffixed name, so the existing smoke stays green. Keep
    `review.ts` and `smoke.ts` on the raw-builder allowlist (already there).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck && (cd packages/backend && node ./scripts/run-smoke-reviewgate.mjs)</automated>
  </verify>
  <done>Gate accepts the typed union; event names are attempt-suffixed; sendDecision cancels via the index; both smoke gates (decision + timeout) still pass against a running convex dev.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure telemetry builder + tests</name>
  <files>packages/core/src/buildTelemetry.ts, packages/core/src/buildTelemetry.test.ts</files>
  <behavior>
    - Given a terminal outcome (sent | rejected | expired | failed) + usage + counters, returns a row with ALL OPSG-01 fields: tokensIn, tokensOut, costUsd, durationMs, decisionCounts, regenerateCount, reviewOutcome — each present or explicitly null.
    - A rejected/expired outcome with no LLM usage still yields a complete row (nulls, not missing keys).
    - regenerateCount and decision counters are carried through from the outcome, not recomputed.
  </behavior>
  <action>
    Create `packages/core/src/buildTelemetry.ts`: pure `buildTelemetry(outcome)` returning the
    telemetry row shape (matching the `telemetry` table from plan 02-01). Domain logic, no Convex
    imports. Tokens/cost accumulate route + draft + each regenerate from the passed usage. Add
    `buildTelemetry.test.ts` (vitest) covering the behaviors above — especially the "all fields
    present or explicitly null" completeness that OPSG-01 requires.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core exec vitest run buildTelemetry</automated>
  </verify>
  <done>Builder yields a complete OPSG-01 row for every terminal outcome; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Write-once telemetry mutation</name>
  <files>packages/backend/convex/telemetry.ts</files>
  <action>
    Create `convex/telemetry.ts`: an internalMutation `writeTerminal({ requestId, correlationId,
    outcome })` that calls `buildTelemetry(outcome)` and inserts ONE `telemetry` row at the terminal
    transition. Written FROM the outcome — no incremental patching (avoids OCC contention on a hot row
    and half-written rows that look complete — CONTEXT). The pipeline (plan 02-06) calls this at every
    terminal state (sent | rejected | expired | failed). Add `telemetry.ts` to the raw-builder allowlist
    if it uses internal builders.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck && pnpm --filter @pikar/backend test importGuard</automated>
  </verify>
  <done>writeTerminal inserts exactly one row per request; typecheck + importGuard green.</done>
</task>

</tasks>

<verification>
- Decision union replaces the untyped string; regenerate loop is attempt-safe (REVW-01).
- sendDecision cancels via the by_correlation index (no full scan).
- One telemetry row, all OPSG-01 fields present-or-null, written once (OPSG-01).
</verification>

<success_criteria>
- `run-smoke-reviewgate.mjs` passes; `vitest run buildTelemetry` green; typecheck + importGuard green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-thin-end-to-end-slice/02-04-SUMMARY.md`.
</output>
