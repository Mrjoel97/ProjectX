---
phase: 02-thin-end-to-end-slice
plan: 07
type: execute
wave: 4
depends_on: ["02-06"]
files_modified:
  - packages/backend/convex/auth.ts
  - apps/web/middleware.ts
  - apps/web/app/layout.tsx
  - apps/web/app/providers.tsx
  - apps/web/app/(app)/layout.tsx
  - apps/web/app/(app)/page.tsx
  - apps/web/app/signin/page.tsx
  - apps/web/app/privacy/page.tsx
  - apps/web/package.json
autonomous: true
requirements: [OPSG-07, BETA-04]
must_haves:
  truths:
    - "A user signs in with Google (openid email profile) and unauthenticated app routes redirect to sign-in"
    - "The authenticated app shell shows navigation and a persistent red dead-letter badge whose count comes from a live query and does not clear until resolved"
    - "The privacy policy lists Vercel (AI Gateway) and OpenAI as active processors"
  artifacts:
    - path: "packages/backend/convex/auth.ts"
      provides: "Google sign-in provider (replaces Password)"
      contains: "Google"
    - path: "apps/web/middleware.ts"
      provides: "Convex Auth route gating"
      contains: "convexAuthNextjsMiddleware"
    - path: "apps/web/app/(app)/layout.tsx"
      provides: "authenticated app shell + nav + ops badge"
      contains: "useQuery"
  key_links:
    - from: "apps/web/app/(app)/layout.tsx"
      to: "deadLetters.newCount"
      via: "useQuery drives the persistent badge"
      pattern: "newCount"
---

<objective>
Stand up the authenticated surface: swap Convex Auth to the Google sign-in provider
(reusing the Google Cloud client the Gmail flow needs), wire the Next.js auth
middleware + server provider, build the app shell with navigation and the persistent
dead-letter badge (OPSG-07's operator visibility), and add the two new processors to
the privacy policy.

Purpose: nothing user-facing exists yet (only privacy/ and terms/); the whole phase
needs sign-in and a shell. The persistent badge is OPSG-07's "a failure nobody sees is
a failure nobody fixes" — a live count that does not clear until resolved.

Output: Google auth, middleware, app shell + badge, sign-in page, privacy edit.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-thin-end-to-end-slice/02-CONTEXT.md
@.planning/phases/02-thin-end-to-end-slice/02-RESEARCH.md
@apps/web/app/layout.tsx
@apps/web/app/providers.tsx
@packages/backend/convex/auth.ts
@apps/web/app/privacy/page.tsx

<interfaces>
<!-- Convex Auth Next.js integration (Research): -->
convex/auth.ts: swap `Password` for `Google({ authorization: { params: { scope: "openid email profile" }}})`.
Env: AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET.
Next side: ConvexAuthNextjsServerProvider in layout, convexAuthNextjsMiddleware + createRouteMatcher
in middleware.ts to gate app routes. @convex-dev/auth 0.0.94 is pinned — do not bump.
providers.tsx currently uses ConvexProvider — switch to the Convex Auth Next.js client provider.
Backend query for the badge: deadLetters.newCount (tenantQuery, plan 02-06).
NEXT_PUBLIC_CONVEX_URL is read at module scope (see providers.tsx comment).
/connect-gmail and the submit/queue/ops pages are added by plans 02-08/02-09 — the shell nav
links to them; the routes themselves are auth-gated (not public → NOT added to sitemap.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Google sign-in + Next.js auth wiring</name>
  <files>packages/backend/convex/auth.ts, apps/web/middleware.ts, apps/web/app/layout.tsx, apps/web/app/providers.tsx, apps/web/app/signin/page.tsx, apps/web/package.json</files>
  <action>
    Backend: in `convex/auth.ts` replace the `Password` provider with `Google` requesting only
    `openid email profile` (the restricted gmail.modify scope stays OUT of the front door — that is
    the separate /connect-gmail flow from plan 02-05). Keep `auth.ts` on the raw-builder allowlist.

    Web: add `@convex-dev/auth` to apps/web deps (pinned 0.0.94). Wrap `providers.tsx` with the Convex
    Auth Next.js client provider and `layout.tsx` with `ConvexAuthNextjsServerProvider`. Create
    `middleware.ts` with `convexAuthNextjsMiddleware` + `createRouteMatcher` gating the `(app)` route
    group (unauthenticated → redirect to `/signin`); leave `/`, `/privacy`, `/terms`, `/signin` public.
    Create `app/signin/page.tsx` with a "Sign in with Google" button calling the auth signIn.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck && pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Google provider replaces Password; middleware gates app routes; sign-in page builds; web typecheck + build green.</done>
</task>

<task type="auto">
  <name>Task 2: App shell + persistent dead-letter badge</name>
  <files>apps/web/app/(app)/layout.tsx, apps/web/app/(app)/page.tsx</files>
  <action>
    Create the authenticated `(app)` route group: `layout.tsx` = the app shell (nav links to
    Submit, Requests, Review queue, Ops, Connect Gmail — some targets land in 02-08/09; render the
    links now) plus a persistent ops badge in the shell chrome — a red count from `useQuery(
    api.deadLetters.newCount)` that renders whenever count > 0 and does NOT auto-clear (modeled on an
    unread-mail badge, not a toast — CONTEXT). `page.tsx` = a simple authenticated landing/dashboard.
    Layout/visual design is Claude's discretion. Loading/empty states are Claude's discretion.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck && pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Shell renders nav + a live red badge bound to deadLetters.newCount; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Privacy processor list edit</name>
  <files>apps/web/app/privacy/page.tsx</files>
  <action>
    Add Vercel (AI Gateway) and OpenAI to the privacy policy's processor list — the pipeline now sends
    user content to both, and a policy that omits an active processor is defective (CONTEXT). Text edit
    only. Do NOT touch sitemap.ts (no new PUBLIC page; /connect-gmail is auth-gated).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Privacy page names Vercel AI Gateway + OpenAI as processors; build green.</done>
</task>

</tasks>

<verification>
- Google sign-in gates the app (BETA-04 prerequisite; INTK-04 identity).
- Persistent badge bound to a live deadLetters query (OPSG-07).
- Privacy policy processor list current.
</verification>

<success_criteria>
- `pnpm --filter @pikar/web typecheck && build` green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-thin-end-to-end-slice/02-07-SUMMARY.md`.
</output>
