---
phase: 02-thin-end-to-end-slice
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/contracts/src/routing.ts
  - packages/contracts/src/routing.test.ts
  - packages/contracts/skills/executive-router.md
  - packages/contracts/skills/email-drafter.md
  - packages/contracts/src/skills/executiveRouter.ts
  - packages/contracts/src/skills/emailDrafter.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/convex/llm.ts
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03]
must_haves:
  truths:
    - "A valid model routing object (route + steps + rationale) parses; an unknown route value is rejected rather than defaulted"
    - "The Executive Agent produces a routing decision and a step list before anything runs"
    - "Router and drafter prompts load from the skills registry — no prompt is hardcoded in source"
  artifacts:
    - path: "packages/contracts/src/routing.ts"
      provides: "routingDecision Zod schema (enum route + steps[] + rationale) + parse helper returning a discriminated result"
      contains: "z.enum"
    - path: "packages/backend/convex/llm.ts"
      provides: '"use node" internalActions route + draft via generateObject through the AI Gateway'
      contains: "use node"
    - path: "packages/contracts/src/skills/executiveRouter.ts"
      provides: "executive-router + email-drafter seed bodies as derived .ts constants (contracts convention)"
      contains: "export"
  key_links:
    - from: "packages/backend/convex/llm.ts"
      to: "skills registry"
      via: "load active skill body via ctx.runQuery before generateObject"
      pattern: "runQuery"
    - from: "packages/backend/convex/llm.ts"
      to: "packages/contracts/src/routing.ts"
      via: "generateObject schema = routingSchema"
      pattern: "routingSchema"
---

<objective>
Build the Executive Agent's LLM surface: a schema-constrained routing decision
(AGNT-01/03), a model-generated step plan (AGNT-02), and the two-call route→draft
action reached through the Vercel AI Gateway — with both prompts loaded from the
skills registry (CLAUDE.md §5).

Purpose: put AGNT-03's "never a silent default" guarantee in the type system (a
parse failure or `sub_agent`/unknown value is surfaced, not defaulted) and give the
pipeline (plan 02-06) its `route` and `draft` steps.

Output: the `routingDecision` contract + tests, two seeded skills, and `convex/llm.ts`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-thin-end-to-end-slice/02-CONTEXT.md
@.planning/phases/02-thin-end-to-end-slice/02-RESEARCH.md
@packages/backend/convex/skills.ts
@packages/contracts/src/index.ts

<interfaces>
<!-- Existing skills registry contract (convex/skills.ts). Reuse it — do not rebuild. -->
Skills are immutable per version; seed bodies ship as derived .ts constants kept in
sync with a canonical .md by a vitest assertion (Phase-1 pattern). There is a loader
that returns the active body for a skill name, and `activateSkill` for rollback.
Read skills.ts to find the exact exported loader/seed function names and reuse them.

AI Gateway routing (Research): `model: "openai/gpt-4o-mini"` passed to `generateObject`
routes through the Vercel AI Gateway when `AI_GATEWAY_API_KEY` is set — NO provider
package import. `generateObject` returns `{ object, usage }`; `usage` feeds telemetry.

The LLM module MUST be `"use node"` and contain ONLY actions (repo rule from 01-07);
DB reads go through `ctx.runQuery`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: routingDecision contract + tests</name>
  <files>packages/contracts/src/routing.ts, packages/contracts/src/routing.test.ts</files>
  <behavior>
    - A well-formed object { route: "direct_llm", steps: [{n,description}], rationale } parses ok.
    - route "direct_tool" and "sub_agent" are recognized enum members (sub_agent is valid but unimplemented downstream).
    - An unknown route value ("foo") FAILS parse — the helper returns a failure result, never a defaulted route.
    - Missing steps or malformed shape fails parse.
  </behavior>
  <action>
    Create `packages/contracts/src/routing.ts`: a Zod `routingSchema =
    z.object({ route: z.enum(["direct_llm","direct_tool","sub_agent"]),
    steps: z.array(z.object({ n: z.number(), description: z.string() })).min(1),
    rationale: z.string() })`. Export a `parseRouting(value): { ok: true; value } |
    { ok: false; reason: "unknown_route" }` helper wrapping `.safeParse` — the discriminated
    failure carries `unknown_route` so the pipeline dead-letters with a distinct reason
    (AGNT-03), never a default. Export the inferred `RoutingDecision` type. Field shapes
    are Claude's discretion (CONTEXT) — keep them minimal. Add `routing.test.ts` (vitest)
    covering the behaviors above.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts exec vitest run routing</automated>
  </verify>
  <done>routing.test passes: valid parses, unknown route rejected as `unknown_route`, malformed rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Seed executive-router + email-drafter skills</name>
  <files>packages/contracts/skills/executive-router.md, packages/contracts/skills/email-drafter.md, packages/contracts/src/skills/executiveRouter.ts, packages/contracts/src/skills/emailDrafter.ts, packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <action>
    Two skill-registry rows, NOT one (CONTEXT: routing and drafting have different failure
    modes and Phase-8 SkillOpt optimizes them against different evidence). Author canonical
    `.md` bodies:
    - `executive-router.md` — instructs the model to classify the goal and emit the routing
      decision + step plan matching `routingSchema` (route, steps[], rationale). In Phase 2
      the step plan usually reads "1. Draft email  2. Send via Gmail".
    - `email-drafter.md` — instructs the model to draft a subject + plain-text body for the
      user's goal. Recipient is supplied structurally (never model-derived — CONTEXT).
    Follow the EXISTING Phase-1 convention verbatim (precedent: `executive-agent.classifier.md`
    + `executiveAgentClassifier.ts`, imported into `convex/skills.ts` as
    `@pikar/contracts/skills/executiveAgentClassifier`):
    - canonical `.md` sources → `packages/contracts/skills/executive-router.md` and
      `email-drafter.md` (domain content lives in the pure `contracts` package, CLAUDE.md §1 —
      NOT the backend adapter layer).
    - derived `.ts` body constants → `packages/contracts/src/skills/executiveRouter.ts` and
      `emailDrafter.ts` (Convex cannot fs.read repo files), imported into `convex/skills.ts`.
    - EXTEND the existing `packages/backend/convex/skills.test.ts` drift guard (the same
      mechanism it already uses for the classifier) with an assertion that each derived constant
      equals its canonical `.md`.
    Wire the seed path through the existing skills insert surface in skills.ts — reuse it, add no
    mutating audit or new registry mechanics.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test skills</automated>
  </verify>
  <done>Both skills seed via the existing registry; the .ts↔.md drift assertion passes; skills remain immutable-per-version (no new mutating surface added).</done>
</task>

<task type="auto">
  <name>Task 3: LLM route + draft action (AI Gateway)</name>
  <files>packages/backend/convex/llm.ts</files>
  <action>
    Create `convex/llm.ts` starting with `"use node"`, containing ONLY internalActions:
    - `route({ requestId })`: read the request goal via `ctx.runQuery`, load the
      `executive-router` active body via the skills loader (`ctx.runQuery`), call
      `generateObject({ model: MODEL, schema: routingSchema, system: routerBody, prompt: goal })`.
      Run `parseRouting` on the object; on failure throw `Error("unknown_route")`. Return
      `{ routing, usage }`. Add a `ponytail:` comment at the prompt site: unredacted user text
      in P2; a `redact` step slots in ahead of this call at GRDL-01 (Phase 3). Do NOT hand-roll
      a partial redactor.
    - `draft({ requestId })`: load `email-drafter` body, call `generateObject` (or generateText)
      to produce `{ subject, body }`; return `{ subject, body, usage }`.
    Use a single `MODEL` string constant (e.g. `"openai/gpt-4o-mini"`) so GRDL-03/05 (Phase 3)
    can vary it (Research Open Q1). Do NOT import `@ai-sdk/openai` or `@convex-dev/agent`
    (CONTEXT locked). `usage` (inputTokens/outputTokens — confirm field names against the
    installed `ai@7.0.20`) is returned for telemetry. Add `llm.ts` to the raw-builder allowlist
    only if it needs internal builders (it uses internalAction from _generated/server — allowed).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>llm.ts typechecks; route throws `unknown_route` on parse failure; both actions return usage. Runtime behavior is exercised by the pipeline smoke in plan 02-06.</done>
</task>

</tasks>

<verification>
- routing contract enforces the enum and rejects unknown routes (AGNT-01/03).
- Step plan shape (steps[]) present in the schema (AGNT-02).
- Both prompts load from the registry; no hardcoded prompt in llm.ts (CLAUDE.md §5).
</verification>

<success_criteria>
- `vitest run routing` + `test skills` green.
- `pnpm --filter @pikar/backend typecheck` green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-thin-end-to-end-slice/02-02-SUMMARY.md`.
</output>
