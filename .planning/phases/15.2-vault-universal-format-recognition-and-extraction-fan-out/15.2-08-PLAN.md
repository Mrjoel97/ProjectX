---
phase: 15.2-vault-universal-format-recognition-and-extraction-fan-out
plan: 08
type: execute
wave: 8
depends_on: ["15.2-07"]
files_modified:
  - packages/vault/src/officeText.ts
  - packages/vault/src/officeText.test.ts
  - packages/vault/src/rawText.ts
  - docs/playbooks/vault.md
  - .planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-VALIDATION.md
autonomous: false
requirements: [SC#2, SC#4]

must_haves:
  truths:
    - "A KPI deck whose numbers live in CHARTS extracts those numbers, not only its slide titles"
    - "A SmartArt agenda block extracts ONCE, not twice (data1.xml and drawing1.xml are the same content)"
    - "Master / layout boilerplate ('Click to edit Master title style') never appears in an extracted deck"
    - "A ZIP office document with no readable text anywhere reports `empty_extraction` with its plain-language remedy — NEVER `ready`"
    - "The owner's `Rejection Review- Mar 2026.pptx` reads its rejection figures, and the Entities panel is no longer empty"
  artifacts:
    - path: "packages/vault/src/officeText.ts"
      provides: "labelled() — a structural header is emitted ONLY for a part that yielded content; pptxText walks charts/diagrams/notes via the slide's own rels"
      contains: "function labelled("
    - path: "packages/vault/src/officeText.test.ts"
      provides: "scaffolding-only ⇒ \"\" for BOTH xlsx and pptx; chart/SmartArt/notes fan-out; the negative list (layouts/masters/drawing) pinned"
    - path: "docs/playbooks/vault.md"
      provides: "the false-ready FAMILY rule + the PPTX entry list (positive AND negative) + the measured ceilings"
      contains: "15.2-08"
  key_links:
    - from: "packages/vault/src/officeText.ts pptxText"
      to: "ppt/slides/_rels/slideN.xml.rels"
      via: "Target= resolution, so a chart's values land under the slide that shows them"
      pattern: "_rels/slide"
    - from: "packages/vault/src/officeText.ts"
      to: "packages/backend/convex/vaultExtract.ts:392 empty_extraction"
      via: "returning \"\" for a text-free archive, so the EXISTING guard fires — no second failure mechanism"
      pattern: "labelled\\("
---

<objective>
Close the last instance of the false-ready family, and read the entries that were already in the
archive.

**HALF B (the important half) — the FAMILY, not the deck.** `xlsxText` (`officeText.ts:65`) and
`pptxText` (`:74`) emit `` `Sheet ${n}` `` / `` `Slide ${n}` `` **unconditionally**. Output that is
100% scaffolding and 0% content is therefore non-empty, `vaultExtract.ts:392`'s `empty_extraction`
guard never fires, and the row reports **`ready`**. A password-protected or chart-only `.xlsx` lands
as a ready document containing nothing but `Sheet 1 / Sheet 2 / Sheet 3`, and the agent then grounds
confidently on scaffolding. Per CLAUDE.md §8 this is a shared-shape bug: **fix the family once.**

**HALF A — routing, not parsing.** `pptxText` reads `<a:t>` from `ppt/slides/slideN.xml` and nothing
else. The owner's KPI deck reached `ready` with **356 characters** — slide titles only — and the
Entities panel correctly said "No entities extracted for this document." because `extractGraph` was
handed 356 chars of headings. **The graph plane is fine; the parser starved it.** The numbers live in
`ppt/charts/chart*.xml` and the agenda in `ppt/diagrams/data1.xml` — entries already present in the
same `unzipSync` result the function is holding, exactly like 15.2-02's ZIP-entry dispatch.

Purpose: this bug shape has now been found FIVE times in this phase. The rule the phase converged on
and that this plan closes the last instance of: **never let a non-empty string stand in for "we got
content."** The success signal is a COUNT of parts that yielded content (`okPages` in 15.2-06,
`okSheets` in 15.2-07, `parts.length` here).

Output: `packages/vault/src/officeText.ts` + its tests, the playbook, and the VALIDATION gap entry
that 15.2-05/06/07 each left open. **Zero `convex/` edits, zero new dependencies, `officeText.ts`
still absent from the barrel.**
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-CONTEXT.md
@.planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-05-SUMMARY.md
@.planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-06-SUMMARY.md
@.planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-07-SUMMARY.md
@packages/vault/src/officeText.ts
@packages/vault/src/officeText.test.ts
@packages/vault/src/xlsText.ts
@packages/vault/src/rawText.ts

<interfaces>
<!-- Read off the codebase and off the OWNER'S REAL DECK. Reuse these; do not write second versions. -->

**The two false-ready sites, verbatim:**
```typescript
// officeText.ts:65 — xlsxText, inside the per-sheet map
return [`Sheet ${n}`, ...rows].join("\n");
// officeText.ts:74 — pptxText, inside the per-slide map
.map(({ xml, n }) => [`Slide ${n}`, ...runsOf(xml, "a:t")].join("\n"))
```

**The guard they defeat** (`packages/backend/convex/vaultExtract.ts:392`, do NOT edit it):
```typescript
if (extracted.text.trim().length === 0) { await fail("empty_extraction"); return null; }
```
It **trims** — so a whitespace-only result already fails honestly. `Sheet 1\nSheet 2` does not.

**The user-facing copy that already exists** (`apps/web/app/(app)/dashboard/vault/failureCopy.ts:80`):
```typescript
empty_extraction: {
  title: "We opened this file but found no readable text.",
  remedy: "If it's a scan or a photo, try a clearer or higher-resolution version.",
}
```
This is why the fix **returns `""` rather than throwing**: `office_parse_failed` means *the archive is
broken*, which is a lie about a perfectly valid chart-only deck. `empty_extraction` means *we opened
it and there was nothing to read*, which is true, is already wired, and already has a remedy. Adding
a throw would be a second failure mechanism for a case the existing one already describes correctly
(the 15.2-06 rung-2 decision).

**The idiom to reuse** (`xlsText.ts`, 15.2-07):
```typescript
let okSheets = 0;
// ... per part: if (body.length === 0) return;  okSheets++;  parts.push(`Sheet ${i + 1}\n${body}`);
if (okSheets === 0) throw new Error("xls_parse_failed: no cell values");
```
Its header is emitted INSIDE the loop, only for a sheet with content. Its source comment already
names `officeText.ts:65/:74` as the family this plan closes.

**Helpers already in `officeText.ts` — reuse, do not re-write (ladder rung 2):**
```typescript
function rawRunsOf(xml: string, tag: string): string[];   // :14 undecoded
function runsOf(xml: string, tag: string): string[];      // :21 decoded, attribute-tolerant
function numericSorted(entries, re: RegExp): { xml, n }[];// :34 numeric entry ordering
```

**MEASURED — the owner's actual deck**, `C:/Users/expert/Desktop/Rejection Review- Mar 2026.pptx`
(same file as row `mx7a40n7460cj3d1ww97bms6wd8bb9zg`: its first two slides match the stored 356-char
text byte for byte). 81 entries. This is measurement, not inference — re-measure if you doubt it:

| entry group | count | `<a:t>` | `<c:v>` | verdict |
|---|---|---|---|---|
| `ppt/slides/slideN.xml` | 7 | 11 total | — | **READ** (already read today = the 356 chars) |
| `ppt/charts/chartN.xml` | 3 | 0 | **49** | **READ** — the KPI numbers live here |
| `ppt/diagrams/data1.xml` | 1 | 8 | — | **READ** — the Objective/Agenda prose |
| `ppt/diagrams/drawing1.xml` | 1 | 8 | — | **DO NOT READ — byte-identical content to data1.xml** |
| `ppt/notesSlides/notesSlideN.xml` | 3 | 1 each | — | READ, but each is only an `<a:fld>` slide number ("1","2","3") |
| `ppt/slideLayouts/*` (11) + `slideMasters/*` + `notesMasters/*` + `handoutMasters/*` | 15 | ~110 | — | **DO NOT READ — "Click to edit Master title style" ×N + the deck title repeated on every layout** |
| `ppt/media/image{1.png,2.wmf,3.wmf}` | 3 | — | — | unreadable by any text walker (slides 6–7 are picture exports) |
| `ppt/embeddings/*` | **0 — the directory does not exist** | | | charts use `externalData` → `file:///C:\Users\Zainab Bottlers\Desktop\…xlsx`, a path on the AUTHOR'S machine |

Slide → part attachment, read from `ppt/slides/_rels/slideN.xml.rels` (`Target="../charts/chart1.xml"`):
`slide2 → data1 + drawing1 + notesSlide2`, `slide3 → chart1`, `slide4 → chart2`, `slide5 → chart3`,
`slide6/7 → media only`.

**MEASURED PREDICTION.** A prototype of the shape below, run against that real file, produced
**1,181 characters (356 → 1,181, 3.3×)**, with:
- `Slide 3` carrying `Rej %  46023  46054  46082  4.7100000000000003E-2 …`
- `Slide 4` / `Slide 5` carrying `ZBIJ / ZBBW / ZBFL` × `Jan-26 / Feb-26 / Mar-26` × values
- `Slide 2` carrying the Objective/Agenda block **once**
- `Slide 6` / `Slide 7` still title-only — the picture-slide ceiling, unchanged and unfixable here
- notes contributing **nothing**, because the `<a:fld>` strip removes the slide-number field

Treat 1,181 as the expectation, not a guarantee: if the executor's result differs materially, that is
a finding to report, not a number to bend the code toward.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A structural header only ever labels a part that yielded content (the FAMILY — SC#4)</name>
  <files>packages/vault/src/officeText.ts, packages/vault/src/officeText.test.ts</files>
  <behavior>
Extend `officeText.test.ts`. Same conventions as the file already uses: `zipSync`/`strToU8`,
`xlsxOf(...)` / `pptxOf(...)` helpers, **no binary fixture in the repo**, no MIME argument anywhere.

**Existing expectations must stay green BYTE-FOR-BYTE.** In particular
`"Sheet 1\nQ3 revenue"`, `"Sheet 2\ntwo\n\nSheet 10\nten"`, `"Sheet 1\n7\t8"` and
`"Slide 1\nTitle\nSubtitle & more\n\nSlide 2\nBody\n\nSlide 10\nLast"` must not be edited. If one of
them needs editing to pass, **stop and report it** — that means the gate is rewriting bodies instead
of testing them.

New cases (the false-ready family):
- **XLSX, scaffolding only:** a workbook with `xl/workbook.xml` + two `sheet1/sheet2.xml` that carry
  `<row>`s of value-less `<c/>` cells (or no `<row>` at all) → `extractOfficeText(bytes).text` is
  **`""`**. Assert `=== ""`, not `.trim() === ""` — a "Sheet 1\nSheet 2" result is what ships today.
- **XLSX, mixed:** sheet1 empty, sheet2 has `7\t8` → the output is **`"Sheet 2\n7\t8"`** with **no
  `Sheet 1` header anywhere**. This is the load-bearing case: the surviving sheet keeps its OWN
  number (2), it is not renumbered to 1. Assert `.not.toContain("Sheet 1")`.
- **PPTX, scaffolding only:** `ppt/presentation.xml` + two slides whose only `<a:t>` is inside an
  `<a:fld …>` slide-number field (and a `ppt/slideLayouts/slideLayout1.xml` full of "Click to edit
  Master title style") → **`""`**.
- **PPTX, whitespace-only run:** a slide whose single `<a:t xml:space="preserve">   </a:t>` is blank
  → that slide contributes nothing and emits no header.
- **PPTX, mixed:** slide1 blank, slide2 has text → `"Slide 2\n…"`, and `.not.toContain("Slide 1")`.
- **The structural throws are UNCHANGED and still reachable:** `office_parse_failed: no worksheets`
  (an `xl/workbook.xml` with no `xl/worksheets/sheetN.xml`) and `office_parse_failed: no slides`.
  Pin both again here so nobody "simplifies" them into the new empty path — *no sheets at all* is a
  broken archive; *sheets with nothing in them* is an empty document. Different facts, different
  endings.
- **Static scan** (the 15.2-07 precedent — strip line comments first, assert against CODE, because
  this file's comments will name the banned form): every occurrence of a part-label template literal
  (`` `Sheet ${ ``, `` `Slide ${ ``) in `officeText.ts` appears as an argument to `labelled(`. This is
  the durable lock; the behaviour tests are the proof.
  </behavior>
  <action>
**First, the audit (CLAUDE.md §8 — grep every caller, fix the shared shape once).** Read
`packages/vault/src/officeText.ts` and `packages/vault/src/rawText.ts` end to end and list EVERY site
that puts a literal into the output unconditionally. Do not assume the two known sites are all of
them. Record the result in the SUMMARY either way — "audited, found exactly two" is a finding. For
reference, the expected disposition (verify it, do not trust it):
`docxText` / `odfText` / `markupText` return `""` honestly; `rtfText` / `oleText` throw;
`epubText` joins with `"\n\n"` and can therefore return whitespace-only — which the downstream guard
**trims**, so it fails honestly today. If you find a third real instance, fix it in this task and say
so; if `epubText` is cheaper to make explicit than to reason about, that is inside your latitude.

Then add ONE helper and route both walkers through it:

```typescript
/**
 * A structural header (`Sheet 3`, `Slide 7`) is SCAFFOLDING. Emitted unconditionally it makes an
 * empty extraction non-empty, so `empty_extraction` never fires and a document with nothing in it
 * reports `ready` — a plausible failure, which is worse than a failure. The label is therefore
 * bound to the body: no body, no label, no part. The success signal downstream is the COUNT of
 * surviving parts, never the truthiness of the joined string (`okPages` 15.2-06, `okSheets` 15.2-07).
 */
function labelled(label: string, body: string): string | null {
  return body.trim().length === 0 ? null : `${label}\n${body}`;
}
```

`trim()` is the **test**, never a rewrite: the returned body is the original string, so every
existing expectation stays byte-identical (a sheet whose first row is blank still renders its blank
row). Do not be tempted to return the trimmed body.

- `xlsxText`: build `rows` as today, then `labelled(\`Sheet ${n}\`, rows.join("\n"))`, drop the
  `null`s, `join("\n\n")`. Keep the `no worksheets` throw.
- `pptxText`: same shape. Filter out runs whose `.trim()` is empty **but push the run untrimmed**
  (minimal diff — the existing expectations contain no whitespace-only run, and a whitespace-only run
  must not be able to hold a header up on its own). Keep the `no slides` throw.
- Both return `""` when nothing survived. **Do not throw.** One line at the dispatcher or on
  `labelled` saying why: the archive parsed fine, so `office_parse_failed` would be a lie;
  `vaultExtract.ts:392` already turns `""` into `empty_extraction`, which is the accurate reason and
  already carries user-facing copy.

Do NOT touch `vaultExtract.ts`, `xlsText.ts`, the barrel, or `package.json`. No new dependency.

**Mutation check (house standard — confirm the mutation actually applied before trusting the
result, then revert):** make `labelled` return `` `${label}\n${body}` `` unconditionally. Expect the
scaffolding-only and mixed rows to go RED, and the static scan to stay GREEN (that asymmetry is worth
recording: the scan locks the SHAPE, the behaviour tests lock the RULE — neither alone is enough).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test -- officeText &amp;&amp; pnpm --filter @pikar/vault typecheck</automated>
  </verify>
  <done>`labelled()` exists and is the only path by which a `Sheet N`/`Slide N` header reaches the output; a scaffolding-only XLSX and a scaffolding-only PPTX each return `""` (asserted `=== ""`); a mixed workbook/deck emits ONLY the surviving part with its own original number; both `no worksheets`/`no slides` throws still fire; every pre-existing expectation in `officeText.test.ts` is unedited and green; the static scan is mutation-confirmed; `officeText.ts` and `rawText.ts` are audited with the result written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read the entries already in the archive — charts, SmartArt and notes (SC#2)</name>
  <files>packages/vault/src/officeText.ts, packages/vault/src/officeText.test.ts</files>
  <behavior>
All fixtures built with `pptxOf({...})` + `strToU8`. The entry names and the shapes below are taken
from the owner's real deck (see `<interfaces>`) — they are not invented.

- **Chart values reach the slide that shows them.** `slide3.xml` with one `<a:t>Trend 2026</a:t>`,
  `ppt/slides/_rels/slide3.xml.rels` containing `Target="../charts/chart1.xml"`, and
  `ppt/charts/chart1.xml` with one `<c:ser>` holding `<c:v>Rej %</c:v>`, three category `<c:v>`s and
  three value `<c:v>`s → the output's `Slide 3` part contains `Trend 2026` AND a **tab-joined**
  series line carrying all seven values in document order.
- **Multiple series stay separate:** a chart with three `<c:ser>` blocks yields **three** lines, one
  per series — not one 21-value line. (Real shape: `ZBIJ / ZBBW / ZBFL`.)
- **Chart titles:** `<a:t>` runs inside `chartN.xml` appear before the series lines.
- **SmartArt reads ONCE.** `slide2.xml.rels` targeting BOTH `../diagrams/data1.xml` and
  `../diagrams/drawing1.xml`, both carrying the identical eight `<a:t>` runs → each phrase appears
  **exactly once** in the output. Assert with a count (`text.split("Agenda:").length - 1 === 1`), not
  with `toContain` — `toContain` passes on duplicated text and this is the measured hazard.
- **Speaker notes** attached via `../notesSlides/notesSlide1.xml` appear under their slide, **but** a
  notes slide whose only `<a:t>` sits inside `<a:fld type="slidenum">…</a:fld>` contributes nothing
  and (with Task 1's gate) causes no empty header. Both cases in one fixture.
- **THE NEGATIVE LIST — a regression test, not a nicety.** A deck carrying
  `ppt/slideLayouts/slideLayout1.xml`, `ppt/slideMasters/slideMaster1.xml`,
  `ppt/notesMasters/notesMaster1.xml` and `ppt/handoutMasters/handoutMaster1.xml`, each stuffed with
  `<a:t>Click to edit Master title style</a:t>` and a footer `<a:t>Monthly Rejection Overview</a:t>`
  → **none of that text appears in the output**. Measured: the owner's deck has 15 such parts holding
  ~110 runs of boilerplate, so a naive "read every `<a:t>` in the archive" would inject the deck title
  22 times and bury the real content.
- **Orphans are not silently dropped.** A `chart9.xml` referenced by NO slide's rels still appears as
  its own labelled part after the slides. (Coverage by construction — a rels file that fails to parse
  must degrade to "the numbers are present but unattached", never back to titles-only.)
- **Determinism:** two calls on the same bytes are byte-identical (the existing per-format
  determinism convention).
- **Regression:** a deck with slides and no rels files at all still extracts exactly as it does
  today.
  </behavior>
  <action>
Extend `pptxText` in `packages/vault/src/officeText.ts`. **This is ROUTING, not parsing** — every
entry is already in the `entries` map `unzipSync` returned; nothing new is unzipped, no dependency is
added, and no second XML walker is written (`runsOf` / `rawRunsOf` / `numericSorted` already do it).

Per slide `n`, in this order:
1. slide runs — `runsOf(xml.replace(FLD, ""), "a:t")` where `FLD` strips `<a:fld …>…</a:fld>` blocks
   (a slide-number / date field is scaffolding, not content — measured: it is the ENTIRE content of
   this deck's three notes slides).
2. read `entries[\`ppt/slides/_rels/slide${n}.xml.rels\`]` if present, take each `Target="…"`,
   resolve a leading `../` to `ppt/`, and dispatch on the resolved path:
   - `ppt/charts/chartN.xml` → chart lines (below)
   - `ppt/diagrams/dataN.xml` → `runsOf(xml, "a:t")`. **`drawingN.xml` is NOT in this list** —
     measured byte-duplicate of `dataN.xml`; reading both doubles the agenda. Match on
     `^ppt/diagrams/data\d+\.xml$`, do not match `diagrams/*` generally.
   - `ppt/notesSlides/notesSlideN.xml` → `runsOf(xml.replace(FLD, ""), "a:t")`
   - anything else (layouts, masters, media, themes, colors/style parts) → **ignored**. The dispatch
     is an ALLOW-LIST of three content part types; a rels file points at plenty of scaffolding.
   Track consumed paths in a `Set` so a part referenced from two slides is emitted once.
3. `labelled(\`Slide ${n}\`, lines.join("\n"))` — Task 1's gate. A picture-only slide with no text
   now emits no header at all, which is the honest answer.

Chart lines (`chartN.xml`): title runs `runsOf(xml, "a:t")` first, then split on `</c:ser>` and, for
each fragment containing `<c:ser`, emit `runsOf(fragment, "c:v").map(trim).filter(Boolean).join("\t")`.
Tab-joined matches `xlsxText`'s and `xlsText`'s row convention, so the downstream model sees ONE
convention for every spreadsheet-shaped thing in this repo. `<c:v>` is the cached value inside
`<c:numCache>`/`<c:strCache>`, and it also carries the series NAME through `<c:tx><c:strRef>` — so one
tag covers names, categories and values (measured on all three of the owner's charts).

After the slide loop, sweep any unconsumed `ppt/charts/chartN.xml` / `ppt/diagrams/dataN.xml` into
their own labelled parts (label them by entry path) so an unreferenced chart is never silently lost.

**Required `ponytail:` block naming these ceilings, all MEASURED against the owner's deck — do not
build any of them:**
- **Picture slides are unreadable.** Slides 6 and 7 are image exports (`image1.png`, two `.wmf`);
  they yield one title run each and nothing more. **No text walker will ever read them.** Upgrade
  path: render each slide and route it through the hosted OCR rail 15.2-06 built — a different rail,
  a real cost, and out of scope here. Say so plainly rather than letting it look like coverage.
- **Cached values are emitted verbatim.** Chart categories come back as Excel DATE SERIALS
  (`46023 / 46054 / 46082` = the Jan/Feb/Mar 2026 month ends) and percentages as full-precision
  floats (`4.7100000000000003E-2`, displayed as 4.71%). Same family as 15.2-07's `46067`. Upgrade
  path: read `<c:formatCode>` and apply it.
- **Embedded worksheets are OUT OF SCOPE by measurement, not by omission.** This deck has **no
  `ppt/embeddings/` directory at all**; its charts declare `externalData` pointing at
  `file:///C:\Users\Zainab Bottlers\Desktop\…xlsx` — a path on the author's machine. The cached
  `<c:v>` values are therefore the complete numeric truth available inside the archive. If a real
  deck ever surfaces a `ppt/embeddings/*.xlsx`, the upgrade is one line — recurse
  `extractOfficeText` on that entry, since it is a self-identifying ZIP. **An `externalData` target
  must never be dereferenced** (it is an attacker-controlled path/URL in an untrusted upload).
- **Masters and layouts are deliberately never read** — boilerplate, pinned by a test.

**Then measure against the real file** — a one-shot run, NOT a committed test (the locked "no binary
fixtures in repo" convention): point a throwaway script at
`C:/Users/expert/Desktop/Rejection Review- Mar 2026.pptx`, print `text.length` and the text, and
record the number in the SUMMARY. **Expected ≈ 1,181 chars (from 356).** Confirm by eye that
`Slide 3/4/5` carry the figures, that `Agenda:` appears once, and that no "Click to edit" appears.
A material difference is a FINDING to report, not a number to tune the code toward. **Write the
script with the Write tool, not a heredoc** — 15.2-02 recorded that heredocs in this environment
halve backslashes even when quoted, which silently corrupts every regex in this task.

**Mutation checks (confirm applied, then revert):**
1. add `drawingN.xml` to the diagram allow-list → the `Agenda:` count row must go RED (the duplicate
   hazard is caught by a test, not by a comment).
2. widen the rels dispatch to accept any `Target` → the master/layout boilerplate row must go RED.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test &amp;&amp; pnpm --filter @pikar/vault typecheck</automated>
  </verify>
  <done>A PPTX fixture's chart series values, SmartArt prose and speaker notes appear under the slide their rels attach them to; the SmartArt agenda appears exactly ONCE despite `data1` and `drawing1` both being referenced; master/layout/handout boilerplate appears NOWHERE; an unreferenced chart still appears as its own part; `<a:fld>` slide-number fields contribute nothing; output is deterministic across two calls; both mutations confirmed RED then reverted; the real deck measured at ~1,181 chars (from 356) with the figures present, recorded in the SUMMARY; no new dependency, `pnpm-lock.yaml` untouched, `officeText.ts` still off the barrel, zero `convex/` edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — the owner's deck reads its numbers, and a text-free deck fails honestly</name>
  <action>
PAUSE. Complete the three preparation steps in <what-built> FIRST (restart :3000, re-extract the
known row after proving deployment freshness, build the probe file), then present the verification
steps below to the owner verbatim and WAIT for the resume signal. Do not self-approve, do not infer
approval from a green test suite or from a row's character count alone — the character count is
Claude's evidence, the browser render is the owner's — and do not continue to Task 4 until the owner
has answered. Record whatever the owner actually reports, at the evidence level they gave it.
  </action>
  <what-built>
Both halves, offline-green and measured against the real file. What offline CANNOT prove: that the
round trip (upload → sniff → zip rail → `extractOfficeText` → ingest → `extractGraph` → the Entities
panel) actually delivers it, and that a text-free document now shows the user a plain-language
failure. **`failureCopy` has never been rendered in a browser at any point in this phase** — a `.pptx`
that now correctly fails is the first chance to confirm that surface, and closes half of what
15.2-04/05 left open.

**BEFORE the checkpoint, the executor does two things:**

1. **Restart the `:3000` web server.** It is a `next start` (PID 14512 at time of writing) that
   **predates its own build** — launched 2026-07-26 21:35:06 against a `.next` rebuilt
   2026-07-27 16:22:39 — and has not been restarted since 15.2-05 flagged it. This is the Phase-14
   `voice.md` trap: a UI symptom misread as a code failure because the running bundle predates the
   code. **Any browser observation before this restart is worthless.** Ask the owner before killing
   the process (the standing rule on convex/web PIDs), then rebuild and restart.
2. **Re-extract the known row, don't re-upload.** Row `mx7a40n7460cj3d1ww97bms6wd8bb9zg`
   (`Rejection Review- Mar 2026.pptx`) still holds its `storageId`, so
   `npx convex run internal.vaultExtract.extractDoc` re-runs from the stored bytes — the 15.2-06
   precedent. **Prove the deployment is CURRENT first** (touch probe: touch the source, measure the
   `convex dev` CPU delta — an idle reading is consistent with both "already pushed" and "watch not
   running"). Extraction is the pure `zip` rail: **$0 in model spend**; only the re-embed costs cents.
   Read back `size` / the audit `charCount` and the first ~600 chars BEFORE the gate and write them
   into the playbook marked `PENDING`, per the 15.2-05/06/07 precedent — a session limit must not be
   able to lose the observation. Run the Convex CLI from `packages/backend`.
3. **Build the false-ready probe file.** With `zipSync`, write a deliberately text-free `.pptx` to
   `C:/Users/expert/Desktop/pikar-false-ready-probe.pptx` (**outside the repo** — the no-binary-fixture
   rule): `ppt/presentation.xml`, ONE `ppt/slides/slide1.xml` whose only `<a:t>` is inside an
   `<a:fld>` slide-number field, and a `ppt/slideLayouts/slideLayout1.xml` full of "Click to edit
   Master title style". It must have a slide — a deck with NO slides throws `office_parse_failed`,
   which is a different (and also correct) ending; this probe is specifically for
   `empty_extraction`. On `main` today this file would report **`ready`**.
  </what-built>
  <how-to-verify>
**1. The owner's KPI deck — did the numbers arrive?**
   - Open http://localhost:3000/dashboard/vault (after the restart above) and open
     **`Rejection Review- Mar 2026.pptx`**.
   - The extracted-text pane should now show, under **Slide 3 / Slide 4 / Slide 5**, the rejection
     figures — the `ZBIJ / ZBBW / ZBFL` rows against `Jan-26 / Feb-26 / Mar-26` — and under
     **Slide 2** the Objective/Agenda block. Roughly **1,181 characters, up from 356**.
   - **The Entities & Relationships panel is the actual acceptance surface.** It said "No entities
     extracted for this document." It should not any more.
   - **Expected to STILL be missing, and this is honest, not a failure:** the content of slides 6 and
     7 ("Component wise Breakdown"). They are picture exports; no text walker can read them. Please
     confirm you see their titles only, so the ceiling is observed rather than assumed.
   - Sanity check on duplication: the Agenda block should appear **once**.

**2. The false-ready probe — does a text-free file now fail in plain English?**
   - Upload `pikar-false-ready-probe.pptx` from your Desktop (it is a deliberately empty test file;
     delete it afterwards).
   - It must land as **failed**, showing *"We opened this file but found no readable text."* plus the
     remedy — **not** a ready document, and **not** a raw error code.
   - Tell me which words you actually see. This is the first time `failureCopy` has ever been
     rendered in a browser, so "it failed" is not enough — the copy itself is what is being verified.
  </how-to-verify>
  <resume-signal>Type "approved" — or paste what you actually saw (character count, whether entities appeared, and the exact failure wording). If the deck still reads 356 chars, say so and STOP: that means the deployment did not carry the new bundle, not that the parser is wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record it where it survives — playbook + the VALIDATION gap 15.2-05/06/07 left open (§9)</name>
  <files>docs/playbooks/vault.md, .planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-VALIDATION.md</files>
  <action>
**`docs/playbooks/vault.md`** — append a NEW `### Phase 15.2 — 15.2-08` subsection at the END of the
file, inside the existing `## Phase 15.2` container. Do not edit any earlier subsection (append-only
shared singleton; resolve any conflict keep-both). Bump the `Last verified:` header line to
`2026-07-27 (22)` and lead it with what actually changed.

Record as INVARIANTS (rules, not narrative — a rule only a SUMMARY reader finds is not a rule, the
15.2-07 lesson):
- **THE FALSE-READY FAMILY IS CLOSED, and the rule outlives the two sites:** *never let a non-empty
  string stand in for "we got content."* A structural header is scaffolding and is bound to its body
  via `labelled()`. The success signal is a COUNT of parts that yielded content — `okPages` (15.2-06),
  `okSheets` (15.2-07), `parts.length` (here). **Five instances of this shape were found in one
  phase.** Anyone adding a new walker starts from the count.
- **`""` not a throw, and why:** the archive parsed, so `office_parse_failed` would be a lie;
  `vaultExtract.ts:392` already turns `""` into `empty_extraction`, which is accurate and already has
  user-facing copy. *No sheets at all* (broken archive) and *sheets with nothing in them* (empty
  document) are different facts with different endings — do not merge them.
- **The PPTX entry list, positive AND negative**, as the measured table in this plan: READ
  `slides/slideN.xml`, `charts/chartN.xml` (`<c:v>`), `diagrams/dataN.xml`, `notesSlides/notesSlideN.xml`
  (minus `<a:fld>`); **NEVER** `diagrams/drawingN.xml` (byte-duplicate of `dataN` — it doubles the
  text) and **NEVER** slideLayouts / slideMasters / notesMasters / handoutMasters (boilerplate:
  ~110 runs of "Click to edit Master title style" plus the deck title on every layout). Attachment is
  via `slides/_rels/slideN.xml.rels`, with an orphan sweep so nothing is silently dropped.
- **The measured ceilings**, each with its upgrade path: picture-only slides are unreadable by any
  text walker (upgrade = the 15.2-06 hosted-OCR rail, a different rail with real cost); chart caches
  emit Excel date serials and full-precision floats verbatim (upgrade = `<c:formatCode>`); embedded
  worksheets are absent from this deck and its charts point at `externalData` on the author's machine
  — **an `externalData` target must never be dereferenced**.
- **The live numbers from Task 3** — before/after char counts, whether the Entities panel populated,
  and **the exact `failureCopy` wording the owner reported**, at the evidence level the owner actually
  gave it. Do not round an approval up (the standing rule of this phase).

**`15.2-VALIDATION.md`** — this file's own closing section lists the parser false-ready family as
open item 1 and `failureCopy`-never-rendered as open item 2. Add 15.2-08's rows to the Per-Task
Verification Map (08-T1 … 08-T4, wave 8) and update those two open items to their true state:
item 1 CLOSED by this plan; item 2 closed **only if** the owner actually reported the copy — if the
probe upload was skipped, say so plainly and leave it open. Item 3 (Excel-authored `.xls` date cells)
is untouched by this plan and stays open. Do **not** touch `nyquist_compliant`.

Finally: `graphify update . && node scripts/extract-convex-edges.mjs` (the graph fixup — a rebuild
drops the injected Convex edges).
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs &amp;&amp; grep -q "15.2-08" docs/playbooks/vault.md</automated>
  </verify>
  <done>`docs/playbooks/vault.md` carries a `### Phase 15.2 — 15.2-08` subsection with the family rule, the `""`-not-throw reasoning, the positive AND negative PPTX entry lists, all four measured ceilings and the live result at its true evidence level; `Last verified` bumped to (22); `15.2-VALIDATION.md` has the four 08-T rows and its open items 1 and 2 updated honestly (item 3 untouched, `nyquist_compliant` untouched); `check-playbooks.mjs` exit 0; graph refreshed.</done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @pikar/vault test                 # every pre-existing expectation still green
pnpm --filter @pikar/vault typecheck
pnpm --filter @pikar/backend test vault -- --maxWorkers=1   # Windows: re-run -w1 before trusting a red
node scripts/check-playbooks.mjs
git diff --name-only | grep -c "packages/backend/convex" || true   # expect 0 — no Convex edits
git diff --name-only | grep -c "pnpm-lock.yaml"          || true   # expect 0 — no new dependency
grep -c "officeText" packages/vault/src/index.ts                   # expect 0 — still subpath-only
```

**This tree is SHARED.** Phases 16 (Lane R) and 17 (Lane K) are live in the same working copy and
other sessions' files are already modified/staged (`convex/dispatch.ts`, `convex/evaluations.ts`,
`graphify-out/*`). **Commit with an explicit pathspec** (`git commit -- <paths>`), never `-a`, never
a bare `git commit` after a broad `git add`. `docs/playbooks/vault.md`, `STATE.md` and `ROADMAP.md`
are append-only shared singletons: resolve conflicts keep-both.
</verification>

<success_criteria>
- **The false-ready family is closed and CHECKABLE:** a chart-only / text-free ZIP office document
  produces `empty_extraction` and its user-facing remedy — **never `ready`** — proven offline for both
  XLSX and PPTX with `=== ""` assertions, mutation-confirmed, and confirmed live on the probe upload.
  The fix is in the shared shape (`labelled`), not in the pptx path alone.
- **The owner's deck reads its numbers:** ~1,181 chars (from 356), chart series and SmartArt prose
  attached to their slides, agenda present exactly once, no master boilerplate, and the Entities
  panel no longer empty — confirmed by the owner in a browser running a bundle newer than its code.
- **The ceilings are named, not silently missed:** picture-only slides, date serials / float
  precision, and embedded worksheets each have a `ponytail:` line with an upgrade path.
- **No new dependency, no `convex/` edit, `officeText.ts` still off the barrel,** every pre-existing
  test expectation unedited and green.
- **`failureCopy` rendered in a browser for the first time** — or its absence reported plainly rather
  than closed.
</success_criteria>

<output>
Create `.planning/phases/15.2-vault-universal-format-recognition-and-extraction-fan-out/15.2-08-SUMMARY.md`
</output>

