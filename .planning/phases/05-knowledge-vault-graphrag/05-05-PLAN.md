---
phase: 05-knowledge-vault-graphrag
plan: 05
type: execute
wave: 5
depends_on: ["05-03", "05-04"]
files_modified:
  - packages/backend/convex/vaultGround.ts
  - packages/backend/convex/vault.ts
  - packages/backend/convex/vaultGround.test.ts
  - docs/playbooks/vault.md
requirements: [VALT-03, VALT-04]
autonomous: true

must_haves:
  truths:
    - "vaultGround({query}) runs rag.search (hybrid) seeds -> maps seed entries to graph nodes -> hop-capped (<=2) graph expand -> merges/dedupes/ranks into one context block, scoped to the requesting tenant only"
    - "a different tenant's data never appears in a grounding result (namespace + tenant guard)"
    - "listVaultDocs returns the tenant's vaultDocuments (cheap query, no vectors) and the 4 stats derive from it (TOTAL FILES, PROCESSED=ready, STORAGE USED=sum size, CATEGORIES=6)"
    - "the search box uses the SAME rag.search primitive scoped to the active category tab"
    - "vaultDownloadUrl returns a short-lived signed URL only to the owning tenant (cross-tenant -> null), never logged"
    - "docEntities({vaultDocId}) returns the graphNodes/graphEdges extracted from THIS doc for the detail panel"
  artifacts:
    - path: "packages/backend/convex/vaultGround.ts"
      provides: "hybrid vector+graph grounding action (VALT-03)"
      contains: "searchType"
    - path: "packages/backend/convex/vault.ts"
      provides: "listVaultDocs + vaultStats + vaultSearch + vaultDownloadUrl + docEntities (read plane)"
      contains: "vaultDownloadUrl"
    - path: "packages/backend/convex/vaultGround.test.ts"
      provides: "SMOKE:: hybrid fusion + tenant-scope coverage"
  key_links:
    - from: "packages/backend/convex/vaultGround.ts"
      to: "internal.vaultGraph.expand"
      via: "seed docIds -> expand(hopCap=GRAPH_HOP_CAP) -> fuse"
      pattern: "expand"
    - from: "packages/backend/convex/vault.ts"
      to: "ctx.storage.getUrl"
      via: "vaultDownloadUrl tenant-guarded signed URL (attachmentUrls pattern)"
      pattern: "getUrl"
---

<objective>
Build the read plane: `vaultGround` (VALT-03 hybrid vector + hop-capped graph retrieval, tenant-scoped, verified E2E in-lane) and the browse/search/download/detail queries the UI consumes (VALT-04) — `listVaultDocs`, `vaultStats`, the category-scoped `vaultSearch`, the tenant-guarded `vaultDownloadUrl`, and `docEntities`.

Purpose: `vaultGround` is the standalone grounding action (its cockpit/pipeline call-site is DEFERRED — Lane A). The search box reuses the same `rag.search` primitive ("one surface, two callers"). Download copies the proven `attachmentUrls` bearer-capability pattern (§4).
Output: `vaultGround.ts`, the `vault.ts` read surfaces, tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-vault-graphrag/05-RESEARCH.md
@packages/backend/convex/plans.ts
@packages/backend/convex/lib/functions.ts

<interfaces>
<!-- RAG search (verified dts, RESEARCH Pattern 4): action-only -->
```ts
rag.search(actionCtx, { namespace, query, limit, searchType: "hybrid", vectorScoreThreshold })
  => { results, entries } // entries[].metadata?.vaultDocId links back to the metadata row
```
<!-- @pikar/vault (Plan 01): fuse(results, seedDocIds, neighborDocIds) => { context, docIds }; GRAPH_HOP_CAP -->
<!-- vaultGraph (Plan 03): internal.vaultGraph.expand({tenantId, seedDocIds, hopCap}) => neighbor docIds -->
<!-- attachmentUrls pattern (plans.ts, verified): tenantQuery guards on the row, returns ctx.storage.getUrl(...) ONLY to the owner; the URL is a bearer capability, NEVER logged (§4) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vaultGround — hybrid vector + hop-capped graph action</name>
  <files>packages/backend/convex/vaultGround.ts, packages/backend/convex/vaultGround.test.ts</files>
  <behavior>
    - vaultGround({query}) on a seeded 2-doc corpus (SMOKE:: search fixture) returns a merged context whose docIds include BOTH the vector-seed doc and a graph-neighbor doc reached within <=2 hops.
    - A doc 3 hops from any seed is NOT included (hop cap).
    - Called as tenant B, it returns NOTHING from tenant A's corpus (namespace = tenantId + tenant guard).
  </behavior>
  <action>
    Create `vaultGround.ts` exporting `vaultGround = tenantAction({...})` (RESEARCH Pattern 4). Handler: `rag.search(ctx, { namespace: ctx.tenantId, query, limit: 8, searchType: "hybrid", vectorScoreThreshold: 0.2 })` → map `entries[].metadata?.vaultDocId` to `seedDocIds` → `ctx.runQuery(internal.vaultGraph.expand, { tenantId: ctx.tenantId, seedDocIds, hopCap: GRAPH_HOP_CAP })` → `fuse(results, seedDocIds, neighborDocIds)` (from `@pikar/vault`) → return `{ context, docIds }`. Add a `SMOKE::` bypass (Pitfall 4) that returns deterministic seed "results" WITHOUT calling `rag.search` (offline), so the graph-expand + fuse path is convex-testable without embedding. `rag.search` is action-only (Pitfall 1) — `tenantAction` is correct. Write `vaultGround.test.ts` FIRST driving the SMOKE path over a graph seeded via `vaultGraph.upsertGraph`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test vaultGround</automated>
  </verify>
  <done>vaultGround.test.ts green: vector-seed + ≤2-hop graph merge, hop-cap exclusion, cross-tenant returns nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: browse + stats + category search + download + docEntities (read plane)</name>
  <files>packages/backend/convex/vault.ts, packages/backend/convex/vaultGround.test.ts</files>
  <behavior>
    - listVaultDocs({category?}) returns the tenant's vaultDocuments via by_tenant; a category filters to one `category` field; another tenant's docs never appear.
    - vaultStats() returns { totalFiles, processed (status==='ready'), storageUsedBytes (Σ size), categories: 6 }.
    - vaultDownloadUrl({vaultDocId}) returns a signed URL for the owner; a cross-tenant caller gets null (never another tenant's URL).
    - docEntities({vaultDocId}) returns { nodes, edges } extracted from THIS doc (graphEdges by_tenant_source → endpoints), owner-guarded.
    - vaultSearch({query, category?}) returns the same rag.search hybrid results post-filtered to the active category (Open-Q1 recommendation: post-filter on the joined vaultDocuments.category).
  </behavior>
  <action>
    Append to `vault.ts`: `listVaultDocs` + `vaultStats` (`tenantQuery`, cheap — no vectors; RESEARCH browse example; CATEGORIES = 6 const), `vaultDownloadUrl` (`tenantQuery`, mirror `plans.ts attachmentUrls` — guard on the row, return `ctx.storage.getUrl(storageId)` ONLY to the owner, NEVER log it, §4), `docEntities` (`tenantQuery`, owner-guarded), and `vaultSearch` (`tenantAction` reusing the same `rag.search` hybrid primitive as `vaultGround`, post-filtered to `category` — Open-Q1). Add the browse/download/scope cases to `vaultGround.test.ts` (or a `vault.test.ts` append) via convex-test. Bump `docs/playbooks/vault.md` Last verified → `05-05`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test vault</automated>
  </verify>
  <done>list/stats/download-guard/docEntities/category-search tests green; download URL is owner-only; stats derive from the cheap query.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test vault vaultGround` — grounding + browse + download-guard green.
- `pnpm --filter @pikar/backend typecheck` — read plane compiles.
- `node scripts/check-playbooks.mjs` — clears.
</verification>

<success_criteria>
- `vaultGround` fuses vector similarity with ≤2-hop graph neighbors, scoped to the requesting user only (VALT-03), verified in-lane offline.
- Browse queries + the 4 stats + the category-scoped search + the tenant-guarded download all work off the cheap metadata plane (VALT-04).
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-vault-graphrag/05-05-SUMMARY.md`.
</output>
