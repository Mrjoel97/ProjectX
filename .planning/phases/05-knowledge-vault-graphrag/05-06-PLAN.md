---
phase: 05-knowledge-vault-graphrag
plan: 06
type: execute
wave: 6
depends_on: ["05-04", "05-05"]
files_modified:
  - apps/web/app/(app)/dashboard/vault/page.tsx
  - apps/web/app/(app)/dashboard/vault/VaultStats.tsx
  - apps/web/app/(app)/dashboard/vault/CategoryTabs.tsx
  - apps/web/app/(app)/dashboard/vault/Dropzone.tsx
  - apps/web/app/(app)/dashboard/vault/DocGrid.tsx
  - apps/web/app/(app)/dashboard/vault/PreviewModal.tsx
  - apps/web/app/(app)/layout.tsx
  - docs/playbooks/vault.md
requirements: [VALT-04]
autonomous: true

must_haves:
  truths:
    - "/dashboard/vault renders the two-screenshot layout 1:1: 'Knowledge Vault' headline, Refresh button + Loading pill, 4 stat tiles, 6 category tabs (active = teal pill), upload dropzone with the exact copy, search bar + N ITEMS + grid/list toggle"
    - "the dropzone uploads via ctx.storage.generateUploadUrl then vaultUpload; a TXT/MD/CSV upload shows processing then ready reactively; a Brain-Dump paste routes to vaultIngestText"
    - "a doc opens an in-place preview modal (text/render) with metadata + this-doc entities + Download; delete removes it and the grid updates"
    - "honest-zero empty states (0 files / 0 MB) render exactly like the screenshots when the vault is empty"
    - "a Knowledge Vault nav item points at /dashboard/vault; tokens come from globals.css (no new component library, no hardcoded hex a token covers)"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/vault/page.tsx"
      provides: "the Knowledge Vault route composing the sections against the live queries"
      min_lines: 40
    - path: "apps/web/app/(app)/dashboard/vault/Dropzone.tsx"
      provides: "upload dropzone (generateUploadUrl -> vaultUpload) + Brain-Dump paste"
    - path: "apps/web/app/(app)/dashboard/vault/VaultStats.tsx"
      provides: "the 4 stat tiles (TOTAL FILES / PROCESSED / STORAGE USED / CATEGORIES 6)"
  key_links:
    - from: "apps/web/app/(app)/dashboard/vault/page.tsx"
      to: "api.vault.listVaultDocs / api.vault.vaultStats"
      via: "useQuery reactive browse + stats"
      pattern: "useQuery"
    - from: "apps/web/app/(app)/dashboard/vault/Dropzone.tsx"
      to: "api.vault.vaultUpload"
      via: "generateUploadUrl POST then vaultUpload mutation"
      pattern: "vaultUpload"
    - from: "apps/web/app/(app)/layout.tsx"
      to: "/dashboard/vault"
      via: "NAV entry 'Knowledge Vault'"
      pattern: "/dashboard/vault"
---

<objective>
Build the Knowledge Vault route (VALT-04) matching `brand-024242.png` / `brand-024258.png` 1:1 — headline, Refresh + Loading pill, 4 stat tiles, 6 category tabs, upload dropzone, search bar with N ITEMS + grid/list toggle, in-place preview modal with metadata + this-doc entities + download/delete — wired to the Plan 04/05 queries. Add the Knowledge Vault nav item.

Purpose: This is the user-facing surface of the vault (browse/search/preview/download/delete). CLAUDE.md §10 / BRAND §8: the committed screenshots are the source of truth; reuse `globals.css` tokens and the cockpit card idioms, no new component library.
Output: the `/dashboard/vault` route + components + nav entry.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-vault-graphrag/05-CONTEXT.md
@docs/design/BRAND.md
@docs/design/brand/brand-024242.png
@docs/design/brand/brand-024258.png
@apps/web/app/globals.css
@apps/web/app/(app)/layout.tsx
@apps/web/app/(app)/dashboard/workspace/page.tsx

<interfaces>
<!-- Backend queries/mutations (Plans 04/05) consumed via @pikar/backend api: -->
```ts
api.vault.listVaultDocs({ category? }) // reactive browse rows
api.vault.vaultStats()                 // { totalFiles, processed, storageUsedBytes, categories }
api.vault.vaultUpload({ storageId, filename, mimeType, size, contentHash, text? })
api.vault.vaultIngestText({ text, title?, source: 'paste' })
api.vault.vaultDownloadUrl({ vaultDocId })
api.vault.docEntities({ vaultDocId })
api.vault.vaultSearch({ query, category? }) // action, hybrid, category-scoped
ctx.storage.generateUploadUrl()            // client POSTs bytes, then vaultUpload
```
Tokens (globals.css, verified): --teal-600 #009689 (active tab pill, Refresh, "Click to upload" link),
--canvas #f8fafc, --card #fff, --ink/--ink-soft, --rule #d8dbe0. Stat-tile icon badges = rounded-square
solid tiles (teal/green/blue/purple per brand-024258). "Loading" pill = small dark pill top-right.
The 6 tabs (exact order): My Uploads · Workspace Docs · Images · Videos · Google Docs · Brain Dumps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vault route shell — headline, stat tiles, category tabs, nav entry</name>
  <files>apps/web/app/(app)/dashboard/vault/page.tsx, apps/web/app/(app)/dashboard/vault/VaultStats.tsx, apps/web/app/(app)/dashboard/vault/CategoryTabs.tsx, apps/web/app/(app)/layout.tsx</files>
  <action>
    Create `/dashboard/vault/page.tsx` (client) rendering the top of the screenshots: the "Knowledge Vault" display headline, a teal-600 Refresh button + a small dark "Loading" pill (shown while `vaultStats`/`listVaultDocs` are `undefined`), and `VaultStats` — the 4 tiles (TOTAL FILES, PROCESSED, STORAGE USED MB, CATEGORIES 6) from `api.vault.vaultStats` with the rounded-square icon badges (teal/green/blue/purple) per BRAND §5. `CategoryTabs` renders the 6 tabs in exact order with the active teal-600 pill; lift the active category to `page.tsx` state. Honest zeros when empty (0 / 0 MB). Add a `{ href: "/dashboard/vault", label: "Knowledge Vault" }` entry to the `NAV` array in `(app)/layout.tsx`. Tokens only — no hardcoded hex a token covers (§10). Reuse cockpit card idioms; no component library.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Route renders headline + Refresh + Loading pill + 4 tiles + 6 tabs off the live stats; nav entry present; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Dropzone (upload + paste) + search bar + grid/list</name>
  <files>apps/web/app/(app)/dashboard/vault/Dropzone.tsx, apps/web/app/(app)/dashboard/vault/DocGrid.tsx, apps/web/app/(app)/dashboard/vault/page.tsx</files>
  <action>
    `Dropzone.tsx`: the dashed dropzone with the cloud icon in a soft-teal circle, "Click to upload or drag and drop", subtext "Searchable: PDF, DOCX, XLSX, CSV, TXT, Markdown" / "Storage-only: Images, Videos (not embedded)". On file select/drop → `useMutation(api...generateUploadUrl)` (or the storage action) → POST bytes → `api.vault.vaultUpload({ storageId, filename, mimeType, size, contentHash, text })` (read TXT/MD/CSV text client-side + compute contentHash; binaries upload bytes with no text → `pending_extraction` server-side). Include a Brain-Dump paste affordance (textarea → `api.vault.vaultIngestText({ text, source: 'paste' })`). `DocGrid.tsx`: the search bar ("Search my uploads…"), the `N ITEMS` count, the grid/list toggle, and the item cards/rows from `api.vault.listVaultDocs({ category })` showing per-item status (processing / ready / pending_extraction / failed badge). Search calls `api.vault.vaultSearch` scoped to the active category. Compose both into `page.tsx` below the tabs.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Dropzone uploads TXT/MD/CSV (→processing→ready reactively) and pastes Brain Dumps; search + N ITEMS + grid/list render; item status badges reflect the row; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Preview modal — text/render + metadata + this-doc entities + download/delete</name>
  <files>apps/web/app/(app)/dashboard/vault/PreviewModal.tsx, apps/web/app/(app)/dashboard/vault/DocGrid.tsx</files>
  <action>
    `PreviewModal.tsx`: an in-place modal (Esc/X to close, NOT a route change) showing the stored text (or image/video for those kinds), a detail panel with title + metadata (kind, source, size, added, status), the entities & relationships from `api.vault.docEntities` as chips/list (surfaces VALT-02 in the UI), a Download action that fetches `api.vault.vaultDownloadUrl` on demand and triggers the browser download (never render the URL into logs), a Delete action calling `api.vault.deleteVaultDoc` (grid updates reactively), and an "Open in workspace" plain `<Link href="/dashboard/workspace">` (no Lane-A edits — navigate only). Wire the item click in `DocGrid.tsx` to open it. Bump `docs/playbooks/vault.md` Last verified → `05-06`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Clicking an item opens the modal with metadata + this-doc entity chips + working Download + Delete + Open-in-workspace link; delete updates the grid; typecheck clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` — the whole route compiles against the api types.
- `pnpm --filter @pikar/web build` — the vault route builds (no runtime-only token/hex regressions).
- Visual: `/dashboard/vault` matches `brand-024242` / `brand-024258` (human-verified in Plan 07).
- `node scripts/check-playbooks.mjs` — clears (vault.md bumped).
</verification>

<success_criteria>
- The route matches the two screenshots 1:1 (headline, Refresh + Loading pill, 4 tiles, 6 tabs, dropzone copy, search + N ITEMS + grid/list) using only globals.css tokens.
- Upload (TXT/MD/CSV) + Brain-Dump paste ingest reactively; preview shows metadata + this-doc entities; download + delete work; honest-zero empty states.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-vault-graphrag/05-06-SUMMARY.md`.
</output>
