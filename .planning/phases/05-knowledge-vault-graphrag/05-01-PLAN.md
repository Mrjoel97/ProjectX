---
phase: 05-knowledge-vault-graphrag
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docs/playbooks/vault.md
  - docs/playbooks/watch.json
  - packages/vault/package.json
  - packages/vault/tsconfig.json
  - packages/vault/src/constants.ts
  - packages/vault/src/normalize.ts
  - packages/vault/src/categories.ts
  - packages/vault/src/traversal.ts
  - packages/vault/src/fusion.ts
  - packages/vault/src/index.ts
  - packages/vault/src/normalize.test.ts
  - packages/vault/src/categories.test.ts
  - packages/vault/src/traversal.test.ts
  - packages/vault/src/fusion.test.ts
requirements: [VALT-02, VALT-03]
autonomous: true

must_haves:
  truths:
    - "normalizeName collapses case/whitespace so the same entity name across two docs maps to one key (cross-doc dedup foundation)"
    - "hop-capped BFS over an edge adjacency returns neighbors within <= GRAPH_HOP_CAP hops and never explodes past the cap"
    - "fusion merges vector-seed doc ids with graph-neighbor doc ids into one deduped, ranked list (vector score first, graph proximity as tiebreak)"
    - "categoryFor maps (source, mimeType) to exactly one of the 6 vault categories and classifies searchable vs storage-only formats"
    - "the new vault playbook is registered in watch.json so every later vault file (packages/vault, convex/vault*, the vault route) is covered by the section-9 Stop hook"
  artifacts:
    - path: "packages/vault/src/traversal.ts"
      provides: "hop-capped BFS traversal over graph edges (pure, no Convex import)"
      min_lines: 20
    - path: "packages/vault/src/fusion.ts"
      provides: "vector-seed + graph-expand merge/dedupe/rank"
      min_lines: 20
    - path: "packages/vault/src/constants.ts"
      provides: "VAULT_FILE_CAP_BYTES and GRAPH_HOP_CAP consts"
      contains: "GRAPH_HOP_CAP"
    - path: "docs/playbooks/vault.md"
      provides: "the vault subsystem playbook (invariants + watched paths)"
    - path: "docs/playbooks/watch.json"
      provides: "vault.md watched-path registration"
      contains: "packages/vault"
  key_links:
    - from: "docs/playbooks/watch.json"
      to: "docs/playbooks/vault.md"
      via: "vault.md key with packages/vault + convex/vault* + vault-route prefixes"
      pattern: "vault.md"
---

<objective>
Establish the Lane-C vault foundation: (1) the `docs/playbooks/vault.md` playbook + its `watch.json` registration so every subsequent new vault file is covered by the §9 Stop hook, and (2) the pure-TS `@pikar/vault` domain package holding the framework-agnostic logic every Convex vault adapter will import — name normalization, hop-capped BFS traversal, vector+graph fusion, category mapping, and the file-cap / hop-cap constants.

Purpose: CLAUDE.md §1 mandates domain logic live in pure `packages/*` with `convex/` as thin adapters. This package is unit-testable with zero backend, and registering the playbook FIRST prevents the §9 Stop hook from blocking every later plan that creates `convex/vault*.ts` or the vault route.
Output: `docs/playbooks/vault.md`, an updated `watch.json`, and a tested `@pikar/vault` workspace package.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-vault-graphrag/05-CONTEXT.md
@.planning/phases/05-knowledge-vault-graphrag/05-RESEARCH.md
@.planning/PARALLELIZATION.md
@docs/playbooks/watch.json
@docs/playbooks/TEMPLATE.md
@packages/pii/package.json

<interfaces>
<!-- Mirror the existing pure package shape. @pikar/pii is the template. -->
From packages/pii/package.json (mirror this for @pikar/vault):
```json
{ "name": "@pikar/vault", "version": "0.0.0", "private": true, "type": "module",
  "exports": { ".": "./src/index.ts", "./*": "./src/*.ts" },
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "devDependencies": { "vitest": "3.2.7" } }
```
`packages/*` is already globbed by pnpm-workspace.yaml — no workspace edit needed.

The 6 vault categories (CONTEXT — auto-assigned at ingest, single `category` field):
  "my-uploads" (file upload), "brain-dumps" (pasted text), "workspace-docs" (agent-generated),
  "images" (image/*), "videos" (video/*), "google-docs" (deferred).
Searchable MIME/formats (Phase-5 wired): text/plain, text/markdown, text/csv.
Storage-only / accept-but-defer: application/pdf, DOCX/XLSX/PPTX, image/*, video/*.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the vault playbook and register its watched paths (§9)</name>
  <files>docs/playbooks/vault.md, docs/playbooks/watch.json</files>
  <action>
    Copy `docs/playbooks/TEMPLATE.md` to `docs/playbooks/vault.md` and fill it for the Knowledge Vault subsystem:
    - Purpose: per-user vault + GraphRAG (VALT-01..04), built on the installed `@convex-dev/rag@0.7.5` + `@convex-dev/workflow` ingest + a bespoke graph plane.
    - Invariants (from CONTEXT + CLAUDE.md): domain logic in `@pikar/vault` / thin `convex/vault*` adapters (§1); `namespace = tenantId` per-user isolation (VALT-03); `rag.add/search/delete` are ACTION-only, `addAsync/deleteAsync/list/getEntry/findEntryByContentHash` are mutation/query-safe; the graph-extractor lives in a DEFAULT-runtime (V8) `vaultLlm.ts` — NEVER a second `"use node"` module; redact-then-extract (extractor gets `safeText`); graph/audit/deadLetter payloads carry refs+hashes+counts ONLY, raw text lives ONLY in `vaultDocuments.text` + rag chunks (§4); cross-doc dedup upserts on `(tenantId, type, normalizedName)`; hop cap = 2; delete cascades with orphan GC; the pinned `rag`/`workflow` versions must not be bumped (§6).
    - How to verify: point at `pnpm --filter @pikar/vault test`, `pnpm --filter @pikar/backend test vault`, `smoke:vault`, and the Playwright vault spec.
    - Set `Last verified:` to `05-01`.
    In `docs/playbooks/watch.json`, add a new `"vault.md"` key (do NOT reorder existing keys — append-only, Pitfall 8) whose array registers ALL vault path prefixes so later plans are covered upfront: `"packages/vault/"`, `"packages/backend/convex/vault.ts"`, `"packages/backend/convex/vaultRag.ts"`, `"packages/backend/convex/vaultIngest.ts"`, `"packages/backend/convex/vaultLlm.ts"`, `"packages/backend/convex/vaultGraph.ts"`, `"packages/backend/convex/vaultGround.ts"`, `"apps/web/app/(app)/dashboard/vault/"`, `"packages/backend/scripts/run-smoke-vault.mjs"`.
    ponytail: the `graph-extractor` skill sources are already covered by the `skill-registry.md` prefixes — do NOT duplicate them here (Pitfall 7).
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>vault.md exists from TEMPLATE with real invariants; watch.json has a `vault.md` key listing every vault prefix; check-playbooks exits 0 (valid JSON, registered).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold @pikar/vault + normalize + categories + constants (test-first)</name>
  <files>packages/vault/package.json, packages/vault/tsconfig.json, packages/vault/src/constants.ts, packages/vault/src/normalize.ts, packages/vault/src/categories.ts, packages/vault/src/index.ts, packages/vault/src/normalize.test.ts, packages/vault/src/categories.test.ts</files>
  <behavior>
    - normalizeName("  Acme   Corp ") === normalizeName("acme corp") (lowercase, trim, collapse internal whitespace) → same entity dedupes to one node.
    - normalizeName preserves distinct names ("Acme" !== "Acme Inc").
    - categoryFor({source:'upload', mimeType:'text/plain'}) === "my-uploads"; ({source:'paste'}) === "brain-dumps"; ({source:'agent'}) === "workspace-docs"; ({mimeType:'image/png'}) === "images"; ({mimeType:'video/mp4'}) === "videos".
    - isSearchable('text/plain'|'text/markdown'|'text/csv') === true; isSearchable('application/pdf'|'image/png') === false (accept-but-defer).
    - VAULT_FILE_CAP_BYTES is a positive integer const; GRAPH_HOP_CAP === 2.
  </behavior>
  <action>
    Mirror `@pikar/pii`'s package.json/tsconfig for a pure ESM vitest package (name `@pikar/vault`, no Convex dep). Implement `normalize.ts` (`normalizeName`), `categories.ts` (`categoryFor(source, mimeType)` → one of the 6 categories; `isSearchable(mimeType)` for the Phase-5 searchable set TXT/MD/CSV, everything else storage-only/deferred), and `constants.ts` (`VAULT_FILE_CAP_BYTES` mirroring the `PLAN_ATTACHMENT_CAP_BYTES` 8 MiB pattern, `GRAPH_HOP_CAP = 2`). Re-export everything from `index.ts`. Write the tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test</automated>
  </verify>
  <done>normalize + categories + constants tests pass; `@pikar/vault` builds and exports the symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hop-capped BFS traversal + vector/graph fusion (test-first)</name>
  <files>packages/vault/src/traversal.ts, packages/vault/src/fusion.ts, packages/vault/src/index.ts, packages/vault/src/traversal.test.ts, packages/vault/src/fusion.test.ts</files>
  <behavior>
    - bfsNeighbors(adjacency, seedNodeIds, hopCap): returns all node ids reachable within `hopCap` hops, EXCLUDING seeds; a node 3 hops away is NOT returned when hopCap=2; cycles terminate (no infinite loop); empty seeds → [].
    - fuse(vectorResults, seedDocIds, neighborDocIds): produces a deduped ordered doc-id list where every vector-seed doc precedes graph-only neighbors, a doc appearing in both is listed ONCE, and ranking is deterministic (vector score desc, then graph proximity). Returns { context, docIds } shape the `vaultGround` action will fill (context assembly may be a thin join here; the action supplies chunk text).
  </behavior>
  <action>
    Implement `traversal.ts` as a pure BFS over an in-memory adjacency map (`Map<nodeId, nodeId[]>`), capped at `GRAPH_HOP_CAP` hops, visited-set guarded. Implement `fusion.ts` `fuse(...)` doing the merge/dedupe/rank per the CONTEXT "vector-seed → graph-expand → merge/rank" fusion (Claude's Discretion on exact scoring — keep it deterministic and documented with a `ponytail:` comment). Zero Convex imports (the Convex `vaultGraph.expand` query in Plan 03 will build the adjacency from `graphEdges` and call `bfsNeighbors`; `vaultGround` in Plan 05 will call `fuse`). Tests FIRST (RED) then implement (GREEN). Leave one runnable assert-based check per ponytail §8 (the colocated tests satisfy this).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test</automated>
  </verify>
  <done>traversal + fusion tests pass; both exported from index.ts; all `@pikar/vault` tests green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/vault test` — all pure-domain tests green (normalize, categories, traversal, fusion).
- `node scripts/check-playbooks.mjs` — exits 0 (vault.md registered, no untracked new-file block).
- `pnpm --filter @pikar/vault typecheck` — clean.
</verification>

<success_criteria>
- `@pikar/vault` exists as a pure package with normalizeName, bfsNeighbors (≤GRAPH_HOP_CAP), fuse, categoryFor/isSearchable, and the two constants — all tested, zero Convex imports.
- `docs/playbooks/vault.md` exists and `watch.json` registers every vault path prefix, so subsequent plans creating `convex/vault*.ts` and the vault route do not trip the §9 hook.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-vault-graphrag/05-01-SUMMARY.md`.
</output>
