---
phase: 05-knowledge-vault-graphrag
plan: 07
type: execute
wave: 7
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - apps/web/e2e/vault.spec.ts
  - packages/backend/scripts/run-smoke-vault.mjs
  - packages/backend/convex/vaultSmoke.ts
  - packages/backend/package.json
  - docs/playbooks/vault.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
requirements: [VALT-01, VALT-03, VALT-04]
autonomous: false

must_haves:
  truths:
    - "a Playwright vault spec drives the SMOKE:: ingest end-to-end: upload/paste -> processing -> ready reactively, search returns it, preview + download + delete work, honest-zero start"
    - "smoke:vault runs the REAL embed + hybrid search + vaultGround on a live convex dev deployment and returns a sensible merged context"
    - "the vault playbook + watch.json are final (Last verified bumped after the human-verify) and the phase is marked complete in STATE.md + ROADMAP.md"
    - "the UI is human-verified against brand-024242/024258 1:1"
  artifacts:
    - path: "apps/web/e2e/vault.spec.ts"
      provides: "Playwright E2E over the SMOKE:: ingest (VALT-04)"
    - path: "packages/backend/scripts/run-smoke-vault.mjs"
      provides: "live embed/search/ground gate (VALT-01/03)"
    - path: "packages/backend/package.json"
      provides: "smoke:vault script"
      contains: "smoke:vault"
  key_links:
    - from: "packages/backend/package.json"
      to: "packages/backend/scripts/run-smoke-vault.mjs"
      via: "smoke:vault npm script"
      pattern: "run-smoke-vault"
---

<objective>
Close Phase 5: the Playwright vault E2E (over the offline SMOKE:: ingest), the live `smoke:vault` gate (real embed + hybrid search + `vaultGround` on a dev deployment), the playbook/watch finalization, the STATE/ROADMAP ticks, and the human-verify checkpoint against the two committed screenshots.

Purpose: The offline suites prove structure; the live smoke proves the real OpenAI embed + RRF retrieval quality (Manual-Only in VALIDATION); the human-verify confirms the UI matches the brand 1:1. This is the phase gate before `/gsd:verify-work`.
Output: E2E spec, smoke script + npm script, finalized docs, human-verified vault.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-vault-graphrag/05-VALIDATION.md
@packages/backend/scripts/run-smoke-fanout.mjs
@packages/backend/package.json
@docs/design/brand/brand-024242.png
@docs/design/brand/brand-024258.png
@.planning/PARALLELIZATION.md

<interfaces>
<!-- Smoke harness (verified run-smoke-fanout.mjs): must(action,args) / pollPass(assert,args) from ./smokeRun.mjs -->
<!-- A vaultSmoke.ts internal helper module (mutations/actions the smoke script drives) is allowed;
     the SMOKE:: seams already exist in embed/search/extract (Plans 03-05). -->
<!-- Live smoke uses tenant "smoke"; the dev deployment has OPENAI_API_KEY (RESEARCH). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright vault E2E over the SMOKE:: ingest</name>
  <files>apps/web/e2e/vault.spec.ts</files>
  <action>
    Write `vault.spec.ts` (reuse the existing Playwright auth harness / storageState) driving `/dashboard/vault`: assert the honest-zero start (0 files, 0 MB, 4 tiles, 6 tabs, dropzone copy). Paste a Brain Dump (or upload a TXT via the SMOKE:: text so no live embed is needed) → assert the item appears `processing` then `ready` reactively → assert `N ITEMS` increments and search surfaces it → open the preview modal (metadata + entity chips) → delete → assert the grid returns to empty. Playwright-discovered + type-loads; a live green run may be deferred to `/gsd:verify-work` if the headless auth harness cannot complete (precedent: prior phases), but the spec must be complete and discovered.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test vault --list</automated>
  </verify>
  <done>vault.spec.ts is Playwright-discovered and type-loads; it exercises upload/paste→ready→search→preview→delete over the SMOKE:: path.</done>
</task>

<task type="auto">
  <name>Task 2: live smoke:vault (real embed + hybrid search + vaultGround)</name>
  <files>packages/backend/scripts/run-smoke-vault.mjs, packages/backend/convex/vaultSmoke.ts, packages/backend/package.json</files>
  <action>
    Create `run-smoke-vault.mjs` (mirror `run-smoke-fanout.mjs`: `must`/`pollPass` from `./smokeRun.mjs`) for tenant "smoke" against a live `convex dev` deployment: ingest 2–3 short briefs that share an entity (real embed via `rag.add`) → poll until `status:ready` → run `vaultSearch` and assert a non-empty ranked result → run `vaultGround({query})` and assert the merged context includes a graph-neighbor doc reached via the shared entity (proves hybrid vector+graph live). Assert NO raw brief text appears in any audit/deadLetters/telemetry row (§4, mirror the fanout redaction needle pattern). Add the minimal `vaultSmoke.ts` internal helpers the script drives (seed/assert), and register `"smoke:vault": "node ./scripts/run-smoke-vault.mjs"` in `packages/backend/package.json`. A failure-proof try/finally cleans up seeded rows.
  </action>
  <verify>
    <automated>node -e "const p=require('./packages/backend/package.json'); if(!p.scripts['smoke:vault']) throw new Error('smoke:vault script missing'); console.log('ok')"</automated>
  </verify>
  <done>smoke:vault exists and (run live during the checkpoint) ingests→embeds→search+ground return a sensible merged context; no raw text in any log plane. Live run may be executed at the checkpoint / verify-work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the vault against the brand + close the phase</name>
  <files>docs/playbooks/vault.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Pause for the human-verify described below — all automation is complete (Plans 01-06 built the vault; Tasks 1-2 added the E2E spec + live smoke). After the user approves, bump `docs/playbooks/vault.md` Last verified → `05-07`, tick Phase 5 plans complete in `.planning/ROADMAP.md`, and update `.planning/STATE.md` (Lane-C append discipline — keep other lanes' progress on any merge conflict).</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>User approves the vault matches the brand 1:1 and ingest/search/ground/download/delete work live; docs/playbooks/vault.md + STATE.md + ROADMAP.md finalized; check-playbooks exits 0.</done>
  <what-built>The full Knowledge Vault: ingest (TXT/MD/CSV + Brain Dumps) → embed + graph-extract → hybrid grounding + browse/search/preview/download/delete UI at /dashboard/vault, matching brand-024242/024258.</what-built>
  <how-to-verify>
    1. Start the lane's stack (`npx convex dev` + `pnpm dev`) and open `/dashboard/vault`.
    2. Confirm the page matches `docs/design/brand/brand-024242.png` and `brand-024258.png` 1:1 — headline, Refresh + Loading pill, 4 stat tiles (with colored icon badges), 6 tabs (My Uploads active teal pill), dropzone copy, search bar + N ITEMS + grid/list toggle, honest zeros.
    3. Upload a TXT/MD/CSV and paste a Brain Dump → watch each go processing → ready; confirm TOTAL FILES / PROCESSED / STORAGE USED update.
    4. Search a term → the item surfaces; open the preview → metadata + extracted entity chips + Download (file downloads) + Open-in-workspace link; Delete → the grid updates and the count drops.
    5. Run `pnpm --filter @pikar/backend smoke:vault` against the live deployment → confirm real embed + hybrid retrieval + vaultGround return a sensible merged context and the redaction assert passes.
    After approval: bump `docs/playbooks/vault.md` Last verified → `05-07`, tick Phase 5 plans complete in `.planning/ROADMAP.md`, and update `.planning/STATE.md` (Lane-C append discipline — keep other lanes' progress on any merge conflict).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (UI mismatch, ingest/search/ground defect).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web exec playwright test vault --list` — spec discovered.
- `pnpm --filter @pikar/backend smoke:vault` — live embed/search/ground green (run at the checkpoint).
- `pnpm test` — full offline suite green (the pre-existing audit.test.ts red is NOT a regression — do not chase it).
- `node scripts/check-playbooks.mjs` — clears (vault.md Last verified → 05-07).
</verification>

<success_criteria>
- The vault UI is human-verified 1:1 against the two screenshots.
- `smoke:vault` proves live embed + hybrid vector+graph retrieval on a real deployment (VALT-01/03).
- The Playwright spec covers the browse/search/preview/delete loop (VALT-04).
- Phase 5 is marked complete in STATE.md + ROADMAP.md; playbook finalized.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-vault-graphrag/05-07-SUMMARY.md`.
</output>
