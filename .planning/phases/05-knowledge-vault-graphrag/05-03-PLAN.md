---
phase: 05-knowledge-vault-graphrag
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - packages/backend/convex/vaultLlm.ts
  - packages/backend/convex/vaultGraph.ts
  - packages/backend/convex/vaultGraph.test.ts
  - docs/playbooks/vault.md
requirements: [VALT-02]
autonomous: true

must_haves:
  truths:
    - "extractGraph redacts (pii.scanText, fail-closed) BEFORE the model call and returns typed {nodes, edges} from the registry graph-extractor skill"
    - "a SMOKE:: sentinel returns a deterministic graph fixture with NO model call (offline convex-test path)"
    - "upsertGraph dedupes the same entity across two docs to ONE graphNodes row (upsert on tenantId+type+normalizedName)"
    - "each new edge increments both endpoints' degree; the expand query returns BFS neighbor docIds within <= GRAPH_HOP_CAP hops, tenant-scoped"
  artifacts:
    - path: "packages/backend/convex/vaultLlm.ts"
      provides: "DEFAULT-runtime (V8) extractGraph internalAction (generateObject + skill + SMOKE seam)"
      contains: "generateObject"
    - path: "packages/backend/convex/vaultGraph.ts"
      provides: "upsertGraph mutation (dedup + degree) + expand traversal query"
      contains: "upsertGraph"
    - path: "packages/backend/convex/vaultGraph.test.ts"
      provides: "convex-test coverage of cross-doc dedup + degree + BFS scope"
  key_links:
    - from: "packages/backend/convex/vaultLlm.ts"
      to: "skills.getActiveSkill"
      via: "runQuery(internal.skills.getActiveSkill, {name: GRAPH_EXTRACTOR_SKILL})"
      pattern: "GRAPH_EXTRACTOR_SKILL"
    - from: "packages/backend/convex/vaultGraph.ts"
      to: "@pikar/vault bfsNeighbors"
      via: "expand builds adjacency from graphEdges indexes and calls bfsNeighbors"
      pattern: "bfsNeighbors"
---

<objective>
Build the graph plane (VALT-02): the `extractGraph` LLM action that turns a document's redacted text into typed entities+relationships, and the `vaultGraph` adapter that upserts them with cross-doc dedup + degree bookkeeping and answers hop-capped neighbor expansion for grounding.

Purpose: This is the genuinely-new code of the phase (the vector half is the installed component). Cross-doc dedup on `(tenantId, type, normalizedName)` is what makes the graph actually connect documents — the payoff of GraphRAG. The extractor MUST be a default-runtime (V8) action, never a second `"use node"` module (Pitfall 3).
Output: `vaultLlm.ts` (extractor), `vaultGraph.ts` (upsert + expand), tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-vault-graphrag/05-RESEARCH.md
@packages/backend/convex/guardrails.ts
@packages/backend/convex/skills.ts
@packages/backend/convex/cockpit.test.ts

<interfaces>
<!-- From @pikar/vault (Plan 01, verified exports): -->
```ts
normalizeName(name: string): string
bfsNeighbors(adjacency: Map<string, string[]>, seedNodeIds: string[], hopCap: number): string[]
GRAPH_HOP_CAP: number // === 2
```
<!-- Registry loader (skills.ts, verified) — reachable from a V8 action via runQuery: -->
```ts
internal.skills.getActiveSkill({ name }): { body, version, skillId } // fails closed unseeded
```
<!-- PII (verified): scanText(text) → { ok: true, value: { safeText, counts } } | { ok: false } -->
<!-- Extractor output contract (RESEARCH Code Example): -->
{ nodes: {type:string; name:string}[]; edges: {from:string; to:string; rel:string}[]; costUsd: number }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extractGraph — V8 generateObject action, redact-then-extract, SMOKE seam</name>
  <files>packages/backend/convex/vaultLlm.ts</files>
  <action>
    Create `vaultLlm.ts` as a DEFAULT-runtime module (NO `"use node"` directive — Pitfall 3; if the internal-graph inference cliff appears, annotate explicit `Promise<...>` return types per the 02-06/§96 mitigation, NOT a node switch). Export `extractGraph` as an `internalAction` (internalAction is NOT import-banned, §2/Pitfall 6): args `{ vaultDocId, tenantId }`. Handler: load `internal.skills.getActiveSkill({ name: GRAPH_EXTRACTOR_SKILL })` (fails closed unseeded, §5), fetch the doc text via `internal.vault.getDoc` (created in Plan 04 — reference `internal.vault.getDoc`; if not yet present at author time, define a minimal local `internal.vaultGraph.getDocText` query instead and note the seam), run `scanText(text)` and THROW on `!ok` (fail closed, §4). If the safeText carries a `SMOKE::` sentinel (mirror `llm.ts parseSmoke`), return a deterministic `smokeGraphFixture(safeText)` (parse a compact `SMOKE::graph::NodeA|NodeB|rel` grammar into `{nodes, edges, costUsd:0}`) with NO model call (Pitfall 4). Otherwise `generateObject({ model: openai(DEFAULT_MODEL), system: skill.body, prompt: safeText, schema: jsonSchema<{nodes,edges}>({...}) })` and return `{ ...object, costUsd: priceUsage(DEFAULT_MODEL, usage) }`. The returned payload carries typed names/rels ONLY — no raw document text (§4).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>extractGraph compiles as a V8 internalAction; SMOKE path returns a fixture with no model call; scan failure throws; no `"use node"` in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: vaultGraph — upsert dedup + degree bookkeeping + BFS expand</name>
  <files>packages/backend/convex/vaultGraph.ts, packages/backend/convex/vaultGraph.test.ts</files>
  <behavior>
    - upsertGraph({tenantId, sourceDocId, nodes, edges}) inserts each distinct entity ONCE; re-upserting the same `(tenantId, type, normalizedName)` from a SECOND doc reuses the existing node id (query graphNodes by_tenant_normalized filtered by type).
    - Each inserted edge patches fromNode.degree += 1 and toNode.degree += 1.
    - After ingesting two docs that both mention "Acme", the graph has exactly ONE Acme node whose degree reflects edges from both docs.
    - expand({tenantId, seedDocIds, hopCap}) maps seed docs → their nodes (edges by_tenant_source) → bfsNeighbors over an adjacency built from by_tenant_fromNode/by_tenant_toNode → returns neighbor sourceDocIds within hopCap hops; a doc 3 hops away is excluded at hopCap=2; a different tenant's nodes are never returned (tenant scope).
  </behavior>
  <action>
    Implement `vaultGraph.ts` with `upsertGraph` (`internalMutation`) using the RESEARCH Pattern 5 upsert-on-normalizedName + degree bookkeeping (`normalizeName` from `@pikar/vault`), and `expand` (`internalQuery`) that gathers edges for the seed docs, builds the `Map<nodeId, nodeId[]>` adjacency via the traversal indexes, and delegates to `bfsNeighbors(adjacency, seedNodeIds, hopCap)` then resolves neighbor nodes back to their `graphEdges.sourceDocId`s. Keep the domain logic (BFS) in `@pikar/vault`; `vaultGraph.ts` is the thin Convex adapter (§1). Write `vaultGraph.test.ts` FIRST using `convexTest(schema, modules)` — no rag/workflow registration needed (this path never embeds). Cover cross-doc dedup, degree, hop-cap exclusion, and tenant isolation.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test vaultGraph</automated>
  </verify>
  <done>vaultGraph.test.ts green: one node for a cross-doc entity, correct degree, ≤2-hop expansion, cross-tenant returns nothing. Bump vault.md Last verified → 05-03.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test vaultGraph` — dedup/degree/BFS/tenant-scope green.
- `pnpm --filter @pikar/backend typecheck` — vaultLlm + vaultGraph compile (no `"use node"`, no inference cliff).
- `node scripts/check-playbooks.mjs` — clears (vault.md bumped).
</verification>

<success_criteria>
- The same entity across two docs dedupes to ONE `graphNodes` row (GraphRAG connectivity).
- Hop-capped (≤2) neighbor expansion works and is tenant-scoped.
- The extractor redacts before the model call, loads its prompt from the registry, and has a deterministic offline SMOKE path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-vault-graphrag/05-03-SUMMARY.md`.
</output>
