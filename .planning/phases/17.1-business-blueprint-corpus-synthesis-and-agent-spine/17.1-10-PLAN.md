---
phase: 17.1-business-blueprint-corpus-synthesis-and-agent-spine
plan: 10
type: execute
wave: 8
depends_on: ["17.1-06", "17.1-07", "17.1-09"]
files_modified:
  - docs/playbooks/vault.md
  - docs/playbooks/onboarding.md
  - docs/playbooks/cockpit.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [BLPR-01, BLPR-02]

must_haves:
  truths:
    - "The `business-blueprint` skill row is seeded and ACTIVE on the real deployment, at a KNOWN version."
    - "A blueprint built from the owner's REAL vault has been inspected: how many fields the citation gate dropped, and what one run actually cost."
    - "A real cockpit turn that calls NO tools demonstrably knows the business — verified by reading the agent's answer, not by asserting it should be there."
    - "The golden eval still passes with a blueprint-bearing tenant."
  artifacts:
    - path: "docs/playbooks/vault.md"
      provides: "The recorded live-gate result with `Last verified` bumped"
      contains: "Last verified"
  key_links:
    - from: "the live deployment"
      to: "skills.business-blueprint"
      via: "npx convex run skills:seedSkills, then verify WHICH version carries the body"
      pattern: "seedSkills"
---

<objective>
Run the live gate and record it. **Offline green does NOT close this phase** — CONTEXT marks this
NON-NEGOTIABLE, and the repo has the receipts: the 2026-07-26 `pdf-lib` defect *"was invisible
OFFLINE too — Node/vitest recover CJS named exports via `cjs-module-lexer`"*, so a page-cap test
passed against a production path that could never run.

Purpose: three of this phase's claims are unprovable by any test. `rag.search` is never exercised
offline at all (the `SMOKE::` seam at `vaultGround.ts:48-60` bypasses embedding), every citation test
uses fixtures, and convex-test drives `vaultGroundHydrated` directly without ever proving the model
saw the spine.

Output: seven live-gate results recorded in the playbooks, and both requirements evidenced.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-business-blueprint-corpus-synthesis-and-agent-spine/17.1-CONTEXT.md
@.planning/phases/17.1-business-blueprint-corpus-synthesis-and-agent-spine/17.1-VALIDATION.md
@.planning/phases/17.1-business-blueprint-corpus-synthesis-and-agent-spine/17.1-09-SUMMARY.md

@docs/playbooks/vault.md
@.planning/REQUIREMENTS.md

<environment>
Traps that have cost hours in this repo — read before running anything:
- **The convex CLI runs from `packages/backend` only.**
- **`pnpm eval:golden` is Windows-fragile.** A network blip destabilizes `convex dev` into a retry
  storm and the eval returns `InternalServerError` at $0. Kill ALL convex processes, start ONE clean
  `convex dev`, verify it is stable, THEN run the eval.
- **`pnpm start` serves a FROZEN production build.** A live session once read as a total grounding
  failure purely because the running bundle predated the feature. **Rebuild before any UAT and check
  the build timestamp against `git log` before believing a UI symptom.**
- **Next dev OOMs on the workspace page** — use a production build for UAT replays.
- **Skill version-collision:** `seedSkills` publishes at `maxVersion + 1`, and optimizer dry-run
  candidates occupy versions. Plan-authored version pins are routinely WRONG against the live DB.
  **Verify which version actually carries your body before trusting anything.**
- `UV_HANDLE_CLOSING` assertion noise from the convex CLI is benign.
</environment>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline gate, then seed and verify the skill on the real deployment (L5)</name>
  <files>docs/playbooks/vault.md</files>
  <action>
1. Full offline gate first — there is no point spending on a live run against a red tree:
   - `pnpm --filter @pikar/core test`
   - `pnpm --filter @pikar/contracts test`
   - `pnpm --filter @pikar/backend test --maxWorkers=1` (expect the one documented red:
     `audit.test.ts`'s `auditCounts` row)
   - `pnpm --filter @pikar/backend typecheck` — ZERO errors in non-test files
   - `pnpm --filter @pikar/web typecheck`
   - `node scripts/check-playbooks.mjs` prints nothing

2. **L5 — seed the skill.** From `packages/backend`: `npx convex run skills:seedSkills`. Then
   **verify which version carries the body** — do not trust the seed's report. Read the `skills`
   rows for `business-blueprint` and confirm the ACTIVE row's `body` matches
   `businessBlueprintSkillBody` (compare a hash or a distinctive line, not a claim). The Phase-10
   precedent is explicit: grounding shipped at `cockpit-agent@14`, not the `@13` the plan named.
   Record the actual active version number in `vault.md`.

3. Confirm `vaultGround`'s dependencies are healthy on the deployment: at least one `ready` vault
   document exists for the owner tenant, and the owner tenant has a `tenantProfiles` row (without
   one, `buildBlueprintDraft` correctly REFUSES and the rest of this plan cannot run).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test && pnpm --filter @pikar/contracts test && ! pnpm --filter @pikar/backend test --maxWorkers=1 2>&1 | grep "FAIL " | grep -v "audit.test.ts" | grep -q . && pnpm --filter @pikar/web typecheck && test -z "$(echo '{}' | node scripts/check-playbooks.mjs check)"</automated>
  </verify>
  <done>Every offline suite is at its documented baseline; `business-blueprint` is ACTIVE on the deployment and the version that carries the body is recorded by number, not assumed.</done>
</task>

<task type="auto">
  <name>Task 2: The measurable live gate — L2, L3, L4, L6</name>
  <files>docs/playbooks/vault.md, docs/playbooks/onboarding.md</files>
  <action>
Run each and RECORD THE NUMBER, not a verdict. "0 dropped" and "8 dropped" are both signals.

**L2 — citation validity on the real corpus.** Build a blueprint from the owner's real vault
(`buildBlueprintDraft`). Record: how many candidates the model returned, how many the citation gate
DROPPED, and the drop reason breakdown (`bad_citation` / `unknown_field` / `not_derivable` /
`empty`). Every offline test used fixtures; this is the first time the gate meets a real model.

**L3 — `rag.search` probe quality.** The `SMOKE::` seam bypasses embedding, so **no offline test
exercises `rag.search` at all**. Inspect the sources each probe returned: are they the documents a
human would have picked for that field? Record at least one probe that worked and one that did not.
Precedent: the chunk-precision defect was found live, not offline (`vault.md:3` item 11).

**L4 — real cost.** `~6 probes × 8000 chars ≈ 12k input tokens` is an estimate inherited from the
spec, not a measurement. Read `priceUsage` / `recordSpend` after the run and record the actual USD.

**L6 — golden eval.** Kill all convex processes, start ONE clean `convex dev`, verify it is stable,
then `pnpm --filter @pikar/backend eval:golden`. Fixtures 25/26 ride the `searchVault` shape; if the
eval tenant now has a confirmed blueprint, the cockpit turn prompt carries the spine and the
grounding output changes. A change is not automatically a failure — record what moved and whether
the fixtures still pass. If they do not, the question to answer is whether the spine improved the
answer or blew the context.

Record all four in `docs/playbooks/vault.md` (the repo's format for live-gate results) with
`Last verified` bumped, and mirror the citation-gate number into `docs/playbooks/onboarding.md`.
  </action>
  <verify>
    <automated>test -z "$(echo '{}' | node scripts/check-playbooks.mjs check)" && grep -q "Last verified" docs/playbooks/vault.md</automated>
  </verify>
  <done>L2, L3, L4 and L6 each have a recorded NUMBER or observation in `vault.md`; the golden-eval outcome is recorded either way; `Last verified` is bumped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The live gate the owner must run — L1 and L7</name>
  <files>(none — this task changes no file; it produces the observations Task 4 records)</files>
  <action>
STOP and hand the session to the owner. Claude has already automated everything automatable: the
skill is seeded (Task 1) and L2/L3/L4/L6 are measured and recorded (Task 2). L1 and L7 are the two
claims that require a human IN the product — L1 needs someone to READ an agent answer and judge
whether it knew the business, and L7 needs someone to type a value, upload a contradicting
document, and confirm their own typed value survived.

Before handing over, rebuild the production bundle and print its timestamp alongside `git log -1`
so the owner is not verifying a frozen build. Then present the instructions below verbatim and
WAIT. Do not proceed to Task 4 until the owner responds.
  </action>
  <verify>
    <automated>MANUAL — checkpoint:human-verify. Automated coverage for this behaviour is plan 06 task 2 (the no-tool-turn test) and plan 03 task 2 (the precedence test); neither can prove the MODEL saw it.</automated>
  </verify>
  <done>The owner has replied "approved", or has described exactly what the agent said and what the diff showed.</done>
  <what-built>
The full blueprint flow is live on the deployment: the profile page's blueprint card builds a draft,
shows the diff, and confirms it; the confirmed blueprint is injected into every cockpit turn prompt
(SEAM 1) and into `evaluations`/`voiceDoc` grounding as a separate `spine` field (SEAM 2). The
`business-blueprint` skill is seeded and active. The measurable half of the live gate (L2-L6) has
been run and recorded in `docs/playbooks/vault.md`.
  </what-built>
  <how-to-verify>
**Rebuild the production bundle first** and check its timestamp against `git log` — `pnpm start`
serves a FROZEN build, and a stale bundle has previously read as a total feature failure.

**L1 — the standing-context gate (the headline claim of BLPR-02).**
1. Open `/dashboard/profile` and confirm the blueprint card shows your live blueprint.
2. Open the cockpit and send a turn that calls NO tools — e.g. *"draft a short email to Bob about
   what we do."* Do NOT ask it to search the vault.
3. Read the draft. Does it describe YOUR business specifically — your offering, your customer, your
   stage — rather than generic filler?
4. **Read the evidence, do not assert it should be there.** If the agent cites a source, check the
   citation. If it says something about your business you never typed, ask it where that came from;
   the answer should name one of your documents.
   ✅ Pass = the agent knew the business on a turn that searched nothing.
   ❌ Fail = generic output. Report exactly what it said.

**L7 — the contradiction flow.**
1. On `/dashboard/profile`, type a `targetCustomer` value in the business card.
2. Upload a document to the vault that plainly says something different, and wait for it to reach
   `ready`.
3. Return to the blueprint card — it should show the *"N documents added since this was confirmed"*
   banner. Confirm the banner is **calm and not amber**.
4. Click **Rebuild**. The diff should show your typed value against the document-derived one as a
   **contradiction**, **defaulted OFF**, side by side, with the source document named.
5. Click **Confirm WITHOUT ticking the contradiction.** Then re-open the profile card and confirm
   your typed `targetCustomer` is **unchanged**. That is D5 — the whole point of the phase.
6. Optional: rebuild again, tick the contradiction, confirm, and check that the blueprint changed
   while your typed profile text did not.

Also confirm: the blueprint appears in the vault as a normal document card, and DELETING it from the
vault leaves the app working (the cockpit just stops carrying the spine — it must not error).
  </how-to-verify>
  <resume-signal>Type "approved" or describe exactly what the agent said / what the diff showed</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the outcome and evidence both requirements</name>
  <files>docs/playbooks/vault.md, docs/playbooks/onboarding.md, docs/playbooks/cockpit.md, .planning/REQUIREMENTS.md</files>
  <action>
Record the L1 and L7 results verbatim in `docs/playbooks/vault.md` (what the agent actually said, not
a verdict) and bump `Last verified` on all three playbooks.

Update `.planning/REQUIREMENTS.md` — **append-only, keep-both on any merge conflict** (the
shared-singleton discipline in `.planning/PARALLELIZATION.md`):
- **BLPR-01** — evidence: the pure precedence + citation tests, the fail-closed synthesis, the
  refusal on a tenant with no tier row, the confirm gate, and the L2/L4 live numbers.
- **BLPR-02** — evidence: SEAM 1 (the no-tool-turn test + L1) and SEAM 2 (the `spine` field +
  `evaluations`/`voiceDoc` consumption + the three pinned non-regressions). **State plainly that the
  *"proposes a reviewable diff when new documents drift"* clause is met by automatic DETECTION plus a
  one-click rebuild and diff, NOT by an automatic trigger** — automatic Stage-2 triggering is
  deferred until a bulk-ingest completion event exists (15.2 Phase 2). Do not claim automation the
  phase did not ship.

Also record the two ceilings this phase deliberately accepted, so the next reader does not
rediscover them as bugs:
1. `unincorporated`'s bounded 100-row scan reads whole `vaultDocuments` rows on the hot path
   (Convex has no projection); the upgrade path is a stored counter incremented in `vaultIngest`,
   deferred because that file is 15.2's.
2. `voiceDoc`'s spine rides every `search_document` turn and realtime input tokens are re-billed per
   turn; the upgrade path is moving it to the session mint in `voiceToken.ts`.
  </action>
  <verify>
    <automated>test -z "$(echo '{}' | node scripts/check-playbooks.mjs check)" && grep -c "BLPR-0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The L1/L7 evidence is recorded verbatim; all three playbooks have `Last verified` bumped; both requirements carry evidence lines, and BLPR-02's explicitly disclaims automatic drift triggering.</done>
</task>

</tasks>

<verification>
- Every offline suite at its documented baseline; `check-playbooks` prints nothing.
- `business-blueprint` ACTIVE on the deployment at a recorded version number.
- L1-L7 all recorded in `docs/playbooks/vault.md` with numbers and quotes, not verdicts.
- The owner has approved the L1/L7 checkpoint.
</verification>

<success_criteria>
- The claims no test can make have been made by a real run and written down.
- The playbooks tell the next reader what the blueprint costs, what it drops, and what it does not
  do automatically.
- BLPR-01 and BLPR-02 both carry honest evidence, with BLPR-02's drift clause scoped correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-business-blueprint-corpus-synthesis-and-agent-spine/17.1-10-SUMMARY.md`
</output>
