---
phase: 17.1-business-blueprint-corpus-synthesis-and-agent-spine
plan: 10
status: blocked
audited: 2026-08-12
blocks:
  - 18-10
  - 18-09
---

# 17.1-10 live-gate audit

## Decision

`17.1-10` is **not closed**. The existing evidence must not be relabelled as a pass:

- L2-L5 have live evidence.
- L6 is **failed/inconclusive** after a repaired, Blueprint-bearing golden run timed out without
  certifying output.
- L1 and L7 have no owner observation or approval.
- `17.1-10-SUMMARY.md` does not exist.

Therefore Phase 18 plan `18-10` must stop at Task 1, and `18-09` cannot run because it depends on
`18-10`. No Phase 18 source or playbook edit is authorized by this audit.

## Reconstructed evidence

| Gate | Evidence | Classification | Result |
|---|---|---|---|
| L2 citation validity | Commit `99b2bef`: one owner-Vault rebuild returned 2 candidates across 8 source documents; 0 drops in every recorded reason bucket; both candidates were additions and the confirmed Blueprint was not changed. | Live measured evidence | Recorded, not a phase pass |
| L3 retrieval quality | Commit `99b2bef`: the revenue probe returned 4 mostly generic/irrelevant hits and missed the Northwind profile; the binding-constraint probe returned 5 hits including the profile and 2 directly relevant memos. | Product-quality defect/ceiling in retrieval, separate from the citation gate | Mixed; defect remains recorded |
| L4 cost | Commit `99b2bef`: the governed rebuild ledger delta was $0.01 on `openai/gpt-4o-mini`. | Live measured evidence | Recorded |
| L5 active skill | Commit `23b9dc4`: `business-blueprint@1` was read back ACTIVE on `local-joel_feruzi-pikar_ai_50c69-1`; its SHA-256 matched the LF-normalized canonical body. The owner tenant had a tier/profile row and 25 ready Vault documents. | Live deployment evidence | Passed at the recorded deployment/time |
| L6 golden eval, original premise | Commit `7eee6e3`: the golden tenant previously had no confirmed Blueprint, so an otherwise green run could not test the claimed spine behavior. The repair added an eval-only Blueprint seed plus a rendered-spine assertion before the paid fixture loop. | Confirmed product/harness defect, fixed in code | Fixed offline; live certification still required |
| L6 golden eval, repaired live run | Commit `2ce7e27`: the one authorized command timed out after 1808.1 seconds (exit 124), captured no runner stdout, and left processes alive. Three eval tenants prove the seed mutation ran, but no rendered `evalblpr` assertion, case totals, retry count, spend total, or evidence-row count was recovered. | Environment/runtime failure plus insufficient evidence | **RED/unknown** |
| L1 no-tool standing context | No qualifying product observation. The browser artifacts under `output/playwright/phase-17.1-uat` reach sign-in/loading/Google authentication only. | Owner-only visual/content judgment; insufficient evidence | Not run |
| L7 contradiction flow | No qualifying contradiction-diff observation or owner verdict. | Owner-only workflow/content judgment; insufficient evidence | Not run |

The browser console also records `onboarding:status` query timeouts and repeated authentication
attempts. Those are environment symptoms, not evidence that L1 or L7 passed or failed.

## Zero-spend audit checks run on 2026-08-12

1. `node packages/backend/scripts/run-eval-golden.mjs --self-check`
   - PASS: 36 fixtures valid; the Blueprint seed/assert-before-paid-loop invariant remains present.
2. Focused Blueprint/cockpit/vault seam tests
   - `blueprint.test.ts`: 33/33 passed.
   - `cockpitBlueprint.test.ts`: 8/8 passed.
   - `vaultGround.test.ts`: 16/16 passed.
   - `searchVaultSpine.test.ts` timed out once at its 20-second per-test ceiling during the combined
     four-file run, then passed 2/2 in 8.12 seconds when rerun alone.

These checks establish that the offline repair is intact. They cannot replace L6's paid live gate
or the owner's L1/L7 judgments.

## Recovery-readiness recheck on 2026-08-12

The zero-spend preflight was repeated from the shared checkout without starting Convex, seeding a
tenant, or invoking a model:

1. `node packages/backend/scripts/run-eval-golden.mjs --self-check`
   - PASS: the current corpus is **36 fixtures**, not the 35 recorded by the failed 2026-08-10 run.
   - The runner still pins `openai/gpt-4o-mini` and enforces `COST_CAP_USD = 2.0`.
2. Focused Blueprint/spine suites
   - `blueprint.test.ts`, `cockpitBlueprint.test.ts`, and `vaultGround.test.ts`: **57/57 passed**.
   - `searchVaultSpine.test.ts` alone: **2/2 passed**.
3. Wider offline gate
   - `@pikar/core`: **842/842 passed**.
   - `@pikar/contracts`: **31/31 passed**.
   - Serial `@pikar/backend` suite: exit **0**.
   - `@pikar/web` typecheck: exit **0**.
   - Backend typecheck: exit **2** only for three `calendar.test.ts` index-typing errors; there were
     **zero non-test TypeScript errors**. Calendar is outside this plan's ownership and was not
     changed.
4. Shared-checkout and process readiness
   - Port **3210 is free**. No Convex backend was started.
   - Process command-line enumeration was denied by Windows, so this recheck does not claim a
     stronger process-tree proof than the port observation.
   - `check-playbooks` is blocked only by the foreign untracked
     `apps/web/e2e/skill-authoring.spec.ts` requiring `docs/playbooks/cockpit.md`; no foreign file
     was changed to clear another lane's obligation.
   - The local `packages/backend/.env.local` currently names
     `dev:woozy-wren-368`, while the L5 evidence and failed L6 attempt name
     `local:local-joel_feruzi-pikar_ai_50c69-1`. A recovery command must therefore carry an explicit
     deployment target; relying on ambient configuration is unsafe.

This preflight authorizes no spend and closes no gate. It makes the next required authorization
numeric and bounded: **one** unfiltered 36-fixture L6 run, **USD 2.00 maximum total model spend**,
**no automatic full-run retry**, plus permission for its throwaway `eval-<8 hex>` tenant and
evaluation-evidence writes on the explicitly named deployment.

## Minimal checkpoint to resume

Two separate approvals/evidence steps are required, in this order.

### 1. Authorize one bounded L6 recovery run

This is a paid, live, mutating operation: it creates a throwaway `eval-<8 hex>` tenant, seeds live
RAG/Blueprint data, runs the full unfiltered golden corpus, and writes evaluation evidence. Do not
run it implicitly from this audit.

Prerequisites:

- explicit authorization for **one unfiltered 36-fixture run with a USD 2.00 maximum**, no
  automatic full-run retry, and the throwaway deployment/evidence writes;
- sufficient configured model balance;
- one stable local Convex backend on port 3210, with all stale Convex/eval process trees stopped;
- the configured deployment must explicitly be
  `local:local-joel_feruzi-pikar_ai_50c69-1` (the L5 evidence deployment), unless the owner names a
  different target; do not inherit the current cloud-dev value from `.env.local`;
- stdout/stderr must be captured continuously to a persistent log, with no 30-minute wrapper that
  can discard output while leaving the child process alive;
- record the run id, every case verdict, retries, total cost, rendered-spine assertion, and evidence
  row count before cleanup.

Free preflight from the repository root:

```powershell
node packages/backend/scripts/run-eval-golden.mjs --self-check
```

After authorization and a stable backend, the certifying command is the plan's full, unfiltered
gate (no `--only`, because filtered runs do not write certifying evidence):

```powershell
pnpm --filter @pikar/backend eval:golden
```

A timeout, missing stdout, missing rendered `evalblpr` proof, partial corpus, or unreconciled spend
keeps L6 RED/unknown. Do not retry automatically.

### 2. Owner performs L1 and L7

Only after L6 produces a conclusive pass, present Task 3 from `17.1-10-PLAN.md` verbatim. The owner
must inspect a real no-tool cockpit response for business-specific grounding (L1), then exercise
the real contradicting-document diff and confirm that declining the contradiction preserves the
typed `targetCustomer` (L7). Record what was observed verbatim; do not infer approval from browser
automation, screenshots, or silence.

## Phase 18 resume rule

Resume `18-10` only when all of the following exist:

1. conclusive L6 evidence;
2. explicit owner evidence for L1 and L7;
3. `17.1-10-SUMMARY.md` closing the plan without overstating either result.

Until then, do not add the `origin !== "agent"` drift exclusion: `18-10` explicitly protects the
live drift number from being changed before the 17.1 gate measures it. Because `18-09` depends on
`18-10`, it remains blocked as well.
