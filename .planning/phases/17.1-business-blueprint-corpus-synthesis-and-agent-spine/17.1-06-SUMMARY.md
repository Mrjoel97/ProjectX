---
phase: 17.1-business-blueprint-corpus-synthesis-and-agent-spine
plan: 06
subsystem: backend-cockpit-context
tags: [convex, cockpit, blueprint, prompt-assembly, tenant-isolation, fail-open]

# Dependency graph
requires:
  - phase: 17.1 plan 03
    provides: "The capped renderSpine business-blueprint context block"
  - phase: 17.1 plan 04
    provides: "Tenant-isolated, fail-open spineForTenant read plane"
provides:
  - "buildTurnPrompt as the one cockpit turn-prompt assembly, with a byte-identical null-spine fallback"
  - "A fail-open spineForTenant read on every model-backed cockpit turn, independent of tool use"
  - "VALIDATION item 24 through the real blueprint read/render chain plus a mutation-pinned topology scan"
affects: [17.1-07 grounding spine seam, 17.1-10 live gate, BLPR-02]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tenant standing context belongs in the turn prompt; the versioned registry skill remains the system prompt"
    - "A narrow internalAction test shim returns assembled input without widening a production return shape"

key-files:
  created:
    - packages/backend/convex/cockpitBlueprint.test.ts
  modified:
    - packages/backend/convex/llm.ts
    - packages/backend/convex/llmRedaction.test.ts
    - docs/playbooks/cockpit.md

key-decisions:
  - "Read the cockpit spine only after the no-model SMOKE path, then fail open so a blueprint fault costs context rather than the turn"
  - "Keep system: skill.body unchanged; tenant blueprint content is turn context and never part of the versioned cockpit-agent body"
  - "Keep BLPR-02 phase-level pending until plan 17.1-07 supplies SEAM 2 and the later plans close the drift clause"

patterns-established:
  - "One helper owns prompt ordering: spine, history, plan context, then the current user line"
  - "Raw-source guards strip comments and anchor buildTurnPrompt({ so definitions and documentation cannot create false passes"

requirements-completed: [BLPR-02]

# Metrics
duration: 29min
completed: 2026-07-29
---

# Phase 17.1 Plan 06: Standing Cockpit Blueprint Context Summary

**Every model-backed cockpit turn now receives the confirmed business blueprint before history and plan context, while missing or broken blueprint state preserves the exact legacy prompt.**

## Performance

- **Duration:** 29 min
- **Started:** 2026-07-29T21:23:18Z
- **Completed:** 2026-07-29T21:52:14Z
- **Tasks:** 2/2 (both TDD: RED commit → GREEN commit)
- **Files modified:** 4

## Accomplishments

- Added `buildTurnPrompt`, the one cockpit prompt assembly. A `null` spine contributes zero bytes;
  a live spine leads the prompt; bounded history and plan context follow; `The user says:` remains
  the final line.
- `runCockpitAgent` reads `internal.blueprint.spineForTenant` after the SMOKE short-circuit and
  fails open around that additive read. The tool set, step limit, production return shape and every
  `system: skill.body` line remain unchanged.
- Added `__cockpitTurnPrompt` plus VALIDATION item 24. The integration test drives
  `spineForTenant → liveForTenant → renderSpine`, proves a no-tool turn carries the business name,
  pins the no-blueprint bytes, verifies tenant isolation and a deleted-document pointer, and
  requires exactly two shared-helper call sites.

## Task Commits

Each TDD phase was committed atomically:

1. **Task 1 RED: pin the one production prompt assembly** - `c707b63`
2. **Task 1 GREEN: inject the fail-open cockpit spine** - `6299169`
3. **Task 2 RED: prove standing context through the real read chain** - `260ab22`
4. **Task 2 GREEN: add the narrow prompt shim and mutation guard** - `276eaf7`

## Files Created/Modified

- `packages/backend/convex/llm.ts` - Private prompt helper, production spine read, and the
  prompt-return test shim.
- `packages/backend/convex/cockpitBlueprint.test.ts` - VALIDATION item 24 behavior and
  mutation-verifiable raw-source topology guard.
- `packages/backend/convex/llmRedaction.test.ts` - Production-only one-helper and exact
  null-fallback source contract.
- `docs/playbooks/cockpit.md` - SEAM 1 ordering, independence, fail-open and system-prompt
  invariants.

## Decisions Made

- Kept the blueprint entirely in the turn prompt. Putting tenant content in `system` would make the
  versioned `cockpit-agent` row stop describing what the model saw.
- Queried the spine after the SMOKE return path, so deterministic no-model operations do not pay
  for standing context they cannot consume.
- Used a dedicated internalAction shim instead of widening `runCockpitAgent`'s production return
  shape with test-only prompt content.
- Left the global `BLPR-02` requirement pending. This plan completes SEAM 1, while plan 17.1-07
  still owns SEAM 2 and later plans own the reviewable drift portion of the requirement.

## Deviations from Plan

None - plan executed exactly as written.

## Mutation Check

- Replaced production's `spineForTenant` assignment with `spine = null`.
- `cockpitBlueprint.test.ts` went **1 RED / 4 green** at the production read/topology assertion.
- The pre-existing `runCockpitAgent.test.ts` suite remained **24/24 green**, proving the new test
  adds the missing seam coverage rather than changing the old harness.
- Restoring the query returned `cockpitBlueprint.test.ts` to **5/5 green**.

## Verification

- `pnpm --filter @pikar/backend test runCockpitAgent --maxWorkers=1` — **24/24 passed**
- `pnpm --filter @pikar/backend test llmRedaction --maxWorkers=1` — **43/43 passed**
- `pnpm --filter @pikar/backend test cockpitBlueprint --maxWorkers=1` — **5/5 passed**
- `pnpm --filter @pikar/backend test --maxWorkers=1` — **50 files, 761/761 passed**
- `pnpm --filter @pikar/backend typecheck` filtered to non-test files — **0 production errors**
- `echo '{}' | node scripts/check-playbooks.mjs check` equivalent — **no output**
- Scope guard — **no `vaultGround.ts`, `evaluations.ts` or `voiceDoc.ts` changes**
- System-prompt diff guard — **0 changed `system: skill.body` lines from `821382f`**

## Issues Encountered

- The plan text refers to 18 `system: skill.body` call sites, while the branch baseline contains
  17. No call site was added, removed or edited; the stronger diff guard proves the entire system
  prompt surface is unchanged from the requested base.
- The full suite prints existing asynchronous component-registration and AI SDK warnings to
  stderr, but reached a fully green Vitest summary: 50 files and 761 tests passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 17.1-07 can add the independent `vaultGroundHydrated.spine` field for evaluation and
  voice-document callers without touching this cockpit seam.
- Plan 17.1-10 still owns the real-deployment no-tool cockpit proof and golden-eval gate.

## Self-Check: PASSED

All five required paths exist and commits `c707b63`, `6299169`, `260ab22`, and `276eaf7` resolve
as commits on this branch.

---
*Phase: 17.1-business-blueprint-corpus-synthesis-and-agent-spine*
*Completed: 2026-07-29*
