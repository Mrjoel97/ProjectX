---
phase: 22-owner-authorization-primitive-requireowner
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - packages/backend/convex/optimizerConfig.ts
  - packages/backend/convex/optimizerConfig.test.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/convex/importGuard.test.ts
  - docs/playbooks/authorization.md
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [GOVN-01]

must_haves:
  truths:
    - "A non-owner cannot read global optimizer state, change the kill switch, read candidate bodies, or activate/roll back a skill."
    - "Every non-owner refusal occurs before a config write, status flip, or candidate-body return."
    - "The owner path still uses the single optimizer row and the single shared `activateSkillVersion` EVAL_GATE."
    - "Internal CI/eval/seeding/operator functions remain callable without a browser identity and retain their existing gate semantics."
  artifacts:
    - path: "packages/backend/convex/optimizerConfig.ts"
      provides: "Owner-wrapped public global-config read and kill-switch mutation"
      contains: "ownerMutation"
    - path: "packages/backend/convex/skills.ts"
      provides: "Owner-wrapped candidate-body query and candidate activation"
      contains: "ownerQuery"
    - path: "packages/backend/convex/importGuard.test.ts"
      provides: "Static named-endpoint wrapper regression guard"
      contains: "ownerQuery"
    - path: "packages/backend/convex/skills.test.ts"
      provides: "Owner/non-owner disclosure, state-immutability, rollback, and EVAL_GATE proofs"
  key_links:
    - from: "optimizerConfig.setOptimizerEnabled"
      to: "requireOwner"
      via: "ownerMutation executes authorization before writeConfig"
      pattern: "setOptimizerEnabled = ownerMutation"
    - from: "skills.activateCandidate"
      to: "activateSkillVersion"
      via: "ownerMutation adds authorization without duplicating or bypassing EVAL_GATE"
      pattern: "await activateSkillVersion"
    - from: "skills.candidatesForReview"
      to: "candidate skill bodies"
      via: "ownerQuery rejects before any global registry rows are read or returned"
      pattern: "candidatesForReview = ownerQuery"
---

<objective>
Put the durable owner boundary in front of every known Phase-8 global control while preserving the
internal optimizer/eval machinery.

Purpose: authentication alone currently exposes a global kill switch, global candidate prompt
bodies, and live skill activation to every signed-in tenant. The substrate from 22-01 lets these
functions fail before their handlers execute.

Output: four owner-wrapped public endpoints, real owner/non-owner Convex tests, a static wrapper
guard, and updated authorization/skill-registry operational guidance.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/22-owner-authorization-primitive-requireowner/22-RESEARCH.md
@.planning/phases/22-owner-authorization-primitive-requireowner/22-VALIDATION.md
@.planning/phases/22-owner-authorization-primitive-requireowner/22-01-SUMMARY.md
@packages/backend/convex/optimizerConfig.ts
@packages/backend/convex/optimizerConfig.test.ts
@packages/backend/convex/skills.ts
@packages/backend/convex/skills.test.ts
@packages/backend/convex/importGuard.test.ts
@docs/playbooks/authorization.md
@docs/playbooks/skill-registry.md

<interfaces>
Plan 22-01 exports:

```ts
export const ownerQuery = customQuery(query, customCtx(requireOwner));
export const ownerMutation = customMutation(mutation, customCtx(requireOwner));
```

The shared helpers are deliberately unchanged:

```ts
// optimizerConfig.ts
async function getConfig(ctx: QueryCtx) { ... }
async function writeConfig(ctx: MutationCtx, args: ConfigPatch): Promise<void> { ... }
export const getOptimizerConfig = internalQuery(...);
export const setOptimizerConfig = internalMutation(...);

// skills.ts
async function activateSkillVersion(ctx: MutationCtx, name: string, version: number): Promise<void>
export const activateSkill = internalMutation(...);
```

Owner tests must create real auth rows:

```ts
const userId = await t.run((ctx) => ctx.db.insert("users", { owner: true }));
const asOwner = t.withIdentity({ subject: `${userId}|session_a` });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Owner-gate the optimizer read and kill switch before the handler</name>
  <files>packages/backend/convex/optimizerConfig.ts, packages/backend/convex/optimizerConfig.test.ts</files>
  <behavior>
    - A real `owner: true` user reads the dormant default and can flip the one shared config row.
    - A real user with absent or false owner receives `OWNER_REQUIRED` from
      `getOptimizerStatus` and `setOptimizerEnabled`.
    - A refused read returns no global config; a refused write leaves the table absent, and a
      refusal against an existing row leaves every field byte-for-byte unchanged.
    - An unauthenticated call still throws `UNAUTHENTICATED`.
    - `getOptimizerConfig` and `setOptimizerConfig` remain internal and work with no identity.
  </behavior>
  <action>
Import `ownerQuery` and `ownerMutation` from `lib/functions.ts`. Change only the two public ops
wrappers:

- `getOptimizerStatus`: `tenantQuery` -> `ownerQuery`;
- `setOptimizerEnabled`: `tenantMutation` -> `ownerMutation`.

Do not put authorization inside `getConfig` or `writeConfig`; the internal eligibility and CI
functions intentionally share those helpers without a browser identity. Update misleading comments
that equate authentication with ownership.

Rewrite the public-wrapper tests to insert actual `users` rows and authenticate with subjects built
from their ids. Add non-owner tests for both read and write, including zero-write and existing-row
immutability assertions. Keep the internal single-row/default/cooldown tests unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/optimizerConfig.test.ts convex/owner.test.ts --maxWorkers=1</automated>
  </verify>
  <done>The public global optimizer read/write reject every non-owner before disclosure or mutation,
  while the internal CI/eligibility seams and one-row behavior remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner-gate candidate bodies and activation without moving the EVAL_GATE</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - An owner can list candidate before/after bodies and activate a candidate carrying exact
      passing evidence.
    - A non-owner query rejects with `OWNER_REQUIRED` and receives no candidate bodies.
    - A non-owner activation or rollback attempt rejects and leaves active/candidate/archived
      statuses unchanged.
    - An owner still cannot activate an unevaluated gated candidate: `EVAL_GATE` remains the
      refusal and no status changes.
    - `internal.skills.activateSkill`, seeding, evidence recording, and active-skill reads still
      work without browser identity; rollback remains evidence-exempt by target status.
  </behavior>
  <action>
Replace the public wrappers only:

- `activateCandidate`: `tenantMutation` -> `ownerMutation`;
- `candidatesForReview`: `tenantQuery` -> `ownerQuery`.

Keep `activateSkillVersion` the single status-flip helper. Never move `requireOwner` into it: doing
so would break trusted internal eval/seeding/operator callers and conflate authorization with the
independent evidence gate. Do not copy the candidate body or evidence prose into an audit record.

Convert the old `"owner_a"` authenticated-only fixtures to real inserted users. Cover absent/false
owner and owner true. For every refused activation, snapshot and then assert all relevant skill
statuses. Add a public rollback attempt by a non-owner and retain an internal rollback success with
no evidence. For the disclosure query, the rejection itself is the no-body boundary; anchor it with
an owner query that proves the fixture really contains the expected before/after bodies.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts convex/owner.test.ts --maxWorkers=1</automated>
  </verify>
  <done>Candidate prompt bodies and every public status transition are owner-only, and the shared
  internal EVAL_GATE/rollback semantics remain exactly one implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add a named-endpoint wrapper guard and record the internal/public boundary</name>
  <files>packages/backend/convex/importGuard.test.ts, docs/playbooks/authorization.md, docs/playbooks/skill-registry.md</files>
  <behavior>
    - A source guard fails if any of `getOptimizerStatus`, `setOptimizerEnabled`,
      `activateCandidate`, or `candidatesForReview` is declared with a tenant wrapper.
    - The guard proves each expected source file and function declaration was actually found, so
      a rename/removal cannot make it pass vacuously.
    - The playbooks state that owner authorization and skill evidence are independent gates and
      that internal machine seams remain internal.
  </behavior>
  <action>
Extend the existing eager raw-source scan in `importGuard.test.ts` with a small explicit table:

```ts
[
  ["optimizerConfig.ts", "getOptimizerStatus", "ownerQuery"],
  ["optimizerConfig.ts", "setOptimizerEnabled", "ownerMutation"],
  ["skills.ts", "activateCandidate", "ownerMutation"],
  ["skills.ts", "candidatesForReview", "ownerQuery"],
]
```

Assert that each named export is declared with its expected wrapper. Use an escaped declaration
pattern narrowly anchored to `export const <name> = <wrapper>(`; do not attempt to infer every
future "admin-ish" semantic with regex. The durable future rule belongs in code review and the
authorization playbook.

Update `authorization.md` with the four protected functions and the rule that a global/admin-like
client function starts with an owner wrapper. Update `skill-registry.md` to replace the old
"authenticated identity is owner" description: public activation is
`owner authorization AND EVAL_GATE`, public rollback is owner-authorized but evidence-exempt by
status, and internal activation remains trusted plus EVAL_GATE. Bump each playbook's `Last
verified` line only after its watched changes and checks are actually read.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/importGuard.test.ts convex/optimizerConfig.test.ts convex/skills.test.ts --maxWorkers=1 &amp;&amp; node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The current four endpoints are structurally pinned to owner wrappers, and the playbooks
  preserve the distinction between owner authorization, evidence gating, and internal trust.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/owner.test.ts convex/optimizerConfig.test.ts convex/skills.test.ts convex/importGuard.test.ts --maxWorkers=1`
- `pnpm --filter @pikar/backend test -- --maxWorkers=1`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`
- Mutation check: move one endpoint back to its tenant wrapper; both its static source guard and
  behavioral non-owner test must turn red, then revert and confirm green.
- Mutation check: move authorization below `writeConfig` or `activateSkillVersion`; the
  zero-mutation/status-immutability test must turn red, then revert and confirm green.
</verification>

<success_criteria>
- All known global Phase-8 public controls reject non-owners server-side.
- Candidate prompt bodies cannot be disclosed to a non-owner.
- Refused writes cannot mutate global config or skill status.
- Internal CI/eval/seeding seams and the one shared EVAL_GATE remain operational.
</success_criteria>

<output>
After completion, create
`.planning/phases/22-owner-authorization-primitive-requireowner/22-02-SUMMARY.md`, recording the
owner/non-owner matrix, the static guard result, internal compatibility checks, and both observed
mutation reds.
</output>
