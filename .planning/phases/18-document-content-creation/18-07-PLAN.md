---
phase: 18-document-content-creation
plan: 07
type: execute
wave: 5
depends_on: ["18-02", "18-04", "18-06"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/app/(app)/dashboard/vault/DocGrid.tsx
  - apps/web/e2e/cockpit-created-document.spec.ts
autonomous: true
requirements: [ACTN-04]

must_haves:
  truths:
    - "A created artifact is SEEN in the conversation: an Output card renders with a title, an UPPERCASE type badge read off the row's own form field, a subline and the rendered artifact snippet."
    - "A turn that created nothing renders nothing — the card returns null, like SourceCard."
    - "A user can tell at a glance what they gave the system from what it wrote: the vault grid marks agent-authored documents with an AGENT chip."
    - "The card never uses the amber --held token, which belongs to the approval gate alone."
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "OutputCard — a self-querying dumb renderer over vaultSources.byThread({ role: 'created' }), badging off row.form"
      contains: "OutputCard"
    - path: "apps/web/app/(app)/dashboard/vault/DocGrid.tsx"
      provides: "the AGENT provenance chip beside StatusChip"
      contains: "origin"
    - path: "apps/web/e2e/cockpit-created-document.spec.ts"
      provides: "the data-testid=output-card presence assertion (SC6 partial)"
      min_lines: 20
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx OutputCard"
      to: "api.vaultSources.byThread"
      via: "useQuery with { role: 'created' } — the existing SourceCard call passes nothing and is unchanged"
      pattern: "role: \"created\""
    - from: "apps/web/app/(app)/dashboard/vault/DocGrid.tsx"
      to: "vaultDocuments.origin"
      via: "doc.origin !== undefined renders the AGENT chip"
      pattern: "doc\\.origin"
---

<objective>
Make the created artifact VISIBLE — the Output card in the conversation (SC#6) and the provenance
chip in the vault grid.

Purpose: SC#6 is the only success criterion that is about the user's eyes. `docs/design/BRAND.md:101-102`
specifies an Output card today and nothing implements it. And CONTEXT.md locks that *the vault UI must
visually distinguish agent-created from user-uploaded documents* — a user has to tell at a glance what
they gave the system from what it wrote.
Output: Pattern 8, plus the SC#6 e2e handle. Zero new tables, zero new queries, zero new routes.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-04-SUMMARY.md
@.planning/phases/18-document-content-creation/18-06-SUMMARY.md
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@apps/web/app/(app)/dashboard/vault/DocGrid.tsx
@apps/web/app/globals.css

**CLAUDE.md §10 is binding: read `docs/design/BRAND.md` (especially `:85-120`) and the real product
screenshots in `docs/design/brand/` BEFORE writing any markup.** Use the `globals.css` CSS variables;
never hardcode a hex a token covers. **The app deliberately has no component library — do not add one.**

**Verified anchors at HEAD (2026-08-01):**
- `SourceCard` — `cards.tsx:1251-1285`. THE template: a self-querying dumb renderer over a per-thread
  content-plane row that returns `null` when the turn produced nothing. Its click-through to
  `/dashboard/vault` is at `:1274-1280`.
- `briefingSheet`, `capsTeal`, `traceText` — existing style helpers in `cards.tsx`. REUSE them.
- `StatusChip` in `DocGrid.tsx:66-80` — the chip to sit beside.
- `PreviewModal.tsx:98` — `canDownload = Boolean(doc.storageId)`. Already wired to
  `vault.vaultDownloadUrl` (`vault.ts:318`). **Nothing to build for download.**
- `VERB` map `cards.tsx:1139-1178` — plan 18-02 already added the `createDocument` entry. Do NOT
  touch it here; do NOT touch `traceParity.test.ts`.
- `api.vaultSources.byThread` returns a `Doc<"vaultSources">`. After plans 18-02/18-04 that row is
  `{ tenantId, threadId, docIds, titles, count, createdAt, role?, snippet?, form? }`. **`form` is the
  type badge's only data source** — `vaultDocuments.kind` (`created_document`/`created_content`) is
  never read by `byThread`, so do not reach for it.

**BRAND constraints (non-negotiable):**
- Titled card on the `--card` sheet, an UPPERCASE type badge pill (`DOCUMENT` / `POST`), a subline,
  and the rendered artifact (the `snippet`), with tracked-caps labels (BRAND §3/§4).
- **NEVER amber.** `--held` belongs to the approval gate alone (BRAND §2). A created artifact is not
  held.
- Label text carries the meaning, never colour alone (BRAND §6). The AGENT chip must READ "AGENT".
</context>

<tasks>

<task type="auto">
  <name>Task 1: OutputCard — the artifact is seen (SC6)</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    Add an `OutputCard` component modelled line-for-line on `SourceCard` (`:1251-1285`): a
    self-querying dumb renderer that calls `useQuery(api.vaultSources.byThread, { threadId,
    role: "created" })` and returns `null` when there is no row. Mount it where `SourceCard` mounts,
    in the same turn-scoped position.

    ⚠ The EXISTING `SourceCard` `useQuery` call passes **no** `role` and must stay byte-unchanged —
    that is what keeps grounding cards working (plan 18-04 made `role` optional on `byThread`).

    Render, per BRAND:
    - the card title (`titles[0]`, or a count-aware heading when `count > 1` — multiples per turn are
      locked as allowed);
    - an UPPERCASE type badge pill — `DOCUMENT` when `row.form === "long"`, `POST` when
      `row.form === "short"`. **`form` is an optional field on the `vaultSources` row** (schema:
      plan 18-02; `insert` arg: plan 18-04; written by the tool: plan 18-06) precisely so this badge
      needs no second query and no lookup. Fall back to `DOCUMENT` when `form` is absent (rows
      written before this phase). ⛔ Do NOT add a schema field here, do NOT add a second `useQuery`,
      and do NOT guess from `count`;
    - a subline;
    - the `snippet` as the rendered artifact preview;
    - a click-through to `/dashboard/vault` — the `SourceCard:1274-1280` context-sanctioned pattern.
      **Do NOT build an inline PreviewModal here**; that is the same deferred upgrade already
      recorded at `SourceCard`, and CONTEXT.md locks *"do not invent a second way to display an
      artifact"*.
    - `data-testid="output-card"` on the card root (Task 3 asserts it).

    Reuse `briefingSheet` / `capsTeal` / `traceText`. ⛔ No amber, no `--held`. ⛔ No new component
    library, no new dependency. ⛔ No new Convex query — `byThread` already exists.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit && pnpm --filter @pikar/web exec biome check "app/(app)/dashboard/workspace/cards.tsx"</automated>
  </verify>
  <done>`OutputCard` exists, returns `null` for a turn with no created row, renders title + UPPERCASE badge (off `row.form`) + subline + snippet on the `--card` sheet with `data-testid="output-card"`; `SourceCard`'s `useQuery` call has zero diff; web typecheck and Biome are clean; `grep -c "held" ` on the new component is 0.</done>
</task>

<task type="auto">
  <name>Task 2: The AGENT provenance chip in the vault grid</name>
  <files>apps/web/app/(app)/dashboard/vault/DocGrid.tsx</files>
  <action>
    Add an "AGENT" chip beside `StatusChip` (`DocGrid.tsx:66-80`), gated on `doc.origin !== undefined`
    (so both `"agent"` and `"agent_promoted"` show it). Match the existing chip's markup and token
    usage exactly — read `StatusChip` first. Label text carries the meaning (BRAND §6); do not rely
    on colour alone, and do not use `--held`.

    `listVaultDocs` (`vault.ts:270-279`) already `.collect()`s every row with no kind/status/origin
    filter, so created documents already appear in this grid. **Nothing else is needed** — do not add
    a query, a filter, or a parallel management UI (rename/delete reuse the existing vault UI, locked).

    Add a `ponytail:` comment on the grid's filter noting the known ceiling: **`vault.vaultSearch` is
    the same rag primitive as grounding, so created documents will NOT appear in the vault UI's
    search box** (they are deliberately never ingested — that IS the retrieval exclusion). Name the
    upgrade path: a ~3-line title-substring fallback in this filter. Accepted for beta; it is an
    explicit owner question in plan 18-09's gate. **Do NOT solve it by ingesting.**
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit && pnpm --filter @pikar/web exec biome check "app/(app)/dashboard/vault/DocGrid.tsx"</automated>
  </verify>
  <done>An AGENT chip renders for any row with `origin` set and for no other row; web typecheck and Biome clean; the ponytail ceiling comment is present; zero new queries.</done>
</task>

<task type="auto">
  <name>Task 3: Author the SC6 e2e spec (the RUN is plan 18-09's precondition)</name>
  <files>apps/web/e2e/cockpit-created-document.spec.ts</files>
  <action>
    Add a Playwright spec modelled on the existing `apps/web/e2e/cockpit-attachment.spec.ts` (read it
    first — it is the closest shipped analogue, and it already solves auth via `auth.setup.ts`).
    It asserts that after a turn that created a document, `[data-testid="output-card"]` is present
    and its text contains the artifact title.

    **Drive the turn with the SMOKE sentinel plan 18-06 registered:**

    ```
    SMOKE::agent::create=long:SMOKE::route=direct_llm:: Quarterly one-pager
    ```

    ⚠ **The nested `SMOKE::route=direct_llm::` prefix lives INSIDE the topic and is load-bearing — do
    not "clean it up".** `SMOKE::agent::create=` only selects the tool; it does NOT keep the model out
    of the loop. `draftDocument` calls `generateObject` unless the safeText it receives ITSELF starts
    with `SMOKE::route=…::` (`parseSmoke`, `llm.ts:155-157`, is `^`-anchored). `apps/web/e2e` runs
    with no `OPENAI_API_KEY`, so a bare topic makes the real call throw, the tool returns a failure
    sentence, NO `vaultDocuments`/`vaultSources` row is written, and `[data-testid="output-card"]`
    never renders. The shipped precedent is exact — `cockpit-attachment.spec.ts:21` is
    `const ATTACH = "SMOKE::agent::attach=SMOKE::route=direct_llm:: Quarterly proposal"`.

    ⚠ **There is no other way.** e2e runs with **no gateway key** — that is the entire reason the
    `SMOKE::agent::` grammar exists. `cockpit-attachment.spec.ts` drives everything through
    `SMOKE::agent::attach=…`. Read the exact sentinel string out of `18-06-SUMMARY.md` (18-06 is
    required to record it in the nested form); if the `create=` op is not in `llm.ts` at all four
    sites, **STOP** — the spec cannot pass and the fix is plan 18-06's, not yours.

    ⚠ **This is SC#6 PARTIAL and must be labelled as such in the spec's own comment.** An e2e can
    assert the testid exists; it **cannot** assert that the card looks like BRAND §5's Output card or
    that it reads well. That judgement is human and is a Manual-Only row in `18-VALIDATION.md`,
    executed in plan 18-09.

    **PRECONDITIONS FOR ACTUALLY RUNNING IT — read before you try, and do NOT try to satisfy them
    here** (all verified at HEAD):
    - `apps/web/playwright.config.ts` pins `baseURL: "http://127.0.0.1:3111"` and has **NO
      `webServer` block** — it drives an ALREADY-RUNNING stack. `next start` defaults to **:3000**,
      so the commonly-copied `build && start` line points Playwright at a port nothing is serving
      (`ERR_CONNECTION_REFUSED` before any assertion runs). If you start a production build, pin the
      port: `pnpm --filter @pikar/web start -- -p 3111`.
    - a live `convex dev` (**not** `--once`) — the SMOKE path needs the local backend.
    - `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` for a user seeded in that live deployment;
      `auth.setup.ts` throws without them. **An executor cannot mint these.**

    Because of those, **this task authors the spec; plan 18-09's phase gate RUNS it** (it already
    stands up a deployment, a seeded registry and a production build). Do not mark the spec "passing"
    on a run you did not do.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test e2e/cockpit-created-document.spec.ts --list && pnpm --filter @pikar/web exec tsc --noEmit && grep -c "output-card" apps/web/e2e/cockpit-created-document.spec.ts</automated>
  </verify>
  <done>`--list` discovers the spec (it parses, compiles and is picked up by the chromium project with its `setup` dependency); web `tsc --noEmit` is clean; the spec asserts BOTH the testid AND the title text (a testid-only assertion is vacuous) and sends the `create=` SMOKE sentinel; its comment states plainly that the BRAND judgement is manual and that the live run is plan 18-09's precondition.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web exec tsc --noEmit` — exit 0.
- `pnpm --filter @pikar/web build` — production build green (this is the gate that catches the workspace page).
- `pnpm --filter @pikar/web exec playwright test e2e/cockpit-created-document.spec.ts --list` — the
  spec is discovered. The actual RUN needs a live `convex dev` + `next` on **:3111** +
  `E2E_USER_EMAIL`/`E2E_USER_PASSWORD`, and is a precondition of plan 18-09's phase gate.
- `pnpm --filter @pikar/web exec biome check "app/(app)/dashboard/workspace/cards.tsx" "app/(app)/dashboard/vault/DocGrid.tsx"` — clean.
  ⚠ **`pnpm --filter @pikar/web exec` sets cwd to `apps/web`**, so paths are RELATIVE to `apps/web`.
  A repo-root-relative `apps/web/app/...` path resolves to nothing and biome exits **non-zero** with
  *"No files were processed in the specified paths"* — a RED that has nothing to do with your code.
  From the repo root the other form works: `pnpm exec biome check "apps/web/app/(app)/..."`.
- `git diff --stat apps/web/app/\(app\)/dashboard/workspace/cards.tsx` shows ONLY the `OutputCard` addition — the `VERB` map from plan 18-02 and `SourceCard` are untouched.
- `grep -n "\-\-held\|#[0-9a-fA-F]\{6\}" ` on both touched files — no amber token, no hardcoded hex a `globals.css` variable covers.
</verification>

<success_criteria>
- SC#6 (automated half): a created artifact renders in an Output card, not only as a silent vault row; the card is reachable by `data-testid` in a spec that is authored and discoverable here, and executed at plan 18-09's gate.
- Agent-authored documents are visually distinguishable from user uploads in the vault grid.
- Zero new tables, zero new queries, zero new routes, zero new dependencies, no component library added.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-07-SUMMARY.md`.
Record: the exact badge/subline copy used, which `globals.css` tokens were used, the SMOKE sentinel
string the spec sends, and an explicit statement that **both** the BRAND conformance judgement
(SC#6's human half) **and the Playwright run itself** are still OWED and belong to plan 18-09.
</output>
