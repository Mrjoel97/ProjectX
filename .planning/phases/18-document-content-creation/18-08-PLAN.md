---
phase: 18-document-content-creation
plan: 08
type: execute
wave: 6
depends_on: ["18-03", "18-06"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
autonomous: false
requirements: [ACTN-04]

must_haves:
  truths:
    - "The active cockpit-agent body teaches createDocument — the tool exists to the model, not just to the codebase."
    - "The body carries the locked trigger rule: explicitly asked -> create directly; agent-suggested -> confirm first."
    - "The body teaches short vs long and revise-by-#index."
    - "The .md and its one-line mirror are byte-identical, so skills.test.ts stays green."
    - "No candidate carrying another lane's un-evaluated prose is minted."
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "one new ## section teaching createDocument"
      contains: "createDocument"
    - path: "packages/contracts/src/skills/cockpitAgent.ts"
      provides: "the regenerated one-line mirror on :8"
      contains: "createDocument"
  key_links:
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "packages/contracts/src/skills/cockpitAgent.ts:8"
      via: "byte-identity, asserted by skills.test.ts:735 — editing the .md alone turns that row RED"
      pattern: "cockpit-agent\\.md"
---

<objective>
Teach the active `cockpit-agent` body about `createDocument`, and regenerate its one-line mirror in
the same commit.

Purpose: **a tool the body does not teach is a tool that does not exist.** This repo has been bitten
by the withheld-tool pattern twice — at RPLY-01, and again at 16-09 for ~$0.46 of paid diagnosis.
Everything built in plans 18-01 through 18-07 is invisible to the model until this lands and the
resulting candidate is activated.

⚠ **PARKED. Waves 1-5 of Phase 18 are runnable TODAY; this plan is not.** At HEAD, `STATE.md`
records Phase 16 at 8/9 (16-09's live eval unrun, no `OPENAI_API_KEY` securely available) and
`REQUIREMENTS.md` has `ACTN-03` unchecked. Task 1's checkpoint will therefore say **blocked** if run
now. That is by design and it is why this plan is `autonomous: false` with a BLOCKING checkpoint
rather than a silent `stop and report` inside an autonomous plan. Plan 18-10 is the other parked
plan (behind `17.1-10`); the two parks are INDEPENDENT and both must clear before plan 18-09's
phase gate can run.

⚠ **THIS PLAN IS THE PHASE'S EXECUTION BLOCKER.** `cockpit-agent` is a `GATED_SKILLS` singleton with
exactly ONE candidate stream, and Lane R holds candidate **v16 un-activated**. A Phase-18 edit landing
while that candidate is open mints a candidate carrying **both lanes' prose**, and whichever lane
evals next certifies instructions it never tested — a correctness failure INSIDE the skill registry,
not a merge conflict a rebase fixes.
Output: Registration Checklist rows 11 and 12.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/PARALLELIZATION.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-06-SUMMARY.md
@packages/contracts/skills/cockpit-agent.md
@packages/contracts/src/skills/cockpitAgent.ts
@docs/playbooks/skill-registry.md

**Read 18-RESEARCH.md Pitfall 1 and § Registration Checklist rows 11/12/15, and
`.planning/PARALLELIZATION.md` § *Phases 18 + 19* (THE HARD RULE) before touching anything.**

**Verified anchors at HEAD (2026-08-01):**
- `packages/contracts/src/skills/cockpitAgent.ts` — the single escaped literal on **`:8`**. The
  5-line "AUTO-DERIVED" header is `:1-5`. **There is NO generator script** in this repo and this plan
  does not add one — regenerate MECHANICALLY with the throwaway one-liner in Task 2.
  ⚠ `packages/contracts/skills/cockpit-agent.md` is **26,167 bytes**. Hand-escaping that to
  byte-identity against `skills.test.ts:735` is a multi-iteration red loop, and this is the plan
  parked behind a paid Phase-16 gate — the least affordable place in the phase to burn cycles.
- `packages/backend/convex/skills.test.ts:735` — `["cockpit-agent.md", cockpitAgentSkillBody]`
  asserts LF-normalized byte-identity. **Editing the `.md` without the mirror turns that row RED.**
  This is the surface most often forgotten.
- `seedSkills` — `packages/backend/convex/skills.ts:334-355`: compares the file body against the
  **NEWEST** row (not the active one) and mints `maxVersion + 1` on any difference.
- `COCKPIT_AGENT_SKILL` is in `GATED_SKILLS` (`packages/contracts/src/skill.ts:41`, listed `:170-176`).
- Recorded state at research time: `cockpit-agent@15` is ACTIVE and contains zero mentions of the
  research tool; candidate **v16** exists and is un-activated; Phase 16's gate has run TWICE, both
  RED ($0.4376 spent, nothing activated).

⛔ **`packages/contracts/skills/cockpit-agent.md` is CLEAN at HEAD** (last touched by `68afb7b`), so
the contested state is **invisible in `git status`**. Verify against the LIVE Convex `skills` table,
never against the working tree.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — confirm the gated candidate stream is free</name>
  <files>(none — this task changes no file; it produces the go/no-go observation Task 2 depends on)</files>
  <action>
STOP and read the LIVE Convex `skills` table before touching `cockpit-agent.md`. There is nothing to
automate first: the whole point is that the contested state is invisible in the working tree, so no
local command can answer this. Run the reads below, present the output, and WAIT. Do not start Task 2
until the answer is "clear to edit".
  </action>
  <what-built>
    Nothing yet. This gate exists because the next task edits a GATED singleton that another lane
    holds, and the contested state is invisible in `git status`.
  </what-built>
  <how-to-verify>
    Run these and paste the output:

    1. `cd packages/backend && npx convex data skills --limit 100` (or
       `npx convex run skills:listForOwner` if that read exists) — filter for `name = "cockpit-agent"`.
       Report: the ACTIVE version number, and whether ANY row has `status: "candidate"`.
    2. Confirm Phase 16 is closed: `ACTN-03` is marked Complete in `.planning/REQUIREMENTS.md`, and
       `.planning/STATE.md`'s Lane R row says the eval gate PASSED with a recorded run id, per-case
       verdicts, total cost and the active `research-specialist` version.

    **PROCEED only if BOTH are true:**
    - there is NO `status: "candidate"` row for `cockpit-agent`, and
    - Phase 16 is closed with a recorded PASSING run.

    **STOP if either is false.** Do not read *"the gate ran"* as *"the gate passed"* — it ran twice
    and failed twice. Budget the remaining Phase-16 gate honestly (~$0.83 clean / ~$1.06 with one
    research retry) and note that the runner UNDERSTATES research-fixture cost by ~10×; cross-check
    against `npx convex data audit` (free) before believing any cost figure.
  </how-to-verify>
  <resume-signal>Paste the live `cockpit-agent` rows and the Phase-16 status, then type "clear to edit" or "blocked".</resume-signal>
  <verify>
    <automated>MANUAL — live Convex `skills` table read. No offline command can see a candidate row; `cockpit-agent.md` is CLEAN at HEAD so `git status` shows nothing. Recorded as a Manual-Only row in 18-VALIDATION.md.</automated>
  </verify>
  <done>The live `cockpit-agent` rows are recorded (active version + candidate presence), Phase 16 is confirmed closed with a PASSING run, and the owner has said "clear to edit". If either condition failed, execution STOPS here and the phase reports blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the tool — one ## section + the regenerated mirror, ONE commit</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts</files>
  <action>
    Add **ONE** `##` section to `packages/contracts/skills/cockpit-agent.md`. Read the neighbouring
    tool sections first and match their structure, length and imperative register exactly — this body
    is a gated artifact and prose bloat costs a paid eval. Content, and nothing beyond it:

    1. **What `createDocument` is:** it writes a standalone document or piece of content and saves it
       to the user's vault. **It saves only — it never sends anything.** (Delivery is the existing
       attachment path and its existing Approve gate.)
    2. **`short` vs `long`:** `long` for proposals, one-pagers and reports; `short` for posts, ad copy
       and headlines. Long-form gets a PDF download; short-form does not.
    3. **The locked trigger rule (U4), one sentence, imperative:** *"When the user asks for a
       document, call `createDocument`. When creating one is YOUR idea, say what you would write and
       wait for a yes."*
    4. **Revise by `replace`:** to change a document already created in this conversation
       ("make it shorter", "change the tone"), re-issue `createDocument` with the instruction folded
       into `topic` and `replace` set to the 1-based `#index` of that document. It rewrites the same
       document in place.

    Teach EXACTLY the schema in `18-06-SUMMARY.md`. A body that teaches an argument the tool does not
    have is worse than silence.

    ⛔ Do NOT teach a `confirmed` flag — the tool has no such argument, deliberately.
    ⛔ Do NOT add a second `##` section, and do NOT restructure anything else in the file. The diff
    must be one contiguous addition.
    ⛔ Do NOT touch `document-drafter.md` or `content-drafter.md`.

    **Then REGENERATE `packages/contracts/src/skills/cockpitAgent.ts` in the SAME commit.** Do NOT
    hand-escape 26 KB. Keep the 5-line "AUTO-DERIVED" header (`:1-5`) verbatim and rewrite line 8
    with a THROWAWAY one-liner (nothing new is committed — "there is no generator script" stays true):

    ```sh
    node -e "const{readFileSync}=require('fs');process.stdout.write('export const cockpitAgentSkillBody = '+JSON.stringify(readFileSync('packages/contracts/skills/cockpit-agent.md','utf8').replace(/\r\n/g,'\n'))+';')"
    ```

    `JSON.stringify` emits exactly the LF-normalized, correctly-escaped literal
    `skills.test.ts:735` compares against. Prove byte-identity by running `skills.test.ts`, not by eye.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts && pnpm --filter @pikar/contracts exec tsc --noEmit</automated>
  </verify>
  <done>`skills.test.ts` is green including the `["cockpit-agent.md", cockpitAgentSkillBody]` byte-identity row; the `.md` diff is one contiguous `##` section; both files are in ONE commit; contracts typecheck exit 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts` — green (this is the ONLY automated guard for row 12).
- `pnpm --filter @pikar/backend test` — no new reds vs. the phase-start baseline.
- `git show --stat HEAD` — exactly two files, `cockpit-agent.md` and `cockpitAgent.ts`, in one commit.
- ⚠ **There is NO offline guard for whether the ACTIVE body teaches the tool.** Landing this file
  publishes a **CANDIDATE** (`maxVersion + 1`) on the next `seedSkills`; the tool stays invisible to
  the model until that candidate passes an eval and is activated. That flip, and the live turn that
  proves the model actually calls the tool, are plan 18-09's phase gate. **Do not close the phase on
  a green suite here.**
</verification>

<success_criteria>
- Registration Checklist rows 11 and 12 closed, together, in one commit.
- No candidate carrying another lane's un-evaluated prose was minted (proved by Task 1's live check).
- The body teaches the tool, both forms, the trigger rule and revise-by-#index — and nothing else.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-08-SUMMARY.md`.
Record: the live `cockpit-agent` rows observed at Task 1 (active version, candidate presence), the
version number this edit will mint on the next `seedSkills`, and the exact `##` section text.
⚠ State plainly that the tool remains INVISIBLE to the model until the minted candidate is activated,
and that plan 18-09 owns that activation and its cost.
</output>
