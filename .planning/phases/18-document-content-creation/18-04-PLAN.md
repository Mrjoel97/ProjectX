---
phase: 18-document-content-creation
plan: 04
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - packages/backend/convex/vault.ts
  - packages/backend/convex/vaultSources.ts
  - packages/backend/convex/createdDocs.test.ts
autonomous: true
requirements: [ACTN-04]

must_haves:
  truths:
    - "A created artifact is a tenant-scoped vaultDocuments row with text present, mimeType text/markdown, origin agent and status ready — visible in the vault grid, previewable, downloadable when long-form."
    - "A created artifact is invisible to another tenant: tenant B's listVaultDocs never contains it."
    - "A created artifact is never ingested — it has no ragEntryId and no graph nodes, so neither half of the grounding engine can return it."
    - "A revision patches the SAME _id: one row before, one row after, new text, no version history."
    - "A revision cannot overwrite a user-uploaded document, cannot cross a tenant boundary, and a bad #index changes nothing."
  artifacts:
    - path: "packages/backend/convex/vault.ts"
      provides: "insertCreatedDoc and patchCreatedDoc internalMutations"
      exports: ["insertCreatedDoc", "patchCreatedDoc"]
    - path: "packages/backend/convex/vaultSources.ts"
      provides: "role + snippet + form on insert; a role-filtered byThread read"
      contains: "role"
    - path: "packages/backend/convex/createdDocs.test.ts"
      provides: "SC1, SC1b, SC3 and SC7 assertions (the drift-exclusion test is plan 18-10's)"
      min_lines: 120
  key_links:
    - from: "packages/backend/convex/vault.ts insertCreatedDoc"
      to: "vaultIngest.startIngest"
      via: "DELIBERATE OMISSION — there is no call, and the absence IS the retrieval exclusion"
      pattern: "startIngest"
    - from: "packages/backend/convex/vault.ts patchCreatedDoc"
      to: "vaultSources by_thread"
      via: "role-filtered latest-wins read resolving a 1-based #index to a docId inside the DB plane"
      pattern: "r\\.role === \"created\""
---

<objective>
Build the governed write plane for created artifacts: two internal mutations in `vault.ts`, the
Output-card row shape in `vaultSources.ts`, and the test file that proves SC1, SC1b, SC3 and SC7.

⚠ **The blueprint drift exclusion moved OUT of this plan into plan 18-10 (wave 6).** It is one line
in `blueprint.ts` and it cannot land until `17.1-10`'s live gate has run — parking it here would halt
wave 2 of an `autonomous: true` plan on a precondition no executor can satisfy. Waves 1-5 are
runnable today; 18-08 and 18-10 are the parked ones.

Purpose: this is the phase's data contract. The tool in plan 18-06 is a thin caller over it. The
retrieval exclusion is implemented here **by not writing a line** — no `startIngest` call — which is
the whole mechanism, so a reviewer looking for a filter will find an omission and must be told why
in a comment.
Output: Registration Checklist rows 5 and 13. (Pattern 9's one-line drift edit is plan 18-10's.)
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-02-SUMMARY.md
@packages/backend/convex/vault.ts
@packages/backend/convex/vaultSources.ts
@packages/backend/convex/vaultGround.test.ts
@docs/playbooks/vault.md

**Read 18-RESEARCH.md § Pattern 1, § Pattern 2b, § Pattern 4, § Pattern 9 and § Validation
Architecture (the two copyable test snippets) before writing.** Pattern 1 and 2b contain the exact
handler bodies — copy them, not CONTEXT.md's four-field summary, which is missing three
schema-REQUIRED fields (`size`, `contentHash`, `createdAt`).

<interfaces>
```ts
// packages/vault/src/categories.ts — categoryFor({ source: "agent" }) → "workspace-docs"
export function categoryFor(doc: { source: VaultSource }): string;   // :30-44

// packages/backend/convex/lib/hash.ts:5
export function contentHash(s: string): Promise<string>;             // sha-256 hex

// packages/backend/convex/vaultIngest.ts:30-46 — the SOLE legal ingest start.
// ⛔ Phase 18 does NOT call it and does NOT edit the file.
export async function startIngest(ctx: MutationCtx, ...): Promise<void>;

// packages/backend/convex/vaultSources.ts — the shape to EXTEND (append-only, by_thread latest-wins)
export const insert = internalMutation({ args: { tenantId, threadId, docIds, titles, count, createdAt } });
export const byThread = tenantQuery({ args: { threadId } });
```
</interfaces>

**Verified anchors at HEAD (2026-08-01):**
- `ingestFromAttachment` — `vault.ts:571-615`. THE precedent: an internal mutation inserting a vault
  row on another module's behalf, with an explicit `tenantId` arg. Copy its shape.
- `listVaultDocs` — `vault.ts:270-279`. `.collect()`s the tenant partition with NO kind/status/origin
  filter, so created docs appear in the grid for free.
- `vaultDownloadUrl` — `vault.ts:318`. Already signs `storageId`. No new route needed.
- `confirmBlueprint` — `blueprint.ts:346-369`. The SHIPPED precedent for a vault row at
  `status: "ready"` that never calls `startIngest`; its patch branch at `:346-356` is `patchCreatedDoc`'s shape.

**⚠ Test-authoring cautions:**
- `stableTenant` was **DELETED** (Lane O `d62c46c`). Use plain string subjects:
  `t.withIdentity({ subject: "tenant_a" })`. `vaultGround.test.ts:21-23`'s `asTenant` is the current idiom.
- `packages/backend/convex/vault.test.ts` contains a literal **NUL byte** (~offset 5982) — ripgrep
  and Grep classify it as binary and skip it **silently**. Use `grep -a` or the Read tool for it.
- `importGuard.test.ts` needs no registration — `import.meta.glob` auto-scans.
- The backend suite is **edge-runtime**; `node:fs` is unavailable without a
  `// @vitest-environment node` pragma on line 1. `createdDocs.test.ts` needs no source read, so it
  needs no pragma.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: insertCreatedDoc — the governed write, and the ingest call that is deliberately absent</name>
  <files>packages/backend/convex/vault.ts, packages/backend/convex/createdDocs.test.ts</files>
  <behavior>
    - SC1: `insertCreatedDoc({ tenantId: "tenant_a", title, form: "long", markdown, contentHash, storageId })` inserts ONE `vaultDocuments` row with `mimeType: "text/markdown"`, `text` = the markdown, `source: "agent"`, `category: "workspace-docs"`, `origin: "agent"`, `status: "ready"`, `kind: "created_document"`, and a `size` equal to the UTF-8 byte length.
    - `form: "short"` yields `kind: "created_content"` and, when no `storageId` is passed, a row with `storageId` undefined (⇒ `PreviewModal`'s `canDownload` is false ⇒ no Download button, for free).
    - SC1b: the row has NO `ragEntryId`. `vaultGround` / `searchVault` can never return it.
    - SC3: tenant B's `api.vault.listVaultDocs` does NOT contain the row; tenant A's does.
    - `storageId` is written STRAIGHT THROUGH from args: pass one and the row has it, omit it and the row does not. `insertCreatedDoc` renders NOTHING — the PDF is made in the tool (plan 18-06), never in the mutation.
  </behavior>
  <action>
    **`packages/backend/convex/vault.ts`** — add `insertCreatedDoc` exactly as written in
    18-RESEARCH.md § Pattern 1 (a new `internalMutation`, mirroring `ingestFromAttachment`'s
    explicit-`tenantId`, internal-only shape). All twelve fields are required by the schema; do not
    trim the list.

    ⛔ **Do NOT call `startIngest`.** Write the comment that says so and why — *"deliberately not
    ingested: no rag entry and no graph nodes ⇒ structurally unreachable by both halves of
    `runVaultGround`; this IS the locked retrieval exclusion (CONTEXT.md), and the shipped precedent
    is `blueprint.ts:357`. Promotion later = patch `origin` to `agent_promoted` + call the
    already-exported `startIngest` — a UI addition, not a migration."* Without that comment the next
    reader "fixes" the omission.

    ⛔ Do NOT edit `vaultIngest.ts`. Do NOT create a new convex module — a new module under
    `packages/` forces a `watch.json` entry and `check-playbooks.mjs:125-134` **blocks the turn**.
    `vault.ts` is the right home precisely because it costs zero registration.
    ⛔ `kind` values are `"created_document"` / `"created_content"`. Do NOT use `"document"` —
    `smoke.ts:819 seedVoiceDocSession` already writes it.
    ⛔ `mimeType` stays `"text/markdown"` for BOTH forms (locked). Storing `application/pdf` would
    land the row at `pending_extraction` and round-trip it through `vaultExtract` to recover text we
    authored (`vault.ts:196-216`).

    Also add the **ponytail note about the vault UI search box**: `vault.vaultSearch` is the same rag
    primitive, so created docs will not appear in the vault UI's search results — accepted for beta,
    upgrade path is a ~3-line title-substring fallback in `DocGrid`'s filter. Owner question, raised
    in plan 18-09's gate.

    **`packages/backend/convex/createdDocs.test.ts` — NEW file.** Copy the first snippet from
    18-RESEARCH.md § *Validation Architecture* (it compiles at HEAD) and extend to cover the
    `form: "short"` no-`storageId` case. TDD: write the tests first and see them fail on the missing
    export.

    🛑 **ONE LINE OF THAT SNIPPET IS WRONG — do not copy it verbatim.** It calls `insertCreatedDoc`
    with NO `storageId` and then asserts `expect(row.storageId).toBeDefined();  // long-form ⇒ a
    derived PDF`. Per Pattern 1 the handler writes `storageId` straight through from args and renders
    no PDF, so that assertion is guaranteed RED and looks like a real defect in the mutation — the
    trap being that "fixing" it drags `pdf-lib` into a Convex mutation, which is illegal (it is not a
    `"use node"` module). Do ONE of:
    - store a real blob first and pass its id —
      `const storageId = await t.run(async (ctx) => ctx.storage.store(new Blob(["%PDF-1.4"], { type: "application/pdf" })));`
      then pass `storageId` and keep the `toBeDefined()` assertion; **or**
    - drop that line here and assert the long/short `storageId` consequence in plan 18-06's tool test,
      where the PDF is actually produced.
    Record which you chose in the SUMMARY. The `form: "short"` case asserts
    `expect(row.storageId).toBeUndefined()` either way.

    (The RESEARCH doc has been annotated at that line; the plan is authoritative.)
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/createdDocs.test.ts</automated>
  </verify>
  <done>`insertCreatedDoc` is exported from `vault.ts`; `grep -c "startIngest" packages/backend/convex/vault.ts` is unchanged from HEAD (the new mutation adds none); all Task-1 behaviours pass; `git diff packages/backend/convex/vaultIngest.ts` is empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Output-card row shape + patchCreatedDoc (replace-in-place, SC7)</name>
  <files>packages/backend/convex/vaultSources.ts, packages/backend/convex/vault.ts, packages/backend/convex/createdDocs.test.ts</files>
  <behavior>
    - `vaultSources.insert` accepts optional `role: "created"`, `snippet` and `form`; `byThread({ threadId, role })` returns the latest row matching `role` INCLUDING its `form`, and `byThread({ threadId })` (no role) still returns the latest grounding row — the shipped `SourceCard` behaviour is unchanged.
    - A row inserted with `form: "long"` reads back `form === "long"`. This is the ONLY thing plan 18-07's UPPERCASE type badge (`DOCUMENT` / `POST`) can read: `byThread` returns a `vaultSources` row and the short/long discriminator otherwise lives only on `vaultDocuments.kind`, which this row never reads.
    - SC7: after `insertCreatedDoc` + a `vaultSources` row with `docIds: [id]`, `patchCreatedDoc({ tenantId, threadId, index: 1, ... })` returns `{ ok: true }`, the tenant still has exactly ONE created row, its `text` is the new markdown, and its `_id` is unchanged.
    - `index: 2` against a one-doc row returns `{ ok: false }` and changes nothing.
    - A row belonging to `tenant_b`, or a row with `origin` absent (a user upload), returns `{ ok: false }` and is not patched.
    - long→short: passing no `storageId` REMOVES the field, and the returned `oldStorageId` is the superseded one.
  </behavior>
  <action>
    **`packages/backend/convex/vaultSources.ts`** — add `role: v.optional(v.literal("created"))`,
    `snippet: v.optional(v.string())` and
    `form: v.optional(v.union(v.literal("short"), v.literal("long")))` to `insert`'s args (all three
    pass straight through to the insert — the schema fields landed in plan 18-02),
    and add `role: v.optional(v.literal("created"))` to `byThread`'s args with a **filtered** read:
    `.order("desc").take(20)` then `.find(r => r.role === role)`. Carry a `ponytail:` comment naming
    the 20-row window and when to raise it.

    ⚠ **The role filter is LOAD-BEARING — do not simplify it back to `.first()`.** The table is
    append-only per thread and `by_thread` is latest-wins, so a bare `.first()` returns whatever the
    LAST turn wrote: one `searchVault` turn between a create and a revise and `docIds[index-1]`
    resolves to a USER-UPLOADED doc, the origin guard below correctly refuses, and the locked
    *"make that one shorter"* silently stops working. It fails safe, but it fails.

    **`packages/backend/convex/vault.ts`** — add `patchCreatedDoc` exactly as written in
    18-RESEARCH.md § Pattern 2b. Key properties, all mandatory:
    - the `#index → docId` resolution happens **INSIDE the mutation**, so no raw `_id` ever reaches
      the model and no new internalQuery is needed;
    - **BOTH guards**, not one: `doc.tenantId !== a.tenantId` (isolation) AND `doc.origin !== "agent"`
      (never let a revise overwrite an upload, and never silently rewrite a promoted doc);
    - it returns `{ ok: false }` on any refusal — it does **not** throw. The caller turns that into a
      sentence;
    - it returns `oldStorageId` so the tool can delete the superseded PDF **after** the patch
      persists (the `regenerateAttachment` ordering, `llm.ts:1327`).

    ⛔ ONE row, latest content wins, patching the same `_id`. NO version history, NO second row, NO
    `versions` field (locked).

    **`createdDocs.test.ts`** — add the SC7 block. TDD: RED first.

    MUTATION CHECK (mandatory, record in the SUMMARY): delete the `doc.origin !== "agent"` guard and
    confirm the "cannot overwrite a user upload" test goes RED; revert and confirm green.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/createdDocs.test.ts</automated>
  </verify>
  <done>Both mutations exported; `insert` accepts and stores `role`/`snippet`/`form`; every SC7 behaviour above passes; the mutation check was observed RED then reverted green; `SourceCard`'s existing no-role `byThread` call still returns grounding rows.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/createdDocs.test.ts convex/vault.test.ts` — green.
- `pnpm --filter @pikar/backend test` — no new reds vs. the phase-start baseline.
- `pnpm exec turbo run typecheck --filter=@pikar/backend --force` — **delta 0** vs. the number in
  18-02-SUMMARY, and **zero** errors in any production `convex/*.ts` file.
- `git diff --stat packages/backend/convex/vaultIngest.ts packages/core/src/actionType.ts packages/backend/convex/cockpit.ts packages/backend/convex/plans.ts` — **empty**. Any diff there means the design drifted.
- `node scripts/check-playbooks.mjs` — expect it to demand `vault.md` and `cockpit.md`. That obligation is plan 18-09's; do NOT satisfy it by bumping a foreign `Last verified`. (`onboarding.md` is not forced by this plan any more — `blueprint.ts` moved to plan 18-10.)
</verification>

<success_criteria>
- SC#1: a created artifact is a tenant-scoped vault row with `text`, distinct from a plan attachment.
- SC#3: tenant isolation asserted on the created artifact, and the row carries a real provenance discriminator.
- SC#7: revision replaces in place on the same `_id`, with both guards proven.
- The retrieval exclusion is TRUE structurally (no `startIngest` call). The DRIFT exclusion is plan 18-10's, parked behind `17.1-10`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-04-SUMMARY.md`.
Record: the mutation-check result, which of the two `storageId` fixes was taken in Task 1, the exact
`insertCreatedDoc` / `patchCreatedDoc` signatures
(plan 18-06 calls them), the decision that ONE `vaultSources` row per turn carries all N `docIds`
(this is what makes `replace: 2` addressable — do not change it later), and the `vaultSources.insert`
arg list including `form` (plan 18-06 passes it, plan 18-07 reads it for the type badge).
</output>
