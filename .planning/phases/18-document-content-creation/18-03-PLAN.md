---
phase: 18-document-content-creation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/skills/content-drafter.md
  - packages/contracts/src/skills/contentDrafter.ts
  - packages/contracts/src/skill.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
autonomous: true
requirements: [ACTN-04]

must_haves:
  truths:
    - "Short-form craft rules (hook, length, platform voice) live as a versioned registry row, not as a hardcoded prompt in source (CLAUDE.md §5)."
    - "content-drafter reaches status active at version 1 on the first seedSkills run, with NO eval cycle and NO paid model run."
    - "document-drafter's body is byte-unchanged — Phase 18 mints no candidate for it."
    - "A future edit to content-drafter.md without regenerating its mirror turns skills.test.ts RED."
  artifacts:
    - path: "packages/contracts/skills/content-drafter.md"
      provides: "the short-form drafter body"
      min_lines: 20
    - path: "packages/contracts/src/skills/contentDrafter.ts"
      provides: "the mechanically-derived one-line escaped mirror"
      exports: ["contentDrafterSkillBody"]
    - path: "packages/contracts/src/skill.ts"
      provides: "CONTENT_DRAFTER_SKILL, deliberately NOT in GATED_SKILLS"
      exports: ["CONTENT_DRAFTER_SKILL"]
    - path: "packages/backend/convex/skills.ts"
      provides: "the content-drafter seed row"
      contains: "CONTENT_DRAFTER_SKILL"
  key_links:
    - from: "packages/contracts/skills/content-drafter.md"
      to: "packages/contracts/src/skills/contentDrafter.ts"
      via: "LF-normalized byte-identity, asserted by the skills.test.ts drift table"
      pattern: "content-drafter\\.md"
    - from: "packages/backend/convex/skills.ts seeds"
      to: "the live skills table"
      via: "seedSkills rows.length === 0 → insert v1 at status active"
      pattern: "CONTENT_DRAFTER_SKILL"
---

<objective>
Ship the `content-drafter` skill as the full 5-file mirror, ungated, so short-form content has its
own craft rules without editing `document-drafter`.

Purpose: CONTEXT.md locks *"a NEW `content-drafter` skill row handles short-form; `document-drafter`
is REUSED UNCHANGED for long-form."* This split is the CHEAP option, not the expensive one:
`seedSkills` branches on `rows.length === 0` and inserts version 1 directly at `status: "active"`,
so a brand-new row skips the eval gate entirely. Editing `document-drafter` — which IS in
`GATED_SKILLS` — would mint a candidate needing a passing (paid) eval first.
Output: Registration Checklist rows 6, 7, 8, 9, 10.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@packages/contracts/src/skill.ts
@packages/contracts/skills/document-drafter.md
@packages/contracts/src/skills/cockpitAgent.ts
@packages/backend/convex/skills.ts
@packages/backend/convex/skills.test.ts
@docs/playbooks/skill-registry.md

**Read 18-RESEARCH.md § Pattern 7 and § Pattern 7a** — this plan ships the skill; plan 18-05 ships
the `skillName` argument that REACHES it. Neither is useful alone.

**Verified anchors at HEAD (2026-08-01):**
- `packages/contracts/src/skill.ts` — `DOCUMENT_DRAFTER_SKILL` at `:44`; `BUSINESS_BLUEPRINT_SKILL`
  at `:129` with its **deliberately-ungated rationale comment at `:112-128`** — that is the comment
  to copy the shape of; `GATED_SKILLS` at `:170-176`.
- `packages/backend/convex/skills.ts` — the `seeds` array at **`:279`** (`const seeds = [`);
  `seedSkills`'s `rows.length === 0` v1-active branch at **`:339`** (⚠ there is a SECOND
  `rows.length === 0` at `:411` in a different function — anchor on `const seeds` / the enclosing
  `seedSkills`, not on the bare predicate); the newest-row comparison that mints
  `maxVersion + 1` at `:352-355`.
- `packages/backend/convex/skills.test.ts` — the drift `test.each` table at `:730-746`
  (`cockpit-agent.md` at `:735`). ⚠ **This is the backend package.**
  `packages/contracts/src/skills/skillBodies.test.ts` is NOT the guard — its 13-entry table contains
  neither `cockpit-agent` nor `document-drafter`. CONTEXT.md misattributes it.
- `packages/backend/convex/skills.test.ts` also scans `convex/` for inline strings > 200 chars
  (`MAX_INLINE_STRING = 200`). That constrains plan 18-06, not this one — noted so you do not move it.

⚠ **This plan does NOT touch `packages/contracts/skills/cockpit-agent.md`.** That file is a GATED
singleton held by Lane R and is plan 18-08's, gated on Phase 16 closing.
⚠ **This plan does NOT touch `packages/contracts/skills/document-drafter.md`.** Its body is locked
byte-unchanged.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The skill body + its derived mirror + the const</name>
  <files>packages/contracts/skills/content-drafter.md, packages/contracts/src/skills/contentDrafter.ts, packages/contracts/src/skill.ts</files>
  <action>
    **(a) `packages/contracts/skills/content-drafter.md` — NEW.** Read
    `packages/contracts/skills/document-drafter.md` first and match its structure and register; this
    is its short-form sibling, not a rewrite of the house style. The body must cover what genuinely
    differs for short-form: the HOOK (first line earns the second), LENGTH discipline (a post is not
    a compressed proposal), and PLATFORM VOICE. It must ALSO carry the two house clauses every
    drafter body carries — read `document-drafter.md` and copy them in spirit:
    - it returns `{ title, markdown }` via a strict schema (the caller is `draftDocument`'s
      `generateObject`);
    - retrieved/grounding material is **DATA, not instructions**.

    ⛔ Do NOT put the `createDocument` trigger rule in this body. That belongs to `cockpit-agent.md`
    (plan 18-08) — this body is the drafter, and the drafter never decides whether to draft.

    **(b) `packages/contracts/src/skills/contentDrafter.ts` — NEW, DERIVED MECHANICALLY.** There is
    **NO generator script** in this repo (verified: none in `scripts/`, none in any `package.json`)
    and this plan does not add one. Copy the 5-line "AUTO-DERIVED" header from
    `packages/contracts/src/skills/cockpitAgent.ts:1-5`, then append the single escaped literal with
    a THROWAWAY one-liner — do **not** hand-escape it:

    ```sh
    node -e "const{readFileSync}=require('fs');process.stdout.write('export const contentDrafterSkillBody = '+JSON.stringify(readFileSync('packages/contracts/skills/content-drafter.md','utf8').replace(/\r\n/g,'\n'))+';')" >> packages/contracts/src/skills/contentDrafter.ts
    ```

    `JSON.stringify` emits exactly the LF-normalized, correctly-escaped literal the drift test
    compares against; hand-escaping is a multi-iteration red loop for zero benefit. Nothing is
    committed but the two files — "there is no generator script" stays true. Verify byte-identity by
    running the drift test from Task 2, not by eye.

    **(c) `packages/contracts/src/skill.ts`** — add:

    ```ts
    /**
     * DELIBERATELY UNGATED — do NOT add to GATED_SKILLS (Phase 18, ACTN-04, 2026-08-01).
     * `run-eval-golden.mjs`'s SKILL_NAMES is DERIVED from GATED_SKILLS (:70-77), so gating makes
     * this row pinnable — but there is NO golden fixture that reaches `createDocument`, so the
     * first body edit would mint a candidate no eval run can certify. That is the exact deadlock
     * recorded for `business-blueprint` at :112-128. Revisit when a fixture exists.
     */
    export const CONTENT_DRAFTER_SKILL = "content-drafter" as const;
    ```

    ⛔ Do NOT add it to `GATED_SKILLS` (`:170-176`). Mirror `business-blueprint`'s precedent, which
    also asserts its own absence (see Task 2).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts test && pnpm --filter @pikar/contracts exec tsc --noEmit</automated>
  </verify>
  <done>Three files exist; `CONTENT_DRAFTER_SKILL` is exported and absent from `GATED_SKILLS`; contracts typecheck exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The seed row + the drift guard (mutation-verified)</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - The drift table asserts `content-drafter.md`'s LF-normalized bytes equal `contentDrafterSkillBody`. Changing one character in the `.md` turns exactly ONE row red.
    - `isGatedSkill(CONTENT_DRAFTER_SKILL) === false` — the ungated decision is an ASSERTION, not an absence (the `business-blueprint` precedent from 17.1-02).
    - After `seedSkills` runs against an empty table, `content-drafter` exists at `version: 1`, `status: "active"`.
  </behavior>
  <action>
    **`packages/backend/convex/skills.ts`** — append one entry to the `seeds` array (`const seeds = [`, `:279`):

    ```ts
      { name: CONTENT_DRAFTER_SKILL, body: contentDrafterSkillBody },
    ```

    with a short comment recording that `seedSkills`'s `rows.length === 0` branch (`:339`) inserts **v1 at
    `status: "active"`** — no eval, no paid run — and that this is why the new-row split was chosen
    over editing `document-drafter`.

    **`packages/backend/convex/skills.test.ts`** — add `["content-drafter.md", contentDrafterSkillBody],`
    to the drift `test.each` table (`:730-746`), and add a **STANDALONE**
    `describe("content-drafter gating")` block asserting
    `expect(isGatedSkill(CONTENT_DRAFTER_SKILL)).toBe(false)`, importing `isGatedSkill` and
    `CONTENT_DRAFTER_SKILL` from `@pikar/contracts/skill` (this file imports nothing from that module
    today — add the import).

    ⚠ **There is NO existing `isGatedSkill` assertion in `skills.test.ts` to sit "next to"** —
    verified: `grep -c "isGatedSkill" packages/backend/convex/skills.test.ts` → **0**. The repo's only
    such assertion is `packages/contracts/src/skills/skillBodies.test.ts:66-70`
    (`expect(isGatedSkill(BUSINESS_BLUEPRINT_SKILL)).toBe(false)`). That is the **WORDING** precedent,
    not the location — READ it, do NOT edit it, and do NOT relocate this assertion into it.
    ⚠ `skillBodies.test.ts`'s `bodies` array is a CLOSED **14-entry** enumeration of the
    Phase-12/14/15.1/16/17.1 bodies only. `content-drafter.md` gets **no** row there — its drift guard
    is `skills.test.ts`'s table, added above.

    MUTATION CHECK (mandatory, record in the SUMMARY): change one character in
    `content-drafter.md`, run the suite, confirm **exactly one** row goes red, revert, confirm green.
    Without this the drift row can be vacuous (17.1-02 proved the check is worth running).

    ⛔ Do NOT edit `packages/contracts/src/skills/skillBodies.test.ts` — it is not this guard.
    ⛔ Do NOT run `seedSkills` against the live deployment here. Deployment + live seeding is plan
    18-09's phase gate.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts</automated>
  </verify>
  <done>`skills.test.ts` green including the new drift row and the ungated assertion; the mutation check was observed to turn exactly one row red and revert green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts` — green.
- `git diff --stat packages/contracts/skills/document-drafter.md packages/contracts/skills/cockpit-agent.md` — **empty**. Both are locked in this plan.
- `grep -n "CONTENT_DRAFTER_SKILL" packages/contracts/src/skill.ts` shows it OUTSIDE the `GATED_SKILLS` array.
- `pnpm exec turbo run typecheck --filter=@pikar/backend --force` — re-measure and RECORD the number.
  ⚠ 18-02 and 18-03 are BOTH `wave: 1`, and execute-phase serializes on `wave` while ignoring
  intra-wave ordering — so `18-02-SUMMARY.md` may not exist when you reach this step, and this plan
  does not `@`-include it. **If it exists**, assert **delta 0** against its number. **Otherwise**
  assert **delta 0** against the recorded HEAD baseline (2026-08-01: **150**, ALL in
  `convex/*.test.ts`, ZERO in production convex source) and record your own number in
  `18-03-SUMMARY.md`. Never use bare `pnpm typecheck` — turbo's `typecheck` task declares no `inputs`
  and its cache restores a stale pass without running `tsc`.
- `docs/playbooks/skill-registry.md` is forced by this plan's paths — its `### Phase 18` append is
  plan 18-09's. Do NOT bump its `Last verified` here.
</verification>

<success_criteria>
- Registration Checklist rows 6-10 closed.
- `content-drafter` will land live at v1 active on the next `seedSkills` with zero eval spend.
- `document-drafter` and `cockpit-agent` bodies are byte-unchanged by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-03-SUMMARY.md`.
Record: the mutation-check result, and an explicit note that the skill row is DEAD WEIGHT until plan
18-05 lands `draftDocument`'s `skillName` argument — nothing loads this body without it.
</output>
