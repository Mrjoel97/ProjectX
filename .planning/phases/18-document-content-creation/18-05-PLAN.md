---
phase: 18-document-content-creation
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-03"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/documentDraft.test.ts
autonomous: true
requirements: [ACTN-04]

must_haves:
  truths:
    - "A caller can ask draftDocument for the content-drafter body — the new skill row is reachable, not dead weight."
    - "Every shipped draftDocument caller is byte-identical: omitting skillName still loads document-drafter, and document-drafter's BODY is untouched."
    - "A second output format (a self-contained HTML page) is emitted through the SAME governed path: PII scan -> registry drafter -> cap -> ctx.storage.store -> ref-only return."
    - "The two shipped Phase-3.3 attachment callers pass three arguments and behave exactly as before."
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "draftDocument skillName arg; renderAndStore format param; generateAttachment optional format property"
      contains: "skillName"
    - path: "packages/backend/convex/documentDraft.test.ts"
      provides: "SC7b (content-drafter reach, fail-closed unseeded) and SC4 (the html branch)"
      contains: "CONTENT_DRAFTER_SKILL"
  key_links:
    - from: "packages/backend/convex/llm.ts draftDocument"
      to: "internal.skills.getActiveSkill / getSkillVersion"
      via: "const name = skillName ?? DOCUMENT_DRAFTER_SKILL, used at BOTH lookup branches"
      pattern: "skillName \\?\\? DOCUMENT_DRAFTER_SKILL"
    - from: "packages/backend/convex/llm.ts renderAndStore"
      to: "renderHtmlDocument"
      via: "the html branch of the format switch — the ONLY producer of stored HTML bytes"
      pattern: "renderHtmlDocument\\(draft\\.title, draft\\.markdown\\)"
---

<objective>
Thread two parameters through `llm.ts`: the `skillName` that makes `content-drafter` reachable, and
the `format` that makes a second output format emittable through the shipped governed path.

Purpose: ROADMAP SC#4 words this phase as a **parameterization**, not a feature build. And Pattern 7a
is the phase's quiet blocker — `draftDocument` hardcodes `DOCUMENT_DRAFTER_SKILL` at BOTH lookup
branches (`:3049` and `:3052`) and exposes no skill-name argument, so plan 18-03's skill row loads
nowhere until this lands. That is the **withheld-tool pattern** this repo has already paid ~$0.46 to
diagnose once.
Output: Registration Checklist row 4, plus Pattern 5 Steps B and C.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-01-SUMMARY.md
@.planning/phases/18-document-content-creation/18-03-SUMMARY.md
@packages/backend/convex/documentDraft.test.ts
@docs/playbooks/cockpit.md

**Read 18-RESEARCH.md § Pattern 7a (mandatory) and § Pattern 5 Steps B + C before editing.**
Pattern 7a contains the exact args block and the exact three-line handler change.

<interfaces>
```ts
// packages/core/src/documentGen.ts — NEW in plan 18-01, import from @pikar/core
export type DocFormat = "pdf" | "html";
export const formatSpec: (f: DocFormat) => { ext: string; mimeType: string };
export function renderHtmlDocument(title: string, markdown: string): string;
export function buildDocFilename(topic: string, date: string, existing?: readonly string[], format?: DocFormat): string;

// packages/contracts/src/skill.ts — NEW in plan 18-03
export const CONTENT_DRAFTER_SKILL = "content-drafter";
export const DOCUMENT_DRAFTER_SKILL = "document-drafter";   // :44, shipped

// packages/backend/convex/skills.ts — BOTH already take `name: v.string()`. ZERO change needed here.
internal.skills.getActiveSkill   // :98-101
internal.skills.getSkillVersion  // :249-264
```
</interfaces>

**Verified anchors at HEAD (2026-08-01) — anchor on SYMBOLS, not on CONTEXT.md's ranges:**
- `draftDocument` — `llm.ts:3029-3088`; args `:3030-3037`; handler destructure `:3038-3041`; the two
  hardcoded `DOCUMENT_DRAFTER_SKILL` lookups at **`:3049`** and **`:3052`**; the SMOKE short-circuit
  sits AFTER the lookup, so the fail-closed contract is exercised offline.
- `renderAndStore` — the closure `const renderAndStore = async (` at **`:893`**, body ends **`:954`**
  (CONTEXT.md's `890-951` lands on the doc comment). The PII scan is `:898-899`; the pdf render
  `:925`; `buildDocFilename` `:934-938`; the two `"application/pdf"` literals `:947-949` and
  `:950-953`; the `skillVersions?.[DOCUMENT_DRAFTER_SKILL]` pin `:908`.
- `generateAttachment` — `:1275-1298` (its description already carries the confirm rule inline at
  `:1277`). `regenerateAttachment` — `:1300-1330`, delete-after-persist at `:1327`.
- `buildCockpitTools` — `:726-764`; `skillVersions?: Record<string, number>` is the append-only 5th
  arg at `:734-737`. The record is **keyed by skill NAME**, so a second drafter needs no new plumbing.
- `markdownToPdf` — `:3527`.

⚠ **`skillVersions?.[CONTENT_DRAFTER_SKILL]` is always `undefined` today and that is CORRECT, not a
gap:** `run-eval-golden.mjs`'s `SKILL_NAMES` is derived from `GATED_SKILLS` (`:70-77`) and
`content-drafter` is deliberately ungated. The pin resolves to the active row, which is the intent.

⛔ **WATCH-ONLY — if you edit any of these, the design drifted:** `packages/core/src/actionType.ts`,
`cockpit.ts` (`_ARM_TABLE`), `plans.ts` (`patchPlan`/`resetPlan`), the `PlanCard` kind chain.
⛔ **Do NOT refactor `renderAndStore` out of its closure** (Pitfall 6). It captures `tenantId`,
`planId`, `skillVersions` and `ctx` from `buildCockpitTools`, and it writes
`plans.recordAttachments` on failure. Plan 18-06's tool is a SIBLING closure, not a caller of it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: draftDocument gains a closed skillName — the reach that makes content-drafter live (SC7b)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/documentDraft.test.ts</files>
  <behavior>
    - `draftDocument({ tenantId, safeText, safeTextHash, skillName: CONTENT_DRAFTER_SKILL })` loads the `content-drafter` body from the registry.
    - Unseeded, that same call fails closed with `NO_ACTIVE_SKILL` — the existing contract, inherited verbatim.
    - `draftDocument({ ... })` with `skillName` ABSENT loads `document-drafter`, and the three existing assertions (`documentDraft.test.ts:18/:32/:43`) plus `runCockpitAgent.test.ts:592-608` pass **unchanged**.
    - `skillVersion` pinning still works, and works for BOTH names (the record is name-keyed).
  </behavior>
  <action>
    **`packages/backend/convex/llm.ts`** — append to `draftDocument`'s args after `skillVersion`
    (`:3036`), copying Pattern 7a's block verbatim including its comment:

    ```ts
        skillName: v.optional(
          v.union(v.literal(DOCUMENT_DRAFTER_SKILL), v.literal(CONTENT_DRAFTER_SKILL)),
        ),
    ```

    **CLOSED union, not `v.string()`** — this action is internal-only and model-unreachable, but an
    open name would let any caller point the drafter at any registry row.

    Then, in the handler, add `skillName` to the destructure (`:3038-3041`) and collapse the two
    hardcoded lookups (`:3049`, `:3052`) onto ONE name:

    ```ts
        const name = skillName ?? DOCUMENT_DRAFTER_SKILL;
        const skill: { body: string; version: number } =
          skillVersion !== undefined
            ? await ctx.runQuery(internal.skills.getSkillVersion, { name, version: skillVersion })
            : await ctx.runQuery(internal.skills.getActiveSkill, { name });
    ```

    ⛔ Keep the lookup **before** the SMOKE short-circuit so the fail-closed contract stays
    exercised offline. ⛔ `internal.skills.getActiveSkill` / `getSkillVersion` need ZERO change —
    both already take `name: v.string()`. ⛔ `document-drafter`'s BODY is not touched: this changes
    who is ASKED for, never what it says. CONTEXT.md's "REUSED UNCHANGED" lock holds.

    **`documentDraft.test.ts`** — add the two SC7b rows next to the existing fail-closed pins at
    `:18-40`. TDD: RED first (the arg does not exist).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/documentDraft.test.ts convex/runCockpitAgent.test.ts</automated>
  </verify>
  <done>`skillName` exists as a closed optional union; both lookup branches read one `name`; the content-drafter row loads and fails closed unseeded; every pre-existing `draftDocument` assertion passes with ZERO edits to it; `git diff packages/contracts/skills/document-drafter.md` is empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: format threads through renderAndStore and reaches the attachment path (SC4 / SC4b)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/documentDraft.test.ts</files>
  <behavior>
    - `renderAndStore(topic, existing, replaceIndex)` — three args, UNCHANGED behaviour: a PDF, `application/pdf`, a `.pdf` filename. SC4b.
    - `renderAndStore(topic, existing, replaceIndex, "html")` stores a `text/html` blob whose filename ends `.html`, produced by `renderHtmlDocument(draft.title, draft.markdown)`.
    - Both formats traverse the SAME chain: PII scan (fail-closed) -> `internal.llm.draftDocument` -> byte-cap check -> `ctx.storage.store` -> a REF returned, never bytes.
    - The `render=fail::` SMOKE seam and `setError`'s block-on-render-fail still fire for the html branch.
  </behavior>
  <action>
    **`packages/backend/convex/llm.ts`, inside the `renderAndStore` closure (`:893-954`)** — four
    sites, exactly as tabulated in 18-RESEARCH.md § Pattern 5 Step B:

    | Site | Today | After |
    |---|---|---|
    | `:893-897` signature | `(topic, existing, replaceIndex)` | `+ format: DocFormat = "pdf"` |
    | `:925` | `bytes = await markdownToPdf(draft.title, draft.markdown);` | `bytes = format === "pdf" ? await markdownToPdf(...) : new TextEncoder().encode(renderHtmlDocument(draft.title, draft.markdown));` |
    | `:934-938` | `buildDocFilename(topic, today, others.map(a => a.filename))` | `..., format)` |
    | `:947-949` + `:950-953` | `"application/pdf"` ×2 | hoist `const { mimeType } = formatSpec(format);` and use it twice |

    **`generateAttachment` (`:1275-1298`)** — Step C: add `format: { type: "string", enum: ["pdf","html"] }`
    as an **OPTIONAL** property on its `inputSchema` and pass it through to `renderAndStore`.
    No new tool, no new `agentSteps.tool` literal, no new VERB entry, no new schema union member.
    `gmail.ts` needs ZERO changes — `buildMime` emits `Content-Type: ${a.mimeType}` generically
    (`gmail.ts:131`), so an HTML part rides the existing MIME builder.

    ⛔ Leave `regenerateAttachment` (`:1300-1330`) at three args. The default is what makes SC4b true.
    ⛔ Do NOT add a second size constant. `exceedsByteCap` / `PLAN_ATTACHMENT_CAP_BYTES` (8 MiB) is
    the plan-attachment ceiling and is shared by both formats — that sharing is exactly what SC#4
    asks for.

    **PLANNER NOTE, read this before you go looking for a gap:** the second format is reachable ONLY
    through the EMAIL attachment path, and that is CORRECT. Plan 18-06's `createDocument` never calls
    `renderAndStore` and never emits HTML — CONTEXT.md locks *markdown is the artifact of record* for
    a created document. 🚫 Do NOT "fix" that asymmetry by storing HTML as the created document's
    artifact of record; a non-markdown vault row lands at `pending_extraction` and round-trips
    through `vaultExtract` to recover text we rendered from.

    **`documentDraft.test.ts`** — add the SC4 html-branch test, driven offline by the existing
    `SMOKE::` / `render=fail::` seams. TDD: RED first.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/documentDraft.test.ts convex/cockpitTools.test.ts</automated>
  </verify>
  <done>`renderAndStore` takes a defaulted `format`; there is no `"application/pdf"` string literal left inside it; the html branch stores `text/html` with a `.html` filename through the same PII/cap/store chain; every three-arg caller is unchanged and its tests pass unedited.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/documentDraft.test.ts convex/runCockpitAgent.test.ts convex/cockpitTools.test.ts convex/llmRedaction.test.ts` — green.
- `pnpm --filter @pikar/backend test` — no new reds vs. the phase-start baseline.
- `pnpm exec turbo run typecheck --filter=@pikar/backend --force` — **delta 0** vs. 18-02-SUMMARY's number, zero errors in production `convex/*.ts`.
- `git diff --stat packages/core/src/actionType.ts packages/backend/convex/cockpit.ts packages/backend/convex/plans.ts packages/backend/convex/gmail.ts packages/contracts/skills/document-drafter.md` — **empty**.
- `grep -c "renderAndStore" packages/backend/convex/llm.ts` — the closure was NOT extracted; it still has exactly its HEAD call sites plus none.
</verification>

<success_criteria>
- SC#4: `renderAndStore`'s hardcoded `application/pdf` and `buildDocFilename`'s hardcoded `.pdf` are gone; a self-contained HTML page is emitted through the same governed path with zero new tables and zero new routes.
- SC#4b: Phase-3.3 attachment behaviour is byte-identical.
- Registration Checklist row 4 closed — `content-drafter` is now REACHABLE.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-05-SUMMARY.md`.
Record: the exact `draftDocument` argument list after the change (plan 18-06 calls it and MUST pass
`skillName` + `skillVersion: skillVersions?.[skillName]`), and an explicit statement that HTML is
reachable on the ATTACHMENT path only, by design — so plan 18-06's executor does not "fix" it.
</output>
