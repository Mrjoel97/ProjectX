---
phase: 18-document-content-creation
plan: 09
type: execute
wave: 7
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06", "18-07", "18-08"]
files_modified:
  - docs/playbooks/cockpit.md
  - docs/playbooks/skill-registry.md
  - docs/playbooks/vault.md
  - docs/playbooks/onboarding.md
  - .planning/ROADMAP.md
  - .planning/phases/18-document-content-creation/18-VALIDATION.md
autonomous: false
requirements: [ACTN-04]

must_haves:
  truths:
    - "Every subsystem Phase 18 touched has its invariants written down where the next changer will read them (CLAUDE.md §9)."
    - "ROADMAP no longer contradicts the locked decision in the two places it did — the arm pre-commitment and the 'groundable without new extraction work' clause."
    - "The four surfaces with no offline guard are verified LIVE: the trace row renders, the active body teaches the tool, the model calls it, and the trigger rule holds both ways."
    - "The Output card is judged against BRAND by a human, which no e2e can do."
  artifacts:
    - path: "docs/playbooks/cockpit.md"
      provides: "a ### Phase 18 subsection covering createDocument, the format parameter and the spec->markup boundary"
      contains: "Phase 18"
    - path: "docs/playbooks/vault.md"
      provides: "a ### Phase 18 subsection covering insertCreatedDoc/patchCreatedDoc and the deliberate non-ingest"
      contains: "Phase 18"
    - path: "docs/playbooks/skill-registry.md"
      provides: "a ### Phase 18 subsection covering content-drafter (ungated, v1) and the skillName reach"
      contains: "Phase 18"
    - path: "docs/playbooks/onboarding.md"
      provides: "a ### Phase 18 subsection covering the origin drift exclusion"
      contains: "Phase 18"
    - path: ".planning/ROADMAP.md"
      provides: "corrected Phase 18 SC block (:702-712) and finalized plan list"
      contains: "ACTN-04"
  key_links:
    - from: "the deployed Convex skills table"
      to: "the model's system prompt"
      via: "seedSkills + the eval-gate activation of the cockpit-agent candidate — the ONLY thing that makes the tool exist to the model"
      pattern: "cockpit-agent"
---

<objective>
Discharge Phase 18's §9 documentation obligation, correct the two places ROADMAP contradicts the
locked decision, and run the live phase gate that proves the four surfaces no offline test can see.

Purpose: 18-RESEARCH.md names exactly two surfaces with **no automated guard at all** — the tool key
(#3, nothing cross-checks it) and whether the ACTIVE body teaches the tool (#11/#15). Both are
covered only by a live turn. The missing-literal failure mode throws inside a swallowed SDK callback,
so **every offline test is green either way.** A green suite is not evidence here.
Output: four playbook appends, two ROADMAP corrections, a filled `18-VALIDATION.md`, and a recorded
live gate.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-VALIDATION.md
@.planning/phases/18-document-content-creation/18-01-SUMMARY.md
@.planning/phases/18-document-content-creation/18-04-SUMMARY.md
@.planning/phases/18-document-content-creation/18-06-SUMMARY.md
@.planning/phases/18-document-content-creation/18-08-SUMMARY.md
@docs/playbooks/watch.json
@docs/README.md

**Read 18-RESEARCH.md Pitfall 4 (the playbook table), Pitfall 7 (the two ROADMAP contradictions) and
§ Validation Architecture → Sampling Rate → Phase gate.**

**Phase 18's forced playbooks and WHY each is forced** (all four already cover every path — **no
`watch.json` edit is needed**, and none should be made):

| Playbook | Files Phase 18 EDITED (these force it) |
|---|---|
| `cockpit.md` | `llm.ts`, `cards.tsx`, `documentGen.ts`, `vaultSources.ts` |
| `skill-registry.md` | `contracts/skills/*`, `contracts/src/skills/*`, `contracts/src/skill.ts`, `skills.ts`, `skills.test.ts` |
| `vault.md` | `vault.ts`, `apps/web/.../dashboard/vault/DocGrid.tsx` |
| `onboarding.md` | `blueprint.ts` (one line) |

⛔ **NEVER bump a foreign playbook's `Last verified`.** `check-playbooks.mjs:55-63` builds its
changed-set from `git diff` over the **whole working tree plus untracked files**, so it will demand
playbooks for OTHER lanes' dirty files too. Satisfying those by bumping `Last verified` claims
verification of a diff you never read. Append inside your own `### Phase 18` subsection only.
⛔ `docs/playbooks/cockpit.md` is contested three ways (dirty from Lane R, claimed by `17.1-10`,
forced here). The `.git/claude-playbooks-ack.json` escape is **shared, not session-keyed**, and
re-arms whenever a foreign lane saves.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The four ### Phase 18 playbook subsections (CLAUDE.md §9)</name>
  <files>docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md, docs/playbooks/vault.md, docs/playbooks/onboarding.md</files>
  <action>
    Append ONE `### Phase 18` subsection to each of the four playbooks. Read each playbook's existing
    structure first and write into its own idiom. Do **not** restate the plans — record the
    **invariants a future changer would otherwise break**, sourced from the plan SUMMARYs:

    **`cockpit.md`** — the `createDocument` tool exists and records NO step row itself (recording is
    central in `runAgentLoop`'s SDK callbacks); its `agentSteps.tool` literal and `cards.tsx` VERB
    entry must always land together or `traceParity` is RED, and a MISSING literal is silent in prod;
    `threadId` comes from `readPlan()`, never from the model; `renderAndStore` is a closure and must
    NOT be extracted; `format` defaults to `pdf` so every three-arg caller is Phase-3.3-identical;
    HTML is reachable on the ATTACHMENT path only, by design; the SC#5 boundary is
    string -> `tokenizeMarkdown` -> `DocToken[]` -> renderer, guarded by a structural test whose
    convention is *escape it or name it `*Html`*. Also record the DEFERRED trace-less tools
    (`resetPlan`, `recordScorecardAnswer`) and the exact 4-line fix.

    **`vault.md`** — `insertCreatedDoc` / `patchCreatedDoc` are the only writers of created
    artifacts; **`startIngest` is deliberately NOT called and that omission IS the retrieval
    exclusion** (promotion later = patch `origin` to `agent_promoted` + call the already-exported
    `startIngest`); `mimeType` stays `text/markdown` for both forms and the PDF is a DERIVED download;
    "no PDF for short-form" is the structural ABSENCE of `storageId`; revision replaces in place on
    the same `_id` with BOTH a tenant guard and an `origin` guard; the known ceiling that
    `vault.vaultSearch` will not find created documents.

    **`skill-registry.md`** — `content-drafter` is DELIBERATELY UNGATED and why (no golden fixture
    reaches `createDocument`, so gating deadlocks it at v1 on its first body edit — the
    `business-blueprint` precedent); `document-drafter`'s body is untouched and `draftDocument`'s
    `skillName` changes who is ASKED for, never what is said; the closed-union reason for `skillName`;
    the `cockpit-agent` singleton rule (no lane edits it while another holds an un-activated
    candidate) and that its `.md` and `:8` mirror must land in ONE commit.

    **`onboarding.md`** — the drift predicate now has three conjuncts; `origin !== "agent"` (not
    `=== undefined`) is deliberate so a PROMOTED doc counts as drift; the two inline
    `"business_blueprint"` literals were swapped to the existing `BLUEPRINT_KIND`; `DRIFT_SCAN_CAP`
    saturates the count at 100.

    Bump `Last verified` on these four ONLY, and only because Phase 18 actually read and changed
    their subsystems. ⛔ Do NOT edit `docs/playbooks/watch.json` — Phase 18 created no new module
    under `packages/`, which is exactly what keeps this at zero.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Four `### Phase 18` subsections exist; `check-playbooks.mjs` reports no Phase-18 path outstanding (foreign-lane demands may remain and must be left alone, named explicitly in the SUMMARY); `watch.json` has zero diff.</done>
</task>

<task type="auto">
  <name>Task 2: Correct ROADMAP's two contradictions and finalize the phase block</name>
  <files>.planning/ROADMAP.md, .planning/phases/18-document-content-creation/18-VALIDATION.md</files>
  <action>
    **`.planning/ROADMAP.md:702-712` — ONE edit, BOTH clauses** (18-RESEARCH.md Pitfall 7):

    (a) The **Goal** line currently says *"`packages/core/src/actionType.ts:29-31` already
        pre-commits Phase 18 to the existing `externalAction` arm and that pre-commitment stands."*
        That is the opposite of the owner's locked 2026-07-31 decision. Replace with the locked
        no-arm / no-`ACTION_TYPES`-member / no-`plans.kind`-widening shape, noting that
        `externalAction` is retrier-driven and `dispatchGuard.test.ts:181` pins it to *"only the
        Calendar retrier action and its non-Node terminal."* (The comment's real range is `:30-32`,
        not `:29-31`.)

    (b) SC#4's closing clause currently reads *"The vault's markup rail already sniffs `text/html`
        (`sniff.ts:124`), so the artifact is groundable without new extraction work."* That is the
        direct opposite of the locked retrieval exclusion, and a later planner will derive an ingest
        call straight out of it. Replace with: *"and created artifacts are deliberately NOT ingested,
        so they are visible and downloadable but excluded from vault retrieval and from the blueprint
        drift signal (Phase 18 CONTEXT, locked)."* (`text/html` is `sniff.ts:125`, not `:124`.)

    Also fix the two off-by-one citations in the same block: `documentGen.ts`'s second `.pdf` is
    `:176` (`:177` is `n++`), and `actionType.ts`'s comment is `:30-32`.

    Then update the Phase 18 entry's `**Plans**: TBD` line to the real count and the checkbox list
    (9 plans, `18-01` … `18-09`, each with its one-line objective).

    ⚠ `gsd-tools roadmap update-plan-progress` **can edit the WRONG phase's section** (observed:
    it wrote phase 16's counts over 17.1's). If you use it, `git diff .planning/ROADMAP.md`
    afterwards and confirm every changed line belongs to Phase 18. Hand-edit otherwise.

    **`18-VALIDATION.md`** — fill in the measured runtimes (core suite: 18 files / 375 tests / 25.6 s
    wall-clock ~52 s with pnpm overhead; one backend test file ~21 s / ~44 s wall), the re-measured
    typecheck baseline from `18-02-SUMMARY.md`, and flip `wave_0_complete: true`. Leave
    `nyquist_compliant` at `false` until Task 3's Manual-Only rows have actually been executed —
    setting it true on planning-time conditions alone is the exact false signal 15.2 called out.
  </action>
  <verify>
    <automated>grep -n "groundable without new extraction\|pre-commitment stands" .planning/ROADMAP.md</automated>
  </verify>
  <done>The grep returns NOTHING (both contradictions are gone); the Phase 18 block lists 9 plans; `18-VALIDATION.md` carries real measured numbers and `wave_0_complete: true`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: THE PHASE GATE — the live turns no offline test can replace</name>
  <files>(none — this task changes no source file; it produces the observations 18-09-SUMMARY.md records)</files>
  <action>
Automate everything automatable FIRST, then hand the session to the owner. Claude runs preconditions
1-5 below (full suite, forced typecheck delta, deploy, `seedSkills`, the eval-gate activation of the
`cockpit-agent` candidate, and a fresh production web build whose timestamp is printed alongside
`git log -1` so the owner is not verifying a frozen build). Only the four live turns and the BRAND
judgement are the human's.

Then present the instructions below verbatim and WAIT. Do not write the SUMMARY, do not mark ACTN-04
complete, and do not flip `nyquist_compliant` until the owner responds.
  </action>
  <what-built>
    All of Phase 18: the pure format/HTML renderer, the schema and trace registration, the ungated
    `content-drafter` row, the vault write plane with its deliberate non-ingest, the `skillName` and
    `format` parameters, the `createDocument` tool with `replace`-in-place revision, the Output card
    and the AGENT chip, and the `cockpit-agent` teaching section.
  </what-built>
  <how-to-verify>
    **Preconditions — do these first, in order:**
    1. Full suite green: `pnpm test`.
    2. `pnpm exec turbo run typecheck --filter=@pikar/backend --force` → **delta 0** vs. the number in
       `18-02-SUMMARY.md`, and **zero** errors in any production `convex/*.ts`. (Never bare
       `pnpm typecheck` — it lies.)
    3. Deploy, then run `seedSkills`. Confirm in the LIVE `skills` table: `content-drafter` is at
       **version 1, status active** (no eval, no spend), and `cockpit-agent` has a NEW **candidate**.
    4. **Activate the `cockpit-agent` candidate through its eval gate.** Until that flip the tool is
       invisible to the model and turns 1-4 below cannot pass. Record the run id, per-case verdicts
       and total cost; cross-check the cost against `npx convex data audit` (free) — the runner
       understates research-fixture cost by ~10×.
    5. If replaying in a browser, use a PRODUCTION build (`pnpm --filter @pikar/web build && start`).
       Next dev OOMs on the workspace page, and a stale `next start` will show you yesterday's UI.

    **The four live turns. Record the observed result of each — not "looks right".**

    1. **"Write me a one-pager on X."** Expect: creates with **NO confirmation round-trip** (the
       explicit-ask half of the locked trigger rule); the workspace trace shows a row with the new
       VERB copy (**this is the ONLY proof the `agentSteps.tool` literal works — a missing literal
       throws inside a swallowed SDK callback and is invisible offline**); the Output card appears in
       the conversation; the vault grid shows the AGENT chip; Download works; `npx convex data audit`
       shows a `document.created` row carrying **refs/hashes/ids/booleans only** — open it and confirm
       no topic text and no prose.
    2. **"Give me three LinkedIn post options."** Expect: three short vault rows, **NO Download
       button** on any of them, and bodies that read like posts rather than like a proposal — that
       reading is the ONLY proof `content-drafter` was the body actually loaded.
    3. **"Make the second one shorter."** Expect: the SAME vault row is rewritten in place, the
       tenant's created-row COUNT is unchanged, and the Output card refreshes.
    4. **A turn where the agent SUGGESTS a document.** Expect: it **asks first** (the
       agent-suggested half of the trigger rule). Structurally unprovable offline — the tool cannot
       see whose idea it was.

    **The design judgement (SC#6's human half):** does the Output card look like the Output card
    specified at `docs/design/BRAND.md:101-102` — title, UPPERCASE type badge, subline, rendered
    artifact — and read well? An e2e asserted the testid exists; it cannot assert this.

    **Three owner questions to settle here, all recorded during planning:**
    - **The vault UI search box will not find created documents** (same rag primitive as grounding;
      they are deliberately never ingested). Accepted for beta with a `ponytail:` comment naming the
      ~3-line title-substring fallback in `DocGrid`'s filter. **Accept, or ask for the fallback now?**
      (Do NOT solve it by ingesting.)
    - **`resetPlan` and `recordScorecardAnswer` are live tools with no `agentSteps.tool` literal**, so
      they are silently trace-less in production today. Phase 18 deliberately did NOT fix them
      (2 foreign literals + 2 VERB copy strings Phase 18 did not design). The fix is ~4 lines in the
      block Phase 18 already edited. **Fix now as a follow-up, or leave recorded?**
    - **Promote-to-reference** — the `origin: "agent_promoted"` flag is in the data model; the control
      that flips it is deferred to its own phase, as locked. **Confirm still deferred.**
  </how-to-verify>
  <resume-signal>Paste the four turn results, the audit payload, the eval run id + cost, and answers to the three owner questions. Then type "approved" or describe what failed.</resume-signal>
  <verify>
    <automated>MANUAL — live cockpit turns. Structurally unprovable offline: the missing-literal failure throws inside a swallowed AI-SDK callback, the active skill body is invisible to offline tests, and the trigger rule cannot be seen by the tool. Preconditions ARE automated: `pnpm test` + `pnpm exec turbo run typecheck --filter=@pikar/backend --force` (delta 0 vs 18-02-SUMMARY) + `node scripts/check-playbooks.mjs`.</automated>
  </verify>
  <done>All four live turns executed with observed results recorded verbatim; the `document.created` audit row inspected and confirmed refs-only; the eval run id, per-case verdicts and cost recorded and cross-checked against `npx convex data audit`; the BRAND judgement given; all three owner questions answered. Only then may `nyquist_compliant: true` be set and ACTN-04 be marked Complete.</done>
</task>

</tasks>

<verification>
- `pnpm test` — full suite green.
- `pnpm exec turbo run typecheck --filter=@pikar/backend --force` — delta 0, zero production errors.
- `node scripts/check-playbooks.mjs` — no Phase-18 path outstanding.
- `grep -rn "groundable without new extraction\|pre-commitment stands" .planning/ROADMAP.md` — empty.
- All four live turns recorded with observed results; the Manual-Only rows in `18-VALIDATION.md`
  executed and marked; only THEN set `nyquist_compliant: true`.
- `.planning/REQUIREMENTS.md` `ACTN-04` marked Complete — and only after the live gate, not on green
  offline tests.
</verification>

<success_criteria>
- All six phase Success Criteria are demonstrated: SC1/SC2/SC3/SC4/SC5/SC7 by automated test, SC6 by
  e2e handle plus a human BRAND judgement, and the two unguarded registration surfaces by live turn.
- CLAUDE.md §9 discharged for all four touched subsystems; no foreign playbook's `Last verified` bumped.
- ROADMAP no longer contradicts the locked decision.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-09-SUMMARY.md`.
Record: the four live turn results VERBATIM (including the audit payload keys observed), the eval run
id / verdicts / cost for the `cockpit-agent` activation, the owner's answer to each of the three
questions, and which playbook demands were foreign-lane and deliberately left unsatisfied.
State the evidence level precisely — do not round "the owner confirmed the stated criterion" up to
"everything is correct."
</output>
