---
phase: 18-document-content-creation
plan: 10
type: execute
wave: 6
depends_on: ["18-02", "18-04"]
files_modified:
  - packages/backend/convex/blueprint.ts
  - packages/backend/convex/createdDocs.test.ts
autonomous: false
requirements: [ACTN-04]

must_haves:
  truths:
    - "A created artifact does not inflate the blueprint 'unincorporated documents' drift count."
    - "A PROMOTED created artifact (origin agent_promoted) DOES still count as drift — the locked 'only if promoted' reading."
    - "17.1-10's live gate ran BEFORE any Phase-18 artifact could corrupt the number it measures."
  artifacts:
    - path: "packages/backend/convex/blueprint.ts"
      provides: "the origin !== agent drift exclusion, and BLUEPRINT_KIND replacing the two inline literals"
      contains: "origin"
    - path: "packages/backend/convex/createdDocs.test.ts"
      provides: "the drift-exclusion assertion (extends the file plan 18-04 created)"
      contains: "unincorporatedCountForTenant"
  key_links:
    - from: "packages/backend/convex/blueprint.ts unincorporatedFor"
      to: "vaultDocuments.origin"
      via: "the drift predicate"
      pattern: "origin !== \"agent\""
---

<objective>
Land Pattern 9: one conjunct on the blueprint drift predicate so agent-authored documents stop
counting as *"new business facts not yet in your Blueprint"*, plus the one-line constant tidy in the
same edit.

Purpose: this is one line of code in its own plan for exactly ONE reason — **sequencing.**
`blueprint.ts:566-573` counts every `ready` tenant doc as drift, and `17.1-10`'s live gate is unrun.
A Phase-18 artifact landing before that gate corrupts the number the gate exists to read. Leaving
this task inside plan 18-04 (wave 2, `autonomous: true`) would halt an autonomous wave on a
precondition no executor can satisfy. Parking it at wave 6, `autonomous: false`, with an explicit
blocking checkpoint, makes the halt visible instead of implicit.
Output: 18-RESEARCH.md Pattern 9.

⚠ **This plan and plan 18-08 are the two PARKED plans of Phase 18.** Waves 1-5 are runnable today.
18-08 is parked behind Phase 16 closing (the `cockpit-agent` candidate stream); this plan is parked
behind `17.1-10`. They are INDEPENDENT parks — neither unblocks the other — and both must clear
before plan 18-09's phase gate can run.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/PARALLELIZATION.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-04-SUMMARY.md
@packages/backend/convex/blueprint.ts

**Read 18-RESEARCH.md § Pattern 9 before editing.**

**Verified anchors at HEAD (2026-08-01) — anchor on SYMBOLS, not on ranges. `blueprint.ts` is dirty
from a foreign lane, so line numbers drift; re-`grep` each symbol before editing.**
- `unincorporatedFor` — `blueprint.ts:553-572`, predicate at `:571`. It has **TWO** exclusions today
  (`kind !== BLUEPRINT_KIND` **and** `!sourceSet.has(doc._id)`), not one.
- `BLUEPRINT_KIND` already exists at `blueprint.ts:81` (used at `:346` and `:360`) — the file is
  inconsistent, not constant-less. The two inline `"business_blueprint"` literals are at `:199` and
  `:571`. **CONTEXT.md's "there is no shared constant" is WRONG.**
- `DRIFT_SCAN_CAP = 100` (`:42`) — the count saturates at 100, so a test fixture must stay under it.
- `unincorporatedCountForTenant` args are `{ tenantId: v.string(), sourceDocIds: v.array(v.string()) }`
  (`blueprint.ts:237-239`) — `sourceDocIds` is **REQUIRED**; omitting it throws `ArgumentValidationError`.

⚠ `packages/backend/convex/createdDocs.test.ts` was CREATED by plan 18-04 (wave 2). This plan
**extends** it. Different waves, so no file race.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — confirm 17.1-10's live gate has run</name>
  <files>(none — this task changes no file; it produces the go/no-go observation Task 2 depends on)</files>
  <action>
STOP before touching `blueprint.ts`. Report the state below and WAIT. There is nothing to automate
first: whether a live gate RAN is a project-state fact, not a working-tree fact.
  </action>
  <what-built>
    Nothing yet. This gate exists because the next task changes the very number `17.1-10`'s unrun
    live gate is designed to measure.
  </what-built>
  <how-to-verify>
    Report:
    1. `.planning/STATE.md`'s Lane 17.1 row — is it 10/10, and does `17.1-10`'s SUMMARY record the
       live gate as EXECUTED (not merely planned)?
    2. `ls .planning/phases/17.1-*/17.1-10-SUMMARY.md` — does it exist?

    **PROCEED only if `17.1-10` is closed with its live gate recorded as run.**

    **If it is NOT:** stop and report blocked. Do not "just land the one line" — the whole reason
    this task is a separate plan is that landing it early silently corrupts another lane's
    measurement, and the corruption is invisible afterwards.
  </how-to-verify>
  <resume-signal>Paste the 17.1 lane status and the SUMMARY check, then type "clear to edit" or "blocked".</resume-signal>
  <verify>
    <automated>MANUAL — project-state read. Whether a live gate RAN is not observable from the working tree. Recorded as a Manual-Only row in 18-VALIDATION.md.</automated>
  </verify>
  <done>17.1-10's closure is recorded (or the phase reports blocked here). The observation is written verbatim into 18-10-SUMMARY.md either way.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The drift exclusion (Pattern 9, one line + one tidy)</name>
  <files>packages/backend/convex/blueprint.ts, packages/backend/convex/createdDocs.test.ts</files>
  <behavior>
    - `internal.blueprint.unincorporatedCountForTenant({ tenantId, sourceDocIds: [] })` returns the SAME number before and after an `insertCreatedDoc` for that tenant.
    - A row patched to `origin: "agent_promoted"` DOES count — the locked *"only if promoted"* half.
    - The pre-existing `blueprint.test.ts` suite is green with zero edits to it.
  </behavior>
  <action>
    **`packages/backend/convex/blueprint.ts:571`**, inside `unincorporatedFor` — add ONE conjunct to
    the existing two-exclusion predicate, and in the SAME edit swap the two inline
    `"business_blueprint"` literals (`:199` and `:571`) to the **already-existing** `BLUEPRINT_KIND`
    constant at `:81`:

    ```ts
      const docIds = ready
        .filter((doc) => doc.kind !== BLUEPRINT_KIND && doc.origin !== "agent" && !sourceSet.has(doc._id))
        .map((doc) => doc._id);
    ```

    `origin !== "agent"` — NOT `origin === undefined` — is deliberate: a **promoted** doc
    (`agent_promoted`) DOES count as drift, which is exactly the locked *"only if promoted"*.

    ⛔ Do NOT introduce a new constant. `BLUEPRINT_KIND` exists.

    **`createdDocs.test.ts`** — add the drift-exclusion test using the second copyable snippet in
    18-RESEARCH.md § *Validation Architecture*. `sourceDocIds` is REQUIRED — pass `[]` so that path
    excludes NOTHING and `origin !== "agent"` is the only predicate that can keep the row out. Keep
    the fixture under 100 `ready` docs (`DRIFT_SCAN_CAP`) or the assertion is meaningless. Add the
    `agent_promoted` counterpart so the "NOT `=== undefined`" choice is asserted, not just commented.
    TDD: RED first.

    ⚠ **Playbook:** `blueprint.ts` is watched by `onboarding.md`, which belongs to Lane 17.1. Its
    `### Phase 18` append is plan 18-09's. **Do NOT bump `onboarding.md`'s `Last verified` here or
    there** — Phase 18's contribution is this single conjunct; the rest of that playbook's
    outstanding verification is `17.1-10`'s.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/createdDocs.test.ts convex/blueprint.test.ts</automated>
  </verify>
  <done>The drift predicate has three conjuncts; both inline `"business_blueprint"` literals are gone; both drift tests (excluded, and promoted-counts) pass; the pre-existing `blueprint.test.ts` suite is green with zero edits to it.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/createdDocs.test.ts convex/blueprint.test.ts` — green.
- `pnpm --filter @pikar/backend test` — no new reds vs. the phase-start baseline.
- `pnpm exec turbo run typecheck --filter=@pikar/backend --force` — **delta 0** vs. the number in
  18-02-SUMMARY, and **zero** errors in any production `convex/*.ts` file.
- `git diff --stat` shows exactly two files: `blueprint.ts` and `createdDocs.test.ts`.
- `git diff docs/playbooks/onboarding.md` — **empty**. This plan writes no playbook.
</verification>

<success_criteria>
- The drift exclusion is TRUE by predicate, and its "promoted still counts" half is asserted.
- `17.1-10`'s measurement was not corrupted — proved by Task 1's recorded observation, not assumed.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-10-SUMMARY.md`.
Record: the Task-1 observation verbatim (was 17.1-10 closed, and on what evidence), the final
three-conjunct predicate, and confirmation that `onboarding.md` was NOT touched by this plan.
</output>
