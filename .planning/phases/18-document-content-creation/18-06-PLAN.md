---
phase: 18-document-content-creation
plan: 06
type: execute
wave: 4
depends_on: ["18-02", "18-04", "18-05"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
autonomous: true
requirements: [ACTN-04]

must_haves:
  truths:
    - "The agent can call createDocument({topic, form}) and a governed artifact lands in the tenant's vault."
    - "The tool returns a plain sentence — never bytes, never a URL, never a raw _id. A refusal is a returned string, never a throw out of the governed loop."
    - "Creation has NO external side effect: the tool body reaches no gmail send, no workflow.start, no internal.cockpit.* dispatch."
    - "Long-form gets a derived PDF (storageId present ⇒ a Download button); short-form does not (storageId absent), and short-form is drafted by content-drafter, not document-drafter."
    - "The audit row for a creation carries hashes, ids, enums and booleans ONLY — never the topic and never the prose."
    - "One vaultSources row per turn carries all N docIds, which is what makes 'make the second one shorter' addressable."
    - "A turn can drive createDocument OFFLINE, with no gateway key, via a SMOKE:: sentinel — the only way any e2e can produce a created document."
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "the createDocument tool — a sibling closure in buildCockpitTools — and its four-site SMOKE op registration"
      contains: "createDocument: tool("
    - path: "packages/backend/convex/cockpitTools.test.ts"
      provides: "the SC2 no-external-side-effect source scan and the renderer-bypass guard"
      contains: "createDocument"
  key_links:
    - from: "packages/backend/convex/llm.ts createDocument"
      to: "internal.vault.insertCreatedDoc / internal.vault.patchCreatedDoc"
      via: "ctx.runMutation from the \"use node\" action — the recordAttachments shape"
      pattern: "internal\\.vault\\.(insert|patch)CreatedDoc"
    - from: "packages/backend/convex/llm.ts createDocument"
      to: "internal.llm.draftDocument"
      via: "skillName selected by form — the ONLY thing that reaches the content-drafter body"
      pattern: "skillName"
    - from: "packages/backend/convex/llm.ts AgentSmokeOp"
      to: "SMOKE_OP_TOOL.create"
      via: "the CLOSED four-site SMOKE registration — a new op cannot compile without naming its tool"
      pattern: "create: \"createDocument\""
    - from: "packages/backend/convex/llm.ts createDocument"
      to: "internal.audit.log"
      via: "a refs-only document.created event emitted from the TOOL, not from cockpit.ts"
      pattern: "document\\.created"
---

<objective>
Add the `createDocument` tool: one key in `buildCockpitTools`, a closed `form` enum, an optional
`replace` #index carrying the locked replace-in-place revision, and a refs-only audit row.

Purpose: this is the surface the model actually calls. Everything before it is machinery; without
this the phase is a set of unreachable exports.
Output: Registration Checklist rows 3 and **16** (the SMOKE op), plus Patterns 2, 2b and 7.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-document-content-creation/18-CONTEXT.md
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@.planning/phases/18-document-content-creation/18-04-SUMMARY.md
@.planning/phases/18-document-content-creation/18-05-SUMMARY.md
@packages/backend/convex/cockpitTools.test.ts
@docs/playbooks/cockpit.md

**Read 18-RESEARCH.md § Pattern 2, § Pattern 2b, § Pattern 7 and Pitfall 6 before writing.**
Pattern 2 contains the exact tool shape; Pattern 2b contains the revision contract.

<interfaces>
```ts
// packages/backend/convex/vault.ts — landed in plan 18-04
internal.vault.insertCreatedDoc
  ({ tenantId, title, form: "short"|"long", markdown, contentHash, storageId? }) => Id<"vaultDocuments">
internal.vault.patchCreatedDoc
  ({ tenantId, threadId, index, title, form, markdown, contentHash, storageId? })
    => { ok: false } | { ok: true; oldStorageId?: Id<"_storage"> }

// packages/backend/convex/vaultSources.ts — landed in plan 18-04
internal.vaultSources.insert ({ tenantId, threadId, docIds, titles, count, createdAt, role?, snippet?, form? })
  // ⚠ `form` is OPTIONAL in the validator but MANDATORY at this call site: it is the ONLY data
  // source for plan 18-07's UPPERCASE type badge (`byThread` returns a `vaultSources` row, and the
  // short/long discriminator otherwise lives only on `vaultDocuments.kind`, which that row never
  // reads). Omit it and every short-form turn silently badges `DOCUMENT`.

// packages/backend/convex/llm.ts — landed in plan 18-05
internal.llm.draftDocument ({ tenantId, safeText, safeTextHash, skillVersion?, skillName? })
  => { title: string; markdown: string }

// packages/contracts/src/skill.ts — landed in plan 18-03
CONTENT_DRAFTER_SKILL | DOCUMENT_DRAFTER_SKILL

// shipped
markdownToPdf(title, markdown)          // llm.ts:3527
contentHash(s): Promise<string>         // convex/lib/hash.ts:5
```
</interfaces>

**Verified anchors at HEAD (2026-08-01):**
- tool record — `llm.ts:1015-1965` (CONTEXT.md's `1012-1975` is off).
- `generateAttachment` `:1275-1298` — the exact `tool({ description, inputSchema: jsonSchema<...>, execute })`
  shape to read off. Its description carries the confirm rule inline at `:1277`.
- `regenerateAttachment` `:1300-1330` — the 1-based `#index` revise idiom and the
  **delete-AFTER-the-new-ref-persists** ordering at `:1327`.
- `searchVault` `:1682-1760` — reads `plan.threadId` from `readPlan()` at `:1700`/`:1732`. That
  single call is both the cross-tenant guard and the key the Output card and the revision index are
  scoped by. **`threadId` NEVER comes from the model.**
- step callbacks `:2208-2222` / `:2223-2235` — step recording is CENTRAL. A new tool needs only the
  schema literal (plan 18-02) and the VERB entry (plan 18-02). **This tool records no step row itself.**
- `research.ts:188-211` — the refs-only audit shape to copy.
- **The SMOKE agent grammar — a CLOSED FOUR-SITE registration, all in this file.** `AgentSmokeOp`
  union `:2449-2463`, `parseAgentSmoke`'s switch `:2465-2528`,
  `const SMOKE_OP_TOOL: Record<AgentSmokeOp["kind"], StepTool>` `:2534-2549` (typed TOTAL, so a new
  op cannot compile without naming its tool), `runAgentSmokeOp`'s switch `:2551-2586`. There is
  **no `create=` op at HEAD**, which is why Task 1 adds one. `attach=<topic>` → `generateAttachment`
  is the idiom to copy.
- `PreviewModal.tsx:98` — `canDownload = Boolean(doc.storageId)`. The short-form/long-form UI
  consequence is already built; nothing to add.

⛔ **Do NOT add an audit call in `cockpit.ts`.** It emits exactly two events (`plan.canceled` `:719`,
`plan.rescheduled` `:768`) and `llmRedaction.test.ts:77-107` **pins that call-site count at exactly 2**.
Audit from the tool instead (the `calendarComplete.ts:73` precedent).
⛔ **Do NOT refactor or call `renderAndStore`** (Pitfall 6). It is a closure capturing `planId`, it
writes `plans.recordAttachments` on failure (wrong for a vault artifact), and it is hardwired to the
long-form drafter at `:908`. `createDocument` is a SIBLING closure reusing `scanText` ->
`internal.llm.draftDocument` -> `markdownToPdf` directly.
⛔ **`skills.test.ts` scans `convex/` for inline strings > 200 chars** (`MAX_INLINE_STRING = 200`).
SPLIT the tool `description` across concatenated string literals.
⛔ **WATCH-ONLY:** `actionType.ts`, `cockpit.ts` `_ARM_TABLE`, `plans.ts` `patchPlan`/`resetPlan`, the
`PlanCard` kind chain. No `ACTION_TYPES` member, no arm, no `plans.kind` widening. Editing any of
them means the design drifted — stop and re-read 18-CONTEXT.md § *Architecture shape*.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The createDocument tool — create path (SC1, SC7b, Pattern 7)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - `createDocument({ topic, form: "long" })` runs: PII `scanText` (fail-closed) -> `draftDocument({ skillName: DOCUMENT_DRAFTER_SKILL, skillVersion: skillVersions?.[skillName] })` -> `markdownToPdf` -> `ctx.storage.store` -> `insertCreatedDoc` with the resulting `storageId` -> a refs-only `document.created` audit -> one `vaultSources` row with `role: "created"`.
    - `createDocument({ topic, form: "short" })` runs the same chain with `skillName: CONTENT_DRAFTER_SKILL` and **NO** PDF and **NO** `storageId`.
    - `execute` returns `Promise<string>` — a plain sentence naming the title, never a URL, never bytes, never an `_id`.
    - A PII refusal or a draft failure returns a SENTENCE; it does not throw out of the governed loop.
    - The audit payload contains only `{ topicHash, form, vaultDocId: String(id), hasPdf }` — no topic text, no prose.
    - A turn whose text is `SMOKE::agent::create=long:SMOKE::route=direct_llm:: Quarterly one-pager` drives ONE governed `createDocument` call OFFLINE (no gateway key), and the SMOKE step row records `tool: "createDocument"` — read from `SMOKE_OP_TOOL`, so the invoke and the step row can never drift. ⚠ The NESTED `SMOKE::route=direct_llm::` prefix is part of the TOPIC and is load-bearing: `SMOKE::agent::create=` only picks the tool, it does NOT keep the model out of the loop. `parseSmoke` is `text.match(/^SMOKE::route=([a-z_]+)::/)` (`llm.ts:155-157`) and is `^`-anchored on the safeText `draftDocument` itself receives — without the prefix, `generateObject` fires for real, throws with no key, and no `vaultDocuments`/`vaultSources` row is written at all. Shipped precedent: `cockpit-attachment.spec.ts:21` — `SMOKE::agent::attach=SMOKE::route=direct_llm:: Quarterly proposal`.
    - The `vaultSources` row written by a created turn carries `form` — asserted by a source scan in `cockpitTools.test.ts`, not left to prose.
    - `create=short:…` drives the short-form branch. A malformed `create=` value (no colon) parses to `null` and drives nothing, exactly like a malformed `regenerate=`.
  </behavior>
  <action>
    Add ONE key to the tool record (`llm.ts:1015-1965`), as a sibling closure inside
    `buildCockpitTools`. Copy 18-RESEARCH.md § Pattern 2's shape exactly, including the SPLIT
    description literal and the closed `form` enum.

    **Claude's-discretion resolved by the planner: an EXPLICIT closed `form` argument, NOT
    inference.** Reasons, recorded so nobody re-opens it: (i) it is assertable in a convex-test —
    inference is only assertable against a live model; (ii) a closed JSON-Schema enum is this repo's
    established way to let the model steer without letting it inject prose (`setMode` `:1157`,
    `evaluateBusiness`'s framework enum); (iii) it deterministically selects BOTH the skill row and
    the PDF branch, which keeps the branch in code rather than in a heuristic.

    Mandatory invariants:
    - `threadId` comes from `readPlan()`, never from the model (the `searchVault` `:1700` idiom).
    - `skillVersion: skillVersions?.[skillName]` — the record is name-keyed, so no new plumbing.
      `skillVersions?.[CONTENT_DRAFTER_SKILL]` resolving to `undefined` is correct, not a gap.
    - Long-form: `markdownToPdf(draft.title, draft.markdown)` ->
      `ctx.storage.store(new Blob([bytes], { type: "application/pdf" }))` -> pass `storageId`.
      Short-form: no render, no store, no `storageId`. **"No PDF for short-form" is expressed as the
      structural ABSENCE of `storageId`** — not a flag, not a mime check. `PreviewModal`'s
      `canDownload` already does the rest.
    - **Multiple artifacts per turn are allowed** (locked). Write **ONE** `vaultSources` row per turn
      carrying ALL N `docIds`/`titles` (the existing `count` field already means N), with
      `role: "created"`, a `snippet` of the first ~240 chars, **and `form`** — the row's `form` is
      the ONLY thing plan 18-07's UPPERCASE type badge (`DOCUMENT` / `POST`) can read, because
      `byThread` returns a `vaultSources` row and the short/long discriminator otherwise lives only
      on `vaultDocuments.kind`. **NOT one row per artifact** — one-row-per-artifact makes
      `replace: 2` unreachable in Task 2.
    - Audit: ONE `internal.audit.log` from the TOOL, modelled on `research.ts:188-211`. Refs, hashes,
      ids, enums and counts only (CLAUDE.md §4).
    - **Spend: add NOTHING.** The loop's `preCall`/`recordSpend` and the kill-switch already govern
      the turn — `renderAndStore`'s own comment at `:887-892` says so. No artifact-count cap (locked).
    - The **locked trigger rule** (*explicitly asked -> create directly; agent-suggested -> confirm
      first*) lands in the tool `description`'s closing clause (the `generateAttachment:1277`
      precedent) and in `cockpit-agent.md` (plan 18-08) — **and NOWHERE ELSE.** 🚫 Do NOT invent a
      `confirmed: boolean` argument: a model-supplied confirmation flag is the model grading its own
      trigger, which is worse than the prose. There is no code branch and no offline test for it; it
      is verified by live UAT in plan 18-09.

    **The SMOKE op — Registration Checklist row 16, and it is a BLOCKER for plan 18-07's e2e.**
    `apps/web/e2e/` runs with **no gateway key**; the `SMOKE::agent::` sentinel is the ONLY way a
    live-stack turn produces a tool call offline. There is no `create=` op at HEAD, so without this
    edit plan 18-07's spec can never make an Output card appear. Add the op at **all four sites** in
    `llm.ts` — the record is typed TOTAL, so missing one is a compile error, not a silent gap:

    1. `AgentSmokeOp` union (`:2449-2463`): `| { kind: "create"; form: "short" | "long"; topic: string }`
    2. `parseAgentSmoke`'s switch (`:2465-2528`) — copy the `regenerate` case's first-colon split
       (the topic may itself carry a `SMOKE::` prefix):
       ```ts
       case "create": {
         // <short|long>:<topic> — split on the FIRST colon, like `regenerate=`/`personalize=`.
         const c = val.indexOf(":");
         if (c < 0) return null;
         const f = val.slice(0, c).trim();
         if (f !== "short" && f !== "long") return null; // the SAME closed enum the inputSchema enforces
         return { kind: "create", form: f, topic: val.slice(c + 1) };
       }
       ```
    3. `SMOKE_OP_TOOL` (`:2534-2549`): `create: "createDocument",`
    4. `runAgentSmokeOp`'s switch (`:2551-2586`):
       `case "create": return invokeTool(tools, name, { topic: op.topic, form: op.form });`

    Also extend the grammar comment block above `SMOKE_NOW_MS` with `| create=<short|long>:<topic>`,
    matching the existing lines.

    ⛔ Do NOT add a `replace` variant to the SMOKE grammar. Plan 18-07's spec needs ONE created
    document; the revision path is proven by `cockpitTools.test.ts` and by the live gate. A second op
    is a second registration surface for zero coverage.

    **`cockpitTools.test.ts`** — add the tool-shape assertions (closed `form` enum, `replace`
    optional, `execute` returns a string); a **source-scan assertion that the `createDocument` tool
    body's `vaultSources.insert` call site passes `form`** (with a non-vacuity floor — assert the
    sliced body was found and is non-empty; `form` is the badge's only data source and nothing else
    in the offline suite catches its absence); **and** a `parseAgentSmoke` round-trip assertion for
    `create=long:X` / `create=short:X` / the malformed `create=X` → `null` case. TDD: RED first.

    ⚠ **Every `create=` sentinel — in this test file, in plan 18-07's e2e spec, in plan 18-09's live
    gate — must carry the NESTED `SMOKE::route=direct_llm::` prefix inside the topic**, e.g.
    `SMOKE::agent::create=long:SMOKE::route=direct_llm:: Quarterly one-pager`. That is precisely why
    the `create` case splits on the FIRST colon: `val.slice(c + 1)` must hand the prefix through
    UNTRIMMED and UNSTRIPPED so `parseSmoke`'s `^`-anchored match still fires downstream. Assert it
    in the round-trip test: `create=long:SMOKE::route=direct_llm:: X` parses to
    `{ kind: "create", form: "long", topic: "SMOKE::route=direct_llm:: X" }`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/cockpitTools.test.ts convex/llmRedaction.test.ts</automated>
  </verify>
  <done>`createDocument` is a key in the tool record; both forms take the correct skill; short-form stores no `storageId`; the `vaultSources` row carries `role`/`snippet`/`form`; the `create=` SMOKE op exists at all four sites and `SMOKE_OP_TOOL.create === "createDocument"`; the audit payload is refs-only; `llmRedaction.test.ts` is green with `cockpit.ts`'s audit call-site count still exactly 2; the description contains no inline string over 200 chars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The replace #index revision path + the SC2 no-side-effect scan</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - `createDocument({ topic, form, replace: 1 })` routes to `patchCreatedDoc` instead of `insertCreatedDoc`, resolving `#1` against the latest `role: "created"` vaultSources row for the thread.
    - `{ ok: false }` (bad index / foreign tenant / a user upload) returns a plain sentence explaining nothing was changed. It does NOT throw.
    - On `{ ok: true, oldStorageId }` where `oldStorageId !== storageId`, the superseded PDF bytes are deleted with `ctx.storage.delete(oldStorageId)` **AFTER** the patch persists — no orphan, no dangling ref.
    - A revise writes a NEW `vaultSources` row with the same `docIds` and a fresh `titles`/`snippet`/`form`, so the Output card refreshes with zero new code.
    - SC2 (source scan): the `createDocument` tool body contains no `gmail`, no `workflow.start` and no `internal.cockpit.*` reference.
    - Renderer-bypass guard: `renderAndStore`'s html branch reads `renderHtmlDocument(draft.title, draft.markdown)` and the file contains no `encode(draft.markdown)`.
  </behavior>
  <action>
    Add the optional `replace: { type: "number", description: "1-based #index of a document already
    created in this conversation to rewrite in place." }` property to the SAME tool's `inputSchema`,
    with `required: ["topic", "form"]` — `replace` stays optional, create is the default shape.

    **One tool, not two** — a second tool key would cost a second `agentSteps.tool` literal, a second
    VERB entry, a second `##` teaching block in the CONTESTED `cockpit-agent.md`, and a second
    registration row that can silently rot. An optional property on a schema this plan is already
    writing costs **zero** registration surface. The repo precedent is exact: `regenerateAttachment`
    (`:1300-1330`) revises from a new topic at a 1-based `#index`.

    ⛔ NEVER let a raw `_id` reach the model — the index resolves inside `patchCreatedDoc`.
    ⛔ `origin` stays `"agent"` on a revise. A promoted doc (`agent_promoted`) fails the guard and
    cannot be silently rewritten. That is what the guard is for.

    **`cockpitTools.test.ts`** — add:
    (a) the SC2 scan: slice the `createDocument` tool body out of `llm.ts` source and assert it
        matches none of `/gmail/i`, `/workflow\.start/`, `/internal\.cockpit\./`. The file already
        carries the `// @vitest-environment node` pragma on line 1 — keep it.
    (b) the renderer-bypass guard from 18-RESEARCH.md § Pattern 6 (optional third guard): assert
        `renderAndStore`'s html branch calls `renderHtmlDocument(draft.title, draft.markdown)` and
        that `llm.ts` contains no `encode(draft.markdown)`.
    Both need a non-vacuity floor (assert the slice was actually found and is non-empty) or they pass
    trivially when the anchor moves.

    MUTATION CHECK (mandatory, record in the SUMMARY): insert a harmless `internal.cockpit.` reference
    inside the tool body and confirm the SC2 scan goes RED; revert and confirm green.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/cockpitTools.test.ts convex/createdDocs.test.ts convex/dispatchGuard.test.ts</automated>
  </verify>
  <done>`replace` is optional on the same tool key; a refusal returns a sentence; the superseded PDF is deleted after the patch persists; the SC2 scan and the renderer-bypass guard both pass with non-vacuity floors and the SC2 mutation check was observed RED then reverted green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` — no new reds vs. the phase-start baseline; `traceParity.test.ts`, `llmRedaction.test.ts` and `dispatchGuard.test.ts` all green.
- `pnpm exec turbo run typecheck --filter=@pikar/backend --force` — **delta 0** vs. 18-02-SUMMARY's number, zero errors in production `convex/*.ts`.
- `git diff --stat packages/core/src/actionType.ts packages/backend/convex/cockpit.ts packages/backend/convex/plans.ts packages/backend/convex/traceParity.test.ts docs/playbooks/watch.json` — **empty**. Any diff means the design drifted or a new module was created.
- SC#2 remains structurally true at zero cost and this plan must not weaken it: `executePlan` is a `tenantMutation` unreachable by name from the tool loop (`dispatchGuard.test.ts:222-252`), `patchPlan` deliberately cannot write a delivery handle (`plans.ts:200-203`), and `startFanout` (`cockpit.ts:462`) is the sole `workflow.start` site.
</verification>

<success_criteria>
- SC#1: the agent produces a standalone artifact as a tenant vault asset; only a ref is returned, never bytes.
- SC#2: creation has no external side effect, asserted by a source scan; delivery still crosses the shipped Approve gate through the untouched attachment path.
- SC#3: audit/telemetry carry refs only.
- SC#7: revision replaces in place, reachable from the model by `#index`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-content-creation/18-06-SUMMARY.md`.
Record: the final tool `description` text and the `form`/`replace` schema verbatim (plan 18-08's
`cockpit-agent.md` section must teach EXACTLY this), the audit payload keys, the mutation-check
result, and **the exact SMOKE sentinel string plan 18-07's spec must send** — it MUST carry the
nested route prefix inside the topic, or the turn is not offline:
`SMOKE::agent::create=long:SMOKE::route=direct_llm:: Quarterly one-pager`.
⚠ State plainly that the tool is INVISIBLE to the model until plan 18-08 teaches it in the active
`cockpit-agent` body — a tool the body does not teach is a tool that does not exist.
</output>
