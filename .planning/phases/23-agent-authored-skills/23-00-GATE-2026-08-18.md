# Phase 23 prerequisite gate — run 2026-08-18, before the first edit

Plan `23-01-PLAN.md` Task 1 opens with a blocking gate rather than an edit. This file is that
gate's result. It exists because the gate did NOT fully pass and execution proceeded anyway on an
explicit owner decision — an unrecorded deviation is how a gate stops meaning anything.

## Result

| Check the plan requires | Result |
|---|---|
| Phase 21 `21-07-SUMMARY.md` completed | PASS — present |
| Phase 21 schema-valid `21-LIVE-RESULT.json` | **FAIL — the file does not exist** |
| SKILL-01 closed in `REQUIREMENTS.md` + roadmap | PASS — `[x]` in both |
| Phase 22 verification `passed` (not `human_needed`) | PASS — `22-VERIFICATION.md` `status: passed` |
| GOVN-01 checked complete in `REQUIREMENTS.md` | PASS — `[x]` |
| Phase 22 roadmap row closed | **FAIL at read time, FIXED here** — `ROADMAP:386` was `- [ ]` |
| Landed Phase 21/22 seams still match this plan's assumptions | PASS — read-only inventory below |

## The two failures, stated exactly

**1. `21-LIVE-RESULT.json` does not exist, and was not written by accident.** Phase 21 closed on
2026-08-18 with all three of its success criteria proven live, but plan `21-08` steps 3-4 —
*tenant runtime attribution*, i.e. which registry row a specialist run actually used — were not
executed and could not be: the author tenant `kn790hj6…` is `e2e-wave6@pikar.test`, a synthetic row
with no recoverable password and no reset flow, and a fresh specialist turn is a paid model call,
contradicting that plan's own "$0" truth. The evidence is `21-LIVE-PARTIAL-2026-08-18.md`. The
artifact was deliberately withheld because a half-filled object reads as a pass.

**This file has NOT been created to satisfy the gate.** Fabricating it would be the exact defect
class this repository has shipped before: an unobserved fact stored as an observed one.

**2. Phase 22's roadmap checkbox was stale.** The phase body already read `3/3 complete` and
`22-VERIFICATION.md` reads `status: passed`; only the top-line box lagged (the known
`gsd-tools phase complete` roadmap no-op). Flipped to `[x]` during this gate, carrying forward the
limit `22-VERIFICATION.md` records about itself: the owner/non-owner `/ops` DOM half was closed at
`29103e9` by `opsPresentation.test.ts` component/mount evidence, **not** by a live browser.

## Owner decision

Execute waves 1-5 (`23-01` … `23-05`, all `autonomous: true`, offline, $0). Waves 6-9 STOP for a
re-cut. The deviation is not inherited silently: every wave 1-5 summary points here.

## Why waves 1-5 are unaffected, stated as a falsifiable claim

The missing artifact concerns *live runtime attribution*. Waves 1-5 add a data vocabulary, a
candidate-only writer, a bounded tool grant, eval fixtures, and an owner-gated activation
transition — none of which reads, asserts, or depends on which row a live specialist turn used.
If a wave-1-5 change is ever found to depend on that fact, this claim is wrong and the work is
blocked, not merely annotated.

## Why waves 6-9 are NOT merely "deferred paperwork"

`23-08-PLAN.md` carries the must-have: *"One authorized author-tenant runtime turn attributes the
exact active tenant skill id/name/version/hash."* That is the **same step** `21-08` could not run,
for the same reason. It will not become runnable by being reached later. The re-cut must answer it
against the identity topology that actually exists — owner `kn73kmc…` (Google) and the second
human identity `kn735m0c…` (`joel.feruzi+phase21b@gmail.com`, admitted through the BETA-01 invite
door, **invite already spent**) — and must price the paid specialist turn rather than assert $0.

## Read-only seam inventory (all present, all unchanged)

`packages/contracts/src/skill.ts`: `GATED_SKILLS`, `isGatedSkill`, `USER_AUTHORABLE_SKILLS`,
`USER_AUTHORABLE_SKILL_METADATA`, `isUserAuthorableSkill`, `USER_SKILL_ADAPTATION_MAX_BYTES`,
`composeUserSkillBody`, `EvalEvidence`, `EvalEvidenceTenantTarget`, `hasPassingEvidence`,
`hasPassingTenantEvidence`.

`packages/backend/convex/schema.ts`: `tenantSkills` with `author: v.union(v.literal("system"),
v.literal("user"))` and the comment at line 230 — *"Phase 23 may append `agent` here without
granting it activation."* Phase 21 pre-cut this seam for Phase 23.

`packages/backend/convex/skills.ts`: `insertCandidate`, `activateSkill`, `activateCandidate`,
`candidatesForReview`, `recordEvalEvidence`, `loadEffectiveSkill`, `getEffectiveSkill`,
`publishUserCandidate`, `myUserSkills`, `recordTenantEvalEvidence`, `inspectTenantSkill`,
`tenantCandidatesForReview`, `activateTenantCandidate`, `rollbackTenantSkill`.
