---
phase: 23-agent-authored-skills
plan: 07
type: execute
wave: 7
depends_on: ["23-06"]
files_modified:
  - .planning/phases/23-agent-authored-skills/23-EVAL-RESULT.json
autonomous: false
requirements: [SKILL-02]

must_haves:
  truths:
    - "The paid run targets the literal candidate id and deployment/suite identity frozen in 23-LIVE-HANDOFF; no candidate is republished, substituted, approved, or activated."
    - "Fresh exact-row readback proves current full-suite passing evidence on that row while status remains candidate and ownerApproval remains absent."
    - "Tenant B's preserved authenticated non-owner session calls the public activateAgentCandidate mutation with the exact candidate id and receives OWNER_REQUIRED before handler reads/writes."
    - "Candidate/effective/global/foreign/audit state is byte-equivalent across the non-owner refusal, proving evidence alone cannot activate."
    - "One exact, recursively closed, refs-only eval-result artifact binds the evidence/refusal state to the immutable handoff hash for Plan 23-08."
  artifacts:
    - path: ".planning/phases/23-agent-authored-skills/23-EVAL-RESULT.json"
      provides: "Refs-only full-eval and public non-owner-refusal result bound to handoff bytes"
      contains: "phase23-eval-result.v1"
  key_links:
    - from: "23-LIVE-HANDOFF candidate.id"
      to: "eval:golden --tenant-skill"
      via: "literal exact id after deployment/suite/pre-state revalidation"
      pattern: "tenant-skill"
    - from: "foreignStorageStateRef"
      to: "api.skills.activateAgentCandidate"
      via: "browser context using B's saved JWT against the public mutation endpoint"
      pattern: "OWNER_REQUIRED"
---

<objective>
Consume the immutable handoff, obtain fresh explicit authorization for the full exact-row eval, run
the held-out gate, prove evidence alone cannot bypass requireOwner, and freeze those facts before
any owner activation. Synthetic-tenant runtime attribution is not part of this gate.

Purpose: separate the paid evidence boundary from the irreversible human activation boundary so no
checkpoint can lose or ambiguously mutate the exact candidate state.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/23-agent-authored-skills/23-06-SUMMARY.md
@.planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json
@.planning/phases/23-agent-authored-skills/validate-live-artifact.mjs

<interfaces>
No task calls `publishAgentCandidate`, sends an authoring request, bootstraps another owner, or
invokes activation as owner. The paid eval command is built from `handoff.candidate.id` only.
Tenant B calls the PUBLIC Convex HTTP mutation using the JWT loaded at runtime from the fixed
gitignored `handoff.authRefs.foreignStorageStateRef`; the JWT never enters logs/artifacts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate the immutable handoff and all zero-spend prerequisites</name>
  <files>23-LIVE-HANDOFF.json and configured deployment read-only</files>
  <action>
Validate the handoff with the shared exact-schema validator and hash its bytes. Re-run source and
candidate inspectors twice and machine-compare deployment/suite, candidate provenance/source/
lineage/hash/status, evidence/approval absence, baseline, effective/global/foreign state, and auth
path labels. Load both storage states without printing them; decode JWT subjects and prove A/B ids
match the handoff and differ. Re-run runner self-check and focused free tests. Abort if any byte,
suite, dependency, auth, or clean-gate fact differs.
  </action>
  <verify><automated>node .planning/phases/23-agent-authored-skills/validate-live-artifact.mjs handoff .planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json --forbid-env PHASE23_PRIVATE_NEEDLE &amp;&amp; pnpm --filter @pikar/backend eval:golden -- --self-check &amp;&amp; pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>The exact parked row, suite, deployment, and two reusable identities still match the handoff.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Obtain fresh authorization for the full exact-row eval</name>
  <files>configured deployment state; no repository write</files>
  <action>
Present candidate id/name/version/body hash, suite revision/hash/count, models, and a hard eval cost
cap. Itemize the paid work as one full unfiltered held-out/adversarial eval against the literal
handoff candidate id. State that evidence writes only on an all-green full suite, activation is
excluded from this task, and no specialist runtime-attribution turn is requested or authorized. A
prior approval is invalid. Do not continue on silence, ambiguity, or a budget-only answer.
  </action>
  <verify>Record the owner's authorization verbatim and bind it to candidate/model/suite/eval cap.</verify>
  <done>The owner explicitly authorized the exact full eval.</done>
  <resume-signal>Owner explicitly authorizes the described eval under the displayed cap.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the exact full gate and prove it does not auto-activate</name>
  <files>configured deployment state; no repository source edit</files>
  <action>
Run `pnpm --filter @pikar/backend eval:golden -- --tenant-skill <handoff candidate id>` with no
filter. Require the runner's current-suite preflight before its first Convex/model call. Record
run id, pass/total, retries, model, actual cost, suite identity, and exact tenant target. Re-inspect
twice: candidate must remain parked, gatePassed true, approval absent, active/baseline/global/foreign
unchanged. A failed/partial/over-cap run writes no result artifact and does not proceed.
  </action>
  <verify><automated>$h = Get-Content -Raw '.planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json' | ConvertFrom-Json; pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill $h.candidate.id --foreign-tenant $h.foreignBefore.tenantId --expect-status candidate --expect-evidence passing --expect-gate-passed true --expect-rollback-eligible false --json</automated></verify>
  <done>Current full-suite exact evidence exists and nothing is active or approved.</done>
</task>

<task type="auto">
  <name>Task 4: Use B's public non-owner call and freeze the evidence-only boundary</name>
  <files>.planning/phases/23-agent-authored-skills/23-EVAL-RESULT.json</files>
  <action>
Open a browser/API context from B's fixed foreign storage state, extract its JWT only in memory, and
POST the exact candidate id to public `skills:activateAgentCandidate`. Require `OWNER_REQUIRED`.
Read exact candidate/effective/global/foreign/audit snapshots before and after and require canonical
byte equality; candidate already has valid evidence so the refusal is non-vacuously authorization.

Write `phase23-eval-result.v1` with handoff SHA-256, candidate/deployment/suite refs, eval run/counts/
retries/model/cost, passing candidate snapshot, B subject/tenant hash (not JWT/email), refusal code,
and before/after state hashes/counts. No approval/runtime/rollback fields belong in this schema.
Validate it with the shared exact recursive schema/value scanner and do not modify it in Plan 23-08.
  </action>
  <verify><automated>node .planning/phases/23-agent-authored-skills/validate-live-artifact.mjs eval .planning/phases/23-agent-authored-skills/23-EVAL-RESULT.json --forbid-env PHASE23_PRIVATE_NEEDLE</automated></verify>
  <done>One immutable artifact proves exact full evidence plus a real public non-owner refusal with zero state change.</done>
</task>

</tasks>

<verification>
- Handoff is exact-schema valid and byte-hashed before spend.
- Full unfiltered current-suite evidence targets one literal candidate id.
- Status remains candidate and approval absent after evidence.
- A real authenticated B public call is refused by owner authorization with unchanged state.
- No owner activation, runtime-attribution probe, rollback, or closure occurs.
</verification>

<success_criteria>
- Evidence alone is proven insufficient both by stored state and a public non-owner call.
- The result carries no credential/content and is immutable input to the owner activation plan.
- Paid scope/cost is explicit and measured.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-07-SUMMARY.md` with handoff hash, verbatim
authorization, eval run/cost/counts, B refusal/state hashes, and eval-result hash. Keep SKILL-02 open.
</output>
