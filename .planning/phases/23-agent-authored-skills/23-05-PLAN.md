---
phase: 23-agent-authored-skills
plan: 05
type: execute
wave: 5
depends_on: ["23-04"]
files_modified:
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - apps/web/app/(app)/ops/page.tsx
  - apps/web/app/(app)/ops/tenantSkillReview.test.ts
  - apps/web/app/(app)/dashboard/workspace/SkillAuthoringPanel.tsx
  - apps/web/app/(app)/dashboard/workspace/skillAuthoring.test.ts
  - docs/playbooks/skill-registry.md
  - docs/playbooks/authorization.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "Agent activation is a distinct ownerMutation taking one exact candidate id; user activation refuses agent rows and agent activation refuses user rows."
    - "The one atomic transition requires current full-suite exact-row evidence and writes ownerApproval plus active status together—neither can exist alone after a failed mutation."
    - "Only a genuinely active row or server baseline is rollback eligible; rollback is owner-only, evidence-exempt, one transaction, and never edits body/provenance/evidence/approval."
    - "The owner UI shows bounded agent provenance/diff/eval refs and explicit activation/rollback controls; non-owners mount none of that content."
  artifacts:
    - path: "packages/backend/convex/skills.ts"
      provides: "Owner/eval conjunction and immutable one-write rollback for agent rows"
      contains: "activateAgentCandidate"
    - path: "packages/backend/convex/skills.test.ts"
      provides: "Non-vacuous four-cell truth table, isolation, approval atomicity, and rollback proof"
    - path: "apps/web/app/(app)/ops/page.tsx"
      provides: "Owner-only agent candidate review and action surface"
  key_links:
    - from: "activateAgentCandidate ownerMutation"
      to: "transitionSkillActivation"
      via: "private agent mode requiring hasPassingAgentTenantEvidence and supplying server-derived approval patch"
      pattern: "ownerApproval"
    - from: "ops agent Activate"
      to: "exact candidate id"
      via: "api.skills.activateAgentCandidate; never name/version/newest"
      pattern: "activateAgentCandidate"
---

<objective>
Add the separate human-owner half of the gate and the bounded owner review surface, then prove the
eval+owner conjunction and one-write rollback for agent-authored rows.

Purpose: an agent-authored candidate must be impossible to activate through Phase 21's user path or
any identity-free path. This plan is offline/free and does not perform a live eval or activation.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/design/BRAND.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/23-agent-authored-skills/23-04-SUMMARY.md
@.planning/phases/22-owner-authorization-primitive-requireowner/22-VERIFICATION.md
@packages/backend/convex/skills.ts
@apps/web/app/(app)/ops/page.tsx
@docs/playbooks/authorization.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a separate exact-id owner/eval activation transition for agent rows</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - `activateAgentCandidate` is exported only as `ownerMutation`; non-owner refusal occurs before
      handler reads, even when the candidate already has valid evidence.
    - It requires `author=agent`, `status=candidate`, no existing approval, and
      `hasPassingAgentTenantEvidence` for the exact row/current suite.
    - One private transition archives the tenant/name active row, marks it rollback eligible, then
      patches the exact target active with rollback eligibility and server-derived owner approval.
    - Approval uses `ctx.userId`, current server time, and eval run id parsed from the same evidence;
      no client field supplies it and a failed evidence gate leaves no approval.
    - Existing user/global/internal activation semantics remain green and cannot activate agent rows.
  </behavior>
  <action>
Extend the private activation planner with an explicit agent mode; do not make the internal global
activation helper owner-aware. Preserve the single active-status patch site already pinned by
`skills.test.ts`. Add a distinct refs-only `skill.agent_candidate_activated` audit event. The agent
tool region must not name this export. Keep approval embedded on the row so Convex atomicity makes
approval+activation indivisible.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Only the exact current-suite-evaluated agent row plus a real owner mutation can become active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the four-cell conjunction, cross-tenant isolation, and immutable rollback</name>
  <files>packages/backend/convex/skills.test.ts</files>
  <behavior>
    - no eval + non-owner/no act: candidate remains; positive candidate witness exists.
    - eval + non-owner: `OWNER_REQUIRED`, all row/active/audit bytes unchanged.
    - no eval + owner: `EVAL_GATE`, ownerApproval absent and active unchanged.
    - eval + owner: exact row active, prior active archived/eligible, approval owner/run recorded.
    - Tenant A evidence/id/owner act never changes colliding Tenant B; stale suite evidence fails.
    - Rollback to the exact prior-active/baseline id is one transaction, changes only statuses and
      eligibility plane, preserves candidate body/hash/evidence/approval/source lineage, and cannot
      target a never-active candidate even if its status is manually archived.
  </behavior>
  <action>
Build the truth table against real convex-test identities and an existing candidate in every cell.
Compare complete before/after row snapshots for refusals. Mutation-check removal of owner wrapper,
eval predicate, candidate id, suite identity, or rollbackEligible; each must turn its named test red.
Use a two-tenant same-name/version collision so name/version evidence cannot accidentally pass.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Both independent gates and one-write rollback are non-vacuously mutation-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tenant status and owner review surfaces without widening activation</name>
  <files>packages/backend/convex/skills.ts, apps/web/app/(app)/ops/page.tsx, apps/web/app/(app)/ops/tenantSkillReview.test.ts, apps/web/app/(app)/dashboard/workspace/SkillAuthoringPanel.tsx, apps/web/app/(app)/dashboard/workspace/skillAuthoring.test.ts</files>
  <behavior>
    - The bounded review query includes user and agent rows with a closed author discriminant; agent
      rows show Executive/source refs, current base/candidate diff, evidence counts/run, and approval state.
    - Fixture prompts/expected outputs/raw evidence and foreign user data never render.
    - Agent rows call only `activateAgentCandidate`; user rows retain `activateTenantCandidate`.
    - Activate stays disabled until exact current-suite evidence passes; rollback choices include
      only non-active `rollbackEligible` rows.
    - The whole review component mounts once inside exact `isOwner`; tenant-visible ops sections remain.
    - The tenant's existing authoring panel lists its own user- and agent-authored adaptations with
      an honest author/status label and strict gate boolean, but still returns no row id, base/full
      body, raw evidence, owner identity, fixture, or activation control.
  </behavior>
  <action>
Evolve the existing owner panel rather than add another admin page. Add a tenant-scoped bounded read
(or carefully evolve the landed read while preserving its public disclosure contract) so the
workspace authoring panel can show the agent-created adaptation the user explicitly requested.
Preserve brand tokens, keyboard focus, loading/empty/error/busy states, and bounded behavior.
Render source refs only in the owner review and only as shortened opaque ids, not content. Keep UI
hiding as presentation only—the ownerMutation remains the boundary.
  </action>
  <verify><automated>pnpm --filter web exec vitest run 'app/(app)/ops/tenantSkillReview.test.ts' 'app/(app)/dashboard/workspace/skillAuthoring.test.ts' &amp;&amp; pnpm --filter web exec tsc --noEmit</automated></verify>
  <done>The tenant sees honest inert status, the owner sees the exact review/actions, and neither surface widens authority.</done>
</task>

<task type="auto">
  <name>Task 4: Close playbook/watch gates and source-separation mutations</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/authorization.md, docs/playbooks/cockpit.md, packages/backend/convex/skills.test.ts, apps/web/app/(app)/ops/tenantSkillReview.test.ts, apps/web/app/(app)/dashboard/workspace/skillAuthoring.test.ts</files>
  <action>
Document the independent eval and owner trust boundaries, exact approval state, identity-free runner
separation, and rollback exemption. Source-scan that `authorSkillCandidate` cannot reference either
activation export and that only the owner panel does. Apply/restore UI mount removal and backend
wrapper downgrade mutations. Run playbook and diff gates. Record `$0 spend` and that no live state
was changed.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1 &amp;&amp; pnpm --filter web exec vitest run 'app/(app)/ops/tenantSkillReview.test.ts' 'app/(app)/dashboard/workspace/skillAuthoring.test.ts' &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The owner/eval boundary is documented, watched, source-separated, and mutation-proven.</done>
</task>

</tasks>

<verification>
- Every truth-table refusal compares unchanged state, not just an error string.
- Exactly one active-status patch site exists for tenant rows.
- Agent approval is written in the same transaction that activates and survives later rollback unchanged.
- No paid/live call occurs.
</verification>

<success_criteria>
- An active agent row without exact current-suite evidence and owner approval is impossible through exported paths.
- User activation, agent activation, internal global activation, and rollback stay explicitly separated.
- Owner UI exposes only bounded review data and exact actions.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-05-SUMMARY.md` with truth-table results,
activation/approval audit shapes, UI checks, restored mutations, and `$0 spent`.
</output>
