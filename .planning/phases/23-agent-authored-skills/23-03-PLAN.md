---
phase: 23-agent-authored-skills
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/runCockpitAgent.test.ts
  - packages/backend/convex/llmRedaction.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - packages/backend/convex/traceParity.test.ts
  - docs/playbooks/agent-runtime.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "authorSkillCandidate is constructed only for a real Executive-Agent turn with trusted thread and turn lineage."
    - "Specialist, ingestion, voice, toolless, smoke-without-lineage, and generic test contexts do not contain the authoring closure at all."
    - "The tool input is only allowed name plus bounded adaptation; its return is inert refs/status and never base body, fixtures, evidence, approval, or activation."
    - "The real runAgentLoop path threads the grant and lineage; a test-only direct builder cannot be the sole working route."
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "Executive-only structurally constructed authorSkillCandidate tool"
      contains: "grantSkillAuthoring"
    - path: "packages/backend/convex/runCockpitAgent.test.ts"
      provides: "Real-loop candidate creation and source-lineage proof"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "Closed activity label for authorSkillCandidate"
  key_links:
    - from: "runAgentLoop toolNames === undefined"
      to: "buildCockpitTools agentContext.grantSkillAuthoring"
      via: "derived code-owned Executive grant plus threadId/turnId"
      pattern: "grantSkillAuthoring"
    - from: "authorSkillCandidate.execute"
      to: "internal.skills.publishAgentCandidate"
      via: "trusted tenant/thread/turn injection"
      pattern: "publishAgentCandidate"
---

<objective>
Expose the inert writer as one structurally Executive-only tool in the existing governed cockpit
loop, with real-loop, trace, redaction, and structural-absence proof.

Purpose: capability minimization must be code shape, not a prompt instruction or a post-construction
filter. This plan still adds no activation/eval/owner control and performs no paid model call.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/23-agent-authored-skills/23-02-SUMMARY.md
@packages/backend/convex/llm.ts
@packages/backend/convex/cockpitTools.test.ts
@packages/backend/convex/runCockpitAgent.test.ts
@docs/playbooks/agent-runtime.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct one bounded authoring tool only under the Executive grant</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - The tool record key is absent unless grant + tenant + threadId + turnId are all present.
    - The JSON schema has exact keys `{name, authoredBody}`, closed allowed names, required fields,
      and `additionalProperties:false`.
    - Execution injects tenant/source refs and calls only `publishAgentCandidate`.
    - Tool output contains candidate id/name/version/status/inserted and awaiting-review copy only.
    - Its source region contains no activation, evidence, approval, owner, scheduler, request,
      delivery, tool-grant, or eval-fixture access.
  </behavior>
  <action>
Follow the existing `grantDispatch` conditional-spread pattern. Build the closure only inside the
granted branch; do not construct then filter it. Reuse `AGENT_AUTHORABLE_SKILLS` for the JSON enum.
Do not edit the cockpit-agent skill body or seed a skill candidate in this plan—the tool description
must carry the explicit-use guidance while the hard guarantee stays in code.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/cockpitTools.test.ts --maxWorkers=1</automated></verify>
  <done>The only new capability is a two-field candidate publisher, absent outside the Executive grant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the grant through the real loop and close trace parity</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/schema.ts, packages/backend/convex/runCockpitAgent.test.ts, packages/backend/convex/traceParity.test.ts, apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <behavior>
    - `runAgentLoop` derives authoring permission only when `toolNames === undefined`, and passes the
      actual tenant/thread/turn already used by activity tracing.
    - A scripted real loop invokes the tool and persists one candidate whose source ids match the
      real loop, while active id/status remain unchanged.
    - A specialist allow-list—even one containing the literal tool name—cannot obtain it because
      the closure was never built.
    - `agentSteps.tool` and the UI verb map both contain exactly `authorSkillCandidate`; parity tests
      prevent a swallowed activity step.
  </behavior>
  <action>
Add the optional grant to the existing `agentContext`, and derive it at the one site where
`toolNames` and trusted lineage coexist. Ensure the production `runCockpitAgent -> runAgentLoop ->
buildCockpitTools` path supplies it. Extend the closed trace union/label exactly once. Use the
scripted model seam for zero-spend execution; do not rely only on `__invokeCockpitTool`.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/cockpitTools.test.ts convex/runCockpitAgent.test.ts convex/traceParity.test.ts --maxWorkers=1</automated></verify>
  <done>The production Executive loop can author a candidate and every specialist remains structurally unable.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no content/capability leakage and document the new grant</name>
  <files>packages/backend/convex/llmRedaction.test.ts, docs/playbooks/agent-runtime.md, docs/playbooks/cockpit.md</files>
  <action>
Plant high-entropy adaptation text, invoke the scripted real loop, positively witness the candidate
and activity step, and scan audit/DLQ/telemetry/error/tool returns for content leakage. Snapshot the
Executive and specialist tool key sets. Apply/restore three required mutations: unconditional tool
construction, deriving the grant from `toolNames.includes(...)`, and adding an activation call to
the tool region. Update both playbooks with the explicit-intent-only v1 rule and the structural
grant boundary. Record that no cockpit skill body was changed and no live selection behavior is yet
claimed; that is Plan 23-06's browser gate.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/cockpitTools.test.ts convex/runCockpitAgent.test.ts convex/llmRedaction.test.ts convex/traceParity.test.ts --maxWorkers=1 &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>Structural absence, real-loop wiring, trace visibility, and log privacy are mutation-proven.</done>
</task>

</tasks>

<verification>
- Exactly one existing `generateText` loop remains; no authoring loop or scheduler exists.
- Executive real-loop invocation creates a candidate, not an active row.
- Specialist and generic contexts do not contain the authoring tool closure.
- No prompt body or adaptation reaches log/control planes.
</verification>

<success_criteria>
- The Executive Agent can reach candidate creation only through one bounded tool.
- Capability withholding is by construction and survives malicious tool-name requests.
- No activation/evidence/owner capability was added to any model-visible record.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-03-SUMMARY.md` with tool key snapshots,
real-loop lineage evidence, trace parity, redaction results, and restored mutations.
</output>
