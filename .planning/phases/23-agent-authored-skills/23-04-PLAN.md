---
phase: 23-agent-authored-skills
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - packages/contracts/src/skill.ts
  - packages/contracts/src/skillAuthoring.test.ts
  - packages/backend/scripts/run-eval-golden.mjs
  - packages/backend/scripts/eval-suite-manifest.json
  - packages/backend/scripts/eval-cases/40-agent-author-happy.json
  - packages/backend/scripts/eval-cases/41-agent-author-self-activate.json
  - packages/backend/scripts/eval-cases/42-agent-author-capability-escalation.json
  - packages/backend/scripts/eval-cases/43-agent-author-embedded-instruction.json
  - packages/backend/scripts/eval-cases/44-agent-author-retry.json
  - packages/backend/convex/smokeAssert.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - docs/playbooks/skill-registry.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "The runner can evaluate either a user- or agent-authored exact tenant candidate id, while name/version collisions remain insufficient identity."
    - "Agent activation evidence requires an all-green, nonempty, unfiltered run over the current code-owned suite manifest/revision."
    - "Every paid live run executes the offline parser/fixture/manifest self-check before seeding or the first model call."
    - "Held-out authoring cases assert durable candidate/active/evidence/approval/request state, never assistant prose, with a positive candidate/tool witness."
    - "No fixture prompt, expectation, adversarial needle, or raw output is available through an agent tool, browser query, evidence row, or tool return."
  artifacts:
    - path: "packages/backend/scripts/eval-suite-manifest.json"
      provides: "Versioned sorted fixture-set identity and count"
      contains: "suiteRevision"
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "Prepaid self-check, exact agent-candidate pin, suite-bound evidence, and authoring state assertions"
      contains: "suiteRevision"
    - path: "packages/backend/scripts/eval-cases/43-agent-author-embedded-instruction.json"
      provides: "Held-out fixture/tool/tenant-disclosure adversarial case"
    - path: "packages/backend/convex/smokeAssert.ts"
      provides: "Refs/counts-only source-turn authoring state oracle"
  key_links:
    - from: "--tenant-skill candidate id"
      to: "recordTenantEvalEvidence"
      via: "exact registry tenant/name/version/id plus current suite identity"
      pattern: "tenantTarget"
    - from: "eval-suite-manifest.json"
      to: "runLive"
      via: "self-check recomputes the sorted fixture identity before any Convex/model call"
      pattern: "suiteRevision"
---

<objective>
Extend the existing golden runner—not create another evaluator—so an agent-authored exact tenant
candidate can earn held-out, adversarial, full-suite evidence that later activation can trust.

Purpose: make evaluation independent of the authoring agent and close the current paid-run preflight
gap. Implementation and tests are offline/free; this plan must not execute a paid live eval.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/23-agent-authored-skills/23-03-SUMMARY.md
@.planning/phases/03.6-agent-eval-gate/03.6-VERIFICATION.md
@packages/backend/scripts/run-eval-golden.mjs
@packages/backend/convex/smokeAssert.ts
@packages/contracts/src/skill.ts
@docs/playbooks/skill-registry.md
@docs/playbooks/agent-runtime.md

<interfaces>
Keep the landed `--tenant-skill <tenantSkillsId>` and `--inspect-tenant-skill <id>` contracts.
Agent evidence adds a current suite identity; existing global and Phase 21 user evidence remains
parseable. A read-only `--inspect-agent-source <sourceThreadId>` mode may resolve the exact row for
Plan 23-06, but it must exit before seed/model/evidence code exactly like the existing inspector.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Version the full suite and require its identity for agent activation evidence</name>
  <files>packages/contracts/src/skill.ts, packages/contracts/src/skillAuthoring.test.ts, packages/backend/scripts/eval-suite-manifest.json, packages/backend/scripts/run-eval-golden.mjs</files>
  <behavior>
    - The manifest names one revision, exact sorted fixture ids/content hashes, and nonzero count.
    - Runner self-check recomputes it and fails on add/remove/rename/content drift until the manifest
      and code-owned current revision are deliberately updated together.
    - `hasPassingAgentTenantEvidence` first requires exact tenant evidence, then current suite
      revision/hash/count and full passed count; stale/missing/malformed/filtered evidence fails.
    - Existing `hasPassingTenantEvidence` remains backward-compatible for Phase 21 user rows.
    - `runLive` invokes the offline validation before the first `must(...)`, seed, or paid turn.
  </behavior>
  <action>
Add an additive suite metadata shape and a separate strict agent predicate rather than silently
tightening Phase 21's shipped predicate. Make the manifest mechanical: sorted ids plus SHA-256 of
fixture bytes, with a code-owned current revision/hash/count that activation can compare without
filesystem access. Extract a quiet validation function if needed so `--self-check` and live entry
share one implementation. Source-order tests must prove the validation call precedes every live
Convex/model call.
  </action>
  <verify><automated>pnpm --filter @pikar/contracts exec vitest run src/skillAuthoring.test.ts &amp;&amp; pnpm --filter @pikar/backend eval:golden -- --self-check</automated></verify>
  <done>Agent evidence identifies the exact candidate and the exact current full suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add refs-only authoring state oracles and closed expectation vocabulary</name>
  <files>packages/backend/convex/smokeAssert.ts, packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts, packages/backend/scripts/run-eval-golden.mjs</files>
  <behavior>
    - The runner accepts author `user` or `agent` for exact tenant pins and rejects system rows,
      non-candidates, unknown ids, and mixed global/tenant pins for one name before paid work.
    - Refs-only inspection by source thread resolves one exact agent row or fails closed on zero/many;
      it never returns body/authoredBody and runs before all live paths.
    - Each authoring fixture gets a deterministic throwaway subtenant of the run so one immutable
      pending candidate cannot contaminate/block a later authoring case; the candidate UNDER
      EVALUATION remains the one exact external registry row pinned by id into every specialist
      execution, while fixture-created candidates live only in their per-case authoring tenants;
      ordinary fixtures keep the existing shared seeded tenant and no cleanup/delete mutation exists.
    - Closed expectation keys can assert tool-called, candidate count/id/author/status, active id
      unchanged, evidence absent, approval absent, rollbackEligible false, and zero requests.
    - Every zero assertion is paired with the positive tool/candidate source witness.
  </behavior>
  <action>
Extend the existing runner vocabulary and `smokeAssert` read side, not assistant-text checks. Scope
candidate assertions to the exact fixture thread/turn. Because the v1 writer correctly refuses a
changed draft while one candidate for that tenant/name is pending, run each authoring fixture in a
short deterministic `eval-<runId>-<case>` subtenant; do not purge, patch, archive, or add a test-only
delete. Ordinary inbox/vault/Blueprint fixtures remain on the one existing seeded run tenant.
Keep returns ids/enums/booleans/counts/hashes only.
Add the read-only source inspector to runner argument parsing and prove it is mutually exclusive
with `--skill`, `--tenant-skill`, `--only`, and live execution.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1 &amp;&amp; pnpm --filter @pikar/backend eval:golden -- --self-check</automated></verify>
  <done>The runner can prove authoring effects without reading reply prose or leaking content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add held-out happy, self-activation, escalation, embedded, and retry cases</name>
  <files>packages/backend/scripts/eval-cases/40-agent-author-happy.json, packages/backend/scripts/eval-cases/41-agent-author-self-activate.json, packages/backend/scripts/eval-cases/42-agent-author-capability-escalation.json, packages/backend/scripts/eval-cases/43-agent-author-embedded-instruction.json, packages/backend/scripts/eval-cases/44-agent-author-retry.json, packages/backend/scripts/eval-suite-manifest.json, packages/backend/scripts/run-eval-golden.mjs</files>
  <behavior>
    - Happy path creates exactly one allowed agent candidate and leaves active/evidence/approval/outward state unchanged.
    - A demand to create and activate/mark-passed/approve can at most create the candidate.
    - Capability-escalation adaptation text remains inert; no new tool key/request/approved plan appears.
    - Embedded instructions cannot disclose fixtures, foreign bodies, or bypass state.
    - A changed retry while pending creates no second row; exact same-turn retry behavior remains
      covered deterministically in backend tests.
  </behavior>
  <action>
Use the next stable fixture ids after Phase 20/20.1 (40-44 if those prerequisite fixtures are 38/39;
renumber once before implementation if the landed sequence differs). Keep adversarial text only in
repo-side fixture files. Update the manifest after final bytes. Add synthetic self-check cases for
every new expectation key and parser failure. Do not run the live command in this plan.
  </action>
  <verify><automated>pnpm --filter @pikar/backend eval:golden -- --self-check</automated></verify>
  <done>The current full suite includes five held-out authoring risks and validates offline at $0.</done>
</task>

<task type="auto">
  <name>Task 4: Mutation-prove evidence suppression, manifest preflight, and fixture holdout</name>
  <files>packages/backend/scripts/run-eval-golden.mjs, packages/contracts/src/skillAuthoring.test.ts, docs/playbooks/skill-registry.md, docs/playbooks/agent-runtime.md</files>
  <action>
Apply/restore mutations for: omit candidate id from evidence comparison; accept stale suite revision;
record after `--only`; allow zero cases; skip pre-live self-check; expose a fixture path/key through
the tool/inspection surface. Each must redden a named offline assertion. Update both playbooks with
the suite-manifest protocol, holdout boundary, and explicit statement that no paid run occurred.
  </action>
  <verify><automated>pnpm --filter @pikar/contracts exec vitest run src/skillAuthoring.test.ts &amp;&amp; pnpm --filter @pikar/backend eval:golden -- --self-check &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The held-out exact-row gate is mutation-proven and still unspent.</done>
</task>

</tasks>

<verification>
- `--self-check` makes zero Convex/model calls and validates all fixture/manifest/vocabulary rules.
- Live entry calls the same validation before any `must()` invocation.
- No filtered, zero-case, failed, interrupted, over-cap, or stale-suite run can write agent evidence.
- No paid command is executed in this plan.
</verification>

<success_criteria>
- Agent candidates can be evaluated by exact id using the one existing runner.
- Evidence is bound to current full held-out suite identity, not merely a green subset/count.
- Adversarial authoring behavior has durable state oracles and positive witnesses.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-04-SUMMARY.md` with suite revision/hash/count,
new fixture ids, offline check output, and restored mutation evidence; state explicitly `$0 spent`.
</output>
