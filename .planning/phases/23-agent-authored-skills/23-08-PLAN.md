---
phase: 23-agent-authored-skills
plan: 08
type: execute
wave: 8
depends_on: ["23-07"]
files_modified:
  - .planning/phases/23-agent-authored-skills/23-LIVE-RESULT.json
autonomous: false
requirements: [SKILL-02]

must_haves:
  truths:
    - "Owner activation happens only through A's actual UI click on the exact row proven by immutable handoff/eval-result artifacts; no CLI/internal/agent substitute is allowed."
    - "The activation transaction records ownerApproval with A's real user id and the exact eval run id while preserving immutable candidate bytes/source/evidence."
    - "One authorized author-tenant runtime turn attributes the exact active tenant skill id/name/version/hash; one authorized B turn remains on B's pre-state."
    - "Owner rollback restores the exact handoff baseline in one transition and preserves the agent row's body/hash/evidence/approval/source lineage."
    - "Global and B state never change; refs-only live result validates exactly and remains immutable input to the separate closure plan."
  artifacts:
    - path: ".planning/phases/23-agent-authored-skills/23-LIVE-RESULT.json"
      provides: "Closed refs-only owner activation/runtime/isolation/rollback result bound to both prior artifacts"
      contains: "phase23-live-result.v1"
  key_links:
    - from: "23-EVAL-RESULT eval.runId"
      to: "ownerApproval.evalRunId"
      via: "same-row owner UI activation transaction"
      pattern: "ownerApproval"
    - from: "23-LIVE-HANDOFF rollbackBaseline.id"
      to: "rollbackTenantSkill"
      via: "literal owner target plus post-readback hash comparison"
      pattern: "rollbackBaseline"
---

<objective>
Consume the immutable handoff and evidence result, require the owner's actual exact-row UI act,
observe tenant runtime/isolation within the already-authorized bound, roll back to the exact baseline,
and freeze a validated refs-only final artifact. Documentation and requirement closure are 23-09.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/23-agent-authored-skills/23-07-SUMMARY.md
@.planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json
@.planning/phases/23-agent-authored-skills/23-EVAL-RESULT.json
@.planning/phases/23-agent-authored-skills/validate-live-artifact.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate both immutable artifacts and remaining paid allowance at zero spend</name>
  <files>23-LIVE-HANDOFF.json, 23-EVAL-RESULT.json, and deployment state read-only</files>
  <action>
Run the shared exact validator on both files, hash bytes, and require eval-result.handoffSha256.
Re-read the exact candidate twice: candidate + passing current-suite evidence + absent approval,
effective/baseline/global/B state and audit counts must match the two artifacts. Revalidate A owner
and B non-owner JWT subjects from fixed gitignored storage states without printing them. Compute
remaining authorized allowance from measured eval cost; stop if it cannot cover the two itemized
turns without exceeding the original combined cap. Run focused free gates.
  </action>
  <verify><automated>node .planning/phases/23-agent-authored-skills/validate-live-artifact.mjs handoff .planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json --forbid-env PHASE23_PRIVATE_NEEDLE &amp;&amp; node .planning/phases/23-agent-authored-skills/validate-live-artifact.mjs eval .planning/phases/23-agent-authored-skills/23-EVAL-RESULT.json --forbid-env PHASE23_PRIVATE_NEEDLE &amp;&amp; pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Exact evidence-only state and enough already-authorized runtime allowance remain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner reviews and clicks Activate on the exact candidate</name>
  <files>configured deployment state; no repository source edit</files>
  <action>
Open `/ops` using A's owner storage state. Present the exact artifact candidate id, agent/source
provenance, base/candidate diff, suite/run/counts/cost refs, and absent approval. The owner must
personally click Activate on that row. Do not use CLI, an internal mutation, browser scripting that
clicks without owner observation, or auto-approval. After the click, read-only machine verification
requires active status, rollbackEligible true, byte-identical immutable hash/source/evidence,
ownerApproval A user id/time and Task-7 eval run id, prior active archived/eligible, and unchanged
global/B state. If declined/wrong, stop.
  </action>
  <verify>Exact read-only candidate/foreign inspector after the owner's click must match both prior artifacts and the expected atomic transition.</verify>
  <done>The owner personally activated the exact evaluated row and machine readback proves it.</done>
  <resume-signal>Owner confirms the exact-row UI activation after personally clicking Activate.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Spend only the authorized two turns on runtime attribution and isolation</name>
  <files>configured deployment state; no repository source edit</files>
  <action>
Run one ordinary governed specialist turn in A that reaches the candidate's named specialist; read
bounded attribution/activity and require exact tenant skill id/name/version/body hash plus positive
completion. Run one corresponding B turn using B's saved authenticated state; require its effective
id/hash remain exactly foreign pre-state and never contain A's candidate id/hash/private needle.
Record each turn cost and abort before either if the original combined cap would be exceeded. No
retry is authorized unless still inside both the itemized turn count and cap; default is no retry.
  </action>
  <verify><automated>pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill $((Get-Content -Raw '.planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json' | ConvertFrom-Json).candidate.id) --foreign-tenant $((Get-Content -Raw '.planning/phases/23-agent-authored-skills/23-LIVE-HANDOFF.json' | ConvertFrom-Json).foreignBefore.tenantId) --expect-status active --expect-evidence passing --expect-gate-passed true --expect-rollback-eligible true --json</automated></verify>
  <done>A ran the exact active row, B did not, within the already-authorized two-turn cap.</done>
</task>

<task type="auto">
  <name>Task 4: Roll back to the literal baseline and create the closed final artifact</name>
  <files>.planning/phases/23-agent-authored-skills/23-LIVE-RESULT.json</files>
  <action>
As A owner, call public `rollbackTenantSkill` with literal handoff.rollbackBaseline.id. Require one
atomic transition: baseline active at the exact handoff hash; agent row archived/rolled_back and
still rollbackEligible with byte-identical bodyHash/evidence/ownerApproval/source/lineage; global/B
unchanged; exactly one refs-only rollback audit event.

Write `phase23-live-result.v1` bound to handoff and eval-result SHA-256 with exact candidate/baseline,
owner activation/approval, A/B runtime attribution and costs, isolation, rollback, audit, and total
authorized/measured spend refs. Validate every nested exact key set, enum/type/ref/hash/count and all
string values/private needle through the shared validator.
  </action>
  <verify><automated>node .planning/phases/23-agent-authored-skills/validate-live-artifact.mjs live .planning/phases/23-agent-authored-skills/23-LIVE-RESULT.json --forbid-env PHASE23_PRIVATE_NEEDLE</automated></verify>
  <done>Rollback restored the exact prior bytes and one closed refs-only artifact proves the lifecycle.</done>
</task>

</tasks>

<verification>
- Human activation is a non-auto action and exact-row UI click.
- Approval/eval ids match and immutable candidate bytes survive activation/rollback.
- Exactly two bounded runtime turns use distinct authenticated identities.
- Final artifact is recursively closed and credential/content-free.
- Requirement/roadmap remain open for 23-09 to close from all three immutable artifacts.
</verification>

<success_criteria>
- An active agent row without current full-suite evidence and separate owner approval is impossible through exported code.
- A executes the exact active row; B remains isolated.
- Owner rollback restores the exact eligible baseline in one write transaction.
- The final lifecycle artifact is sufficient, immutable input for truthful SKILL-02 closure.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-08-SUMMARY.md` with all artifact hashes,
verbatim owner activation result, runtime costs/attribution/isolation, rollback, and validation.
Keep SKILL-02 and the roadmap open for 23-09.
</output>
