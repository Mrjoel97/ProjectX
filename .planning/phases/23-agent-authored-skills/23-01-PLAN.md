---
phase: 23-agent-authored-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/skill.ts
  - packages/contracts/src/skillAuthoring.test.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/skills.test.ts
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "Execution stops before edits unless Phase 21 has a validated 21-LIVE-RESULT and SKILL-01 closure, Phase 22 verification is passed and GOVN-01 is closed, and both landed seams still match source."
    - "The Phase 21 tenantSkills overlay remains the sole tenant skill plane; no agent row enters the deployment-global skills table."
    - "Agent-authorable names are a closed, eval-reachable subset of the landed USER_AUTHORABLE_SKILLS set."
    - "Persisted provenance can distinguish system, user, and Executive-Agent rows without weakening existing user-row validation."
    - "Agent-only source lineage and owner-approval state are server-owned optional fields; legacy and Phase 21 rows remain readable."
  artifacts:
    - path: "packages/contracts/src/skill.ts"
      provides: "Closed agent-authorable set and provenance/evidence contracts"
      contains: "AGENT_AUTHORABLE_SKILLS"
    - path: "packages/backend/convex/schema.ts"
      provides: "Additive tenantSkills provenance and approval shape"
      contains: "v.literal(\"agent\")"
    - path: "packages/contracts/src/skillAuthoring.test.ts"
      provides: "Closed-set/subset and composition regression proof"
  key_links:
    - from: "AGENT_AUTHORABLE_SKILLS"
      to: "USER_AUTHORABLE_SKILLS"
      via: "executable subset assertion over only eval-reachable specialist names"
      pattern: "AGENT_AUTHORABLE_SKILLS"
    - from: "tenantSkills.author"
      to: "agent provenance fields"
      via: "discriminated behavioral tests; caller values are never trusted"
      pattern: "authorAgentId"
---

<objective>
Reconcile Phase 23 with the landed Phase 21 overlay and add only the additive contracts/schema
needed to represent an immutable agent-authored candidate and its later owner approval.

Purpose: freeze the trustworthy data vocabulary before any model-reachable writer exists. This plan
does not publish a row, expose a tool, change activation, run an eval, or add UI.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-RESEARCH.md
@.planning/phases/21-user-authored-skills-and-routines/21-04-SUMMARY.md
@packages/contracts/src/skill.ts
@packages/backend/convex/schema.ts
@packages/backend/convex/skills.ts
@docs/playbooks/skill-registry.md

<interfaces>
The landed Phase 21 names are authoritative: `tenantSkills`, `USER_AUTHORABLE_SKILLS`,
`composeUserSkillBody`, `hasPassingTenantEvidence`, `loadEffectiveSkill`, and the existing
candidate/evidence/activation/rollback helpers. Extend them; do not fork them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the closed agent-authoring contract on the existing composition seam</name>
  <files>packages/contracts/src/skill.ts, packages/contracts/src/skillAuthoring.test.ts</files>
  <behavior>
    - `AGENT_AUTHORABLE_SKILLS` is closed, duplicate-free, and a subset of both
      `USER_AUTHORABLE_SKILLS` and `GATED_SKILLS`; every member has a live runner route.
    - `isAgentAuthorableSkill` rejects arbitrary registry strings and every known non-runnable or
      ungated skill.
    - Agent adaptations reuse the existing UTF-8 byte cap and `composeUserSkillBody`; no second
      composer, replacement-prompt format, or capability list is introduced.
    - Additive provenance types describe `authorAgentId`, trusted source thread/turn refs, and an
      owner approval bound to one eval run without containing prose, bodies, fixtures, or output.
  </behavior>
  <action>
Before editing, require Phase 21's completed `21-07-SUMMARY.md` and schema-valid
`21-LIVE-RESULT.json`, plus SKILL-01/roadmap closure. Also require Phase 22's verification status to
be `passed` (not `human_needed`), GOVN-01 checked complete in REQUIREMENTS, and its roadmap row
closed; this means the outstanding owner/non-owner `/ops` DOM proof has happened independently and
Phase 23 does not inherit/quietly close another phase's gate. Re-run a read-only seam inventory and
compare both landed interfaces to this plan's assumptions. If either dependency is partial or a
seam changed, stop for re-planning; these plans are not permission to execute against it.

Add the smallest closed-set/type additions beside the landed user-authoring contract. Prefer the
same three dispatched specialists unless current code proves a smaller reachable set is necessary.
Pin the subset relationship mechanically in the existing authoring test rather than duplicating
names in backend code. Reuse the Phase 21 cap/composer exactly; the agent authors only the bounded
adaptation section and never a full replacement body.
  </action>
  <verify><automated>pnpm --filter @pikar/contracts exec vitest run src/skillAuthoring.test.ts</automated></verify>
  <done>The pure contract has one closed, eval-reachable agent set and no second composition system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tenantSkills additively for agent lineage and atomic owner approval</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - Existing system/user rows validate unchanged; no migration/backfill is required.
    - `author` accepts `agent`; agent source fields and `ownerApproval` are optional at schema level
      so old rows remain valid, while behavioral fixtures pin the allowed cross-field combinations.
    - Owner approval contains only owner user id, approval timestamp, and the exact eval run id.
    - A source-thread/turn index supports exact idempotence and refs-only live inspection without a
      table scan; it does not create a public cross-tenant read.
    - Body, authoredBody, version, author, source lineage, base lineage, and approval remain
      immutable except for the single later activation transaction explicitly planned in 23-05.
  </behavior>
  <action>
Extend the existing table rather than creating an agent table. Add `agent` to the author union and
the minimum optional provenance/approval fields. Use `v.id("users")` for owner identity and plain
code-owned literals/opaque refs for the Executive Agent and source lineage. Add only the index
needed to recover/retry by trusted source turn/thread. Do not add status values unless an observed
Phase 21 collision requires one; the Phase 23 v1 pending policy will refuse a changed second draft.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>The one tenant overlay can represent agent provenance and later owner proof without weakening old rows.</done>
</task>

<task type="auto">
  <name>Task 3: Record the reconciled invariants and run the Wave-0 mutation checks</name>
  <files>docs/playbooks/skill-registry.md, packages/contracts/src/skillAuthoring.test.ts, packages/backend/convex/skills.test.ts</files>
  <action>
Update the skill-registry playbook with the exact landed Phase 21 seam and the Phase 23 data-plane
rules: tenant overlay only, closed agent set, server-owned provenance, no tool/activation yet, and
approval as structured refs. Execute and restore mutations that widen the agent set to an ungated
skill, remove the subset assertion, or allow an agent row without source lineage. Record the named
reds in the summary; do not leave a mutation in the tree.
  </action>
  <verify><automated>pnpm --filter @pikar/contracts exec vitest run src/skillAuthoring.test.ts &amp;&amp; pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1 &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The contract/schema boundary and its mutation witnesses are documented and green.</done>
</task>

</tasks>

<verification>
- Phase 21 live closure and Phase 22 passed/GOVN-01 closure are revalidated before the first edit.
- No new registry table or prompt composer exists.
- Agent-authorable is mechanically narrower than user-authorable and eval-gated.
- Existing Phase 21 user/system fixtures remain green.
- Schema additions are optional-on-read and content-free outside the tenantSkills content plane.
</verification>

<success_criteria>
- Phase 23 is grounded in final Phase 21 names rather than research placeholders.
- The data model can later prove author=agent, exact source lineage, exact eval, and exact owner act.
- No model-reachable capability or activation path exists yet.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-01-SUMMARY.md` with exact fields/indexes,
closed names, test counts, and restored mutation evidence.
</output>
