---
phase: 23-agent-authored-skills
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "One narrow internal mutation can mint an Executive-Agent row only as status candidate."
    - "The model-supplied surface is exactly name plus bounded adaptation; tenant, author, ids, base, version, status, evidence, approval, and rollback eligibility are trusted server facts."
    - "An identical source-turn retry returns the same row; a changed draft while any candidate for that tenant/name is pending is refused without patching or superseding it."
    - "The active effective row is unchanged and the one insert-only audit event contains refs/counts only."
  artifacts:
    - path: "packages/backend/convex/skills.ts"
      provides: "Candidate-only internal writer and refs-only source inspection"
      contains: "publishAgentCandidate"
    - path: "packages/backend/convex/skills.test.ts"
      provides: "Spoof, isolation, idempotence, immutability, pending-policy, and audit tests"
  key_links:
    - from: "publishAgentCandidate"
      to: "composeUserSkillBody"
      via: "the same global-core composition and effective-row lineage used by Phase 21"
      pattern: "composeUserSkillBody"
    - from: "trusted source turn"
      to: "tenantSkills row"
      via: "indexed exact retry before pending-candidate refusal"
      pattern: "sourceTurnId"
---

<objective>
Implement the inert, candidate-only server writer that the Executive Agent tool will call later.

Purpose: make candidate creation correct, immutable, tenant-scoped, idempotent, and auditable while
it is still unreachable from any model. No llm/tool, eval, activation, owner UI, or paid work occurs.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-agent-authored-skills/23-RESEARCH.md
@.planning/phases/23-agent-authored-skills/23-VALIDATION.md
@.planning/phases/23-agent-authored-skills/23-01-SUMMARY.md
@packages/backend/convex/skills.ts
@packages/backend/convex/skills.test.ts
@docs/playbooks/skill-registry.md

<interfaces>
The internal mutation receives trusted server args
`{tenantId, sourceThreadId, sourceTurnId, name, authoredBody}`. Those are not the future model tool
schema: the tool supplies only `{name, authoredBody}` and injects the other three from its action
context. `authorAgentId` is hardcoded to the Executive Agent identity in server code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the minimum shared immutable-publish primitive without changing user behavior</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - Every existing `publishUserCandidate` result, baseline, version, lineage, idempotence, audit,
      error, and user provenance test remains byte/shape compatible.
    - Shared code derives the global composition core and effective lineage separately, preserving
      Phase 21's non-recursive composition fix.
    - The primitive accepts a trusted provenance discriminant, not arbitrary author/status fields.
  </behavior>
  <action>
Refactor only enough of `publishUserCandidate` to reuse its baseline/version/composition insertion
rules. Keep public args and returns unchanged. Do not introduce an abstraction outside `skills.ts`,
and do not touch activation. Run the focused suite before and after the refactor so the refactor is
not bundled with the agent behavior in one unobservable jump.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>The Phase 21 writer is unchanged in behavior and one private insertion seam is reusable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the candidate-only Executive-Agent writer and exact retry policy</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - The internal mutation rejects non-agent-authorable names before reads/writes and blank/over-cap
      adaptations before an insert.
    - Inserted facts are `author: agent`, hardcoded Executive Agent id, trusted source refs,
      `status: candidate`, `rollbackEligible: false`, no evidence, no approval, exact base lineage.
    - Its validator has no author/status/version/evidence/approval/base/body/rollback argument.
    - Exact same-turn retries return `{inserted:false}` and the same id/version; differing args under
      one source turn fail closed; a different draft with a pending row is refused and changes zero rows.
    - Tenant A/B collisions never cross-read, dedupe, or share a baseline.
  </behavior>
  <action>
Add `publishAgentCandidate` as an `internalMutation`, not a public tenant mutation. Resolve exact
same-turn retries first through the new source index, then apply the v1 pending rule: refuse a
changed draft while any candidate exists for the same tenant/name. Do not archive/supersede a
never-active candidate. Compose against the global active core, record lineage against the current
effective row, and reuse the Phase 21 baseline rule. Add a refs-only internal source inspection
query for later UAT/handoff; it returns ids/status/hash/provenance only, never body/authoredBody.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Trusted server code can create exactly one inert agent candidate, and nothing can promote it.</done>
</task>

<task type="auto">
  <name>Task 3: Lock candidate-only structure, log privacy, and active-row immutability</name>
  <files>packages/backend/convex/skills.test.ts, docs/playbooks/skill-registry.md</files>
  <action>
Add source-region/key-set tests proving the writer contains no activation/evidence/owner helper call,
has no caller-supplied authority fields, and patches no row. Plant a high-entropy adaptation, invoke
the real mutation, positively witness the candidate, then scan audit/DLQ/telemetry/errors for the
needle while asserting the exact refs-only audit key set. Mutation-check: inserted active status,
caller-supplied tenant/author, missing tenant predicate, and archived pending candidate must each
turn the named test red; restore and rerun. Update the playbook with the v1 pending/refusal policy.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts convex/llmRedaction.test.ts --maxWorkers=1 &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The writer is structurally candidate-only, tenant-isolated, content-private, and mutation-proven.</done>
</task>

</tasks>

<verification>
- A real candidate row positively witnesses every absence assertion.
- Active effective id/hash remains unchanged after publish and retry/refusal paths.
- No code path in this plan calls `transitionSkillActivation` or records eval/approval state.
</verification>

<success_criteria>
- The inert writer exists and is safe before a model can reach it.
- A retry cannot mint duplicate immutable versions and a changed pending draft cannot gain rollback status.
- The only log write is refs-only and correlated to the trusted source turn.
</success_criteria>

<output>
Create `.planning/phases/23-agent-authored-skills/23-02-SUMMARY.md` with the exact internal args,
pending policy, test counts, audit key set, and restored mutations.
</output>
