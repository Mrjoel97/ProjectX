---
phase: 22.1-beta-admission-readiness-legal-deployment-ci-typechecking-and-identity-boundary-hardening
plan: 03
type: execute
wave: 3
depends_on: ["22.1-01", "22.1-02"]
autonomous: false
requirements: [GOVN-03]
files_modified:
  - .planning/phases/22.1-beta-admission-readiness-legal-deployment-ci-typechecking-and-identity-boundary-hardening/22.1-03-SUMMARY.md
resume: "REFRESHED 2026-08-10. Implementation is substantially landed in 49756f4, 72dd652, 6136d5f, de2fdc2 and cf1c5fd. Do not replay the formatter/type-error sweep. Resume with the final local gate, owner-approved anti-vacuous GitHub proof, and summary."

must_haves:
  truths:
    - "The landed CI workflow runs typecheck, lint, unit tests and build on main pushes and pull requests."
    - "A quiescent current tree passes pnpm typecheck, pnpm lint, pnpm test and pnpm build without any remembered error baseline."
    - "The broad formatter and test-strictness sweeps are not rerun; current source is treated as landed work and only failures from the final gate are investigated."
    - "With explicit owner approval, three isolated scratch-branch heads make CI fail independently at typecheck, lint/format and test, and their run URLs are retained."
    - "Closing this plan does not complete GOVN-03: tenant data deletion and export remain unimplemented requirement scope."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Hermetic install -> typecheck -> lint -> test -> build GitHub gate"
      contains: "pnpm typecheck"
    - path: "docs/playbooks/ci-gate.md"
      provides: "Operational contract for the zero-baseline gate and committed Convex generated types"
      contains: "baseline"
    - path: ".planning/phases/22.1-beta-admission-readiness-legal-deployment-ci-typechecking-and-identity-boundary-hardening/22.1-03-SUMMARY.md"
      provides: "Landed-commit inventory, local gate output and three red CI run URLs"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "the same four root scripts used by the final local gate"
      pattern: "pnpm (typecheck|lint|test|build)"
---

<objective>
Close 22.1-03 from the state the repository actually reached.

The CI workflow, generated-type strategy, dead-directive cleanup, known-red test fix, strictness
repairs and the large formatting/lint reconciliation are already in history. Replaying the old plan
would rewrite hundreds of concurrent files and erase the evidentiary value of those commits. This
resume plan therefore performs only a quiescent final local gate, the still-missing owner-approved
anti-vacuous GitHub proof, and honest closure documentation.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PARALLELIZATION.md
@.github/workflows/ci.yml
@package.json
@docs/playbooks/ci-gate.md
@.planning/phases/22.1-beta-admission-readiness-legal-deployment-ci-typechecking-and-identity-boundary-hardening/22.1-01-SUMMARY.md
@.planning/phases/22.1-beta-admission-readiness-legal-deployment-ci-typechecking-and-identity-boundary-hardening/22.1-02-SUMMARY.md

<completed_work>
- `49756f4` — first CI workflow, root scripts and CI playbook.
- `72dd652` — removed 100 dead `@ts-expect-error import.meta.glob` directives.
- `6136d5f` — fixed the known-red onboarding rate-limiter registration race.
- `de2fdc2` — reduced the remaining backend test strictness failures before the final reconciliation.
- `cf1c5fd` — made CI hermetic with committed Convex generated types and landed the formatter/lint/
  strictness reconciliation. This is the broad sweep the old Tasks 3-6 described; do not run it again.
</completed_work>

<current_contract>
`.github/workflows/ci.yml` now installs the frozen workspace and runs, in order,
`pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build`. It intentionally does not contact a
Convex deployment during CI: generated API/DataModel files are committed. Any plan instruction to
run deployment-backed codegen in CI is superseded by `cf1c5fd` and the current playbook.
</current_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: quiescent final local gate</name>
  <files>None unless a genuine regression is isolated</files>
  <action>
1. Require a quiescent shared tree. `git status --short` may contain known owner/concurrent paths, but
   none may overlap source/config files imported by the four gates. If another lane is editing the
   code under test, stop and rerun later; do not stash, reset or format around it.
2. Inspect the five completed commits above and confirm `.github/workflows/ci.yml`, the four root
   scripts and committed `packages/backend/convex/_generated/` files are present.
3. Run sequentially from the repository root:
   - `pnpm typecheck`
   - `pnpm lint`
   - `pnpm test`
   - `pnpm build`
   - `node scripts/check-playbooks.mjs`
4. Record command, exit code, package/test counts and timestamp. A nonzero result is a current
   regression to isolate; it is not permission to restore the old numeric baseline or rerun
   `pnpm format` across the repository.
5. If a fix is genuinely required, make the smallest targeted change in a new closure commit and
   rerun all five commands. Do not fold unrelated dirty-tree files into it.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>All four CI-equivalent root commands and the playbook check exit 0 on one quiescent tree.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: authorize the three-red-run GitHub proof and scratch-branch deletion</name>
  <files>None — remote scratch branch and workflow runs only</files>
  <action>Present the local-green evidence and exact temporary-branch procedure, then wait for explicit push/delete approval or defer.</action>
  <decision>May the executor push and later delete one temporary GitHub branch used only to prove each CI stage fails?</decision>
  <context>
This is the only missing original Task 7 evidence. It mutates the remote repository and deliberately
creates red workflow runs, so local implementation authority is insufficient. It must never use main
and must not dirty the shared working tree.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed on scratch branch</name>
      <pros>Proves the gate is anti-vacuous at three independent stages and supplies durable run URLs.</pros>
      <cons>Creates intentional red CI history and requires remote branch push/delete authority.</cons>
    </option>
    <option id="defer">
      <name>Defer remote proof</name>
      <pros>No remote mutation or intentional CI failure.</pros>
      <cons>22.1-03 stays open with local green evidence only.</cons>
    </option>
  </options>
  <verify>
    <automated>git status --short</automated>
  </verify>
  <done>The owner explicitly authorized the scratch-branch push/delete proof, or the plan remains open without remote mutation.</done>
  <resume-signal>Reply `proceed scratch CI proof` or `defer`.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: produce three isolated red CI runs, then restore green and delete the branch</name>
  <files>None retained — all tripwires live only on the temporary worktree/remote scratch branch</files>
  <action>After approval, create the isolated worktree/branch, push the three independent tripwire heads and final clean head, capture workflow URLs, then delete the scratch branch/worktree as described below.</action>
  <what-built>A locally green CI-equivalent gate whose remote failure behaviour has not yet been observed.</what-built>
  <how-to-verify>
After Task 2 approval:

1. Create a temporary git worktree and a uniquely named branch from the clean current base. Do not
   reuse the shared worktree and do not include credentials, deployment values or owner data.
2. Push three successive branch heads, with each head clearing the preceding defect before adding the
   next, so every red run has one expected cause:
   - Head A: one deliberate TypeScript type error; confirm CI stops red at **Typecheck** and save URL.
   - Head B: remove the type error and add one harmless formatting violation in a tracked scratch
     source; confirm Typecheck passes and **Lint** alone turns red; save URL.
   - Head C: remove the format violation and add one deliberately failing isolated unit assertion;
     confirm Typecheck/Lint pass and **Test** turns red; save URL.
3. Push a final clean head removing the test tripwire. Confirm the same workflow returns green through
   Build. Save this recovery URL as supporting evidence.
4. Delete the remote scratch branch and remove the temporary worktree only after the URLs and commit
   ids are recorded. Do not rewrite or merge the scratch commits into main.
5. If branch protection, authentication or workflow permissions block any step, stop and report the
   exact blocker; never weaken the workflow to make the proof easier.
  </how-to-verify>
  <verify>
    <automated>git status --short</automated>
  </verify>
  <done>Three URLs show the intended independent red stages, a fourth shows clean green recovery, and the scratch branch/worktree no longer exists.</done>
  <resume-signal>Reply `approved` with the three red run URLs and final green URL, or describe the failed stage.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: write the closure summary without over-closing GOVN-03</name>
  <files>.planning/phases/22.1-beta-admission-readiness-legal-deployment-ci-typechecking-and-identity-boundary-hardening/22.1-03-SUMMARY.md</files>
  <action>
Create the summary with:
- the five landed implementation commits and what each contributed;
- the exact final local command results and timestamp;
- three red CI run URLs, expected failing stage and tripwire commit id, plus the final green URL;
- confirmation the scratch branch/worktree were removed and no tripwire reached main;
- the current hermetic generated-types decision from `cf1c5fd`, superseding the old deploy-key/codegen
  wording;
- residual requirement status: **GOVN-03 remains Pending** because tenant data deletion and export are
  not implemented. Completing the CI plan is not evidence for those controls.

Run `git diff --check` and `node scripts/check-playbooks.mjs`. Commit only the summary (and any
separately justified targeted regression fix from Task 1) path-scoped.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>22.1-03 has local-green and anti-vacuous remote evidence, while GOVN-03 remains honestly open.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` and `node scripts/check-playbooks.mjs` exit 0 on one quiescent tree.
- Three distinct GitHub run URLs show Typecheck-, Lint- and Test-stage failures for isolated tripwires.
- A fourth URL shows the cleaned scratch head green through Build.
- The remote scratch branch is deleted; no tripwire commit is merged into main.
- `pnpm format` and the old broad formatter sweep are never rerun by this resume plan.
</verification>

<success_criteria>
- Repository verification is locally green and remotely proven non-vacuous.
- The closure summary is reproducible from commit ids, command outputs and workflow URLs.
- The plan closes SC3’s CI work without falsely completing GOVN-03’s tenant delete/export promise.
</success_criteria>

<output>
Create `22.1-03-SUMMARY.md` only after the owner-approved remote proof is complete. Do not mark
GOVN-03 complete; tenant data deletion/export requires separate implementation and verification.
</output>
