---
phase: 22.1
lane: 22-evidence
produced: 2026-08-16
worktree: lane/22-evidence (forked from closure/phases-14-25 @ c1877ce)
scope: verification only — no product code written
requirements: [GOVN-01, GOVN-03]
verdict: audit GOVN-03 gap list is STALE; a NEW and larger compliance exposure was found in the privacy policy's disclosure sections
---

# Phase 22 / 22.1 — Evidence Reconciliation

Independent verification of the GOVN-03 claims in `.planning/v2.0-phases-14-25-MILESTONE-AUDIT.md`
against current source at `c1877ce`. No product code was written. Every claim below carries
file:line evidence.

> The milestone audit file itself is **untracked** on `closure/phases-14-25`, so it does not exist in
> this worktree. Its GOVN-03 claims were reconciled as quoted in the lane brief, against source.

---

## 0. Headline

Two independent findings, and they point in opposite directions.

1. **The audit's GOVN-03 gap list is stale.** Tenant export, bounded tenant deletion, and an honest
   Microsoft revocation posture all exist, are wired to the UI, are tenant-scoped, and are pinned by
   tests. The hypothesis in the lane brief is **correct**.

2. **But the audit under-called the risk in a different place.** The audit and the 22.1 VERIFICATION
   both scoped "does the policy's *controls* exist" — disconnect, export, erase — and all of those
   pass. Neither checked the policy's ***disclosure*** sections. Sections 4, 7 and 8 of the live
   public privacy policy contain **six materially false or missing statements** about who receives
   user data and where. That is a live compliance exposure on a public production site and is
   **larger than every gap the audit listed.** See §4.

---

## 1. Claim-by-claim reconciliation of the audit's GOVN-03 gap list

| # | Audit claim | Verdict | Evidence |
|---|---|---|---|
| 1 | "tenant export ... absent" | **VERIFIED-ABSENT (claim is FALSE)** | `packages/backend/convex/tenantExport.ts:36` `exportTenantData = tenantQuery({...})`; paging caps at `:14`, `:23`, `:24` |
| 2 | "tenant deletion ... absent" | **VERIFIED-ABSENT (claim is FALSE)** | `packages/backend/convex/tenantDelete.ts:187` `deleteTenantData = tenantAction({ args: { confirmation: v.literal("DELETE MY DATA") } })` `:188`; bounded pager `:84`; authorize gate `:42` |
| 3 | "Microsoft disconnect is local-only" | **VERIFIED-EXISTS — but it is a documented deliberate design, not a gap** | `packages/backend/convex/microsoftAuth.ts:283-301`. Returns a **hard** `revokedAtProvider: false` (`:299`), audited as a flag (`:297`), with a 14-line precision note at `:265-278` naming both Graph mechanisms and why each is refused (over-privilege vs. signing the user out of every MS app) |
| 4 | Phase 22 "server passed; presentation open" | **PARTIAL (stale)** | Server: `packages/backend/convex/lib/functions.ts:53,77,80`. Presentation is **not** open — the mount guard exists at `apps/web/app/(app)/ops/page.tsx:664-667` and is pinned offline by `apps/web/app/(app)/ops/opsPresentation.test.ts` (5 tests, green), which asserts both DOM absence *and* hook-subscription absence for non-owners (`:132-143`) |
| 5 | 22.1 needs "CI evidence" | **VERIFIED-ABSENT (now satisfiable offline)** | 97/97 tests green, 3× `tsc --noEmit` exit 0, playbook hook exit 0. Full transcript in §2 |

### 1a. Google revocation ordering (lane brief claim)

**VERIFIED-EXISTS, exactly as described.** `packages/backend/convex/gmailAuth.ts:315-346`.

- Revoke fires **before** the local delete: `fetch(GOOGLE_REVOKE_ENDPOINT, ...)` at `:325-330`,
  `deleteTokens` at `:334`.
- **200 and 400 both count as revoked**: `const revoked = status === 200 || status === 400;` at
  `:332`, with the rationale at `:320-322` (400 = Google already considers it invalid; same end
  state).
- It POSTs the **refresh** token (`:328`), which kills the whole grant rather than one access token
  — reasoning at `:297-299`.
- The delete runs unconditionally, so a 5xx or network throw cannot strand the refresh token at rest
  (`:293-295`).
- Audit payload is flags + status code only (`:342`), honouring CLAUDE.md §4 while the refresh token
  is in scope one line above (`:340-341`).

### 1b. DataControls wiring (lane brief claim)

**VERIFIED-EXISTS.**

- `apps/web/app/(app)/dashboard/settings/DataControls.tsx:16` references
  `"tenantExport:exportTenantData"`; `:42` references `"tenantDelete:deleteTenantData"`.
- Mounted for real: `apps/web/app/(app)/dashboard/settings/page.tsx:1` imports it, `:41` renders
  `<DataControls />`.
- Pinned by test: `dataControls.test.ts:18`, `:34`, `:35` assert the two literal function references
  and the exact `DELETE_PHRASE`.
- Backing functions are genuinely tenant-scoped — `tenantQuery` (`tenantExport.ts:36`) and
  `tenantAction` (`tenantDelete.ts:187`), both deriving scope from the authenticated identity, never
  from a client arg (`tenantDelete.ts:48`).
- Per-provider truth is rendered rather than averaged into one boolean
  (`DataControls.tsx:29-36`, `:288-317`).

---

## 2. Offline-provable evidence (exact observed output)

The full monorepo suite was **not** run (known OOM, exit 134). Per-package, per-file only.

### 2a. Backend tests

`cd packages/backend && NODE_OPTIONS=--max-old-space-size=3072 npx vitest run convex/owner.test.ts convex/tenantExport.test.ts convex/tenantDelete.test.ts convex/microsoftAuth.test.ts convex/worm.test.ts convex/httpAuth.test.ts --maxWorkers=1`

```
 ✓ convex/microsoftAuth.test.ts (25 tests) 384ms
 ✓ convex/httpAuth.test.ts (13 tests) 1257ms
 ✓ convex/tenantDelete.test.ts (7 tests) 2781ms
 ✓ convex/worm.test.ts (10 tests) 1042ms
 ✓ convex/owner.test.ts (10 tests) 362ms
 ✓ convex/tenantExport.test.ts (4 tests) 670ms

 Test Files  6 passed (6)
      Tests  69 passed (69)
   Duration  66.05s
```

Non-failing stdout worth recording, because it is the WORM posture in §4:

```
stdout | convex/worm.test.ts > WORM cursor mechanics + stub safety (SC-4) > exportAudit stub path does NOT advance the cursor
worm export skipped (stub)
```

### 2b. Web tests

`cd apps/web && NODE_OPTIONS=--max-old-space-size=3072 npx vitest run "app/(app)/dashboard/settings/dataControls.test.ts" "app/(app)/ops/opsPresentation.test.ts" "app/(app)/ops/tenantSkillReview.test.ts" --maxWorkers=1`

```
 ✓ app/(app)/ops/tenantSkillReview.test.ts (15 tests) 19ms
 ✓ app/(app)/ops/opsPresentation.test.ts (5 tests) 66ms
 ✓ app/(app)/dashboard/settings/dataControls.test.ts (5 tests) 13ms

 Test Files  3 passed (3)
      Tests  25 passed (25)
   Duration  6.48s
```

### 2c. Core tests

`cd packages/core && npx vitest run src/tenantData.test.ts --maxWorkers=1`

```
 ✓ src/tenantData.test.ts (3 tests) 13ms
 Test Files  1 passed (1)
      Tests  3 passed (3)
```

**Test total: 97 passed / 97. Zero failures, zero skips.**

### 2d. Typecheck

| Package | Command | Exit |
|---|---|---|
| `packages/core` | `npx tsc --noEmit` | **0** (no output) |
| `packages/backend` | `npx tsc --noEmit` | **0** (no output) |
| `apps/web` | `npx tsc --noEmit` | **0** (no output) |

`packages/backend/convex/_generated/` was present in the worktree, so the CLAUDE.md §7 boot-order
precondition held and the backend typecheck is meaningful rather than vacuous.

### 2e. Playbook hook

`node scripts/check-playbooks.mjs` → **exit 0**, no output.

### 2f. Biome — classified by rule, not by count

`npx biome check` over the seven files this lane touches
(`app/privacy`, `dashboard/settings`, `tenantExport.ts`, `tenantDelete.ts`, `owner.ts`,
`microsoftAuth.ts`, `gmailAuth.ts`) reported **no diagnostics of any rule**:

```
Checked 1 file in 59ms. No fixes applied.
```

No CRLF noise on these paths, and therefore nothing masking a real rule violation. Nothing to
classify.

### 2g. Install

`pnpm install --frozen-lockfile` in the worktree → **exit 0**. All evidence above was actually
executed; none of it is source-only inference.

---

## 3. The owner-authorization primitive — definition and every call site

**Primitive:** `packages/backend/convex/lib/functions.ts`
- `:53` `export async function requireOwner(ctx: QueryCtx | MutationCtx)`
- `:77` `export const ownerQuery = customQuery(query, customCtx(requireOwner))`
- `:80` `export const ownerMutation = customMutation(mutation, customCtx(requireOwner))`
- `:50-53` records that there is deliberately **no `ownerAction`**.

**Grant path:** `packages/backend/convex/owner.ts:47` `bootstrapOwner = internalMutation` — not
client-callable, takes an exact `users._id`, never selects a row itself (`:37-42`), idempotent
(`:54`), audits the transition only (`:62-68`).

**Presentation helper:** `owner.ts:24` `viewer = tenantQuery` — deliberately *not* an `ownerQuery`,
because a non-owner must be able to learn it should not mount the admin surface (`:16-22`). Returns
one boolean and nothing else. Orphan identity fails closed (`:27-30`).

### 3a. Every owner-gated call site (non-test)

| Module | Line | Function | Wrapper |
|---|---|---|---|
| `finance.ts` | 329 | `globalRails` | `ownerQuery` |
| `finance.ts` | 356 | `controls` | `ownerQuery` |
| `finance.ts` | 424 | `setMasterKillSwitch` | `ownerMutation` |
| `finance.ts` | 440 | `setMediaKillSwitch` | `ownerMutation` |
| `finance.ts` | 455 | `setPerRequestBudget` | `ownerMutation` |
| `optimizerConfig.ts` | 84 | `getOptimizerStatus` | `ownerQuery` |
| `optimizerConfig.ts` | 93 | `setOptimizerEnabled` | `ownerMutation` |
| `skills.ts` | 371 | `activateCandidate` | `ownerMutation` |
| `skills.ts` | 391 | `candidatesForReview` | `ownerQuery` |
| `skills.ts` | 1289 | `tenantCandidatesForReview` | `ownerQuery` |
| `skills.ts` | 1360 | `activateTenantCandidate` | `ownerMutation` |
| `skills.ts` | 1384 | `rollbackTenantSkill` | `ownerMutation` |

### 3b. Is the Phase 8 deferral still open? **NO — it is CLOSED.**

Phase 8 left self-improvement/optimizer controls tenant-callable as a known, owner-approved
deferral. In current source they are owner-gated: `optimizerConfig.ts:84,93` and `skills.ts:371,391`
are the exact endpoints that deferral named, and all four now carry `ownerQuery`/`ownerMutation`.
`skills.ts:365-370` further records that `requireOwner` and the `EVAL_GATE` are two *independent*
gates and must stay that way.

### 3c. Owner-grade surfaces that are NOT owner-gated

Only four public functions reachable from `/ops` are not owner-gated. **I checked each for a
cross-tenant read and found none.** These are deliberate, documented, and safe:

| Function | Wrapper | Assessment |
|---|---|---|
| `deadLetters.ts:13` `newCount` | `tenantQuery` | Tenant-scoped. Feeds the shell badge. |
| `deadLetters.ts:25` `listNew` | `tenantQuery` | Tenant-scoped; rows are refs/hashes/counts only. |
| `deadLetters.ts:42` `markResolved` | `tenantMutation` | Tenant-scoped write on the caller's own rows. |
| `opsSignals.ts:28` `evalSignals` | `tenantQuery` | **Verified strictly tenant-scoped**: every read is keyed on `ctx.tenantId` — `:37` telemetry, `:68` audit, `:76` deadLetters. Returns numbers only (`:82-95`). |

The design is stated at `apps/web/app/(app)/ops/page.tsx:655-658` and `:698-700`: `/ops` is a
tenant page, and **only the optimizer section** is owner-gated. A non-owner loading `/ops` sees
their own dead letters and their own eval numbers — which is their data — and no optimizer surface.

**No unguarded owner-grade surface found.** The one nuance worth recording: `owner.viewer` being a
`tenantQuery` is load-bearing and correct, but it means "is there an owner on this deployment" is
inferable only as "am *I* the owner" — no enumeration, no disclosure.

---

## 4. THE PRIVACY POLICY — claim-by-claim against the code

This is the highest-priority section. `apps/web/app/privacy/page.tsx` is served on a public
production site. Every sentence below is a representation to data subjects and to Google's
restricted-scope reviewers.

### 4a. Claims the code DOES deliver

| Policy line | Claim | Verdict |
|---|---|---|
| `:316-321` | "You can disconnect your Google account at any time from within the application ... Revoking access invalidates our tokens immediately." | **DELIVERED** — `gmailAuth.ts:315-346`, revoke-then-delete |
| `:324-344` | "we cannot revoke the grant at Microsoft on your behalf, and we do not claim to" + the two consent URLs | **DELIVERED and unusually honest** — `microsoftAuth.ts:283-301`, hard `false`; URLs mirrored at `DataControls.tsx:26-27` |
| `:342-343` | "When you delete your data, we tell you exactly which of these two outcomes each connected account actually reached." | **DELIVERED** — `tenantDelete.ts:229-243` builds per-provider truth; `DataControls.tsx:288-317` renders it per provider |
| `:138-141` | "We cannot permanently delete your email. Deletion requires the `https://mail.google.com/` scope. We do not request it" | **DELIVERED — TRUE.** `packages/core/src/calendar.ts:50-55`: `GOOGLE_SCOPES` contains gmail.modify, calendar.freebusy, calendar.events, drive.readonly. `https://mail.google.com/` is **absent**. |
| `:299-300` | Portability (Art. 20) — "structured, machine-readable format" | **DELIVERED** — `tenantExport.ts:36`, JSON blob download at `DataControls.tsx:100-107` |
| `:293` | Erasure (Art. 17) | **DELIVERED** — `tenantDelete.ts:187` |
| `:158-160` | "It never reaches our audit log ... references, identifiers, hashes and counts" | **DELIVERED** — enforced convention; audit payloads inspected at `gmailAuth.ts:342`, `microsoftAuth.ts:297`, `owner.ts:67` are all flags/ids only |

### 4b. FALSE OR UNDISCLOSED — live compliance exposure

Six findings. Each gives the exact policy sentence and the exact code that contradicts it.

---

**F-1 — SEVERITY: HIGH. The policy names a processor that is not in the path, and misdescribes how
OpenAI is reached.**

Policy `:229-232`:
> "**Vercel** — hosting of this website and the application interface, and **its AI Gateway, which
> routes our model requests to the provider below.** The AI Gateway transmits message content in
> order to route it; it does not retain or train on it."

Policy `:241-244`:
> "**OpenAI** — ... **Reached through Vercel's AI Gateway (above)**, OpenAI receives the content of
> messages our agents read..."

Code says the exact opposite, in a comment written specifically to record the reversal —
`packages/backend/convex/llm.ts:16-18`:
> "`resolveModel` maps each to a direct-OpenAI LanguageModel via `@ai-sdk/openai` (reads
> `OPENAI_API_KEY` from the deployment env). **The Vercel AI Gateway is NOT used** — BYOK there is
> gated behind paid Vercel credits, a double charge on top of the OpenAI key we already pay for
> (decision 2026-07-13)."

Confirmed at `llm.ts:24` (`import { openai } from "@ai-sdk/openai"`), `:248` and `:250`
(`return openai(id.replace(/^openai\//, ""))`), and by the only OpenAI host in the tree,
`https://api.openai.com`.

The policy describes a data flow through a subprocessor that has been switched off since
2026-07-13. Naming a processor that does not process, in an Art. 28 disclosure list, is a
misrepresentation in both directions.

---

**F-2 — SEVERITY: HIGH. Google is used as an AI model and embedding provider; the policy discloses
Google only as a mail provider.**

Policy `:237-239` — the entire Google entry:
> "**Google** — reading, organising, and sending mail in your connected mailbox, via the Gmail API."

Code sends user content to Google **as an LLM and embedding vendor**, an entirely separate purpose:
- `llm.ts:22` `import { createGoogleGenerativeAI, google } from "@ai-sdk/google"`
- `llm.ts:23` `import { createVertex, vertex } from "@ai-sdk/google-vertex"`
- `llm.ts:244-252` — model ids of the form `"google/gemini-2.5-flash"` resolve to AI Studio or Vertex
- `vaultRag.ts:141` — vault document embeddings POST to
  `https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_EMBEDDING_MODEL}:batchEmbedContents`

§4 `:153-156` tells the user message content goes to "the large-language-model provider named in
section 7" — **singular**. There are two, and the second is undisclosed.

**This one also carries Google-programme risk specifically:** restricted-scope (`gmail.modify`) data
being routed to a second AI vendor that the policy does not name is precisely what the annual
restricted-scope assessment at `:176-178` examines.

---

**F-3 — SEVERITY: HIGH. Three whole subprocessors are absent from section 7.**

Section 7 `:224-246` lists exactly five: Convex, Vercel, AWS, Google, OpenAI. Missing:

| Undisclosed recipient | Evidence | What it receives |
|---|---|---|
| **fal.ai** | `http.ts:259-290`, asset hosts `:279` `["fal.media","fal.ai","fal.run"]` | Media-generation prompts and generated assets |
| **Tavily** | `llm.ts:411` `fetch("https://api.tavily.com/search", ...)`, key at `:400` | Agent web-search queries, which are derived from user instructions |
| **Alibaba Cloud (WAN)** | `media.ts:929-933` — `WAN_API_BASE_URL` is validated to `.maas.aliyuncs.com`; assets from `.aliyuncs.com` at `:1315` | Video/image generation prompts and output assets |

---

**F-4 — SEVERITY: HIGH. The international-transfers section is factually wrong.**

Policy `:253-255`:
> "Our providers are established in, or process data in, **the United States.** Where personal data
> is transferred outside the UK or European Economic Area, we rely on the European Commission's
> Standard Contractual Clauses, the UK International Data Transfer Addendum, or an adequacy
> decision..."

`media.ts:932` pins the WAN endpoint to `.maas.aliyuncs.com`, and the region actually exercised is
**`ap-southeast-1`** (Singapore) — `media.test.ts:1016` stubs
`https://workspace.ap-southeast-1.maas.aliyuncs.com`, `:1229` returns assets from
`https://assets.oss-ap-southeast-1.aliyuncs.com`.

So there is an **Alibaba Cloud, APAC-region** transfer that the policy neither names as a processor
(F-3) nor covers in its transfer statement. "Our providers ... process data in the United States" is
a false statement as written, and the Art. 46 safeguard the section offers to produce on request
does not exist for this recipient.

---

**F-5 — SEVERITY: HIGH. The Google grant requests Drive and Calendar; the policy discloses only
`gmail.modify`.**

Policy `:116-119`:
> "If you choose to connect a Google account, Pikar requests the `gmail.modify` scope. Google
> classifies this as a **restricted** scope..."

Policy `:122-125` — the bolded "we do not pretend otherwise" paragraph:
> "Our agents can read the email in your mailbox, draft and send messages as you, and organise your
> mail with labels."

Actual grant — `packages/core/src/calendar.ts:50-55`:
```
GOOGLE_SCOPES = [ GMAIL_MODIFY_SCOPE, CALENDAR_FREEBUSY_SCOPE, CALENDAR_EVENTS_SCOPE, DRIVE_READONLY_SCOPE ]
```
with `DRIVE_READONLY_SCOPE = "https://www.googleapis.com/auth/drive.readonly"` at `:40`,
`CALENDAR_FREEBUSY_SCOPE` at `:26`, `CALENDAR_EVENTS_SCOPE` at `:29`.

Drive read access is **live, not aspirational** — `vaultDrive.ts:64-65` calls
`https://www.googleapis.com/drive/v3/files` and `/drive/v3/drives`.

`gmailAuth.ts:301-302` states the design plainly: "ONE Google grant covers mail AND calendar ... so
this ends inbox reads, sends, free/busy and event creation together." The disconnect copy is honest
about the breadth; **the privacy policy is not.** A user reading section 4 would not learn that
connecting Google lets Pikar read their Drive.

This is the single worst mismatch in the file, because §4 is the section that exists specifically to
be maximally candid about scope breadth — and it under-states the grant.

---

**F-6 — SEVERITY: MEDIUM-HIGH. Microsoft mailbox data is collected and never disclosed.**

`MICROSOFT_SCOPES` — `packages/core/src/microsoft.ts:50-56` — requests
`offline_access`, `openid/profile/email` (`:37`), `Calendars.ReadWrite` (`:23`), **`Mail.Send`
(`:26`)** and **`Mail.Read` (`:30`)**.

The policy's collection inventory (§2 `:51-76`) and its dedicated mailbox section (§4 `:115-185`)
are **Google-only**. Microsoft appears exactly once in the whole document — the disconnect paragraph
at `:324`. So the policy tells the user how to disconnect a Microsoft account whose data it never
admits to collecting, and Microsoft is absent from the §7 processor list.

Compounding it: **no code calls Mail.Read or Mail.Send.** The only Microsoft modules are
`microsoftAuth.ts` and `microsoftCalendar.ts` (`GRAPH_BASE` at `:26`); a search for `sendMail` or
`/me/messages` under `microsoft*.ts` returns nothing. So the app holds a token granting full mailbox
read and send **for a capability it does not implement** — an Art. 5(1)(c) data-minimisation problem
on top of the disclosure problem. `microsoftAuth.ts:229` even surfaces a `mailReady` boolean for
that unimplemented capability.

---

### 4c. The reverse check — code that is *more* protective than the policy says

Recording these so the fix does not accidentally weaken accurate copy:

- Policy `:268-271` promises the audit log "**is** exported to write-once storage". The exporter is
  real — `worm.ts:77-82` sets `ObjectLockMode: "COMPLIANCE"` with a retain-until date, scheduled
  daily at `crons.ts:10`. **But it is env-gated**: `worm.ts:55` returns `{skipped:true}` when
  `WORM_BUCKET` is unset, and the test transcript in §2a shows `worm export skipped (stub)` firing.
  The sentence is present-tense and therefore **only true if `WORM_BUCKET` + `AWS_REGION` +
  `AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY` are set on the production deployment.** This is a
  config assertion the owner must confirm, not a code gap — flagged in the runbook (§6c).
- `DataControls.tsx:156-165` discloses more than the policy requires (tokens never exported, audit
  archive omitted). Accurate.

### 4d. Separate defect found while reading DataControls (not a policy issue)

`DataControls.tsx:300-313` renders the Microsoft consent links whenever
`entry.localRowDeleted && !entry.revokedAtProvider` — **without checking `entry.provider`.**

A failed Google revoke produces exactly that state: `tenantDelete.ts:200-212` sets
`googleFailure = !result.revoked` and `googleRevoked = false` while `localRowDeleted` is
`connected.googleConnected === true` (`:231-234`). The UI then tells the user to remove their
**Google** consent at `account.microsoft.com` / `myapps.microsoft.com`. The correct destination for
Google is `myaccount.google.com/permissions`, which the policy already cites at `:318`.

Low blast radius (only on a Google revoke failure), but it is wrong guidance on an erasure surface.
Not fixed here — this lane writes no product code.

---

## 5. Recommended remediation order (no code written by this lane)

1. **F-5** and **F-2** first — they touch the Google restricted-scope representation.
2. **F-1**, **F-3**, **F-4** — rewrite §7 to the real processor set and §8 to the real regions.
3. **F-6** — either disclose Microsoft mailbox collection, or drop `Mail.Read`/`Mail.Send` from
   `MICROSOFT_SCOPES` until implemented. **Dropping is the smaller, safer change** and removes the
   minimisation finding at the same time.
4. §4d UI defect — one `entry.provider === "microsoft"` condition.
5. Confirm `WORM_BUCKET` in production or soften §9's present tense.

Note for whoever does this: `HAS_PLACEHOLDERS` (`apps/web/app/legal.ts`, used at
`privacy/page.tsx:25-32`) already blocks the production build on unresolved bracketed placeholders.
It does **not** and cannot check disclosure accuracy — nothing in CI does. Consider a test that pins
the §7 processor list against the set of external hosts reachable from `packages/backend/convex`.

---

## 6. RUNBOOK — the two live-browser items

Neither can be proven offline. Both need the owner at a real browser against a real deployment.

### 6.0 THE TRAP — read this before either run

`packages/backend/.env.local` carries `CONVEX_DEPLOYMENT`, which is what `npx convex run` and
`npx convex env` target. `apps/web/.env.local` carries `NEXT_PUBLIC_CONVEX_URL`, which is what the
**browser** talks to (`apps/web/app/providers.tsx:11`; the note at `:7-9` warns that
`npx convex dev` does not write it for you).

**If those two name different deployments — e.g. backend points at a CLOUD deployment while the web
app points at a LOCAL one — every seed and every `bootstrapOwner` lands where the browser cannot see
it.** The only symptom is the onboarding gate rejecting a tenant you just seeded, or `owner.viewer`
returning `{isOwner:false}` for the account you just promoted. It does not error. It looks like a
code bug and is not.

**Mandatory pre-flight for both runs:**

```bash
# 1. Print both and compare by eye. They must resolve to the SAME deployment.
grep CONVEX_DEPLOYMENT packages/backend/.env.local
grep NEXT_PUBLIC_CONVEX_URL apps/web/.env.local

# 2. Confirm the browser's deployment is the one you are about to write to.
cd packages/backend && npx convex env list      # must run from packages/backend
```

Do not proceed until they match. Then, in the browser, confirm you are signed in as the intended
account **before** promoting anyone.

---

### 6a. Phase 22 — owner vs non-owner `/ops` DOM + mount-guard observation

**Purpose:** prove the guard at `apps/web/app/(app)/ops/page.tsx:664-667` holds in a real browser —
specifically that a non-owner never *subscribes* to the owner-only queries, not merely that the
optimizer panel is invisible.

**Prerequisites**
- The deployment both `.env.local` files agree on (§6.0).
- Two seeded, onboarded accounts on that deployment: `OWNER` and `NONOWNER`. Both must have
  completed onboarding or the gate intercepts before `/ops` renders.
- `OWNER` promoted, and the promotion proven idempotent:
  ```bash
  cd packages/backend
  npx convex run owner:bootstrapOwner '{"userId":"<users._id of OWNER>"}'   # expect {"changed":true,...}
  npx convex run owner:bootstrapOwner '{"userId":"<users._id of OWNER>"}'   # expect {"changed":false,...}
  ```
  The `changed:true` → `changed:false` transition is itself part of the proof (`owner.ts:44-45`).
- `NONOWNER` must **never** be passed to `bootstrapOwner`.
- Build: **production build, not `next dev`** — the workspace route OOMs under dev.

**Steps**
1. Sign in as `NONOWNER`. Open `/ops`. Open DevTools → Network → filter WS, and the React DevTools
   component tree.
2. Record: is there any element matching `aria-label="Optimizer kill switch"`? Is the text
   `Optimizer` present as a section label?
3. In the WS frames, record the set of subscribed query paths.
4. Sign out. Sign in as `OWNER`. Repeat 2–3 on `/ops`.

**Exact observation that constitutes proof**
- As `NONOWNER`: **no** `aria-label="Optimizer kill switch"` in the DOM, **no** `>Optimizer</p>`
  label, and — the load-bearing part — the WS subscription set contains **none** of
  `optimizerConfig:getOptimizerStatus`, `skills:candidatesForReview`,
  `skills:tenantCandidatesForReview`. Absence from the *subscription* set is the real assertion;
  DOM absence alone would also be produced by a CSS-hidden panel, which `page.tsx:698-700` explains
  would still leak.
- `NONOWNER` **does** still see `>Eval signals</p>` and `>Dead letters</p>` — that is correct and
  deliberate (§3c), not a failure.
- As `OWNER`: the optimizer section renders and those three subscriptions appear.
- Bonus, if easy: with DevTools, call `optimizerConfig:setOptimizerEnabled` directly as `NONOWNER`
  and confirm the **server** rejects it. This is the actual trust boundary; the mount guard is only
  presentation (`owner.ts:20-22`).

---

### 6b. Phase 22.1 — destructive disposable-tenant deletion run

**Purpose:** prove `tenantDelete:deleteTenantData` erases a real tenant, reports per-provider truth,
and leaves the audit archive standing.

**DESTRUCTIVE AND IRREVERSIBLE. Read the account name twice.**

**Prerequisites**
- §6.0 satisfied.
- A **disposable** account — `DISPOSABLE` — that is not the owner and holds nothing anyone wants.
  Do **not** use `OWNER`: deletion removes the `users` row (`tenantDelete.ts:250-256`), which would
  destroy the owner grant and require re-running `bootstrapOwner`.
- `DISPOSABLE` onboarded, with a Google account connected (a throwaway Google account) so the
  revoke path is actually exercised. Optionally also a Microsoft account, to observe the asymmetry.
- Some content present: a vault document, a contact, a plan — so `deletedByTable` is non-trivial.
- Before deleting, click **Download my data** and keep the JSON. It is the before-picture.

**Steps**
1. Sign in as `DISPOSABLE`. Go to `/dashboard/settings`.
2. Click **Download my data**. Save the file. Note `limits.totalRows` and confirm no `accessToken` /
   `refreshToken` string appears anywhere in it.
3. In the erasure card, type `DELETE MY DATA` exactly. Confirm the button only enables on the exact
   phrase (`DataControls.tsx:76`) — try `delete my data` first and confirm it stays disabled.
4. Click **Delete my data**. Do not close the tab.
5. Read the per-provider list that renders beneath.
6. Independently verify at Google: open `myaccount.google.com/permissions` for the throwaway account.

**Exact observation that constitutes proof**
- The status line reads `Erased — N rows across M tables. Your account data and content are gone.`
  with `N` in the same order as the export's `limits.totalRows`.
- The provider list shows **Google: "revoked at the provider."** and, if Microsoft was connected,
  **Microsoft: "removed here only — not revoked at the provider."** followed by the two
  `account.microsoft.com` / `myapps.microsoft.com` links. That asymmetry, rendered in one list, is
  the GOVN-03 honesty property.
- At `myaccount.google.com/permissions`, the **Pikar entry is gone** — this is the proof
  `gmailAuth.ts:325-332` actually reached Google, which no offline test can establish.
- Signing back in as `DISPOSABLE` lands on onboarding, not a populated dashboard.
- Audit survives: `npx convex run` a read of the `audit` table for that tenant and confirm rows
  remain, holding only refs/ids/counts. `tenantDelete.ts` cannot reach the audit table by
  construction (pinned by `tenantDelete.test.ts`, "cannot reach audit/global tables and preserves
  immutable audit rows").
- **Known cosmetic defect to expect** (§4d): if the Google revoke *fails*, the UI will wrongly offer
  Microsoft consent links for Google. Record it if seen; it does not invalidate the run.

---

### 6c. Third item the owner should confirm while live (config, not code)

Confirm on the production deployment:

```bash
cd packages/backend && npx convex env list | grep -E "WORM_BUCKET|AWS_REGION"
```

If `WORM_BUCKET` is unset, `worm.ts:55` short-circuits and the daily cron writes nothing — which
makes privacy policy `:268-271` ("are exported to write-once storage that **cannot be modified or
deleted**") untrue in production. Either set it or soften the sentence to future tense.

---

## 7. Bottom line

- **The lane hypothesis is confirmed.** The audit's GOVN-03 gap list is stale: export, bounded
  deletion, and honest Microsoft posture all exist, are wired, are tenant-scoped, and are tested.
  Phase 22's "presentation open" is also stale.
- **Phase 8's owner-gating deferral is closed.** No unguarded owner-grade surface remains.
- **Offline evidence is green across the board:** 97/97 tests, 3× `tsc --noEmit` exit 0, playbook
  hook exit 0, biome clean. Zero reds to report.
- **But the audit looked at the wrong half of the policy.** The *controls* the policy promises are
  all delivered. The *disclosures* it makes are wrong in six places — undisclosed AI vendors
  (Google Gemini/Vertex), undisclosed subprocessors (fal.ai, Tavily, Alibaba Cloud), a named
  processor that is not used (Vercel AI Gateway), a false US-only transfer statement, an
  under-stated Google grant that hides live Drive access, and Microsoft mailbox scopes that are
  requested, undisclosed, and unimplemented.

The correct milestone status for GOVN-03 is **not** "gaps closed" and **not** "gaps as listed" — it
is "the listed gaps are closed; a distinct and more serious disclosure defect is open on a public
production page."

---

_Produced by the 22-evidence lane. Verification only — no product code was written._
