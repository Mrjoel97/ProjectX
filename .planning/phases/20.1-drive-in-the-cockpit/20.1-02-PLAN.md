---
phase: 20.1-drive-in-the-cockpit
plan: 02
type: execute
wave: 2
depends_on: ["20.1-01", "20-12"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/scripts/eval-cases/39-drive-read.json
  - packages/backend/scripts/run-eval-golden.mjs
  - docs/playbooks/agent-runtime.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/skill-registry.md
  - docs/playbooks/vault.md
  - .planning/phases/20.1-drive-in-the-cockpit/20.1-02-SUMMARY.md
autonomous: false
requirements: [VALT-15]
scope: "Drive body/fixture candidate cycle and real metadata-only UAT. Substrate is 20.1-01."

must_haves:
  truths:
    - "The Drive body edit starts from the exact active media fingerprint evidenced by 20-12."
    - "A Drive-specific fixture proves ordinary Drive language selects a read tool from agent-step state."
    - "All four watched playbooks are updated before the first live/paid pause."
    - "Paid seed/eval, exact-version activation and later real Drive UAT are sequential, distinct authorities/checkpoints."
    - "Real UAT names a real result while import/ingest/budget movement remains exactly zero."
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "Drive read guidance and import-is-the-user's-click rule"
      contains: "findInDrive"
    - path: "packages/backend/scripts/eval-cases/39-drive-read.json"
      provides: "Plain-language Drive-read selection fixture"
      contains: "driveReadToolCount"
    - path: "docs/playbooks/agent-runtime.md"
      provides: "Drive observable/fixture and candidate-pinned gate contract before live pause"
      contains: "driveReadToolCount"
    - path: ".planning/phases/20.1-drive-in-the-cockpit/20.1-02-SUMMARY.md"
      provides: "Candidate/eval/activation and real Drive zero-ingest UAT evidence"
  key_links:
    - from: "packages/backend/scripts/eval-cases/39-drive-read.json"
      to: "packages/backend/scripts/run-eval-golden.mjs"
      via: "driveReadToolCount reads findInDrive/listDriveFolders agent-step records"
      pattern: "driveReadToolCount"
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "packages/contracts/src/skills/cockpitAgent.ts"
      via: "same-commit generated mirror"
      pattern: "cockpitAgentSkillBody"
---

<objective>
Teach, certify and activate the Drive read tools built by 20.1-01, then separately observe a real
metadata-only Drive lookup with zero import or ingest-budget movement.

This plan begins only after the media-bearing 20-12 body is active. It owns one offline body/fixture
edit, one paid candidate gate, one activation authority and one later real Drive UAT checkpoint.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-drive-in-the-cockpit/20.1-01-SUMMARY.md
@.planning/phases/20-media-canvas/20-12-SUMMARY.md
@packages/contracts/skills/cockpit-agent.md
@packages/backend/scripts/run-eval-golden.mjs
@docs/playbooks/agent-runtime.md
@docs/playbooks/cockpit.md
@docs/playbooks/skill-registry.md
@docs/playbooks/vault.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive body, mirror, owed fixture and every watched playbook</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, packages/backend/scripts/eval-cases/39-drive-read.json, packages/backend/scripts/run-eval-golden.mjs, docs/playbooks/agent-runtime.md, docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md, docs/playbooks/vault.md</files>
  <action>
Use 20-12's recorded active version/sha as the expected predecessor. Make the offline edit:

1. Add one Drive section: `findInDrive` answers "where is X"; `listDriveFolders` walks one level;
   import remains the user's click in the Vault Drive panel; the agent names the result and never
   claims import; reauth relays the exact reconnect sentence.
2. Regenerate the TypeScript mirror in the same commit and run drift tests.
3. Add fixture 39 using ordinary "which folder in my Drive has the Q3 numbers?" language. Add closed
   `driveReadToolCount` from `agentSteps` for the two read tools, not reply prose. Require at least one
   read call, zero recipients/attachments and no applied plan. Add self-checks that reject zero as
   vacuous, fail absent/write-tool calls, mutation-check the reader and raise the fixture floor by one.
4. Before the first pause, update: `agent-runtime.md` for the observable/fixture/gate;
   `skill-registry.md` for owed fixture and lifecycle; `cockpit.md` for selection/reconnect/no-import;
   `vault.md` for the user-click import boundary and real-UAT evidence slots. Run playbook gate now.
  </action>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The offline body/mirror/fixture and all four watched playbooks are green; no live read or seed occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: authorize live registry read, candidate seed and paid Drive gate</name>
  <files>None — live registry/eval state only</files>
  <action>Present offline proofs and expected predecessor, then wait for a proceed signal naming a hard maximum before reading/mutating live registry or running paid eval.</action>
  <decision>May the executor verify the live predecessor, seed the Drive candidate and run its complete gate?</decision>
  <context>The complete gate historically costs about $0.35 plus the new case. Any paid one-case diagnostic is inside the same maximum. Google Drive itself is not called by the eval fixture; real Drive access waits for Task 5.</context>
  <options>
    <option id="proceed"><name>Proceed with a named maximum</name><pros>Produces exact candidate evidence.</pros><cons>Consumes one complete paid golden run.</cons></option>
    <option id="hold"><name>Hold</name><pros>No live mutation/spend.</pros><cons>VALT-15 remains offline-only.</cons></option>
  </options>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The owner approved an explicit maximum, or execution stopped without live access.</done>
  <resume-signal>Reply `proceed Drive gate, max $X` or `hold`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: verify predecessor, seed and evaluate the Drive candidate</name>
  <files>None — live skill/eval rows only</files>
  <action>After approval, read live active/candidate rows. Stop unless active version/sha equals 20-12 and the stream is empty. Seed, read back actual candidate version/sha, optionally falsify fixture 39 within the same maximum, then run the complete gate pinned to that candidate. Record run/count/retries/pins/cost, attach evidence and leave candidate parked. Partial runs record no evidence.</action>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The exact Drive candidate has complete green evidence and remains unactivated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: owner activates the evidenced Drive candidate</name>
  <files>None — owner-gated live skill row only</files>
  <action>Present exact candidate/gate evidence and wait for exact-version authorization; activate only that candidate and read back active version/sha. Do not call real Drive yet.</action>
  <what-built>A Drive candidate pinned to a complete green gate.</what-built>
  <how-to-verify>Review version/sha, full case count, retries and cost. After authorization, activate exact version and prove live active version/sha equals evidence. Rejection leaves candidate parked.</how-to-verify>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The exact evidenced Drive candidate is active, or the plan stopped parked.</done>
  <resume-signal>Reply `activate Drive candidate <actual-version>` or describe the rejection.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: real Drive metadata-only zero-ingest UAT</name>
  <files>None — real Google metadata response and observed refs/counts only</files>
  <action>Only after Task 4 active-row readback, run the real connected-account lookup and record zero-import evidence.</action>
  <what-built>An active Drive-aware cockpit body over the offline read-only substrate.</what-built>
  <how-to-verify>
Use a connected `drive.readonly` account and a non-sensitive folder/file whose expected folder name is
known. Ask in plain language which folder contains it, then drill one level. Confirm returned names
match Drive and the agent never claims import/content ingestion. Compare ingest budget, vault rows,
reservations and Drive-import workflow rows before/after: all deltas zero. Confirm refs-only audit/
trace payloads contain no Drive name. If a genuine pre-widening token exists, also observe the exact
reconnect sentence and zero fetch; otherwise retain the offline ordering proof and label that live
case unavailable rather than fabricating it.
  </how-to-verify>
  <verify><automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultDrive.test.ts convex/dispatchGuard.test.ts</automated></verify>
  <done>A real Drive result is named accurately with zero import/reservation/ingest-budget movement and no name in log planes.</done>
  <resume-signal>Reply `approved` with real result and zero-ingest evidence, or describe the mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: append evidence-only playbook updates and summary</name>
  <files>docs/playbooks/agent-runtime.md, docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md, docs/playbooks/vault.md, .planning/phases/20.1-drive-in-the-cockpit/20.1-02-SUMMARY.md</files>
  <action>Do not redesign instructions after UAT. Append only actual version/sha, gate run/count/retries/cost, activation authorization/readback, real Drive refs/counts, zero deltas and unavailable pre-widen token note if applicable. Create summary, run playbook gate/diff check, then mark VALT-15 complete only if every validation row is satisfied.</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>The candidate lifecycle and real zero-ingest UAT are reproducibly evidenced.</done>
</task>

</tasks>

<verification>
- 20-12's exact active media fingerprint is the predecessor.
- All four watched playbooks are current before the first pause; post-UAT changes are evidence-only.
- Paid gate, activation and real Drive UAT are distinct sequential authorities/checkpoints.
- Real Drive names match while every import/ingest/budget delta remains zero.
- Runner self-check, targeted tests, playbook gate and diff check pass.
</verification>

<success_criteria>
- The active agent selects the read tools for ordinary Drive language and relays reauth honestly.
- No agent path can import or spend ingest budget.
- A real metadata-only lookup is observed and documented with zero write-side movement.
</success_criteria>

<output>
Create `20.1-02-SUMMARY.md` after real UAT. Mark VALT-15 complete only from full evidence.
</output>
