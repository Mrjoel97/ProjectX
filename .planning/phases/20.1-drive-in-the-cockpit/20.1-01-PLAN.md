---
phase: 20.1-drive-in-the-cockpit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/vaultDrive.ts
  - packages/backend/convex/vaultDrive.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/dispatchGuard.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/vault.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [VALT-15]
scope: "Offline Drive/tool substrate only. No skill body, registry, eval, activation or real Google call; those are 20.1-02."

must_haves:
  truths:
    - "findInDrive searches Drive through the existing tenantAction/client and escapes query-language text."
    - "The cockpit has two read-only Drive tools with matching trace literals/verbs and offline SMOKE ops."
    - "A structural test proves neither tool can import, reserve, export, land or ingest."
    - "All changes and tests are offline; no candidate or provider state changes."
  artifacts:
    - path: "packages/backend/convex/vaultDrive.ts"
      provides: "Tenant-scoped findInDrive using the existing Drive client"
      exports: ["findInDrive"]
    - path: "packages/backend/convex/llm.ts"
      provides: "listDriveFolders/findInDrive tools plus both SMOKE operations"
    - path: "packages/backend/convex/dispatchGuard.test.ts"
      provides: "Non-vacuous no-import/no-spend structural guard"
    - path: "docs/playbooks/vault.md"
      provides: "Search boundary, escaping rule and no-import architecture before any live pause"
  key_links:
    - from: "packages/backend/convex/llm.ts"
      to: "packages/backend/convex/vaultDrive.ts"
      via: "thin ctx.runAction calls to the existing tenantActions"
      pattern: "listDriveFolders|findInDrive"
---

<objective>
Build the entire read-only Drive/tool substrate at $0 and stop before the shared skill registry.

The Executive Agent is not taught these tools here. This plan is independently resumable and safe to
execute before 20-12 becomes active; plan 20.1-02 owns body/fixture, live candidate/eval/activation
and real Drive UAT.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/backend/convex/vaultDrive.ts
@packages/backend/convex/llm.ts
@docs/playbooks/vault.md
@docs/playbooks/cockpit.md

<interfaces>
`vaultDrive.listDriveFolders` already handles root, shared drives, `reauth` before token/network work,
and both shared-drive flags. Reuse it; do not build a second Drive client. Drive names may reach the
model response, but never audit/trace payloads. `drive.readonly` remains the entire grant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: findInDrive through the existing Drive boundary</name>
  <files>packages/backend/convex/vaultDrive.ts, packages/backend/convex/vaultDrive.test.ts</files>
  <action>
Add `tenantAction findInDrive({query})` returning the existing refusal union or bounded hits
`{id,name,kind,parentName?,readable}`. Use the existing `driveUrl` helper and both shared-drive params.
Search `name contains` plus `fullText contains`, `trashed=false`, pageSize 20, no pagination. Escape
single quotes at the Drive query-language boundary (percent encoding is not sufficient). Test the
actual fetch query, quote injection and pre-widening `reauth` with fetch never called.
  </action>
  <verify><automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultDrive.test.ts</automated></verify>
  <done>Drive search is bounded, shared-drive-aware and query-language safe.</done>
</task>

<task type="auto">
  <name>Task 2: register both read tools with trace parity</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/schema.ts, apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
Add `listDriveFolders` and `findInDrive` to the cockpit tool record. They return sentences/refs/names,
never throw or return bytes. Add both `agentSteps.tool` literals and both `cards.tsx` VERB entries in
the same commit. Map reauth/refresh/not-connected/bad-id refusals to honest sentences. Never accept
tenantId from the model. Preserve traceParity set equality; do not weaken its non-vacuity floor.
  </action>
  <verify><automated>cd packages/backend &amp;&amp; npx vitest run convex/traceParity.test.ts convex/cockpitTools.test.ts</automated></verify>
  <done>Both read tools are callable, traceable and failure-safe.</done>
</task>

<task type="auto">
  <name>Task 3: structural no-import/no-spend proof</name>
  <files>packages/backend/convex/dispatchGuard.test.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <action>
First assert `llm.ts` contains the two legitimate Drive read calls. Then assert it contains no
reference/call/schedule path to `importDriveFolder`, `reserveFolder`, `openRun`, `exportOne`,
`landFile` or ingest. Assert Drive tools are absent from `SPECIALIST_TOOLS`. Mutation-insert an
import call inside a read tool, observe RED, and revert.
  </action>
  <verify><automated>cd packages/backend &amp;&amp; npx vitest run convex/dispatchGuard.test.ts convex/cockpitTools.test.ts</automated></verify>
  <done>The agent can read Drive and provably cannot import or move ingest budget.</done>
</task>

<task type="auto">
  <name>Task 4: closed offline SMOKE operations</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <action>Add `drive=list:&lt;folderId&gt;` and `drive=find:&lt;query&gt;` across all four closed registration sites: `AgentSmokeOp`, parser, total `SMOKE_OP_TOOL` record and executor. Add round-trip and refusal tests; no gateway/Google key is required.</action>
  <verify><automated>cd packages/backend &amp;&amp; npx vitest run convex/cockpitTools.test.ts</automated></verify>
  <done>Both read tools are offline-drivable and total registration remains compile-checked.</done>
</task>

<task type="auto">
  <name>Task 5: update substrate playbooks before handoff</name>
  <files>docs/playbooks/vault.md, docs/playbooks/cockpit.md</files>
  <action>Before this plan ends, document findInDrive, query escaping, the reused Drive client, both read tools, trace/SMOKE registrations and the structural no-import/no-spend proof. State explicitly that the tools remain invisible until 20.1-02 activates the skill body. Run playbook gate after the content exists.</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>Watched substrate paths have current playbooks and the handoff to 20.1-02 is explicit.</done>
</task>

</tasks>

<verification>
- Targeted backend/core/web tests and typecheck are green at the repository's zero-error invariant.
- Mutation of the no-import guard turns RED and is reverted.
- `node scripts/check-playbooks.mjs` and `git diff --check` pass.
- No registry, eval, Google or ingest-budget state changes occur.
</verification>

<success_criteria>
- The complete offline read-only substrate exists and is structurally unable to import.
- Tool/trace/SMOKE parity is closed.
- 20.1-02 can teach/evaluate the tools without reopening substrate implementation.
</success_criteria>

<output>
Create `.planning/phases/20.1-drive-in-the-cockpit/20.1-01-SUMMARY.md`. Do not mark VALT-15 complete;
the body/activation/UAT closure is 20.1-02.
</output>
