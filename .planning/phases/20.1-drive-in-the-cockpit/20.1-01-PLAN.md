---
phase: 20.1-drive-in-the-cockpit
plan: 01
type: execute
wave: 1
depends_on: ["20-12"]
files_modified:
  - packages/backend/convex/vaultDrive.ts
  - packages/backend/convex/vaultDrive.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/dispatchGuard.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/scripts/eval-cases/39-drive-read.json
  - packages/backend/scripts/run-eval-golden.mjs
  - docs/playbooks/vault.md
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [VALT-15]
resume: "REFRESHED 2026-08-10. Plan-level dependency is the ACTIVE 20-12 cockpit-agent version, not merely a landed source diff. Tasks 1-4 stay offline and resumable; Task 5 owns every live/paid/activation/UAT gate."

must_haves:
  truths:
    - "The model can SEE a tenant's Drive folders and name the one holding what was asked for"
    - "The model CANNOT spend a cent of ingest budget — every import is a human click"
    - "A pre-widening tenant gets the reconnect sentence from the agent, never a 403 or a lie"
    - "A Drive file NAME never reaches an audit payload, a step-trace row or a skill body"
    - "The tool is invisible to the model until the gated cockpit-agent candidate goes active"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "listDriveFolders + findInDrive as READ-ONLY cockpit tools"
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "the section that teaches both tools, and the rule that import is the user's click"
  key_links:
    - from: "packages/backend/convex/llm.ts"
      to: "packages/backend/convex/vaultDrive.ts"
      via: "the SAME tenantActions the picker calls — no second Drive client"
      pattern: "listDriveFolders"
---

<objective>
Let the Executive Agent **read** the user's Google Drive: list folders, drill down, and answer
"which folder has the Q3 numbers". Nothing else.

**The scope does not move and the money does not move.** `drive.readonly` stays the whole grant,
so there is no new consent, no verification wall and no write risk; and the import stays a human
click in the vault picker, so the model has no path to the ingest budget. This phase makes Drive
*legible* to the agent — it deliberately does not make Drive *actionable* by it.

**Hard dependency refreshed 2026-08-10:** begin this plan only after 20-12's media-bearing
`cockpit-agent` candidate is owner-activated and the live row's version/sha matches its evidence.
The first four tasks are otherwise entirely offline and can be resumed independently without
seeding, evaluating, activating, calling Google, importing bytes or spending model/provider money.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-document-content-creation/18-RESEARCH.md
@packages/backend/convex/vaultDrive.ts
@packages/backend/convex/llm.ts
@docs/playbooks/vault.md § 15.3-09

<interfaces>
**Everything this phase needs on the Drive side already exists and is live-verified.**
`vaultDrive.listDriveFolders` (`{parentId?}` → `{folders, files, truncated}`) shipped 2026-08-05 and
was driven against a real Google account: ten real root folders, real drill-down, real per-file
`readable` verdicts. It already returns `reauth` BEFORE `freshAccessToken` and before any network
call, and `dispatchGuard.test.ts` pins that ordering for every Drive action. **Do not write a second
Drive client.** The tool is a thin `ctx.runAction` over this.

**The root level is THREE lists** (`drives.list` + `'root' in parents` + `sharedWithMe`) — already
handled inside `listDriveFolders`. A tool that re-implements "list the root" gets this wrong.

⚠ **`GATED_SKILLS` contains `cockpit-agent`, and it has ONE candidate stream.** Editing
`cockpit-agent.md` mints a candidate at `maxVersion+1` that is invisible to the model until an eval
flips it active. **Phase 18-08 and Phase 20-12 both edit that same body**, so a concurrent edit
would mint a candidate carrying two lanes' prose and certify instructions nobody tested. This phase
is numbered 20.1 for exactly that reason: it runs AFTER both. As of this refresh, 20-12 itself first
clears an unseeded finance-bearing source body and then activates the media-bearing body. Read the
live active version/sha; do not treat a source commit or a completed checkbox as activation.
</interfaces>
</context>

<resume_order>
Tasks 1-4 are the offline implementation seam. Each must leave its targeted tests green and may be
committed independently, so an owner-gate pause never strands a mixed source/live mutation. Task 5
is the sole live boundary: body edit + owed fixture, candidate seed, paid gate, owner activation and
real-Drive no-import UAT. Task 6 is documentation after observed evidence exists.
</resume_order>

<tasks>

<task type="auto">
  <name>Task 1: findInDrive — the search the picker cannot do</name>
  <files>packages/backend/convex/vaultDrive.ts, packages/backend/convex/vaultDrive.test.ts</files>
  <action>
A `tenantAction` `findInDrive({ query: string })` returning `{ ok: true; hits: DriveHit[] }` or the
same refusal union `listDriveFolders` already returns. A hit is
`{ id, name, kind: "folder" | "file", parentName?, readable }`.

**This is the one genuinely new capability.** The picker walks a tree; "which folder has the Q3
numbers" is a search, and Drive already answers it: `q=name contains 'Q3' or fullText contains 'Q3'`
with `trashed=false`. **Both shared-drive params, through `driveUrl` — the existing helper, never an
inline URL** (the source scan in `dispatchGuard.test.ts` forbids one, and a param-less search
returns 200 with an empty list on a shared drive).

⚠ **THE QUERY IS USER TEXT GOING INTO DRIVE'S QUERY LANGUAGE.** `DRIVE_ID_RE` guards ids; it does
not guard this. A `'` in the search term closes the literal and rewrites the query. Escape it
(`replace(/'/g, "\\'")`) or reject it — **decide and test it**, because percent-encoding does
nothing here. This is the same trust boundary 15.3-09 found late.

`pageSize: 20` and no pagination: this answers a question in a conversation, not a file browser.
Mark the ceiling with `ponytail:`.

Tests: (a) the `q` sent contains both `name contains` and `trashed=false` — **assert on
`fetchSpy.mock.calls`, not on the stubbed response**, per the rule vault.md records; (b) a quote in
the query cannot alter the `q` structure; (c) a pre-widening grant returns `reauth` with `fetch`
never called.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultDrive.test.ts</automated>
  </verify>
  <done>Drive is searchable server-side, the query is escaped at the boundary, and the request is asserted.</done>
</task>

<task type="auto">
  <name>Task 2: the two tool keys, and the step-trace pair that must land together</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/schema.ts, apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
Registration Checklist (18-RESEARCH.md) rows **1, 2, 3**. Two tools, both READ-ONLY:

- `listDriveFolders({ folderId?: string })` → the folder/file split for that level.
- `findInDrive({ query: string })` → Task 1.

**Row 1 and row 2 in the SAME commit.** Append `v.literal("listDriveFolders")` and
`v.literal("findInDrive")` to `agentSteps.tool` (`schema.ts`), and add the matching `VERB` entries in
`cards.tsx` — `traceParity.test.ts` asserts set equality **both ways**, so either alone is RED. Do
NOT touch its `>= 22` floor; it is a non-vacuity floor, not a count.

⚠ **THE SILENT TRAP, verbatim from the checklist:** a step insert for a tool name that is not a
literal in that union throws INSIDE the AI-SDK's `onToolExecutionStart` and **the SDK swallows it** —
no trace row in production, every offline test green. This codebase has been bitten twice
(`searchVault`, `evaluateBusiness`). Row 1 is not paperwork.

**`execute` always returns a SENTENCE, never throws** (the 18-06 rule). The refusal union maps to
copy the model can relay: `reauth`/`refresh_failed` → "Your Google connection needs reconnecting —
it was made before Drive access existed." `not_connected` → connect. `bad_folder_id` → a plain
miss. A throw here becomes a dead turn.

**Return REFS AND NAMES TO THE MODEL, never bytes.** These tools return folder/file NAMES (the model
must name the folder) but never file content — reading a document is what the vault is for, and it
goes through ingest so it is governed, chunked and grounded.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/traceParity.test.ts convex/cockpitTools.test.ts</automated>
  </verify>
  <done>Both tools are keys in `buildCockpitTools`, both are step-traceable, and neither can throw.</done>
</task>

<task type="auto">
  <name>Task 3: prove the model cannot spend, and cannot import</name>
  <files>packages/backend/convex/dispatchGuard.test.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <action>
**THIS TASK IS THE POINT OF THE PHASE.** The read tools are easy; the guarantee is that adding them
did not open a paid path.

Static scan, the `llm.ts`-Approve-gate idiom: `llm.ts` holds **NO** reference to
`api.vaultDrive.importDriveFolder`, `internal.guardrails.reserveFolder`, `vaultDrive.openRun`,
`vaultDrive.exportOne` or `vaultDrive.landFile` — **not as a tool key, not inside any tool's
`execute`, and not via `scheduler.runAfter`**. Mutation: add
`ctx.runAction(api.vaultDrive.importDriveFolder, …)` inside `listDriveFolders`' execute → RED.

Non-vacuity FIRST: assert `llm.ts` DOES reference `api.vaultDrive.listDriveFolders`, or the absence
scan passes forever on a file that lost both.

Also pin: the Drive tools appear in the tool record but **not** in `SPECIALIST_TOOLS` — a dispatched
specialist gets no Drive reach (ADR-007 / ADR-012's structural rule).
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/dispatchGuard.test.ts</automated>
  </verify>
  <done>The model can read Drive and provably cannot import from it. Mutation run and RED.</done>
</task>

<task type="auto">
  <name>Task 4: the offline SMOKE ops (four sites, closed)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <action>
Registration Checklist row **16**. `apps/web/e2e/` runs with no gateway key, so without a SMOKE op
no e2e can ever exercise these tools. Add `drive=list:<folderId>` and `drive=find:<query>` at all
FOUR sites — `AgentSmokeOp`, `parseAgentSmoke`, `SMOKE_OP_TOOL`, `runAgentSmokeOp`.

`SMOKE_OP_TOOL` is a TOTAL `Record<AgentSmokeOp["kind"], StepTool>`, so a new op **cannot compile**
without naming its tool. Add the `parseAgentSmoke` round-trip case beside the existing ones.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/cockpitTools.test.ts</automated>
  </verify>
  <done>Both tools are drivable offline, so the e2e and the smoke can reach them without a key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: owner-gated body, candidate, paid eval, activation and real Drive UAT</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, packages/backend/scripts/eval-cases/39-drive-read.json, packages/backend/scripts/run-eval-golden.mjs</files>
  <action>Complete the offline body/fixture work, present the prerequisite fingerprint and cost estimate, wait for each named owner signal, then seed/evaluate/activate and run the real-Drive zero-ingest UAT exactly as described below.</action>
  <what-built>
Tasks 1-4 provide two read-only Drive tools, trace coverage, the structural no-import/no-spend proof
and offline SMOKE drivers. None of that makes the tools visible to the model; visibility requires a
new gated body version and owner activation.
  </what-built>
  <how-to-verify>
**Prerequisite read (no mutation):** prove 20-12's exact media-bearing version/sha is ACTIVE and no
candidate is parked. Stop if it is only committed, seeded, evaluated, or awaiting activation.

**Offline body + fixture work before approval:**
1. Add one Drive section: `findInDrive` answers "where is X"; `listDriveFolders` walks one level;
   import remains the user's click in the Vault Drive panel; the agent names the folder and never
   claims it imported; `reauth` relays the exact reconnect sentence.
2. Regenerate `cockpitAgent.ts` in the same commit and run the mirror drift test.
3. Add `39-drive-read.json`, using ordinary "which folder in my Drive has the Q3 numbers?" language.
   Add a closed `driveReadToolCount` observable derived from `agentSteps` for
   `findInDrive`/`listDriveFolders`, not reply prose. The fixture must require at least one Drive-read
   tool call, zero recipients/attachments and no applied plan. The eval tenant may return reconnect;
   the fixture proves selection and honest degradation, while real folder naming is the UAT below.
4. Add self-checks that reject zero as vacuous, fail on no/extra write tool calls, mutation-check the
   reader, and raise the fixture deletion floor by one from the post-20-12 value.
5. Run contracts/backend targeted tests, runner `--self-check`, playbook check and `git diff --check`.

**Approval boundary:** show the body diff, mirror proof, fixture self-check, active predecessor
version/sha and a cost estimate. Do not seed, run a paid eval, activate, or call real Drive until the
owner replies with a hard maximum and grants live access. A complete cockpit gate historically costs
about **$0.35 plus the new case**; any paid one-case falsification is charged inside the same maximum.

**After approval:** seed, read back the actual candidate version/sha, optionally falsify fixture 39
within the approved cap, then run the complete gate pinned to that exact candidate. A partial run is
diagnostic only and records no evidence. Record run id, full case count, pins, retries and actual
cost. Leave the candidate parked and present the evidence. The owner separately authorizes activation;
activate only that version, then read back the active version/sha and match it to the evidence pin.

**Real Drive UAT after activation (metadata only, zero ingest):**
1. Use a Google account already granted `drive.readonly` with a real non-sensitive folder/file whose
   name can be searched. Record the expected folder name before the turn.
2. Ask the active cockpit, in plain language, which Drive folder has that item. Confirm it calls a
   Drive-read tool, names the real folder/result and does not claim import or content ingestion.
3. Drill one level with a follow-up and confirm the real folder/file names match Google Drive.
4. Compare ingest-budget counters and vault rows before/after: delta must be zero, no
   `importDriveFolder`, reservation, export, land or ingest row may exist, and refs-only audit/trace
   payloads must contain no Drive name.
5. If a genuine pre-widening test token is available, confirm the reconnect sentence and zero Google
   fetches. Do not weaken or fabricate this observation if such a token no longer exists; retain the
   offline ordering proof and label the live case unavailable.
  </how-to-verify>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The exact Drive-bearing candidate is active and a real Drive lookup names the expected result with zero import, reservation or ingest-budget movement.</done>
  <resume-signal>First reply `proceed Drive gate, max $X`; after the green run reply `activate candidate <actual-version>`; after UAT reply `approved` with the real folder and zero-ingest evidence, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: playbooks</name>
  <files>docs/playbooks/vault.md, docs/playbooks/cockpit.md</files>
  <action>
`vault.md`: `findInDrive`, the query-escaping boundary, and why the agent has no import path.
`cockpit.md`: the two new tools, the "import is the user's click" rule, and the gated-body
serialization that made this phase 20.1. Bump both `Last verified` lines.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Both playbooks updated; hook exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm test`, `pnpm typecheck`, `pnpm lint` and `pnpm build` exit 0; no remembered type-error baseline is accepted.
- The complete golden run includes the Drive fixture and is pinned to the exact activated version/sha.
- **The live turn, which nothing offline can replace:** ask the cockpit "which folder in my Drive
  has the Q3 numbers", confirm it names a real folder, then confirm it does NOT claim to have
  imported it.
- A pre-widening tenant asking the same question gets the reconnect sentence, not a 403.
- `$0` of ingest budget moves during the whole phase.
</verification>

<open_questions>
1. **Should the agent be able to import at all, later?** Deliberately OUT here. If it is ever wanted,
   the shape is Calendar's, not a direct tool: a `drive_import` plan kind the model STAGES and the
   human approves, riding `executePlan`'s `armFor(actionTypeOf(plan.kind))` switch. That is a phase
   of its own — a new plan kind, a new card, a new arm — and it should not be smuggled in here.
2. **Creating a Drive folder from Pikar is NOT possible** and this phase does not move toward it:
   it needs `drive.file` or full `drive`, and full `drive` is a RESTRICTED scope requiring a CASA
   assessment — the same verification wall as the social integrations, which is blocked on the legal
   entity existing.
3. **Should `findInDrive` search file CONTENT (`fullText`) or only names?** Content search is one
   `q` clause and much more useful; it also means Drive matches on documents we may then quote a
   name for. Names-only is the conservative default — owner's call at planning time.
</open_questions>
