---
phase: 03.2.1
slug: agent-driven-cockpit
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-12
---

# Phase 03.2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (backend + core), Playwright (web E2E) |
| **Config file** | existing — `packages/backend` + `packages/core` vitest; `apps/web` playwright.config |
| **Quick run command** | `pnpm --filter @pikar/backend test` (or `--filter @pikar/core test`) |
| **Full suite command** | `pnpm test` + `pnpm --filter @pikar/web exec playwright test` |
| **Estimated runtime** | ~45–90 seconds |

---

## Sampling Rate

- **After every task commit:** Run the quick command for the touched package.
- **After every plan wave:** Run the full suite.
- **Before `/gsd:verify-work`:** Full suite green (E2E on the offline `SMOKE::` path).
- **Max feedback latency:** ~90 seconds

---

## Per-Task Verification Map

*Filled by the planner once tasks exist. Every recipient-mutating tool and every log surface is
sampled at 100% (send-adjacent, safety-critical) per the RESEARCH.md Validation Architecture.*

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | — | — | AGNT-01/02 | unit/integration/e2e | (planner fills) | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Mock LanguageModel harness for the `runCockpitAgent` loop integration test (AI SDK mock
      model returning scripted tool calls — no live gateway).
- [ ] Extend the `SMOKE::` sentinel grammar to drive a fixed tool sequence offline (E2E).
- [ ] Extend `llmRedaction` static scan for the reasoning-loop context (names+indices only,
      addresses never reach the model; refs-only logs).

*Existing vitest + Playwright infrastructure otherwise covers this phase.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A real conversational edit ("remove a recipient", "make it more formal") reasons correctly and nothing sends before Approve | AGNT-01/02 | Needs a live LLM reasoning call + real Gmail-connected session; the offline SMOKE path can't exercise real reasoning | Live backend + connected Gmail → type a request, then edit it conversationally → confirm the plan updates correctly and nothing sends before the Approve click |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
