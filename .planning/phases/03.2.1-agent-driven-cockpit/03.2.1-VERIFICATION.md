---
phase: 03.2.1-agent-driven-cockpit
verified: 2026-07-13T21:30:00Z
status: passed
score: 7/7 must-haves verified
---

# Phase 03.2.1: Agent-Driven Cockpit Verification Report

**Phase Goal:** Replace the deterministic `emailIntent` FSM cockpit with an Executive Agent
governed tool-loop (`generateText` + tools in `llm.ts`) so the conversation is flexible
("remove Bob", "make it more formal, add Jane") while every governance invariant survives —
human Approve gate stays a mutation (never a tool), redaction-before-draft, refs-only logs,
and structural facts are never model-invented (validated/resolved at the tool boundary). Clean
cutover (FSM deleted).
**Verified:** 2026-07-13
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `runCockpitAgent` is a real governed `generateText` tool-loop, driven by a thin `sendCockpitMessage` | ✓ VERIFIED | `llm.ts:720-762` — `preCall` gate → `getActiveSkill(COCKPIT_AGENT_SKILL)` as `system` → `runAgentLoop` (`generateText({model, system, prompt, tools, stopWhen: stepCountIs(8)})`) → `recordModelSpend`→`recordSpend`; `CHEAP_MODEL` fallback via `isFallbackEligible` (`llm.ts:640-646`). `cockpit.ts:45-86` `sendCockpitMessage` is a ~40-line driver: ensure thread+plan → save user turn → `ctx.runAction(internal.llm.runCockpitAgent, …)` in try/catch → save reply → return `{threadId}`. |
| 2 | The deterministic `emailIntent` FSM glue is DELETED — no dual engine | ✓ VERIFIED | `grep -n "parseAnswer|nextQuestion|applyAnswer|toIntentState|questionText|EmailIntentState|emptyIntent"` on both `cockpit.ts` and `emailIntent.ts` returns **zero matches**. `emailIntent.ts` (193 lines) now holds only `buildRecipientView`, `applyRecipientEdit`, `parseAddress`, `rankCandidates`, contact types. |
| 3 | `executePlan` is a `tenantMutation` (never an LLM tool); `workflow.start(deliverApprovedPlan)` has a single production call site | ✓ VERIFIED | `cockpit.ts:197` `export const executePlan = tenantMutation({…})`, CAS on `plan.status`, sole `workflow.start(ctx, internal.deliverApprovedPlan.deliverApprovedPlan, …)` call (`cockpit.ts:243`). `buildCockpitTools` (the LLM-reachable tool set) contains no `workflow.start`/`executePlan` reference. A second call site exists only in `smoke.ts` (`smoke:fanout`, a manual/dev-only test harness pre-dating this phase, not reachable from the agent loop or a normal user flow). |
| 4 | Governance invariants hold: redaction-before-draft, refs-only logs, structural facts from the plan ROW not model args, recipient integrity guards | ✓ VERIFIED | `draftBody` tool: `scanText(intent)` (fail-closed) runs before `ctx.runAction(internal.llm.draftCockpit, …)` (`llm.ts:526-541`); proved by `llmRedaction.test.ts` ("draftBody tool redacts … BEFORE any model call") and `cockpitTools.test.ts` ("draftBody redacts … BEFORE the drafting model call", asserting the raw address never lands in the draft). `proposePlan` reads `recipients`/`subject`/`mode`/`body` from `readPlan()` (the row), never from tool args (`llm.ts:543-566`), and refuses an incomplete plan (empty recipients/no subject/no body) — proved by `cockpitTools.test.ts`. `applyRecipientEdit` bounces an invalid address (never enters `recipients`) and bounces a `set` that would empty the list (`emailIntent.ts:68-92`) — proved by `emailIntent.test.ts` + `cockpitTools.test.ts`. Refs-only: `llmRedaction.test.ts` static-scans `cockpit.ts`/`plans.ts` for zero audit/DLQ/telemetry writes and asserts the `mailbox.searched` audit payload + the `workflow.start` context payload carry only refs (`queryHash`/`resultCount`/`planId`). |
| 5 | The reasoning loop is bounded and paused (not DLQ'd) under a governed stop | ✓ VERIFIED | `runCockpitAgent` gates on `internal.guardrails.preCall` BEFORE any reasoning call; a blocked result returns `{reply: PAUSED_REPLY, blocked: reason}` as data (`llm.ts:732-735`). `runCockpitAgent.test.ts` "kill-switch" test asserts `blocked:"kill_switch"`, a paused-copy reply, and **zero** `deadLetters` rows. `stopWhen: stepCountIs(8)` bounds the loop (`llm.ts:633`). |
| 6 | The `cockpit-agent` skill loads from the registry — no hardcoded prompt (§5) | ✓ VERIFIED | `packages/contracts/skills/cockpit-agent.md` (canonical) + `packages/contracts/src/skills/cockpitAgent.ts` (`cockpitAgentSkillBody`, AUTO-DERIVED) + `skills.ts` `seedSkills` row + `skills.test.ts` byte-identical drift assertion (11/11 green). `llmRedaction.test.ts` "llm.ts uses ONLY skill.body as the system prompt" passes (zero hardcoded `system: "…"` literals). `seedSkills` gained a publish-on-change path (edit → new immutable version + activate) exercised live to publish `cockpit-agent` v2 (panel-driven resolution). |
| 7 | A real conversational edit reasons correctly on the live backend and nothing sends before Approve (human-verify) | ✓ VERIFIED (human, already completed) | Plan 06 Task 2 checkpoint was run and APPROVED against the live Gmail-connected deployment (per `03.2.1-06-SUMMARY.md` + `STATE.md`), AFTER four gap-closure commits (`b635d19`, `429c862`, `9efa802`, `042f2cd`) that are confirmed present in `git log` and whose code is confirmed live in the current `llm.ts`/`emailIntent.ts`/`cards.tsx`/`cockpit-agent.md` (direct-OpenAI transport, name-match ranking + multi-select card, panel-driven resolution v2 skill + auto-continue, recipient-wipe guards). No outstanding human-verification action remains for this phase. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `packages/contracts/skills/cockpit-agent.md` + `.../cockpitAgent.ts` | Registry-loadable Executive-Agent prompt | ✓ VERIFIED | Byte-identical (LF-normalized) via `skills.test.ts`; v2 (panel-driven) published live |
| `packages/backend/convex/skills.ts` | `seedSkills` includes `COCKPIT_AGENT_SKILL` row + publish-on-change | ✓ VERIFIED | `skills.ts:108`, publish path `skills.ts:122-137` |
| `packages/core/src/emailIntent.ts` | Slimmed to pure survivors; `buildRecipientView`/`applyRecipientEdit` present, FSM gone | ✓ VERIFIED | 193 lines; grep confirms FSM symbols absent |
| `packages/backend/convex/llm.ts` | `buildCockpitTools`, `buildAgentContext`, `runCockpitAgent` — the governed tool-loop | ✓ VERIFIED | 921 lines; all three exported and wired per the truths above |
| `packages/backend/convex/cockpit.ts` | Thin driver; FSM deleted; `executePlan`/`resolveRecipients`/`proposeEmailPlan`/`listThreadMessages` intact | ✓ VERIFIED | 255 lines; `sendCockpitMessage` ~40 lines; send-safety spine unchanged |
| `packages/backend/convex/cockpitTools.test.ts` | Per-tool governance coverage | ✓ VERIFIED | 6/6 tests green (bounce, index substitution, redaction, refs-only, no-wipe, propose-refusal) |
| `packages/backend/convex/runCockpitAgent.test.ts` | Mock-model loop integration test | ✓ VERIFIED | 4/4 tests green (edit→proposed, remove-by-index, kill-switch no-DLQ, CHEAP_MODEL fallback) |
| `packages/backend/convex/llmRedaction.test.ts` | Static redaction/index-label scan extended | ✓ VERIFIED | 9/9 tests green, including the two 03.2.1-03 additions |
| `apps/web/e2e/cockpit-resolve.spec.ts` | Offline agent-path E2E (name→card→pick→edit→propose, nothing sent) | ✓ VERIFIED | Rewritten onto `SMOKE::agent::` grammar; asserts card state not prose; Playwright-discovered (live run is a stack-dependent manual step, consistent with prior phases) |
| `docs/playbooks/cockpit.md` | Rewritten conversation-engine section, invariants updated, Last verified → 03.2.1-06 | ✓ VERIFIED | Purpose/Data-flow/Invariants/How-to-change/How-to-verify all describe the agent tool-loop; every gap-closure commit (`b635d19`/`429c862`/`9efa802`/`042f2cd`) touched this file in the same commit (§9 compliance) |
| `docs/playbooks/watch.json` | `packages/contracts/skills/` sources covered | ✓ VERIFIED | Covered under `skill-registry.md` prefix `packages/contracts/skills/` |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `cockpit.ts sendCockpitMessage` | `internal.llm.runCockpitAgent` | `ctx.runAction` | ✓ WIRED | `cockpit.ts:67-73` |
| `cockpit.ts executePlan` | `workflow.start(deliverApprovedPlan)` | sole production call site | ✓ WIRED | `cockpit.ts:243-251`; only other call site is the pre-existing `smoke:fanout` test harness |
| `llm.ts buildCockpitTools` | `@pikar/core applyRecipientEdit`/`buildRecipientView` | recipient tools + `buildAgentContext` | ✓ WIRED | `llm.ts:391,418-427` |
| `llm.ts resolveContacts tool` | `internal.gmail.search` → `rankCandidates` → `internal.plans.writeCandidates` | tool `execute` | ✓ WIRED | `llm.ts:439-459` |
| `llm.ts draftBody tool` | `scanText` → `internal.llm.draftCockpit` | redact-then-draft | ✓ WIRED | `llm.ts:526-541`, verified before-order by `llmRedaction.test.ts` |
| `llm.ts runCockpitAgent` | `internal.guardrails.preCall` / `internal.guardrails.recordSpend` | gate before / spend after | ✓ WIRED | `llm.ts:733-735`, `recordModelSpend` at `llm.ts:600-608` called from `runAgentLoop:637` |
| `llm.ts runCockpitAgent` | `internal.skills.getActiveSkill(COCKPIT_AGENT_SKILL)` | system prompt | ✓ WIRED | `llm.ts:738-740` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| AGNT-01 | Plans 01, 02, 03, 04, 05, 06 | Executive Agent classifies/reasons and produces a plan | ✓ SATISFIED | `runCockpitAgent` is a real `generateText` tool-loop (not an inert message store); REQUIREMENTS.md traceability still lists AGNT-01 under Phase 2/Complete — per the phase's own framing this is an intentional reshape of an already-satisfied v1 requirement, not a new ID, so no traceability-table edit was expected. |
| AGNT-02 | Plans 01–06 | User sees the plan before approving; PLAN card + single Approve | ✓ SATISFIED | `proposePlan` tool → `proposeEmailPlan` → PLAN card; `executePlan` is the sole, human-gated Approve mutation; live-verified exactly one Approve, nothing sent before it. |

No orphaned requirements: REQUIREMENTS.md's traceability table has no additional IDs mapped to Phase 3.2.1 beyond what the plans declare.

### Anti-Patterns Found

None blocking. No `TODO`/`FIXME`/placeholder stubs, no empty tool `execute` bodies, no console.log-only handlers found in the touched surface (`llm.ts`, `cockpit.ts`, `emailIntent.ts`). One minor documentation staleness (not a code gap): the `docs/playbooks/cockpit.md` header narrative line still reads "Re-verification pending" from an earlier commit even though the human-verify was subsequently approved and every gap-closure commit thereafter updated the playbook body sections — cosmetic only, does not affect any invariant or the Stop hook.

### Human Verification Required

None outstanding. The phase's single human-verify checkpoint (Plan 06, Task 2) already ran against the live Gmail-connected backend and was APPROVED after the gap-closure commits (`b635d19`, `429c862`, `9efa802`, `042f2cd`), whose code is confirmed present and correct in the current source tree by this verification.

### Gaps Summary

No gaps found. All 7 derived observable truths verified against the actual codebase (not just SUMMARY claims): the FSM is structurally gone (grep-confirmed zero matches), the tool-loop is a real `generateText` loop wrapped by the exact governance rails (`preCall`/`stepCountIs(8)`/`recordSpend`/`CHEAP_MODEL` fallback), every governed tool preserves its primitive's governance (bounce-at-boundary, redact-before-draft, structural-facts-from-row), the send-safety spine (`executePlan`, sole `workflow.start`) is untouched, the skill loads from the registry with no hardcoded prompt, and the playbook + watch.json were updated in lockstep with every code change per §9. Full backend test suite: 79/80 passing, with the single failure being the pre-existing, already-documented (`deferred-items.md`, since Phase 2) `audit.test.ts` "auditCounts not registered" issue — unrelated to this phase's files and not a regression. Core suite: 48/48 green. Backend typecheck clean except the same 7 pre-existing `import.meta.glob` test-file errors documented across all six plan SUMMARYs.

---

*Verified: 2026-07-13*
*Verifier: Claude (gsd-verifier)*
