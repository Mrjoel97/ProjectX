---
phase: 03.2.1-agent-driven-cockpit
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - docs/playbooks/cockpit.md
  - docs/playbooks/watch.json
autonomous: false
requirements: [AGNT-01, AGNT-02]
must_haves:
  truths:
    - "The cockpit playbook's conversation-engine section describes the agent tool-loop, not the FSM"
    - "Every new watched path (cockpit-agent skill) is registered under a playbook or acknowledged (§9 Stop hook clears)"
    - "A real conversational edit reasons correctly on the live backend and nothing sends before Approve (human-verified)"
  artifacts:
    - path: "docs/playbooks/cockpit.md"
      provides: "Rewritten conversation-engine section + invariants for the agent tool-loop; Last verified → 03.2.1-06"
      contains: "runCockpitAgent"
  key_links:
    - from: "docs/playbooks/watch.json"
      to: "the cockpit-agent skill sources + new node tools"
      via: "watched-path registration (or _unassigned ack)"
      pattern: "cockpit"
---

<objective>
Close the phase: rewrite the cockpit playbook for the agent tool-loop (§9), reconcile
`watch.json` for any new watched paths, and run the human-verify checkpoint — a REAL
conversational edit on the live backend, confirming the agent reasons correctly and nothing
sends before Approve (AGNT-01/AGNT-02).

Purpose: definition-of-done. The FSM→agent swap touches watched subsystems, so the playbook
must be updated in the same phase (CLAUDE.md §9), and the only truth the offline suite cannot
prove — real model reasoning over a live Gmail-connected session — needs a human.
Output: updated playbook + watch.json + a recorded human-verify sign-off.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.2.1-agent-driven-cockpit/03.2.1-CONTEXT.md
@.planning/design/agent-driven-cockpit.md
@docs/playbooks/cockpit.md
@docs/playbooks/watch.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the cockpit playbook for the agent tool-loop + reconcile watch.json</name>
  <files>docs/playbooks/cockpit.md, docs/playbooks/watch.json</files>
  <action>
    Rewrite the "conversation engine" content in `docs/playbooks/cockpit.md`: the engine is now
    an Executive Agent `generateText` tool-loop (`runCockpitAgent` in llm.ts) driven by
    `sendCockpitMessage`, NOT the deterministic emailIntent FSM. Update Key files (add
    runCockpitAgent / buildCockpitTools / buildAgentContext / cockpit-agent skill; note the FSM
    is retired, only pure validators survive), Data flow (user turn → preCall → tool-loop →
    tools mutate the plans row → assistant reply → proposePlan → human Approve), and Invariants:
    (a) §2-D — the model sees recipients as index+label only, addresses substituted server-side;
    (b) executePlan stays a tenantMutation + sole workflow.start (never a tool); (c) structural
    facts validated/resolved at the tool boundary; (d) preCall/recordSpend/maxSteps govern the
    loop; (e) refs-only logs hold. Remove any FSM-specific invariants that no longer exist. Bump
    `Last verified` to 03.2.1-06. In `watch.json`, ensure the new cockpit-agent skill sources
    (`packages/contracts/skills/cockpit-agent.md` + `packages/contracts/src/skills/cockpitAgent.ts`)
    are covered — register them under the skill-registry playbook if that is their natural home,
    else acknowledge under `_unassigned`; new node test files need no watch. Verify no new
    uncovered `packages/`/`apps/` source file trips the §9 Stop hook.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>cockpit.md describes the agent tool-loop with updated invariants + Last verified 03.2.1-06; watch.json covers all new watched paths; the playbook Stop-hook check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — a real conversational edit reasons correctly, nothing sends before Approve</name>
  <what-built>
    The cockpit is now an Executive Agent governed tool-loop: any reasonable conversational edit
    ("remove a recipient", "make it more formal", "add Jane") is reasoned by the model calling
    governed tools; the Approve gate, resolution card, and cards are unchanged.
  </what-built>
  <how-to-verify>
    Pre-req: live backend (`convex dev` NOT --once + `next dev` :3111), skills seeded
    (`seedSkills` includes cockpit-agent + email-drafter), a Gmail-connected test session, and
    `AI_GATEWAY_API_KEY` set on the deployment (real reasoning needs the gateway).
    1. Visit `/dashboard/workspace`. Type a request naming a real correspondent by name and a goal
       (e.g. "email Sarah and Bob about the Q3 update").
    2. Confirm the ResolutionCard surfaces real contacts; pick one — it folds into the plan.
    3. Now EDIT conversationally: "actually remove Bob" (or "make it more formal, and change the
       subject to Quarterly Review"). Confirm the PLAN card updates correctly to match — the
       right recipient removed / subject changed / tone reflected — WITHOUT re-walking a rigid
       slot-fill script.
    4. Confirm exactly ONE Approve button and that NOTHING has been sent (no REPORT card, no
       delivery) at any point before you click Approve.
    5. (Optional) Toggle the kill-switch or drain the daily budget and confirm a turn pauses
       conversationally rather than erroring/dead-lettering.
  </how-to-verify>
  <action>
    Present the verification steps below to the user against the live backend and pause for their
    sign-off. This is the single Manual-Only verification for the phase — real model reasoning
    over a Gmail-connected session cannot be exercised on the offline SMOKE path.
  </action>
  <verify>
    <automated>MANUAL — human-verify checkpoint (live reasoning; no offline automation possible)</automated>
  </verify>
  <done>User confirms a real conversational edit updated the plan correctly and nothing sent before Approve.</done>
  <resume-signal>Type "approved" if the edit reasoned correctly and nothing sent before Approve; else describe what misbehaved.</resume-signal>
</task>

</tasks>

<verification>
- `node scripts/check-playbooks.mjs` passes (no unblessed watched-path change, no uncovered source).
- Human confirms: a real conversational edit updates the plan correctly and zero sends occur before Approve.
</verification>

<success_criteria>
AGNT-01/AGNT-02 delivered: the cockpit reasons over conversational edits via the governed
tool-loop, the playbook documents the new engine + invariants, and a human has verified real
reasoning with the zero-sends-before-Approve invariant intact.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2.1-agent-driven-cockpit/03.2.1-06-SUMMARY.md`
</output>
