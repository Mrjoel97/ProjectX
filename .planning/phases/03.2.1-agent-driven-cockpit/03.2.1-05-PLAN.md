---
phase: 03.2.1-agent-driven-cockpit
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/core/src/emailIntent.ts
  - packages/core/src/index.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/core/src/emailIntent.test.ts
  - apps/web/e2e/cockpit-resolve.spec.ts
autonomous: true
requirements: [AGNT-01, AGNT-02]
must_haves:
  truths:
    - "sendCockpitMessage is a thin driver: save user turn → runCockpitAgent → save reply → return threadId"
    - "The emailIntent FSM (applyAnswer/nextQuestion/parseAnswer/questionText/advance/inline resolve-turn) is DELETED — no dual engine"
    - "The surviving pure validators (isValidEmail, parseAddress, rankCandidates, buildRecipientView, applyRecipientEdit) remain"
    - "executePlan stays a tenantMutation and the SOLE workflow.start site (zero sends before Approve) — unchanged"
    - "The offline SMOKE:: E2E drives name → resolution card → edit → propose with nothing sent"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "Thin agent driver; FSM glue removed; resolveRecipients/proposeEmailPlan/executePlan/listThreadMessages intact"
      contains: "runCockpitAgent"
    - path: "packages/core/src/emailIntent.ts"
      provides: "Slimmed to surviving pure helpers only"
      contains: "rankCandidates"
    - path: "apps/web/e2e/cockpit-resolve.spec.ts"
      provides: "Extended offline agent-path E2E (name → card → edit → propose, nothing sent)"
      contains: "REPORT"
  key_links:
    - from: "packages/backend/convex/cockpit.ts sendCockpitMessage"
      to: "internal.llm.runCockpitAgent"
      via: "the driver invokes the tool-loop"
      pattern: "runCockpitAgent"
    - from: "packages/backend/convex/cockpit.ts executePlan"
      to: "workflow.start(deliverApprovedPlan)"
      via: "sole send site, NOT a tool (unchanged)"
      pattern: "workflow\\.start"
---

<objective>
Clean cutover: `sendCockpitMessage` becomes the thin driver over `runCockpitAgent`, the
deterministic FSM is DELETED (no dual engine), `emailIntent` is slimmed to the surviving pure
helpers, and the offline `SMOKE::` E2E is extended to exercise the agent path. The Approve gate
(`executePlan`), the resolution card pick (`resolveRecipients`), `proposeEmailPlan`, and
`listThreadMessages` are UNCHANGED — the send-safety spine is untouched.

Purpose: this is the rollout. The FSM cannot be deleted until the loop replaces it (dependency
order), and the Approve gate contains the blast radius so a clean cutover is safe. After this
plan the cockpit reasons; before it, it only slot-filled.
Output: the driver + FSM deletion + slimmed core + extended E2E.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.2.1-agent-driven-cockpit/03.2.1-CONTEXT.md
@.planning/design/agent-driven-cockpit.md
@packages/backend/convex/cockpit.ts
@packages/core/src/emailIntent.ts
@apps/web/e2e/cockpit-resolve.spec.ts

<interfaces>
```typescript
// The driver calls:
internal.llm.runCockpitAgent({ tenantId, threadId, planId, text }): { reply, blocked? }
// KEEP UNCHANGED in cockpit.ts: cockpitAgent (message store), resolveRecipients (card pick),
// proposeEmailPlan, executePlan (the human Approve mutation + sole workflow.start), listThreadMessages.

// DELETE from cockpit.ts: parseAnswer, toIntentState, questionText, advance, the inline
// resolve-turn block inside sendCockpitMessage, and the FSM imports (applyAnswer/nextQuestion).
// DELETE from packages/core/src/emailIntent.ts: EmailIntentState, Answer, NextQuestion,
// ApplyResult, emptyIntent, nextQuestion, applyAnswer, setOrDrop, dedupe* (if now unused).
// KEEP in emailIntent.ts: isValidEmail (via re-export), parseAddress, rankCandidates,
// ContactMatch/NameCandidates/HeaderRecord, buildRecipientView, applyRecipientEdit.
```
NOTE (§9 playbook): cockpit.ts, llm.ts, emailIntent.ts are all watched under docs/playbooks/cockpit.md.
The playbook rewrite + Last-verified bump is Plan 06 (this plan changes code; Plan 06 blesses it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sendCockpitMessage → thin driver; delete the FSM glue in cockpit.ts</name>
  <files>packages/backend/convex/cockpit.ts</files>
  <action>
    Rewrite `sendCockpitMessage` (keep its tenantAction signature + explicit
    `Promise<{ threadId: string }>` return, §96): (1) ensure thread + plans row on first turn
    (unchanged); (2) save the user's turn to the agent thread; (3) call
    `internal.llm.runCockpitAgent({ tenantId, threadId, planId, text })` inside try/catch;
    (4) save the returned `reply` as the assistant turn (on a caught exception, save a
    non-dead-ending assistant error turn — nothing sent, the partial plan row stays valid);
    (5) return `{ threadId }`. A `blocked` result is surfaced as the paused assistant reply.
    Delete `parseAnswer`, `toIntentState`, `questionText`, `advance`, the inline resolve-turn
    block, and the now-unused `@pikar/core` FSM imports (`applyAnswer`, `nextQuestion`, `Answer`,
    `NextQuestion`, `EmailIntentState`). Keep `resolveRecipients` working: it still folds a card
    pick — re-point it to patch recipients + greetingName + clearCandidates directly (reuse
    `applyRecipientEdit`/plain patch instead of the deleted `applyAnswer(resolution)`; the pick
    addresses are already valid). Leave `proposeEmailPlan`, `executePlan`, `listThreadMessages`
    byte-for-byte unchanged. ponytail: fewest lines — the driver is ~15 lines; delete aggressively.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec tsc -p convex --noEmit</automated>
  </verify>
  <done>cockpit.ts drives runCockpitAgent; FSM glue gone; executePlan/resolveRecipients/proposeEmailPlan/listThreadMessages intact; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Slim emailIntent to survivors; update the backend + core tests</name>
  <files>packages/core/src/emailIntent.ts, packages/core/src/index.ts, packages/backend/convex/cockpit.test.ts, packages/core/src/emailIntent.test.ts</files>
  <action>
    Delete the retired FSM from `packages/core/src/emailIntent.ts` (`EmailIntentState`, `Answer`,
    `NextQuestion`, `ApplyResult`, `emptyIntent`, `nextQuestion`, `applyAnswer`, `setOrDrop`,
    `RecipientMode`, and any dedupe helper left unused after the delete). Keep `isValidEmail`
    re-export, `parseAddress`, `rankCandidates`, the contact types, and Plan 02's
    `buildRecipientView`/`applyRecipientEdit`. Update `packages/core/src/index.ts` to drop the
    removed exports. In `packages/core/src/emailIntent.test.ts` delete the FSM-specific tests
    (nextQuestion/applyAnswer/resolution-fold) and keep the survivor tests (parseAddress,
    rankCandidates, buildRecipientView, applyRecipientEdit). In
    `packages/backend/convex/cockpit.test.ts` delete the `parseAnswer` / `toIntentState` /
    resolve-fold describe blocks (they test deleted code) and KEEP the `executePlan approve-gate`
    tests verbatim (SC4 idempotency / zero-sends / tenant-guard / no-mailbox — unchanged safety).
    ponytail: deletion over addition — if a test asserted deleted behavior, remove it, don't rewrite.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test && pnpm --filter @pikar/backend exec vitest run convex/cockpit.test.ts</automated>
  </verify>
  <done>Core + cockpit tests green with the FSM gone; executePlan gate tests preserved; no dangling import of a deleted symbol.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the offline SMOKE:: E2E to the agent path (edit → propose, nothing sent)</name>
  <files>apps/web/e2e/cockpit-resolve.spec.ts</files>
  <action>
    Update `cockpit-resolve.spec.ts` to drive the agent-driven flow over the `SMOKE::` scripted
    tool sequence (Plan 04): a message that resolves a NAME → the ResolutionCard renders (chips +
    hints, unchanged card) → pick a contact → a conversational EDIT (e.g. change subject or add a
    recipient) → the PLAN card shows the updated state with exactly ONE Approve. Assert the
    READING/agent invariant: NOTHING is sent — do NOT click Approve, and no REPORT card appears.
    Keep the card selectors that still hold; adjust the guided-question assertions that assumed
    the deterministic FSM copy (the agent's replies differ — assert on the CARD state, which is
    stable, not on exact agent prose). ponytail: assert plan-row-derived UI (cards), not model wording.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test cockpit-resolve.spec.ts --reporter=line</automated>
  </verify>
  <done>The offline agent-path E2E passes: name → card → pick → edit → proposed PLAN, one Approve, no REPORT, nothing sent.</done>
</task>

</tasks>

<verification>
- `pnpm test` green (core + backend); `pnpm --filter @pikar/web exec playwright test cockpit-resolve.spec.ts` green.
- Grep confirms the FSM symbols (`applyAnswer`, `nextQuestion`, `parseAnswer`, `questionText`) are GONE from cockpit.ts + emailIntent.ts (no dual engine).
- Grep confirms `workflow.start(` still appears exactly once, in executePlan (zero sends before Approve invariant intact).
</verification>

<success_criteria>
The cockpit conversation engine is the agent tool-loop end to end: sendCockpitMessage drives
runCockpitAgent, the FSM is deleted, only the pure validators survive, the send-safety spine
(executePlan / resolution card) is unchanged, and the offline E2E proves an edit→propose flow
sends nothing.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2.1-agent-driven-cockpit/03.2.1-05-SUMMARY.md`
</output>
