---
phase: 03.2.1-agent-driven-cockpit
plan: 06
subsystem: ui
tags: [cockpit, executive-agent, tool-loop, playbook, generateText, openai, skills-registry]

# Dependency graph
requires:
  - phase: 03.2.1-05
    provides: "sendCockpitMessage rewired as the thin driver over runCockpitAgent; FSM deleted; E2E on the SMOKE::agent:: grammar"
  - phase: 03.2.1-04
    provides: "runCockpitAgent — the governed generateText tool-loop engine"
provides:
  - "Cockpit playbook rewritten for the agent tool-loop (§9 definition-of-done); Last verified → 03.2.1-06"
  - "Human-verified: a real multi-turn conversational edit reasons correctly on the live Gmail-connected backend, zero sends before Approve (AGNT-01/AGNT-02)"
  - "Live-hardened agent cockpit (gap-closure): pick auto-continue, direct-OpenAI transport, name-match ranking + multi-select card, panel-driven resolution (skill v2), recipient-wipe guards"
affects: [verify-work, phase-3.3-attachment-generation, phase-3.4-personalization, cockpit, skill-registry]

# Tech tracking
tech-stack:
  added: ["@ai-sdk/openai@4.0.11 (direct OpenAI transport, replaces Vercel AI Gateway for the cockpit loop)"]
  patterns:
    - "Panel-driven contact resolution: the agent stops after resolveContacts and never sets a resolved contact itself — the human picks in the ResolutionCard, the pick re-enters the tool-loop (auto-continue)"
    - "seedSkills publish-on-change: an edited skill body mints a new immutable version + activates it (rollback = re-activate a prior version), not a code edit"
    - "Structural-integrity guards at the tool boundary: applyRecipientEdit bounces a set that would empty recipients; proposePlan refuses a zero-recipient/incomplete plan"

key-files:
  created:
    - ".planning/phases/03.2.1-agent-driven-cockpit/03.2.1-06-SUMMARY.md"
  modified:
    - "docs/playbooks/cockpit.md (conversation-engine rewrite: FSM → agent tool-loop; invariants; Last verified → 03.2.1-06)"

key-decisions:
  - "LLM transport for the cockpit loop reversed from the Vercel AI Gateway to DIRECT OpenAI (@ai-sdk/openai@4.0.11, OPENAI_API_KEY): gateway BYOK is gated behind paid Vercel credits (double charge) and the free tier rate-limited the agent loop"
  - "Contact resolution is PANEL-DRIVEN: the agent stops after resolveContacts (cockpit-agent prompt v2); the human's card pick re-enters the tool-loop rather than the model choosing an address"
  - "seedSkills gained a publish-on-change path (edited body → new immutable version + activate); cockpit-agent published to v2 on the deployment"
  - "Recipients can no longer be silently wiped: applyRecipientEdit bounces an emptying set and proposePlan refuses a zero-recipient plan"

patterns-established:
  - "§9 definition-of-done: a phase that swaps a watched subsystem's engine rewrites its playbook + bumps Last verified in the same phase"
  - "Human-verify is the only proof for live model reasoning over a Gmail-connected session — the offline SMOKE suite cannot exercise it"

requirements-completed: [AGNT-01, AGNT-02]

# Metrics
duration: 45min
completed: 2026-07-13
---

# Phase 03.2.1 Plan 06: Phase Close — Cockpit Playbook Rewrite + Live Human-Verify Summary

**Rewrote the cockpit playbook for the Executive-Agent tool-loop (retiring the FSM narrative) and human-verified a real multi-turn conversational edit on the live Gmail-connected backend — resolve-by-name → multi-select pick → agent auto-continue → PLAN → one Approve, nothing sent before it (AGNT-01/AGNT-02).**

## Performance

- **Duration:** ~45 min (incl. live gap-closure during verification)
- **Started:** 2026-07-13
- **Completed:** 2026-07-13
- **Tasks:** 2 (1 auto, 1 human-verify checkpoint — APPROVED)
- **Files modified:** 1 doc (playbook) + gap-closure code committed separately during verification

## Accomplishments

- **Playbook rewrite (§9 definition-of-done):** `docs/playbooks/cockpit.md` now documents `runCockpitAgent` — the governed `generateText` tool-loop — as the LIVE conversation engine. Purpose, Data flow, Invariants, "How to change safely", and "How to verify" were rewritten off the FSM narrative. Removed the now-false invariants ("deterministic control / the LLM is invoked exactly once" and "DECISION #2 — no LLM tool-loop") and replaced them with the governed-loop invariant (`preCall`/`recordSpend`/`stepCountIs(8)` bound the loop) + structural-facts-at-the-tool-boundary + the §2-D index/label rule (no raw address reaches the model). `Last verified` → 03.2.1-06.
- **watch.json needed no edit:** the new `cockpit-agent` skill sources (`packages/contracts/skills/cockpit-agent.md`, `packages/contracts/src/skills/cockpitAgent.ts`) are already covered by the `skill-registry.md` watch prefixes; `node scripts/check-playbooks.mjs` passes (the §9 Stop hook clears).
- **Human-verify APPROVED (live):** the user confirmed end-to-end on the live Gmail-connected deployment — resolve a name → ResolutionCard multi-select pick → agent auto-continues → subject → body → PLAN with the picked recipients shown → exactly one Approve, nothing sent before it. This is the single truth the offline SMOKE suite cannot prove.

## Task Commits

1. **Task 1: Rewrite cockpit playbook for the agent tool-loop + reconcile watch.json** — `468e250` (docs)
2. **Task 2: Human-verify — real conversational edit reasons correctly, nothing sends before Approve** — APPROVED (no commit; verification task)

**Plan metadata:** committed with this SUMMARY + STATE + ROADMAP + REQUIREMENTS.

## Gap-closure during verification (live-only defects)

The human-verify surfaced several defects the offline suite could not catch. These were fixed and committed DURING verification (all on `main`, after Task-1 `468e250`); the human-verify passed AFTER them:

1. **`b635d19`** — a card pick now re-enters the tool-loop (auto-continue; was a dead-end/blank workspace), AND the LLM transport was switched from the Vercel AI Gateway to DIRECT OpenAI (`@ai-sdk/openai@4.0.11`, `OPENAI_API_KEY`) — gateway BYOK is gated behind paid Vercel credits (double charge) and the free tier rate-limited the agent loop.
2. **`429c862`** — `rankCandidates` now ranks/filters by name-match (was frequency-only noise); the ResolutionCard is multi-select.
3. **`9efa802`** — `cockpit-agent` skill prompt v2: contact resolution is PANEL-DRIVEN (the agent stops after `resolveContacts`, never sets a resolved contact itself); `seedSkills` gained a publish-on-change path (edited body → new immutable version + activate). cockpit-agent published to v2 on the deployment.
4. **`042f2cd`** — recipients can no longer be silently wiped (`applyRecipientEdit` bounces a "set" that would empty the list) and `proposePlan` refuses a zero-recipient/incomplete plan.
5. **Runtime (not a commit):** `skills:seedSkills` was run on the live deployment (cockpit-agent had been unseeded → `NO_ACTIVE_SKILL`).

_This bookkeeping close-out did not re-run or re-commit any of that code — it was already committed and deployed._

## Files Created/Modified

- `docs/playbooks/cockpit.md` — conversation-engine section rewritten for the agent tool-loop; invariants updated; Last verified → 03.2.1-06
- `.planning/phases/03.2.1-agent-driven-cockpit/03.2.1-06-SUMMARY.md` — this summary

## Decisions Made

See `key-decisions` frontmatter — the four gap-closure decisions (direct-OpenAI transport reversal, panel-driven resolution, seedSkills publish path, recipient-wipe guards) are the accumulated context for this plan.

## Deviations from Plan

Task 1 executed exactly as written (playbook rewrite + watch reconcile; watch.json required no edit because the skill prefixes already cover the new sources). Task 2's approval came AFTER live gap-closure fixes that were made and committed by the coordinator's session during verification — those are documented above, not re-done here.

## Issues Encountered

The human-verify surfaced live-only defects (blank workspace on pick, frequency-only contact noise, agent self-resolving contacts, unseeded skill → `NO_ACTIVE_SKILL`, gateway rate-limiting/double-charge, silent recipient wipe). All were resolved via the gap-closure commits above, and verification then passed.

## Next Phase Readiness

- Phase 3.2.1 (Agent-Driven Cockpit) is code-complete and live-verified across all 6 plans; the orchestrator owns `verify_phase_goal` + marking the phase complete.
- AGNT-01/AGNT-02 satisfied: the cockpit is a real governed Executive-Agent tool-loop, live-verified with the zero-sends-before-Approve invariant intact.
- Carry-forward (deferred, logged): `cockpit-report.spec.ts` still drives the retired FSM guided flow — needs migration to the SMOKE::agent:: grammar (from Plan 05's deferred items).

## Self-Check: PASSED

- FOUND: `.planning/phases/03.2.1-agent-driven-cockpit/03.2.1-06-SUMMARY.md`
- FOUND: `docs/playbooks/cockpit.md` (Last verified → 03.2.1-06; `check-playbooks.mjs` passes)
- FOUND commits: 468e250 (Task 1), b635d19 / 429c862 / 9efa802 / 042f2cd (gap-closure, already deployed)

---
*Phase: 03.2.1-agent-driven-cockpit*
*Completed: 2026-07-13*
