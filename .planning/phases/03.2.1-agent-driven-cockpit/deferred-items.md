# Deferred Items — Phase 03.2.1

- **audit.test.ts red (pre-existing, NOT caused by 03.2.1-03):** `audit.log inserts exactly one row that round-trips` throws "Component auditCounts is not registered" under convex-test. Documented since the Phase-2 aggregate. Fix is `t.registerComponent("auditCounts", schema, glob)` (the exact pattern cockpitTools.test.ts now uses). Out of scope here (files_modified excludes audit.test.ts). Adopt the registration in audit.test.ts to green it. — logged 03.2.1-03

- **cockpit-report.spec.ts drives the RETIRED FSM guided flow (out of 03.2.1-05 scope):** Plan 05 deleted the deterministic FSM, so the offline `cockpit-report.spec.ts` — which types plain recipients/subject/body messages and expects the FSM's deterministic questions — no longer progresses offline (a non-`SMOKE::agent::` message now falls through to a gateway-less generateText). Plan 05's files_modified scopes the E2E rewrite to `cockpit-resolve.spec.ts` ONLY, so this send-path spec is left for a follow-up: migrate its slot-fill turns to the `SMOKE::agent::add=…/subject=…/body=…/mode=…/propose` grammar (then Approve → REPORT), mirroring the new cockpit-resolve spec. The backend send-safety spine (executePlan gate, fan-out) is unchanged and still covered by cockpit.test.ts + smoke:fanout. — logged 03.2.1-05
