---
phase: 12-business-evaluation-engine
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - packages/backend/convex/llm.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [BEVL-01]
must_haves:
  truths:
    - "The user can ask 'evaluate my business' in the cockpit and the agent runs the read-only tool"
    - "The result renders as an EVALUATION card with per-finding citation + H/M/L confidence chip"
    - "The card shows a distinct 'not enough data to assess' state, separate from a real gap"
    - "A healthy business renders an affirmative 'no gaps found' card"
    - "The agent can store a user-supplied figure via a recordScorecardAnswer tool (cited user-provided, not plan-gated)"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "evaluateBusiness read-tool + recordScorecardAnswer write-tool in buildCockpitTools + SMOKE_OP_TOOL entry"
      contains: "evaluateBusiness"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "EVALUATION card subscribing to api.evaluations.byThread"
      contains: "evaluations.byThread"
  key_links:
    - from: "packages/backend/convex/llm.ts"
      to: "internal.evaluations.runEvaluation"
      via: "the evaluateBusiness tool execute body (mirror searchVault)"
      pattern: "runEvaluation"
    - from: "packages/backend/convex/llm.ts"
      to: "internal.evaluations.recordScorecardAnswer"
      via: "recordScorecardAnswer write-tool in buildCockpitTools (stores a user-supplied figure)"
      pattern: "recordScorecardAnswer"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "api.evaluations.byThread"
      via: "useQuery reactivity (mirror api.plans.byThread)"
      pattern: "evaluations.byThread"
---

<objective>
Surface the engine: register the `evaluateBusiness` read-only cockpit tool (mirroring searchVault
verbatim, shape-1 of the two-shapes rule) and render the EVALUATION card. This makes BEVL-01
user-visible — an on-demand, cited, confidence-labelled, honest-about-thin-data assessment.

Purpose: the invocation surface (cockpit command only) + the result surface (a new card).
Output: evaluateBusiness tool + EVALUATION card, human-verified visually.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-business-evaluation-engine/12-RESEARCH.md
@docs/design/BRAND.md

# The exact tool template to COPY + the card render seam:
@packages/backend/convex/llm.ts
@apps/web/app/(app)/dashboard/workspace/cards.tsx

<interfaces>
<!-- searchVault (verified llm.ts:1300) is the shape to mirror: validated args → readPlan() cross-tenant
     guard → internal.* action → refs-only audit (done inside runEvaluation) → capped fenced string.
     evaluateBusiness differs only: arg is a CLOSED framework enum; body calls runEvaluation. -->
evaluateBusiness: tool({
  description: /* split literals, each <200 chars (§5 scan ceiling, Pitfall 6) */,
  inputSchema: jsonSchema<{ framework?: "swot" | "lean" | "bmc" | "growth-os" }>({ /* optional */ }),
  execute: async ({ framework }): Promise<string> => { /* readPlan → runEvaluation → capped synopsis */ },
});

<!-- SMOKE_OP_TOOL (verified llm.ts:1767) is a TOTAL Record over AgentSmokeOp["kind"] → StepTool;
     add the "evaluateBusiness" mapping there too (Pitfall 2). -->

<!-- From plan 03: api.evaluations.byThread returns the latest row {framework, findings[], gaps[],
     notEnoughData[], verdict, createdAt}. Card type mirrors cards.tsx:37 (Plan = NonNullable<...>). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the evaluateBusiness read-tool</name>
  <files>packages/backend/convex/llm.ts</files>
  <action>
In buildCockpitTools (llm.ts:607), add `evaluateBusiness` alongside searchVault, copying its shape:
validated `jsonSchema` args (optional CLOSED framework enum — absent = auto-pick, so a model can't inject
prose, the setMode precedent), `const plan = await readPlan();` cross-tenant guard, then
`ctx.runAction(internal.evaluations.runEvaluation, { tenantId, threadId, framework })`. FAIL OPEN — a
hiccup returns an honest "I couldn't assess" string, never throws out of the loop (SC1). Return a CAPPED
synopsis into the loop ("Assessed on [framework]: N findings, M gaps — shown as an evaluation card"), NOT
the full findings prose (the card carries that); wrap any echoed grounded text in the same
`<vault_context>` fence searchVault uses. Split the tool `description` across concatenated literals each
<200 chars (§5, Pitfall 6). Add the `evaluateBusiness` entry to the SMOKE_OP_TOOL record (Pitfall 2).
The tool NEVER proposes or sends (two-shapes rule — acting is plan 05).
Also register `recordScorecardAnswer` in the SAME buildCockpitTools call — a small validated WRITE tool
(args `{ field: string, value: string }`) that calls internal.evaluations.recordScorecardAnswer
(tenant-scoped), emits a refs-only audit, and returns a short confirmation string into the loop. It is a
DIRECT scorecard write (the user handing over their own figure, cited "user-provided") — NOT plan-gated,
it does not cross the Approve gate. It is a quiet write: no agentStep, so NO agentSteps.tool-union /
SMOKE_OP_TOOL entry is needed for it (only evaluateBusiness emits a step). This is the "store" half of the
LOCKED vault-first→ask→store loop reaching the LLM loop; the engine carry-forward (plan 03) consumes it.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools</automated>
  </verify>
  <done>BOTH evaluateBusiness (read) and recordScorecardAnswer (write) are registered in buildCockpitTools; evaluateBusiness args are a closed enum + it maps in SMOKE_OP_TOOL; recordScorecardAnswer takes {field,value}, is tenant-scoped + not plan-gated; the cockpitTools test asserts both tool keys present.</done>
</task>

<task type="auto">
  <name>Task 2: EVALUATION card</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, docs/playbooks/cockpit.md</files>
  <action>
Add an EVALUATION card component subscribing via `useQuery(api.evaluations.byThread, { threadId })`
(mirror the PlanCard/BriefingCard pattern at cards.tsx:37/41). Render, per BRAND.md + globals.css tokens
(no hardcoded hex a token covers; no component library): framework section header; per-line findings each
with a small H/M/L confidence chip + inline citation linking the source doc (the SourceCard doc-link
precedent, 10-03); a leverage-ranked gap list (≤5, rest under a "more" disclosure) — render an "Act on
this" control per gap (its handler is wired in plan 05; here it may be a disabled/no-op placeholder);
the affirmative HEALTHY state ("No gaps found on [framework] — your business is solid here" + cited
strengths); and a VISUALLY DISTINCT "Not enough data to assess — add X to your vault/profile to unlock
this" state (never styled like a gap). Reuse the existing `box`/`label` inline styles. Match the committed
screenshots before inventing patterns.
Also update docs/playbooks/cockpit.md for the new evaluateBusiness tool + EVALUATION card (both watched paths) and bump its `Last verified:` line (§9 Stop hook).
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>EVALUATION card compiles, subscribes to evaluations.byThread, renders findings+chips+citations, healthy + not-enough-data states distinct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the EVALUATION card visually (BEVL-01)</name>
  <files>none — verification only</files>
  <action>Human verification of the cockpit tool + card built in Tasks 1-2. No code is written; follow the how-to-verify steps against a running deployment.</action>
  <what-built>The evaluateBusiness cockpit tool + the EVALUATION card (the sole manual-only item in 12-VALIDATION.md).</what-built>
  <how-to-verify>
    1. Run the app (prod-build for UAT — Next dev OOMs on the workspace page; memory: dev-replay-gotchas).
    2. In the cockpit ask "evaluate my business" (and try "run a SWOT").
    3. Confirm: an EVALUATION card appears with framework sections; each finding has a H/M/L chip + a
       clickable citation; the gap list is leverage-ranked and capped at ~5 with a "more" disclosure.
    4. On a thin/idea-stage profile: confirm the distinct "not enough data to assess" state (NOT styled as a gap).
    5. On a healthy fixture: confirm the affirmative "no gaps found" card.
    6. Confirm no numeric % / viability score anywhere.
  </how-to-verify>
  <verify>Manual — human confirms the card renders per BRAND.md across all states.</verify>
  <done>Card shows framework sections, per-finding H/M/L chip + citation, ≤5 ranked gaps, distinct not-enough-data + affirmative healthy states, no numeric scores.</done>
  <resume-signal>Type "approved" or describe visual/behaviour issues.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` + `pnpm --filter web typecheck` green.
- The tool is read-only (no propose/send); the card renders all four states (findings / healthy / not-enough-data / loading).
</verification>

<success_criteria>
A cockpit command produces a cited, confidence-labelled EVALUATION card that is honest about thin data
and affirmative when healthy — BEVL-01 visible end-to-end (acting wired in plan 05, eval-gated in plan 06).
</success_criteria>

<output>
After completion, create `.planning/phases/12-business-evaluation-engine/12-04-SUMMARY.md`
</output>
