---
phase: 12-business-evaluation-engine
plan: 06
type: execute
wave: 5
depends_on: ["12-03", "12-04", "12-05"]
files_modified:
  - packages/backend/scripts/run-eval-golden.mjs
  - packages/backend/convex/smoke.ts
  - packages/backend/scripts/eval-cases/27-grounded-assessment.json
  - packages/backend/scripts/eval-cases/28-healthy-no-gaps.json
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - docs/playbooks/agent-runtime.md
  - docs/playbooks/skill-registry.md
autonomous: false
requirements: [BEVL-01, BEVL-02]
must_haves:
  truths:
    - "A grounded-assessment golden fixture asserts an evaluation row with ≥1 finding + citation (live model)"
    - "A healthy-business golden fixture asserts gapCount 0 — 'no gaps' is a first-class eval-tested outcome (SC #2)"
    - "The cockpit-agent knows WHEN to call evaluateBusiness (taught via a gated candidate body, not hardcoded)"
    - "The gated rubric + cockpit-agent candidates activate ONLY through a recorded passing pnpm eval:golden (SC #4)"
  artifacts:
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "evaluationPresent/gapCount expect keys + smoke reads + bumped fixture floor"
      contains: "evaluationPresent"
    - path: "packages/backend/scripts/eval-cases/27-grounded-assessment.json"
      provides: "grounded-assessment golden fixture riding the SMOKE:: seam"
    - path: "packages/backend/scripts/eval-cases/28-healthy-no-gaps.json"
      provides: "healthy-business fixture (gapCount 0)"
  key_links:
    - from: "packages/backend/scripts/run-eval-golden.mjs"
      to: "packages/backend/convex/smoke.ts"
      via: "smoke:evaluationCountForThread / gapCountForThread reads (mirror briefingCountForThread)"
      pattern: "evaluationCountForThread|gapCountForThread"
---

<objective>
Close the phase gate: extend the eval harness's CLOSED expect vocabulary for the new evaluation
observables, add the two golden fixtures (grounded-assessment + healthy-no-gaps), teach the cockpit-agent
WHEN to call evaluateBusiness (a gated candidate body edit, §5), and run `pnpm eval:golden` green to
record evidence and activate the gated candidates (SC #4). This is the phase-boundary gate — after this,
`/gsd:verify-work`.

Purpose: SC #1 (a finding carries a citation, live), SC #2 (healthy = zero gaps, eval-tested), SC #4
(rubrics activated only through the eval gate).
Output: harness extension + 2 fixtures + cockpit-agent teaching + a recorded green eval run + activation.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-business-evaluation-engine/12-RESEARCH.md
@.planning/phases/12-business-evaluation-engine/12-VALIDATION.md

# The harness + a fixture to copy the shape from:
@packages/backend/scripts/run-eval-golden.mjs
@packages/backend/scripts/eval-cases/25-vault-grounded.json

<interfaces>
<!-- CLOSED expect vocabulary EXPECT_KEYS (verified run-eval-golden.mjs:43) — ADD keys here + a case in
     evaluateExpect + a smoke read (the briefingPresent precedent: smoke:briefingCountForThread wired at
     the case runner). New keys: evaluationPresent (bool), gapCount (number). -->
const EXPECT_KEYS = new Set([ ...existing, "evaluationPresent", "gapCount" ]);

<!-- selfCheck floor (verified run-eval-golden.mjs:189): `fixtures.length >= 18`. There are 25 fixtures
     on disk today; adding 2 → 27. Bump the floor to `>= 27`. -->

<!-- Fixtures are REAL-model turns; SMOKE::-prefixed turns are REJECTED (:201) — they ground via seeded
     docs through the SMOKE:: grounding seam, not SMOKE:: turns. Shape: {id, description, turns[], expect{}, needles[]}. -->

<!-- Version-collision (Pitfall 1): before eval/activate, verify which live version carries YOUR body
     (getSkillVersion, skills.ts:210) — seedSkills mints maxVersion+1; optimizer dry-runs occupy versions.
     Phase 10 shipped cockpit-agent@14, not the planned @13. Pin the VERIFIED version in --skill. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the expect vocabulary + smoke reads + fixture floor</name>
  <files>packages/backend/scripts/run-eval-golden.mjs, packages/backend/convex/smoke.ts, docs/playbooks/agent-runtime.md</files>
  <action>
Add `evaluationPresent` and `gapCount` to the EXPECT_KEYS set (run-eval-golden.mjs:43) and to
`evaluateExpect` (a case each): evaluationPresent asserts an evaluation row exists for the thread (read
from a new `smoke:evaluationCountForThread` > 0 — mirror briefingPresent/briefingCountForThread), gapCount
asserts the latest evaluation's gap count (read from `smoke:gapCountForThread`). Thread these smoke reads
through the case runner exactly as briefingCount/synopsis are (the positional-arg precedent). Add the two
smoke reads to smoke.ts (tenant-scoped queries over the evaluations table). Bump selfCheck's floor from
`>= 18` to `>= 27` (25 on disk + 2 new). Keep the closed-vocabulary rejection intact (unknown key throws).
Update docs/playbooks/agent-runtime.md for the new expect keys + smoke reads + fixture floor and bump its `Last verified:` line (§9).
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>evaluationPresent/gapCount accepted; smoke reads wired; floor is >= 27; self-check (offline, zero convex) green.</done>
</task>

<task type="auto">
  <name>Task 2: Two golden fixtures + cockpit-agent teaching</name>
  <files>packages/backend/scripts/eval-cases/27-grounded-assessment.json, packages/backend/scripts/eval-cases/28-healthy-no-gaps.json, packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, docs/playbooks/agent-runtime.md, docs/playbooks/skill-registry.md</files>
  <action>
Create 27-grounded-assessment.json: a real-model turn ("evaluate my business") grounding via seeded
tenant docs (the SMOKE:: grounding seam, NOT a SMOKE:: turn), expect `{ evaluationPresent: true }` with
needles asserting a finding + citation. Create 28-healthy-no-gaps.json: a healthy-business seeded profile,
expect `{ evaluationPresent: true, gapCount: 0 }` (SC #2). Edit the cockpit-agent skill body (canonical
cockpit-agent.md + derived cockpitAgent.ts, keeping the sync assertion green) to teach WHEN to call
evaluateBusiness (intent recognition: "evaluate my business" / "run a SWOT" / "run a growth diagnosis";
auto-pick vs. explicit framework; read-only, one card). Also teach: after asking the user for a missing financial figure the diagnosis needs, call `recordScorecardAnswer` with their answer so it is stored (cited "user-provided") and not re-asked — the "store" half of the LOCKED vault-first→ask→store loop (no new fixture; the grounded-assessment flow gates it). This body edit is a gated candidate → it rides
the eval gate in Task 3. Do NOT hardcode any rubric prose in source (§5).
Update docs/playbooks/agent-runtime.md (new golden fixtures) and docs/playbooks/skill-registry.md (cockpit-agent teaching candidate) and bump each `Last verified:` line (§9).
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check && pnpm --filter @pikar/contracts test -- skillBodies && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Both fixtures parse under the closed vocabulary (floor 27); cockpit-agent body teaches WHEN to call evaluateBusiness AND to call recordScorecardAnswer after asking for a figure; md↔ts stays byte-identical; self-check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the eval gate green + activate gated candidates (SC #4)</name>
  <files>none — verification + activation only</files>
  <action>Human-run eval gate over the fixtures + gated candidate bodies from Tasks 1-2. No new code; follow the how-to-verify steps to run pnpm eval:golden and activate on green.</action>
  <what-built>The full eval gate: harness + fixtures + gated candidate bodies (rubrics + cockpit-agent) ready to activate.</what-built>
  <how-to-verify>
    1. Stabilize the eval env (memory eval-env-recovery): kill ALL convex processes → ONE clean
       `npx convex dev` from packages/backend → confirm stable (no retry storm) before evaluating.
    2. Verify which live version carries each new body (`npx convex run skills:getSkillVersion ...`,
       Pitfall 1) — seedSkills mints maxVersion+1; pin the VERIFIED versions, not the planned ones.
    3. Run `pnpm eval:golden` (optionally `--skill cockpit-agent@<verified>` and the rubric pins). Confirm
       GREEN including 27-grounded-assessment (≥1 finding + citation) and 28-healthy-no-gaps (gapCount 0).
    4. On green, activate the gated candidates (growth-os-diagnostic + the persona rubrics + cockpit-agent)
       through activateSkill — the EVAL_GATE refuses any gated candidate lacking passing evidence (SC #4).
    5. Report the eval run cost + the activated versions.
  </how-to-verify>
  <verify>Manual — pnpm eval:golden green including fixtures 27 + 28; gated candidates activated via passing evidence.</verify>
  <done>Eval run recorded green; growth-os-diagnostic + persona rubrics + cockpit-agent candidates ACTIVE only through the passing run (SC #4); cost + versions reported.</done>
  <resume-signal>Type "gate green — activated @<versions>" or paste the failing fixture output.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm eval:golden` recorded GREEN (evidence written to the gated rows); both new fixtures pass.
- The gated rubric + cockpit-agent candidates are ACTIVE only via the recorded passing run (SC #4).
- Fixture floor is 27; the closed vocabulary still rejects unknown keys.
</verification>

<success_criteria>
The grounded-assessment and healthy-no-gaps behaviours are eval-gated and live-verified; the evaluation
rubrics are minted AS gated skills activated only through the Phase-3.6 eval gate — Phase 12's governance
bar (SC #4) and honest-no-gaps bar (SC #2) are met. Ready for /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/12-business-evaluation-engine/12-06-SUMMARY.md`
</output>
