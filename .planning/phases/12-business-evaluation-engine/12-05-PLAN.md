---
phase: 12-business-evaluation-engine
plan: 05
type: execute
wave: 4
depends_on: ["12-03", "12-04"]
files_modified:
  - packages/backend/convex/evaluations.ts
  - packages/backend/convex/schema.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - packages/backend/convex/gapAction.test.ts
  - docs/playbooks/business-evaluation.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [BEVL-02]
must_haves:
  truths:
    - "Tapping 'Act on this' on a gap creates a proposed PLAN through the existing Approve→execute spine"
    - "The proposed plan's body IS a concrete grounded next-step memo citing the relevant specialist playbook"
    - "Approving a gap-action persists the next-step memo as a vault doc — NOT an email"
    - "The review stays read-only; only 'Act on this' crosses the Approve gate (two-shapes rule)"
  artifacts:
    - path: "packages/backend/convex/evaluations.ts"
      provides: "actOnGap mutation (gap → memo-plan proposed) + memo terminal on approve"
      exports: ["actOnGap"]
    - path: "packages/backend/convex/gapAction.test.ts"
      provides: "convex-test: tapped gap → proposed transition; approve → persisted memo (not email)"
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "api.evaluations.actOnGap"
      via: "the 'Act on this' control (useMutation)"
      pattern: "actOnGap"
    - from: "packages/backend/convex/evaluations.ts"
      to: "internal.plans.insertPlan"
      via: "reuse the collecting→proposed plan spine (no new proposal store)"
      pattern: "insertPlan|proposeEmailPlan"
---

<objective>
Wire the gated ACTING side of BEVL-02: a tapped gap becomes an approvable next-step memo. The review is
read-only; "Act on this" is the ONLY thing that crosses into the plan→Approve→execute spine (two-shapes
rule). Building the actual fix (the Grand Slam Offer, the lead campaign) is OUT of scope — the memo is the
honest Phase-12 stand-in; specialist execution is deferred to Phase 15+.

MEMO-TERMINAL DECISION (Claude's discretion, resolved): the LAZY route (research recommendation) — the
proposed plan's `body` IS the memo, drafted at propose time; Approve marks it accepted and persists the
memo as a `vaultDocuments` doc-kind `next_step_memo`. NO new fan-out workflow; deliverApprovedPlan
(gmail-only) is NOT touched — the generalized executor (ACTN-01) is Phase 15, not pulled forward.

Purpose: gaps → governed, approvable, saved next actions; healthy = nothing to act on.
Output: actOnGap mutation + memo terminal + the "Act on this" handler + a transition/persistence test.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-business-evaluation-engine/12-RESEARCH.md

# Reuse the plan lifecycle — do NOT build a new proposal store:
@packages/backend/convex/plans.ts
@packages/backend/convex/deliverApprovedPlan.ts

<interfaces>
<!-- plans spine (verified plans.ts): insertPlan{tenantId,threadId}→planId (starts "collecting");
     patchPlan{planId, body, status, ...}; setPlanStatus{planId, status}. Status enum PINNED
     (collecting→proposed→approved→scheduled|delivering→done|canceled). A memo plan has NO recipients. -->
internal.plans.insertPlan / patchPlan / setPlanStatus

<!-- From plan 03: an evaluations row's gaps[] each carry {label, leverageRank, route, playbook,
     citationDocId?}. actOnGap takes {evaluationId | threadId, gapIndex}. -->

<!-- Memo persistence: reuse the Phase-11 pattern — a next_step_memo is "just another vault doc"
     (vaultDocuments, kind free string). Persist on Approve; specialist skill is NAMED (gap.route),
     not run. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: actOnGap → proposed memo-plan</name>
  <files>packages/backend/convex/evaluations.ts, packages/backend/convex/schema.ts</files>
  <behavior>
    - actOnGap({threadId, gapIndex}) reads the gap from the latest evaluations row, drafts a concrete
      grounded next-step memo (steps for that gap, citing the relevant specialist playbook by name) into
      a plan body, creates a plan via insertPlan, patches body + a memo discriminator (no recipients),
      and moves it to "proposed" through the existing spine.
    - The memo NAMES the target specialist skill (offer-architect/money-model-designer/lead-engine) but
      does NOT run it (deferred).
    - A healthy evaluation (zero gaps) exposes no gap to act on — nothing crosses the gate.
  </behavior>
  <action>
Add `actOnGap` to evaluations.ts (tenantMutation, §2 wrappers). It reads the gap, composes the memo body
(the memo-writer prose may come from a memo skill body or a deterministic template citing gap.playbook —
keep it grounded, no fabricated metrics), calls internal.plans.insertPlan, patchPlan (body + status
"proposed"), reusing the collecting→proposed spine (do NOT build a new proposal store). Add a minimal
plan discriminator for a memo-plan (e.g. a `kind: "memo"` optional field on plans in schema.ts, or reuse
an existing marker) so the memo terminal (Task 2) and the executor can tell it is not an email. Recipients
stay empty.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- gapAction</automated>
  </verify>
  <done>actOnGap turns a gap into a proposed memo-plan (no recipients) through the pinned spine; healthy → no gap to act on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Memo terminal on Approve + gapAction.test.ts</name>
  <files>packages/backend/convex/evaluations.ts, packages/backend/convex/gapAction.test.ts, docs/playbooks/business-evaluation.md</files>
  <behavior>
    - Approving a memo-plan persists the memo body as a vaultDocuments doc-kind "next_step_memo"
      (tenant-scoped) and marks the plan done — WITHOUT invoking deliverApprovedPlan / gmail.send.
    - gapAction.test.ts: tapped gap → plan status "proposed"; approve → a next_step_memo vault doc exists
      for the tenant AND no gmail send path was taken (assert the terminal is the memo persist, not email).
  </behavior>
  <action>
Add the memo terminal: on Approve of a memo-plan, persist the body as a `next_step_memo` vault doc and set
the plan status terminal — a small dedicated path, NOT deliverApprovedPlan (which only fans out gmail.send;
Pitfall 3). Route the executePlan/Approve mutation to branch on the memo discriminator so an email plan
still uses the gmail terminal and a memo plan uses the persist terminal. Write gapAction.test.ts
(convex-test) covering the proposed transition and the memo-persisted-not-emailed assertion.
ponytail: no new workflow/fan-out; the laziest correct terminal is a persist + status flip.
Update docs/playbooks/business-evaluation.md for the gap-action + memo terminal (the evaluations.ts change) and bump its `Last verified:` line (§9).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- gapAction && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Approve persists a next_step_memo vault doc and never enters gmail.send; both transitions asserted green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the 'Act on this' control</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, docs/playbooks/cockpit.md</files>
  <action>
Wire the EVALUATION card's per-gap "Act on this" control (placeholder from plan 04) to
`useMutation(api.evaluations.actOnGap)` passing {threadId, gapIndex}. On success the existing PLAN card
renders the proposed memo-plan (Approve gate) via the reactive plans.byThread subscription already in
cards.tsx — no new surface. Keep the read-only review intact: only this control writes.
Update docs/playbooks/cockpit.md for the wired "Act on this" control and bump its `Last verified:` line (§9).
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>"Act on this" calls actOnGap; the resulting proposed memo-plan surfaces in the existing PLAN card; web typechecks.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (proposed transition + memo-not-email terminal).
- `pnpm --filter web typecheck` green.
- deliverApprovedPlan.ts UNCHANGED (memo does not ride the gmail terminal); review stays read-only.
</verification>

<success_criteria>
Each surfaced gap becomes a concrete, approvable next-step memo through the existing Approve→execute spine;
approving it saves the memo (not an email); a healthy business has nothing to act on — BEVL-02 satisfied
without pulling specialist execution (Phase 15+) forward.
</success_criteria>

<output>
After completion, create `.planning/phases/12-business-evaluation-engine/12-05-SUMMARY.md`
</output>
