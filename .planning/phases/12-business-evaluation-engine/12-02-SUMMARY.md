---
phase: 12-business-evaluation-engine
plan: 02
subsystem: skills
tags: [skill-registry, eval-gate, growth-os, swot, lean-canvas, bmc, rubric, prompts, gated-skills]

# Dependency graph
requires:
  - phase: 01-foundation-governance-substrate
    provides: skill registry (skills table, seedSkills, GATED_SKILLS, EVAL_GATE), md→ts derived-constant pattern
  - phase: 11-persona-onboarding-business-profile
    provides: businessProfile.ts canonical-md→derived-ts mirror precedent
provides:
  - 7 registered GATED skill names (growth-os-diagnostic, swot, lean-canvas, bmc, offer-architect, money-model-designer, lead-engine)
  - canonical .md + byte-identical derived .ts bodies for each (the METHOD the engine loads at runtime)
  - contracts-side md↔ts no-drift sync test covering all 7
  - seedSkills entries so a boot seeds all 7 (bootstrap v1 active; edits publish eval-gated candidates)
affects: [12-03 evaluateBusiness engine (loads framework rubric bodies by name), 12-06 eval gate (activates rubric candidates), 15+ specialist execution]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Framework rubric prompt = versioned gated skill row (never hardcoded); engine loads the METHOD by name"
    - "Shared grounding/honesty rubric baked into every framework body: cite vault source per finding, H/M/L confidence, explicit not-enough-data state, no numeric %, affirmative healthy state"
    - "Method knowledge in skill bodies (original wording); user data grounds findings — no copyrighted book text in repo"

key-files:
  created:
    - packages/contracts/skills/growth-os-diagnostic.md
    - packages/contracts/skills/swot.md
    - packages/contracts/skills/lean-canvas.md
    - packages/contracts/skills/bmc.md
    - packages/contracts/skills/offer-architect.md
    - packages/contracts/skills/money-model-designer.md
    - packages/contracts/skills/lead-engine.md
    - packages/contracts/src/skills/{growthOsDiagnostic,swot,leanCanvas,bmc,offerArchitect,moneyModelDesigner,leadEngine}.ts
    - packages/contracts/src/skills/skillBodies.test.ts
  modified:
    - packages/contracts/src/skill.ts
    - packages/backend/convex/skills.ts
    - docs/playbooks/skill-registry.md

key-decisions:
  - "All 7 evaluation/specialist skills are GATED — their METHOD leaves the building as findings/next-step memos, exactly the eval-gate risk profile; bootstrap v1 seeds active so gating costs nothing until the first edit"
  - "growth-os-diagnostic folds the diagnostic gate order + financial-spine metric defs + 7-level positioning into ONE body matching plan 12-01's pure diagnose() gate order"
  - "Framework bodies written as prompts to the ASSESSING model (grounding rules, quadrants/blocks, output shape) — original wording derived from Skills/*/references, zero book text"

patterns-established:
  - "Contracts-side md↔ts drift test (skillBodies.test.ts) mirrors the backend skills.test.ts no-drift precedent for new skill bodies added in contracts"
  - "Derived .ts constants generated byte-identically from canonical .md via a throwaway generator so the sync assertion is guaranteed green"

requirements-completed: [BEVL-01]

# Metrics
duration: 17min
completed: 2026-07-24
---

# Phase 12 Plan 02: Framework Rubric Skill Registration Summary

**7 gated evaluation skills registered — 4 framework rubrics (growth-os-diagnostic / swot / lean-canvas / bmc) the engine loads to assess a business + 3 specialist targets (offer-architect / money-model-designer / lead-engine), each a canonical .md → byte-identical derived .ts body with original-wording METHOD and no book text.**

## Performance

- **Duration:** ~17 min
- **Started:** 2026-07-24T20:35:00Z
- **Completed:** 2026-07-24T20:52:00Z
- **Tasks:** 3
- **Files modified:** 19 (16 created, 3 modified)

## Accomplishments
- 7 new skill-name consts + `GATED_SKILLS` entries so `isGatedSkill` returns true for every one (activation now requires eval evidence — SC #4)
- 4 framework rubric bodies the engine loads at runtime, each carrying the shared grounding rubric (per-finding vault citation, H/M/L confidence, explicit "not enough data to assess" state, no numeric %, affirmative healthy no-gaps result)
- `growth-os-diagnostic` folds the Market→Offer→Money-Model→Leads→Scale gate order, the financial-spine definitions (LTGP, CAC, ratio, 30-day cash, CFA, master switch) and 7-level positioning — the METHOD matching plan 12-01's pure `diagnose()`
- 3 specialist METHOD memos an approved gap-action will name as its target (execution deferred to Phase 15+)
- `skillBodies.test.ts` asserts all 7 md↔ts pairs byte-identical; `seedSkills` seeds all 7; playbook updated + `Last verified` bumped

## Task Commits

1. **Task 1: Register 7 gated skill names** - `6ee2208` (feat)
2. **Task 2: Framework rubric bodies (growth-os-diagnostic / swot / lean-canvas / bmc)** - `65e9f87` (feat)
3. **Task 3: Specialist bodies + sync test + seedSkills + playbook** - `2ef5f0e` (feat)

## Files Created/Modified
- `packages/contracts/src/skill.ts` - 7 name consts + 7 `GATED_SKILLS` entries
- `packages/contracts/skills/*.md` (7) - canonical METHOD bodies (original wording, no book text)
- `packages/contracts/src/skills/*.ts` (7) - byte-identical derived bundler-safe constants
- `packages/contracts/src/skills/skillBodies.test.ts` - md↔ts no-drift assertion for all 7 (7 tests, green)
- `packages/backend/convex/skills.ts` - imports + 7 `seedSkills` entries
- `docs/playbooks/skill-registry.md` - new `Last verified` line, refreshed seeded/gated lists, registered new test file

## Decisions Made
- **All 7 gated, bootstrap-active:** every body's output (findings, next-step memos) leaves the building, so all 7 join `GATED_SKILLS`; the `rows.length===0` bootstrap path still seeds v1 active on a fresh registry, so gating is free until the first edit and rubric activation flows through the plan-06 eval gate.
- **One folded growth-os body:** rather than three separate diagnostic-tree/financial-spine/roadmap skills, the routing method, shared metric definitions, and level positioning live in one `growth-os-diagnostic` body so the engine loads a single rubric matching the pure `diagnose()` gate order.
- **Generator-guaranteed sync:** derived `.ts` constants were emitted from the canonical `.md` by a throwaway script (`JSON.stringify` of LF-normalized bytes), so the byte-identity the sync test enforces is structural, not hand-typed.

## Deviations from Plan

None - plan executed exactly as written. (Task 1's verify preferred `test`, but the new sync test does not exist until Task 3; `typecheck` — the plan's own fallback — was used for Task 1 and the full contracts `test` ran green after Task 3.)

## Issues Encountered
- **Pre-existing backend failures surfaced (out of scope, NOT caused by 12-02):** `convex/audit.test.ts > "audit.log inserts exactly one row that round-trips"` fails, and `pnpm --filter @pikar/backend typecheck` is red (~52 errors, all in `*.test.ts` — `import.meta.glob`/`WorkflowId`/`mintClientSecret` type gaps). Both reproduce with 12-02's changes stashed out. Production `skills.ts` compiles clean (0 errors) and `skills.test.ts` passes all 42. Logged to `deferred-items.md`; root-cause belongs to the audit-subsystem owner / an earlier plan, not this task (scope boundary).

## User Setup Required
None - no external service configuration required. (A deployment re-seed picks up the 7 new skills on the next `npm run seed` / dev boot; rubric candidates activate only through the plan-06 eval gate.)

## Next Phase Readiness
- Plan 12-03 (`evaluateBusiness`) can load any of the 4 framework rubric bodies by name via `getActiveSkill` / version-pinned `getSkillVersion`.
- Plan 12-06 eval gate activates the gated rubric candidates after a green `pnpm eval:golden` run — verify which live version carries each body before eval/activate (optimizer dry-runs / prior candidates occupy versions; memory: skill-version-collision).
- **Concern:** the pre-existing `audit.test.ts` failure should be triaged before the phase's `/gsd:verify-work` gate (it is red independent of this plan).

## Self-Check: PASSED

All 15 created files present on disk; all 3 task commits (`6ee2208`, `65e9f87`, `2ef5f0e`) present in history. Contracts sync test green (7/7); backend `skills.test.ts` green (42/42); `check-playbooks.mjs` exit 0.

---
*Phase: 12-business-evaluation-engine*
*Completed: 2026-07-24*
