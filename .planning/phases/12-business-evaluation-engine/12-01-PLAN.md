---
phase: 12-business-evaluation-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/growth/scorecard.ts
  - packages/core/src/growth/financialSpine.ts
  - packages/core/src/growth/financialSpine.test.ts
  - packages/core/src/growth/diagnose.ts
  - packages/core/src/growth/diagnose.test.ts
  - packages/core/src/growth/index.ts
  - docs/playbooks/growth-diagnostic.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [BEVL-01]
must_haves:
  truths:
    - "diagnose(scorecard) returns the correct gate/route for each financial regime"
    - "An unknown/null financial input makes diagnose ASK, never fabricate a route or metric"
    - "ltgpCac and cfa match the Growth OS worked examples exactly"
    - "A healthy scorecard routes to the Scale/no-gap branch (SC #2 in code form)"
  artifacts:
    - path: "packages/core/src/growth/financialSpine.ts"
      provides: "ltgpCac() and cfa() pure functions (port of ltgp_cac.py / cfa.py)"
      exports: ["ltgpCac", "cfa"]
    - path: "packages/core/src/growth/diagnose.ts"
      provides: "diagnose() top-down gate router + leverageRank() (port of diagnose.py)"
      exports: ["diagnose", "leverageRank"]
    - path: "packages/core/src/growth/scorecard.ts"
      provides: "Scorecard type + all-nullable default (from business-scorecard.template.json)"
      exports: ["Scorecard", "emptyScorecard"]
  key_links:
    - from: "packages/core/src/growth/diagnose.ts"
      to: "packages/core/src/growth/financialSpine.ts"
      via: "diagnose imports ltgpCac/cfa for Gate 2/3 checks"
      pattern: "ltgpCac|cfa"
---

<objective>
Port the Growth OS DIAGNOSTIC math from Python to pure-TS so the evaluation engine can route a
scorecard to its single highest-leverage constraint without Convex (CLAUDE.md §1). This is the
deterministic core of BEVL-01's "no fabricated metrics" guarantee: the port preserves
diagnose.py's conservative "unknown → ask, don't guess" behaviour in the type system.

Purpose: give the Convex engine (plan 03) a testable, framework-agnostic diagnosis it merely orchestrates.
Output: packages/core/src/growth/{scorecard,financialSpine,diagnose}.ts + assert-based tests + a playbook.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-business-evaluation-engine/12-RESEARCH.md
@.planning/phases/12-business-evaluation-engine/12-VALIDATION.md

# Source to PORT (do NOT run the Python; do NOT port scorecard.py's CLI):
@Skills/growth-os/scripts/diagnose.py
@Skills/growth-os/scripts/ltgp_cac.py
@Skills/growth-os/scripts/cfa.py
@Skills/growth-os/assets/business-scorecard.template.json

<interfaces>
<!-- The Scorecard shape is the template.json above, ALL fields nullable. The engine (plan 03)
     parses grounded vault text into this and calls diagnose(). Port targets: -->

// financialSpine.ts — port of ltgp_cac.py / cfa.py (gross profit, never revenue):
export function ltgpCac(i: { grossProfitPerPurchase: number; purchases: number;
  acqSpend: number; customers: number; industryAvgCac?: number | null }):
  { ltgp: number; cac: number; ratio: number; scalable: boolean;
    ceiling: "business_model" | "advertising" | null };   // ratio>=3 => scalable
export function cfa(i: { thirtyDayCash: number; cac: number; serviceCost: number }):
  { ratio: number; achieved: boolean; additionalCustomersFunded: number };  // ratio>=1 achieved

// diagnose.ts — port of diagnose.py (top-down, stop at first failing gate):
export type Prescription = { constraint: string; gate: 0|1|2|3|"scale";
  route: string; playbook: string; reason: string; proofMetric: string;
  roadmapLevel: number | null; ask?: string };   // ask set => "not enough data", NOT a route
export function diagnose(sc: Scorecard): Prescription;   // healthy => gate:"scale" (no gap)
export function leverageRank(scs: Prescription[]): Prescription[]; // gate order Market<Offer<Money<Leads
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scorecard type + financial-spine math</name>
  <files>packages/core/src/growth/scorecard.ts, packages/core/src/growth/financialSpine.ts, packages/core/src/growth/financialSpine.test.ts, packages/core/src/growth/index.ts</files>
  <behavior>
    - ltgpCac: grossProfitPerPurchase 150 × purchases 30 = LTGP $4,500; acqSpend $3,000 / 20 customers = CAC $150; ratio 30; scalable true (worked example from ltgp_cac.py).
    - ltgpCac master switch: CAC below industryAvgCac×3 → ceiling "business_model"; CAC above → ceiling "advertising"; industryAvgCac null → ceiling null (don't guess).
    - ltgpCac guard: customers <= 0 throws/returns a null-CAC (no divide-by-zero fabrication).
    - cfa: ratio = thirtyDayCash / (cac + serviceCost); >=1 achieved; boundary at exactly 1 (achieved) and 2 (additionalCustomersFunded = 1); <1 → achieved false, funded 0.
    - cfa guard: (cac + serviceCost) <= 0 → not achieved, no NaN.
  </behavior>
  <action>
Create packages/core/src/growth/scorecard.ts: a `Scorecard` TS type mirroring
business-scorecard.template.json with EVERY leaf `T | null` (identity, financials, position,
offer_card, model_card, lead_card, history[]), plus `export const emptyScorecard: Scorecard`
= the template's all-null default. Nullable is load-bearing: a null is a "not enough data" nudge,
never a fabricated value (Pitfall 4).

Create financialSpine.ts: pure `ltgpCac()` and `cfa()` per the interface + worked examples above.
LTGP uses GROSS PROFIT per purchase (never revenue). Constants FLOOR_RATIO=3.0, INDUSTRY_MULTIPLE=3.0.
Guard every divisor; an unknown input returns a null field, it does NOT invent a number.

index.ts re-exports scorecard + financialSpine (and diagnose after Task 2).
Write financialSpine.test.ts: plain `assert`-based self-check (ponytail — no framework beyond the
existing vitest runner), asserting the worked examples + the two CFA boundaries + the divisor guards.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- financialSpine</automated>
  </verify>
  <done>ltgpCac returns LTGP 4500 / CAC 150 / ratio 30; cfa boundaries at 1 and 2 assert green; divisor guards return null/false not NaN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diagnose() gate router + leverage rank</name>
  <files>packages/core/src/growth/diagnose.ts, packages/core/src/growth/diagnose.test.ts, packages/core/src/growth/index.ts</files>
  <behavior>
    - Gate 0 Market: identity.market === viable false → route offer-architect/01-select-market, gate 0.
    - Gate 1 Offer: no offer OR value-equation min score <= 3 OR commodity → offer-architect, gate 1.
    - Gate 2 Money Model: 30-day payback fails / thirtyDayCash < cac / single offer type / LTGP:CAC < 3 with CAC within industry norm → money-model-designer, gate 2.
    - Gate 3 Leads: CAC > 3× industry avg OR no active channel → lead-engine, gate 3.
    - Else Scale: healthy → gate "scale", route "compound working channels", NO gap (SC #2 in code).
    - CONSERVATIVE: a gate whose inputs are null → prescription.ask set (which figure to ask for), NOT a route and NOT a fabricated metric (Pitfall 4).
    - leverageRank sorts prescriptions by gate order (0<1<2<3<scale) — "fix the ONE bottleneck first".
  </behavior>
  <action>
Port diagnose.py verbatim in behaviour into `diagnose(sc: Scorecard): Prescription`: top-down gates,
STOP at the first failing gate, emit ONE prescription. Import ltgpCac/cfa from financialSpine.ts for
the Gate 2/3 numeric checks. Preserve the conservative branch: when a gate's required financial field
is null, return a prescription with `ask` set (naming the missing input) instead of routing — this is
the "vault-first, then ask, then store" honesty in code. The healthy path returns gate:"scale" with
no constraint (the no-gaps branch).

Add `leverageRank(Prescription[]): Prescription[]` sorting by gate order for the ≤5 gap list (plan 04/05).
diagnose.test.ts: assert one case per gate regime, the null→ask (not-route) case, and the healthy→scale case.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- diagnose</automated>
  </verify>
  <done>Each gate regime routes to the named specialist; null financials → ask (no route, no metric); healthy → scale/no-gap; leverageRank orders by gate.</done>
</task>

<task type="auto">
  <name>Task 3: Growth-diagnostic playbook + watch registration (§9)</name>
  <files>docs/playbooks/growth-diagnostic.md, docs/playbooks/watch.json</files>
  <action>
Create docs/playbooks/growth-diagnostic.md from docs/playbooks/TEMPLATE.md covering the pure-TS growth
port: invariants (gross-profit-not-revenue; conservative unknown→ask; FLOOR_RATIO/INDUSTRY_MULTIPLE=3.0;
all-nullable scorecard), how to change safely, how to verify (the two test commands), operations.
Register the watched prefix `packages/core/src/growth/` under this playbook's entry in
docs/playbooks/watch.json so the Stop hook protects it. Set the `Last verified` line to today.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Playbook exists, watch.json maps packages/core/src/growth/ to it, check-playbooks passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test` green (both new test files).
- No new dependency added (packages/core reused — ponytail rung 2; no packages/growth).
- Growth math is Convex-free and portable (§1).
</verification>

<success_criteria>
diagnose/ltgpCac/cfa are pure, tested, and conservative-by-design; the engine (plan 03) can import
them and get a single leverage-ranked prescription with fabricated-metric-impossible-on-null-input.
</success_criteria>

<output>
After completion, create `.planning/phases/12-business-evaluation-engine/12-01-SUMMARY.md`
</output>
