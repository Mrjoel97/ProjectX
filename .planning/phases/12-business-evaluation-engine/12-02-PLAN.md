---
phase: 12-business-evaluation-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/skill.ts
  - packages/contracts/skills/growth-os-diagnostic.md
  - packages/contracts/skills/swot.md
  - packages/contracts/skills/lean-canvas.md
  - packages/contracts/skills/bmc.md
  - packages/contracts/skills/offer-architect.md
  - packages/contracts/skills/money-model-designer.md
  - packages/contracts/skills/lead-engine.md
  - packages/contracts/src/skills/growthOsDiagnostic.ts
  - packages/contracts/src/skills/swot.ts
  - packages/contracts/src/skills/leanCanvas.ts
  - packages/contracts/src/skills/bmc.ts
  - packages/contracts/src/skills/offerArchitect.ts
  - packages/contracts/src/skills/moneyModelDesigner.ts
  - packages/contracts/src/skills/leadEngine.ts
  - packages/contracts/src/skills/skillBodies.test.ts
  - packages/backend/convex/skills.ts
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [BEVL-01]
must_haves:
  truths:
    - "growth-os-diagnostic, swot, lean-canvas, bmc are registered gated skill names"
    - "offer-architect, money-model-designer, lead-engine are registered gated skill names (execution deferred)"
    - "Each rubric body is a canonical .md → derived .ts constant kept byte-identical by a sync test"
    - "No copyrighted book text; original wording derived from Skills/*/references"
  artifacts:
    - path: "packages/contracts/src/skill.ts"
      provides: "7 new skill-name consts + GATED_SKILLS entries"
      contains: "GROWTH_OS_DIAGNOSTIC_SKILL"
    - path: "packages/contracts/src/skills/skillBodies.test.ts"
      provides: "md↔ts sync assertion for every new skill body"
    - path: "packages/backend/convex/skills.ts"
      provides: "seedSkills entries for the 7 new skills"
      contains: "seeds"
  key_links:
    - from: "packages/contracts/src/skills/growthOsDiagnostic.ts"
      to: "packages/contracts/skills/growth-os-diagnostic.md"
      via: "derived-constant sync assertion"
      pattern: "growthOsDiagnosticSkillBody"
---

<objective>
Register the evaluation frameworks as GATED skills in the registry (§5, SC #4): the four framework
rubrics (growth-os-diagnostic, swot, lean-canvas, bmc) whose METHOD bodies the engine loads at runtime,
plus the three specialist skills (offer-architect, money-model-designer, lead-engine) that approved
gap-actions will NAME as targets (their execution is deferred to Phase 15+). Prompts are versioned DB
rows, never hardcoded — this plan writes the canonical bodies + registration; activation happens through
the eval gate in plan 06.

Purpose: the engine grounds findings in the USER's vault; the METHOD (how to assess) lives in these skill bodies.
Output: 7 gated skill names + canonical/derived bodies + sync test + seedSkills entries.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-business-evaluation-engine/12-RESEARCH.md

# The METHOD source (original wording, NON-book-text — do NOT ingest the Hormozi PDFs):
@Skills/growth-os/references/diagnostic-tree.md
@Skills/growth-os/references/financial-spine.md
@Skills/growth-os/references/roadmap-7-levels.md

<interfaces>
<!-- Existing pattern to MIRROR (do not invent a new one): -->
@packages/contracts/src/skills/businessProfile.ts   <!-- canonical-md→derived-ts constant + header -->
@packages/contracts/src/skills/cockpitAgent.ts       <!-- gated-skill body precedent -->

<!-- packages/contracts/src/skill.ts current shape (verified): -->
export const COCKPIT_AGENT_SKILL = "cockpit-agent" as const;   // ...name consts...
export const GATED_SKILLS: readonly string[] = [ COCKPIT_AGENT_SKILL, DOCUMENT_DRAFTER_SKILL,
  INBOX_DIGEST_SKILL, REPLY_DRAFTER_SKILL ];   // ADD the 7 new names here

<!-- seedSkills lives in packages/backend/convex/skills.ts (~:240 `seeds` array, gated branch :281).
     A brand-new gated skill on a FRESH registry seeds v1 ACTIVE (bootstrap exemption); an edit
     publishes maxVersion+1 as CANDIDATE. Verify the live version before eval (Pitfall 1). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 7 gated skill names</name>
  <files>packages/contracts/src/skill.ts</files>
  <action>
Add seven name consts mirroring the existing `as const` pattern:
GROWTH_OS_DIAGNOSTIC_SKILL="growth-os-diagnostic", SWOT_SKILL="swot", LEAN_CANVAS_SKILL="lean-canvas",
BMC_SKILL="bmc", OFFER_ARCHITECT_SKILL="offer-architect", MONEY_MODEL_DESIGNER_SKILL="money-model-designer",
LEAD_ENGINE_SKILL="lead-engine". Add ALL SEVEN to the `GATED_SKILLS` array so `isGatedSkill` returns
true — activation now requires eval evidence (SC #4). Do not touch the existing four entries.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts test 2>/dev/null || pnpm --filter @pikar/contracts typecheck</automated>
  </verify>
  <done>7 new consts exported; all 7 present in GATED_SKILLS; contracts typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Framework rubric bodies (swot / lean / bmc / growth-os-diagnostic)</name>
  <files>packages/contracts/skills/growth-os-diagnostic.md, packages/contracts/skills/swot.md, packages/contracts/skills/lean-canvas.md, packages/contracts/skills/bmc.md, packages/contracts/src/skills/growthOsDiagnostic.ts, packages/contracts/src/skills/swot.ts, packages/contracts/src/skills/leanCanvas.ts, packages/contracts/src/skills/bmc.ts</files>
  <action>
For each of the 4 FRAMEWORK rubrics the engine loads for auto-pick, follow the businessProfile.ts pattern
EXACTLY — a canonical `packages/contracts/skills/<name>.md` (METHOD in ORIGINAL wording; NO Hormozi book
text) + a derived bundler-safe `packages/contracts/src/skills/<camel>.ts` `export const <name>SkillBody`
constant (Convex runtime can't fs.read) with the businessProfile.ts header:
- growth-os-diagnostic: folds diagnostic-tree.md + financial-spine.md metric defs + roadmap-7-levels
  positioning (the routing method matching the pure diagnose() gate order).
- swot / lean-canvas / bmc: the persona-fallback framework method (quadrants / canvas blocks + the rules
  "cite a vault source per finding, H/M/L confidence, explicit not-enough-data state, no numeric %").
Sync assertions + seedSkills entries are added in Task 3 (kept together so one test covers all 7).
ponytail: bodies are prose the ENGINE loads at runtime — method-only (no user data, no book text).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts typecheck</automated>
  </verify>
  <done>4 framework md↔ts pairs exist with the businessProfile.ts header; no book text; contracts typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Specialist bodies + sync test + seedSkills + playbook</name>
  <files>packages/contracts/skills/offer-architect.md, packages/contracts/skills/money-model-designer.md, packages/contracts/skills/lead-engine.md, packages/contracts/src/skills/offerArchitect.ts, packages/contracts/src/skills/moneyModelDesigner.ts, packages/contracts/src/skills/leadEngine.ts, packages/contracts/src/skills/skillBodies.test.ts, packages/backend/convex/skills.ts, docs/playbooks/skill-registry.md</files>
  <action>
1. The 3 SPECIALIST bodies (offer-architect / money-model-designer / lead-engine) — same canonical-md →
   derived-ts pattern as Task 2; the specialist METHOD a next-step memo will cite (registered now,
   execution deferred to 15+). ORIGINAL wording, NO book text.
2. One `skillBodies.test.ts` with a vitest sync assertion per skill for ALL 7 bodies (Task 2's 4 + these 3)
   — md bytes === ts constant — mirroring the existing sync-assertion precedent.
3. Add all 7 to the `seeds` array in packages/backend/convex/skills.ts (name + body + gated) so a boot seeds them.
4. Update docs/playbooks/skill-registry.md for the 7 new gated skills (registration path + bootstrap-vs-edit
   note) and bump its `Last verified:` line — same plan that changes the watched skill files (§9 Stop hook).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts test -- skillBodies && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All 7 md↔ts pairs assert byte-identical; seedSkills lists all 7; no book text; contracts test + check-playbooks green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/contracts test` green (sync assertions).
- `pnpm --filter @pikar/backend typecheck` (seedSkills entries compile).
- Grep confirms no raw Hormozi book text; §5 no-hardcoded-prompt (bodies are registry rows, not source strings in feature code).
</verification>

<success_criteria>
7 evaluation/specialist skills are registered gated names with canonical bodies loadable from the DB
registry; the engine (plan 03) can load a framework rubric by name; activation is deferred to the eval
gate (plan 06). No book text entered the repo.
</success_criteria>

<output>
After completion, create `.planning/phases/12-business-evaluation-engine/12-02-SUMMARY.md`
</output>
