---
phase: 03-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/package.json
  - packages/backend/convex/convex.config.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/pipeline.ts
  - packages/backend/convex/telemetry.ts
  - packages/core/src/buildTelemetry.ts
  - packages/core/src/buildTelemetry.test.ts
autonomous: true
requirements: [GRDL-01, GRDL-04, GRDL-06]

must_haves:
  truths:
    - "components.actionCache exists in the generated api (GRDL-04 component installed at the exact 0.3.1 pin, CLAUDE.md §6)"
    - "requests rows can carry safeText/safeTextHash and be looked up by (tenantId, safeTextHash) — the cache-key→text recovery path (GRDL-01/04)"
    - "scanning and blocked are legal request statuses; blocked is a legal telemetry reviewOutcome (governed-stop terminal, GRDL-01)"
    - "guardrailConfig table exists for the kill switch + per-request budget (default-on-read — NO seed migration, GRDL-06)"
  artifacts:
    - path: "packages/backend/convex/convex.config.ts"
      provides: "action-cache component registration"
      contains: "app.use(cache)"
    - path: "packages/backend/convex/schema.ts"
      provides: "requests.safeText/safeTextHash/lastInstruction + by_tenant_safeTextHash index + guardrailConfig table + scanning/blocked status literals"
      contains: "by_tenant_safeTextHash"
    - path: "packages/core/src/buildTelemetry.ts"
      provides: "blocked ReviewOutcome member"
      contains: "blocked"
  key_links:
    - from: "packages/backend/package.json"
      to: "@convex-dev/action-cache"
      via: "exact 0.3.1 pin (no caret)"
      pattern: "\"@convex-dev/action-cache\": \"0.3.1\""
    - from: "packages/backend/convex/telemetry.ts"
      to: "packages/core buildTelemetry"
      via: "terminalOutcome validator accepts blocked"
      pattern: "v\\.literal\\(\"blocked\"\\)"
---

<objective>
Install `@convex-dev/action-cache` 0.3.1 (exact pin) and lay every schema/type rail the guardrail wiring needs: `requests.safeText`/`safeTextHash`/`lastInstruction` + the `by_tenant_safeTextHash` index, the `guardrailConfig` single-row table, the `scanning`/`blocked` status literals, and the `blocked` telemetry outcome. All fields optional / default-on-read — **NO @convex-dev/migrations run is required in this phase; do not invent one** (03-RESEARCH Pitfall 7).

Purpose: plans 03-03/04/05 build on these rails; doing them first keeps every later diff purely behavioral.
Output: component registered + codegen'd, schema extended, `blocked` outcome legal end-to-end.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-guardrails/03-RESEARCH.md
@packages/backend/convex/convex.config.ts
@packages/backend/convex/schema.ts
@packages/backend/convex/pipeline.ts
@packages/backend/convex/telemetry.ts
@packages/core/src/buildTelemetry.ts

<interfaces>
Existing status unions to extend IN LOCKSTEP (they are kept in sync by comment-contract):
- schema.ts `requests.status` union (11 literals: submitted…awaiting_reauth)
- pipeline.ts `REQUEST_STATUS` (same 11 literals)

Existing telemetry outcome plumbing:
- packages/core/src/buildTelemetry.ts: `ReviewOutcome` type ("sent" | "rejected" | "expired" | "failed") consumed by `TerminalOutcome`/`TelemetryRow`
- telemetry.ts `terminalOutcome` validator: v.union of the same literals
- schema.ts `telemetry.reviewOutcome` is `v.string()` — needs NO schema change
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + register @convex-dev/action-cache 0.3.1</name>
  <files>packages/backend/package.json, packages/backend/convex/convex.config.ts</files>
  <action>
1. `pnpm --filter @pikar/backend add -E @convex-dev/action-cache@0.3.1` (`-E` = exact pin; verify package.json shows `"0.3.1"` with NO caret — CLAUDE.md §6, pre-1.0 component). Peer `convex ^1.24.8` is satisfied by the repo's 1.42.1.
2. In convex.config.ts add (following the existing component comment style):
```typescript
import cache from "@convex-dev/action-cache/convex.config.js";
// Phase-3 component (GRDL-04). Pinned exact — do not bump casually (CLAUDE.md §6).
app.use(cache);
```
3. Codegen: if a `convex dev` process is running against the local backend it re-pushes automatically; otherwise run `npx convex dev --once` and then RESTART `npx convex dev` (never leave the deployment on `--once` — the workpool stops and smokes hang, 01-06 rule). NEVER trust the CLI exit code on Windows/Node24 — judge by the output banner and by the generated types existing.
4. Confirm `components.actionCache` is now typed: `grep -r "actionCache" packages/backend/convex/_generated/api.d.ts` (or typecheck a scratch reference and delete it).
  </action>
  <verify>
    <automated>node -e "const p=require('./packages/backend/package.json');if(p.dependencies['@convex-dev/action-cache']!=='0.3.1')throw new Error('not exact-pinned')" && pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Exact 0.3.1 pin in package.json; app.use(cache) registered; codegen emits components.actionCache; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Schema rails — safeText fields, index, guardrailConfig, new statuses</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/pipeline.ts</files>
  <action>
In schema.ts:

1. `requests` table — add three OPTIONAL fields (no backfill, no migration):
   - `safeText: v.optional(v.string())` — redacted goal, written by guardrails.prepare (content plane, like goal/draft)
   - `safeTextHash: v.optional(v.string())` — SHA-256 hex of safeText; the cache-key member
   - `lastInstruction: v.optional(v.string())` — the SCANNED (redacted) latest regenerate instruction (plan 03-04 writes it)
   and a new index `.index("by_tenant_safeTextHash", ["tenantId", "safeTextHash"])` — the deterministic hash→text recovery lookup for the cached action (03-RESEARCH Pattern 3).

2. `requests.status` union — add `v.literal("scanning")` and `v.literal("blocked")` (now 13 literals). Update the "11 observable pipeline stages" comments to 13.

3. New table (default-on-read; a missing row means defaults with the switch OFF — zero seed):
```typescript
// GRDL-06 kill switch + per-request budget. SINGLE row, upserted by
// guardrails.setKillSwitch; read with .first() (≤1 row — no index needed).
guardrailConfig: defineTable({
  killSwitch: v.boolean(),
  budgetUsdPerRequest: v.number(),
  updatedAt: v.number(),
}),
```

In pipeline.ts: add the SAME two literals to `REQUEST_STATUS` (kept in sync with schema.ts by the existing comment contract). Touch NOTHING else in pipeline.ts — the workflow rewiring is plan 03-04's.

State in the summary explicitly: no migration needed — all additions are optional fields, new union literals, or a default-on-read table (Pitfall 7).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck && pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>Schema pushes clean (dev deployment accepts it); by_tenant_safeTextHash index exists; existing backend tests all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: "blocked" telemetry outcome end-to-end</name>
  <files>packages/core/src/buildTelemetry.ts, packages/core/src/buildTelemetry.test.ts, packages/backend/convex/telemetry.ts</files>
  <behavior>
    - buildTelemetry({ reviewOutcome: "blocked", durationMs, decisionCounts: {}, regenerateCount: 0, usages: [] }) returns a complete TelemetryRow with reviewOutcome "blocked" and null token/cost fields (no LLM ran before a governed stop)
    - the existing all-outcomes loop test covers "blocked" alongside sent/rejected/expired/failed
  </behavior>
  <action>
1. packages/core/src/buildTelemetry.ts: add `"blocked"` to the `ReviewOutcome` union (a governed guardrail stop is a first-class terminal — GRDL-01's fail-closed outcome).
2. buildTelemetry.test.ts: extend the outcome-loop test arrays to include "blocked" (write the test change first, watch it fail on the type, then apply 1).
3. telemetry.ts: add `v.literal("blocked")` to the `terminalOutcome.reviewOutcome` v.union. The schema column is v.string() — no schema change.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test && pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>blocked is a legal ReviewOutcome in the package, the validator, and the tests; core suite green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend typecheck` and `pnpm --filter @pikar/core test` green
- package.json pins action-cache at exactly 0.3.1
- dev deployment accepts the schema push (watch the `convex dev` output banner, not exit codes)
</verification>

<success_criteria>
- components.actionCache generated and typed
- requests can persist redaction output and be found by (tenantId, safeTextHash)
- scanning/blocked statuses and blocked telemetry outcome compile everywhere
- No migration was created (all changes optional/default-on-read)
</success_criteria>

<output>
After completion, create `.planning/phases/03-guardrails/03-02-SUMMARY.md`
</output>
