---
phase: 29-unified-knowledge-and-routines
plan: 12
type: execute
wave: 8
depends_on: ["29-11"]
files_modified:
  - packages/core/src/routineSchedule.ts
  - packages/core/src/routineSchedule.test.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/routines.ts
  - packages/backend/convex/routines.test.ts
  - packages/backend/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [ROUT-02]
must_haves:
  truths:
    - "Deferred branch proves recurrence tables, scheduler calls, controls and dependency are absent."
    - "Enabled branch self-schedules one-shot local occurrences with unique run claims, no overlap/catch-up, bounded retry/cost and per-run external approval."
    - "No scheduler, schema or dependency change begins until the decision parser validates the selected branch; invalid enable-safe fails closed to defer."
  artifacts:
    - path: packages/backend/convex/routines.test.ts
      provides: Conditional absence or scheduler state-machine proof
  key_links:
    - from: packages/backend/convex/routines.ts
      to: packages/core/src/routineSchedule.ts
      via: enabled branch next-occurrence/run-key decisions
      pattern: "nextOccurrence|runKey"
---
<objective>Execute the recurrence decision: prove absence when deferred, or implement the safe backend when enabled.

Purpose: make conditional scope executable without weakening the manual baseline.
Output: affirmative deferral proof or tested recurrence state machine.</objective>
<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md
@.planning/phases/29-unified-knowledge-and-routines/29-11-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce the selected branch</name>
  <files>packages/core/src/routineSchedule.test.ts, packages/core/src/routineSchedule.ts, packages/backend/convex/schema.ts, packages/backend/convex/routines.test.ts, packages/backend/convex/routines.ts, packages/backend/package.json, pnpm-lock.yaml</files>
  <action>Before touching schema, scheduler code, package manifests or the lockfile, run `check-routine-gate.mjs --validate-decision`. If the artifact says enable-safe but validation is nonzero, fail closed by recording defer and make no recurrence implementation edits. If `defer`, add/execute a static absence test asserting no routines table/module/UI route, dynamic scheduler call or Temporal dependency, then make no implementation edits. Only a parser-green `enable-safe` branch may install the exact polyfill, add tenant routine/run tables once, and implement atomic one-shot arm/claim with unique `{routineId, localOccurrence, templateVersion}` key, one active run, skip missed/overlap, readiness/auth auto-pause, bounded retry, cost reserve/settle, pause/revoke/tombstone and refs/counts audit. External outputs only create proposed plans.</action>
  <verify><automated>node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --validate-decision &amp;&amp; pnpm --filter @pikar/core test -- routineSchedule &amp;&amp; pnpm --filter @pikar/backend test -- routines schema guardrails</automated></verify>
  <done>Deferred has proven structural absence, or enabled has a mutation-verified tenant state machine with no autonomous external write.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove scheduler, OAuth and replay races</name>
  <files>packages/backend/convex/routines.test.ts</files>
  <action>Deferred: run the absence proof and document manual baseline as final. Enabled: use fake timers/scheduled-function draining for duplicate callbacks, concurrent claims, pause before/after start, stale version/material change, reauth auto-pause, reconnect no catch-up, explicit resume next-future time, transient/permanent retry, reserve/settle replay and two-tenant isolation. Mutation-test unique claim and status recheck.</action>
  <verify><automated>node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --validate-decision &amp;&amp; pnpm --filter @pikar/backend test -- routines routineDecision gmailAuth guardrails</automated></verify>
  <done>The selected outcome is fully automated and cannot be mistaken for the other branch.</done>
</task>
</tasks>
<verification>- Conditional focused tests, backend typecheck and watcher pass.</verification>
<success_criteria>- ROUT-02 remains true under either approved branch.</success_criteria>
<output>Create `29-12-SUMMARY.md` and state `recurrence: deferred|enabled-safe` prominently.</output>
