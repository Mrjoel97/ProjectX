---
phase: 29-unified-knowledge-and-routines
plan: 08
type: execute
wave: 5
depends_on: ["29-07"]
files_modified:
  - packages/backend/convex/pinnedWorkflows.ts
  - packages/backend/convex/pinnedWorkflows.test.ts
  - packages/backend/convex/cockpit.ts
  - apps/web/app/(app)/dashboard/workflows/PinnedWorkflowButton.tsx
  - apps/web/app/(app)/dashboard/workflows/PinnedWorkflowButton.test.tsx
  - apps/web/app/(app)/dashboard/workflows/page.tsx
autonomous: true
requirements: [ROUT-02]

must_haves:
  truths:
    - "A pinned workflow captures exact approved template/candidate lineage and performs a fresh readiness check on every manual run."
    - "Run again creates a fresh cockpit request/correlation and can never replay an old plan or approval."
    - "Manual reruns remain available whether recurrence is enabled or deferred."
  artifacts:
    - path: packages/backend/convex/pinnedWorkflows.ts
      provides: Tenant pin CRUD/readiness/fresh-rerun adapter
    - path: apps/web/app/(app)/dashboard/workflows/PinnedWorkflowButton.tsx
      provides: Pin and Run again controls with readiness states
  key_links:
    - from: packages/backend/convex/pinnedWorkflows.ts
      to: packages/backend/convex/cockpit.ts
      via: fresh sendCockpitMessage request path
      pattern: "sendCockpitMessage"
    - from: apps/web/app/(app)/dashboard/workflows/PinnedWorkflowButton.tsx
      to: packages/backend/convex/pinnedWorkflows.ts
      via: explicit listPins/pinWorkflow/unpinWorkflow/checkReadiness/runAgain APIs mounted by workflows/page.tsx
      pattern: "(pinWorkflow|runAgain)"
---

<objective>
Ship the safe manual routine baseline: a version-pinned workflow that always starts a fresh governed run.

Purpose: satisfy ROUT-02 without assuming recurrence and collect real repeat-use evidence.
Output: tenant pin/readiness/rerun backend plus accessible Pin and Run again controls.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-unified-knowledge-and-routines/29-05-SUMMARY.md
@packages/backend/convex/cockpit.ts
@docs/design/BRAND.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement tenant pins and fresh reruns</name>
  <files>packages/backend/convex/pinnedWorkflows.test.ts, packages/backend/convex/pinnedWorkflows.ts, packages/backend/convex/cockpit.ts</files>
  <action>Expose tenant-scoped `listPins`, `pinWorkflow`, `unpinWorkflow`, `checkReadiness`, and `runAgain` APIs. Pin exact template ID/version, active tenant candidate version, customization hash, safe input defaults and source preferences. Before each run resolve current code-owned tools, candidate/eval status, connections and budget; name blockers before spend. Re-fire through the existing public/internal twin of `sendCockpitMessage` with a new request/correlation/thread as appropriate. Never copy/reopen an approved plan. Emit pin/run refs, readiness counts, repeat ordinal, outcome/cost/latency only.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- pinnedWorkflows cockpit audit telemetry</automated></verify>
  <done>Two runs produce distinct correlations/plans and both cross current readiness and approval gates.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add accessible Pin and Run again controls</name>
  <files>apps/web/app/(app)/dashboard/workflows/PinnedWorkflowButton.tsx, apps/web/app/(app)/dashboard/workflows/PinnedWorkflowButton.test.tsx, apps/web/app/(app)/dashboard/workflows/page.tsx</files>
  <action>Mount `PinnedWorkflowButton` for each eligible active pack on `workflows/page.tsx`; wire it explicitly to `listPins`, `pinWorkflow`, `unpinWorkflow`, `checkReadiness`, and `runAgain`. Show exact version, connection readiness and last/manual-run outcome. Pin/unpin and Run again have honest pending/refused/reauth/eval-stale states, keyboard/focus behavior and no recurring language. Running navigates to the fresh workspace conversation/plan.</action>
  <verify><automated>pnpm --filter @pikar/web test -- pinned-workflows &amp;&amp; pnpm --filter @pikar/web typecheck</automated></verify>
  <done>The safe repeat workflow is obvious, accessible and never implies unattended execution.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- pinnedWorkflows cockpit`
- `pnpm --filter @pikar/web test -- pinned-workflows`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Manual repeat use works end-to-end through fresh governance.
- No scheduler, recurrence rule or standing approval is involved.
</success_criteria>

<output>Create `29-08-SUMMARY.md`.</output>
