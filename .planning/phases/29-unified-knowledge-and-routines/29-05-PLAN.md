---
phase: 29-unified-knowledge-and-routines
plan: 05
type: execute
wave: 3
depends_on: ["29-04"]
files_modified:
  - packages/core/src/workflowCustomization.ts
  - packages/core/src/workflowCustomization.test.ts
  - packages/backend/convex/workflowPacks.ts
  - packages/backend/convex/workflowPacks.test.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/evaluations.ts
autonomous: true
requirements: [ROUT-01]

must_haves:
  truths:
    - "Users customize only approved versioned template fields and can never submit tool grants, code, secrets, URLs or MCP configuration."
    - "Every save creates or reuses an immutable tenant candidate with exact template lineage, diff hash, author, eval evidence and rollback target."
    - "Activation and rollback use Phase 21's one tenant eval choke point."
  artifacts:
    - path: packages/backend/convex/workflowPacks.ts
      provides: Validated template customization to Phase 21 candidate adapter
    - path: packages/backend/convex/workflowPacks.test.ts
      provides: Candidate-only, isolation, stale-base and no-tool-widening proof
  key_links:
    - from: packages/backend/convex/workflowPacks.ts
      to: Phase 21 tenant candidate API
      via: candidate insert/eval/activation/rollback exports proven by 29-01
      pattern: "candidate"
---

<objective>
Turn approved Phase 27 templates into schema-driven tenant candidates through Phase 21's existing gate.

Purpose: replace generic prompt editing with safe business customization while preserving immutable lineage and code-owned tools.
Output: pure schema/rendering refinements, tenant backend adapter and eval integration tests.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-unified-knowledge-and-routines/29-01-SUMMARY.md
@.planning/phases/29-unified-knowledge-and-routines/29-DEPENDENCY-EVIDENCE.md
@docs/playbooks/skill-registry.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Compile approved customization schemas deterministically</name>
  <files>packages/core/src/workflowCustomization.test.ts, packages/core/src/workflowCustomization.ts</files>
  <action>Load Phase 27 template manifests and define closed typed fields for terminology, tone, thresholds, source preferences and bounded instruction blocks. Validate lengths/enums/numbers and deterministically render against the exact template version. Reject extra properties, tool names, executable/code fields, secrets, remote URLs and MCP/server settings structurally. Compute normalized diff/material-change hash without logging content.</action>
  <verify><automated>pnpm --filter @pikar/core test -- workflowCustomization</automated></verify>
  <done>Legal values render repeatably; every authority-widening or stale/unknown field fails closed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Publish tenant candidates through the Phase 21 choke point</name>
  <files>packages/backend/convex/workflowPacks.test.ts, packages/backend/convex/workflowPacks.ts, packages/backend/convex/skills.ts, packages/backend/convex/evaluations.ts</files>
  <action>Accept only `{templateId, templateVersion, baseCandidateVersion, values}` from a tenant wrapper. Resolve the approved manifest server-side, render in pure code and call Phase 21's tenant candidate-only writer. Persist provenance beside the candidate: upstream manifest/hash, author user ref, diff hash, previous active/rollback target and evidence refs. Use optimistic concurrency on the base version. Reuse exact-version held-out eval, activation and rollback; add no status flip and never touch ADR-003 global rows.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- workflowPacks skills evaluations &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Two tenants can customize the same pack independently; candidates cannot self-activate or widen tools.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/core test -- workflowCustomization`
- `pnpm --filter @pikar/backend test -- workflowPacks skills evaluations`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- ROUT-01 backend path is immutable, tenant-scoped, lineage-complete and eval-gated.
- Raw prompt/tool editing is structurally absent.
</success_criteria>

<output>Create `29-05-SUMMARY.md`.</output>
