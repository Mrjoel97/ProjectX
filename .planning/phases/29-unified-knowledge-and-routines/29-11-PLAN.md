---
phase: 29-unified-knowledge-and-routines
plan: 11
type: execute
wave: 7
depends_on: ["29-10"]
files_modified:
  - packages/core/src/routineSchedule.ts
  - packages/core/src/routineSchedule.test.ts
  - packages/backend/convex/routineDecision.test.ts
  - packages/backend/scripts/check-routine-gate.mjs
  - .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md
  - docs/decisions/013-standing-routine-governance.md
autonomous: false
requirements: [ROUT-02]
must_haves:
  truths:
    - "Recurrence has an explicit approve/reapproval, OAuth, DST, provider-read, missed-run, identity, overlap, retry, cost, pause/revoke and audit decision before schema or scheduler code."
    - "A failed or unproven row selects defer and leaves manual reruns as the completed deliverable."
    - "Enable-safe is selectable only when a machine parser confirms every closed matrix row is green and required OAuth, DST and provider rows carry live evidence."
  artifacts:
    - path: .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md
      provides: Evidence matrix and enable-safe/defer verdict
    - path: packages/backend/scripts/check-routine-gate.mjs
      provides: Fail-closed recurrence eligibility and decision validator
  key_links:
    - from: .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md
      to: 29-12-PLAN.md
      via: machine-readable `decision: enable-safe|defer` frontmatter
      pattern: "decision:"
---
<objective>Resolve the recurrence governance/proof gate without presuming a scheduler ships.

Purpose: make deferral a valid, verified result and prevent “routine means cron.”
Output: Temporal/runtime spike, evidence matrix, blocking owner decision and immutable ADR/deferral record.</objective>
<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>
<context>
@.planning/phases/29-unified-knowledge-and-routines/29-RESEARCH.md
@.planning/design/scheduled-send.md
@docs/playbooks/cockpit.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Produce a closed, machine-checked recurrence matrix</name>
  <files>packages/core/src/routineSchedule.test.ts, packages/core/src/routineSchedule.ts, packages/backend/convex/routineDecision.test.ts, packages/backend/scripts/check-routine-gate.mjs, .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md</files>
  <action>Spike `@js-temporal/polyfill` in the Convex runtime without committing it unless enable is chosen. Test IANA fixed/New York/Berlin spring/fall semantics, skip-missed default, stable occurrence key, overlap/retry/material-change classifiers. Create a closed YAML matrix in the decision artifact where every declared row is `{id, status: pass|fail|missing, evidenceType: automated|live|manual, evidenceRef}`. Required rows include `oauth-expiry-reauth`, `dst-boundary`, and `provider-read`. Implement `check-routine-gate.mjs`: `--matrix` validates closed schema/enumerations even with red rows; `--eligibility` exits zero only when all rows are `pass`, every ref is non-empty, and those three required rows use `live` evidence; absent/malformed/unknown rows fail closed. `--validate-decision` accepts a correctly recorded `defer`, but applies the same green eligibility check to `enable-safe`.</action>
  <verify><automated>pnpm --filter @pikar/core test -- routineSchedule &amp;&amp; pnpm --filter @pikar/backend test -- routineDecision gmailAuth cockpit &amp;&amp; node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --matrix</automated></verify>
  <done>The closed matrix parses; a missing or non-live required row cannot yield enable-safe.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify required live recurrence evidence</name>
  <files>.planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md</files>
  <action>Pause for an owner to resolve and inspect the three required live evidence references. Do not synthesize, simulate or relabel missing evidence as live.</action>
  <what-built>A closed recurrence evidence matrix and fail-closed parser.</what-built>
  <how-to-verify>Inspect the real OAuth expiry/reauth trace, a real DST-boundary execution trace and a real connected-provider read trace. Confirm each evidence reference resolves to the cited run and that unavailable or simulated evidence remains fail/missing, never live-pass.</how-to-verify>
  <verify><automated>node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --matrix</automated></verify>
  <done>The owner either verifies all required live refs or leaves their rows non-green so enable-safe stays hidden.</done>
  <resume-signal>Type "live evidence verified" or describe invalid/missing evidence</resume-signal>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Select the recurrence outcome under the parser gate</name>
  <files>.planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md</files>
  <action>Run eligibility immediately before presenting the decision. When nonzero, offer defer only. When zero, offer defer and enable-safe with their consequences.</action>
  <decision>Select recurrence outcome after reviewing the complete evidence matrix.</decision>
  <context>Immediately run the parser before presenting the choice. Show `enable-safe` only when `node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --eligibility` exits zero; otherwise expose only `defer`. Enable-safe permits standing approval only for read/in-app preparation; every external write materializes a per-run proposed plan.</context>
  <options>
    <option id="defer"><name>Defer recurrence (recommended if any proof is missing)</name><pros>Preserves governance and completes ROUT-02 honestly with manual reruns.</pros><cons>No unattended preparation yet.</cons></option>
    <option id="enable-safe"><name>Enable safe recurrence</name><pros>Automates read/in-app preparation under a standing template.</pros><cons>Adds state/scheduler/OAuth/DST operations; external writes still require each run's approval.</cons></option>
  </options>
  <verify><automated>node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --matrix</automated></verify>
  <done>A permitted option is selected; enable-safe was impossible unless eligibility exited zero.</done>
  <resume-signal>Select: defer or enable-safe</resume-signal>
</task>
<task type="auto">
  <name>Task 4: Record and revalidate the binding recurrence decision</name>
  <files>.planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md, docs/decisions/013-standing-routine-governance.md</files>
  <action>Write machine-readable decision frontmatter and complete rationale, then run `--validate-decision`. If an enable-safe artifact fails validation, rewrite it to defer before any downstream work. On defer, do not create the ADR or install a dependency; record explicit absence checks. On enable-safe, add a new ADR (never edit accepted ADRs), commit the proven Temporal choice and lock reapproval/auth/time/missed/idempotency/overlap/retry/cost/pause/audit semantics. This plan never edits schema or scheduler modules.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- routineDecision &amp;&amp; node packages/backend/scripts/check-routine-gate.mjs .planning/phases/29-unified-knowledge-and-routines/29-RECURRENCE-DECISION.md --validate-decision</automated></verify>
  <done>Exactly one binding outcome exists and downstream plans can branch without guessing.</done>
</task>
</tasks>
<verification>- The parser validates `defer`, or validates enable-safe only with all rows green and live OAuth/DST/provider evidence; no schema or scheduler file is touched.</verification>
<success_criteria>- No recurrence implementation can start without a proved, owner-approved enable-safe record.</success_criteria>
<output>Create `29-11-SUMMARY.md`.</output>
