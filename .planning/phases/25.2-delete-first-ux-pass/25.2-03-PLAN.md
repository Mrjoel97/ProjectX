---
phase: 25.2-delete-first-ux-pass
plan: 03
type: execute
wave: 3
depends_on: ["25.2-02"]
files_modified:
  - apps/web/app/page.tsx
  - apps/web/app/homeEntry.test.ts
autonomous: true
requirements: [G16]
must_haves:
  truths:
    - "The home page says the beta is private and by invitation before the first button"
    - "'Request access' opens the existing waitlist door at /signup, not a mail client"
    - "'Sign in' and 'See how it works' are unchanged"
  artifacts:
    - path: "apps/web/app/homeEntry.test.ts"
      provides: "source scan: the CTA row has no mailto and links to /signup; the eyebrow names the invitation"
  key_links:
    - from: "page.tsx CTA row"
      to: "/signup Waitlist door (signup/page.tsx:250)"
      via: "plain href"
---

<objective>
Item 10 of 25.2-RESEARCH.md. Owner decision 3 (invite-only) makes this copy: say so, and send strangers to the form that already exists. The footer mailto stays — it is contact, not admission.
</objective>

<context>
@.planning/phases/25.2-delete-first-ux-pass/25.2-RESEARCH.md
@docs/design/BRAND.md §1 (executive, plain, honest)
`apps/web/app/page.tsx` has no playbook owner in watch.json; nothing to bump. The Google consent sentence (G16's other half) already exists on connect-gmail/page.tsx — no change.
</context>

<tasks>
1. page.tsx: eyebrow "Private beta · by invitation"; CTA "Request access" → `href="/signup"`; keep the other two CTAs.
2. homeEntry.test.ts: source scan on the hero block.
3. `pnpm --filter @pikar/web test`, typecheck, biome.
</tasks>
