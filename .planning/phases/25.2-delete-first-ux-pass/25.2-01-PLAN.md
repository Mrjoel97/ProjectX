---
phase: 25.2-delete-first-ux-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(app)/layout.tsx
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx
  - apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts
  - apps/web/app/(app)/dashboard/profile/connections.ts
  - apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx
  - apps/web/app/(app)/dashboard/profile/connections.test.ts
  - packages/core/src/connectionsSurface.test.ts
  - apps/web/app/(app)/dashboard/workspace/ChatPane.tsx
  - apps/web/app/(app)/dashboard/workspace/page.tsx
  - apps/web/app/(app)/deleteFirst.test.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/onboarding.md
autonomous: true
requirements: [G14]
must_haves:
  truths:
    - "A non-owner's rail has no 'Soon' placeholder and no Compliance entry; the owner's still has Compliance with its dead-letter badge"
    - "The delivery report never prints a raw status token; every status the badge colours has a label"
    - "No approvals string says tenant, deployment, or scheduler; an unknown refusal code is shown as words"
    - "The Connections page shows nothing it cannot connect; the three 'Not available' rows are gone"
    - "The composer has no disabled model pill; 'Adapt a business skill' is owner-only"
  artifacts:
    - path: "apps/web/app/(app)/deleteFirst.test.ts"
      provides: "One source-scan suite pinning items 1-7 (the pattern approvalsView.test.ts already uses for inline JSX)"
  key_links:
    - from: "layout.tsx NAV"
      to: "api.owner.viewer"
      via: "Compliance entry filtered by isOwner in the Shell"
    - from: "cards.tsx delivery rows"
      to: "DELIVERY_STATUS"
      via: "label lookup with the enum as the fallback key, never rendered raw"
---

<objective>
Items 1-7 of 25.2-RESEARCH.md: delete the placeholder and the dead pill, hide operator surfaces from non-owners, and turn four developer strings and one raw enum into plain words. No layout or design change.
</objective>

<context>
@.planning/phases/25.2-delete-first-ux-pass/25.2-RESEARCH.md
@docs/design/BRAND.md (§1 voice: second person, plain, honest; §6 accessibility)
@docs/playbooks/dashboard-pages.md, @docs/playbooks/cockpit.md, @docs/playbooks/onboarding.md

Notes for the executor:
- `api.owner.viewer` is already the client owner flag (admin/page.tsx, FinanceTabs.tsx, workspace/page.tsx:437). Reuse it; do not add a second one.
- `packages/core/src/connectionsSurface.test.ts` has FIVE describes; only "the blocked rows are information, not decoration" scans BLOCKED. Delete that describe alone. `connections.test.ts`'s databases case exists only to check a BLOCKED string; delete that test, keep the file.
- Keep `badge()`'s colour map keyed on the enum; only the rendered text changes.
- Tests: source scans over the component files (readFileSync + regex), plus pure-function assertions on `refusalMessage`. No live Convex hooks are rendered.
</context>

<tasks>
1. layout.tsx: remove the `soon` NAV entry and the `is-soon` branch; gate the Compliance entry (and DeadLetterBadge) on `useQuery(api.owner.viewer).isOwner === true`.
2. cards.tsx: add `DELIVERY_STATUS` beside `badge()`; render `DELIVERY_STATUS[r.status] ?? r.status.replaceAll("_", " ")`.
3. ApprovalsView.tsx: rewrite the three strings and the refusal fallback per RESEARCH item 4.
4. connections.ts / ConnectionsPanel.tsx: delete BLOCKED and its render block; fix the two tests.
5. ChatPane.tsx: delete the disabled Auto pill. page.tsx: wrap the "Adapt a business skill" menu item in the owner check.
6. deleteFirst.test.ts: one describe per item, source scans; run `pnpm --filter @pikar/web test` and `pnpm --filter @pikar/core test`.
7. Bump the three playbooks in the same commit.
</tasks>
