---
phase: 25.2-delete-first-ux-pass
plan: 02
type: execute
wave: 2
depends_on: ["25.2-01"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/SplitPane.tsx
  - apps/web/app/(app)/dashboard/workspace/splitPane.test.ts
  - apps/web/app/(app)/layout.tsx
  - apps/web/app/globals.css
  - docs/playbooks/cockpit.md
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [G15]
must_haves:
  truths:
    - "Below 48rem the cockpit shows ONE pane at full width, chat first, with a visible 'Show work' / 'Back to chat' toggle; at 48rem and above nothing changes"
    - "Below 48rem the icon rail is gone and a four-item labelled bottom bar (Command Center, Approvals, My Workspace, Knowledge Vault) navigates; the approvals badge is on it"
    - "Keyboard and pointer resizing of the split pane still work at desktop widths"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/splitPane.test.ts"
      provides: "paneLayout(narrow, showWork) truth table + a source scan that the toggle is rendered only in the narrow branch"
  key_links:
    - from: "SplitPane"
      to: "window.matchMedia('(max-width: 48rem)')"
      via: "a `narrow` state set in useEffect (SSR-safe: false on the server)"
    - from: "layout.tsx tabbar"
      to: "NAV"
      via: "filter by the four hrefs, so a renamed rail entry renames the tab"
---

<objective>
Items 8-9 of 25.2-RESEARCH.md: a phone gets a usable chat and four labelled tabs. CSS media query plus one state flag; no new component library, no new dependency, no change at desktop widths.
</objective>

<context>
@.planning/phases/25.2-delete-first-ux-pass/25.2-RESEARCH.md
@docs/design/BRAND.md (§4 layout, §6 accessibility: 44px targets, visible focus, aria-current)
@docs/playbooks/cockpit.md (SplitPane invariants: per-user persisted split, 20-80 clamp, keyboard resize)

Notes for the executor:
- `globals.css` already has a `@media (max-width: 48rem)` block at ~794 that shrinks the rail to icons. Replace its body: hide `.rail`, show `.tabbar`, pad `.app-frame` bottom by the bar height. Tokens only (`--card`, `--rule`, `--ink`, `--ink-soft`, `--accent`), no hex.
- SplitPane: export `paneLayout(narrow: boolean, showWork: boolean)` returning `{ showLeft, showRight, columns }`; the component uses it. The toggle is a `.composer-pill`-styled button with `aria-pressed`.
- The tab bar is a `<nav aria-label="Primary (compact)">` rendered by the Shell beside the rail, always in the DOM, hidden by CSS above 48rem. Reuse `isActive` and `ApprovalsBadge`.
</context>

<tasks>
1. SplitPane.tsx: `paneLayout` helper, `narrow` state via matchMedia, the toggle, grid columns from the helper.
2. layout.tsx: `TABBAR_HREFS` (four), `<nav className="tabbar">` mapped from NAV, `aria-current="page"` on the active tab.
3. globals.css: `.tabbar` styles + the rewritten 48rem block.
4. splitPane.test.ts: truth table for `paneLayout`; source scan that `matchMedia("(max-width: 48rem)")` and the toggle exist; layout source scan that the tab bar has exactly four hrefs and each is in NAV.
5. Bump cockpit.md and dashboard-pages.md.
</tasks>
