---
phase: 25.2-delete-first-ux-pass
plan: 02
status: complete
completed: 2026-09-05
commits: [319badb]
requirements-completed: [G15]
---

# 25.2-02 — Items 8-9: the cockpit on a phone

**What changed:**
- `workspace/SplitPane.tsx`: `paneLayout(narrow, showWork, pct)` decides; `narrow` is a live `matchMedia("(max-width: 48rem)")` state read after mount (SSR renders the desktop grid). Below 48rem one pane shows at a time — chat first, "Show work" / "Back to chat" toggle with `aria-pressed`. Both panes stay mounted and are toggled with `hidden`, so drafts and subscriptions survive; the drag handle is hidden while narrow. Desktop behaviour unchanged.
- `layout.tsx`: `TABBAR_HREFS` (Command Center, Approvals, My Workspace, Knowledge Vault) rendered as `<nav className="tabbar">` from NAV with `aria-current="page"` and the approvals badge.
- `globals.css`: `.tabbar` styles (fixed, 3.6rem, 44px targets, the rail's gradient family); the old 48rem block that shrank the rail to 15 icons now hides `.rail`, shows `.tabbar`, and shortens `.app-frame`; the `is-soon` / `rail-soon` rules left by 25.2-01 deleted.

**Pinned by:** `workspace/splitPane.test.ts` (truth table + source scans, 6 tests).
**Measured:** web 901/901 (+6), web `tsc` 0, biome errors 0 (the 14 `noDescendingSpecificity` warnings in globals.css pre-date this plan).
**Not done, deliberately:** no redesign of either pane's contents at narrow widths; the tab bar has no "more" item — the other pages stay reachable by URL and from the Command Center.
