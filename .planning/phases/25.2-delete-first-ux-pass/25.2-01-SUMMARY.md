---
phase: 25.2-delete-first-ux-pass
plan: 01
status: complete
completed: 2026-09-05
commits: [1ead554]
requirements-completed: [G14]
---

# 25.2-01 — Items 1-7: deletions and copy

**What changed (all `apps/web` unless noted):**
1. `layout.tsx`: "Join Community · Soon" deleted; every NAV entry has an href; Compliance is `ownerOnly` and the rail filters on `api.owner.viewer` (the flag admin and Finance already used). The URL and `/ops` still resolve for the owner.
2. `workspace/cards.tsx`: `deliveryStatusLabel()` — the delivery report shows words; the enum keeps the badge colour.
3. `approvals/ApprovalsView.tsx`: "This tenant's daily budget" → "Your daily budget is used up for today"; "The deployment budget is paused" → "Pikar's shared daily budget is paused for today"; "The scheduler won the race…" → "This email had already started sending, so its time could not be moved"; an unmapped refusal code is humanised.
4. `profile/connections.ts` deleted; the three "Not available" rows removed from `ConnectionsPanel.tsx` AND `SegmentAnatomy.tsx` (the research named one surface; the typecheck found the second). Two tests that existed only to scan those rows removed (`connections.test.ts` databases case, `packages/core/src/connectionsSurface.test.ts` blocked-rows describe). `SEGMENT_BLOCKED` in `segmentCopy.ts` is now an unused export, left for the next pass.
5. `workspace/ChatPane.tsx`: the disabled "Auto" model pill deleted.
6. `workspace/page.tsx`: "Adapt a business skill" renders only for the owner.

**Pinned by:** `apps/web/app/(app)/deleteFirst.test.ts` (8 tests, source scans in the approvalsView.test.ts idiom).
**Measured:** web 895/895 (+8), core 1495/1495 (−6 deleted), web + core `tsc` 0, biome errors 0.
**Playbooks:** dashboard-pages.md, cockpit.md, onboarding.md bumped in the same commit.
**Deviation from plan:** SegmentAnatomy.tsx added to `files_modified` (second consumer of BLOCKED).
