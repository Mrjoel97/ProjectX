---
phase: 37-planning-corpus-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/archive/**
  - scripts/check-planning.mjs
  - .claude/settings.json
  - docs/playbooks/watch.json
  - docs/playbooks/ci-gate.md
autonomous: true
requirements: []
---

# 37-01 — One repair pass over the planning corpus, then a rule that keeps it honest

**Source:** `37-RESEARCH.md` (measured 2026-09-06) and the four owner decisions of the same day:
archive STATE history · tick bucket A only, annotate bucket B `(open: …)` · archive the loose files ·
keep the ignored local PDFs.

## Tasks

1. **STATE.md** → exactly one frontmatter block regenerated from the progress table (phase and plan
   counts), a body under 80 lines (where the truth lives, last three closes, next in the merged
   order, session continuity, the one-block rule); the whole prior file copied unchanged to
   `.planning/archive/STATE-history-2026-09-06.md`.
2. **ROADMAP.md** → a Phase 37 block; the progress table regenerated in numeric order from the phase
   directories (one row per heading; rows that were already consistent kept verbatim; the seven
   stale rows, Phase 9 and Phase 31 rebuilt; partial phases name their open plans); the "NOT
   AUTHORITATIVE" banner replaced by the two-line contract the hook enforces. Heading blocks are NOT
   re-ordered — their order is insertion history and the table is the index.
3. **REQUIREMENTS.md** → bucket A ticked (checkbox + traceability row "Complete (2026-09-06, Phase 37
   repair) — <evidence>"); bucket B checkbox lines annotated `*(open: <one line>)*`; bucket C
   untouched. Bucket A shrank from 15 to 12 on the evidence pass (VALT-13 `human_needed`, VALT-15
   deferred to an unrun 20.1-02, BETA-01 declined by 25-02, BETA-02 unproven without BETA-05).
4. **Archive** → `git mv` of CONTEXT-HANDOFF, WAVE-0, PARALLELIZATION, the three v2.0 audit files,
   `debug/`, `todos/`, `codebase/` under `.planning/archive/` with a README table.
5. **Rule** → `scripts/check-planning.mjs` (four checks, see the script header) as a second Stop hook
   in `.claude/settings.json`; both hook scripts registered under `ci-gate.md` in `watch.json`;
   `ci-gate.md` Last-verified block.
6. **Verify** → the script exits 0 on the repaired tree; in a scratch copy each of five mutations
   (a stacked block, a heading without a row, a Complete row over an open plan, a summarised
   directory behind a non-Complete row, a Complete phase with a Pending requirement and no note)
   exits 1 with exactly its own line.

## Not in scope

Re-ordering the 64 heading blocks; running the hook in CI; touching any code path; the three
git-ignored PDFs.
