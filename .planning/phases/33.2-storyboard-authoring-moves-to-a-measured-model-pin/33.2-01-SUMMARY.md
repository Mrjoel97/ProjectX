# 33.2-01 — SUMMARY

**Status:** COMPLETE. **B1, B2, B3 met.** **Wave:** 1 · **Branch:** `feat/28.1-billing-mapping` (main tree).
**Commit:** `bbaf034` — `feat(33.2-01): the media director bills its own model lane, and a storyboard retry stops re-buying its research`.

## What shipped

- **The fourth lane.** `packages/cost/src/cost.ts`: `OR_MEDIA_MODEL` / `OR_MEDIA_FALLBACK_MODEL`
  plus the `MEDIA_MODEL` / `MEDIA_FALLBACK_MODEL` aliases beside PACK, exported from `index.ts`.
  Shipped ALIASED (default / research-fallback) so the commit moved no bill; no PRICING row of its
  own (computed-key collapse rule). `cost.test.ts`: both priced, fallback off the cheap tier — and
  deliberately NO `!== DEFAULT_MODEL` inequality until the pin moved (33.2-03 added it).
- **The lookup arm.** `convex/llm.ts` `runSpecialistTurn`: `media-director` →
  `[MEDIA_MODEL, MEDIA_FALLBACK_MODEL]`. `dispatch.test.ts` MODEL PIN test pins all three lanes
  and holds the tripwires.
- **Research reuse (L5).** `research.persistFindings` writes `researchQuestionHash`
  (`contentHash(question)`); `research.recentFindingsForQuestion` answers "do `web_research`
  findings for this hash exist on this tenant since `sinceMs`"; `dispatch.groundMediaBrief` skips
  the grounding pass when they do (`FINDINGS_REUSE_MS = 24 h`). Schema: `vaultDocuments.researchQuestionHash`
  optional. Three reuse tests in `dispatch.test.ts` (`groundTwice`): same brief reuses, a
  different brief re-buys, a stale finding re-buys.

## Measured

cost 95/95 · dispatch / cockpitTools / research green · tsc 0 outside the connector lane.
Playbooks: guardrails.md (the lane), cockpit.md (the lookup arm and reuse).
