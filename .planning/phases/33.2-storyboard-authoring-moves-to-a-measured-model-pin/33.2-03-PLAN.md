---
phase: 33.2-storyboard-authoring-moves-to-a-measured-model-pin
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/cost/src/cost.ts
  - packages/cost/src/cost.test.ts
  - docs/decisions/032-the-storyboard-model-is-chosen-by-a-bake-off-on-the-production-path.md
  - docs/playbooks/media.md
  - docs/playbooks/guardrails.md
  - .planning/phases/33.2-storyboard-authoring-moves-to-a-measured-model-pin/33.2-BAKEOFF.md
autonomous: false
requirements: [B6, B7]

user_setup:
  - service: openrouter
    why: "Four candidate rounds over 12 briefs x 2 passes: ~$2.5 research once + ~$3.5 media turns. Estimated total under $8."
    dashboard_config:
      - task: "Credits funded (owner confirmed topped up 2026-09-04)"
        location: "openrouter.ai -> Credits"

must_haves:
  truths:
    - "Four reports exist and the ADR's decision follows L4's rule from their numbers alone"
    - "The pin moved only if the winner beat the baseline by >= 3 of 24 passes with no clock truncation"
    - "One reel rendered on the chosen pin, owner-viewed"
---

<objective>
Run the bake-off, pin by the rule, record it, and prove the pin on one live reel.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Four rounds</name>
  <action>
    For each candidate in PRD L3 order: point OR_MEDIA_MODEL at it (adding a PRICING row verified
    against the 2026-09-04 catalogue for any id not yet priced), wait for `convex dev` to push, run
    `node scripts/run-storyboard-bakeoff.mjs --candidate <id> --repeat 2`. Keep every report.
  </action>
</task>

<task type="auto">
  <name>Task 2: The decision</name>
  <action>
    Apply L4. Write 33.2-BAKEOFF.md with the table and the arithmetic. Pin OR_MEDIA_MODEL at the
    winner (or restore the alias if none clears the threshold). If the pin moved, add the
    `MEDIA_MODEL !== DEFAULT_MODEL` inequality to cost.test.ts (the pack idiom). ADR-032. Playbooks.
  </action>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: One reel on the new pin</name>
  <action>Owner generates a reel from the cockpit; the storyboard proposes clean first time and the reel renders.</action>
</task>

</tasks>
