---
phase: 33.2-storyboard-authoring-moves-to-a-measured-model-pin
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/backend/scripts/run-storyboard-bakeoff.mjs
  - packages/backend/scripts/storyboard-briefs.json
  - packages/backend/convex/smoke.ts
  - packages/backend/convex/smoke.test.ts
  - docs/playbooks/watch.json
  - docs/playbooks/media.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [B4, B5]

must_haves:
  truths:
    - "--self-check validates the corpus with zero convex calls"
    - "The runner refuses a --candidate that is not the code-owned MEDIA_MODEL and fails a pass whose modelId differs"
    - "The facts query returns counts and codes only"
  artifacts:
    - path: "packages/backend/scripts/run-storyboard-bakeoff.mjs"
      provides: "the bake-off runner"
      contains: "codeOwnedMediaModel"
    - path: "packages/backend/scripts/storyboard-briefs.json"
      provides: "12 briefs"
  key_links:
    - from: "packages/backend/scripts/run-storyboard-bakeoff.mjs"
      to: "packages/backend/convex/dispatch.ts"
      via: "convex run dispatch:runMedia"
      pattern: "dispatch:runMedia"
---

<objective>
A runner that drives the production media dispatch for one candidate over the corpus and writes a
per-candidate report; a §4-clean query that reads the parser's verdict off the plan row.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: storyboardFactsForPlan</name>
  <files>packages/backend/convex/smoke.ts, packages/backend/convex/smoke.test.ts</files>
  <action>
    internalQuery({planId}) → { kind: "two"|"salvaged"|"refused"|"none", reason?, variation?,
    targetDurationSeconds?, sceneCount, altSceneCount, generatedSeconds, kinds: Record<visual,count>,
    unverifiedCount, adjustmentCount, lostReason? } derived from shots/altShots/proposalRefusal/
    lostVariation/deckAdjustments. Counts and codes only; no narration, prompt, title or description.
    One convex-test proving a landed two-deck row and a refused row read back correctly.
  </action>
  <verify>cd packages/backend && npx vitest run convex/smoke.test.ts -t storyboardFacts</verify>
</task>

<task type="auto">
  <name>Task 2: The runner and the corpus</name>
  <files>packages/backend/scripts/run-storyboard-bakeoff.mjs, packages/backend/scripts/storyboard-briefs.json, docs/playbooks/watch.json</files>
  <action>
    Flags: --candidate <id> (required for a live run; must equal the alias-resolved MEDIA_MODEL read
    off cost.ts), --repeat N (default 1), --only <substring>, --self-check, --dump.
    State file `.tmp/storyboard-bakeoff/tenants.json`: brief id → {tenantId, seeded docs}; a brief
    with no entry is seeded once (vaultSmoke:seedCorpus + smoke:seedGoldenEvalBlueprint) and reused
    by every later invocation so research findings are reused (L5). Each pass: smoke:seedCockpitPlan
    (fresh plan+thread) → dispatch:runMedia (route media, question = brief, depth 1, envelope 500)
    → assert modelId === candidate → smoke:storyboardFactsForPlan. Cost cap $3 per invocation
    (exit 2). Report `.tmp/storyboard-bakeoff/<candidate>.json` + a markdown table on stdout.
    Corpus: 12 briefs per the CONTEXT. Register both files under media.md in watch.json.
  </action>
  <verify>cd packages/backend && node scripts/run-storyboard-bakeoff.mjs --self-check</verify>
</task>

</tasks>
