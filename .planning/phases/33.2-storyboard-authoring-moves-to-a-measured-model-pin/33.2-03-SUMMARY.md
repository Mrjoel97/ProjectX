# 33.2-03 — SUMMARY

**Status:** COMPLETE — Tasks 1-3, **B6 + B7 met**. Task 3: the owner generated a reel on the new
pin on 2026-09-05 ("the reel was good and it was created"). Two gaps the owner reported from that
run were closed in the same session as 33.2-04 (`media.md`, `vault.md` playbook blocks): the
canvas Generate left the plan `proposed` (a second Approve, a second bill), and every vault ingest
had been failing since 2026-08-27 on five stale copies of the model resolver. **Wave:** 3 · **Branch:** `feat/28.1-billing-mapping` (main tree).
**Commits:** `1981933` — `fix(33.2-03): the agent loop audits its fallback, and the media director gets its own 90 s clock`;
the pin commit (this close-out) — `feat(33.2-03): the storyboard pin moves to gpt-4.1-mini by the bake-off rule (ADR-032)`.

## Task 1 — the rounds

Ran twice. The 2026-09-04 rounds (45 s clock) are archived as contaminated
(`.tmp/storyboard-bakeoff/clock45/`): the runner then asserted the pin, and the spend rows showed
37 of 72 passes answered by the fallback. That surfaced three lane defects, fixed in `1981933` and
recorded in PRD Amendment 1 (L7 90 s media clock, L8 audited fallback, L9 executed-model assertion).

The 2026-09-05 rounds (90 s clock, executed model asserted, the owner's new OpenRouter account):

| Candidate | clean two-deck of 24 | verdict |
| --- | ---: | --- |
| gpt-4o-mini (baseline) | 14 | reference |
| gpt-5.6-luna | 11 | below baseline |
| **gpt-4.1-mini** | **18** | **winner, +4 ≥ 3** |
| gpt-5.6-sol | 3 of 3 then `TimeoutError` at pass 4 | disqualified (clock) |
| claude-sonnet-5 | `TimeoutError` at pass 1 | disqualified (clock) |

Full tables, per-brief grid, refusal codes, the direct sonnet-5 reasoning probes, the money:
`33.2-BAKEOFF.md`.

## Task 2 — the decision

- `OR_MEDIA_MODEL = OR_RESEARCH_FALLBACK_MODEL` (gpt-4.1-mini); `OR_MEDIA_FALLBACK_MODEL = OR_DEFAULT_MODEL`
  (gpt-4o-mini — a rollover must change the model; the baseline is the fast one). The two premium
  candidate constants and PRICING rows left with the bake-off.
- `cost.test.ts`: `MEDIA_MODEL !== DEFAULT_MODEL`, `MEDIA_MODEL !== MEDIA_FALLBACK_MODEL`, both priced.
- ADR-032 `docs/decisions/032-the-storyboard-model-is-chosen-by-a-bake-off-on-the-production-path.md`.
- Playbooks: media.md, guardrails.md (Last verified 2026-09-05).

## Task 3 — DONE (owner-verified 2026-09-05)

One reel generated from the cockpit on the new pin; the storyboard proposed clean first time and
the reel rendered. Owner's words: "the reel was good and it was created".

## Measured

cost 95/95 · dispatch + runCockpitAgent 158/158 · tsc 0 outside the connector lane.

## Left on the table, recorded

- sonnet-5 / sol need a LANE change to be measured fairly (per-model reasoning effort; the deck
  turn as its own action). Out of scope here; ADR-032 names it as the upgrade path.
- The 12 s generated cap is not parser-enforced; `45-illegal-duration` is a refusal fixture
  (ceiling 22); `narration_too_long` dominates every model's failures.
- `vaultLlm:extractGraph` logs an `or/`-prefix warning in the dev log — another lane's.
- Production's OpenRouter key is the OLD account's; the dev deployment is on the new one.
