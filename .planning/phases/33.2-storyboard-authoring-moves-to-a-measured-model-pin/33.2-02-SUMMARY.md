# 33.2-02 — SUMMARY

**Status:** COMPLETE. **B4, B5 met.** **Wave:** 2 · **Branch:** `feat/28.1-billing-mapping` (main tree).
**Commits:** `ee74cde` — `feat(33.2-02): the storyboard bake-off runner, its twelve-brief corpus, and the facts query it reads`;
`2673418` — `fix(33.2-02): the bake-off asserts the model that ANSWERED, from spend rows, not the pin`.

## What shipped

- **The runner.** `packages/backend/scripts/run-storyboard-bakeoff.mjs` — `--candidate`,
  `--repeat`, `--only`, `--self-check`. Reads the code-owned `MEDIA_MODEL` off cost.ts through the
  alias chain (≤ 6 hops) and refuses a candidate that is not the pin. Per pass: fresh
  `smoke:seedCockpitPlan` → production `dispatch:runMedia` (route media, depth 1, envelope 500 ¢,
  question = brief) → `smoke:storyboardFactsForPlan`. Seeds each brief's tenant once
  (`vaultSmoke:seedCorpus` + `smoke:seedGoldenEvalBlueprint`, state in `.tmp/storyboard-bakeoff/tenants.json`).
  `COST_CAP_USD` 3.0 per invocation. Report per candidate in `.tmp/storyboard-bakeoff/`. Exit 0/1/2.
- **The corpus.** `scripts/storyboard-briefs.json` — twelve Northwind briefs, 15/30/45/60 s, including a
  deliberate-refusal duration, a forbidden-asks brief, an uploaded-footage claim, a 490-char long brief.
- **The facts queries.** `smoke.storyboardFactsForPlan` (kind, reason, variation, lostReason,
  target seconds, scene counts, generated seconds, kinds, unverified, adjustments — counts and
  codes only, two tests hold that no model prose crosses) and `smoke.modelsForPlan` (agentSteps →
  `turnId` → `spendEvents` by `agentloop:<turnId>:` prefix → the set of models that ANSWERED).
- **The fix in the second commit.** The first runner asserted `run.modelId` (the pair the lookup
  chose). Reconstruction from spend rows showed 37 of the first 72 passes were answered by the
  fallback. The runner now asserts `smoke:modelsForPlan` is exactly `[candidate]` with `rowCount > 0`
  and aborts the round otherwise (exit 2, `EnvironmentAbort`).

## Measured

`--self-check` green (twelve briefs validated, caught a 519-char brief → trimmed) · dispatch facts
describe green · playbooks: media.md (runner + corpus registered in `watch.json`), agent-runtime.md.
