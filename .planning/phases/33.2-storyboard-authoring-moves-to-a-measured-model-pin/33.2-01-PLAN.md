---
phase: 33.2-storyboard-authoring-moves-to-a-measured-model-pin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cost/src/cost.ts
  - packages/cost/src/index.ts
  - packages/cost/src/cost.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/dispatch.ts
  - packages/backend/convex/dispatch.test.ts
  - packages/backend/convex/research.ts
  - packages/backend/convex/schema.ts
  - docs/playbooks/guardrails.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [B1, B2, B3]

must_haves:
  truths:
    - "media-director bills MEDIA_MODEL / MEDIA_FALLBACK_MODEL; offer-architect still bills the defaults"
    - "The lane is aliased to today's id, so this plan changes no behaviour and no bill"
    - "A second groundMediaBrief for the same brief on the same tenant inside 24 h runs no turn and writes no document"
  artifacts:
    - path: "packages/cost/src/cost.ts"
      provides: "OR_MEDIA_MODEL / OR_MEDIA_FALLBACK_MODEL and their MEDIA_* aliases"
      contains: "MEDIA_MODEL"
    - path: "packages/backend/convex/llm.ts"
      provides: "the fourth arm of the skill-name lookup"
      contains: "[MEDIA_MODEL, MEDIA_FALLBACK_MODEL]"
  key_links:
    - from: "packages/backend/convex/dispatch.ts"
      to: "packages/backend/convex/research.ts"
      via: "internal.research.recentFindingsForQuestion"
      pattern: "recentFindingsForQuestion"
---

<objective>
Give the media-director specialist its own model lane (aliased, behaviour-neutral) and stop a
storyboard retry from re-buying research it already has.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: The lane</name>
  <files>packages/cost/src/cost.ts, packages/cost/src/index.ts, packages/cost/src/cost.test.ts, packages/backend/convex/llm.ts, packages/backend/convex/dispatch.test.ts</files>
  <action>
    In cost.ts add `OR_MEDIA_MODEL = OR_DEFAULT_MODEL` and `OR_MEDIA_FALLBACK_MODEL = OR_RESEARCH_FALLBACK_MODEL`
    as ALIASES (a second string literal spelling the same id would collapse a PRICING computed key —
    the file's own rule), with the two-line comment that names the lane's reason and the measurement
    plan 03 owes. Add `MEDIA_MODEL` / `MEDIA_FALLBACK_MODEL` aliases beside `PACK_MODEL`, export them
    from index.ts. Mirror the pack block in cost.test.ts: both price, fallback is not CHEAP_MODEL.
    In llm.ts add `isMedia = skillName === MEDIA_DIRECTOR_SKILL` and a fourth arm
    `[MEDIA_MODEL, MEDIA_FALLBACK_MODEL]` in the lookup. In dispatch.test.ts's THE MODEL PIN test,
    media now asserts the media pair and `offer-architect` (BASE) becomes the genuine default case;
    the source tripwire gains the media pair string.
  </action>
  <verify>pnpm --filter @pikar/cost test; cd packages/backend && npx vitest run convex/dispatch.test.ts -t "MODEL PIN"</verify>
</task>

<task type="auto">
  <name>Task 2: Findings reuse</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/research.ts, packages/backend/convex/dispatch.ts, packages/backend/convex/dispatch.test.ts</files>
  <action>
    schema.ts: `researchQuestionHash: v.optional(v.string())` on vaultDocuments (widen-only).
    research.ts: persistFindings writes `researchQuestionHash: await contentHash(a.question)` (the
    hash it already computes for the audit row — compute once, use twice); add
    `recentFindingsForQuestion` internalQuery({tenantId, questionHash, sinceMs}) → boolean over
    `by_tenant_kind` (`kind: "web_research"`, `createdAt >= sinceMs`) filtered on the hash.
    dispatch.ts groundMediaBrief: after the blank-brief exit, `if (await ctx.runQuery(...recentFindingsForQuestion, {tenantId, questionHash: await contentHash(brief), sinceMs: Date.now() - FINDINGS_REUSE_MS})) return;`
    with `FINDINGS_REUSE_MS = 24h` and a comment naming the retry cost it saves.
    Tests in the existing groundMediaBrief describe: same brief twice → one turn, one doc;
    different brief → two docs; a doc older than the window (patch createdAt) → researched again.
  </action>
  <verify>cd packages/backend && npx vitest run convex/dispatch.test.ts -t "groundMediaBrief"; npx tsc --noEmit -p packages/backend</verify>
</task>

</tasks>

<verification>
Core/cost/backend targeted suites green, tsc clean, biome clean on touched files, playbook gate
satisfied (guardrails.md + cockpit.md `Last verified` bumped with the lane and the reuse rule).
</verification>
