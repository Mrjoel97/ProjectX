---
phase: 34-goal-engine-v0-the-agenda-speaks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/agenda.ts
  - packages/core/src/agenda.test.ts
  - packages/core/src/index.ts
  - packages/core/src/notificationTemplates.ts
  - packages/core/src/tenantData.ts
  - packages/core/src/tenantData.test.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/agenda.ts
  - packages/backend/convex/agenda.test.ts
  - packages/backend/convex/lib/agenda.ts
  - packages/backend/convex/evaluations.ts
  - packages/backend/convex/proactiveReview.ts
  - packages/backend/convex/proactiveReview.test.ts
  - packages/backend/convex/routines.test.ts
  - packages/backend/convex/_generated/api.d.ts
  - apps/web/app/(app)/dashboard/CommandCenter.tsx
  - apps/web/app/(app)/dashboard/commandCenter.test.ts
  - apps/web/app/(app)/_components/NotificationsBanner.tsx
  - docs/decisions/033-the-agenda-is-a-lifecycle-table-and-the-weekly-review-stages-one-proposal.md
  - docs/playbooks/business-evaluation.md
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/audit-dead-letter.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [G13]
must_haves:
  truths:
    - "Every gap the weekly review surfaces has an agenda row with a lifecycle that survives the weekly rewrite: open / proposed / acted / dismissed / recurring"
    - "The weekly review stages at most ONE proposal per tenant per week, through applyActOnGap, onto the approvals surface, behind the Approve gate; a waiting proposal is not re-staged; a dismissed gap is not staged"
    - "A done memo no longer blocks the review thread forever (plan_busy)"
    - "The Command Center shows at most three rows: current gaps with status word, reason, citations and the goal they block, then the review's asks as interview openers; the only controls are stage-for-approval and dismiss"
    - "A staged week notifies agenda_proposal (deep link to approvals); the kind is outside NOTIFICATION_KINDS"
    - "Nothing sends, schedules or recurs beyond the existing Monday cron; the agent writes no goal"
  artifacts:
    - path: "packages/backend/convex/agenda.test.ts"
      provides: "sync stages once; waiting proposal not re-staged; approved → acted → recurring → re-proposed; dismissal holds and lifts on come-back; dismiss refuses proposed; both doors mark the same row; current is bounded, cited, goal-linked, tenant-scoped"
    - path: "packages/core/src/agenda.test.ts"
      provides: "the lifecycle table, plan-fate mapping, segmentForRoute"
  key_links:
    - from: "proactiveReview.reviewOne"
      to: "agenda.syncFromReview → lib/agenda.syncAgenda → evaluations.applyActOnGap"
      via: "one internal mutation after runEvaluation, its own catch"
    - from: "evaluations.applyActOnGap (both terminals)"
      to: "lib/agenda.markAgendaProposed"
      via: "review thread only; upsert"
    - from: "CommandCenter ConnectedAgenda"
      to: "api.agenda.current / api.evaluations.actOnGap / api.agenda.dismiss"
      via: "own subscription, own SectionBoundary"
---

<objective>
Goal Engine v0 — the chief-of-staff proposes. One new table, one exported function, one new read
model, one new Command Center section, one new notification kind. Propose-only; nothing recurs.
</objective>

<context>
@.planning/phases/34-goal-engine-v0-the-agenda-speaks/34-RESEARCH.md
@.planning/design/goal-engine.md (§2 convictions, §3 prohibitions, §4 v0)
@docs/decisions/033-the-agenda-is-a-lifecycle-table-and-the-weekly-review-stages-one-proposal.md
@docs/playbooks/business-evaluation.md, @docs/playbooks/dashboard-pages.md

Notes for the executor:
- `applyActOnGap` is the ONLY staging door. Export it; do not copy its recycle/terminal logic.
- `lib/agenda.ts` exists to break the evaluations ↔ agenda import cycle. Keep the pure rules in core.
- The web `_generated/api.d.ts` is hand-maintained in this worktree (no deployment for codegen):
  add `agenda` and `lib/agenda` in sorted position.
- `proactiveReview.test.ts`: stub both model keys empty; warm `./dispatch.ts` in a `beforeAll`.
</context>

<tasks>
1. core: `agenda.ts` (statuses, `nextAgendaStatus`, `agendaStatusFromPlan`, `segmentForRoute`, `AGENDA_LIMIT`, `AGENDA_STATUS_WORD`), export, `AGENDA_PROPOSAL_MESSAGE`, `agenda: "tenant_owned"`, count 58.
2. schema: `agenda` table (`by_tenant`, `by_tenant_key`); header 58 + name.
3. backend: `lib/agenda.ts` (`syncAgenda`, `markAgendaProposed`); `agenda.ts` (`syncFromReview`, `current`, `dismiss`); `evaluations.ts` (export, done-memo recycle, two `markAgendaProposed` calls); `proactiveReview.ts` (sync hop, `agenda_proposal`); api.d.ts; routines module pin.
4. web: `AgendaCard` + `ConnectedAgenda` + boundary; banner deep link; test harness (`agenda:current`, 6 boundaries, markers, landmark) + agenda tests.
5. Tests: backend `agenda.test.ts`; core `agenda.test.ts`; review tests adjusted (kind, audit rows, warm-up).
6. ADR-033; playbooks + watch.json; typecheck; both backend shards; CI lint LAST.
</tasks>
