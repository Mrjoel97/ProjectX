---
phase: 07-resilience-operations-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/retention.ts
  - packages/core/src/retention.test.ts
  - packages/core/src/reviewThreshold.ts
  - packages/core/src/reviewThreshold.test.ts
  - packages/core/src/notificationTemplates.ts
  - packages/core/src/notificationTemplates.test.ts
  - packages/core/src/buildTelemetry.ts
  - packages/core/src/index.ts
  - packages/backend/package.json
  - packages/backend/convex/schema.ts
  - docs/playbooks/watch.json
  - docs/playbooks/audit-dead-letter.md
autonomous: true
requirements: [OPSG-03, REVW-02, OPSG-05]
must_haves:
  truths:
    - "The pure retention, review-threshold, and notification-template logic is unit-testable without Convex"
    - "The audit table can be windowed by ts without a full table scan (global by_ts index exists)"
    - "@aws-sdk/client-s3 is installed so worm.ts can import it in the OPSG-03 plan"
    - "A plan carries an optional revise counter + escalated flag so the cockpit review gate can enforce a cap"
  artifacts:
    - path: "packages/core/src/retention.ts"
      provides: "NDJSON serialize of audit rows + WORM object-key builder + retain-until-date math (pure)"
      exports: ["serializeAuditNdjson", "wormObjectKey", "retainUntilDate", "RETENTION_MS"]
    - path: "packages/core/src/reviewThreshold.ts"
      provides: "Edit/reject/regenerate threshold constants + fail-closed breach classifier (pure)"
      exports: ["MAX_REGENERATE", "classifyReviewDecision"]
    - path: "packages/core/src/notificationTemplates.ts"
      provides: "Closed NotificationKind union + static refs/counts-only message labels (§4)"
      exports: ["NotificationKind", "notificationMessage"]
    - path: "packages/backend/convex/schema.ts"
      provides: "audit.by_ts global index + plans.reviseCount/plans.escalated optional fields"
      contains: "by_ts"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "retention.ts, reviewThreshold.ts, notificationTemplates.ts"
      via: "barrel re-export"
      pattern: "export \\* from \"./(retention|reviewThreshold|notificationTemplates)\""
    - from: "packages/backend/package.json"
      to: "@aws-sdk/client-s3"
      via: "dependencies entry"
      pattern: "@aws-sdk/client-s3"
---

<objective>
Wave-0 foundation for Phase 7. Push every piece of non-trivial hardening logic into pure
`@pikar/core` (unit-testable — workflows do NOT run under convex-test), install the one new
dependency, and make the schema/index changes the feature plans build on. Nothing here wires a
Convex function; it lays the contracts the OPSG-03, REVW, AGNT, and OPSG-05 plans consume.

Purpose: keep the Convex adapters thin (§1) and give the downstream plans tested primitives instead
of inline logic.
Output: three new pure modules + tests, the `@aws-sdk/client-s3` dep, and the additive schema
changes (audit `by_ts` index, `plans.reviseCount`/`plans.escalated`, `ReviewOutcome` += "escalated").
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-resilience-operations-hardening/07-CONTEXT.md
@.planning/phases/07-resilience-operations-hardening/07-RESEARCH.md
@.planning/phases/07-resilience-operations-hardening/07-VALIDATION.md

<interfaces>
Existing pure-core conventions to mirror (packages/core/src/buildTelemetry.ts):
- `export type ReviewOutcome = "sent" | "rejected" | "expired" | "failed" | "blocked";` — ADD "escalated".
- Modules are framework-agnostic, exported through packages/core/src/index.ts, one test file each.

Existing audit table (packages/backend/convex/schema.ts:15-24):
```
audit: defineTable({ tenantId, correlationId, eventType, actor, payload, ts })
  .index("by_tenant_ts", ["tenantId", "ts"])
  .index("by_correlation", ["correlationId"])
```
Only per-tenant [tenantId, ts] — the WORM export windows CROSS-tenant by ts, so it needs a global `by_ts`.

Existing plans table carries the cockpit plan row (status: "proposed"|"approved"|"scheduled"|"canceled"|...).
The cockpit review gate (07-04) needs a redraft counter + an escalated flag; add them OPTIONAL (no migration).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure retention + notification-template modules</name>
  <files>packages/core/src/retention.ts, packages/core/src/retention.test.ts, packages/core/src/notificationTemplates.ts, packages/core/src/notificationTemplates.test.ts, packages/core/src/buildTelemetry.ts, packages/core/src/index.ts</files>
  <behavior>
    retention.ts:
    - serializeAuditNdjson(rows): one JSON object per line, newline-terminated, stable key order; empty rows → "".
    - wormObjectKey(sinceTs, maxTs): "audit/YYYY-MM-DD/{sinceTs}-{maxTs}.ndjson" (date prefix from maxTs, UTC).
    - retainUntilDate(nowMs): new Date(nowMs + RETENTION_MS); RETENTION_MS a named default constant.
    notificationTemplates.ts:
    - NotificationKind closed union covering every Phase-7 site: "validation.rejected" | "guardrail.blocked"
      | "review.expired" | "review.escalated" | "retry.limit" | "agent.timeout" | "deadletter" | "awaiting_reauth".
    - notificationMessage(kind): a STATIC label per kind (refs/counts only, NEVER interpolated content, §4).
    buildTelemetry.ts: add "escalated" to ReviewOutcome.
  </behavior>
  <action>
    Create the two pure modules matching the buildTelemetry.ts house style (framework-agnostic, no Convex
    imports). serializeAuditNdjson must be deterministic (sort keys) so a re-export of the same window is
    byte-identical (idempotent PutObject). notificationMessage returns fixed strings only — this is the §4
    firewall the whole OPSG-05 matrix relies on; a template must not accept a content argument. Add "escalated"
    to buildTelemetry.ts ReviewOutcome (the REVW-02 escalation terminal reuses the telemetry builder). Re-export
    all three from index.ts. Tests: assert NDJSON round-trips + key-order stability, key format, retain-until
    math, and that notificationMessage(kind) is defined for EVERY NotificationKind member and contains no `${`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test retention notificationTemplates</automated>
  </verify>
  <done>Both modules exported + green; every NotificationKind has a static message; ReviewOutcome includes "escalated".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure review-threshold policy (fail-closed classifier)</name>
  <files>packages/core/src/reviewThreshold.ts, packages/core/src/reviewThreshold.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - MAX_REGENERATE = 3 (named default constant, the ponytail knob per 07-CONTEXT; per-tenant policy is the ceiling).
    - classifyReviewDecision({ decision, regenerateCount }) →
        decision "approve"|"edit_text" → { action: "proceed" }
        decision "reject"              → { action: "terminate", outcome: "rejected" }
        decision "regenerate" & regenerateCount < MAX_REGENERATE → { action: "regenerate" }
        decision "regenerate" & regenerateCount >= MAX_REGENERATE → { action: "escalate", reason: "regenerate_limit" }
    - The escalate branch is the FAIL-CLOSED fix: past the cap a regenerate must NOT fall through to delivery.
  </behavior>
  <action>
    Pure decision function — no Convex, no Date.now. This is the single source of truth both the pipeline gate
    (07-03) and the cockpit gate (07-04) call so the "regenerate past cap = unapproved send" bug is fixed once,
    not per caller (ponytail root-cause). Document in a comment that edit_text/reject are single-shot terminals
    (they break/terminate the gate, so their "counters" cannot exceed 1 per session) — the enforceable threshold
    is the regenerate cap; the breach path is escalate, never deliver. Test the four branches + the exact
    cap boundary (regenerateCount === MAX_REGENERATE escalates, === MAX_REGENERATE-1 regenerates).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test reviewThreshold</automated>
  </verify>
  <done>classifyReviewDecision escalates at the cap and never returns "proceed" for a regenerate past MAX_REGENERATE.</done>
</task>

<task type="auto">
  <name>Task 3: Install @aws-sdk/client-s3, add schema index + plan counter fields, register watched paths</name>
  <files>packages/backend/package.json, packages/backend/convex/schema.ts, docs/playbooks/watch.json, docs/playbooks/audit-dead-letter.md</files>
  <action>
    1. `pnpm --filter @pikar/backend add @aws-sdk/client-s3` (the ONE new dep, per 07-RESEARCH; worm.ts is already
       "use node"). Do NOT bump any pinned pre-1.0 component (§6).
    2. schema.ts: add a global `.index("by_ts", ["ts"])` to the `audit` table (keep the existing by_tenant_ts +
       by_correlation). Adding an index is not a write path — Convex backfills it (no migration, OPSG-06 moot).
    3. schema.ts: add to the `plans` table two OPTIONAL fields — `reviseCount: v.optional(v.number())` and
       `escalated: v.optional(v.boolean())` — the cockpit review-gate counter/terminal (07-04) reads/writes these.
       Optional ⇒ existing rows need no backfill.
    4. watch.json: register the new/unwatched Phase-7 paths so §9's hook can protect them. Add under
       "audit-dead-letter.md": `packages/backend/convex/notifications.ts`, `packages/backend/convex/notifyExternal.ts`,
       `packages/core/src/retention.ts`, `packages/core/src/notificationTemplates.ts`. Add under "cockpit.md":
       `packages/backend/convex/pipeline.ts`, `packages/backend/convex/review.ts`, `packages/core/src/reviewThreshold.ts`.
       (Editing watch.json once here keeps every later plan off this shared file.)
    5. audit-dead-letter.md: note the by_ts index now backs auditSince, that the WORM export dep is installed, and
       bump its "Last verified" line (this plan changes worm's watched siblings). Keep it brief.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>@aws-sdk/client-s3 in package.json; audit has a by_ts index; plans has reviseCount+escalated; watch.json covers the new paths; typecheck + check-playbooks green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test` green (retention, reviewThreshold, notificationTemplates).
- `pnpm --filter @pikar/backend typecheck` clean with the new schema fields + index.
- `node scripts/check-playbooks.mjs` exit 0 (watch.json + audit-dead-letter.md consistent).
</verification>

<success_criteria>
Downstream plans (02–05) can import serializeAuditNdjson/wormObjectKey/retainUntilDate,
classifyReviewDecision/MAX_REGENERATE, and NotificationKind/notificationMessage; the audit table is
by_ts-indexed; plans carries the cockpit review counter fields; the new dep is installed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-resilience-operations-hardening/07-01-SUMMARY.md`
</output>
