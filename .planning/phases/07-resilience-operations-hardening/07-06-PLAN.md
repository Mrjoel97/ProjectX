---
phase: 07-resilience-operations-hardening
plan: 06
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - docs/playbooks/audit-dead-letter.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/agent-runtime.md
autonomous: false
requirements: [OPSG-03, OPSG-05, REVW-02, REVW-03, AGNT-04]
user_setup:
  - service: aws-s3
    why: "The live smoke:worm at phase close needs the real Object-Lock bucket + AWS creds (OPSG-03) — set up in 07-02"
    env_vars:
      - name: WORM_BUCKET
        source: "AWS S3 — a bucket created with Object Lock ENABLED (set via `npx convex env set`, NEVER Vercel)"
      - name: AWS_ACCESS_KEY_ID
        source: "AWS IAM — `npx convex env set`"
      - name: AWS_SECRET_ACCESS_KEY
        source: "AWS IAM — `npx convex env set`"
      - name: AWS_REGION
        source: "AWS — the bucket's region"
must_haves:
  truths:
    - "The full offline suite (backend + core) is green and check-playbooks exits 0 across all Phase-7 changes"
    - "No code path lets a past-cap regenerate/re-propose reach delivery, and executePlan refuses an escalated plan (fail closed, grep-proven)"
    - "A real WORM object lands in S3 with COMPLIANCE Object Lock + retain-until + checksum, and a delete/overwrite is refused (owner-inspected)"
    - "Each failure class fires ONE in-app notification AND one best-effort external email; a failed external send does not recursively notify"
    - "SC#4 is recorded as PARTIALLY met — WORM export implemented, hot-audit sweep DEFERRED per owner (never a silent gap)"
  artifacts:
    - path: "docs/playbooks/audit-dead-letter.md"
      provides: "Phase-7 close: WORM export + notification-matrix invariants + the deferred-sweep record, Last verified bumped"
      contains: "sweep"
  key_links:
    - from: "the whole Phase-7 substrate"
      to: "owner human-verify sign-off"
      via: "live smokes + S3 console inspection + the notification-matrix walk-through"
      pattern: "smoke:worm"
---

<objective>
Phase-7 close. Prove the whole resilience/ops-hardening slice as a WHOLE — the pieces are unit- and
smoke-tested inside their own plans, but the workflow/scheduler/S3 behaviors are only PROVABLE on a
live deployment (07-VALIDATION.md "Manual-Only Verifications"): a real WORM object under Object Lock,
a review-expiry/breach driving an escalated terminal with NO send, the full in-app + external
notification matrix, and no notification loop. Then record SC#4's partial scope (export met, sweep
deferred) so the verifier never reads the deferral as a silent gap.

Purpose: the phase gate — full offline suite + the live smokes only a running deployment can prove +
the owner sign-off, mirroring the 3.6/3.7/6 close precedent.
Output: a verified green sweep, an owner-approved notification matrix + WORM object, closed playbooks.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-resilience-operations-hardening/07-CONTEXT.md
@.planning/phases/07-resilience-operations-hardening/07-VALIDATION.md
@.planning/phases/07-resilience-operations-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full offline sweep + fail-closed grep-proofs + SC#4 partial record</name>
  <files>docs/playbooks/audit-dead-letter.md, docs/playbooks/cockpit.md, docs/playbooks/agent-runtime.md</files>
  <action>
    1. Run the full offline suites and record results:
       `pnpm --filter @pikar/core test` and `pnpm --filter @pikar/backend test` (note the ONE documented
       pre-existing red — `audit.test.ts auditCounts` — is a known non-regression; everything Phase-7 touched
       must be green). `node scripts/check-playbooks.mjs` exit 0.
    2. Grep-prove the fail-closed security invariants land as designed (the highest-value assertions of the phase):
       - pipeline.ts: the review-gate loop routes a past-cap regenerate to the `escalated` terminal and there is
         NO path from a past-cap regenerate to the DELIVER block (the pipeline.ts:250 fallthrough is gone).
       - cockpit.ts: `executePlan` returns `review_escalated` (or equivalent) BEFORE the CAS flip when the plan is
         escalated, and `proposeEmailPlan` caps re-proposes at `MAX_REGENERATE` (imported from @pikar/core, not inlined).
       - worm.ts: `advanceCursor` is unreachable unless the `PutObjectCommand` promise resolved (no advance on throw).
       - notify sites carry only `notificationMessage(kind)` static labels (the llmRedaction §4 scan is green).
    3. Bump "Last verified" on the three touched playbooks (audit-dead-letter.md, cockpit.md, agent-runtime.md) and
       add ONE explicit line to audit-dead-letter.md's retention/WORM section: **SC#4 is PARTIALLY met — the WORM
       export is implemented; the hot-audit-copy sweep/delete is DEFERRED per owner ruling (2026-07-21, export-only;
       §3/ADR-002 stay literally intact, no new ADR). A future phase may add a §3-reconciling retention-delete path.**
    This is the record the goal-backward verifier reads so SC#4's deferred half is never scored as a silent miss.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test && pnpm --filter @pikar/backend test && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Both suites green (bar the documented audit.test.ts non-regression); the four fail-closed grep-proofs hold; audit-dead-letter.md records SC#4 partial; playbooks' Last verified bumped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live smokes + owner human-verify of the Phase-7 gate (the manual-only proofs)</name>
  <action>
    Pause for the owner to run the live walk-through below — S3 durability, scheduler/workflow timing,
    and real deliverability/no-loop behavior are not unit-testable (07-VALIDATION Manual-Only; the
    RPLY-01/VOIC precedent). Nothing here is autonomous: the executor sets up the run and waits.
  </action>
  <what-built>
    The complete Phase-7 resilience slice, deployed and running: real WORM S3 Object-Lock export (OPSG-03),
    the fail-closed review gate + timeout notifications (REVW-02/03), the agent-timeout notification (AGNT-04),
    and the in-app + external notification matrix with a loop guard (OPSG-05). Task 1 has already proven the
    offline suite green and the fail-closed invariants by grep; this checkpoint proves what only a live
    deployment can — S3 durability, scheduler/workflow timing, and real deliverability/no-loop behavior.
  </what-built>
  <how-to-verify>
    Prereq: the Object-Lock bucket + AWS creds are set in the Convex deployment env (07-02 user_setup);
    a Gmail-connected harness user is signed in.

    1. WORM (OPSG-03): run `npm run smoke:worm` against the real Object-Lock bucket. Open the S3 console and
       inspect ONE exported `audit/…​.ndjson` object — confirm it shows Object Lock = COMPLIANCE, a
       RetainUntilDate in the future, and a checksum. Attempt to delete/overwrite it — S3 must REFUSE.
    2. Review gate (REVW-02/03): run `npm run smoke:pipeline` driving (a) a review-inactivity expiry and
       (b) a regenerate breach (>3). Confirm the request ends `expired` / `escalated`, the matching
       notification fired, and NO email was sent in either case.
    3. Agent timeout (AGNT-04): force an Executive-Agent turn to exhaust its timeout; confirm ONE in-app
       `agent.timeout` notification appears and the chat shows the non-dead-ending "nothing was sent" reply.
    4. Notification matrix (OPSG-05): trigger each failure class — validation reject, escalation, retry breach,
       review timeout, agent timeout, dead-letter (`npm run smoke:dlq`). Confirm for the serious classes ONE
       in-app notification AND one external email arrive per event, and — critically — that a FAILED external
       send (e.g. disconnect Gmail mid-flight) does NOT recursively trigger a "your notification failed"
       send (no loop) and the in-app row still lands (fail closed).
    5. Confirm you understand SC#4 is intentionally partial: WORM export YES, hot-audit sweep DEFERRED.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 7, or describe any defect (which becomes a gap-closure plan).</resume-signal>
</task>

</tasks>

<verification>
- Offline: `pnpm --filter @pikar/core test` + `pnpm --filter @pikar/backend test` green (bar the documented
  audit.test.ts non-regression); `node scripts/check-playbooks.mjs` exit 0.
- Live (owner human-verify): the five checks above — a real Object-Locked WORM object (delete refused),
  expiry/breach terminals with no send, an agent-timeout notification, the in-app + external matrix with no loop.
- SC#4 recorded partial (export met, sweep deferred) in audit-dead-letter.md.
</verification>

<success_criteria>
Phase 7 is complete: all five requirements (AGNT-04, REVW-02, REVW-03, OPSG-03, OPSG-05) are met on both the
workflow and the live cockpit path, proven offline (suite + grep-proofs) and live (smokes + owner sign-off),
with SC#4 explicitly recorded as partially met (WORM export implemented; hot-audit sweep deferred per owner).
</success_criteria>

<output>
After completion, create `.planning/phases/07-resilience-operations-hardening/07-06-SUMMARY.md` and, on owner
"approved", flip the Phase-7 ROADMAP/STATE markers to complete (record SC#4 partial in the close note).
</output>
