---
phase: 07-resilience-operations-hardening
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/backend/convex/pipeline.ts
  - packages/backend/convex/review.ts
  - packages/backend/convex/pipeline.test.ts
  - packages/backend/scripts/run-smoke-pipeline.mjs
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [REVW-02, REVW-03, OPSG-05]
must_haves:
  truths:
    - "A regenerate decision past MAX_REGENERATE terminates the request as escalated — it can NEVER fall through to delivery (fail closed, backend-enforced)"
    - "The escalated terminal writes a review.escalated audit + a retry.limit notification + an escalated telemetry row (no send)"
    - "The review-inactivity timeout branch fires a review.expired notification (not just audit+telemetry)"
    - "The review gate's threshold decision comes from @pikar/core classifyReviewDecision, not inline logic"
  artifacts:
    - path: "packages/backend/convex/pipeline.ts"
      provides: "Fail-closed review-gate loop driven by classifyReviewDecision + escalated terminal + timeout notify"
      contains: "classifyReviewDecision"
  key_links:
    - from: "packages/backend/convex/pipeline.ts (review gate loop)"
      to: "@pikar/core classifyReviewDecision"
      via: "escalate branch terminates instead of breaking to DELIVER"
      pattern: "classifyReviewDecision"
    - from: "packages/backend/convex/pipeline.ts (timeout branch)"
      to: "internal.notifications.notify"
      via: "review.expired notification added beside the existing audit"
      pattern: "review\\.expired"
---

<objective>
Harden the durable-workflow review gate (`pipeline.ts` / `review.ts`, the substrate REVW-02/03's
requirement language targets). Two things:
1. REVW-02 (HIGHEST PRIORITY — a security fix): past `MAX_REGENERATE` a `regenerate` decision
   currently breaks out of the loop and falls through to DELIVER — an UNAPPROVED SEND (pipeline.ts:250-277).
   Route every gate decision through `@pikar/core classifyReviewDecision` so a past-cap regenerate
   terminates as `escalated` (audit + notify + telemetry, NO send). Fail closed at the workflow
   boundary — never rely on the UI hiding a button.
2. REVW-03: the review-inactivity timeout branch (pipeline.ts:233) writes audit + telemetry but no
   notification — add the `review.expired` escalation notification (~3 lines).

Purpose: close the unapproved-send hole and the missing timeout notification on the workflow path.
Output: a fail-closed review gate with an `escalated` terminal + a notifying timeout branch.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-resilience-operations-hardening/07-CONTEXT.md
@.planning/phases/07-resilience-operations-hardening/07-RESEARCH.md
@packages/backend/convex/pipeline.ts
@packages/backend/convex/review.ts

<interfaces>
From @pikar/core (07-01):
- `MAX_REGENERATE` (=3), `classifyReviewDecision({ decision, regenerateCount })` →
  { action: "proceed" } | { action: "regenerate" } | { action: "terminate", outcome: "rejected" }
  | { action: "escalate", reason: "regenerate_limit" }
- `notificationMessage(kind)` for kind "review.expired" | "review.escalated" | "retry.limit"; ReviewOutcome += "escalated".

The bug (pipeline.ts:250-277): `if (evt.decision === "regenerate" && attempt < MAX_REGENERATE) { ...; continue; }`
then falls to `// approve | edit_text (| regenerate past the cap — the UI stops offering it)` → `break` → DELIVER.
REQUEST_STATUS union is defined in pipeline.ts:30-44 (add "escalated"). The stopBlocked helper (pipeline.ts:122-138)
is the exact pattern to mirror: setStatus + audit + notify + telemetry as one governed terminal.
The timeout branch (pipeline.ts:233-238): setStatus("expired") + audit("review.expired") + writeTelemetry("expired") — add notify.
review.ts owns the awaitEvent-timeout race (armTimeout/fireTimeout) — unchanged; the loop already arms it each attempt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail-closed review gate via classifyReviewDecision + escalated terminal</name>
  <files>packages/backend/convex/pipeline.ts, packages/backend/convex/pipeline.test.ts</files>
  <behavior>
    - A `regenerate` at attempt < MAX_REGENERATE loops (unchanged behavior).
    - A `regenerate` at attempt >= MAX_REGENERATE → escalated terminal: status "escalated", audit "review.escalated",
      notify kind "retry.limit", telemetry reviewOutcome "escalated", return null (NO DELIVER). This is the bug fix.
    - approve / edit_text → proceed to DELIVER (unchanged). reject → rejected terminal (unchanged).
  </behavior>
  <action>
    Add "escalated" to the REQUEST_STATUS union (pipeline.ts:30-44). Add an `escalate` terminal helper mirroring
    stopBlocked: setStatusStep("escalated") + audit("review.escalated") + notify({ kind: "retry.limit", requestId,
    message: notificationMessage("retry.limit") }) + writeTelemetry("escalated"). Refactor the review-gate loop
    (pipeline.ts:240-278) to call `classifyReviewDecision({ decision: evt.decision, regenerateCount })` and switch on
    the action: "regenerate" runs the existing redraft block then continue; "escalate" calls the escalate terminal and
    `return null`; "terminate" is the reject path; "proceed" breaks to DELIVER. The past-cap regenerate MUST hit
    "escalate" — it can no longer reach the `break`. Keep decisionCounts accumulation (OPSG-01). Do NOT change review.ts.
    Note: edit_text/reject are single-shot terminals per the classifier (they break/terminate), so their "counters"
    can't exceed 1 — the enforced threshold is the regenerate cap; document this in a comment (REVW-02 scope honesty).
    Tests (pipeline.test.ts, pure-callable seam): assert classifyReviewDecision wiring — a regenerate at count=3 yields
    the escalate action (the workflow loop itself is not convex-test-runnable; assert the decision + that the escalate
    terminal helper writes status/audit/notify/telemetry via a mutation-level test where reachable; the end-to-end is
    smoke).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test pipeline</automated>
  </verify>
  <done>Past-cap regenerate returns the escalate action and drives the escalated terminal (status+audit+notify+telemetry); no code path lets a past-cap regenerate reach DELIVER.</done>
</task>

<task type="auto">
  <name>Task 2: Review-inactivity timeout notification (REVW-03)</name>
  <files>packages/backend/convex/pipeline.ts</files>
  <action>
    In the timeout branch (pipeline.ts:233-238), add — between the `review.expired` audit and the telemetry write —
    `await step.runMutation(internal.notifications.notify, { tenantId, kind: "review.expired", requestId,
    message: notificationMessage("review.expired") })`. Mirror stopBlocked's ordering. Message is the static §4 label
    (refs only, no content). SEVEN_DAYS (pipeline.ts:46) stays the default review-inactivity timeout per owner.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test pipeline</automated>
  </verify>
  <done>The timeout branch fires a review.expired notify beside its existing audit+telemetry; NO delivery on timeout (unchanged).</done>
</task>

<task type="auto">
  <name>Task 3: Extend smoke:pipeline + playbook</name>
  <files>packages/backend/scripts/run-smoke-pipeline.mjs, docs/playbooks/cockpit.md</files>
  <action>
    run-smoke-pipeline.mjs: extend the live smoke to drive (a) a review-expiry → assert `expired` status + a
    review.expired notification fired + NO send; (b) a regenerate breach (4 regenerates) → assert `escalated`
    status + a retry.limit notification + NO send. Reuse the existing sentinel/driver shape in the script.
    cockpit.md: document the fail-closed review-gate invariant (past MAX_REGENERATE = escalated terminal, never a
    send) and the timeout/breach notifications; note the gate substrate is the pipeline workflow (retired /submit
    path) and the live cockpit gets its parallel guard in 07-04. Bump "Last verified".
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>smoke:pipeline exercises expiry + breach terminals; cockpit.md documents the fail-closed gate; check-playbooks green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test pipeline` green (escalate-at-cap decision + terminal writes).
- `node scripts/check-playbooks.mjs` exit 0.
- Manual (phase gate): `npm run smoke:pipeline` drives expiry + breach; confirm escalated/expired status,
  notification fired, NO send occurred.
</verification>

<success_criteria>
REVW-02 (workflow path): a past-cap regenerate is backend-rejected as `escalated` (audit + notify + telemetry,
no send) — the unapproved-send bug is fixed, fail closed. REVW-03: the review-timeout branch notifies. Both
terminals feed the OPSG-05 matrix (retry.limit + review.expired kinds).
</success_criteria>

<output>
After completion, create `.planning/phases/07-resilience-operations-hardening/07-03-SUMMARY.md`
</output>
