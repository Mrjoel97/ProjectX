---
phase: 07-resilience-operations-hardening
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - packages/backend/convex/notifications.ts
  - packages/backend/convex/notifyExternal.ts
  - packages/backend/convex/deadLetter.ts
  - packages/backend/convex/llmRedaction.test.ts
  - packages/backend/convex/notifications.test.ts
  - packages/backend/scripts/run-smoke-dlq.mjs
  - docs/playbooks/audit-dead-letter.md
autonomous: true
requirements: [OPSG-05]
must_haves:
  truths:
    - "notify is the single choke point: it always inserts the in-app row, then best-effort dispatches an external message (email)"
    - "External dispatch fails closed to in-app — a failed send never throws and never blocks the in-app notification"
    - "A failed/dead-lettered external send NEVER recursively notifies (no notification loop)"
    - "A dead-letter event fires a user notification (both onPipelineComplete and deadLetterRecipient)"
    - "Every notify message/payload carries refs/ids/counts only — no raw content (§4), enforced by a static scan"
  artifacts:
    - path: "packages/backend/convex/notifyExternal.ts"
      provides: "Best-effort external (email) dispatch reusing the governed Gmail send; loop-guarded, fail-closed"
      exports: ["dispatch"]
    - path: "packages/backend/convex/notifications.ts"
      provides: "notify choke point: in-app insert + scheduled best-effort external dispatch"
      contains: "notifyExternal"
  key_links:
    - from: "packages/backend/convex/notifications.ts (notify)"
      to: "internal.notifyExternal.dispatch"
      via: "ctx.scheduler.runAfter(0, ...) after the in-app insert (best-effort, non-blocking)"
      pattern: "notifyExternal"
    - from: "packages/backend/convex/deadLetter.ts"
      to: "internal.notifications.notify"
      via: "deadletter notification added beside the existing audit (both handlers)"
      pattern: "kind:.*deadletter"
---

<objective>
Complete the OPSG-05 user-facing notification matrix. The escalation/timeout/retry-breach notify
sites are added by 07-03 (review.expired, retry.limit) and 07-04 (agent.timeout, retry.limit);
validation-rejection + guardrail.blocked + awaiting_reauth already fire. This plan closes the rest:
1. The DLQ notification — deadLetter.ts writes the operator badge + audit but no USER notify. Add it
   at BOTH terminals (onPipelineComplete + deadLetterRecipient).
2. External dispatch — per owner ruling OPSG-05 is IN-APP + EXTERNAL. Make `notify` the single choke
   point: always insert the in-app row, then best-effort dispatch an external email via the governed
   Gmail send. External is fail-closed to in-app and MUST NOT create a notify-loop (a failed send never
   recursively notifies). Payloads stay refs/counts only (§4).
3. Extend the redaction static scan to every notify call-site + deadLetter/worm payloads.

Purpose: one wiring layer so every failure terminal in the phase surfaces to the user (in-app + email).
Output: notify choke point + external adapter + DLQ notify + an extended §4 scan.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-resilience-operations-hardening/07-CONTEXT.md
@.planning/phases/07-resilience-operations-hardening/07-RESEARCH.md
@packages/backend/convex/notifications.ts
@packages/backend/convex/deadLetter.ts

<interfaces>
From @pikar/core (07-01): `NotificationKind`, `notificationMessage(kind)`.

notify today (notifications.ts:38-55): internalMutation({ tenantId, kind, message, requestId? }) → inserts one
in-app row. Signature stays; add the external dispatch AFTER the insert.

Gmail send seam (gmail.ts): `freshAccessToken(ctx, tenantId)` (module helper, ~line 30 — export it or add a
thin sibling) refreshes the OAuth access token; `buildMime(to, subject, body)` (gmail.ts:78) builds the RFC-2822
message; the send is a fetch to the Gmail messages.send endpoint (see gmail.send, gmail.ts:155). The external
adapter reuses these to email the user's OWN connected mailbox address a STATIC failure subject/body (no content).

deadLetter.ts: onPipelineComplete (deadLetter.ts:43-49 writes deadletter.written audit) and deadLetterRecipient
(deadLetter.ts:123-129) — add the user notify beside each audit. requestId is in context.payload / args (a
redaction-safe ref).

llmRedaction.test.ts: the existing static scan asserting no content reaches audit/telemetry payloads — extend
its scanned scope to the notify call-sites and deadLetter/worm payloads (07-VALIDATION Wave-0 gap).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: External dispatch adapter (best-effort, fail-closed, loop-guarded)</name>
  <files>packages/backend/convex/notifyExternal.ts, packages/backend/convex/notifications.ts, packages/backend/convex/notifications.test.ts</files>
  <behavior>
    - notify always inserts the in-app row first (unchanged, the fail-closed floor), THEN schedules external dispatch.
    - notifyExternal.dispatch emails the user's own mailbox a STATIC subject/body (kind label only, no content).
    - A dispatch failure (no Gmail token, refresh fail, send 4xx/5xx) is swallowed — it NEVER throws, NEVER calls
      notify, NEVER writes a deadLetter (loop guard). At most a console.warn (refs only).
  </behavior>
  <action>
    Create notifyExternal.ts with `dispatch` (internalAction: { tenantId, kind }). It calls freshAccessToken; if the
    user has no connected mailbox / refresh fails, return silently (fail closed to the already-inserted in-app row).
    Else buildMime to the user's own address with a static subject like "Pikar: <kind label>" and a static body from
    notificationMessage(kind) — NO requestId/content interpolated (§4). Wrap the whole send in try/catch that logs a
    refs-only warn and returns — a failed external send must not throw, notify, or dead-letter (the notify-loop guard).
    In notifications.ts notify: after the in-app insert, `await ctx.scheduler.runAfter(0, internal.notifyExternal.dispatch,
    { tenantId, kind })` (scheduling an action from a mutation is the correct seam; runAfter(0) keeps notify's own
    transaction fast and isolates a slow/failing send from the in-app write). Keep notify's signature. Pin `kind` to
    NotificationKind at the call boundary if cheap. Tests (notifications.test.ts): notify inserts the in-app row and
    schedules dispatch; a dispatch with no token does not throw and inserts nothing further (no loop).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test notifications</automated>
  </verify>
  <done>notify inserts in-app + schedules external; dispatch is best-effort, never throws, never re-notifies; static §4 subject/body only.</done>
</task>

<task type="auto">
  <name>Task 2: Dead-letter user notification (both terminals)</name>
  <files>packages/backend/convex/deadLetter.ts, packages/backend/scripts/run-smoke-dlq.mjs</files>
  <action>
    In onPipelineComplete (after the deadletter.written audit, deadLetter.ts:49) add — only when a requestId ref is
    present in context.payload — `await ctx.runMutation(internal.notifications.notify, { tenantId: context.tenantId,
    kind: "deadletter", requestId, message: notificationMessage("deadletter") })`. Mirror the same in
    deadLetterRecipient (after its audit, deadLetter.ts:129) with its args.requestId. Payload/message refs-only (§4).
    run-smoke-dlq.mjs: extend to assert a deadletter notification row is written when a workflow dead-letters (the
    smoke already drives a failing workflow). Do NOT notify on the external-send failure path (loop guard — that path
    lives in notifyExternal and never reaches here).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test deadLetter</automated>
  </verify>
  <done>Both DLQ terminals fire a deadletter user notification beside their audit; smoke:dlq asserts it; no content in payload.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the §4 redaction scan + playbook</name>
  <files>packages/backend/convex/llmRedaction.test.ts, docs/playbooks/audit-dead-letter.md</files>
  <action>
    Extend the llmRedaction.test.ts static scan scope to the notify call-sites (pipeline.ts, cockpit.ts, deadLetter.ts,
    notifications.ts, notifyExternal.ts) and the deadLetter/worm payloads: assert no notify/dispatch call interpolates
    a content field (no `${...body}` / subject / recipient into message or the external subject/body) — messages come
    from notificationMessage (static) only. Verify the scan is RED on a deliberate content leak then revert (the house
    rule). audit-dead-letter.md: document the notification matrix (in-app choke point + best-effort external, fail-closed,
    loop-guarded) and the §4 static-scan coverage. Bump "Last verified".
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test llmRedaction && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The scan covers every notify/external site and fails on a content leak; playbook documents the matrix; check-playbooks green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test notifications deadLetter llmRedaction` green.
- `node scripts/check-playbooks.mjs` exit 0.
- Manual (phase gate): trigger each failure class (validation reject, escalation, retry breach, review timeout,
  agent timeout, dead-letter) → one in-app notification AND one external email per event; confirm a FAILED external
  send does not recursively notify.
</verification>

<success_criteria>
OPSG-05 met: notifications fire (in-app + external email) for validation rejection, escalations, retry-limit
breaches, timeouts, and dead-letter events; external dispatch is best-effort, fails closed to in-app, and never
loops; all payloads/messages are refs/counts-only (§4, statically enforced).
</success_criteria>

<output>
After completion, create `.planning/phases/07-resilience-operations-hardening/07-05-SUMMARY.md`
</output>
