---
phase: 07-resilience-operations-hardening
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-03"]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/backend/convex/runCockpitAgent.test.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [AGNT-04, REVW-02, OPSG-05]
must_haves:
  truths:
    - "An Executive Agent turn whose model call times out (abort exhausted after the CHEAP_MODEL retry) fires an agent.timeout notification and returns a non-dead-ending error turn (nothing sent)"
    - "The cockpit plan tracks a revise counter; re-proposing past MAX_REGENERATE marks the plan escalated + notifies (retry.limit) instead of looping unbounded"
    - "executePlan refuses to approve/send an escalated plan (fail closed — the cockpit equivalent of the pipeline unapproved-send guard)"
    - "The turn-lifecycle finally still terminalizes the activity trace on every exit (unchanged)"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "reviseCount increment + escalated guard in proposeEmailPlan/executePlan + agent.timeout notify in the driver"
      contains: "escalated"
  key_links:
    - from: "packages/backend/convex/cockpit.ts (sendCockpitMessage driver)"
      to: "internal.notifications.notify"
      via: "agent.timeout notification on an exhausted-timeout catch"
      pattern: "agent\\.timeout"
    - from: "packages/backend/convex/cockpit.ts (executePlan)"
      to: "plan.escalated guard"
      via: "refuse to approve an escalated plan (fail closed)"
      pattern: "escalated"
---

<objective>
Harden the LIVE cockpit path (AGNT-04 + REVW-02's cockpit equivalent, per owner ruling BOTH PATHS):
1. AGNT-04: the Executive-Agent tool-loop already bounds every model call with
   `AbortSignal.timeout(CALL_TIMEOUT_MS)` + one CHEAP_MODEL fallback, but on an EXHAUSTED timeout the
   driver just saves a generic error turn — no notification, no escalation. Fire an `agent.timeout`
   notification on that path (the driver's existing catch/finally is the seam), staying non-dead-ending.
2. REVW-02 (cockpit): the live gate has no retry counter. A re-propose (agent redraft of an
   already-proposed plan) is the cockpit's "regenerate". Count it on the plan; past `MAX_REGENERATE`
   mark the plan `escalated` + notify `retry.limit` and stop re-proposing (bounded, not unbounded).
   `executePlan` refuses to approve/send an escalated plan — the cockpit fail-closed guard mirroring
   the pipeline unapproved-send fix in 07-03.

Purpose: the shipped UX (cockpit) gets the timeout notification + a bounded, fail-closed review gate.
Output: agent-timeout notification + a cockpit revise cap that escalates and blocks a send.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-resilience-operations-hardening/07-CONTEXT.md
@.planning/phases/07-resilience-operations-hardening/07-RESEARCH.md
@packages/backend/convex/cockpit.ts

<interfaces>
From @pikar/core (07-01): `MAX_REGENERATE`, `notificationMessage("agent.timeout" | "retry.limit")`.
Schema (07-01): `plans.reviseCount?: number`, `plans.escalated?: boolean` (optional, no migration).

Seams (cite exactly):
- sendCockpitMessage driver (cockpit.ts:45-131): try { runCockpitAgent } catch { generic reply } finally { trace finish }.
  The catch is currently `catch {}` (no binding). AGNT-04 adds: bind the error, and on an exhausted-timeout/abort
  class failure fire `internal.notifications.notify({ tenantId: ctx.tenantId, kind: "agent.timeout",
  message: notificationMessage("agent.timeout") })` (requestId optional — a cockpit turn has none). Keep the
  non-dead-ending reply + the finally-terminalized trace byte-for-byte.
- runCockpitAgent/runAgentLoop (llm.ts:1512-1568): primary AbortSignal.timeout → isFallbackEligible → one
  CHEAP_MODEL retry; a second failure throws to the driver. A timeout IS fallback-eligible, so an exhausted
  timeout surfaces as the thrown error the driver catches. Expose enough (an error shape/flag) for the driver
  to recognize the timeout class without leaking content.
- proposeEmailPlan (cockpit.ts:340-352): patches the drafted plan + flips status → "proposed". This is the
  redraft seam — increment reviseCount here when the plan was ALREADY proposed before this call.
- executePlan (cockpit.ts:415-524): human approve gate. Add an `escalated` refusal sibling to the existing
  gmail_not_connected / send_time_too_far early guards (return { ok: false, reason: "review_escalated" }).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent-timeout notification (AGNT-04)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/runCockpitAgent.test.ts</files>
  <behavior>
    - A runCockpitAgent turn whose primary AND CHEAP_MODEL fallback both time out (abort) → the driver fires one
      agent.timeout notification and returns the non-dead-ending error reply. Nothing is sent; the trace finishes.
    - A normal (non-timeout) thrown failure → generic error turn, NO agent.timeout notification (don't over-notify).
  </behavior>
  <action>
    In runAgentLoop's catch (llm.ts:1565-1568): when the fallback also fails, ensure the propagated error is
    recognizable as a timeout/abort class (e.g. preserve the AbortError name, or wrap with a typed marker) WITHOUT
    including any prompt/content. In the driver (cockpit.ts:96-121): change `catch {` to `catch (e) {`, keep the
    generic reply, and if `e` is the timeout/abort class, `await ctx.runMutation(internal.notifications.notify, {
    tenantId: ctx.tenantId, kind: "agent.timeout", message: notificationMessage("agent.timeout") })`. The notify is
    inside the try/catch tail, NOT the finally (the finally stays trace-only, invariant 11). Add a runCockpitAgent.test.ts
    case: a scripted mock whose primary + fallback both throw an AbortError → assert an agent.timeout notification row
    was inserted and the reply is the non-dead-ending string. A non-abort throw asserts NO agent.timeout notify.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test runCockpitAgent</automated>
  </verify>
  <done>Exhausted-timeout turn inserts exactly one agent.timeout notification + returns the safe reply; a non-timeout failure inserts none; the finally still terminalizes the trace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cockpit revise cap → escalate + fail-closed executePlan (REVW-02)</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts</files>
  <behavior>
    - First propose of a plan → status "proposed", reviseCount stays 0/undefined.
    - Each RE-propose of an already-proposed plan → reviseCount += 1.
    - A re-propose that would push reviseCount past MAX_REGENERATE → set plans.escalated = true + notify retry.limit;
      the plan is NOT re-proposed for further edits (bounded).
    - executePlan on an escalated plan → { ok: false, reason: "review_escalated" } (no CAS flip, no seed, no send).
  </behavior>
  <action>
    In proposeEmailPlan (cockpit.ts:352): before the patch, read the current plan; if it was already "proposed"
    (a redraft), compute next = (reviseCount ?? 0) + 1. Use classifyReviewDecision or a direct compare to
    MAX_REGENERATE (reuse the @pikar/core constant — do NOT hardcode 3): if next > MAX_REGENERATE, patch
    { escalated: true } and `internal.notifications.notify({ tenantId, kind: "retry.limit", message:
    notificationMessage("retry.limit") })` and return a refusal WITHOUT re-proposing; else patch
    { ...content, status: "proposed", reviseCount: next }. In executePlan (cockpit.ts:428-433 guard cluster): add
    `if (plan.escalated) return { ok: false, reason: "review_escalated" }` as a sibling early guard (before the CAS
    flip) so an escalated plan can never be approved/sent — the cockpit fail-closed guard. Extend the executePlan
    return type union with "review_escalated". Tests (cockpit.test.ts, convex-test-runnable mutations): re-propose a
    plan 4× → escalated flag set + retry.limit notification + status not advanced; executePlan on an escalated plan
    returns review_escalated and seeds no requests / starts no workflow.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpit</automated>
  </verify>
  <done>Re-proposing past MAX_REGENERATE escalates + notifies; executePlan refuses an escalated plan (no send). MAX_REGENERATE is imported from @pikar/core, not inlined.</done>
</task>

<task type="auto">
  <name>Task 3: Playbooks</name>
  <files>docs/playbooks/cockpit.md, docs/playbooks/agent-runtime.md</files>
  <action>
    agent-runtime.md: extend the agent-timeout invariant (11) — an exhausted CALL_TIMEOUT_MS abort now fires an
    agent.timeout notification via the driver's catch (non-dead-ending; finally stays trace-only). Bump "Last verified".
    cockpit.md: document the live-gate revise cap (proposeEmailPlan increments reviseCount; past MAX_REGENERATE →
    escalated + retry.limit notify) and the executePlan escalated fail-closed refusal (review_escalated). Bump "Last verified".
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Both playbooks document the new invariants; check-playbooks exit 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test runCockpitAgent cockpit` green.
- `node scripts/check-playbooks.mjs` exit 0.
- Manual (phase gate): force an agent turn to time out and confirm one in-app agent.timeout notification +
  the safe reply; re-ask the agent to redraft a plan >3× and confirm it escalates + Approve is refused.
</verification>

<success_criteria>
AGNT-04: an Executive-Agent timeout fires an escalation notification and stays non-dead-ending. REVW-02
(cockpit): the live gate is bounded and fails closed — a past-cap redraft escalates + notifies and cannot be
sent. Both notification kinds (agent.timeout, retry.limit) feed the OPSG-05 matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/07-resilience-operations-hardening/07-04-SUMMARY.md`
</output>
