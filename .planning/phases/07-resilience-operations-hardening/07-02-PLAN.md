---
phase: 07-resilience-operations-hardening
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/backend/convex/wormCursor.ts
  - packages/backend/convex/worm.ts
  - packages/backend/convex/worm.test.ts
  - packages/backend/scripts/run-smoke-worm.mjs
  - docs/playbooks/audit-dead-letter.md
autonomous: true
requirements: [OPSG-03]
user_setup:
  - service: aws-s3
    why: "WORM (S3 Object Lock) immutable audit archive — OPSG-03"
    env_vars:
      - name: WORM_BUCKET
        source: "AWS S3 — a bucket created with Object Lock ENABLED (cannot be enabled after creation)"
      - name: AWS_ACCESS_KEY_ID
        source: "AWS IAM — set via `npx convex env set` (NEVER Vercel — Convex env, §7)"
      - name: AWS_SECRET_ACCESS_KEY
        source: "AWS IAM — set via `npx convex env set`"
      - name: AWS_REGION
        source: "AWS — the bucket's region"
    dashboard_config:
      - task: "Create an S3 bucket with Object Lock enabled at creation + a default COMPLIANCE retention"
        location: "AWS S3 console → Create bucket → Object Lock: Enable"
must_haves:
  truths:
    - "The daily cron exports new audit rows to S3 as NDJSON under COMPLIANCE Object Lock with a checksum"
    - "The WORM cursor advances ONLY after a confirmed durable PutObject — never on a throw/partial write"
    - "auditSince windows the audit table by the by_ts index (no full .collect() scan)"
    - "With WORM_BUCKET unset the action still takes the clean stub-skip path (never advances the cursor)"
    - "The hot audit table is never deleted/swept (export-only; SC#4 sweep DEFERRED per owner)"
  artifacts:
    - path: "packages/backend/convex/worm.ts"
      provides: "Real S3 PutObject export replacing the Phase-1 throw"
      contains: "PutObjectCommand"
    - path: "packages/backend/convex/wormCursor.ts"
      provides: "auditSince rewritten onto the by_ts index"
      contains: "withIndex(\"by_ts\""
  key_links:
    - from: "packages/backend/convex/worm.ts"
      to: "@aws-sdk/client-s3 PutObjectCommand"
      via: "COMPLIANCE Object Lock + ChecksumAlgorithm SHA256 export"
      pattern: "ObjectLockMode.*COMPLIANCE"
    - from: "packages/backend/convex/worm.ts"
      to: "internal.wormCursor.advanceCursor"
      via: "advance ONLY after the PutObject promise resolves"
      pattern: "advanceCursor"
---

<objective>
Complete the OPSG-03 WORM stub: the daily cron's `worm.exportAudit` action becomes a real
`@aws-sdk/client-s3` PutObject of the incremental audit window as NDJSON, under COMPLIANCE-mode
Object Lock with a checksum, advancing the cursor only after a confirmed durable write. Rewrite
`auditSince` onto the new `by_ts` index so the export never full-scans the unbounded audit table.

Per owner ruling (07-CONTEXT): EXPORT ONLY. Do NOT add any delete/sweep of the hot Convex `audit`
table — SC#4's sweep clause is DEFERRED this phase (§3/ADR-002 stay literally intact, no new ADR).
Purpose: true immutability lives outside Convex; this closes the compliance-archive half of SC#4.
Output: a working real WORM export + an index-backed export window + an extended live smoke.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-resilience-operations-hardening/07-CONTEXT.md
@.planning/phases/07-resilience-operations-hardening/07-RESEARCH.md
@packages/backend/convex/worm.ts
@packages/backend/convex/wormCursor.ts

<interfaces>
From @pikar/core (07-01): `serializeAuditNdjson(rows)`, `wormObjectKey(sinceTs, maxTs)`,
`retainUntilDate(nowMs)`, `RETENTION_MS`.

worm.ts today (STUB): reads the cursor, clean no-op + return when WORM_BUCKET unset (must stay),
else the Phase-7 branch that currently `throw`s. The exact recipe + AWS gotchas are inline at
worm.ts:41-56. worm.ts is "use node" (may hold ONLY actions — cursor DB logic stays in wormCursor.ts).

Verified PutObject shape (07-RESEARCH Pattern 1, AWS docs):
```
new PutObjectCommand({
  Bucket: process.env.WORM_BUCKET,
  Key: wormObjectKey(since, maxTs),
  Body: ndjson,
  ChecksumAlgorithm: "SHA256",              // REQUIRED with Object Lock retention (Pitfall 4)
  ObjectLockMode: "COMPLIANCE",             // must be paired with a retain-until date
  ObjectLockRetainUntilDate: retainUntilDate(Date.now()),
})
```
wormCursor.auditSince (rewrite target) currently `ctx.db.query("audit").collect()` then filters —
replace with `.withIndex("by_ts", q => q.gt("ts", since)).take(limit ?? 10_000)` (index added in 07-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite auditSince onto the by_ts index</name>
  <files>packages/backend/convex/wormCursor.ts, packages/backend/convex/worm.test.ts</files>
  <action>
    Replace the full-scan in `auditSince` with an index range read on the by_ts index added in 07-01:
    `ctx.db.query("audit").withIndex("by_ts", q => q.gt("ts", since)).take(limit ?? 10_000)`. Rows come back
    ts-ascending from the index, so drop the manual `.sort`. Remove the stale ponytail comment about the
    missing index. getCursor/advanceCursor are unchanged. Extend worm.test.ts to assert auditSince returns
    only rows with ts > since, oldest-first, bounded by limit — and that it does not read the whole table
    (seed rows across ts, assert the window).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test worm</automated>
  </verify>
  <done>auditSince uses withIndex("by_ts", ...); the window test passes; no .collect() on audit remains.</done>
</task>

<task type="auto">
  <name>Task 2: Real S3 PutObject export in worm.ts (advance only after durable write)</name>
  <files>packages/backend/convex/worm.ts, packages/backend/convex/worm.test.ts</files>
  <action>
    Replace the `throw` in the WORM_BUCKET-set branch with the real export:
    1. `const rows = await ctx.runQuery(internal.wormCursor.auditSince, { since })`. If rows.length === 0,
       return `{ exported: 0 }` WITHOUT a PutObject and WITHOUT advancing (nothing to archive).
    2. `const ndjson = serializeAuditNdjson(rows)` (@pikar/core).
    3. `const maxTs = rows.reduce((m, r) => Math.max(m, r.ts), since)`.
    4. `await s3.send(new PutObjectCommand({ ... }))` per the verified shape above (COMPLIANCE + SHA256 +
       paired retain-until from retainUntilDate). Construct the S3Client once at module scope from
       AWS_REGION. Await the promise — the cursor advance is UNREACHABLE unless PutObject resolves.
    5. ONLY after a resolved PutObject: `await ctx.runMutation(internal.wormCursor.advanceCursor, { ts: maxTs })`.
       On any throw, do NOT advance — the next cron retries the SAME window (same key ⇒ idempotent). Let the
       error propagate (the cron's own failure handling / logs own it) — do not swallow it into a false success.
    KEEP the WORM_BUCKET-unset stub-skip branch byte-for-byte (worm.test.ts locks that it never advances).
    Do NOT delete/sweep any audit rows (export-only, owner ruling).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test worm</automated>
  </verify>
  <done>Stub-skip path unchanged + never advances; the real branch serializes → PutObject(COMPLIANCE,SHA256) → advanceCursor, and advance is unreachable on a PutObject throw (test with a mocked S3 send).</done>
</task>

<task type="auto">
  <name>Task 3: Extend the live smoke + playbook</name>
  <files>packages/backend/scripts/run-smoke-worm.mjs, docs/playbooks/audit-dead-letter.md</files>
  <action>
    run-smoke-worm.mjs: keep the existing stub-skip assertion as the default (WORM_BUCKET unset). Add a second
    mode gated on `process.env.WORM_BUCKET` being set: run worm:exportAudit and assert the output reports a
    non-negative export count (not the skip line). Do NOT try to assert S3 durability from Node — that is the
    manual console inspection (07-VALIDATION Manual-Only): the smoke proves the action runs against a live
    deployment; a human confirms one object in the S3 console carries ObjectLockMode + RetainUntilDate + checksum.
    audit-dead-letter.md: document the real export path, the "advance only after durable write" invariant, the
    Object-Lock bucket precondition (enabled at creation) + the Convex env vars (NEVER Vercel), and record that
    the hot-copy sweep is DEFERRED (export met, sweep deferred — SC#4 partial). Bump "Last verified".
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>smoke:worm has a bucket-set path; playbook documents the export invariants + the deferred sweep; check-playbooks green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test worm` green (index window + real-export-advances-only-on-success).
- `node scripts/check-playbooks.mjs` exit 0.
- Manual (phase gate): `npm run smoke:worm` against a real Object-Lock bucket; inspect ONE object in the S3
  console — ObjectLockMode/RetainUntilDate + checksum present, and a delete attempt is refused.
</verification>

<success_criteria>
OPSG-03 export half met: audit rows export on schedule to WORM S3 under COMPLIANCE Object Lock with a
checksum, the cursor advances only after a durable write, and auditSince no longer full-scans. SC#4 is
partially met — export implemented, hot-copy sweep DEFERRED per owner (the verifier records this, not a gap).
</success_criteria>

<output>
After completion, create `.planning/phases/07-resilience-operations-hardening/07-02-SUMMARY.md`
</output>
