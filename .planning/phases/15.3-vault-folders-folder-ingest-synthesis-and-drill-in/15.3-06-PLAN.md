---
phase: 15.3-vault-folders-folder-ingest-synthesis-and-drill-in
plan: 06
type: execute
wave: 5
depends_on: [01, 04, 05]
files_modified:
  - packages/contracts/skills/folder-digest.md
  - packages/contracts/src/skills/folderDigest.ts
  - packages/contracts/src/skill.ts
  - packages/contracts/src/skillBodies.test.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/vaultDigest.ts
  - packages/backend/convex/vaultDigest.test.ts
  - packages/backend/convex/vaultFolders.ts
  - docs/playbooks/vault.md
autonomous: true
requirements: [VALT-08, VALT-09, VALT-10]

must_haves:
  truths:
    - "The digest is retrievable through the normal rails — proven by ragEntryId, never by its origin literal"
    - "The digest states what the folder could not read"
    - "A digest is never a member of its own source set"
    - "Adding a member marks the digest stale; nothing costs money until the user clicks Rebuild"
    - "Staleness is exact at folder scale — not bounded by the global 100-row drift cap"
  artifacts:
    - path: "packages/backend/convex/vaultDigest.ts"
      provides: "the V8 synthesis action + the stale set-difference"
      contains: "startIngest"
  key_links:
    - from: "packages/backend/convex/vaultDigest.ts"
      to: "packages/backend/convex/vaultIngest.ts"
      via: "the digest is a vault document — it grounds ONLY because its insert calls startIngest"
      pattern: "startIngest"
---

<objective>
Synthesise the folder digest as a vault document, and surface staleness with the 17.1 rebuild idiom.

**The single most misreadable fact in this phase:** `origin:"folder_digest"` is INERT. There is no
`origin` predicate anywhere in retrieval — the `origin:"agent"` exclusion is the *absent*
`startIngest` call (`vault.ts:627-634`). So the digest becomes groundable only because its insert
calls `startIngest`, and a plan that forgets that call produces a silently ungroundable digest **with
every origin-literal test still green.** The observable check is `ragEntryId != null`.

Output: the `folder-digest` skill row, a V8 synthesis action, the insert-plus-ingest, and an exact
folder-scoped staleness diff.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-vault-folders-folder-ingest-synthesis-and-drill-in/15.3-CONTEXT.md
@.planning/phases/15.3-vault-folders-folder-ingest-synthesis-and-drill-in/15.3-RESEARCH.md
@packages/backend/convex/research.ts
@packages/backend/convex/vaultLlm.ts
@packages/backend/convex/blueprint.ts

<interfaces>
`research.ts:154-184` is the EXACT template for an agent-synthesised markdown document that grounds:
insert with a `kind` marker, `categoryFor({source:"agent"})`, `mimeType:"text/markdown"`,
`status:"processing"`, then `startIngest` with a lineage `correlationId`.

`vaultLlm.ts` is the shape for the action: **V8, NOT `"use node"`** (`:3-7` explains a second node
module re-triggers the TS internal-graph inference cliff), explicit `Promise<...>` return type, skill
loaded FIRST and fail-closed (`:113-118`), `scanText` before the model (`:126`), `SMOKE::` sentinel
(`:81-100`), `priceUsage` (`:146`).

`blueprint.ts` staleness idiom: `unincorporatedFor` (`:553-574`) + `blueprintState` (`:428-439`) +
`confirmBlueprint` (`:372-378`) rewriting the source-doc list. `DRIFT_SCAN_CAP = 100` (`:42`) is a
**`.take()`, not a result limit** — cloned verbatim onto a 300-member folder it inspects an arbitrary
100 and can report 0 stale while dozens are.

`seedSkills` (`skills.ts:360-369`) inserts at v1/active only when `rows.length === 0`; otherwise
`maxVersion + 1`. **Never pin a version number in a plan.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The folder-digest skill row</name>
  <files>packages/contracts/skills/folder-digest.md, packages/contracts/src/skills/folderDigest.ts, packages/contracts/src/skill.ts, packages/backend/convex/skills.ts, packages/contracts/src/skillBodies.test.ts</files>
  <action>
CLAUDE.md §5 — no hardcoded prompts. Five mechanical edits, modelled on `graph-extractor`:

1. `packages/contracts/skills/folder-digest.md` — the canonical prompt. Its OUTPUT CONTRACT section
   must name all three required parts: **(1) what the folder IS** (manifest — counts, kinds, date
   range, per-document one-line identity), **(2) what it SAYS** (cross-document synthesis), **(3)
   what could NOT be read** (failed/unsupported documents, stated plainly). Part 3 is not optional
   and must be stated in the contract section, not in guidance prose.
2. `packages/contracts/src/skills/folderDigest.ts` — the body constant, hand-derived, LF-normalised
   (there is no generator script).
3. `FOLDER_DIGEST_SKILL = "folder-digest" as const` in `skill.ts`.
4. **APPEND** a seeds row at the END of the array in `skills.ts` (it carries an append-only warning).
5. A drift row in `skillBodies.test.ts`'s `bodies` array so MD↔TS byte identity is enforced.

Mark it UNGATED with the `CONTENT_DRAFTER_SKILL` rationale block: `run-eval-golden.mjs` validates
`--skill` against a list derived from `GATED_SKILLS` and drives `runCockpitAgent` over text fixtures,
so there is no fixture that reaches a folder digest — gating it deadlocks the skill at v1 on its
first body edit. Add `expect(isGatedSkill(FOLDER_DIGEST_SKILL)).toBe(false)`.
  </action>
  <verify>
    <automated>cd packages/contracts &amp;&amp; npx vitest run 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>The skill exists in all five places, is pinned ungated, and MD↔TS drift is a test failure.</done>
</task>

<task type="auto">
  <name>Task 2: The synthesis action</name>
  <files>packages/backend/convex/vaultDigest.ts</files>
  <action>
A V8 `internalAction` (no `"use node"`), explicit return type. Order:

1. Load `FOLDER_DIGEST_SKILL` fail-closed via `internal.skills.getActiveSkill` — FIRST, so the
   offline path exercises it too.
2. `SMOKE::digest::` sentinel short-circuit for convex-test.
3. Gather inputs **without collecting member `text`**:
   - Parts 1 and 3 from PROJECTED metadata only — title, kind, docType, identityLine, status,
     failureReason, size, createdAt. No text.
   - Part 2 from a BOUNDED head slice per member (`capGraphText` is the precedent) under an overall
     char budget mirroring `vaultGround.ts:29-30`'s `PER_DOC_CHAR_CAP`/`TOTAL_CHAR_CAP`.
   A folder of 300 rows × 400 KB is 120 MB; collecting member text would blow the read cap on the
   very feature that is supposed to make a big folder usable.
4. `scanText` fail-closed before the model call.
5. `generateObject`/`generateText`, priced with `priceUsage`, spend recorded on the **ingest** rail
   (plan 03's selector) — the digest is folder work.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "vaultDigest\.ts" ; test $? -ne 0</automated>
  </verify>
  <done>The action loads its skill first, never reads member text wholesale, and charges the ingest rail.</done>
</task>

<task type="auto">
  <name>Task 3: Insert the digest as a vault document — and INGEST it</name>
  <files>packages/backend/convex/vaultDigest.ts, packages/backend/convex/vaultFolders.ts</files>
  <action>
Copy `research.ts:167-184` verbatim in shape:

```typescript
kind: "folder_digest", category: categoryFor({ source: "agent" }), source: "agent",
mimeType: "text/markdown", text: markdown, status: "processing", origin: "folder_digest",
```
then **`await startIngest(ctx, { vaultDocId, tenantId, correlationId })`**. Patch
`digestDocId`/`digestSourceDocIds`/`digestBuiltAt` on the folder in the same mutation, mirroring
`blueprint.ts:372-378`.

**Recursion guard — the chosen shape:** the digest carries **NO `folderId`**; the folder points at it
via `digestDocId`. A digest then cannot enter a member-set query at all, so no predicate is needed
anywhere and drill-in renders it from `folder.digestDocId` with one `ctx.db.get`. This mirrors
`unincorporatedFor`'s `kind !== "business_blueprint"` self-exclusion — a synthesis must never be an
input to itself.

Wire the completion transition in `vaultFolders.ts` (plan 04 left the named call) to schedule this.
Order at completion is fixed: settle → digest → flip to `"complete"`.

Put a loud comment on the `startIngest` line: **this call is the only reason the digest is
groundable. Deleting it leaves every origin-literal assertion green and the feature silently dead.**
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultDigest.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>The digest row is inserted and ingested; it has no `folderId`; the folder records its id and sources.</done>
</task>

<task type="auto">
  <name>Task 4: Exact folder-scoped staleness</name>
  <files>packages/backend/convex/vaultDigest.ts</files>
  <action>
Clone `unincorporatedFor`'s shape as `unincorporatedForFolder(ctx, tenantId, folderId, sourceDocIds)`:
read members via `by_tenant_folder`, filter `status === "ready" && !sourceSet.has(_id)`, return
`{count, docIds}`.

**Bound it by the folder's own `memberCount + 1`, NOT by the global `DRIFT_SCAN_CAP = 100.`** A
folder's member count is known and bounded by pre-flight, so this keeps the 17.1 idiom (a real set
difference, which the drill-in needs because it renders the stale ids) while being exact. Cloning
`.take(100)` onto a 300-member folder returns 0 stale while dozens are stale and the banner silently
never fires.

Expose a folder read that returns `{state: "none"|"fresh"|"stale", unincorporatedCount}` — the
`blueprintState:428-439` pattern — plus a `rebuildDigest` mutation that re-runs synthesis and
rewrites `digestSourceDocIds` (the `confirmBlueprint:372-378` shape). **No model call fires until the
user clicks.**
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultDigest.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>Staleness is exact for folders of any size and costs nothing until Rebuild.</done>
</task>

<task type="auto">
  <name>Task 5: Tests</name>
  <files>packages/backend/convex/vaultDigest.test.ts</files>
  <action>
1. **Groundability — the assertion that matters.** After completion, `ragEntryId != null` AND
   `runVaultGround` returns the digest. *Mutation: delete the `startIngest` call → the `origin ===
   "folder_digest"` assertion stays GREEN and the `ragEntryId` assertion goes RED.* Write both
   assertions side by side so the difference is on the page.
2. **Non-recursion.** `!digestSourceDocIds.includes(digestDocId)`, and `unincorporatedForFolder`
   returns 0 immediately after a build. *Mutation: give the digest a `folderId` → count goes to 1 and
   the banner fires forever.*
3. **Staleness fires and clears.** Build → add a member → 1 → rebuild → 0.
4. **Scan-cap sufficiency — the test that exposes the real risk.** Seed `DRIFT_SCAN_CAP + 20` members
   where only the LAST-indexed 5 are unincorporated; assert the count is **5**. A naive `.take(100)`
   clone returns 0.
5. **Part 3 is present.** A folder with one failed member produces a digest whose text names the
   unreadable document. The honest-manifest promise lives in the artifact the agent reads, not only
   in the UI.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultDigest.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>All five pass with their mutations run and recorded.</done>
</task>

<task type="auto">
  <name>Task 6: Playbook</name>
  <files>docs/playbooks/vault.md</files>
  <action>
`### 15.3-06 — the folder digest`: the inert-literal warning and that `ragEntryId` is the observable
check; the no-`folderId` recursion guard; the three-part contract with part 3 mandatory; the
member-count-bounded diff and why the global cap was not reused; the ungated skill and the
`seedSkills` version rule (verify with `getActiveSkill`, never pin a number). Bump `Last verified`.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Playbook updated; hook exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm test` green; `pnpm typecheck` clean.
- `npx convex run skills:getActiveSkill '{"name":"folder-digest"}'` returns a row — read back whatever
  version appears; do not assert v1.
- The digest appears in grounding for a completed folder and not for an ingesting one.
</verification>
