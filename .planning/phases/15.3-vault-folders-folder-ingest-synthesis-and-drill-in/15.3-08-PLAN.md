---
phase: 15.3-vault-folders-folder-ingest-synthesis-and-drill-in
plan: 08
type: execute
wave: 8
depends_on: [01, 03, 04, 07]
files_modified:
  - packages/core/src/docType.ts
  - packages/core/src/docType.test.ts
  - packages/core/src/index.ts
  - packages/contracts/skills/document-classifier.md
  - packages/contracts/src/skills/documentClassifier.ts
  - packages/contracts/src/skill.ts
  - packages/contracts/src/skillBodies.test.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/vaultLlm.ts
  - packages/backend/convex/vaultIngest.ts
  - packages/backend/convex/vault.ts
  - packages/backend/convex/vaultClassify.test.ts
  - packages/vault/src/constants.ts
  - apps/web/app/(app)/dashboard/vault/PreviewModal.tsx
  - apps/web/app/(app)/dashboard/vault/DocGrid.tsx
  - docs/playbooks/vault.md
autonomous: true
requirements: [VALT-12]

must_haves:
  truths:
    - "A user-set identity is NEVER overwritten by re-classification"
    - "A classifier failure degrades the label — it never fails the document"
    - "A model string outside the closed union can never reach the database"
    - "Single-file uploads are classified too, not only folder members"
    - "The downloaded file still carries its real filename"
  artifacts:
    - path: "packages/core/src/docType.ts"
      provides: "the closed DOC_TYPES union + label totality table + isDocType guard"
      exports: ["DOC_TYPES", "DOC_TYPE_LABEL", "isDocType"]
  key_links:
    - from: "packages/backend/convex/vaultIngest.ts"
      to: "packages/backend/convex/vaultLlm.ts"
      via: "ONE new workflow step at the single convergence point for every text-producing path"
      pattern: "classifyDoc"
---

<objective>
Give every document a machine-derived type and a human-readable identity line — *"2025 P&L"*, not
*"a spreadsheet"* — that the user can correct and that nothing ever overwrites.

This is a first-class classifier, so it runs for **single-file uploads too**, not only folder
members. One workflow step at the single convergence point covers both.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.3-vault-folders-folder-ingest-synthesis-and-drill-in/15.3-CONTEXT.md
@.planning/phases/15.3-vault-folders-folder-ingest-synthesis-and-drill-in/15.3-RESEARCH.md
@packages/core/src/businessProfile.ts
@packages/backend/convex/vaultLlm.ts
@packages/backend/convex/vaultIngest.ts
@apps/web/app/(app)/dashboard/profile/ShapePanel.tsx

<interfaces>
Closed-union idiom: `export const X = [...] as const` + `type X = (typeof X)[number]`, plus a
separate `as const satisfies Record<Union, T>` label table (`actionType.ts:45-56`) so a missing
member is a **compile error at the table**, never a silent ternary fallthrough.

**Union home: `packages/core`.** `apps/web` does not depend on `@pikar/vault`, and both
`Dropzone.tsx:22-26` and `CategoryTabs.tsx:13-15` record that as deliberate — with the duplicated
caps being the defect it produced. `ShapePanel.tsx` already imports `REVENUE_STAGES` from
`@pikar/core` to drive a `<select>`: the exact precedent for this UI shape. *(Plan 02 added
`@pikar/vault` as a web dep for constants; the union still goes in core, because that is where the
`<select>`-driving precedent lives. Do not create a third local re-declaration.)*

Schema fields already landed in plan 01: `docType`, `identityLine`, `identityUserSet`.

`vaultIngest.ingestDoc` (`:104-143`) — the ONE convergence point: direct TXT/MD/CSV reach it via
`vault.ts:198-201`, every deferred binary via `vault.ingestExtractedText` (`:738-758`), and
`vaultSweep.retryExtraction` re-enters it.

`PreviewModal` has **no existing edit affordance** — it is read-only. `ShapePanel.tsx` is the
edit-form precedent (`<select>` + `<input>` + save, `field`/`label`/`primaryButton` from
`profile/styles.ts`), including its `useRef` seed guard at `:108-124`.

`seedSkills` assigns `maxVersion + 1`. **Never pin a version.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The closed union</name>
  <files>packages/core/src/docType.ts, packages/core/src/docType.test.ts, packages/core/src/index.ts</files>
  <action>
```typescript
export const DOC_TYPES = ["pnl","balance_sheet","cash_flow","invoice","contract","policy",
  "deck","report","plan","correspondence","spreadsheet_other","unclassified"] as const;
export type DocType = (typeof DOC_TYPES)[number];
export const DOC_TYPE_LABEL = { … } as const satisfies Record<DocType, string>;
export const isDocType = (x: unknown): x is DocType => …;
```
`unclassified` is required by the locked decision and is also the classifier's fallback.

Add the **two-direction compile bridge** beside the classify code (mirroring `tenantProfile.ts:58-67`)
so a literal added to the schema union but not the TS union — or vice versa — is a **typecheck
failure**, not a runtime surprise.

Tests: the `actionType.test.ts:84-91` pair — a COMPLETE label table typechecks, a `_MISSING_ARM` twin
is `@ts-expect-error`. That pins the mechanism rather than merely relying on it. Plus `isDocType`
coercion: `"financial_statement"`, `"P&L"`, `""` all fail the guard.
  </action>
  <verify>
    <automated>cd packages/core &amp;&amp; npx vitest run src/docType.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>The union, label totality and guard exist; a member without a label is a compile error.</done>
</task>

<task type="auto">
  <name>Task 2: The classifier skill row</name>
  <files>packages/contracts/skills/document-classifier.md, packages/contracts/src/skills/documentClassifier.ts, packages/contracts/src/skill.ts, packages/backend/convex/skills.ts, packages/contracts/src/skillBodies.test.ts</files>
  <action>
Same five mechanical edits as the digest skill. Two specifics:

- **The literal enum keys belong in the OUTPUT CONTRACT section, not in guidance prose** — a recorded
  lesson from this repo. List the `DOC_TYPES` literals verbatim, plus *"use `unclassified` when none
  fits — never force a nearest match."*
- **DELIBERATELY UNGATED.** Copy the `CONTENT_DRAFTER_SKILL` rationale block: `run-eval-golden.mjs`
  drives `runCockpitAgent` over text fixtures and has no fixture that reaches vault ingest, so gating
  this deadlocks it at v1 on its first body edit. Three skills already carry that warning. Pin it:
  `expect(isGatedSkill(DOCUMENT_CLASSIFIER_SKILL)).toBe(false)`.
  </action>
  <verify>
    <automated>cd packages/contracts &amp;&amp; npx vitest run 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>The skill exists in all five places, ungated and drift-guarded.</done>
</task>

<task type="auto">
  <name>Task 3: classifyDoc, and the one workflow step</name>
  <files>packages/backend/convex/vaultLlm.ts, packages/backend/convex/vaultIngest.ts, packages/vault/src/constants.ts</files>
  <action>
**`vaultLlm.classifyDoc`** — clone `extractGraph` in the SAME module (keeps the V8 / no-`"use node"`
rule and the explicit-return-type mitigation): skill loaded fail-closed FIRST; `getDocText`;
`scanText` fail-closed before the model; a `SMOKE::classify::<docType>|<identity line>` sentinel above
the cap so convex-test drives it offline; `generateObject` with a `jsonSchema` whose `docType`
carries `enum: [...DOC_TYPES]` so the provider constrains output rather than prose alone; then
**validate with `isDocType` and coerce anything else to `"unclassified"`** — never trust a model
string into a closed union.

**Input slice:** add `DOC_CLASSIFY_CHAR_CAP = 8_000` + `capClassifyText` to
`packages/vault/src/constants.ts` — identity lives in the first page, so do NOT reuse the 120k graph
cap. Assert `DOC_CLASSIFY_CHAR_CAP < GRAPH_EXTRACT_CHAR_CAP` in `constants.test.ts` (which already
asserts the analogous graph<extract relationship).

**⚠ A classifier failure must NOT fail the document.** A throw in `step.runAction` exhausts retries
and then `onIngestComplete` marks the row `failed` — so an unseeded skill row would fail EVERY ingest
in the deployment, and by the honest-manifest rule would inflate the folder's failure count for a
cosmetic label. **Wrap the model call in try/catch INSIDE `classifyDoc`** and return
`{docType:"unclassified", identityLine:"", costUsd:0}` on any throw. `ponytail:` comment on the
deliberate asymmetry: `getActiveSkill` is fail-closed by contract, but here the fallback is a degraded
LABEL, not a degraded grounding corpus.

**The workflow step:** ONE new step in `ingestDoc` between extractGraph and `recordSpend`, then an
`applyClassification` mutation. **Fold `identity.costUsd` into the existing single `recordSpend`
sum** — its own call would charge a fresh `Math.ceil` cent per document ($3 for $0.10 of real spend
over 300 docs). Cost is ~$0.00034/doc; 300 docs ≈ $0.10 = 0.4% of the window.

Sequence the seed before this reaches production.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultClassify.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>Classification runs once per document on every path, degrades on failure, and adds no second spend call.</done>
</task>

<task type="auto">
  <name>Task 4: User-set wins — in code, not in a prompt</name>
  <files>packages/backend/convex/vault.ts</files>
  <action>
**`internal.vault.applyClassification`** (internalMutation beside `markReady`): tenant guard, then
```typescript
if (doc.identityUserSet === true) return null;
```
and only then patch. **There must be NO branch anywhere that writes docType/identityLine over a
user-set row — that ABSENCE is the guarantee**, in the exact sense `blueprint.ts:358-364` records for
`mergeBlueprint`. The paths that make this load-bearing already ship: `retryExtraction` and
`ingestExtractedText` both restart `ingestDoc` on an existing row.

**`setDocIdentity`** (tenantMutation beside `deleteVaultDoc`): derive the `docType` validator from
the schema (`schema.tables.vaultDocuments.validator.fields.docType` — the `tenantProfile.ts:47`
rung-2 precedent) so the arg union cannot drift from the table. **Trim and length-cap `identityLine`
(~120 chars) at the write boundary** — it is user input that will be rendered into the folder digest
and therefore into a model prompt, so it is a trust boundary, not a cosmetic field
(`sanitizeAgentName` is the precedent). Set `identityUserSet: true`.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultClassify.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL"</automated>
  </verify>
  <done>A user-set row survives re-classification unchanged; the identity line is sanitised at the boundary.</done>
</task>

<task type="auto">
  <name>Task 5: The two UI surfaces</name>
  <files>apps/web/app/(app)/dashboard/vault/PreviewModal.tsx, apps/web/app/(app)/dashboard/vault/DocGrid.tsx</files>
  <action>
**PreviewModal** — add an Identity block between the `<dl>` metaRows (`:308-331`) and the failure box
(`:334`): a `<select>` over `DOC_TYPES` rendering `DOC_TYPE_LABEL[t]`, an `<input>` for the identity
line, and Save calling `setDocIdentity`. Widen the existing `busy` state machine (`:38`) with
`"identity"` rather than adding a second state. **Seed both fields ONCE behind a `useRef` guard**
(`ShapePanel.tsx:108-124`) — `listVaultDocs` is a live subscription and re-seeding on every row update
clobbers an in-progress edit. Show a small "You set this" note when `identityUserSet` is true, so the
never-overwritten promise is visible.

**DocGrid** — no query change needed (`VaultDoc` derives from the query's return type). Primary span
(`:298-310`) becomes `{doc.identityLine || doc.title}` with `title={doc.title}` kept as tooltip;
secondary line (`:311-313`) becomes `{doc.title} · {fmtSize(doc.size)}`. Optionally a
`DOC_TYPE_LABEL` chip beside the existing chips — meaning in the label, not the colour (BRAND §6).

**Do NOT touch** `a.download = doc.title` (`PreviewModal.tsx:68`) or `alt={doc.title}` (`:167`) — the
downloaded file must keep its real filename. Update the `aria-label`s (`DocGrid:357/:375/:391`,
`PreviewModal:112/:493`) to follow the visible primary line so the accessible name matches the screen.

`voice/DocPicker.tsx` still renders titles — out of scope, noted so "why does the picker show
filenames" is a known gap, not a bug report.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -3</automated>
  </verify>
  <done>The grid shows identities, the modal edits them, and downloads keep real filenames.</done>
</task>

<task type="auto">
  <name>Task 6: Tests + playbook</name>
  <files>packages/backend/convex/vaultClassify.test.ts, docs/playbooks/vault.md</files>
  <action>
1. **User-set-wins — the one must-have behavioural test.** Seed a doc, `setDocIdentity`, then call
   `applyClassification` with DIFFERENT values → the row is byte-unchanged. *Mutation: delete the
   guard → RED.* **Then the same test with `identityUserSet` absent must show the patch landing** —
   otherwise the guard could be trivially satisfied by never writing at all.
2. **Fail-closed unseeded, degrading not failing:** run ingest without `seedSkills` → `getActiveSkill`
   throws `NO_ACTIVE_SKILL` inside `classifyDoc`, **and the document still reaches `ready`** with
   `unclassified`.
3. **Closed-union coercion** into the DB (the guard that keeps the schema `v.union` from throwing an
   unhandled "invalid argument" inside the workflow).
4. **Offline end-to-end** via the `SMOKE::classify::` sentinel through the whole `ingestDoc` workflow.

Playbook `### 15.3-08 — document identity`: the union home and why not `packages/vault`; the ungated
pin; the degrade-don't-fail asymmetry; user-set precedence as an ABSENCE; the identity line as a trust
boundary; `unclassified` vs absent (grid falls back to filename for both, only the digest manifest
distinguishes them). Bump `Last verified`.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/vaultClassify.test.ts 2>&amp;1 | grep -E "Tests +[0-9]+ passed|FAIL" &amp;&amp; node ../../scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All four pass with mutations run; playbook updated; hook exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm test` green; `pnpm typecheck` clean (guarantees 26–27 are COMPILE checks — a green vitest run
  does not sample them).
- `npx convex run skills:getActiveSkill '{"name":"document-classifier"}'` returns a row; read back
  whatever version appears.
- Live: classify one known document and read the recorded `costUsd` — check it against the
  ~$0.00034/doc estimate rather than asserting from pricing arithmetic.
</verification>
