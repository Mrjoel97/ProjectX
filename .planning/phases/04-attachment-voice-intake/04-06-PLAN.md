---
phase: 04-attachment-voice-intake
plan: 06
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - docs/playbooks/intake.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [INTK-02, INTK-03]
user_setup: []

must_haves:
  truths:
    - "the full offline suite (pnpm test) + Playwright intake spec are green before human-verify"
    - "on the live backend, a real attachment and a real dictation each produce a delivered email whose content reflects them, past PII/cost/review guardrails (SC3)"
    - "the sidecar-killed architectural decision is logged in STATE.md, amending the ROADMAP 'Python sidecar' phrasing"
    - "intake.md Last-verified is bumped to bless the phase's accumulated intake surface (§9)"
  artifacts:
    - path: "docs/playbooks/intake.md"
      provides: "Last-verified bump + verified invariants after live human-verify"
      contains: "Last verified"
    - path: ".planning/STATE.md"
      provides: "sidecar-killed decision log + Phase 4 completion note"
  key_links:
    - from: ".planning/STATE.md decision"
      to: "ROADMAP Phase 4 'Python sidecar' phrasing"
      via: "amend-the-line decision-log entry (Path A, no sidecar)"
      pattern: "sidecar"
---

<objective>
Close Phase 4: prove the offline suite green, run the one genuinely-live human-verify checkpoint
(SC3 — real OCR/transcription + real Gmail delivery), log the sidecar-killed decision, bump the
intake playbook, and tick STATE/ROADMAP.

Purpose: SC1/SC2/SC3 depend on real model behavior (OCR/transcription accuracy) + real Gmail
delivery that the offline SMOKE:: suite cannot exercise. This checkpoint mirrors the Phase 3.3
CKPT-02 human-verify. The architectural ruling (Path A, no `services/*` Python sidecar) is logged
as a STATE.md decision amending the stale ROADMAP "Python sidecar" phrasing.

Output: a verified, closed Phase 4.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attachment-voice-intake/04-RESEARCH.md
@.planning/phases/04-attachment-voice-intake/04-VALIDATION.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full offline suite green + intake.md bump + decision log (pre-checkpoint)</name>
  <files>docs/playbooks/intake.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Run the full offline gate and confirm green: `pnpm test` (all packages) + `pnpm --filter @pikar/web
    exec playwright test intake --list` (the live intake run happens in Task 2 with the Lane A mount).
    (The known pre-existing red `convex/audit.test.ts` auditCounts-unregistered is NOT a regression —
    do not chase it.)

    Update `docs/playbooks/intake.md`: fill in the "How to verify" commands (the VALIDATION.md rows) and
    confirm the three invariants match the shipped code (redact-before-audit/merge; extraction-call
    bounded exception; safeText-only in intakeArtifacts). Bump `Last verified` to today / current short-sha.

    Log the architectural decision in `.planning/STATE.md` Accumulated Context › Decisions (additive):
    "[Phase 4]: Extraction engine = Path A (hosted OpenAI API), NO Python `services/*` sidecar —
    amends ROADMAP Phase 4 Goal's 'Python sidecar' phrasing; matches the two logged sidecar-kill
    precedents (PII pure-TS, SkillOpt no-sidecar)." Add a one-line note to the ROADMAP Phase 4 Goal
    that the sidecar phrasing is superseded (additive line, do NOT rewrite others' blocks).
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Full offline suite green (modulo the documented audit.test.ts pre-existing red); intake.md Last-verified bumped; the sidecar-killed decision is logged in STATE.md + ROADMAP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC3 live human-verify — attachment + dictation → delivered email past guardrails</name>
  <action>
    PAUSE for human verification on the live backend. This checkpoint verifies real OCR/transcription
    accuracy + real Gmail delivery that the offline SMOKE:: suite cannot exercise. See what-built /
    how-to-verify below. Do NOT proceed to Task 3 until the user types "approved".
  </action>
  <what-built>
    The full inbound intake path: attach (image/PDF/audio/document) or dictate → classify → extract/
    OCR/transcribe → redact (scanText) → cost-record → audit(counts) → merge into the governed cockpit
    pipeline via sendCockpitMessage → PLAN → Approve → real Gmail delivery. Requires the Lane A mount of
    <IntakeControls/> into ChatPane to be present (coordinate/merge before this step).
  </what-built>
  <how-to-verify>
    On the live backend (`npx convex dev` + `pnpm dev` in this worktree, OPENAI_API_KEY + Gmail connected):
    1. Attach a REAL PDF and a REAL image in the cockpit composer → confirm each is classified and its
       content is summarized/extracted into the conversation.
    2. Click record, DICTATE a request into the mic (e.g. "send an email to <you> about lunch"), stop →
       confirm the transcript enters as a request turn (like a typed message).
    3. Let the agent propose a plan → click Approve ONCE → confirm the delivered email's content reflects
       the attached / dictated content, and that the audit trail carries refs/counts ONLY (no raw text).
    4. Sanity: nothing was sent before Approve; cost was recorded (spend moved); a poison/PII input is
       redacted in the delivered content.
    This is the SC3 gate — mirrors the Phase 3.3 CKPT-02 human-verify.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tick Phase 4 complete (post-approval)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    After the human-verify is approved: mark INTK-02 + INTK-03 complete in ROADMAP (the requirement
    checkboxes + the requirement-status table), tick Phase 4, and update STATE.md Current Position with
    the phase-close summary + the CKPT approval note (additive; keep-both on any merge conflict per
    PARALLELIZATION.md). Bump intake.md Last-verified again only if the human-verify surfaced fixes.
    If gap-closure defects were fixed DURING verify, note them in STATE per the deviation convention.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>INTK-02 + INTK-03 marked complete; Phase 4 ticked; STATE.md records the CKPT approval; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm test` green (offline gate, modulo documented audit.test.ts red).
- Human-verify approved: real attachment + real dictation → delivered email reflects the content, past guardrails (SC3).
- STATE.md logs the sidecar-killed decision; ROADMAP Phase 4 sidecar phrasing superseded.
- intake.md Last-verified bumped; §9 Stop hook clear.
</verification>

<success_criteria>
- SC1: attachment classified + extracted/OCR'd/transcribed, merged into request context — live-verified.
- SC2: dictation recorded + transcribed → enters the pipeline as a typed request — live-verified.
- SC3: a delivered email reflects attachment/dictation content, past PII/cost/review guardrails — live-verified.
- Phase 4 closed; INTK-02 + INTK-03 complete.
</success_criteria>

<cross_lane_notes>
- The live human-verify requires the Lane A mount of `<IntakeControls/>` in ChatPane.tsx. Coordinate the
  merge so the mount is present on the live backend before running the checkpoint.
</cross_lane_notes>

<output>
After completion, create `.planning/phases/04-attachment-voice-intake/04-06-SUMMARY.md`.
</output>
