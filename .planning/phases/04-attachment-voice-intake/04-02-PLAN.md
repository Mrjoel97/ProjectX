---
phase: 04-attachment-voice-intake
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/skills/attachment-extractor.md
  - packages/contracts/src/skills/attachmentExtractor.ts
  - packages/contracts/src/skill.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [INTK-02]
user_setup: []

must_haves:
  truths:
    - "an active `attachment-extractor` skill row exists in the registry, loadable at runtime (no hardcoded OCR prompt in source, §5)"
    - "the canonical .md and the derived .ts body are byte-identical (drift test green)"
  artifacts:
    - path: "packages/contracts/skills/attachment-extractor.md"
      provides: "canonical OCR/extraction system prompt"
      contains: "Extract"
    - path: "packages/contracts/src/skills/attachmentExtractor.ts"
      provides: "byte-identical derived attachmentExtractorSkillBody const"
      exports: ["attachmentExtractorSkillBody"]
    - path: "packages/contracts/src/skill.ts"
      provides: "ATTACHMENT_EXTRACTOR_SKILL name const"
      contains: "ATTACHMENT_EXTRACTOR_SKILL"
    - path: "packages/backend/convex/skills.ts"
      provides: "seedSkills row appended for attachment-extractor"
  key_links:
    - from: "packages/backend/convex/skills.ts seedSkills"
      to: "attachmentExtractorSkillBody"
      via: "appended { name: ATTACHMENT_EXTRACTOR_SKILL, body: attachmentExtractorSkillBody } row"
      pattern: "ATTACHMENT_EXTRACTOR_SKILL"
    - from: "packages/backend/convex/skills.test.ts"
      to: "attachment-extractor.md"
      via: "drift test.each row asserting .md ↔ .ts byte-identity"
      pattern: "attachment-extractor.md"
---

<objective>
Register the `attachment-extractor` skill via the 5-file mirror so the image/PDF OCR call loads
its system prompt from the skills registry at runtime (CLAUDE.md §5 — no hardcoded prompt).

Purpose: The vision/OCR `generateText` call in Plan 04 needs a system prompt ("extract all text
verbatim, preserving structure; if no text, describe factually; output plain text only"). §5
forbids hardcoding it — it is a versioned registry row with rollback. Transcription needs NO
skill (audio→text is not a generative prompt).

Output: an active `attachment-extractor` registry skill + drift coverage + skill-registry.md bump.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attachment-voice-intake/04-RESEARCH.md
@packages/contracts/src/skills/documentDrafter.ts
@packages/backend/convex/skills.ts

<interfaces>
<!-- The 5-file mirror, copied EXACTLY from the document-drafter precedent. -->
Name const lives in packages/contracts/src/skill.ts (sibling to):
```typescript
export const DOCUMENT_DRAFTER_SKILL = "document-drafter" as const;
```
Derived body const shape (packages/contracts/src/skills/documentDrafter.ts):
```typescript
export const documentDrafterSkillBody = "# Document Drafter (v1)\n\n...";
```
seedSkills row (packages/backend/convex/skills.ts, ~line 98-102) — APPEND to the array:
```typescript
{ name: DOCUMENT_DRAFTER_SKILL, body: documentDrafterSkillBody },
```
Drift test row (packages/backend/convex/skills.test.ts test.each, ~line 148-154) — APPEND:
```typescript
["document-drafter.md", documentDrafterSkillBody],
```
The drift test also asserts each seeded skill has an ACTIVE row (active-seed assertion already in the file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical prompt + derived body + name const</name>
  <files>packages/contracts/skills/attachment-extractor.md, packages/contracts/src/skills/attachmentExtractor.ts, packages/contracts/src/skill.ts</files>
  <action>
    Write `packages/contracts/skills/attachment-extractor.md` — the OCR/extraction system prompt:
    instruct the model to extract ALL text from the provided document/image VERBATIM, preserving
    structure (headings, lists, tables as plain text); if there is no text, describe the visual
    content factually and briefly; output PLAIN TEXT ONLY (no commentary, no markdown fences, no
    "here is the text" preamble). Mirror the tone/structure of document-drafter.md.

    Create `packages/contracts/src/skills/attachmentExtractor.ts` — a byte-identical derived
    `export const attachmentExtractorSkillBody = "..."` (LF-normalized, exactly matching the .md, per
    the documentDrafter.ts header comment). Add `export const ATTACHMENT_EXTRACTOR_SKILL = "attachment-extractor" as const;`
    to `packages/contracts/src/skill.ts` (next to DOCUMENT_DRAFTER_SKILL).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts typecheck</automated>
  </verify>
  <done>The .md, the derived body const, and the name const all exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Append the seedSkills row + drift test row, prove no-drift + active-seed</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <action>
    In `skills.ts`: import `ATTACHMENT_EXTRACTOR_SKILL` (from @pikar/contracts skill consts) and
    `attachmentExtractorSkillBody` (from the derived .ts), and APPEND
    `{ name: ATTACHMENT_EXTRACTOR_SKILL, body: attachmentExtractorSkillBody }` to the `seedSkills`
    array — do NOT touch the existing rows (append-only singleton discipline).
    In `skills.test.ts`: APPEND `["attachment-extractor.md", attachmentExtractorSkillBody]` to the
    drift `test.each` array. The existing active-seed assertion will now also cover the new skill.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- skills</automated>
  </verify>
  <done>skills.test.ts green — the attachment-extractor .md ↔ .ts byte-identity row passes and the skill seeds as active.</done>
</task>

<task type="auto">
  <name>Task 3: Bump skill-registry.md playbook (§9)</name>
  <files>docs/playbooks/skill-registry.md</files>
  <action>
    Add `attachment-extractor` to the "Current skills" list in `docs/playbooks/skill-registry.md`
    (its purpose: inbound OCR/extraction system prompt) and bump the `Last verified` line to today /
    current short-sha. No watch.json edit needed — `packages/contracts/skills/` and `skills.ts` are
    already covered by skill-registry.md's watch prefixes (verified in watch.json).
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>skill-registry.md lists attachment-extractor with a bumped Last-verified; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- skills` green (drift + active-seed).
- No hardcoded prompt literal in any convex source (§5 — the body ships as registry data only).
- Only the appended rows changed in skills.ts / skills.test.ts (append-only singleton).
</verification>

<success_criteria>
- attachment-extractor is a versioned, active registry skill loadable at runtime.
- .md and .ts stay byte-identical (drift test enforces it).
- §9 skill-registry.md updated same-turn.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attachment-voice-intake/04-02-SUMMARY.md`.
</output>
