---
phase: 25.3-scale-constants-and-armed-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/guardrails.ts
  - packages/backend/convex/lib/env.ts
  - packages/backend/convex/proactiveReview.ts
  - packages/backend/convex/proactiveReview.test.ts
  - packages/backend/convex/gmailAuth.ts
  - packages/backend/convex/worm.ts
  - packages/backend/convex/worm.test.ts
  - packages/backend/convex/vaultIngest.ts
  - packages/backend/convex/audit.ts
  - packages/backend/convex/scaleConstants.test.ts
  - docs/playbooks/guardrails.md
  - docs/playbooks/production-beta.md
  - docs/playbooks/business-evaluation.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/audit-dead-letter.md
  - docs/playbooks/vault.md
autonomous: true
requirements: [G17, G1]
must_haves:
  truths:
    - "The three deployment-wide caps are read from env at module load, with the compiled values as fallbacks, and appear in the manifest and on the readiness screen"
    - "No cron or maintenance job reads a whole table in one transaction: the weekly review, the token-expiry scan, the WORM export, retryStuckIngests and backfillAuditCounts are all bounded"
    - "The weekly review checks the deployment budget before scheduling a tenant, spreads its fan-out, and logs a failure's error name instead of swallowing it"
    - "An expiring Gmail grant produces ONE unread reconnect notification, not one per day"
    - "A WORM backlog drains: one run exports as many pages as fit in its budget"
  artifacts:
    - path: "packages/backend/convex/scaleConstants.test.ts"
      provides: "env parse table, token-expiry dedupe across two runs, source pins that the maintenance jobs are batch jobs"
  key_links:
    - from: "guardrails.ts DEPLOYMENT_*_BUDGET_CENTS"
      to: "process.env.DEPLOYMENT_*_BUDGET_CENTS"
      via: "envCents(name, fallback) — literal reads so env.test.ts's scan sees them"
    - from: "proactiveReview.runWeekly (cron target, unchanged name)"
      to: "migrations.runOne(enumerateWeeklyReview) over users"
      via: "one indexed by_tenant_kind .first() per user, rateLimiter.check before runAfter(spread)"
---

<objective>
The four small diffs the audit says make 10k users honest (G17), plus the arming instruction for the sweep (G1). Bounded reads, env-driven ceilings, no new dependency, no sharding.
</objective>

<context>
@.planning/phases/25.3-scale-constants-and-armed-sweep/25.3-RESEARCH.md
@docs/playbooks/guardrails.md, @docs/playbooks/business-evaluation.md, @docs/playbooks/audit-dead-letter.md

Notes for the executor:
- `migrations.define({ table, batchSize, migrateOne })` from `./migrations`; `oneBatchOnly: true` in tests (vaultSweep.test.ts:143). Cron targets keep their NAMES (`runWeekly`, `flagExpiringTokens`) and become one-line wrappers that call `migrations.runOne(..., { reset: true })`.
- `env.test.ts` scans for LITERAL `process.env.NAME` — write three literal reads; a computed lookup is invisible to it.
- `proactiveReview.test.ts`'s tenant-scope guard counts `by_kind` queries; after this plan there are none, so the count pin becomes 0.
- Convex mutations get a seeded `Math.random`; using it for the spread is fine.
</context>

<tasks>
1. guardrails.ts: `envCents`, the three env-driven caps, the `ponytail:` sharding note. env.ts: three manifest rows.
2. proactiveReview.ts: `enumerateWeeklyReview` migration over `users`; `runWeekly` wrapper; catch logs. Test fixtures gain users rows; cron helper runs one batch + drains with fake timers; guard pin → 0.
3. gmailAuth.ts: `scanExpiringTokens` migration with the unread-notification dedupe; `flagExpiringTokens` wrapper.
4. worm.ts: page loop with `pageSize` and an 8-minute budget; worm.test.ts two-page case.
5. vaultIngest.ts / audit.ts: `retryStuckIngestsBatch` and `reinsertAuditCounts` migrations behind the existing names.
6. scaleConstants.test.ts; run backend targeted files + env.test; typecheck; CI lint LAST.
7. Bump the six playbooks.
</tasks>
