---
phase: 06-live-voice-sessions
plan: 05
type: execute
wave: 2
depends_on: [03, 04]
files_modified:
  - packages/backend/convex/voice.ts
  - packages/backend/convex/voice.test.ts
  - packages/backend/convex/llmRedaction.test.ts
  - docs/playbooks/voice.md
autonomous: true
requirements: [VOIC-02, VOIC-03]
must_haves:
  truths:
    - "startSession persists the callId and arms ONE durable watchdog at startedAt+15min that fires forceEndSession; the session is 'active' only once callId is set"
    - "A second concurrent session is prevented (parallel guard via getActiveSession)"
    - "Clean end cancels the watchdog under a status CAS; the CAS no-ops on the 0:00 fire race"
    - "forceEndSession force-terminates the OpenAI call (hangupCall), marks ended_abnormal, and auto-stores a brief"
    - "storeBrief drafts the brief and ingests it as an ordinary vault document (kind:brief) through ingestDoc — the brief body keeps PII; only audit rows are refs/counts-only"
    - "recordUsage accumulates token counters and prices them onto the existing spend rails"
  artifacts:
    - path: "packages/backend/convex/voice.ts"
      provides: "startSession, getActiveSession, recordUsage, endSessionClean, forceEndSession, storeBrief"
      exports: ["startSession", "endSessionClean", "forceEndSession", "storeBrief"]
    - path: "packages/backend/convex/voice.test.ts"
      provides: "fake-timer watchdog arm/fire/cancel + CAS + SMOKE brief→ingest tests"
  key_links:
    - from: "voice.startSession"
      to: "ctx.scheduler.runAt(startedAt+15min, internal.voice.forceEndSession)"
      via: "cancellable watchdogFnId (cockpit.ts deferred-send precedent)"
      pattern: "scheduler\\.runAt"
    - from: "voice.forceEndSession"
      to: "internal.voiceToken.hangupCall"
      via: "ctx.runAction with the stored callId"
      pattern: "hangupCall"
    - from: "voice.storeBrief"
      to: "internal.vaultIngest.ingestDoc"
      via: "workflow.start on a text-first vaultDocuments row (kind:brief)"
      pattern: "ingestDoc"
---

<objective>
The server-side session engine: durable 15-min watchdog, clean/abnormal end paths, per-session
metering, and the brief→vault store. This is the VOIC-02/VOIC-03 core, built entirely on in-repo
precedents (deferred-send scheduler + review-timeout CAS + vault ingestDoc).

Purpose: enforce the cap even on a gone tab, always produce a brief, and never let a session audit
row become a PII/secret honeypot.
Output: `voice.ts` thin adapter (domain logic in @pikar/voice) + full unit coverage.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-voice-sessions/06-RESEARCH.md
@.planning/phases/06-live-voice-sessions/06-CONTEXT.md
@.planning/phases/06-live-voice-sessions/06-VALIDATION.md

# Precedents to copy — read the exact arm/cancel/CAS and ingest lines:
@packages/backend/convex/cockpit.ts        # scheduler.runAt deferred-send + cancelScheduledPlan CAS
@packages/backend/convex/review.ts         # armTimeout/fireTimeout/sendDecision cancel precedent
@packages/backend/convex/vault.ts          # insert vaultDocuments text-first → workflow.start ingestDoc
@packages/backend/convex/llmRedaction.test.ts   # static-scan refs-only precedent (extend it)

<interfaces>
- Tenant-scoped wrappers only (CLAUDE.md #2) — from convex/lib/functions.ts.
- Session FSM + capEndsAt: from @pikar/voice (session.ts). priceRealtime: from @pikar/cost.
- Force-terminate: `ctx.runAction(internal.voiceToken.hangupCall, {callId})` (plan 03).
- Brief content: `ctx.runAction(internal.llm.draftVoiceBrief, {transcript, language})` (plan 04)
  → markdown → insert vaultDocuments {kind:"brief", source:"voice", mimeType:"text/markdown",
  text: markdown /* PII kept — content plane */, status:"processing"} → workflow.start ingestDoc.
- Watchdog: `ctx.scheduler.runAt(endsAt, internal.voice.forceEndSession, {sessionId})` → store
  watchdogFnId. Cancel with `ctx.scheduler.cancel(watchdogFnId)` ONLY under a status CAS
  (active/wrapping) — cancel throws on an already-fired id (cockpit.cancelScheduledPlan Pitfall).
- Metering trust boundary (RESEARCH Pattern 4): client-reported usage is telemetry + best-effort
  budget contribution — the 15-min wall-clock cap is the real cost bound. Do NOT build server-side
  usage reconciliation for beta.
- Audit refs-only (CLAUDE.md #4): voice.session_started/session_ended rows carry {sessionId} +
  counts ONLY — never transcript, callId-as-secret, or the client secret.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: startSession + parallel guard + arm watchdog + endSessionClean (CAS cancel)</name>
  <files>packages/backend/convex/voice.ts, packages/backend/convex/voice.test.ts</files>
  <behavior>
    - startSession({callId}): getActiveSession first — if an active/wrapping session exists, force-end it (or refuse) before starting (parallel guard). Persist callId, set status "active", startedAt=now, endsAt=capEndsAt(now), arm scheduler.runAt(endsAt, forceEndSession) and store the watchdogFnId. Row is "active" only WITH a callId (Pitfall 1).
    - getActiveSession(tenantId): returns the active/wrapping row via by_tenant_status, else null.
    - endSessionClean(sessionId): if status is active/wrapping → cancel watchdogFnId, set status ended_clean. If already ended → no-op (CAS). Emits a refs-only session_ended audit ({sessionId}+counts).
  </behavior>
  <action>
    Copy the arm/cancel lines from cockpit.ts (deferred-send) and review.ts. Use fake timers in
    the test: assert the watchdog is armed at startedAt+15min; clean end cancels it; a clean end
    that races the fire is a CAS no-op (never a double-cancel throw). Assert the parallel guard
    force-ends a prior active session.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voice</automated>
  </verify>
  <done>watchdog armed at +15min; parallel guard holds; clean end cancels under CAS; fire-race is a no-op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: forceEndSession (hangup + auto-store) + storeBrief (ingestDoc) + recordUsage (metering)</name>
  <files>packages/backend/convex/voice.ts, packages/backend/convex/voice.test.ts</files>
  <behavior>
    - forceEndSession(sessionId): CAS — if not active/wrapping, return. Else runAction hangupCall({callId}), set status ended_abnormal, then auto-store the brief (no human present to gate).
    - storeBrief(sessionId[, editedMarkdown]): draft via draftVoiceBrief (or use the passed edited markdown on clean end), insert a vaultDocuments row (kind:"brief", text-first, PII kept), workflow.start ingestDoc, patch session.briefRef. The SMOKE:: transcript path drives brief→ingest→vault "ready" offline.
    - recordUsage(sessionId, {inAudioTok,outAudioTok,textInTok,textOutTok}): patch cumulative counters; priceRealtime the delta; if ok, recordSpend (same kill-switch/budget window). Non-finite counts fail closed (no negative spend).
  </behavior>
  <action>
    Implement forceEndSession/storeBrief/recordUsage. storeBrief mirrors vault.ts:88 text-first
    insert + ingestDoc workflow.start. Do NOT scanText-strip the brief body (RESEARCH anti-pattern
    — it is vault content). convex-test: SMOKE draftVoiceBrief → the brief row reaches vault
    "ready"; abnormal end auto-stores; clean end stores the edited markdown; recordUsage prices
    onto recordSpend and fails closed on a bad count.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voice</automated>
  </verify>
  <done>forceEndSession hangs up + auto-stores + marks ended_abnormal; storeBrief ingests a kind:brief vault doc to ready via SMOKE; recordUsage meters + fails closed.</done>
</task>

<task type="auto">
  <name>Task 3: Refs-only session audit static scan + voice.md invariants</name>
  <files>packages/backend/convex/llmRedaction.test.ts, docs/playbooks/voice.md</files>
  <action>
    Extend llmRedaction.test.ts with a mutation-checked static scan (the 03.7 precedent): the
    voice session audit/telemetry rows in voice.ts carry {sessionId} + counts ONLY — never the
    transcript, the client secret, the API key, or the callId-as-secret. Verify the scan is
    genuinely RED on a deliberate leak (e.g. adding `transcript` to an audit payload), then revert.
    Finalize docs/playbooks/voice.md with the VOIC-02/03 subsystem invariants (watchdog armed once
    / never re-armed / wall-clock; CAS cancel; brief body = content plane, audit = refs-only; the
    metering-is-telemetry trust boundary) + Last verified.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test llmRedaction</automated>
  </verify>
  <done>The refs-only scan is present and mutation-checked; voice.md carries the session invariants; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test voice` + `... test llmRedaction` green.
- Full backend suite green save the documented pre-existing `audit.test.ts auditCounts` red (non-regression).
- Grep: exactly one `scheduler.runAt` in voice.ts (armed once); `scanText` NOT applied to the brief body.
- `node scripts/check-playbooks.mjs` exits 0.
</verification>

<success_criteria>
Every session — clean or abnormal — is capped at 15 minutes by a server timer that survives a gone
tab, always yields a vault-indexed brief, and never writes transcript/secret into an audit row.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-voice-sessions/06-05-SUMMARY.md`.
</output>
