---
phase: 06-live-voice-sessions
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - packages/backend/convex/voiceToken.ts
  - packages/backend/convex/voiceToken.test.ts
  - docs/playbooks/voice.md
autonomous: true
requirements: [VOIC-01, VOIC-02]
must_haves:
  truths:
    - "The browser can obtain a short-lived Realtime client secret without OPENAI_API_KEY ever reaching it"
    - "The mint request body carries the voice-session skill instructions from the registry (not a hardcoded prompt)"
    - "mintClientSecret returns ONLY {clientSecret, expiresAt} — never the API key"
    - "The server can force-terminate a live call by POSTing to the hangup endpoint with the stored callId"
  artifacts:
    - path: "packages/backend/convex/voiceToken.ts"
      provides: "mintClientSecret + hangupCall (plain-runtime actions, global fetch)"
      exports: ["mintClientSecret", "hangupCall"]
    - path: "packages/backend/convex/voiceToken.test.ts"
      provides: "mock-fetch tests: request shape, no-key-leak, hangup targeting"
  key_links:
    - from: "voiceToken.mintClientSecret"
      to: "POST /v1/realtime/client_secrets"
      via: "global fetch, Bearer OPENAI_API_KEY, body carries voice-session skill.body"
      pattern: "client_secrets"
    - from: "voiceToken.hangupCall"
      to: "POST /v1/realtime/calls/{callId}/hangup"
      via: "global fetch, Bearer OPENAI_API_KEY"
      pattern: "hangup"
---

<objective>
The server-side seam between the browser and OpenAI Realtime: mint a short-lived client secret
(so the browser never holds the API key) and force-terminate a live call server-side (the VOIC-02
linchpin the watchdog fires).

Purpose: VOIC-01's browser-direct WebRTC needs an ephemeral secret; VOIC-02's watchdog needs a
server-addressable hangup. Both are plain `fetch` to OpenAI — a DEFAULT-runtime action (NOT
"use node") to avoid re-triggering the TS circular-inference cliff a second node module causes.
Output: `voiceToken.ts` with `mintClientSecret` + `hangupCall`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-voice-sessions/06-RESEARCH.md
@.planning/phases/06-live-voice-sessions/06-CONTEXT.md

# Pinned API shapes (Wave 0) — use these constants, do not re-fetch:
@packages/voice/src/realtime.ts

# Precedents to mirror:
@packages/backend/convex/gmail.ts          # fetch-in-action + reading env secrets server-side
@packages/backend/convex/skills.ts         # getActiveSkill query (load voice-session body)

<interfaces>
- Use the tenant-scoped wrappers from `convex/lib/functions.ts` (CLAUDE.md #2) — never raw
  action/mutation imports.
- Load the persona: `ctx.runQuery(internal.skills.getActiveSkill, { name: VOICE_SESSION_SKILL })`
  → fail-closed if no active skill (NO_ACTIVE_SKILL).
- Endpoints/model/keys come from `@pikar/voice`'s realtime.ts (CLIENT_SECRETS_URL, model snapshot,
  session-config key names, hangupUrl(callId)).
- `OPENAI_API_KEY` is read from Convex env (process.env) — same as the existing OpenAI calls.
- Plain runtime: NO `"use node"` pragma. Use global `fetch`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mintClientSecret — ephemeral secret with the registry persona, no key leak</name>
  <files>packages/backend/convex/voiceToken.ts, packages/backend/convex/voiceToken.test.ts</files>
  <behavior>
    - Loads the voice-session skill body and includes it as the session `instructions` in the mint body (mock fetch asserts the outgoing body carries the skill text).
    - POSTs to CLIENT_SECRETS_URL with `Authorization: Bearer <OPENAI_API_KEY>` and the session config (model snapshot, audio.output.voice, turn_detection semantic_vad, input_audio_transcription) from realtime.ts.
    - Returns ONLY `{ clientSecret, expiresAt }`. The return value NEVER contains OPENAI_API_KEY (assert the key string is absent from the serialized result).
    - Fail-closed if no active voice-session skill (NO_ACTIVE_SKILL) — no hardcoded fallback prompt.
  </behavior>
  <action>
    Implement `mintClientSecret` as a tenant-scoped plain-runtime action. Mock `fetch` in the
    test (gmail.test.ts pattern) — return a fake `{ client_secret, expires_at }` per the pinned
    shape; assert (1) request URL/headers/body carry the skill instructions + Bearer key, (2) the
    resolved result is exactly `{clientSecret, expiresAt}` and contains no `OPENAI_API_KEY`.
    ponytail: no retry/backoff for beta — a failed mint surfaces to the pre-flight UI as "couldn't
    start"; add retry only if live-verify shows flakiness (name the ceiling in a comment).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voiceToken</automated>
  </verify>
  <done>mint returns {clientSecret,expiresAt} only, injects the registry persona, fails closed without an active skill; no-key-leak assertion green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hangupCall — server force-terminate + refs-only discipline</name>
  <files>packages/backend/convex/voiceToken.ts, packages/backend/convex/voiceToken.test.ts, docs/playbooks/voice.md</files>
  <behavior>
    - hangupCall({callId}) POSTs to hangupUrl(callId) with Bearer OPENAI_API_KEY and treats 200 as success.
    - It is an internalAction (called only by the watchdog / beacon path — never client-exposed).
    - Neither mint nor hangup logs or returns the secret/key (a follow-on audit row, if any, carries {sessionId}+counts only — enforced in plan 05's static scan).
  </behavior>
  <action>
    Implement `hangupCall` as a plain-runtime internalAction using `hangupUrl(callId)` from
    realtime.ts. Mock fetch in the test: assert it targets the hangup path for the given callId
    with the Bearer key and resolves on 200 (and surfaces a non-200 as an error the caller can
    log refs-only). Bump docs/playbooks/voice.md with the mint/hangup invariants (key never
    leaves Convex; hangup is the only server-side terminate; both are plain-runtime).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voiceToken</automated>
  </verify>
  <done>hangupCall targets /v1/realtime/calls/{callId}/hangup with the server key and succeeds on 200; voice.md bumped; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test voiceToken` green (request shape, no-key-leak, hangup targeting).
- `node scripts/check-playbooks.mjs` exits 0.
- Grep: no `"use node"` in voiceToken.ts; the returned object never includes the API key.
</verification>

<success_criteria>
The browser can start a Realtime session with a short-lived secret it can safely hold, and the
server can end any live call by callId — the two OpenAI seams VOIC-01 and the VOIC-02 watchdog need.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-voice-sessions/06-03-SUMMARY.md`.
</output>
