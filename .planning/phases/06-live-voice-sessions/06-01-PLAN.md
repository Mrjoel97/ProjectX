---
phase: 06-live-voice-sessions
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/voice/package.json
  - packages/voice/tsconfig.json
  - packages/voice/src/index.ts
  - packages/voice/src/realtime.ts
  - packages/voice/src/brief.ts
  - packages/voice/src/session.ts
  - packages/voice/src/metering.ts
  - packages/voice/src/brief.test.ts
  - packages/voice/src/session.test.ts
  - packages/cost/src/cost.ts
  - packages/cost/src/cost.test.ts
  - docs/playbooks/voice.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [VOIC-02, VOIC-03]
must_haves:
  truths:
    - "The exact live OpenAI Realtime API JSON shapes (client_secrets mint, /v1/realtime/calls handshake, hangup path, response.done usage field names, current model snapshot) are pinned in one constants module before any dependent code hardcodes them"
    - "buildBriefMarkdown produces a fixed-section markdown doc, empty sections rendered 'None', with the full transcript welded on in code"
    - "The session status FSM and capEndsAt(startedAt) are pure and unit-tested"
    - "priceRealtime maps audio/text token counts to USD and fails closed on non-finite/negative inputs"
    - "The voice subsystem is registered in a playbook + watch.json so later plans' new packages/apps files do not trip the Stop hook"
  artifacts:
    - path: "packages/voice/src/realtime.ts"
      provides: "Pinned Realtime API endpoints, model snapshot, session-config keys, response.done usage field names"
      contains: "client_secrets"
    - path: "packages/voice/src/brief.ts"
      provides: "buildBriefMarkdown(sections, transcript, language) pure composer"
      exports: ["buildBriefMarkdown"]
    - path: "packages/voice/src/session.ts"
      provides: "status FSM + capEndsAt"
      exports: ["capEndsAt"]
    - path: "packages/cost/src/cost.ts"
      provides: "priceRealtime + REALTIME_PRICING"
      contains: "priceRealtime"
    - path: "docs/playbooks/voice.md"
      provides: "Voice subsystem playbook"
  key_links:
    - from: "packages/cost/src/cost.ts::priceRealtime"
      to: "Result<number, CostError>"
      via: "fail-closed guard mirroring priceTranscription"
      pattern: "priceRealtime"
    - from: "docs/playbooks/watch.json"
      to: "packages/voice/ + convex/voice*.ts + apps voice route"
      via: "watched-path registration under voice.md"
      pattern: "packages/voice"
---

<objective>
Lay the pure-TS + pricing + docs foundations every later voice plan imports: pin the live
OpenAI Realtime API shapes, build the `packages/voice` pure module (brief composer, session
FSM, metering helper), add `priceRealtime` to `@pikar/cost`, and register the voice subsystem
playbook so the Stop hook is satisfied for the whole phase.

Purpose: nothing that hardcodes Realtime field names, session status, cap math, or realtime
pricing may be written before this plan pins them. This is the Wave 0 foundation.
Output: `packages/voice/*`, `priceRealtime` in `@pikar/cost`, `docs/playbooks/voice.md`,
`watch.json` registration.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-voice-sessions/06-RESEARCH.md
@.planning/phases/06-live-voice-sessions/06-CONTEXT.md
@.planning/phases/06-live-voice-sessions/06-VALIDATION.md

# Reuse patterns — read these interfaces, do not re-derive:
@packages/cost/src/cost.ts        # priceTranscription fail-closed Result pattern to mirror for priceRealtime
@docs/playbooks/TEMPLATE.md       # playbook skeleton
@docs/playbooks/watch.json        # add the voice watched prefixes here

<interfaces>
priceTranscription (packages/cost/src/cost.ts) — the fail-closed shape to copy:
```typescript
export function priceTranscription(seconds: number): Result<number, CostError> {
  if (!Number.isFinite(seconds) || seconds < 0) return err({ code: "over_budget" });
  const minutes = seconds / 60;
  return ok(minutes * TRANSCRIPTION_PRICING.perMinuteUsd);
}
```
Result/err/ok import: `import { type Result, err, ok } from "@pikar/core/result";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/voice + register the voice playbook and watch.json</name>
  <files>packages/voice/package.json, packages/voice/tsconfig.json, packages/voice/src/index.ts, docs/playbooks/voice.md, docs/playbooks/watch.json</files>
  <action>
    Create the `@pikar/voice` package (mirror `packages/cost` package.json/tsconfig — pure TS,
    no Convex, exports from src/index.ts, vitest configured the same way `@pikar/cost` is so
    `pnpm --filter @pikar/voice test` works). Empty index.ts re-export placeholder for now.

    Create `docs/playbooks/voice.md` from `docs/playbooks/TEMPLATE.md` — the voice subsystem
    playbook. Fill the invariants section with the CLAUDE.md-derived contracts this phase must
    hold (they will be reinforced by later plans): domain logic in `packages/voice` (pure),
    `convex/voice*.ts` thin adapters; the 15-min cap is a server wall-clock watchdog armed once
    and never re-armed; the brief BODY is vault content (PII kept) but session audit/telemetry
    rows are refs/counts-only; the live-session + brief prompts load from the skill registry;
    `OPENAI_API_KEY`/client-secret never reach the browser log or an audit row. Set a
    "Last verified" line.

    Register the voice watched-path prefixes in `docs/playbooks/watch.json` under a new
    `"voice.md"` key (so EVERY later voice plan's new files are pre-covered and the Stop hook
    cannot block them): `packages/voice/`, `packages/backend/convex/voice.ts`,
    `packages/backend/convex/voice.test.ts`, `packages/backend/convex/voiceToken.ts`,
    `packages/backend/convex/voiceToken.test.ts`, `apps/web/app/(app)/dashboard/voice/`.
    (llm.ts stays under cockpit.md; contracts/skills stays under skill-registry.md — do NOT
    duplicate them here.)
    ponytail: one playbook up front covers the whole phase; later plans only bump "Last verified".
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>`@pikar/voice` package builds/typechecks; `voice.md` exists and is registered in watch.json with all voice prefixes; check-playbooks exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the live OpenAI Realtime API shapes into packages/voice/src/realtime.ts</name>
  <files>packages/voice/src/realtime.ts, packages/voice/src/index.ts</files>
  <action>
    RESEARCH Open Question 2 / State-of-the-Art: `gpt-realtime`/`client_secrets`/`calls` GA'd
    2026-07-06, PAST the model training cutoff — so verify the shapes against the LIVE docs
    before hardcoding. Use Context7 (resolve OpenAI, query "Realtime API client_secrets calls
    hangup") and/or WebFetch these pages, then pin the confirmed values as typed constants:
      - https://developers.openai.com/api/docs/guides/realtime  (client_secrets mint body,
        calls handshake, turn_detection semantic_vad, input_audio_transcription, response.done usage)
      - https://developers.openai.com/api/reference/resources/realtime/subresources/calls/methods/hangup

    Write `realtime.ts` exporting: endpoint constants (`CLIENT_SECRETS_URL`, `CALLS_URL`, a
    `hangupUrl(callId)` builder), the current default model snapshot string (confirm `gpt-realtime`
    vs `-2.1`/`-mini`), the session-config key names used in the mint body (model, instructions,
    audio.output.voice, turn_detection.type, input_audio_transcription.model), and the
    `response.done` usage field names (input/output audio + text token keys) that the client will
    read for metering. Add a `ponytail:` comment naming the doc URL + date each value was pinned
    from and the upgrade path (re-fetch if the API surface moves). Re-export from index.ts.

    Leave a runnable self-check (assert-based `__main__`-style or a tiny test) that the module
    exports the expected endpoint/model/usage-key names (fails if a key is dropped/renamed).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/voice test realtime</automated>
  </verify>
  <done>realtime.ts pins endpoints, model snapshot, session-config keys, and response.done usage field names, each with a sourced ponytail comment; the self-check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Brief composer + session FSM + metering (packages/voice) and priceRealtime (@pikar/cost)</name>
  <files>packages/voice/src/brief.ts, packages/voice/src/session.ts, packages/voice/src/metering.ts, packages/voice/src/brief.test.ts, packages/voice/src/session.test.ts, packages/voice/src/index.ts, packages/cost/src/cost.ts, packages/cost/src/cost.test.ts</files>
  <behavior>
    - buildBriefMarkdown: given filled sections {summary, decisions[], actionItems[], openQuestions[], discussion} + a turn-by-turn transcript + language, returns fixed-order markdown (Summary → Decisions → Action items → Open questions → Discussion → Transcript); an empty section renders the literal "None"; the transcript is welded verbatim in code (never model-authored).
    - capEndsAt(startedAt): returns startedAt + 15*60*1000 (exported constant for the ms).
    - session FSM: legal transitions active→wrapping→ended_clean and active/wrapping→ended_abnormal; an already-ended status is terminal (helper that later CAS guards reuse).
    - accumulateUsage: folds a stream of {inAudioTok,outAudioTok,textInTok,textOutTok} into cumulative counters.
    - priceRealtime(inAudioTok, outAudioTok, textInTok, textOutTok): USD from REALTIME_PRICING; returns err({code:"over_budget"}) if any input is non-finite or negative (mirror priceTranscription).
  </behavior>
  <action>
    Implement the pure modules per the behavior above. Pull the REALTIME_PRICING rates into
    `packages/cost/src/cost.ts` as a new exported table + `priceRealtime` (rates confirmed in
    Task 2's doc fetch: gpt-realtime audio in ~$32/1M, audio out ~$64/1M, text in ~$4/1M, text
    out ~$24/1M — reconfirm and mark with a `ponytail:` sourced comment). `priceRealtime` fails
    closed exactly like `priceTranscription`. The brief section shape must be strict-mode-legal
    (it becomes the `generateObject` schema in plan 04 — keep field names stable).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/voice test && pnpm --filter @pikar/cost test</automated>
  </verify>
  <done>buildBriefMarkdown (empty→"None", transcript welded), capEndsAt, FSM transition guard, accumulateUsage, and priceRealtime (fail-closed) all unit-green in both packages.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/voice test` and `pnpm --filter @pikar/cost test` green.
- `node scripts/check-playbooks.mjs` exits 0 (voice.md registered, new packages/voice files covered).
- realtime.ts field names are sourced from the live doc (ponytail comment names URL + date).
</verification>

<success_criteria>
Every downstream voice plan can import a pinned API-shape constant, a pure brief composer, a
session FSM, and `priceRealtime` — none of them re-derives a Realtime field name or the cap math.
The Stop hook is satisfied for all future voice paths.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-voice-sessions/06-01-SUMMARY.md`.
</output>
