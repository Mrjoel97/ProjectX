---
phase: 06-live-voice-sessions
plan: 08
type: execute
wave: 5
depends_on: [06, 07]
files_modified:
  - docs/decisions/005-live-voice-browser-direct-realtime.md
  - docs/playbooks/voice.md
autonomous: false
requirements: [VOIC-01, VOIC-02, VOIC-03, VOIC-04]
must_haves:
  truths:
    - "An immutable ADR records the browser-direct OpenAI Realtime WebRTC + wall-clock watchdog + metering-as-telemetry decision"
    - "The voice playbook is finalized with the full subsystem invariants and a bumped Last verified"
    - "The full backend suite is green (save the documented pre-existing red) and the live human-verify passes: real audio round-trip, barge-in, multilingual, the watchdog force-ending a live call at the cap, an abnormal-end brief produced, and a voice-derived plan approved through the normal governed send"
  artifacts:
    - path: "docs/decisions/005-live-voice-browser-direct-realtime.md"
      provides: "Immutable ADR for the voice architecture"
    - path: "docs/playbooks/voice.md"
      provides: "Finalized voice subsystem playbook"
  key_links:
    - from: "live human-verify"
      to: "VOIC-01..04"
      via: "the phase gate no unit can substitute for (RPLY-01 precedent)"
      pattern: "human-verify"
---

<objective>
Close the phase: record the architecture decision, finalize the playbook, prove the whole thing
green, and run the live human-verify walk-through that voice (uniquely) cannot be unit-tested into.

Purpose: the deterministic seams are all covered by Wave 0-4 unit/e2e; this plan captures the
lasting decision record and gates on the real audio + real force-terminate + real Approve→send
that only a human at a mic can confirm.
Output: ADR 005 + finalized voice.md + the passed live gate.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-voice-sessions/06-RESEARCH.md
@.planning/phases/06-live-voice-sessions/06-VALIDATION.md

@docs/decisions/004-agents-humans-peer-actors.md   # ADR format precedent (next number is 005)
@docs/playbooks/voice.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR 005 + finalize the voice playbook + green sweep</name>
  <files>docs/decisions/005-live-voice-browser-direct-realtime.md, docs/playbooks/voice.md</files>
  <action>
    Write ADR 005 (immutable — never edited after acceptance, CLAUDE.md #9) capturing the decided
    architecture: browser-direct OpenAI Realtime API over WebRTC with a server-minted ephemeral
    client secret (no LiveKit, no new dep, no new pinned component); the 15-min cap as a single
    server-side wall-clock watchdog armed once and never re-armed (a mic-drop grace window consumes
    cap time); server force-terminate via the callId hangup endpoint; client-reported usage as
    telemetry/best-effort budget (the cap is the real cost bound); discard raw audio (transcript +
    brief only); the brief as first-class vault content (PII kept) vs refs-only session audit.
    Note the alternatives rejected and why.

    Finalize docs/playbooks/voice.md: consolidate the invariants accreted across plans 01-07 into
    the definitive subsystem contract (how to change it safely, how to verify, operations) and bump
    Last verified. Run the full backend suite and confirm green save the documented pre-existing
    `audit.test.ts auditCounts` red.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>ADR 005 accepted; voice.md finalized + Last verified bumped; full suite green (documented red aside); check-playbooks exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live human-verify walk-through (the VOIC-01..04 phase gate)</name>
  <action>
    Pause for the owner to run the live walk-through below — voice is not unit-testable, so this is
    the gate no unit can substitute for (RPLY-01 precedent). It proves the real audio round-trip,
    barge-in, multilingual auto-detect, the watchdog force-ending a live call at the cap, an
    abnormal-end brief, and a voice-derived plan approved through the normal governed send.
  </action>
  <what-built>The complete live voice feature: pre-flight → live bidirectional voice session (open-mic VAD, barge-in, warm multilingual voice) → 15-min server watchdog → structured markdown brief in the vault → optional brief→plan at the normal single-Approve gate.</what-built>
  <how-to-verify>
    With the dev environment live (web + convex-local-backend + OAuth site; OPENAI_API_KEY set),
    sign in and go to http://localhost:3000/dashboard/voice, then:
    1. Pre-flight: grant mic permission, confirm the level meter moves, read the consent notice, Start.
    2. Hold a real bidirectional conversation. Interrupt the agent mid-utterance (barge-in) — it
       should stop and listen. Speak a second language — it should respond in-language and the
       transcript should be bilingual. Confirm the countdown is always visible.
    3. Either let it reach 15:00 (or force the watchdog) and confirm the call force-ends and an
       abnormal-end brief is produced; OR press End, confirm the "End session & generate brief?"
       dialog, and review/edit the brief.
    4. Confirm the brief lands in the Knowledge Vault (searchable) with the fixed sections + the
       full transcript.
    5. From the post-call summary, "Turn this into a plan" → land at the cockpit review gate →
       single-Approve → confirm the governed fan-out sends (RPLY-01 precedent — nothing sends
       before Approve).
    6. Keyboard-only + screen-reader pass over pre-flight/live/post-call.
  </how-to-verify>
  <resume-signal>Type "approved" once the live walk-through passes, or describe the issues found.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (documented pre-existing red aside).
- `node scripts/check-playbooks.mjs` exits 0.
- The live human-verify walk-through is approved by the owner (the VOIC-01..04 phase gate).
</verification>

<success_criteria>
The architecture is recorded immutably, the playbook is the definitive voice contract, and a human
has confirmed the real audio round-trip, the real force-terminate at the cap, and a real
voice-derived plan approved through the normal governed send — the gate no unit can substitute for.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-voice-sessions/06-08-SUMMARY.md` and mark
Phase 6 requirements Complete in REQUIREMENTS.md + ROADMAP.
</output>
