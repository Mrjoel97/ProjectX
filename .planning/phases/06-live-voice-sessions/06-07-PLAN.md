---
phase: 06-live-voice-sessions
plan: 07
type: execute
wave: 4
depends_on: [05, 06]
files_modified:
  - apps/web/app/(app)/dashboard/voice/PostCall.tsx
  - apps/web/app/(app)/dashboard/voice/page.tsx
  - apps/web/app/(app)/dashboard/voice/AbnormalBriefBanner.tsx
  - apps/web/e2e/voice.spec.ts
  - docs/playbooks/voice.md
autonomous: true
requirements: [VOIC-03, VOIC-04]
must_haves:
  truths:
    - "On a clean end, the brief markdown is shown for review/edit, then stored + indexed on confirm"
    - "The post-call screen asks 'Turn this into a plan?' / 'Just save' (the reliable path for clean AND dropped sessions)"
    - "Turning the brief into a plan seeds a cockpit thread with the brief's Decisions/Action items and lands the user at the existing single-Approve review gate"
    - "An abnormal-end brief (auto-stored) is surfaced for review the next time the user opens the app"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/voice/PostCall.tsx"
      provides: "brief review/edit + store + plan-handoff ask"
    - path: "apps/web/app/(app)/dashboard/voice/AbnormalBriefBanner.tsx"
      provides: "next-app-open surfacing of an auto-stored brief"
    - path: "apps/web/e2e/voice.spec.ts"
      provides: "offline SMOKE-seeded e2e: brief review→store→proposable plan"
  key_links:
    - from: "PostCall 'Turn into a plan'"
      to: "cockpit.sendCockpitMessage → /workspace?thread=<id>"
      via: "seeds the thread with Decisions + Action items; lands at the single-Approve gate"
      pattern: "sendCockpitMessage"
    - from: "PostCall review/edit confirm"
      to: "voice.storeBrief(editedMarkdown)"
      via: "clean-end store after review"
      pattern: "storeBrief"
---

<objective>
The load-bearing post-call surface: review/edit the brief and store it (clean end), ask permission
to convert it into a plan (VOIC-04), and surface an auto-stored brief from a dropped session next
time the app opens. Everything starts from the STORED brief so clean and dropped sessions degrade
identically.

Purpose: VOIC-03's review→store (clean) + VOIC-04's brief→plan handoff, reusing the existing
cockpit pipeline entirely (no new plan pipeline, no new gate).
Output: post-call summary + abnormal-brief surfacing + the offline e2e.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-voice-sessions/06-RESEARCH.md
@.planning/phases/06-live-voice-sessions/06-CONTEXT.md
@.planning/phases/06-live-voice-sessions/06-VALIDATION.md

@docs/design/BRAND.md                      # BINDING (CLAUDE.md #10)
@packages/backend/convex/cockpit.ts        # sendCockpitMessage({text, clientContext}) → {threadId}
@apps/web/app/(app)/dashboard/voice/page.tsx   # the phase state machine seam (plan 06)
@apps/web/e2e/cockpit-briefing.spec.ts     # the SMOKE-seed + tenantId-from-JWT e2e precedent

<interfaces>
- sendCockpitMessage: `tenantAction({threadId?, text, clientContext?}) → {threadId}` — the first
  turn creates the thread + its single plans row. Seed it with the brief's Decisions + Action
  items as the opening user turn, then navigate to /workspace?thread=<threadId> (lands at the
  existing PLAN card + single Approve — VOIC-04 satisfied by construction, RESEARCH Pattern 7).
- storeBrief: `api.voice.storeBrief({sessionId, editedMarkdown})` (plan 05) — clean-end store
  after review.
- Abnormal brief: forceEndSession already stored it (plan 05); this surfaces the existing
  vaultDocuments briefRef for review on next app open — read it, do not re-store.
- The e2e reuses the cockpit-briefing.spec SMOKE-seed convention (SMOKE:: drives the offline brief;
  no live mic). Live audio + the real Approve→send stay the plan-08 human-verify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-call summary — brief review/edit + store + the plan-handoff ask</name>
  <files>apps/web/app/(app)/dashboard/voice/PostCall.tsx, apps/web/app/(app)/dashboard/voice/page.tsx</files>
  <action>
    Build the post-call screen (the always-available surface both end types land on): render the
    brief markdown in an editable textarea (decisions + action items lead, narrative below), a
    "Save to vault" confirm → voice.storeBrief(editedMarkdown) for the clean-end path, and the
    VOIC-04 ask ("Turn this into a plan?" / "Just save"). Wire page.tsx to route a clean end here.
    BRAND tokens, keyboard + SR complete.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit</automated>
  </verify>
  <done>Clean-end brief is reviewable/editable and stores on confirm; the plan-vs-save ask is present and keyboard-operable.</done>
</task>

<task type="auto">
  <name>Task 2: Plan handoff (sendCockpitMessage) + abnormal-end brief surfacing</name>
  <files>apps/web/app/(app)/dashboard/voice/PostCall.tsx, apps/web/app/(app)/dashboard/voice/AbnormalBriefBanner.tsx</files>
  <action>
    Wire "Turn into a plan" → sendCockpitMessage({text: <Decisions + Action items from the brief>,
    clientContext}) → navigate /workspace?thread=<threadId> (the existing single-Approve gate).
    Build AbnormalBriefBanner: on next app open, if the tenant has an auto-stored voice brief not
    yet reviewed, surface it (link to the vault doc + offer the same plan conversion) — this keeps
    VOIC-03 true for dropped sessions without losing the review preference. ponytail: reuse the
    existing vault query for the briefRef; no new backend.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit</automated>
  </verify>
  <done>Plan conversion seeds a cockpit thread + navigates to the review gate; a dropped-session brief is surfaced on next open with the same convert affordance.</done>
</task>

<task type="auto">
  <name>Task 3: Offline e2e — brief review → store → proposable plan</name>
  <files>apps/web/e2e/voice.spec.ts, docs/playbooks/voice.md</files>
  <action>
    Author a Playwright spec (cockpit-briefing.spec pattern: tenantId from the Convex Auth JWT,
    SMOKE-seed the offline brief) that drives the deterministic half of VOIC-03/04: a SMOKE brief
    is reviewable, stores to the vault, and "Turn into a plan" lands a PROPOSABLE plan at the
    cockpit review gate (assert the PLAN card + that nothing sends before Approve). The live audio
    round-trip + the real Approve→governed-send stay the plan-08 human-verify (VALIDATION
    manual-only rows). Bump voice.md.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test:e2e -- voice</automated>
  </verify>
  <done>The offline e2e proves brief→store→proposable-plan without a live mic; voice.md bumped; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web exec tsc --noEmit` exit 0.
- `pnpm --filter @pikar/web test:e2e -- voice` green (offline SMOKE path) — or documented if the
  known E2E harness blocker (E2E_USER_ credentials / Gmail-connected gate) applies, per prior phases.
- `node scripts/check-playbooks.mjs` exits 0.
</verification>

<success_criteria>
Both end types converge on one reviewable, storable brief; converting it enters the normal
single-Approve pipeline; a dropped session's brief is never lost and is surfaced on next open.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-voice-sessions/06-07-SUMMARY.md`.
</output>
