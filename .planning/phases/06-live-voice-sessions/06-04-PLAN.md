---
phase: 06-live-voice-sessions
plan: 04
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/voiceBriefDraft.test.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [VOIC-03]
must_haves:
  truths:
    - "draftVoiceBrief structures the kept transcript into the fixed brief sections via a toolless generateObject call, using the voice-brief registry skill (no hardcoded prompt)"
    - "A SMOKE:: sentinel short-circuits to a deterministic offline brief so the brief→vault→plan path is unit-testable without a model call"
    - "The brief is generated in the spoken language and the full transcript is welded on in code (never model-authored)"
    - "The call is governed like every other model call: DEFAULT→CHEAP fallback, both recordModelSpend'd, fail-closed skill load"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "draftVoiceBrief internalAction (returns brief markdown)"
      contains: "draftVoiceBrief"
    - path: "packages/backend/convex/voiceBriefDraft.test.ts"
      provides: "SMOKE offline path test → deterministic fixed-section markdown"
  key_links:
    - from: "llm.draftVoiceBrief"
      to: "buildBriefMarkdown (@pikar/voice)"
      via: "code welds transcript + composes final markdown"
      pattern: "buildBriefMarkdown"
    - from: "llm.draftVoiceBrief"
      to: "getActiveSkill(voice-brief)"
      via: "fail-closed skill load"
      pattern: "voice-brief|VOICE_BRIEF_SKILL"
---

<objective>
Add the toolless brief-drafting action — the single place a transcript becomes structured brief
sections — mirroring `digestInbox`/`draftDocument` line-for-line so it inherits the governed-spend
rails, the fail-closed registry-skill load, and the SMOKE offline seam.

Purpose: VOIC-03's brief content. `voice.storeBrief` (plan 05) calls this, then ingests the
returned markdown as an ordinary vault document.
Output: `draftVoiceBrief` in `llm.ts` (the sole node module) + its SMOKE-driven test.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-voice-sessions/06-RESEARCH.md
@.planning/phases/06-live-voice-sessions/06-CONTEXT.md

# Mirror these VERBATIM (shape, fallback, SMOKE seam, recordModelSpend):
@packages/backend/convex/llm.ts            # digestInbox + draftDocument + recordModelSpend + SMOKE:: sentinel
@packages/voice/src/brief.ts               # buildBriefMarkdown composer + section shape
@packages/backend/convex/documentDraft.test.ts   # offline-draft test precedent

<interfaces>
- draftVoiceBrief(transcript, language) → Promise<string> (final brief markdown). Give it an
  EXPLICIT return type on every path (RESEARCH Pitfall 1 — circular-inference).
- Skill: `getActiveSkill("voice-brief")` fail-closed (NO_ACTIVE_SKILL), from the registry.
- Schema: reuse the brief section shape from @pikar/voice as the `generateObject` schema
  (strict-mode-legal — every field in properties AND required).
- SMOKE:: seam: a `SMOKE::` prefixed transcript returns a deterministic offline brief (fixed
  sections + a welded sample transcript) with NO model call — the plan-05 convex-test path.
- Governance: DEFAULT_MODEL → isFallbackEligible → one CHEAP_MODEL retry; BOTH branches
  recordModelSpend'd. draftVoiceBrief stays TOOLLESS (generateObject, no tools).
- `draftVoiceBrief` MUST live inside llm.ts (the only "use node" module) — do NOT create a second
  node module (RESEARCH anti-pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: draftVoiceBrief — toolless brief drafting, SMOKE seam, governed spend</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/voiceBriefDraft.test.ts, docs/playbooks/cockpit.md</files>
  <behavior>
    - Given a SMOKE:: transcript, returns deterministic markdown with all fixed sections present, empty sections rendered "None", and the transcript welded verbatim at the end — no model call.
    - Given a real transcript, loads voice-brief, runs generateObject over the brief-section schema with system=skill.body and the transcript as prompt, then composes buildBriefMarkdown(sections, transcript, language).
    - Fails closed if no active voice-brief skill.
    - On a fallback-eligible primary error, retries CHEAP_MODEL; both attempts recordModelSpend.
    - The brief responds in the passed language.
  </behavior>
  <action>
    Copy `digestInbox`/`draftDocument` structure exactly (fail-closed getActiveSkill FIRST →
    SMOKE:: short-circuit AFTER → generateObject with abortSignal timeout + maxRetries:1 →
    DEFAULT→CHEAP fallback both metered). Compose the final markdown via
    `buildBriefMarkdown` from @pikar/voice (the model fills narrative sections; the transcript is
    code-welded — like joinDigest welds code-owned fields). Explicit `Promise<string>` return
    type on all paths. Test drives the SMOKE:: path (documentDraft.test.ts pattern) asserting the
    fixed sections + welded transcript + "None" for an empty section. llm.ts is watched by
    cockpit.md → bump cockpit.md (Last verified + name draftVoiceBrief as the voice brief seam).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voiceBriefDraft</automated>
  </verify>
  <done>SMOKE path returns deterministic fixed-section brief markdown with the welded transcript; skill load is fail-closed; both model branches recordModelSpend; cockpit.md bumped.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test voiceBriefDraft` green.
- Grep: `tools:` absent from the draftVoiceBrief block (generateObject, toolless); `getActiveSkill` with voice-brief present; explicit Promise<string> on every return path.
- `node scripts/check-playbooks.mjs` exits 0 (cockpit.md bumped for the llm.ts edit).
</verification>

<success_criteria>
Plan 05's `storeBrief` can call `draftVoiceBrief` and get back ready-to-ingest brief markdown —
offline via SMOKE for the unit path, governed + registry-driven for the live path.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-voice-sessions/06-04-SUMMARY.md`.
</output>
