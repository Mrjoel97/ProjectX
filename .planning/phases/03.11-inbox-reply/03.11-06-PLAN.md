---
phase: 03.11-inbox-reply
plan: 06
type: execute
wave: 6
depends_on: ["03.11-05"]
files_modified:
  - docs/playbooks/cockpit.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [RPLY-01]

must_haves:
  truths:
    - "The full backend suite + eval gate are green before the checkpoint"
    - "The owner sends a real 'reply to X' through the cockpit and confirms it lands IN-THREAD in Gmail"
    - "The Re: subject and the reply appearing inside the original conversation are both confirmed"
    - "Pitfall 1 is resolved empirically: which of RFC-headers / threadId actually threads is recorded"
    - "RPLY-01 is marked complete in REQUIREMENTS traceability + ROADMAP Phase 3.11"
  artifacts:
    - path: "docs/playbooks/cockpit.md"
      provides: "the reply subsystem invariants + Last verified bump + the empirically-confirmed threading mechanism"
      contains: "reply"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3.11 marked complete with plan list"
      contains: "RPLY-01"
  key_links:
    - from: "the owner's live reply"
      to: "the original Gmail thread"
      via: "In-Reply-To/References (+ threadId) actually grouping the reply in-thread"
      pattern: "in-thread"
---

<objective>
Prove the one thing no unit can (RESEARCH Pitfall 1/4 / VALIDATION Manual-Only): a real "reply to X"
sent through the governed cockpit actually lands IN-THREAD in a real Gmail conversation. Then close the
phase — bump the cockpit playbook with the reply invariants + the empirically-confirmed threading
mechanism, and mark RPLY-01 complete.

Purpose: the Gmail threadId-vs-RFC-header behavior is contradicted in Google's own docs; only a live
send resolves which combination threads. This is the phase gate.
Output: a confirmed in-thread reply + closed phase bookkeeping.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.11-inbox-reply/03.11-RESEARCH.md
@.planning/phases/03.11-inbox-reply/03.11-VALIDATION.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full green sweep before the checkpoint</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
    Run the phase gate suites and record results in the SUMMARY: full `pnpm --filter @pikar/backend test`
    (gmail / cockpit / cockpitTools / llmRedaction / skills all green; the documented pre-existing
    audit.test.ts auditCounts red + any machine-load runCockpitAgent timeout are the known non-regressions
    — name them, do not chase). Confirm cockpit-agent@N is the ACTIVE version (Plan 05 gate). Do NOT run
    `git commit` via gsd-tools (Windows quoting — memory); use plain git. Draft the cockpit.md reply
    invariants (recipient-by-ref no-panel, toolless original-body ingestion, single-arm fan-out unchanged,
    threading fields optional) ready to finalize in Task 3 once the live mechanism is confirmed.
  </action>
  <verify>
    <automated>cd packages/backend && pnpm test 2>&1 | tail -15</automated>
  </verify>
  <done>Full backend suite green save the documented non-regressions; cockpit-agent@N active; cockpit.md reply invariants drafted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live in-thread reply human-verify (checkpoint)</name>
  <action>Human verification checkpoint — the owner sends a real "reply to X" through the cockpit and confirms it lands in-thread in real Gmail (the one thing units cannot prove; Pitfall 1/4). No new artifacts.</action>
  <what-built>The full reply capability: "reply to X" → replyToMessage resolves the target server-side, sets recipient by ref (no panel) + Re: subject + threading, drafts the body toollessly from the original, and delivers through the unchanged single Approve → fan-out. All units + the eval gate (incl. the injection probe) are green.</what-built>
  <how-to-verify>
    1. Open the cockpit workspace with your Gmail-connected account.
    2. Pick a real message in your inbox you can reply to. Type e.g. "reply to <sender>'s <subject> email, tell them <something>".
    3. Confirm the PLAN card shows a `Re: <real subject>` and the recipient as the sender's name (not "(no name)", not an address).
    4. Approve (the single governed Approve). Confirm NOTHING sent before you approved.
    5. Open Gmail (web) and confirm the sent reply appears INSIDE the original conversation thread (grouped, not a new thread), with the Re: subject.
    6. Report which threading actually worked (RFC headers alone, or headers + threadId) — Pitfall 1 empirical resolution.
    If it does NOT thread: report what you saw (new thread? 400 "Subject doesn't match"? wrong In-Reply-To?) — that feeds a gap-closure plan, not a silent pass.
  </how-to-verify>
  <verify>Owner confirms the sent reply appears in-thread in real Gmail with the Re: subject and zero pre-Approve sends; the working threading mechanism is reported.</verify>
  <done>User typed "approved" — an in-thread reply was seen in real Gmail and the threading mechanism recorded.</done>
  <resume-signal>Type "approved" (and the threading mechanism you observed), or describe what went wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the phase — playbook + REQUIREMENTS + ROADMAP + STATE</name>
  <files>docs/playbooks/cockpit.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    After the owner's approval: finalize cockpit.md with the reply invariants + the CONFIRMED threading
    mechanism from the checkpoint (record which of RFC-headers/threadId actually threaded) + bump Last
    verified. Mark RPLY-01 Complete in REQUIREMENTS.md traceability (with the human-verify date). Update
    ROADMAP Phase 3.11: mark complete, fill the plan list (01-06), bump the count from "0 plans". Append a
    STATE.md close-out block (the append-only convention this repo uses) noting the confirmed in-thread
    reply + the threading mechanism. gsd-tools `phase complete` silently no-ops triple-decimal phases
    (memory) — verify the ROADMAP edit landed by hand. Commit with plain git (not gsd-tools commit).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "RPLY-01" .planning/REQUIREMENTS.md && node scripts/check-playbooks.mjs 2>&1 | tail -3</automated>
  </verify>
  <done>cockpit.md carries the reply invariants + confirmed threading mechanism (Last verified bumped); RPLY-01 Complete in traceability; ROADMAP 3.11 closed with the 6-plan list; STATE close-out appended; check-playbooks green.</done>
</task>

</tasks>

<verification>
- Full backend suite green (save documented non-regressions) + eval gate green (Plan 05).
- Owner confirms an in-thread reply in real Gmail with the Re: subject and zero pre-Approve sends.
- The empirical threading mechanism (Pitfall 1) is recorded in cockpit.md.
- RPLY-01 Complete; ROADMAP 3.11 closed; check-playbooks green.
</verification>

<success_criteria>
The phase goal is observably TRUE end to end: "draft a reply to X" is a real, in-thread reply — recipient
by ref (no panel, refs-only to the loop), Re: subject + Gmail threading, body drafted toollessly from the
untrusted original, delivered through the unchanged governed single Approve → fan-out with zero sends
before Approve — and a human has seen it thread in real Gmail.
</success_criteria>

<output>
After completion, create `.planning/phases/03.11-inbox-reply/03.11-06-SUMMARY.md`.
</output>
