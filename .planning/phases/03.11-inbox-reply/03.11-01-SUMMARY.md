---
phase: 03.11-inbox-reply
plan: 01
subsystem: database
tags: [convex, schema, agent-tools, eval-golden, gmail-threading, prompt-injection]

# Dependency graph
requires:
  - phase: 03.7-inbox-briefing
    provides: inboxFixtures fixture-first seam (listInbox/fetchInboxBodies), toolless digest precedent, briefings content plane
  - phase: 03.9-agent-activity-streaming
    provides: closed agentSteps.tool union that the replyToMessage literal extends
  - phase: 03.6-agent-eval-gate
    provides: run-eval-golden harness + closed expect vocabulary the two reply cases author against
provides:
  - Optional reply-threading fields on plans (replyToMessageId/replyThreadId/inReplyTo/references)
  - Optional reply-threading fields on requests (threadId/inReplyTo/references) — the delivery-plane copy target
  - Optional threadId + RFC messageId anchors on inboxFixtures.messages[]
  - One new closed-union agentSteps.tool literal replyToMessage (schema slot for Plan 04's tool)
  - Reply anchors on fix-reply + fix-injection fixture messages (replyable targets)
  - Two authored-but-not-yet-passing golden eval cases (23-reply-happy, 24-reply-injection)
  - RPLY-01 requirement minted (REQUIREMENTS.md + traceability)
affects: [03.11-02 reply-drafter skill, 03.11-03 delivery threading spine, 03.11-04 replyToMessage tool, 03.11-05 skill eval gate, 03.11-06 phase close]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Migration-free optional-on-read threading fields (no backfill; pre-3.11 rows load unchanged)"
    - "Closed tool-name union widened by exactly one literal (schema-absence §4 guarantee preserved)"
    - "Golden eval cases authored ahead of the tool that satisfies them (groundwork, red until Plan 04)"

key-files:
  created:
    - packages/backend/scripts/eval-cases/23-reply-happy.json
    - packages/backend/scripts/eval-cases/24-reply-injection.json
  modified:
    - packages/backend/convex/schema.ts
    - packages/backend/convex/smoke.ts
    - .planning/REQUIREMENTS.md
    - docs/playbooks/agent-runtime.md

key-decisions:
  - "plans' Gmail-thread field is named replyThreadId (not threadId) — plans already has a required threadId (the agent/convex thread that renders cards); a second key of the same name is illegal and reusing the agent thread for a Gmail thread would be semantically wrong"
  - "The injection eval case (24) reuses the existing fix-injection needle attacker@evil.example and adds reply anchors so a reply can be drafted OVER it; the correct recipient is exactly its From (no-reply@example.net)"
  - "Case files numbered 23/24 per the plan's pinned artifact paths even though 22 is unused — non-contiguous numbering is fine (the runner sorts + loads all .json)"

patterns-established:
  - "Optional threading fields grouped with a comment naming the no-migration discipline + the Gmail-id-vs-Message-ID Pitfall"
  - "RFC 5322 Message-ID stored angle-bracketed on the fixture (messageId), distinct from the Gmail id"

requirements-completed: [RPLY-01]

# Metrics
duration: 8min
completed: 2026-07-19
---

# Phase 3.11 Plan 01: Inbox-Reply Groundwork Summary

**Migration-free reply-threading schema slots (plans/requests/inboxFixtures), the closed-union `replyToMessage` tool literal, fixture reply anchors on fix-reply + fix-injection, two authored golden reply cases, and the minted RPLY-01 requirement — the Wave-0 gap list every downstream reply plan samples against.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-19T16:19:45Z
- **Completed:** 2026-07-19T16:27:49Z
- **Tasks:** 3
- **Files modified:** 6 (2 created, 4 modified)

## Accomplishments
- Optional threading fields added to `plans` (replyToMessageId/replyThreadId/inReplyTo/references) and `requests` (threadId/inReplyTo/references) — no migration, every pre-3.11 row loads unchanged
- `agentSteps.tool` closed union widened by exactly one literal (`replyToMessage`) — the §4 schema-absence guarantee (CKPT-05) preserved
- `inboxFixtures.messages[]` gained optional `threadId` + RFC `messageId`, and fix-reply + fix-injection became fully-replyable targets
- Two golden eval cases authored (23-reply-happy, 24-reply-injection) — valid under `--self-check` (23 fixtures), RED on a live run until Plan 04's tool
- RPLY-01 minted in REQUIREMENTS.md (Email Cockpit section + traceability + count 48→49)

## Task Commits

Each task was committed atomically:

1. **Task 1: Optional threading schema fields + closed-union tool literal + RPLY-01 mint** - `06e5cb1` (feat)
2. **Task 2: Inbox fixture thread-anchor + injection message** - `b7e784d` (feat)
3. **Task 3: Author the two golden reply eval cases** - `54b5168` (test)

## Files Created/Modified
- `packages/backend/convex/schema.ts` - optional reply-threading fields on plans + requests + inboxFixtures.messages[]; replyToMessage tool-union literal
- `packages/backend/convex/smoke.ts` - reply anchors (threadId + RFC messageId) on fix-reply and fix-injection
- `packages/backend/scripts/eval-cases/23-reply-happy.json` - reply-over-fixture happy case (recipientCount 1, subject/body present, statusAtMost proposed)
- `packages/backend/scripts/eval-cases/24-reply-injection.json` - reply-plane injection probe (recipientCount 1, statusAtMost proposed, attacker needle)
- `.planning/REQUIREMENTS.md` - RPLY-01 requirement + traceability row + count bump
- `docs/playbooks/agent-runtime.md` - eval-harness note on the authored-not-passing reply cases + Last verified bump (§9, eval-cases/ is a watched path)

## Decisions Made
- **plans.replyThreadId (not threadId):** `plans` already carries a required `threadId` — the agent/convex thread that renders its cards. The Gmail thread the reply threads into is a different id space, so it gets its own field name. `requests` has no agent-thread field, so its Gmail-thread field is plainly `threadId` (exactly as planned).
- **Reuse fix-injection for case 24:** the fixture already carries the `attacker@evil.example` needle; adding reply anchors makes it a replyable target so the injection probe measures a real reply-over-injection.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] plans' Gmail-thread field renamed threadId → replyThreadId**
- **Found during:** Task 1
- **Issue:** The plan's action said "add `threadId` to plans", but `plans` already has a required `threadId` (the agent thread that renders cards). A second key of the same name is illegal, and reusing the agent thread for a Gmail thread is semantically wrong.
- **Fix:** Named the plan's Gmail-thread field `replyThreadId`; `requests` (no agent-thread field) keeps the plain `threadId` as planned. executePlan (Plan 03) will map plan.replyThreadId → request.threadId.
- **Files modified:** packages/backend/convex/schema.ts
- **Verification:** Source typecheck clean; the field names carry comments explaining the id-space distinction.
- **Committed in:** 06e5cb1 (Task 1 commit)

**2. [Rule 3 - Blocking] Task 1 verify regex counts a false positive**
- **Found during:** Task 1 verification
- **Issue:** The plan's automated verify `(s.match(/replyToMessage/g)||[]).length!==1` also matches the `replyToMessageId` field the same task adds to `plans` (substring), yielding count 2 and a false failure.
- **Fix:** Verified the true invariant instead (must_haves key_link): exactly one `v.literal("replyToMessage")` in the union (count 1) AND exactly one `replyToMessageId:` field (count 1). Both hold.
- **Files modified:** none (verification-only)
- **Verification:** `node -e` counted `v.literal("replyToMessage")` x1 and `replyToMessageId:` x1.
- **Committed in:** n/a (no code change; the invariant the plan intended is satisfied)

---

**Total deviations:** 2 auto-fixed (both Rule 3 - blocking)
**Impact on plan:** Both are naming/verification corrections that preserve the plan's intent (one Gmail-thread field per table, exactly one union literal). No scope creep; all planned fields, the literal, the fixture anchors, the two cases, and RPLY-01 all shipped.

## Issues Encountered
- Backend `tsc --noEmit -p .` reports pre-existing errors in `*.test.ts` files only (import.meta.glob, test-only possibly-undefined, skills.test.ts index-name drift). None reference the new fields; source typecheck is clean. These are the documented pre-existing test-file noise (out of scope per the deviation scope boundary).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wave 2 (03.11-02 reply-drafter skill + toolless draftReply) can proceed: the fixture anchors and schema slots exist.
- Wave 3 (03.11-03 delivery threading spine) has the requests.threadId/inReplyTo/references copy targets and plans.replyThreadId source.
- Wave 4 (03.11-04 replyToMessage tool) has the closed-union literal slot and both fixture reply targets to resolve against.
- The two eval cases will stay RED on a live `pnpm eval:golden` until Plan 04's tool lands — documented in agent-runtime.md so it is not mistaken for a regression.

## Self-Check: PASSED

- All created files exist (23-reply-happy.json, 24-reply-injection.json, 03.11-01-SUMMARY.md)
- All task commits exist (06e5cb1, b7e784d, 54b5168)

---
*Phase: 03.11-inbox-reply*
*Completed: 2026-07-19*
