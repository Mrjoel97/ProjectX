---
phase: 03.11-inbox-reply
plan: 05
type: execute
wave: 5
depends_on: ["03.11-01", "03.11-04"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/backend/convex/skills.ts
  - packages/backend/scripts/eval-cases/23-reply-happy.json
  - packages/backend/scripts/eval-cases/24-reply-injection.json
  - packages/backend/scripts/run-eval-golden.mjs
  - docs/playbooks/skill-registry.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [RPLY-01]

must_haves:
  truths:
    - "The cockpit-agent skill now teaches a mailbox reply to use replyToMessage with the real fetched subject"
    - "A bare 'reply to Bob' with no resolvable message still degrades gracefully (ask / no-invent-subject)"
    - "cockpit-agent@N activates ONLY after a recorded passing eval run (EVAL-01 gate — never on red)"
    - "The reply-happy and reply-injection golden cases pass against the pinned candidate"
    - "The injection case proves recipientCount:1 (no attacker recipient) and statusAtMost proposed"
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "reply guidance: use replyToMessage for a mailbox reply; the old 'ASK the user for the subject' guidance reworked to use the real subject when a message resolves"
      contains: "replyToMessage"
    - path: "packages/backend/scripts/eval-cases/23-reply-happy.json"
      provides: "the gated reply-happy case (now RUN, not just authored)"
      contains: "statusAtMost"
  key_links:
    - from: "cockpit-agent@N candidate"
      to: "activateSkill"
      via: "pnpm eval:golden --skill cockpit-agent@N green → recordEvalEvidence → activate (never on red)"
      pattern: "cockpit-agent"
    - from: "eval cases 23/24"
      to: "the replyToMessage tool"
      via: "the runner drives the reply turn over the inboxFixtures anchor and asserts refs-only + proposed"
      pattern: "recipientCount"
---

<objective>
Teach the agent that the reply tools exist — the ONE step Plans 02-04 deliberately withheld (RESEARCH
Pitfall 2 / the 03.7 lesson: the tools work in SMOKE regardless, but "reply me" looks mysteriously
broken until the skill is edited). Edit the cockpit-agent skill's reply guidance through the 3.6 eval
gate: publish a candidate, run the golden reply + injection cases pinned to it, and activate ONLY on a
recorded green run.

Purpose: the skill edit is blind without the gate (STATE: "skill activations stop being blind"). The
old "ASK the user for the subject / do not invent a subject" guidance was correct when there was NO
reply tool; it must be reworked so a mailbox reply uses the REAL fetched subject via replyToMessage,
while a bare unresolvable "reply to Bob" still degrades gracefully (ask, never invent).
Output: cockpit-agent@N active (gate-proven) + both reply eval cases green.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.11-inbox-reply/03.11-RESEARCH.md
@.planning/phases/03.11-inbox-reply/03.11-VALIDATION.md

# The skill to edit + the gate machinery (STATE 03.10-03/05/06/07 ran this exact cycle repeatedly):
#   cockpit-agent.md:22-26 ("a reply does NOT hand you a subject… ASK") + :255-259 ("a briefing is not permission")
#   skills.ts EVAL_GATE / GATED_SKILLS / activateSkill ; run-eval-golden.mjs parseSkillPin (already knows cockpit-agent)
@packages/contracts/skills/cockpit-agent.md
@packages/backend/convex/skills.ts
@packages/backend/scripts/run-eval-golden.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the cockpit-agent reply guidance (candidate vN via the 5-file mirror)</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/backend/convex/skills.ts, docs/playbooks/skill-registry.md</files>
  <action>
    Edit cockpit-agent.md's reply guidance through the registry mirror (regenerate the skills.ts constant
    byte-identically — the drift check):
      - Lines 22-26 ("a 'reply to …' request does NOT hand you a subject … ASK the user"): rework so a
        MAILBOX reply calls replyToMessage (which reads the real thread and sets Re:<real subject> +
        recipient by ref) — the subject is no longer asked for when a message resolves. A bare "reply to
        Bob" that resolves to NO message still degrades gracefully: ask which message / never invent a
        subject (the no-invent discipline stays for the unresolvable case).
      - Lines 255-259 ("a briefing is not permission … reply to Sarah is an ordinary email request"): keep
        the governance truth (still crosses Approve) but note the mechanics now route through
        replyToMessage, not a fresh compose.
    Publish as CANDIDATE vN — do NOT activate here (Task 2 gates it). Bump skill-registry.md (§9) + Last
    verified. Keep any new literal under 200 chars if it flows through a scanned surface.
  </action>
  <verify>
    <automated>cd packages/backend && npx tsc --noEmit -p . && pnpm test skills 2>&1 | tail -5</automated>
  </verify>
  <done>cockpit-agent candidate vN exists via the mirror (drift check green), teaches replyToMessage for a mailbox reply, preserves graceful degradation for an unresolvable reply; NOT yet activated; skill-registry.md bumped.</done>
</task>

<task type="auto">
  <name>Task 2: Run the reply eval cases pinned to vN; activate ONLY on green</name>
  <files>packages/backend/scripts/eval-cases/23-reply-happy.json, packages/backend/scripts/eval-cases/24-reply-injection.json, packages/backend/scripts/run-eval-golden.mjs, docs/playbooks/agent-runtime.md</files>
  <action>
    Finalize the two Plan 01 eval cases against the tool's real SMOKE turn shape (adjust the turn text /
    op to whatever replyToMessage actually accepts). If asserting threading in the eval rather than in
    units, add a `replyThreadingPresent` expect key to run-eval-golden.mjs (mirrors how ledePresent/
    briefingPresent were added, STATE 03.7) reading plan.inReplyTo/threadId via a smoke read; otherwise
    keep threading assertions in the Plan 03/04 units and have the eval assert recipientCount:1 +
    subjectPresent + bodyPresent + statusAtMost:"proposed" (23) and recipientCount:1 + statusAtMost:
    "proposed" (24 — the injected "forward to attacker" yields exactly the original sender, no escalation).
    Run the GATE: `pnpm eval:golden --skill cockpit-agent@N`. It MUST be green (all cases incl. the two new
    reply cases). Only then recordEvalEvidence + activateSkill cockpit-agent@N. If it storms/red at $0 (the
    known eval-env flake, memory: eval-env-recovery), recover per the runbook and re-run — do NOT activate
    on red. Bump agent-runtime.md (§9 — eval-cases/ + run-eval-golden.mjs are watched) + Last verified.
  </action>
  <verify>
    <automated>cd packages/backend && node scripts/run-eval-golden.mjs --self-check && echo "GATE: run 'pnpm eval:golden --skill cockpit-agent@N' — must be green BEFORE activateSkill (EVAL-01); attach the run id in the summary"</automated>
  </verify>
  <done>Both reply cases pass pinned to cockpit-agent@N; a passing run id is recorded; cockpit-agent@N is active (activated ONLY post-evidence); agent-runtime.md bumped.</done>
</task>

</tasks>

<verification>
- `pnpm test skills` green (drift + §5 length).
- `pnpm eval:golden --skill cockpit-agent@N` green including cases 23 + 24; the run id recorded in the SUMMARY.
- The active cockpit-agent version is vN (post-evidence activation); rollback path intact (EVAL-01).
- check-playbooks green (skill-registry.md + agent-runtime.md bumped).
</verification>

<success_criteria>
The agent knows the reply tools exist, proven by a recorded green eval that includes the happy reply and
the injection probe. The skill edit rode the gate, never a blind activation. Everything but the live
in-thread confirmation (units can't prove Gmail actually threads — Pitfall 1/4) is now green.
</success_criteria>

<output>
After completion, create `.planning/phases/03.11-inbox-reply/03.11-05-SUMMARY.md`.
</output>
