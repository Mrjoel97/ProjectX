---
phase: 03.11-inbox-reply
plan: 04
type: execute
wave: 4
depends_on: ["03.11-02", "03.11-03"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/llmRedaction.test.ts
  - packages/backend/convex/cockpitTools.test.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [RPLY-01]

must_haves:
  truths:
    - "replyToMessage resolves the target server-side from a fuzzy ref (sender/subject/range), no message id crosses the loop"
    - "It sets recipient=[fromAddr] directly via patchPlan — no resolveContacts panel round-trip, no writeCandidates"
    - "The recipient shows to the loop by display-name label only; the From address never appears in the tool return"
    - "It sets Re:<real subject> + threadId + inReplyTo + references on the plan in one turn (reply, not compose)"
    - "The original body flows ONLY into the toolless draftReply; never into the tool return or any audit payload"
    - "resetPlan clears the four threading fields (Pitfall 6 — no stale thread on the next fresh compose)"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "replyToMessage tool in buildCockpitTools: server-side resolve → patchPlan recipient-by-ref + threading + Re: subject → fetchInboxBodies → draftReply → labels/counts-only return"
      contains: "replyToMessage"
    - path: "packages/backend/convex/plans.ts"
      provides: "patchPlan accepts the four threading fields; resetPlan clears them"
      contains: "inReplyTo"
    - path: "packages/backend/convex/llmRedaction.test.ts"
      provides: "mutation-checked static scan: original body + From address never in the tool return or audit"
      contains: "replyToMessage"
  key_links:
    - from: "replyToMessage tool"
      to: "plans.patchPlan"
      via: "recipients=[fromAddr] + recipientNames[addr]=displayName + Re:subject + threading, server-side"
      pattern: "patchPlan"
    - from: "replyToMessage tool"
      to: "draftReply (toolless)"
      via: "fetched original body passed ONLY here, never returned to the loop"
      pattern: "draftReply"
    - from: "resetPlan (plans.ts)"
      to: "the threading clear set"
      via: "explicit clear of replyToMessageId/threadId/inReplyTo/references"
      pattern: "threadId"
---

<objective>
Wire the one new governed tool — `replyToMessage` — that turns "reply to X" into a real reply in a
single turn: resolve the target server-side, set the recipient by ref (no panel), set the Re: subject
+ threading fields, draft the body through the toolless draftReply, and return LABELS/COUNTS only.
This is the recipient-by-ref reuse of the UAT-F recipientNames flow (03.10-07) applied to a message
instead of a contact pick.

Purpose: closes the live-UAT gap — the model finally has a tool that reads the real thread instead of
downgrading to a fresh compose. The §2-D refs-only + toolless-ingestion invariants are enforced here by
a mutation-checked static scan (the briefInbox rawBodies precedent).
Output: replyToMessage tool + patchPlan/resetPlan threading writers + the boundary scan.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.11-inbox-reply/03.11-RESEARCH.md

# The precedents to reuse (line refs from RESEARCH):
#   llm.ts:591 buildCockpitTools (add the tool here) ; llm.ts:759 resolveContacts (server-side resolve, labels-only, no-guess)
#   llm.ts:473/502 buildAgentContext + recipientNames (the label render — reuse verbatim, set recipientNames[addr]=displayName)
#   llm.ts:1155 listInbox strips ids (Pitfall 3 — the loop has NO message ids, so the tool takes a fuzzy ref)
#   llm.ts:1202-1218 briefInbox rawBodies (the toolless-boundary static-scan precedent) ; draftReply from Plan 02
#   plans.ts patchPlan / resetPlan (03.10-05 built resetPlan because patchPlan drops undefined — clear must be explicit)
@packages/backend/convex/llm.ts
@packages/backend/convex/plans.ts
</context>

<interfaces>
<!-- Reuse from Plan 02 (draftReply) and Plan 03 (read plane returns target headers server-side). -->
```typescript
// draftReply(tenantId, safeText, originalBody, skillVersion?) -> { body }   [toolless]
// read plane returns for the resolved target: { id, from, subject, threadId, messageIdHeader, referencesHeader }
// patchPlan accepts: recipients, recipientNames, subject, replyToMessageId, threadId, inReplyTo, references
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: patchPlan threading writers + resetPlan clears them</name>
  <files>packages/backend/convex/plans.ts</files>
  <behavior>
    - patchPlan accepts optional replyToMessageId/threadId/inReplyTo/references and writes them.
    - resetPlan clears all four (Pitfall 6: a reply then "start over" must not leave a stale threadId
      that silently threads the next fresh compose into the old conversation).
  </behavior>
  <action>
    Add the four optional threading fields to patchPlan's args + write path. Add all four to resetPlan's
    explicit clear set (03.10-05 built resetPlan precisely because patchPlan drops undefined — the clear
    must name each field, not rely on undefined). ponytail: extend the existing arg objects; no new mutation.
  </action>
  <verify>
    <automated>cd packages/backend && npx tsc --noEmit -p . && node -e "const s=require('fs').readFileSync('convex/plans.ts','utf8'); const r=s.slice(s.indexOf('resetPlan')); if(!/threadId/.test(r)||!/inReplyTo/.test(r)) throw new Error('resetPlan must clear threading');"</automated>
  </verify>
  <done>patchPlan writes the four fields; resetPlan explicitly clears all four; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The replyToMessage tool (server-side resolve → recipient-by-ref → toolless draft)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts, docs/playbooks/cockpit.md</files>
  <behavior>
    - replyToMessage({ sender?, subject?, range? }) resolves to ONE message server-side (search → newest
      match). 0 matches → "I couldn't find that message"; 2+ → list candidates by label and ask (the
      resolveContacts no-guess discipline). It never guesses and never substitutes.
    - On a single match: plan.recipients=[fromAddr], plan.candidates empty (NO writeCandidates, no panel),
      recipientNames[fromAddr.toLowerCase()]=displayName, subject startsWith "Re:" (stripRePrefix avoids
      "Re: Re:"), and replyToMessageId/threadId/inReplyTo/references all set.
    - The tool return string contains the display-name label (#1: Sarah Chen) but NOT the From address,
      NOT the Message-ID, NOT the original body.
  </behavior>
  <action>
    Add replyToMessage to buildCockpitTools (llm.ts:591). inputSchema: sender?/subject?/range? — range an
    ENUM (the §4 audit-payload discipline). Flow (RESEARCH spine map / Code Examples):
      1. server-side resolve via the read plane (Plan 03) — search/listInbox to ONE target; 0/2+ → clarify,
         write nothing (Pitfall 3, resolveContacts no-guess).
      2. parseAddress(target.from) → { displayName, address } (@pikar/core — do not regex).
      3. patchPlan: recipients=[address], recipientNames={[address.toLowerCase()]: displayName ?? ""},
         subject=`Re: ${stripRePrefix(target.subject)}`, replyToMessageId=target.id, threadId,
         inReplyTo=target.messageIdHeader, references=buildReferences(target). NO writeCandidates (no panel).
      4. fetchInboxBodies(target.id) → raw original body (server-side only).
      5. scanText the user's reply intent (GRDL-01, same as draftBody), then draftReply(safeText, originalBody)
         → plan.body. The originalBody flows ONLY into draftReply — never a tool return, never a log.
      6. return LABELS/COUNTS only: `Reply set up to #1 (${displayName ?? "no name"}), subject "Re: …",
         body drafted.` — no address, no Message-ID, no body.
    buildAgentContext already renders recipientNames as `#1: Sarah Chen` (reuse verbatim — the UAT-F path).
    Add cockpitTools.test.ts cases for every <behavior> bullet (recipient-by-ref, empty candidates, Re: +
    threading set, label-not-address in the return, 0/2+ clarify). Bump cockpit.md (§9) + Last verified.
  </action>
  <verify>
    <automated>cd packages/backend && pnpm test cockpitTools 2>&1 | tail -8</automated>
  </verify>
  <done>replyToMessage sets recipient-by-ref + Re: subject + threading + toolless-drafted body in one turn, returns labels not addresses, no panel round-trip, clarifies on 0/2+; cockpitTools green; cockpit.md bumped.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-checked toolless-boundary static scan</name>
  <files>packages/backend/convex/llmRedaction.test.ts</files>
  <action>
    Extend the toolless-boundary static scan (the briefInbox rawBodies precedent, llm.ts:1202-1218 /
    llmRedaction.test.ts) to cover the replyToMessage body-bearing block AND draftReply: assert the fetched
    original body and the resolved From address never appear in the tool return string or in any audit
    payload. Mutation-check each scan the 03.7 way: temporarily leak the body into the return / leak the
    From into the tool string / add tools:{} to draftReply — confirm each mutation makes the scan RED, then
    restore. State in a comment that the scans were mutation-verified (the substituted evidence per the
    03.7 house rule).
  </action>
  <verify>
    <automated>cd packages/backend && pnpm test llmRedaction 2>&1 | tail -8</automated>
  </verify>
  <done>The static scan proves original body + From address never cross to the loop return or an audit payload; each scan is mutation-verified RED-on-leak; llmRedaction green.</done>
</task>

</tasks>

<verification>
- `pnpm test cockpitTools` + `pnpm test llmRedaction` + `pnpm test plans` (if present) green; full `pnpm --filter @pikar/backend test` green at wave merge.
- grep proof: the replyToMessage block has no `writeCandidates` (no panel); the original body appears only in the draftReply call arg, nowhere in the return/audit.
- resetPlan clears all four threading fields.
- check-playbooks green (cockpit.md bumped).
</verification>

<success_criteria>
"Reply to X" now produces a real reply in one governed turn — recipient by ref (no panel, refs-only to
the loop), Re: subject + threading on the plan, body drafted toollessly from the untrusted original —
with the §2-D and toolless-ingestion invariants statically enforced. The plan can now reach `proposed`;
delivery (Plan 03) and the gate (Plan 05) do the rest.
</success_criteria>

<output>
After completion, create `.planning/phases/03.11-inbox-reply/03.11-04-SUMMARY.md`.
</output>
