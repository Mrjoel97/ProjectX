---
phase: 03.11-inbox-reply
plan: 03
type: execute
wave: 3
depends_on: ["03.11-02"]
files_modified:
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/gmailAuth.ts
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/gmail.test.ts
  - packages/backend/convex/cockpit.test.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [RPLY-01]

must_haves:
  truths:
    - "buildMime emits In-Reply-To + References headers when threading is present, none when absent"
    - "In-Reply-To uses the RFC Message-ID header (angle brackets preserved), never the Gmail id (Pitfall 2)"
    - "gmail.send POSTs threadId alongside raw when present (reinforcement; RFC headers are load-bearing)"
    - "executePlan copies threadId/inReplyTo/references from the plan onto every seeded requests row"
    - "getForDelivery returns the threading anchor so send actually sees it (Pitfall 4)"
    - "The read plane can return the target's From, Message-ID, References, Subject, threadId server-side"
  artifacts:
    - path: "packages/backend/convex/gmail.ts"
      provides: "buildMime threading headers + send POST threadId + target-header read"
      contains: "In-Reply-To"
    - path: "packages/backend/convex/gmailAuth.ts"
      provides: "getForDelivery returns threadId/inReplyTo/references from the request row"
      contains: "inReplyTo"
    - path: "packages/backend/convex/cockpit.ts"
      provides: "executePlan copies threading fields to the seeded requests rows"
      contains: "threadId"
  key_links:
    - from: "executePlan requests insert (cockpit.ts:~476)"
      to: "the requests row"
      via: "copy plan.threadId/inReplyTo/references onto each row"
      pattern: "threadId"
    - from: "getForDelivery (gmailAuth.ts:120)"
      to: "gmail.send / buildMime"
      via: "return object gains threadId/inReplyTo/references"
      pattern: "inReplyTo"
    - from: "buildMime (gmail.ts:78)"
      to: "the RFC 2822 bytes"
      via: "In-Reply-To + References header lines after Subject"
      pattern: "In-Reply-To:"
---

<objective>
Plumb the four threading fields end to end through the DELIVERY spine so a reply actually lands
in-thread: the read plane surfaces the target's RFC headers server-side, buildMime emits
In-Reply-To/References, send POSTs threadId, executePlan copies the fields onto the requests rows,
and getForDelivery returns them. This is the seam most likely to silently no-op (Pitfall 4:
getForDelivery is a hand-built projection — a field it doesn't return never reaches send).

Purpose: RESEARCH's stated risk is "missing a seam," not building the wrong thing. Trace the spine
map before writing. This plan owns every delivery-side seam; the tool that WRITES these fields is
Plan 04.
Output: threading survives read → plan → requests → getForDelivery → buildMime → send.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.11-inbox-reply/03.11-RESEARCH.md

# The exact seams (line refs from RESEARCH):
#   gmail.ts:78 buildMime (zero-attachment branch :84; multipart branch below) ; gmail.ts:138 send (POST body :182)
#   gmail.ts:391 listInbox / :484 fetchInboxBodies / :242 search — already fetch metadata/full; extend to return the target headers
#   gmailAuth.ts:120 getForDelivery (projection: subject:r.goal, body:r.editedBody??r.draft — new fields invisible unless added)
#   cockpit.ts:415 executePlan (requests insert ~:476) — single-arm-site startFanout at :371 stays UNCHANGED
@packages/backend/convex/gmail.ts
@packages/backend/convex/gmailAuth.ts
@packages/backend/convex/cockpit.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildMime threading headers + send POST threadId + target-header read</name>
  <files>packages/backend/convex/gmail.ts, packages/backend/convex/gmail.test.ts</files>
  <behavior>
    - buildMime(to, subject, body, [], { inReplyTo, references }) emits `In-Reply-To: <id>` and
      `References: <ids>` header lines after Subject, in BOTH the zero-attachment and multipart branches.
    - buildMime with no threading arg emits neither header (a normal compose is byte-identical to today).
    - In-Reply-To carries the RFC Message-ID header value WITH angle brackets, never the Gmail id (Pitfall 2).
    - send POSTs `{ raw, threadId }` when a threadId is present, `{ raw }` otherwise (Pitfall 1: headers
      are load-bearing, threadId is reinforcement — verified live in Plan 06, not asserted here).
  </behavior>
  <action>
    Extend buildMime (gmail.ts:78) with an optional `threading?: { inReplyTo: string; references: string }`
    param; when present, push the two header lines after the Subject line in the zero-attachment branch
    (:84) AND the multipart branch (RESEARCH Code Examples). Thread the same optional value from the send
    request through to the POST body (gmail.ts:182): `req.threading?.threadId ? { raw, threadId } : { raw }`.
    Also extend the read plane (listInbox/fetchInboxBodies/search — whichever the Plan 04 tool will call)
    so it can return the target message's From, Message-ID header, References header, Subject, and threadId
    server-side (they already fetch format=metadata/full and discard these headers today — just stop
    discarding them for the target). Keep the refs-only audit discipline: these headers/ids are returned to
    the SERVER-SIDE caller, never logged. buildReferences helper = original References (if any) + original
    Message-ID, space-joined. Add gmail.test.ts assertions for every bullet in <behavior> (buildMime is
    pure/exported — assert the header bytes; Message-ID-not-id; send POST includes threadId).
  </action>
  <verify>
    <automated>cd packages/backend && pnpm test gmail 2>&1 | tail -8</automated>
  </verify>
  <done>buildMime emits/omits threading headers per <behavior>; In-Reply-To uses the Message-ID header; send POSTs threadId when present; the read can surface the target headers server-side; gmail suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: executePlan copies threading to requests + getForDelivery returns them</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/gmailAuth.ts, packages/backend/convex/cockpit.test.ts, docs/playbooks/cockpit.md</files>
  <behavior>
    - A plan carrying threadId/inReplyTo/references, when executed, seeds requests rows that each carry
      the same three fields (copied, not dropped).
    - getForDelivery returns threadId/inReplyTo/references on its projection (else send never sees them).
    - A non-reply plan (no threading fields) seeds rows with none and getForDelivery returns none —
      unchanged behavior.
  </behavior>
  <action>
    In executePlan's requests insert (cockpit.ts:~476), copy plan.threadId/inReplyTo/references onto each
    seeded row (Pitfall 4). Do NOT add a second workflow.start — startFanout (cockpit.ts:371) stays the
    sole arm site (the grep-able zero-sends invariant). In getForDelivery (gmailAuth.ts:120), add
    threadId/inReplyTo/references to the returned projection object (it's hand-built — new fields are
    invisible otherwise). Add cockpit.test.ts assertions for the three <behavior> bullets (seed with a
    threading-bearing plan row directly — the tool that writes them is Plan 04; this test proves the COPY,
    not the write). Bump cockpit.md (§9 — cockpit.ts + gmailAuth.ts are cockpit.md-watched) + its Last verified.
  </action>
  <verify>
    <automated>cd packages/backend && pnpm test cockpit 2>&1 | tail -8</automated>
  </verify>
  <done>executePlan copies the three fields to requests rows; getForDelivery returns them; non-reply plans unchanged; single-arm-site intact; cockpit suite green; cockpit.md bumped.</done>
</task>

</tasks>

<verification>
- `pnpm test gmail` + `pnpm test cockpit` green.
- grep proof: exactly one `workflow.start` site (startFanout) — no new arm site added.
- buildMime with no threading arg is byte-identical to the pre-3.11 output (a normal compose unaffected).
- check-playbooks green (cockpit.md bumped).
</verification>

<success_criteria>
Threading survives the entire delivery spine from a plan's fields to the RFC 2822 bytes on the wire,
with the governed single-arm fan-out unchanged. The only missing piece after this plan is the tool
that WRITES the fields (Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/03.11-inbox-reply/03.11-03-SUMMARY.md`.
</output>
