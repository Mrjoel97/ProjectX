---
phase: 33.1-media-provider-migration-to-openrouter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decisions/027-grok-imagine-video-succeeds-sora-2-on-openrouter.md
  - docs/playbooks/media.md
autonomous: true
requirements: [A9]

must_haves:
  truths:
    - "ADR-027 exists, is Accepted, and supersedes ADR-026's video half only"
    - "It states in plain words that xAI becomes a NEW data-transfer counterparty for customer scene prompts, reversing the exact argument ADR-026 called decisive"
    - "It states that widening the duration grid destroys the STRUCTURAL guarantee that a legal reel cannot be built from generated clips alone, leaving only an economic one"
    - "ADR-026 is not edited — ADRs are immutable"
    - "ADR-024's image half is explicitly NOT superseded; only its transport moves"
  artifacts:
    - path: "docs/decisions/027-grok-imagine-video-succeeds-sora-2-on-openrouter.md"
      provides: "The superseding decision record"
      contains: "Supersedes"
      min_lines: 80
    - path: "docs/playbooks/media.md"
      provides: "A dated block pointing at ADR-027 and naming the grid consequence"
      contains: "ADR-027"
  key_links:
    - from: "docs/decisions/027-grok-imagine-video-succeeds-sora-2-on-openrouter.md"
      to: "docs/decisions/026-veo-31-lite-succeeds-sora-2.md"
      via: "a Supersedes line, one direction only"
      pattern: "026-veo-31-lite-succeeds-sora-2"
    - from: "packages/cost/src/media.fixtures.json succession.decisionRecord"
      to: "ADR-027"
      via: "the path string, updated by plan 33.1-04"
      pattern: "027-"
---

<objective>
Write ADR-027, superseding ADR-026's video half, and record the two costs of this reversal in the
words the owner accepted them in.

Purpose: ADR-026 (Accepted 2026-08-27) chose `veo-3.1-lite` and its **decisive** argument was that
Veo *"introduces NO new data-transfer counterparty"*. The owner was shown that argument explicitly
and chose Grok anyway. An ADR that reverses a decision without naming what the reversal costs is a
worse record than no ADR at all — and the same standard applies to the second cost, which the PRD
did not name and this plan does: **the 1-15 grid removes a structural guarantee the reel economics
were resting on.**

This lands in wave 1, beside the music fix, because it is pure record and blocks nothing. It is also
the citation `packages/cost/src/media.fixtures.json`'s `succession.decisionRecord` will point at
from plan 33.1-04 onward, so it must exist before the fixture moves.

Output: `docs/decisions/027-*.md`; a `Last verified` block on `docs/playbooks/media.md`.

Fully offline. No live stack, no spend.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33.1-media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix/33.1-PRD.md
@docs/decisions/026-veo-31-lite-succeeds-sora-2.md
@docs/README.md

<interfaces>
<!-- The facts the ADR must be built from. All verified; none inferred. Do not go re-derive them. -->

**The comparison the owner decided on** (PRD §L1, rates read 2026-08-30):

| | `x-ai/grok-imagine-video` | `veo-3.1-lite` (ADR-026) | `sora-2` (today) |
|---|---|---|---|
| Durations | **1-15, any integer** | 4, 6, 8 | 4, 8, 12 |
| 720p | $0.07/s | $0.05/s w/audio, $0.03 w/o | $0.10/s |
| 480p | $0.05/s | not offered | not offered |
| 9:16 | yes | yes | yes |
| Native audio | none | yes | — |

**Cost 1 — the counterparty.** ADR-026 §"1. It introduces NO new data-transfer counterparty — the
decisive point" argues that every other shortlist candidate means a new vendor relationship and a
fresh decision about where customer prompts go, and that Veo needed neither because ADR-016 had
already admitted Google in principle on 2026-08-07. Grok reverses precisely that. xAI becomes a new
recipient of customer scene prompts. OpenRouter is NOT a new counterparty — the text plane has run
on it since `845f4b1` — but xAI is, and routing through OpenRouter does not make the prompt stop
reaching xAI.

**Cost 2 — the structural guarantee that dies with the grid. Not in the PRD; derived during
planning and confirmed against the code.** `packages/cost/src/media.test.ts` currently asserts *"NOT
ONE target duration is reachable with generated video alone"*, and `media.fixtures.json` records
`targetsUnreachableByGeneratedVideoAlone: [15, 30, 60]`. The reason is arithmetic: every member of
sora-2's `4, 8, 12` grid is a multiple of four, so 15 and 30 cannot be composed at all, and 60 is
composable but costs `60 x $0.10 = $6.00`, over `MEDIA_JOB_CAP_USD` of `$3.50`.

Under a `1..15` grid, every integer target is composable. At `$0.07/s`:
- 15 s all-generated = **$1.05** — under the cap. Reachable.
- 30 s all-generated = **$2.10** — under the cap. Reachable.
- 60 s all-generated = **$4.20** — over the cap. Still unreachable.

So `targetsUnreachableByGeneratedVideoAlone` collapses from `[15, 30, 60]` to `[60]`.
`GENERATED_CLIP_SECONDS`'s own doc comment (`packages/core/src/storyboard.ts:538-545`) calls the
multiple-of-four property *"the real reason the other three kinds exist"*, and
`media-director.md` teaches *"Every legal reel therefore mixes kinds"*. Both become false for 15 s
and 30 s. The kind-mixing discipline stops being STRUCTURAL and becomes purely ECONOMIC — a 30 s
all-generated reel is now merely 7x more expensive than the mixed one, not impossible. The only
remaining defence is the skill body's "THE ORDER TO REACH IN" prose, and `dispatch.ts` already
records what a prose mandate on this skill family is worth: *"violated twice in six attempts, which
is why this is code and not another sentence in the body."*

**Rejections, both on rules rather than taste:**
- `bytedance/seedance-*` — cheapest published per-second rate on OpenRouter's collections page
  ($0.01345/s) and unusable: the live SKU is `{"video_tokens": "0.0000035"}`, billed per video token
  and configuration-dependent, so it is not pre-computable before a request exists.
  `packages/cost/src/media.ts` rule 1 refuses it by construction. This session and ADR-026 reached
  that conclusion independently, which is itself worth recording.
- `x-ai/grok-imagine-image-2.0` — `n: {min: 1, max: 1}`, no batch, and $0.04/image against
  gpt-image-2's ~$0.01. Batch is an explicit owner requirement; a single-vendor story does not
  outrank it. **This is why the image half stays on `gpt-image-2`** and ADR-024's image half is
  untouched.

**What does NOT move:** `MEDIA_JOB_CAP_USD` stays `3.50` (Grok is cheaper than the Sora it
replaces, so ADR-016's never-landed raise to `7.50` stays unlanded). `RUNWAY_DAYS` stays `14` — it
measures a real deadline. `wan2.5-*` rows stay priceable, historical-only, and are not a fallback.

**House style, from `docs/README.md` and ADR-026:** immutable once accepted; supersede, never edit.
Explicit `Supersedes` / `Does NOT supersede` lines at the top. A `Consequences` section stating what
the ADR does NOT itself perform. A `Provenance of this decision` section naming who decided and how
to reverse it. ADR-026's own structure is the template — match it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-027, with both costs stated rather than stepped around</name>
  <files>docs/decisions/027-grok-imagine-video-succeeds-sora-2-on-openrouter.md</files>
  <action>
Write the ADR. Structure it on ADR-026 (read it first — it is in context). Required content, in the
order it should carry the reader:

1. **Status**: Accepted — 2026-08-30.

2. **Supersedes**: ADR-026 **in its video half only** — the `veo-3.1-lite` pin and the Google
   endpoint that was never built. **Does NOT supersede**: ADR-024's image half (`gpt-image-2` and
   the stills decision stand; only the TRANSPORT moves to OpenRouter, and a careless read that
   retires ADR-024 entirely would needlessly reopen a settled stills decision — ADR-026 makes
   exactly this warning and it is still true). Also untouched: ADR-011's price-table-in-code
   consequence, ADR-012's media-authority boundary, ADR-013, ADR-019's scene timeline, and the
   whole-job reservation model.

3. **Context**: `/v1/videos` is an ENDPOINT withdrawal on 2026-09-24, not a model deprecation, so
   there is no same-vendor row to move to. ADR-026 recorded the decision and none of the wiring;
   `succession.replacementWiredUp` is still `false`, which is why the runway tripwire is still
   armed and goes red on 2026-09-10. Add the second, independent driver ADR-026 could not have
   known: the OpenAI account is out of credit (`credit_balance_exhausted`, OpenAI's own error code,
   on the two most recent `mediaJobs` rows), so no media has generated since ~2026-08-17. The text
   plane moved to OpenRouter at `845f4b1`; the media plane never did.

4. **Decision**: `x-ai/grok-imagine-video` via OpenRouter, and `openai/gpt-image-2` via OpenRouter.
   One transport, one funded key.

5. **Why the grid is the reason** — the table above. `1-15 any integer` **retires the
   `illegal_generated_duration` failure class outright**; Veo's `4/6/8` merely narrows it, since 5 s
   and 7 s scenes would still refuse. That failure class is one of the two defects the owner hit
   live on 2026-08-30. Grok has no native audio, which SUITS this pipeline: TTS narration is
   generated separately and muxed in `assemble_final.sh`, so Veo's audio was a feature we would pay
   for and discard.

6. **The counterparty cost, in a section of its own with a heading that names it.** Do not soften
   it and do not bury it in a consequences bullet. State: ADR-026's decisive argument was that Veo
   added no new data-transfer counterparty; this ADR reverses that argument, not merely its
   conclusion. xAI becomes a new recipient of customer scene prompts. Routing through OpenRouter
   does not change that — OpenRouter is a transport we had already approved for text, xAI is a
   company we had not sent anything to. The owner was shown this argument explicitly and chose
   Grok, in exchange for the duration grid. **Say that the owner was shown it**, because the value
   of the record is that a later reader can tell an accepted cost from an overlooked one.

7. **The structural guarantee this ADR spends, in a section of its own.** Use the arithmetic in
   the context block above — 15 s at $1.05 and 30 s at $2.10, both under the $3.50 cap, both
   previously impossible. Name the three places that encode the dying claim
   (`GENERATED_CLIP_SECONDS`'s doc comment, `media.test.ts`'s "NOT ONE target duration is
   reachable" test, `media-director.md`'s "Every legal reel therefore mixes kinds") and state that
   plans 33.1-04 and 33.1-06 update them. State plainly that kind-mixing drops from a structural
   guarantee to an economic preference, that the remaining defence is prose in a skill body, and
   that `dispatch.ts` already records what prose is worth on this skill family. **This is a real
   cost of the decision and the ADR is the only place it will be written down.** If a later reader
   wants it back, the lever is a code-side ceiling on generated-clip share, not another sentence in
   the body — say so, so the reversal starts from a known move.

8. **Rejections on the rule, not on taste**: Seedance and `grok-imagine-image-2.0`, with the
   reasons from the context block. Include the numbers.

9. **Consequences — what this ADR does NOT itself perform**: the submit/poll/download adapter
   against `https://openrouter.ai/api/v1/videos`, the price rows, the `MEDIA_DEFAULT_VIDEO` repin,
   the `GENERATED_CLIP_SECONDS` widening, the `succession.replacementWiredUp` flip (which may only
   be set in the same commit as a landed submit path AND price row — a decision is not a migration,
   and this ADR going in ahead of the wiring is exactly the situation the re-keyed tripwire exists
   to keep visible), and the `media-director` body edit. `MEDIA_JOB_CAP_USD` stays 3.50; ADR-016's
   raise to 7.50 stays unlanded. `wan2.5-*` and the bare `gpt-image-2`/`sora-2` rows stay priceable
   as history and are not fallbacks.

10. **Provenance and reversal**: owner decision of 2026-08-30, taken with the counterparty argument
    on the table. Reversal is a price row plus a pin plus a submit adapter; the rejected candidates
    are recorded above WITH their numbers so a reversal starts from a priced comparison. If the
    data-transfer judgement changes, supersede — do not edit.

**Do not touch `docs/decisions/026-*.md`.** CLAUDE.md §9: ADRs are immutable after acceptance. The
`Supersedes` pointer lives in 027 and points backwards; there is no forward pointer to add.

Commit: `docs(33.1-02): ADR-027 — grok-imagine-video on OpenRouter, and the two costs it spends`.
  </action>
  <verify>
    <automated>test -f docs/decisions/027-grok-imagine-video-succeeds-sora-2-on-openrouter.md &amp;&amp; grep -qi "supersedes" docs/decisions/027-*.md &amp;&amp; grep -q "026-veo-31-lite-succeeds-sora-2" docs/decisions/027-*.md &amp;&amp; grep -qi "counterparty" docs/decisions/027-*.md &amp;&amp; grep -q "1.05" docs/decisions/027-*.md &amp;&amp; grep -q "2.10" docs/decisions/027-*.md &amp;&amp; git diff --quiet HEAD -- docs/decisions/026-veo-31-lite-succeeds-sora-2.md; echo "ADR027_EXIT=$?"</automated>
  </verify>
  <done>ADR-027 exists; it names ADR-026 as superseded in the video half only; it contains a
  dedicated counterparty section and a dedicated structural-guarantee section carrying the $1.05 and
  $2.10 figures; ADR-026's bytes are unchanged (`git diff --quiet` on it exits 0).</done>
</task>

<task type="auto">
  <name>Task 2: The playbook block that makes the ADR findable from the code</name>
  <files>docs/playbooks/media.md</files>
  <action>
Add a new `> Last verified: 2026-08-30 (...)` block at the TOP of `docs/playbooks/media.md`, above
the current 2026-08-27 ADR-026 block. It must:

- Say **ADR-026 IS SUPERSEDED** and name ADR-027, so the very confident 2026-08-27 block below —
  which reads as current and argues at length for Veo — cannot be mistaken for the live decision.
  That block stays (the file is append-at-top history), so this one has to be unambiguous.
- Restate the counterparty reversal in one sentence.
- Carry the grid consequence with its arithmetic: `targetsUnreachableByGeneratedVideoAlone` goes
  `[15, 30, 60]` -> `[60]`; a 30 s all-generated reel becomes possible at $2.10; kind-mixing drops
  from structural to economic.
- State that nothing is wired yet and that `succession.replacementWiredUp` is still `false` — the
  same honesty the 2026-08-27 block showed, and the reason the tripwire is still armed.
- Note that the runway tripwire's `PINNED_MODELS` keying is itself unsafe under a repin (plan
  33.1-04 re-keys it) — recorded here so the re-key is not later mistaken for a convenience.

CRLF gotcha: `docs/playbooks/*.md` in this repo are CRLF. A multi-line `\n` anchor silently no-ops.
Anchor on one unique line, or normalise first and assert the anchor is unique before writing.

Commit: `docs(33.1-02): media playbook — ADR-026 is superseded, and the grid spends a guarantee`.
  </action>
  <verify>
    <automated>head -40 docs/playbooks/media.md | grep -q "ADR-027" &amp;&amp; head -40 docs/playbooks/media.md | grep -qi "2026-08-30" &amp;&amp; cd packages/cost &amp;&amp; npx vitest run 2>&amp;1 | tail -8</automated>
  </verify>
  <done>`media.md`'s top block is dated 2026-08-30, names ADR-027, marks ADR-026 superseded, and
  carries the grid arithmetic. The `packages/cost` suite is still green — `media.test.ts` asserts
  that `## Reconciliation` remains a runnable heading in this file, and an edit at the top has broken
  that split before.</done>
</task>

</tasks>

<verification>
- `grep -c "Last verified" docs/playbooks/media.md` increased by exactly 1.
- `git diff --quiet HEAD~2 -- docs/decisions/026-veo-31-lite-succeeds-sora-2.md` — ADR-026 untouched.
- `cd packages/cost && npx vitest run` — green (the playbook is read by a test in this package).
</verification>

<success_criteria>
- **A9** — ADR-027 exists and states the counterparty cost in plain words, in a section of its own,
  naming that the owner was shown ADR-026's decisive argument and chose Grok anyway.
- The second cost — the structural guarantee the grid spends — is stated with its arithmetic. This
  is not in the PRD; it was derived during planning and the ADR is where it becomes permanent.
- ADR-026 is not edited.
- `docs/playbooks/media.md` carries a bumped `Last verified` pointing at ADR-027.
</success_criteria>

<output>
After completion, create `{phase_dir}/33.1-02-SUMMARY.md`.
</output>
