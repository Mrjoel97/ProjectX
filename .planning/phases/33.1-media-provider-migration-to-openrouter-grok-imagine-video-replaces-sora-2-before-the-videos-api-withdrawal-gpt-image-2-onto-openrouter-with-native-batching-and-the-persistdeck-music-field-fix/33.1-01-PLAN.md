---
phase: 33.1-media-provider-migration-to-openrouter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/plans.ts
  - packages/backend/convex/dispatch.test.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [A1, A2]

must_haves:
  truths:
    - "A storyboard body carrying a `Music:` line persists as `kind: \"media\"` instead of throwing at the write boundary"
    - "The persisted plan row carries the parsed music slug, not just an absent field"
    - "A media dispatch with music writes a `media.deck_persisted` audit terminal"
    - "The SCENE deck path (dispatch.ts:886) — the one the owner's live runs used — is exercised with art direction for the first time"
  artifacts:
    - path: "packages/backend/convex/plans.ts"
      provides: "persistDeck's artDirection validator, widened by one optional field"
      contains: "music: v.optional(v.string())"
    - path: "packages/backend/convex/dispatch.test.ts"
      provides: "the first Music fixtures at the parser->mutation seam"
      contains: "Music"
  key_links:
    - from: "packages/core/src/storyboard.ts parseArtDirection"
      to: "packages/backend/convex/plans.ts persistDeck"
      via: "dispatch.ts:886 (scene) and dispatch.ts:1083 (block) runMutation"
      pattern: "artDirection"
    - from: "persistDeck"
      to: "plans.artDirection.music (schema.ts:755)"
      via: "ctx.db.patch"
      pattern: "artDirection"
---

<objective>
Widen `plans.persistDeck`'s `artDirection` arg validator by the one optional field its own parser
emits and its own schema already stores, and close the test hole that let it ship.

Purpose: **the owner has a broken product right now.** Every storyboard carrying a `Music:` line is
rejected at the write boundary — Convex `v.object` is closed — and `runMedia` throws *after*
`dispatchAndLand` has already landed the memo, so the plan sits at `kind: "memo"` carrying the
model's raw prose. Two of three media dispatches on 2026-08-30 produced `subagent.completed` and
**no `media.deck_*` terminal at all**. This is roughly one line plus the tests that should have
caught it.

Output: `music` accepted by `persistDeck`; `dispatch.test.ts` carries `Music:` fixtures on BOTH deck
paths for the first time; `cockpit.md` records the seam and why it was blind.

**This plan is deliberately alone in wave 1.** It is not held hostage to the provider migration
behind it and must not be merged into it.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33.1-media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix/33.1-PRD.md
@.planning/phases/33.1-media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix/33.1-VALIDATION.md
@packages/backend/convex/plans.ts
@packages/backend/convex/dispatch.test.ts

<interfaces>
<!-- Everything the executor needs. Do NOT go exploring for these. -->

`packages/core/src/storyboard.ts:286-335` — the field, and the closed set that contains it:

```ts
export const MUSIC_MOODS = ["calm", "warm", "upbeat", "cinematic"] as const;
export type MusicMood = (typeof MUSIC_MOODS)[number];

export type ArtDirection = {
  palette: string[]; mood: string; lighting: string; composition: string;
  environment: string; texture: string; typography?: string;
  references: string[]; avoid: string;
  music?: MusicMood;          // <- line 335. Emitted since e2b281f.
};
```

`packages/core/src/storyboard.ts:375,398` — how it is parsed and spread:

```ts
const music = parseMusicMood(fieldOf(section, "Music"));
// ...
...(music === undefined ? {} : { music }),
```

The wire label is `Music`, so a body line reads `- **Music** — upbeat`.
`storyboard.test.ts` already has 14 such fixtures; `dispatch.test.ts` has zero.

`packages/backend/convex/schema.ts:749-755` — the destination, which ALREADY accepts it:

```ts
/** The BED's mood — a `MUSIC_MOODS` slug, or absent for a reel with no music. Stored as a
 *  string, not a union: the closed set is enforced by the PARSER. */
music: v.optional(v.string()),
```

`packages/backend/convex/plans.ts:367-380` — the defect. The validator lists nine fields and
`typography`; `music` is simply absent, and `v.object` is closed:

```ts
artDirection: v.union(
  v.null(),
  v.object({
    palette: v.array(v.string()), mood: v.string(), lighting: v.string(),
    composition: v.string(), environment: v.string(), texture: v.string(),
    typography: v.optional(v.string()), references: v.array(v.string()),
    avoid: v.string(),
    // <- `music: v.optional(v.string())` goes here.
  }),
),
```

`packages/backend/convex/dispatch.ts` — the TWO call sites that pass the parsed object straight
through, so no caller change is needed:

```
886  await ctx.runMutation(internal.plans.persistDeck, { ... artDirection, ... })   // SCENE path
1083 await ctx.runMutation(internal.plans.persistDeck, { ... artDirection, ... })   // BLOCK path
```

Audit terminals, for the A2 assertion (`dispatch.ts:916` scene, `:1103` block):
`eventType: "media.deck_persisted"` — the refusal arms are `"media.deck_refused"` at `:869`,
`:985`, `:1064`. A throw between `subagent.completed` and either terminal writes NEITHER, which is
the live signature the PRD recorded.

Existing fixtures in `dispatch.test.ts`:
- `MEDIA_BODY` (line 2022) — has `## 2. ART DIRECTION` and a **BLOCK DECK**. Reaches call site 1083.
- `TWO_UP_BODY` (line 2806, built from `VAR_BRIEF` + `VAR_A_DECK` + `VAR_B_DECK`) — a **SCENE DECK**
  two-variation body with **NO art-direction section at all**. Reaches call site 886.

**That second gap is the real one.** The scene/variations path is what production runs and what the
owner hit on 2026-08-30, and it has never been exercised with a non-null `artDirection`, let alone
one carrying music.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The RED — a Music line on BOTH deck paths, observed failing with the field present</name>
  <files>packages/backend/convex/dispatch.test.ts</files>
  <behavior>
    - Test 1 (SCENE path, the production one): `TWO_UP_BODY` gains a `## 2. ART DIRECTION` section
      carrying the full nine fields **plus** `- **Music** — upbeat`. The existing two-variation
      terminal test asserts `plan?.artDirection?.music === "upbeat"` AND
      `plan?.artDirection?.palette` is the parsed palette (so the section is proven to have been
      read, not merely tolerated).
    - Test 2 (BLOCK path): `MEDIA_BODY`'s art-direction section gains `- **Music** — cinematic`, and
      the existing "the deck lands on the plan row" test (line ~2085) asserts
      `plan?.artDirection?.music === "cinematic"`.
    - Test 3 (A2, the terminal): a media dispatch whose body carries music writes an audit row with
      `eventType === "media.deck_persisted"`, and writes NO `media.deck_refused`. Assert on the
      audit table, not on the plan row — the PRD's live signature was the ABSENCE of a terminal,
      so absence is what must be excluded.
    - Test 4 (the field is what fails, not the shape): a direct
      `t.mutation(internal.plans.persistDeck, {... artDirection: {<nine fields>, music: "upbeat"}})`
      succeeds, and the persisted row reads back `music: "upbeat"`.
  </behavior>
  <action>
Add the fixtures and assertions above. Do NOT touch `plans.ts` in this task.

**The falsifiability requirement, from VALIDATION.md trap 1: the RED must be observed with the
field present.** A `persistDeck` test that never passes `music` is green before and after the fix.
Run the suite and RECORD, in the commit message, the exact error text and how many tests failed —
it must be `ArgumentValidationError` / `Validator error: Expected one of null, object, got
{...,"music":...}`, and the failure must name `music`. If the suite goes red for any other reason,
the fixture is wrong, not the code.

Two supporting facts so this is not a scavenger hunt:
- `MUSIC_MOODS` is `["calm","warm","upbeat","cinematic"]`. A slug outside it parses to `undefined`
  and the test would be vacuous — use only members of that set.
- `VAR_BRIEF` ends with a blank line and is joined ahead of `## VARIATION A`; `parseArtDirection`
  reads `## 2. ART DIRECTION` from the WHOLE body (`dispatch.ts:885` passes `body`), so the
  section may sit between `VAR_BRIEF` and `## VARIATION A`. Copy the nine-field block from
  `MEDIA_BODY` lines 2027-2036 rather than inventing one.

Commit the RED: `test(33.1-01): the Music line at the parser->mutation seam, on both deck paths`.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/dispatch.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <done>The suite is RED. At least the two deck-path tests and the direct-mutation test fail, every
  failure message contains `music`, and the failure count is recorded. No production file changed.</done>
</task>

<task type="auto">
  <name>Task 2: The fix — one optional field, and the comment that says why it was missed</name>
  <files>packages/backend/convex/plans.ts</files>
  <action>
Add `music: v.optional(v.string())` to `persistDeck`'s `artDirection` object validator, immediately
after `typography` (which is the other optional field, so they read together).

**Mirror the schema, not the core type.** `schema.ts:755` stores `v.optional(v.string())`, not a
union of the four slugs, and its comment states the reason: the closed set is enforced by the
PARSER, not by the write boundary. A `v.union(v.literal(...))` here would be a second, drifting
copy of `MUSIC_MOODS` in a package that cannot import it — the exact drift `isBuyableClipLength`'s
comment in `media.ts` was written about. Match `schema.ts` exactly.

Add a short comment above the field naming the failure class, not just the field — something to the
effect that this validator is a hand-maintained mirror of `plans.artDirection` in `schema.ts` and
that `e2b281f` widened the parser, the schema, the price table, the renderer and the skill body
without widening this, so any future art-direction field lands here in the same commit.

ponytail: this is rung 6 — one line. Do not build a shared validator-from-schema helper; Convex
gives no way to derive one, and inventing an abstraction here is scope this has not earned.

Commit: `fix(33.1-01): persistDeck accepts the music field its own parser emits`.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/dispatch.test.ts convex/plans.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Every test from Task 1 is green. `music: v.optional(v.string())` is present in `plans.ts`
  and matches `schema.ts:755` character for character. No other validator field changed.</done>
</task>

<task type="auto">
  <name>Task 3: The playbook entry, and a full-gate pass</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
`docs/playbooks/watch.json` maps `packages/backend/convex/plans.ts` and
`packages/backend/convex/dispatch.test.ts` to **`cockpit.md`** (not `media.md`). Per CLAUDE.md §9 the
turn cannot finish without it, and the Stop hook enforces this.

Add a new `> Last verified: 2026-08-30 (...)` block at the TOP of `docs/playbooks/cockpit.md`,
above the current top block, in the house style (the file has many; copy the shape). It must record:

- **The defect**: `persistDeck`'s arg validator is a hand-maintained mirror of `plans.artDirection`
  in `schema.ts`, and `v.object` is closed, so a field the parser emits and the schema stores is a
  THROW at the write boundary — after `dispatchAndLand` already landed the memo, which is why the
  plan row survives at `kind: "memo"` carrying raw prose and nothing looks like a crash.
- **The signature to recognise it by**: `subagent.completed` with NO `media.deck_persisted` and NO
  `media.deck_refused`. Neither terminal is written when the mutation throws between them.
- **Why the tests were green**: the parser had 14 `Music` fixtures in `storyboard.test.ts`; the SEAM
  it writes across had zero in `dispatch.test.ts`, and the SCENE/variations path — the one
  production runs — had never been exercised with a non-null `artDirection` at all. Coverage of a
  parser is not coverage of the boundary it writes across.
- **The standing rule**: a new `artDirection` field lands in `schema.ts` AND `plans.ts` in the same
  commit, with a `dispatch.test.ts` fixture carrying it.

Note the CRLF gotcha: `.planning/*.md` and `docs/playbooks/*.md` in this repo are CRLF. Normalise or
anchor on a single unique line when editing; a multi-line `\n` anchor silently no-ops.

Then run the wave gate.

Commit: `docs(33.1-01): cockpit playbook — the closed-validator seam and the terminal that never wrote`.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run 2>&amp;1 | tail -15; cd ../../packages/backend &amp;&amp; npx tsc --noEmit; echo "TSC_EXIT=$?"</automated>
  </verify>
  <done>`cockpit.md` carries a new dated block naming the defect, the audit signature, the coverage
  reason and the standing rule. Backend suite failure count is at or below the pre-plan baseline
  (read the FAILURE COUNT, not the exit code — backend vitest exiting 1 with thousands passing is a
  Windows-only artifact). `tsc --noEmit` is clean.</done>
</task>

</tasks>

<verification>
- `cd packages/backend && npx vitest run convex/dispatch.test.ts` — green, and the new assertions
  name `music` explicitly.
- Mutation check (cheap, mandatory): revert the one line in `plans.ts`, re-run, confirm RED with a
  `music` in the message, restore. A guard that cannot be made to fail is not a guard.
- `git diff --stat HEAD -- "*.ts"` after the final commit — `plans.ts` and `dispatch.test.ts` must
  BOTH appear. A file the plan names but the diff never touches is a coverage hole with a green
  suite over it.
</verification>

<success_criteria>
- **A1** — a storyboard body carrying `- **Music** — upbeat` persists as `kind: "media"`, and the
  plan row reads back `artDirection.music === "upbeat"`, on the SCENE path and the BLOCK path.
- **A2** — the same run writes `media.deck_persisted` and writes no `media.deck_refused`.
- The RED was observed with the field present, and the error named `music`.
- `docs/playbooks/cockpit.md` updated with a bumped `Last verified`.
</success_criteria>

<output>
After completion, create
`.planning/phases/33.1-media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix/33.1-01-SUMMARY.md`.

Record the exact RED error text, the failure count before and after, and confirm the mutation check
was run.
</output>
