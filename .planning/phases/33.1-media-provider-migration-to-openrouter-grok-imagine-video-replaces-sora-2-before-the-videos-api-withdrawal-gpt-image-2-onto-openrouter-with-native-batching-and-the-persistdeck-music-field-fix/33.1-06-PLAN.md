---
phase: 33.1-media-provider-migration-to-openrouter
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - packages/contracts/skills/media-director.md
  - packages/contracts/src/skills/mediaDirector.ts
  - packages/core/src/storyboard.test.ts
  - docs/playbooks/skill-registry.md
  - docs/playbooks/media.md
autonomous: false
requirements: [A3, A6]

user_setup:
  - service: local-stack
    why: "A3 and A6 are live localhost runs and cannot happen in this worktree — `CONVEX_DEPLOYMENT=local:...` is cwd-relative, so `pikar-lane-34` has no `.convex` and would come up empty against nothing."
    dashboard_config:
      - task: "Merge feat/34-media-openrouter into the main tree, owner-timed"
        location: "C:/Users/expert/Desktop/Pikar-Ai — where the BILLING lane is actively writing. Do not switch that tree's branch on your own initiative."
      - task: "Authorise the live spend (~$1-2: one still, one short reel with a non-multiple-of-4 generated clip, TTS, captions, render)"
        location: "openrouter.ai -> Credits"

must_haves:
  truths:
    - "The media-director body teaches the grid the code actually enforces, and its worked answer contains a generated scene at a length the old grid forbade"
    - "The body's worked answer still round-trips through the real parsers"
    - "The .md and the derived .ts are byte-identical, and the seeded row is the version the read-back reports"
    - "One image generates through OpenRouter and appears on the canvas"
    - "One reel renders end to end containing a generated clip whose length is not a multiple of four"
  artifacts:
    - path: "packages/contracts/skills/media-director.md"
      provides: "The corrected grid rule and mixed-kinds claim"
      contains: "generated_video"
    - path: "packages/contracts/src/skills/mediaDirector.ts"
      provides: "The derived body that actually ships — the Convex runtime cannot fs.read repo files"
      contains: "mediaDirectorSkillBody"
  key_links:
    - from: "packages/contracts/skills/media-director.md"
      to: "packages/contracts/src/skills/mediaDirector.ts"
      via: "a hand regeneration held byte-identical by skillBodies.test.ts"
      pattern: "mediaDirectorSkillBody"
    - from: "packages/contracts/src/skills/mediaDirector.ts"
      to: "the live skills row"
      via: "seedSkills, publishing at maxVersion+1 straight to active"
      pattern: "seedSkills"
---

<objective>
Correct the media-director body so it teaches the grid the code now enforces, merge the lane into
the main tree, and watch one image and one reel come out the other end.

Purpose: everything before this plan is offline. **The phase has not delivered anything to the owner
until an image lands on the canvas and a reel renders** — and this repo's own record is that the
last two media phases each closed on production defects no offline gate could find
(`33-10-SUMMARY.md`: "Closing it took two production defects no offline gate could find").

Output: the corrected skill body, seeded and read back; A3 and A6 observed in a browser.

**Not runnable in this worktree at all.** Every task after Task 1 happens in
`C:/Users/expert/Desktop/Pikar-Ai`, after a merge the owner times.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33.1-media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix/33.1-PRD.md
@.planning/phases/33.1-media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix/33.1-VALIDATION.md
@docs/decisions/027-grok-imagine-video-succeeds-sora-2-on-openrouter.md
@packages/contracts/skills/media-director.md

<interfaces>
<!-- The three sentences to change, and the four gates that stand between them and production. -->

**`packages/contracts/skills/media-director.md` — the three places the old grid is taught:**

- **line 172-176**, the rule and the argument built on it:
  *"A `generated_video` scene may only be 4, 8 or 12 seconds. That is the generator's grid and there
  is nothing in between. The other three kinds are any length, and that is not a detail: 4, 8 and 12
  are all multiples of four, so a reel built ONLY from generated clips cannot hit 15 or 30 seconds
  at all, and a 60-second one costs more than the whole job is allowed to. Every legal reel
  therefore mixes kinds."*
- **line 290**, the self-check: *"is every `generated_video` scene 4, 8 or 12 seconds"*.
- **line ~296**, the self-check: *"Does each deck MIX kinds, and did you reach past a
  `generated_video` for a free kind wherever the …"*.

The arithmetic argument in the first is now FALSE (ADR-027 records this): at `$0.07/s` on a `1..15`
grid, a 30-second all-generated reel costs `$2.10` and is under the `$3.50` cap. **The conclusion —
reach for the cheap kinds first — survives; only its justification changes, from impossible to
seven times more expensive.** Rewrite it as a cost argument with the numbers, and keep the
"THE ORDER TO REACH IN" section (line 178+) exactly as it is: that section is now the only defence
left, and ADR-027 says so.

**media-director is DELIBERATELY UNGATED, and CONTEXT.md is wrong about this.** CONTEXT.md says it
is *"a gated skill row (§5) — a body change needs a recorded passing eval run"* and budgets ~$0.35.
It is not:

```ts
// packages/contracts/src/skills/skillBodies.test.ts:130
describe("media-director gating (20-03)", () => {
  test("is DELIBERATELY UNGATED — do not add it to GATED_SKILLS", () => {
    expect(isGatedSkill(MEDIA_DIRECTOR_SKILL)).toBe(false);
  });
});
```

The reason is recorded above that test: `run-eval-golden.mjs` derives its `--skill` list from
`GATED_SKILLS` and drives `runCockpitAgent` over TEXT fixtures, which structurally cannot exercise a
script/art-direction/storyboard turn — so gating it would strand it at v1 on its first body edit
with no runner able to clear the gate. **There is no EVAL_GATE to run and no ~$0.35 to spend.**

That is worse news, not better: `seedSkills` publishes at `maxVersion + 1` straight to ACTIVE, with
no eval between the prose and production. The four gates that DO exist are:

1. **The round trip** — `packages/core/src/storyboard.test.ts:316`, `describe("media-director.md
   round trip — the body's worked answer survives its own rules")`, which `readFileSync`s the `.md`,
   finds `"## A worked answer"`, runs `parseVariations` FIRST and then every per-deck rule against
   BOTH variations. This is the ONLY pre-live gate and 33-09 mutation-verified it three ways (the v2
   body reddens 17 of 99; dropping one `Source: unverified` line reddens 1; giving variation B a
   different `Target duration` reddens 15).
2. **`skillBodies.test.ts`** holds the `.md` and `packages/contracts/src/skills/mediaDirector.ts`
   byte-identical. The Convex runtime cannot `fs.read` repo files, so **the derived `.ts` is what
   ships**. There is no committed generator: regenerate with a throwaway `JSON.stringify` of the
   `.md`, then `biome check --write` (which prefers double quotes).
3. **The live seed and read-back.** Optimizer dry-run candidates occupy version numbers, so the
   version this lands at is NOT predictable from the plan — `seedSkills` publishes at
   `maxVersion + 1`. Read back which version is active and record it; phase 10 shipped at `@14`
   when the plan said `@13`.
4. **The owner in a browser.**

**Repo memory that applies directly here:** a skill-body edit shifted the model's tool arguments —
a new body section made it pass an optional enum it had never passed before, breaking an unrelated
fixture. A/B the prior version on one fixture (~$0.01) before theorising about any behaviour change.

**Live-run prerequisites** (from `docs/playbooks/media.md` and this repo's recorded gotchas):
Next `dev` OOMs on the workspace page — **prod-build and `next start`**. `convex-run` kills the
session. There is one loggable human account (the owner, via Google). The onboarding gate must be
satisfied before the cockpit is reachable. `CONVEX_DEPLOYMENT=local:...` selects a **cwd-relative**
directory: the main tree has the seeded `.convex`, this worktree has none.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The body teaches the grid the code enforces, and the worked answer proves it</name>
  <files>packages/contracts/skills/media-director.md, packages/contracts/src/skills/mediaDirector.ts, packages/core/src/storyboard.test.ts, docs/playbooks/skill-registry.md</files>
  <behavior>
    - The round-trip test still passes: the worked answer's two variations parse, both declare the
      brief's duration, and every `Seconds` column sums exactly.
    - **The worked answer contains at least one `generated_video` scene at a length the old grid
      forbade** (e.g. 7 s), and the round trip proves it parses. A body edit that only changed prose
      would be green whether or not the grid moved — this is what makes the change falsifiable.
    - `skillBodies.test.ts` holds the `.md` and the regenerated `.ts` byte-identical.
    - `isGatedSkill(MEDIA_DIRECTOR_SKILL)` is still `false`. Do not "tidy up the gate list".
  </behavior>
  <action>
1. **Rewrite lines 172-176.** The rule becomes: a `generated_video` scene may be any whole number of
   seconds from 1 to 15. Replace the impossibility argument with the COST argument, with the
   numbers: a generated second costs about `$0.07`, a still costs the same at 12 s as at 2 s, so a
   30-second reel built only from generated clips is roughly seven times the picture cost of a
   mixed one — legal now, and still the wrong deck. Delete the sentence *"Every legal reel therefore
   mixes kinds"*; it is false. Replace it with a sentence saying mixing is now a COST discipline
   rather than an arithmetic necessity, and that the order below is the discipline.

   Leave the **"THE ORDER TO REACH IN"** section untouched. ADR-027 records that it is now the only
   remaining defence, and weakening it in the same edit that removes the structural one would be the
   worst possible combination.

2. **Line 290 self-check** — "is every `generated_video` scene between 1 and 15 seconds". **Line
   ~296 self-check** — keep "Does each deck MIX kinds", it is still the instruction; it is only its
   justification that moved.

3. **The worked answer.** Change at least one `generated_video` scene to a non-multiple-of-4 length
   and rebalance its variation so the `Seconds` column still sums to the declared target EXACTLY —
   the round-trip test checks this and 33-09 measured that a wrong target reddens 15 assertions.
   This is the load-bearing part of the edit: it is the difference between telling the model the
   grid moved and SHOWING it.

   ponytail: the smallest edit that changes the teaching. Do not restructure the body, do not add a
   section, do not "improve" the examples — repo memory is explicit that a new body section made the
   model start passing an optional enum it had never passed before and broke an unrelated fixture.

4. **Regenerate `packages/contracts/src/skills/mediaDirector.ts`** in the SAME commit: throwaway
   `JSON.stringify` of the `.md`, then `biome check --write`. There is no committed generator. If
   `skillBodies.test.ts` is red, the regeneration is stale, not the test.

5. **Mutation-check the round trip** the way 33-09 did, and record the numbers: revert the worked
   answer's changed scene to its old 4/8/12 length WITHOUT rebalancing -> the round trip must redden
   on the sum. Restore. If it does not redden, the worked answer is not actually being parsed and
   the whole gate is decorative.

6. **`docs/playbooks/skill-registry.md`** (`watch.json` maps `packages/contracts/skills/` and
   `packages/contracts/src/skills/` to it): a `> Last verified: 2026-08-30 (...)` block recording
   the edit, that media-director is UNGATED and why (the golden runner cannot drive a storyboard
   turn), that the round trip is therefore the only pre-live gate, the mutation numbers, and that
   the body is AUTHORED AND NOT LIVE until Task 3's seed.

**This task IS runnable in this worktree.** Everything below it is not.
  </action>
  <verify>
    <automated>cd packages/core &amp;&amp; npx vitest run src/storyboard.test.ts 2>&amp;1 | tail -15; cd ../../packages/contracts &amp;&amp; npx vitest run 2>&amp;1 | tail -15</automated>
  </verify>
  <done>The round trip is green with a non-multiple-of-4 generated scene in the worked answer; the
  `.md` and `.ts` are byte-identical; `isGatedSkill` is still `false`; the reversion mutation
  reddened the round trip and the count is recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The merge into the main tree — owner-timed, because another lane is writing there</name>
  <what-built>Plans 33.1-01 through 33.1-06 Task 1, all offline and all green: the music fix, ADR-027,
  the image transport, the duration grid, the re-keyed tripwire, the video adapter, the succession
  flag and the skill body.</what-built>
  <how-to-verify>
**Nothing below can run in `pikar-lane-34`.** `CONVEX_DEPLOYMENT=local:...` is cwd-relative, so this
worktree has no `.convex` and a stack started here comes up EMPTY — and a gap-state gate that passes
against nothing reads exactly like a gate that passed.

**The main tree currently has an ACTIVE billing lane writing into it.** Lanes share one working tree
in this repo: foreign merges and commits land mid-session, `.git/MERGE_HEAD` may be present, and
`git add -A` is never safe. So this merge is yours to time, not the agent's.

1. Confirm the billing lane is at a resting point (no in-flight edit, no unresolved merge).
2. Merge `feat/34-media-openrouter` into whatever branch the main tree is on, or fast-forward it —
   your call. **Do not switch the main tree's branch to `feat/34-media-openrouter` unilaterally.**
3. Before running anything: re-check the seam. A dependency audit taken while another lane commits
   is stale within hours — specifically, confirm nothing on the billing branch has touched
   `packages/cost/src/media.ts`, `packages/backend/convex/media.ts` or `plans.ts` since this lane
   was cut.
4. From the merged main tree, run the wave gate and read the FAILURE COUNT, not the exit code:
   `turbo run test --concurrency=1` (concurrency 1 — vitest spawn-fails otherwise), then
   `npx tsc --noEmit` in BOTH `packages/backend` and `apps/web`, each read on its own line and never
   through a pipe.
5. Baseline first. `vaultDigest.test.ts` and other timeout-sized tests are load-flaky under a full
   suite, and a standing red elsewhere looks identical to a new one.

Reply `merged` plus the failure counts, or `hold` plus the reason.
  </how-to-verify>
  <files>(none — this task changes no file; it moves commits between trees)</files>
  <action>Wait for the owner. Do NOT merge, do NOT switch the main tree's branch, and do NOT run
  anything in `C:/Users/expert/Desktop/Pikar-Ai` before the owner replies `merged`. When they do,
  take a full-suite baseline in the merged tree and compare failure COUNTS, not exit codes.</action>
  <verify>
    <automated>echo "MANUAL: owner-timed merge; the automated gate is the post-merge full suite run in the MAIN tree — turbo run test --concurrency=1 plus tsc --noEmit in packages/backend and apps/web, each read on its own line"</automated>
  </verify>
  <done>The owner has replied `merged`, the post-merge full-suite failure count is at or below the
  pre-merge baseline, and both `tsc --noEmit` runs are clean.</done>
  <resume-signal>Type "merged" with the full-suite failure counts and both tsc results, or "hold".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Seed the body, read back the active version, and watch an image and a reel come out</name>
  <what-built>The whole migration, in the main tree, against the seeded local deployment. This
  checkpoint is A3 and A6 — the two criteria no offline gate in this phase can reach.</what-built>
  <how-to-verify>
**This spends real money (~$1-2) on a no-refunds rail. Authorised in the PRD; not automatic.**

**Step 1 — seed and read back (the agent runs this; you confirm the version).**
From `packages/backend` in the MAIN tree, run `seedSkills`. It publishes at `maxVersion + 1`
straight to ACTIVE — there is no eval gate on this row. **Then read back which version is active and
record it.** Optimizer dry-run candidates occupy version numbers, so the number is not predictable
from this plan; phase 10 shipped at `@14` where the plan said `@13`. A recorded version that was
never read back is a guess.

**Step 2 — A/B the body on one fixture before trusting it (~$0.01).**
Repo memory: a body edit once made the model start passing an optional enum it had never passed
before and broke an unrelated fixture. Run one media brief against the PREVIOUS active version and
one against the new one, and compare the tool arguments, not just the output. If the new body
changes something other than the durations it teaches, stop and report rather than theorising.

**Step 3 — A3, the image (~$0.01-0.04).**
1. Prod-build and `next start -p 3111` in the main tree. **Not `next dev`** — it OOMs on the
   workspace page.
2. Sign in as the owner (Google; it is the only loggable human account) and clear the onboarding
   gate if the tenant is fresh.
3. Ask the cockpit for a standalone image. Approve at the gate.
4. **Expected:** the image generates and appears on the canvas. Not "no error" — the picture is
   visible.
5. **If it fails:** report `mediaJobs.failureReason` and `providerReasonCode` verbatim. Two codes to
   recognise immediately — `credit_balance_exhausted` means the account, not the code; a
   missing-env throw means `OPENROUTER_API_KEY` did not reach the media path, which is Wave 0 item 1
   and would mean the first live call failed on plumbing rather than on anything interesting.

**Step 4 — A6, the reel (~$1).**
1. Ask the cockpit for a short reel and **steer the brief so at least one `generated_video` scene is
   NOT a multiple of four seconds** (a 15-second reel with a 7-second generated scene is the cheapest
   honest test). If the model refuses to write one, say so — that is a finding about the seeded body,
   and it is exactly what Step 1 and Step 2 exist to catch.
2. Approve. Watch the deck through to a rendered reel.
3. **Expected:** the reel renders end to end; the non-multiple-of-4 clip is present at its declared
   length; the storyboard carried a `Music:` line and was not discarded (which is plan 33.1-01
   observed live rather than in a test).
4. **Also confirm the negative that started this phase:** the run wrote a `media.deck_persisted`
   audit row. Two of three runs on 2026-08-30 wrote NO `media.deck_*` terminal at all, and that
   absence is the signature to check for, not the presence of an error.

**Step 5 — the perceptual question, which nothing here asserts.**
Watch the finished reel. Is Grok's output acceptable for something a customer sees? No test in this
phase answers that and none can. If the answer is no, that is a new decision (supersede ADR-027),
not a bug.

**Step 6 — the playbook.** Add a `> Last verified: 2026-08-30 (...)` block to
`docs/playbooks/media.md` recording the seeded version number, the A/B result, and A3/A6 as
OBSERVED with what was seen — the same standard as the 2026-08-16 "VERIFIED IN A BROWSER" entries.
Bump `Last verified`.

Reply `approved` with the seeded version number and what you saw, or describe the failure.
  </how-to-verify>
  <files>docs/playbooks/media.md</files>
  <action>Run steps 1 and 2 (seed, read back the active version, A/B one fixture) yourself in the
  MAIN tree, then hand steps 3-5 to the owner and wait. Record the read-back version number verbatim
  — never the one this plan guessed. Afterwards write step 6's playbook block with what was actually
  observed.</action>
  <verify>
    <automated>echo "MANUAL: A3 and A6 need the seeded local Convex DB (cwd-relative, absent from this worktree) and spend real money on a no-refunds rail — see VALIDATION.md Manual-Only Verifications"</automated>
  </verify>
  <done>The active `media-director` version is read back and recorded; the A/B showed no unintended
  tool-argument change; an image is visible on the canvas; a reel rendered containing a
  non-multiple-of-4 generated clip; the run wrote `media.deck_persisted`; `media.md` records all of
  it with a bumped `Last verified`.</done>
  <resume-signal>Type "approved" with the active version number and what you observed, or describe the issues.</resume-signal>
</task>

</tasks>

<verification>
- Task 1 only: `cd packages/core && npx vitest run src/storyboard.test.ts` and
  `cd packages/contracts && npx vitest run` — green in this worktree.
- After the merge: `turbo run test --concurrency=1` with the failure count compared to a baseline
  taken BEFORE the merge, and `npx tsc --noEmit` in `packages/backend` and `apps/web`, each read on
  its own line.
- A3 and A6 are manual by construction (VALIDATION.md's Manual-Only table): they need the seeded
  local Convex DB, which is cwd-relative and does not exist in this worktree, and A6 spends real
  money on a rail with no refunds.
</verification>

<success_criteria>
- **A3** — one image generates end to end through OpenRouter and is VISIBLE on the canvas.
- **A6** — one reel renders end to end containing a generated clip at a non-multiple-of-4 length,
  and the run wrote `media.deck_persisted`.
- The media-director body teaches 1-15, its worked answer demonstrates it, and the round trip was
  mutation-verified.
- The seeded active version number is READ BACK and recorded, not assumed.
- `docs/playbooks/media.md` and `docs/playbooks/skill-registry.md` both carry bumped
  `Last verified` blocks, and the media one says A3/A6 are now OBSERVED.
</success_criteria>

<output>
After completion, create `{phase_dir}/33.1-06-SUMMARY.md`, and then close the phase: update
`.planning/ROADMAP.md`'s Phase 33.1 entry by hand (`gsd-tools phase complete` silently no-ops the
ROADMAP — the checkbox and table stay stale while only STATE advances, so verify and hand-edit).
</output>
