---
phase: 33.1
slug: media-provider-migration-to-openrouter-grok-imagine-video-replaces-sora-2-before-the-videos-api-withdrawal-gpt-image-2-onto-openrouter-with-native-batching-and-the-persistdeck-music-field-fix
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-30
---

# Phase 33.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Inherits phase 33's infrastructure verbatim — same subsystem, same suites, same gotchas.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest per package (`packages/core`, `packages/cost`, `packages/backend` via convex-test, `apps/web` .ts-only/DOM-less) + Playwright e2e |
| **Config file** | `packages/core/vitest.config.ts`, `packages/cost/vitest.config.ts`, `packages/backend/vitest.config.mts`, `apps/web/vitest.config.mts`, `apps/web/playwright.config.ts` |
| **Quick run command** | `pnpm --filter @pikar/backend test -- media` (or `-- storyboard` in core, `-- media` in cost) |
| **Full suite command** | `turbo run test --concurrency=1` (concurrency 1 — vitest spawn-fail gotcha) |
| **Estimated runtime** | quick ~30-60s per filtered suite; full suite several minutes |

### Gotchas that have cost this repo real time — do not rediscover them

- **`pnpm test -- <filter>` does not filter.** Drop the `--` or run vitest directly. A "filtered"
  run that silently ran everything has been mistaken for a targeted green before.
- **`vitest --root <pkg>` from the repo root breaks convex-test's `_generated` glob** and fabricates
  mass failures. `cd` into the package.
- **Always baseline by FAILURE COUNT before trusting a mutation run.** A standing red elsewhere
  looks identical to a new one.
- **Backend vitest exiting 1 while thousands pass is a Windows-only artifact** — green on Linux CI.
  Read the failure count, not the exit code.
- **Read a gate's exit code on its own line**, never through a pipe.

---

## Sampling Rate

- **After every task commit:** the touched package's filtered suite.
- **After every plan wave:** `turbo run test --concurrency=1` + `npx tsc --noEmit` in BOTH
  `packages/backend` and `apps/web`.
- **Before `/gsd:verify-work`:** full suite green, `packages/cost` green (this is where the runway
  tripwire lives), plus the live localhost runs — which require the MAIN tree, not this worktree.
- **Max feedback latency:** ~90 seconds (filtered suite).

---

## Per-Task Verification Map

*Completed by `gsd-planner` — one row per task, every row carrying an automated command or an
explicit Wave 0 / manual-only justification.*

| Task ID | Plan | Wave | Criterion | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------|-----------|-------------------|-------------|--------|
| 01-T1 | 33.1-01 | 1 | A1/A2 RED: a Music line on both deck paths fails naming `music` | integration (convex-test) | `cd packages/backend && npx vitest run convex/dispatch.test.ts` | yes | ⬜ pending |
| 01-T2 | 33.1-01 | 1 | A1: persistDeck accepts `music`; the row reads it back | integration | `cd packages/backend && npx vitest run convex/dispatch.test.ts convex/plans.test.ts` | yes | ⬜ pending |
| 01-T3 | 33.1-01 | 1 | cockpit.md bumped; backend suite + tsc at baseline | suite + typecheck | `cd packages/backend && npx vitest run` then `npx tsc --noEmit` | yes | ⬜ pending |
| 02-T1 | 33.1-02 | 1 | A9: ADR-027 exists, supersedes 026, names BOTH costs — the counterparty and the $1.05/$2.10 guarantee loss — plus the `MEDIA_GENERATED_SECONDS_CAP` mitigation and that a mitigation is not an impossibility; 026 unedited | file assertion | `grep`-chain over `docs/decisions/027-*.md` + `git diff --quiet HEAD -- docs/decisions/026-*.md` (in-plan) | n/a (new file) | ⬜ pending |
| 02-T2 | 33.1-02 | 1 | media.md top block names ADR-027, both halves of the grid consequence, and that the array stays `[15, 30, 60]`; `## Reconciliation` still a runnable heading | doc + suite | `grep ADR-027` over the first 40 lines of docs/playbooks/media.md, then `cd packages/cost && npx vitest run` | yes | ⬜ pending |
| 03-T1 | 33.1-03 | 2 | **WAVE 0.** One live OpenRouter image call -> `usage.cost`; settles `size` vs `aspect_ratio` | manual probe + evidence file | `test -f .../33.1-PRICE-EVIDENCE.md && grep -q usage ...` | no (created here) | ⬜ pending |
| 03-T2 | 33.1-03 | 2 | Image submit resolves to `openrouter.ai`; reads OPENROUTER_API_KEY; env manifest honest | unit (stubbed fetch) | `cd packages/backend && npx vitest run convex/media.test.ts convex/env.test.ts` | yes | ⬜ pending |
| 03-T3 | 33.1-03 | 2 | A4: the price row equals a real `usage.cost`; parity check reads every entry of a kind | unit + fixture parity | `cd packages/cost && npx vitest run` | yes | ⬜ pending |
| 04-T1 | 33.1-04 | 3 | Grok rows + 1..15 grid + repin; every `_derived` number recomputed, none deleted; `targetsUnreachableByGeneratedVideoAlone` stays `[15, 30, 60]` | unit + fixture parity | `cd packages/cost && npx vitest run` — green EXCEPT the one named reachability test, red by design until 04-T4 | yes | ⬜ pending |
| 04-T2 | 33.1-04 | 3 | **Trap 2.** The runway tripwire still fires for sora-2 after the repin (3 mutations) | unit + mutation | `cd packages/cost && npx vitest run` | yes | ⬜ pending |
| 04-T3 | 33.1-04 | 3 | A5 (persist): 5s and 7s scenes parse with `adjustments: []`; BOTH rendered sentences (`adjustmentNotes` and `refusalText`'s `illegal_duration` arm) become ranges | unit + integration + DOM-less web | `cd packages/core && npx vitest run`; `cd apps/web && npx vitest run`; `cd packages/backend && npx vitest run convex/dispatch.test.ts` | yes | ⬜ pending |
| 04-T4 | 33.1-04 | 3 | **The guarantee, restored in code.** `MEDIA_GENERATED_SECONDS_CAP = 12` in `chooseMediaBatch`; every `TARGET_DURATIONS` member refused with `over_generated_seconds`; the code reaches `refusalText` as its own sentence; the block-deck fork resolved and reported | unit + cross-package compile + mutation (x3) | `cd packages/cost && npx vitest run`; `cd packages/backend && npx vitest run convex/media.test.ts`; `cd apps/web && npx vitest run` | yes | ⬜ pending |
| 05-T1 | 33.1-05 | 4 | **WAVE 0.** One live async video call: `duration: 7`, field names, poll URL, `usage.cost` | manual probe + evidence file | `grep -qi duration .../33.1-PRICE-EVIDENCE.md` | yes (appended) | ⬜ pending |
| 05-T2 | 33.1-05 | 4 | Video submit is JSON on `openrouter.ai`; new sibling poller; Sora submit deleted, poller retained | unit (stubbed fetch) | `cd packages/backend && npx vitest run convex/media.test.ts` | yes | ⬜ pending |
| 05-T3 | 33.1-05 | 4 | **A7/A8 + trap 2/3.** Both kinds resolve to OpenRouter; the flag is asserted beside the routing | unit + mutation | `cd packages/backend && npx vitest run convex/media.test.ts`; `cd packages/cost && npx vitest run` | yes | ⬜ pending |
| 06-T1 | 33.1-06 | 5 | Body teaches 1-15; worked answer carries a non-multiple-of-4 scene; round trip green; .md == .ts | round-trip + byte-identity + mutation | `cd packages/core && npx vitest run src/storyboard.test.ts`; `cd packages/contracts && npx vitest run` | yes | ⬜ pending |
| 06-T2 | 33.1-06 | 5 | Owner-timed merge into the main tree; post-merge full suite at baseline | MANUAL (owner) + suite | `turbo run test --concurrency=1` + `npx tsc --noEmit` x2, IN THE MAIN TREE | yes | ⬜ pending |
| 06-T3 | 33.1-06 | 5 | **A3/A6.** Seed + read back the active version; live image on canvas; live reel with a 7s clip | MANUAL — see Manual-Only table | none (needs the seeded local Convex DB + real spend) | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] **`OPENROUTER_API_KEY` reachable from the media path.** Already set on the local deployment
      (`npx convex env list`), but `media.ts` has never read it — `requireEnvMedia` currently
      demands `OPENAI_API_KEY`. Confirm the env manifest in `convex/lib/env.ts` gains the entry, or
      the first live call fails on a missing-env throw rather than on anything interesting.
- [ ] **One live OpenRouter image call to pin `MEDIA_IMAGE_PRICING`.** The current `$0.01` row is
      self-described as conservative and OpenRouter bills this model per output token. One call
      returns `usage.cost`; that number becomes the constant. **This is the only pre-planned spend
      in the phase (~$0.01–$0.04) and it must be authorised, not assumed.**
- [ ] **One live OpenRouter video call** to confirm the async contract end to end (202 →
      `polling_url` → `unsigned_urls[0]`) before the adapter is declared done. ~$0.28 for a 4s 720p
      clip. Same authorisation rule.

Existing test infrastructure covers everything else — no missing frameworks or configs.

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| An image generates end-to-end and lands on the canvas | A3 | Needs the seeded local Convex DB, which is cwd-relative and does not exist in this worktree | After merge into `C:/Users/expert/Desktop/Pikar-Ai`: prod build, `next start -p 3111`, ask the cockpit for an image |
| A reel renders with non-multiple-of-4 clip lengths | A6 | Same; also spends real money on the no-refunds rail | Same, after A5 is green offline |
| Grok output quality is acceptable for a customer-facing reel | — | Perceptual. Nothing in this phase asserts it, and no test can | Owner watches one finished reel |
| `media-director` grid-rule body change | — | **CORRECTED 2026-08-30 during planning: this skill is DELIBERATELY UNGATED.** `skillBodies.test.ts:130` asserts `isGatedSkill(MEDIA_DIRECTOR_SKILL) === false`, because `run-eval-golden.mjs` derives its `--skill` list from `GATED_SKILLS` and drives `runCockpitAgent` over TEXT fixtures, which cannot exercise a storyboard turn — gating it would strand it at v1 with no runner able to clear the gate. **There is no EVAL_GATE and no ~$0.35 to spend.** That is worse, not better: `seedSkills` publishes at `maxVersion+1` straight to ACTIVE with no eval between prose and production. | The four real gates, in order: the `media-director.md` round trip (`packages/core/src/storyboard.test.ts:316`, the ONLY pre-live gate); `skillBodies.test.ts` byte-identity between the `.md` and the derived `.ts` that actually ships; a live `seedSkills` with the active version READ BACK (not assumed — phase 10 shipped at `@14` where the plan said `@13`); and the owner in a browser. A/B the previous version on one fixture (~$0.01) first — a body edit has shifted this model's tool arguments before. |

---

## Falsifiability Notes

Three specific traps this phase is exposed to, each with the shape that would hide it:

1. **The `music` fix can be tested vacuously.** A test that asserts `persistDeck` succeeds without
   ever passing `music` is green before and after the fix. The RED must be observed with the field
   present, and the assertion must name the field.
2. **`succession.replacementWiredUp = true` is a self-certifying flag.** Setting it turns the runway
   tripwire green whether or not anything was wired — that is exactly the failure `7012068` was
   written to close (`status !== "decision_pending"` went green on an ADR alone). It must be set in
   the same commit as a landed submit path and price row, and the phase should assert the submit
   path independently rather than trusting the flag.
3. **A source scan for `api.openai.com` proves spelling, not routing.** A9's real check is that the
   video and image adapters resolve to an OpenRouter base at runtime, asserted on the resolved
   value, not on the absence of a string.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 / manual-only justification (17 rows, one per task)
- [x] Sampling continuity: the only manual runs are 03-T1, 05-T1 (Wave 0 probes) and 06-T2, 06-T3 (merge + live); no 3 consecutive automated-verify-free tasks
- [x] Wave 0 covers all MISSING references — env manifest in 03-T2, live image call in 03-T1, live video call in 05-T1; both probes flagged for spend authorisation in their plans' `user_setup`
- [x] No watch-mode flags (`vitest run` everywhere)
- [x] Feedback latency < 90s for filtered suites
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned 2026-08-30 by `gsd-planner` — 6 plans, 5 waves, A1-A9 each claimed by exactly one plan. REVISED same day after the owner's decision on the generated-seconds cap: plan 33.1-04 gains Task 4 (18 rows), plan 33.1-02's ADR-027 must now carry two costs and the mitigation. Pending execution.
