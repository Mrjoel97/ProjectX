---
phase: 28-connector-backed-revenue-pack
plan: 09
type: execute
wave: 8
depends_on: ["28-05", "28-06", "28-07", "28-08", "28-22", "28-23", "28-24", "28-25", "28-26"]
files_modified:
  - packages/backend/convex/http.ts
  - packages/backend/convex/connectorConnections.ts
  - packages/backend/convex/connectorConnections.test.ts
  - packages/backend/convex/providerGates.ts
  - apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx
  - apps/web/app/(app)/dashboard/profile/connections.ts
  - apps/web/app/(app)/dashboard/profile/connections.test.tsx
  - docs/playbooks/revenue-connectors.md
autonomous: true
requirements: [REVN-01, REVN-02, REVN-03]

must_haves:
  truths:
    - "One serialized callback owner routes only known providers after consuming one-time state."
    - "Connections omits parked/expired/failed providers and renders lifecycle state only for passed gates."
    - "A provider is connectable only when its own suitability, deployment and live-gate status permit it; no aggregate all-provider flag exists."
  artifacts:
    - path: packages/backend/convex/http.ts
      provides: Sanitized provider callback routes
    - path: packages/backend/convex/connectorConnections.ts
      provides: Tenant-safe connection status/connect/disconnect projections
    - path: apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx
      provides: Connected four-provider lifecycle UI
  key_links:
    - from: provider callback route
      to: provider auth helper
      via: consumed tenant/provider-bound state
      pattern: "hubspot|quickbooks|stripe|paypal"
---

<objective>Integrate the independent provider helpers into one safe callback/status surface without collapsing their gates.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx
@packages/backend/convex/http.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Register sanitized OAuth callbacks and lifecycle projections</name>
  <files>packages/backend/convex/http.ts, packages/backend/convex/connectorConnections.test.ts, packages/backend/convex/connectorConnections.ts</files>
  <action>Register compile-time optional callbacks only for server-gate-passed providers. Parked/expired/failed gates have no callback or connect projection. Tests cover replay/mismatch/isolation, one-passed/three-parked omission, and refreshed live failure immediately removing route and visibility.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- connectorConnections connectorOAuth http</automated></verify>
  <done>Callbacks cannot be replayed/grafted and clients see only safe lifecycle state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Connect the Connections panel without false-ready states</name>
  <files>apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx, apps/web/app/(app)/dashboard/profile/connections.ts, apps/web/app/(app)/dashboard/profile/connections.test.tsx</files>
  <action>Render only server-returned passed providers with checking/connect/ready/reauth/revoke-partial/disconnect states. Never show parked/expired/failed cards; remove a formerly visible provider on refreshed gate failure without hiding other passed providers.</action>
  <verify><automated>pnpm --filter @pikar/web test -- connections &amp;&amp; pnpm --filter @pikar/web typecheck</automated></verify>
  <done>The existing Connections surface truthfully exposes only cleared providers.</done>
</task>
<task type="auto">
  <name>Task 3: Update callback and UI operations</name>
  <files>docs/playbooks/revenue-connectors.md</files>
  <action>Record callback routes, state cleanup, feature gating, per-provider disable/rollback, partial-revoke operations and UI status semantics. A rollback hides connect/workflows but never removes terminal suppression or leaves a live provider grant unrevoked.</action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; pnpm --filter @pikar/backend typecheck</automated></verify>
  <done>Connection lifecycle is operable and independently reversible.</done>
</task>
</tasks>
<verification>
- Provider callback/status and web connection tests green.
- No callback query secret reaches the redirect, audit or telemetry.
- Parked/expired/failed providers are omitted, including immediately after a refresh failure.
</verification>
<success_criteria>- REVN-01..03 have a unified user surface without a unified capability runtime.</success_criteria>
<output>Create `28-09-SUMMARY.md`.</output>
