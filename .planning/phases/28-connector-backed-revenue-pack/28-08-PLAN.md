---
phase: 28-connector-backed-revenue-pack
plan: 08
type: execute
wave: 6
depends_on: ["28-01", "28-04", "28-18", "28-26"]
files_modified:
  - packages/revenue/src/providers/paypal.ts
  - packages/revenue/src/providers/paypal.test.ts
  - packages/backend/convex/paypalAuth.ts
  - packages/backend/convex/paypalConnector.ts
  - packages/backend/convex/paypalConnector.test.ts
  - scripts/smoke-paypal-read.mjs
  - docs/connectors/paypal-suitability.md
  - docs/playbooks/connector-paypal.md
autonomous: true
requirements: [REVN-03]

must_haves:
  truths:
    - "PayPal uses a real third-party merchant/partner grant; app client credentials are never misrepresented as tenant authorization."
    - "Only approved transaction/invoice/settlement/read-dispute permissions and endpoints exist; default refund/payment permissions are refused."
    - "Sandbox parsing cannot by itself mark production partner access approved."
  artifacts:
    - path: packages/backend/convex/paypalConnector.ts
      provides: Partner/merchant-bound read actions
    - path: packages/revenue/src/providers/paypal.ts
      provides: Transaction/invoice/settlement/dispute normalization
  key_links:
    - from: paypalAuth.ts
      to: provider suitability approval
      via: Partner Referrals/merchant binding and exact read permission set
      pattern: "partner|merchantId|third_party"
---

<objective>Implement and independently prove PayPal reads only after partner and permission eligibility is real.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@docs/connectors/paypal-suitability.md
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@docs/playbooks/revenue-connectors.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement partner/merchant-bound PayPal authorization</name>
  <files>packages/backend/convex/paypalAuth.ts, packages/backend/convex/paypalConnector.test.ts</files>
  <action>Branch on the independent admission status. For approved_beta that includes PayPal partner status and the exact read-only permission set, implement the documented seller/partner onboarding flow, merchant binding, encrypted credential lifecycle and revoke/disconnect semantics. For blocked/deferred, create no auth capability and preserve `parked` so other lanes continue. Reject ordinary app client credentials as a tenant connection and reject default PAYMENT/REFUND/UPDATE permissions. Tests cover approved and parked/no-capability paths, wrong partner/merchant/environment, missing permission, callback replay, tenant isolation and revoked access.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- paypalConnector connectorOAuth connectorCredentials</automated></verify>
  <done>Every ready PayPal connection proves delegated access to that tenant's merchant and no write permission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize bounded PayPal financial reads</name>
  <files>packages/revenue/src/providers/paypal.test.ts, packages/revenue/src/providers/paypal.ts, packages/backend/convex/paypalConnector.ts, packages/backend/convex/paypalConnector.test.ts</files>
  <action>If admission is not approved_beta, create only decision/absence tests proving no parser, adapter export, route, capability or UI visibility and skip implementation. Otherwise add closed bounded GET/list operations, strict Money parsing, dropped action/free-text fields and permission/cap/error/two-tenant tests.</action>
  <verify><automated>pnpm --filter @pikar/revenue test -- paypal &amp;&amp; pnpm --filter @pikar/backend test -- paypalConnector</automated></verify>
  <done>PayPal produces bounded payment-rail facts with honest partner/permission/unavailable states.</done>
</task>

<task type="auto">
  <name>Task 3: Create and run the PayPal live-read/revoke gate</name>
  <files>scripts/smoke-paypal-read.mjs, docs/connectors/paypal-suitability.md, docs/playbooks/connector-paypal.md</files>
  <action>If approved_beta, create the delegated-merchant smoke with normal-run, `--self-test` and `--verify-evidence` modes; run controlled reads/revoke and capture refs/methods/paths/counts/coverage/status only. The verifier checks schema, freshness, partner/environment/merchant binding and no sensitive/write fields. If parked, create no smoke and only absence tests checked by the shared lane checker.</action>
  <verify><automated>node scripts/check-provider-lane.mjs --provider paypal --stage engineering</automated></verify>
  <done>The lane has reproducible machine evidence and a proposed passed/parked result.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/revenue test -- paypal`
- `pnpm --filter @pikar/backend test -- paypalConnector`
- `node scripts/smoke-paypal-read.mjs`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- PayPal's share of REVN-03 completes only when its production/live lane passes; parked remains incomplete.
- Refund/payment/dispute mutation remains absent in permissions, exports and endpoints.
</success_criteria>

<output>Create `28-08-SUMMARY.md` with partner decision and evidence refs.</output>
