---
phase: 28-connector-backed-revenue-pack
plan: 15
type: execute
wave: 16
depends_on: ["28-09", "28-10", "28-11", "28-13", "28-20"]
files_modified:
  - packages/backend/convex/revenueTelemetry.ts
  - packages/backend/convex/revenueTelemetry.test.ts
  - packages/backend/convex/opsSignals.ts
  - packages/backend/convex/opsSignals.test.ts
  - docs/playbooks/revenue-connectors.md
autonomous: true
requirements: [REVN-04, REVN-05, REVN-06]

must_haves:
  truths:
    - "Revenue telemetry contains tenant-scoped refs, enums, counts, timestamps and bounded performance fields only."
    - "Customer names, messages, invoice amounts, credentials and raw provider payloads never enter telemetry or audit events."
    - "Overdue recovery is recorded only from a later observed provider status, never inferred from a draft, approval or send."
  artifacts:
    - path: packages/backend/convex/revenueTelemetry.ts
      provides: Shared-event emission for connector and revenue workflow outcomes
    - path: packages/backend/convex/opsSignals.ts
      provides: Bounded outcome projections for Phase 28 success measures
  key_links:
    - from: revenueTelemetry.ts
      to: Phase 27 shared event table/helper
      via: closed refs/counts/status-only event API consumed by plan 21
      pattern: "connector_read|workflow_completed|finance_computed|reminder_staged|recovery_observed"
---

<objective>Measure whether the revenue pack improves outcomes while keeping business and provider content out of telemetry.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@.planning/phases/28-connector-backed-revenue-pack/28-VALIDATION.md
@packages/backend/convex/opsSignals.ts
@docs/playbooks/revenue-connectors.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Emit revenue outcomes through the landed shared event plane</name>
  <files>packages/backend/convex/revenueTelemetry.test.ts, packages/backend/convex/revenueTelemetry.ts</files>
  <action>Inspect and reuse the exact Phase 27 shared event table/helper and privacy contract. Do not create a second telemetry table or parallel analytics plane. Add the minimum closed event kinds for connector lifecycle/read, revenue workflow completion, deterministic finance computation, reminder staging and later provider-observed recovery. Emit tenant ID, opaque refs, provider/workflow/status enums, counts, coverage, timestamps, latency and cost only. If Plan 03 found that the shared schema needed a bounded extension, consume that extension here rather than changing schema again.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- revenueTelemetry telemetry</automated></verify>
  <done>Every meaningful Phase 28 terminal can emit one idempotent, tenant-scoped, content-free outcome event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project bounded success measures without reconstructing business data</name>
  <files>packages/backend/convex/opsSignals.test.ts, packages/backend/convex/opsSignals.ts, packages/backend/convex/revenueTelemetry.test.ts</files>
  <action>Extend the landed operational-signal projection with connector-availability/completion, follow-up completion, overdue-item recovery, response-handling time, finance-coverage/confidence, and workflow cost/latency measures. Use counts, durations, enums and ratios only; never expose or aggregate invoice amounts. Mark denominators and incomplete windows honestly. Count recovery only after a later normalized provider read observes the invoice paid/resolved, and make duplicate/reordered events idempotent.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- revenueTelemetry opsSignals</automated></verify>
  <done>The product can judge usefulness, reliability and efficiency without storing sensitive source content in analytics.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the telemetry privacy boundary and operations guide</name>
  <files>packages/backend/convex/revenueTelemetry.test.ts, packages/backend/convex/opsSignals.test.ts, docs/playbooks/revenue-connectors.md</files>
  <action>Add recursive privacy scans that fail on names, email/message text, descriptions, amounts, currencies paired with amounts, tokens, credentials or raw payload fragments. Add tenant-isolation, duplicate-event, bounded-cardinality, retention and partial-window tests. Document the event vocabulary, allowed fields, recovery semantics, dashboard limitations and rollback/disable procedure.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- revenueTelemetry telemetry opsSignals &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Privacy regressions fail automatically and operators know exactly what the metrics can and cannot claim.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- revenueTelemetry telemetry opsSignals`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Phase 28 success measures are computable from bounded content-free events.
- No telemetry path can leak source payloads, financial amounts, customer content or cross-tenant identifiers.
</success_criteria>

<output>Create `28-15-SUMMARY.md` with the event allow-list, privacy scan and metric semantics.</output>
