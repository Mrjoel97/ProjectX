---
phase: 28-connector-backed-revenue-pack
plan: 07
type: execute
wave: 6
depends_on: ["28-01", "28-04", "28-18", "28-26"]
files_modified:
  - packages/revenue/src/providers/stripe.ts
  - packages/revenue/src/providers/stripe.test.ts
  - packages/backend/convex/stripeAuth.ts
  - packages/backend/convex/stripeConnector.ts
  - packages/backend/convex/stripeConnector.test.ts
  - scripts/smoke-stripe-read.mjs
  - docs/connectors/stripe-suitability.md
  - docs/playbooks/connector-stripe.md
autonomous: true
requirements: [REVN-03]

must_haves:
  truths:
    - "Stripe implementation exists only after Pikar is eligible for Extension read_only; read_write is never requested as a shortcut."
    - "Only connected-account list/retrieve operations are reachable; refunds, capture, finalize/send, dispute updates, transfers and payouts are absent."
    - "Stripe payment-rail facts remain supplemental until deterministic reconciliation selects them."
  artifacts:
    - path: packages/backend/convex/stripeConnector.ts
      provides: Version-pinned connected-account read actions
    - path: packages/revenue/src/providers/stripe.ts
      provides: Balance/payment/invoice/payout/dispute normalization
  key_links:
    - from: stripeConnector.ts
      to: stripeAuth.ts
      via: read-only Extension connection and Stripe-Account binding
      pattern: "read_only|Stripe-Account|oauth/deauthorize"
---

<objective>Implement and independently prove Stripe as a read-only payment rail, conditional on Extension eligibility.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@docs/connectors/stripe-suitability.md
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@docs/playbooks/revenue-connectors.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Extension-only Stripe OAuth/deauthorization</name>
  <files>packages/backend/convex/stripeAuth.ts, packages/backend/convex/stripeConnector.test.ts</files>
  <action>Branch on the independent admission status. For approved_beta with verified Extension configuration, implement test/live-mode-matched authorize/exchange, connected account binding, encrypted connection metadata and `/oauth/deauthorize`; `read_write` is a hard refusal. For blocked/deferred, create no auth capability and preserve `parked` so other lanes continue. Tests cover approved and parked/no-capability paths and reject wrong mode/client/account, non-read-only grant, cross-tenant id, callback replay and partial revoke.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- stripeConnector connectorOAuth connectorCredentials</automated></verify>
  <done>Only a verified read-only Extension connection can become ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize bounded Stripe payment-rail reads and prove write absence</name>
  <files>packages/revenue/src/providers/stripe.test.ts, packages/revenue/src/providers/stripe.ts, packages/backend/convex/stripeConnector.ts, packages/backend/convex/stripeConnector.test.ts</files>
  <action>If admission is not approved_beta, create only decision/absence tests proving no parser, adapter export, route, capability or UI visibility and skip implementation. Otherwise add closed bounded list/retrieve calls, strict minor-unit parsing and static forbidden-write endpoint/export tests.</action>
  <verify><automated>pnpm --filter @pikar/revenue test -- stripe &amp;&amp; pnpm --filter @pikar/backend test -- stripeConnector</automated></verify>
  <done>Stripe returns bounded supplemental facts and has no code path to mutate a connected account.</done>
</task>

<task type="auto">
  <name>Task 3: Create and run the Stripe live-read/deauthorize gate</name>
  <files>scripts/smoke-stripe-read.mjs, docs/connectors/stripe-suitability.md, docs/playbooks/connector-stripe.md</files>
  <action>If approved_beta, create the Extension smoke with normal-run, `--self-test` and `--verify-evidence` modes; run controlled reads/deauthorization and capture refs/methods/paths/counts/status only. The verifier checks schema, freshness, read_only/version/mode/account binding and no sensitive field. If parked, create no smoke and only absence tests checked by the shared lane checker.</action>
  <verify><automated>node scripts/check-provider-lane.mjs --provider stripe --stage engineering</automated></verify>
  <done>The lane has reproducible machine evidence and a proposed passed/parked result.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/revenue test -- stripe`
- `pnpm --filter @pikar/backend test -- stripeConnector`
- `node scripts/smoke-stripe-read.mjs`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Stripe's share of REVN-03 completes only when its production/live lane passes; parked remains incomplete.
- A blocked Extension decision stops implementation/exposure honestly.
</success_criteria>

<output>Create `28-07-SUMMARY.md` with Extension decision and live evidence.</output>
