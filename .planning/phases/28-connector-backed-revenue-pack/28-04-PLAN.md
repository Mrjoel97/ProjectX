---
phase: 28-connector-backed-revenue-pack
plan: 04
type: execute
wave: 4
depends_on: ["28-03"]
files_modified:
  - packages/backend/convex/connectorOAuth.ts
  - packages/backend/convex/connectorOAuth.test.ts
  - packages/backend/convex/connectorFetch.ts
  - packages/backend/convex/connectorFetch.test.ts
  - docs/playbooks/revenue-connectors.md
autonomous: true
requirements: [REVN-01, REVN-02, REVN-03]

must_haves:
  truths:
    - "OAuth state is random, tenant/provider bound, expiring and atomically single-use."
    - "Provider actions can call only compile-time HTTPS origins/paths with bounded timeout, bytes, pages and retries."
    - "A failed later page preserves prior valid data and marks the projection partial."
  artifacts:
    - path: packages/backend/convex/connectorOAuth.ts
      provides: One-time state mint/consume and sanitized callback result helpers
    - path: packages/backend/convex/connectorFetch.ts
      provides: Bounded provider-read transport/result classification without arbitrary method/path
  key_links:
    - from: connectorOAuth.ts
      to: connectorCredentials.ts
      via: callback consumes state before provider-specific exchange then seals credentials
      pattern: "consumeState|expiresAt|usedAt"
---

<objective>Build the shared security mechanics providers may reuse without inventing a generic connector runtime.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@packages/backend/convex/lib/functions.ts
@packages/backend/convex/gmailAuth.ts
@packages/backend/convex/gmail.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement one-time tenant-bound OAuth states</name>
  <files>packages/backend/convex/connectorOAuth.test.ts, packages/backend/convex/connectorOAuth.ts</files>
  <action>Test then implement server-random nonce minting, SHA-256-at-rest lookup, tenant/provider/environment/redirect binding, short expiry and atomic consume-once. Public mint is a tenantMutation/query pair that never accepts tenantId. Callback helpers return bounded status enums and redirect without code/state/token parameters. Prove expired, wrong-provider, wrong-tenant and replay state perform zero exchange/store calls.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- connectorOAuth</automated></verify>
  <done>Callback replay and tenant grafting are structurally refused before credentials exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded read transport and partial-result discipline</name>
  <files>packages/backend/convex/connectorFetch.test.ts, packages/backend/convex/connectorFetch.ts</files>
  <action>Create a test-injectable fetch helper that accepts only a provider-owned fully constructed GET request, AbortSignal timeout and explicit byte/page/item caps. Classify 401, 403, 429/retry-after, 5xx, network, timeout, malformed JSON and repeated cursors into the shared result vocabulary. Retry only documented idempotent reads with a small cap/jitter seam. Preserve completed pages on later failure. Do not accept arbitrary method, origin, path or request body and do not log response bodies.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- connectorFetch</automated></verify>
  <done>All provider reads have the same bounded failure semantics without sharing provider auth or endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Record OAuth/fetch invariants and provider extension rules</name>
  <files>docs/playbooks/revenue-connectors.md</files>
  <action>Document the state lifecycle, callback ordering, redirect hygiene, account binding, provider-specific refresh rule, endpoint allow-list, retry/partial semantics and the rule that two concrete implementations must justify any further shared abstraction. Include diagnostic commands using status/counts only.</action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; pnpm --filter @pikar/backend typecheck</automated></verify>
  <done>Provider plans have explicit mechanics to reuse and explicit behaviors they must keep separate.</done>
</task>
</tasks>

<verification>
- State replay causes no external exchange or DB write.
- No helper can be steered to a write method or unapproved host/path.
- Partial pages retain truthful coverage and never become empty success.
</verification>

<success_criteria>
- The shared auth/transport substrate is security-only, not a user-configurable connector client.
- Provider adapters can remain small, independent and bounded.
</success_criteria>

<output>Create `28-04-SUMMARY.md` with replay/partial mutation evidence.</output>
