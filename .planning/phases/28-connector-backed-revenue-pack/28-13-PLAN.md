---
phase: 28-connector-backed-revenue-pack
plan: 13
type: execute
wave: 11
depends_on: ["28-12"]
files_modified:
  - packages/revenue/src/reminders.ts
  - packages/revenue/src/reminders.test.ts
  - packages/backend/convex/invoiceReminders.ts
  - packages/backend/convex/invoiceReminders.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/revenue-connectors.md
autonomous: true
requirements: [REVN-06]

must_haves:
  truths:
    - "Invoice reminders are staged only from explicit user intent and a server-held invoice reference into an ordinary proposed email plan."
    - "Staging creates zero requests, workflows or sends; approval and delivery remain owned by the existing Phase 19 plan gate."
    - "Consent, suppression and required-footer checks cover immediate, scheduled and legacy delivery terminals, and a suppressed group recipient prevents the whole group send."
  artifacts:
    - path: packages/revenue/src/reminders.ts
      provides: Deterministic invoice-reminder selection, validation and draft-input contracts
    - path: packages/backend/convex/invoiceReminders.ts
      provides: Tenant-scoped invoice-ref-to-proposed-plan staging boundary
  key_links:
    - from: packages/backend/convex/invoiceReminders.ts
      to: existing cockpit email plan creation
      via: proposed PLAN only, never provider or Gmail delivery
      pattern: "proposed|invoiceRef|email"
---

<objective>Stage useful invoice reminders behind Pikar's existing approval and suppression boundary without adding a new send path.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@.planning/phases/28-connector-backed-revenue-pack/28-VALIDATION.md
@packages/backend/convex/llm.ts
@docs/playbooks/cockpit.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define deterministic reminder inputs and source guards</name>
  <files>packages/revenue/src/reminders.test.ts, packages/revenue/src/reminders.ts</files>
  <action>Test then implement the pure selection and template-input boundary for one server-held invoice reference. Validate source provider, external ref, payment state, exact minor-unit amount/currency and due date; reject paid, void, disputed, missing, cross-currency or stale/unavailable records. Produce facts for a toolless/code-owned draft template and never ask an LLM to calculate an amount, lateness or balance.</action>
  <verify><automated>pnpm --filter @pikar/revenue test -- reminders</automated></verify>
  <done>Only a valid unpaid invoice can become immutable reminder draft input, with all financial facts copied from normalized source data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage the reminder as an existing proposed email plan</name>
  <files>packages/backend/convex/invoiceReminders.test.ts, packages/backend/convex/invoiceReminders.ts, packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <action>Add an executive-loop-only staging tool; do not add it to the revenue specialist grant. Require explicit user intent plus a tenant-scoped server invoice ref, refetch and revalidate the invoice, build the deterministic draft, and populate the existing email-plan schema with status `proposed`. Make retries idempotent. Assert zero execution requests, workflows, provider mutations or Gmail sends before approval, and do not add a new ACTION_TYPE, EXTERNAL_TARGET or delivery arm.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- invoiceReminders cockpit plans</automated></verify>
  <done>A reminder appears only as a reviewable proposed plan and all write authority remains downstream of the existing Approve action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove suppression across every delivery terminal and document ownership</name>
  <files>packages/backend/convex/invoiceReminders.test.ts, packages/backend/convex/cockpitTools.test.ts, docs/playbooks/cockpit.md, docs/playbooks/revenue-connectors.md</files>
  <action>Exercise the existing immediate, scheduled and legacy email terminals with the staged plan. Prove consent and suppression are rechecked at delivery time, the required footer survives, and one suppressed recipient aborts the entire grouped send before any recipient starts. Add static assertions that Phase 28 introduced no alternate Gmail/provider send route. Update the cockpit and revenue playbooks with the split ownership: revenue chooses and stages; Phase 19 plans/contacts/Gmail approve and deliver.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- contacts cockpit gmail invoiceReminders &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Every terminal enforces the same suppression contract and the operational ownership boundary is explicit.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/revenue test -- reminders`
- `pnpm --filter @pikar/backend test -- invoiceReminders cockpit plans contacts gmail`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- REVN-06 stages an accurate, idempotent reminder while leaving all external effects behind the existing plan gate.
- Suppression, consent and footer tests are non-vacuous across every reachable delivery terminal.
</success_criteria>

<output>Create `28-13-SUMMARY.md` with the zero-send proof and terminal matrix.</output>
