---
phase: 28-connector-backed-revenue-pack
plan: 06
type: execute
wave: 6
depends_on: ["28-01", "28-04", "28-18", "28-26"]
files_modified:
  - packages/revenue/src/providers/quickbooks.ts
  - packages/revenue/src/providers/quickbooks.test.ts
  - packages/backend/convex/quickbooksAuth.ts
  - packages/backend/convex/quickbooks.ts
  - packages/backend/convex/quickbooks.test.ts
  - scripts/smoke-quickbooks-read.mjs
  - docs/connectors/quickbooks-suitability.md
  - docs/playbooks/connector-quickbooks.md
autonomous: true
requirements: [REVN-02, REVN-05]

must_haves:
  truths:
    - "QuickBooks realm-bound rotating refresh credentials update atomically and only one concurrent refresh succeeds."
    - "The broad Accounting scope is constrained by code to named GET/report endpoints; no entity write is exported."
    - "Cash, AR, AP and revenue reports are bounded, source-attributed and never interpreted as payroll certainty by themselves."
  artifacts:
    - path: packages/backend/convex/quickbooks.ts
      provides: Realm-bound bounded QuickBooks report/read actions
    - path: packages/revenue/src/providers/quickbooks.ts
      provides: Strict report/invoice/bill/account normalization
  key_links:
    - from: quickbooksAuth.ts
      to: connectorCredentials.ts
      via: refresh-token CAS replacing both encrypted access and rotating refresh token
      pattern: "realmId|refreshVersion|invalid_grant"
---

<objective>Implement and independently prove the QuickBooks accounting read rail without exposing its broad write-capable scope.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@docs/connectors/quickbooks-suitability.md
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@docs/playbooks/revenue-connectors.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement realm-bound OAuth with rotating-refresh CAS</name>
  <files>packages/backend/convex/quickbooksAuth.ts, packages/backend/convex/quickbooks.test.ts</files>
  <action>Branch on the independent admission status. For approved_beta with explicit broad Accounting-scope acceptance, implement authorize/exchange, realmId binding, one-hour access expiry, rolling refresh, current-refresh-version CAS/lease and revoke-before-delete. For blocked/deferred, create no auth capability and preserve `parked` so other lanes continue. Tests cover approved and parked/no-capability paths, race two refreshes, ensure one persisted rotation, model stale invalid_grant as reauth, reject realm swaps, and keep tokens/provider bodies out of logs.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- quickbooks connectorCredentials</automated></verify>
  <done>Concurrent refresh cannot corrupt/revoke the tenant grant and realm id never becomes an authorization shortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict QuickBooks report/entity read normalization</name>
  <files>packages/revenue/src/providers/quickbooks.test.ts, packages/revenue/src/providers/quickbooks.ts, packages/backend/convex/quickbooks.ts, packages/backend/convex/quickbooks.test.ts</files>
  <action>If admission is not approved_beta, create only decision/absence tests proving no parser, adapter export, route, capability or UI visibility and skip implementation. Otherwise expose only code-owned GET/query/report operations, bounded reports/entities and strict money parsing; test missing-vs-zero, mixed currency, caps, partials and two tenants.</action>
  <verify><automated>pnpm --filter @pikar/revenue test -- quickbooks &amp;&amp; pnpm --filter @pikar/backend test -- quickbooks</automated></verify>
  <done>QuickBooks supplies validated accounting-authority inputs while provider writes stay unreachable.</done>
</task>

<task type="auto">
  <name>Task 3: Create and run the QuickBooks live report/revoke gate</name>
  <files>scripts/smoke-quickbooks-read.mjs, docs/connectors/quickbooks-suitability.md, docs/playbooks/connector-quickbooks.md</files>
  <action>If approved_beta, create the smoke with normal-run, `--self-test` and `--verify-evidence` modes; run controlled reports/revoke and capture refs/methods/paths/counts/coverage/status only. The verifier checks schema, freshness, realm/environment and no write endpoint or sensitive field. If parked, create no smoke and only absence tests checked by the shared lane checker.</action>
  <verify><automated>node scripts/check-provider-lane.mjs --provider quickbooks --stage engineering</automated></verify>
  <done>The lane has reproducible machine evidence and a proposed passed/parked result.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/revenue test -- quickbooks`
- `pnpm --filter @pikar/backend test -- quickbooks`
- `node scripts/smoke-quickbooks-read.mjs`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- A passed QuickBooks production/live lane satisfies REVN-02; a parked lane remains explicitly incomplete.
- QuickBooks cannot perform a financial write even though its OAuth scope is broad.
</success_criteria>

<output>Create `28-06-SUMMARY.md` with scope-risk acceptance and live evidence.</output>
