---
phase: 28-connector-backed-revenue-pack
plan: 03
type: execute
wave: 3
depends_on: ["28-02", "28-17", "28-18"]
files_modified:
  - packages/revenue/src/credential.ts
  - packages/revenue/src/credential.test.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/connectorCredentials.ts
  - packages/backend/convex/connectorCredentials.test.ts
  - docs/playbooks/revenue-connectors.md
autonomous: true
requirements: [REVN-01, REVN-02, REVN-03]

must_haves:
  truths:
    - "Every new connector credential is AES-256-GCM sealed before persistence and tenant/provider/connection bound by AAD."
    - "Clients, models, audits, telemetry, errors and fixtures never receive plaintext credentials or raw scopes."
    - "All Phase 28 tables/indexes and closed agent-step literals land once in a single schema wave."
  artifacts:
    - path: packages/revenue/src/credential.ts
      provides: Dependency-free Web Crypto seal/open envelope with key-version/AAD contracts
    - path: packages/backend/convex/connectorCredentials.ts
      provides: Internal credential lifecycle and client-safe tenant connection statuses
    - path: packages/backend/convex/schema.ts
      provides: Connection, OAuth-state and contact-provider-ref storage/index foundation
  key_links:
    - from: connectorCredentials.ts
      to: CONNECTOR_CREDENTIAL_KEY_V1
      via: server-only key import and credential seal/open
      pattern: "ciphertextB64|ivB64|keyVersion|AES-256-GCM"
---

<objective>Close the encrypted-secret blocker and land the entire additive Phase 28 schema once.</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-connector-backed-revenue-pack/28-CONTEXT.md
@.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md
@packages/backend/convex/gmailAuth.ts
@packages/backend/convex/schema.ts
@docs/superpowers/specs/2026-08-02-connections-tab-design.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the AES-GCM credential envelope</name>
  <files>packages/revenue/src/credential.test.ts, packages/revenue/src/credential.ts</files>
  <action>Test and implement Web Crypto AES-256-GCM with a fresh 96-bit IV, base64 envelope, explicit `v1` key version and AAD over tenantId/provider/connectionId/environment/keyVersion. Require a decoded 32-byte deployment key. Tests cover round-trip, different IV/ciphertext for identical plaintext, tamper, tenant/provider/environment swap, wrong key, malformed base64 and rotation open-old/reseal-new. Never implement a cipher or deterministic IV.</action>
  <verify><automated>pnpm --filter @pikar/revenue test -- credential</automated></verify>
  <done>Ciphertext is copy-resistant across tenant/provider scope and rotation is testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add all Phase 28 schema/indexes and internal credential lifecycle</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/connectorCredentials.test.ts, packages/backend/convex/connectorCredentials.ts</files>
  <action>In one additive schema edit add encrypted tenant/provider connections; server-owned provider gates keyed provider/environment with admission/live status, evidence ref, expiry, revision and updatedAt; hashed one-time OAuth states; contact-provider refs; optional non-authoritative caches; Phase 27-compatible outcome fields only if required; and closed revenue tool literals. Keep additions backfill-free with no public secret/gate mutation or second telemetry plane. Add two-tenant and plaintext-sentinel scans.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- connectorCredentials schema &amp;&amp; pnpm --filter @pikar/backend typecheck</automated></verify>
  <done>New connectors can store only sealed credentials; public status and logs contain no capability inventory or secret.</done>
</task>

<task type="auto">
  <name>Task 3: Document key setup, rotation, loss and rollback</name>
  <files>docs/playbooks/revenue-connectors.md</files>
  <action>Record exact key generation/validation without printing it, required environment names, fail-closed missing-key behavior, staged key rotation/CAS/read-back, old-key retention until proof, irrecoverable key-loss consequence, revoke-before-delete, partial-revoke operations and rollback. Explicitly state this does not silently migrate existing Google tokens.</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>Operators can deploy/rotate/recover honestly without exposing a key or token.</done>
</task>
</tasks>

<verification>
- Credential mutation tests fail on AAD swap or plaintext persistence.
- Schema change is additive and no later Phase 28 plan owns `schema.ts`.
- Public projections contain no ciphertext, IV, account id, raw scope or provider error body.
</verification>

<success_criteria>
- Encrypted credential storage and two-tenant isolation exist before any provider appears.
- The Phase 28 schema collision is closed in one plan.
</success_criteria>

<output>Create `28-03-SUMMARY.md` with schema inventory and key operational evidence.</output>
