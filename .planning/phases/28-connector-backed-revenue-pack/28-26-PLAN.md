---
phase: 28-connector-backed-revenue-pack
plan: 26
type: execute
wave: 5
depends_on: ["28-03", "28-04", "28-18"]
files_modified:
  - packages/backend/convex/providerGates.ts
  - packages/backend/convex/providerGates.test.ts
  - scripts/check-provider-lane.mjs
  - docs/playbooks/revenue-connectors.md
autonomous: true
requirements: [REVN-01, REVN-02, REVN-03, REVN-05]

must_haves:
  truths:
    - "Provider admission/live status is server-owned machine-readable state, never inferred from UI, docs or module presence."
    - "Only internal authenticated judgment sealing and live-refresh failure paths mutate gates."
    - "Parked, expired or refreshed-failed gates are unavailable to every consumer immediately."
  artifacts:
    - path: packages/backend/convex/providerGates.ts
      provides: Closed provider gate state machine and passed-only server projections
    - path: scripts/check-provider-lane.mjs
      provides: Conditional engineering/evidence/absence verifier and judgment-sealing CLI
  key_links:
    - from: provider judgment plans
      to: providerGates.ts
      via: authenticated refs-only seal-decision operation
      pattern: "passed|parked|expired|failed"
---

<objective>Make independent provider eligibility durable server state consumed consistently by every downstream surface.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@packages/backend/convex/schema.ts @docs/playbooks/revenue-connectors.md @.planning/phases/28-connector-backed-revenue-pack/28-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the server-owned provider gate state machine</name>
  <files>packages/backend/convex/providerGates.test.ts, packages/backend/convex/providerGates.ts</files>
  <action>Implement closed providers HubSpot/QuickBooks/Stripe/PayPal and admission/live states pending/passed/parked/expired/failed over the Plan 03 table. Store evidence ref, environment, revision, expiry and timestamps only. Expose passed-only internal/tenant-safe projections. Permit mutations only from authenticated admin judgment sealing or provider live-refresh failure; stale revisions and evidence cannot re-enable. Tests cover two tenants, replay/CAS, expiry, pass-to-failed and absence of a public mutation.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- providerGates</automated></verify>
  <done>One server-owned gate record determines provider availability across the product.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement conditional lane verification and sealing CLI</name>
  <files>scripts/check-provider-lane.mjs, packages/backend/convex/providerGates.test.ts, docs/playbooks/revenue-connectors.md</files>
  <action>Implement explicit `--provider`, `--stage engineering|final`, `--all`, `--seal-decision pass|park|from-owner`, and `--verify-gate` modes. Engineering mode reads admission: approved_beta requires adapter/parser tests and smoke `--self-test`/`--verify-evidence`; parked requires decision/absence tests and rejects adapter, route or UI visibility. `from-owner` must consume a signed/refs-only owner decision artifact and deterministically resolve to pass or park; it cannot invent a judgment. `--all --stage final` iterates the closed four-provider set, verifies current evidence/expiry and returns non-zero for any inconsistent gate without turning a parked provider into a passed one. Seal mode uses server admin auth, evidence refs and optimistic revision; it cannot seal pass without fresh verified live evidence. Verify-gate proves refreshed failure disables passed projection. Add self-tests for every flag combination and document operations.</action>
  <verify><automated>node scripts/check-provider-lane.mjs --self-test &amp;&amp; pnpm --filter @pikar/backend test -- providerGates</automated></verify>
  <done>Every lane can complete passed or parked with the same executable fail-closed contract.</done>
</task>
</tasks>
<verification>- Gate tests and CLI self-tests cover pass, park, expiry, refresh failure and forbidden re-enable.</verification>
<success_criteria>- Connections, finance, discovery and final close can consume one authoritative provider status.</success_criteria>
<output>Create `28-26-SUMMARY.md` with state transitions and CLI contract.</output>
