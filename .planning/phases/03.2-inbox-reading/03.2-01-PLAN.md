---
phase: 03.2-inbox-reading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/emailIntent.ts
  - packages/core/src/emailIntent.test.ts
autonomous: true
requirements: [CKPT-01]
must_haves:
  truths:
    - "A recipients segment without @ is classified as a NAME to resolve, never stored as an address"
    - "A group-like token (team/everyone/all/staff/group/everybody) is detected and DEFERRED, never resolved"
    - "parseAddress turns a From/To/Cc header value into { displayName?, address }; rankCandidates dedupes by lowercased address and ranks by frequency then recency"
    - "A resolve answer folds chosen addresses + held pendingValid into recipients and clears pending state"
  artifacts:
    - path: "packages/core/src/emailIntent.ts"
      provides: "needs_resolution state + name/group classification + parseAddress + rankCandidates + resolve Answer"
      contains: "pendingResolution"
    - path: "packages/core/src/emailIntent.test.ts"
      provides: "unit tests for classification, group-defer, parseAddress, rankCandidates, resolve fold"
  key_links:
    - from: "applyAnswer (recipients)"
      to: "pendingResolution / pendingValid / groupDeferred"
      via: "per-segment classification"
      pattern: "pendingResolution"
    - from: "rankCandidates"
      to: "parseAddress"
      via: "header parse then dedupe+rank"
      pattern: "parseAddress"
---

<objective>
Extend the pure `emailIntent` brain so a recipients segment that is a NAME (no `@`) becomes a
new `needs_resolution` state instead of a bounced address, add the group-name defer signal, and
add the pure `parseAddress` + `rankCandidates` header helpers the cockpit will use to turn Gmail
metadata into ranked contact candidates. All pure (CLAUDE.md §1 — no Convex/Gmail import), all
unit-tested.

Purpose: this is the deterministic seam (DECISION #2 preserved — NO new LLM tool-loop). The
module only FLAGS that a name needs resolving; the cockpit adapter (Plan 04) does the Gmail
fetch. Keeping parse/rank pure and co-located INSIDE `emailIntent.ts` (not a sibling file) keeps
it under the existing `cockpit.md` playbook watch — a new `packages/core/src/*.ts` file would
trip the §9 Stop hook (research §Playbook coverage gap 2).
Output: the pure contracts Plans 03/04 build against.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-inbox-reading/03.2-CONTEXT.md
@.planning/phases/03.2-inbox-reading/03.2-RESEARCH.md
@.planning/phases/03.2-inbox-reading/03.2-VALIDATION.md
@packages/core/src/emailIntent.ts
@packages/core/src/emailIntent.test.ts
@packages/core/src/validateSubmit.ts

<interfaces>
<!-- The contract Plans 02/03/04 depend on. Keep these exact names/shapes so downstream
     plans wire against them without exploring. Ranking weights are Claude's discretion. -->

The candidate shape produced by rankCandidates and stored on the plans row (Plan 02 schema
mirror; Plan 04 writer). ONE section per unresolved name:

```typescript
// a single ranked contact match for a name
export interface ContactMatch {
  address: string;            // lowercased, deduped
  displayName?: string;       // USER + greeting only
  lastSubject?: string;       // USER-only hint — NEVER sent to the LLM
  lastDateMs?: number;        // USER-only hint
  count: number;              // frequency across the search hits
}
export interface NameCandidates { name: string; matches: ContactMatch[] }

// raw header records the Gmail search action (Plan 03) returns; core parses these
export interface HeaderRecord {
  from?: string; to?: string; cc?: string; subject?: string; date?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: needs_resolution state + name/group classification + resolve answer</name>
  <files>packages/core/src/emailIntent.ts, packages/core/src/emailIntent.test.ts</files>
  <behavior>
    - `applyAnswer({slot:"recipients"})` classifies EACH segment: has `@` + valid → recipient path
      (existing); has `@` + malformed → `rejected` (existing re-ask path, unchanged); NO `@` and a
      group word (team/everyone/all/staff/group/everybody, case-insensitive) → `groupDeferred`;
      NO `@` otherwise → `pendingResolution` (a name to resolve). Multi-word names ("Sarah Chen")
      stay one segment (the tokenizer that splits is in cockpit.ts, Plan 04 — this module receives
      already-segmented values).
    - Same-turn VALID addresses in a turn that ALSO has a name are held in `pendingValid` (NOT
      committed to `recipients` yet — the uniform-confirm decision). A turn of only valid emails
      keeps the existing direct path (no pendingValid, no card).
    - `nextQuestion` returns a new `{ kind:"resolve_recipients"; names: string[] }` whenever
      `pendingResolution` is non-empty — BEFORE ask_subject. `groupDeferred` surfaces as a distinct
      signal so the cockpit can emit the decline copy + re-ask (model it like `rejected`).
    - A new resolve `Answer` (`{ slot:"resolution"; picks: {name;address;displayName?}[] }`) folds
      the picked addresses + all `pendingValid` into `recipients` (deduped) and clears
      `pendingResolution`/`pendingValid`. Captures the FIRST pick's `displayName` for the greeting
      (expose it on the returned state, e.g. `greetingName`).
    - Never-ready-early holds: `pendingResolution` non-empty blocks `ready` just like `rejected`.
  </behavior>
  <action>
    Extend `EmailIntentState` with optional `pendingResolution?: readonly {name:string}[]`,
    `pendingValid?: readonly string[]`, `groupDeferred?: readonly string[]`, `greetingName?: string`.
    Add `resolve_recipients` + (optional) `defer_group` to `NextQuestion`, and the `resolution`
    `Answer` variant. Update `applyAnswer` recipients branch to the classification above and add a
    `resolution` branch. Update `nextQuestion` ordering: pending rejection → resolve_recipients →
    group defer → ask_subject → … . Reuse `isValidEmail` from `./validateSubmit` (the ONE regex —
    do NOT add a second). Keep the module pure (no Date.now/random/I-O) and every applyAnswer
    returning a fresh object. Mark the group word list with a `ponytail:` comment naming the
    ceiling (word list, extend on demonstrated need) per research Open-Q 3.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test</automated>
  </verify>
  <done>Tests cover: "Sarah" → pendingResolution; "team" → groupDeferred; mixed "Sarah" + valid
    email → name in pendingResolution AND the valid held in pendingValid (not recipients);
    resolve answer folds picks + pendingValid into recipients and clears pending state + sets
    greetingName; nextQuestion returns resolve_recipients before ask_subject; pendingResolution
    blocks ready. Existing emailIntent tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pure parseAddress + rankCandidates header helpers (co-located)</name>
  <files>packages/core/src/emailIntent.ts, packages/core/src/emailIntent.test.ts</files>
  <behavior>
    - `parseAddress("Sarah Chen <sarah@acme.com>")` → `{ displayName:"Sarah Chen", address:"sarah@acme.com" }`.
    - `parseAddress("sarah@acme.com")` → `{ address:"sarah@acme.com" }` (no displayName).
    - `parseAddress("not-an-address")` → `null`. Address is lowercased.
    - `rankCandidates(name, records: HeaderRecord[])` parses From/To/Cc of every record, dedupes by
      lowercased address, counts frequency, tracks the most-recent `date` (→ lastDateMs) + its
      subject (→ lastSubject) and displayName, returns `ContactMatch[]` sorted `count desc, then
      lastDateMs desc`, capped ~5. Returns `[]` for zero records.
  </behavior>
  <action>
    Add `parseAddress` (the small regex from research Pattern 2 — mark a `ponytail:` comment naming
    the ceiling: simple `Name <addr>` / bare-addr shapes only, no group-address/comment syntax,
    upgrade on a demonstrated malformed-header failure) and `rankCandidates` INSIDE `emailIntent.ts`
    (NOT a new file — §9 watch coverage). Export both + the `ContactMatch`/`NameCandidates`/
    `HeaderRecord` types from the package index so Plan 04 imports them from `@pikar/core`. A `Date`
    string parse uses `Date.parse` (pure, no clock read). Ranking weights/tie-break are your
    discretion — `count desc, lastDateMs desc` is the lazy-correct default.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test</automated>
  </verify>
  <done>parseAddress handles the three shapes; rankCandidates dedupes + ranks + caps and produces a
    deterministic order for a fixed record set; both exported from `@pikar/core`.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test` green (existing + new cases).
- `pnpm --filter @pikar/core typecheck` (or repo typecheck) green — new exports resolve.
- No new file created under `packages/core/src/` (helpers co-located — §9).
</verification>

<success_criteria>
The pure module flags names for resolution (SC2 seam), defers groups, and exposes parse/rank the
cockpit composes — with zero Convex/Gmail import (nothing can send from here).
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inbox-reading/03.2-01-SUMMARY.md`.
</output>
