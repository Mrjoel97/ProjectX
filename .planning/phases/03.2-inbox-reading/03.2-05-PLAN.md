---
phase: 03.2-inbox-reading
plan: 05
type: execute
wave: 3
depends_on: [03.2-02, 03.2-04]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/app/(app)/dashboard/workspace/ChatPane.tsx
  - apps/web/e2e/cockpit-resolve.spec.ts
  - apps/web/e2e/cockpit-report.spec.ts
autonomous: true
requirements: [CKPT-01]
must_haves:
  truths:
    - "A resolution card renders one section per unresolved name, each with clickable contact chips carrying a hint (last subject / last-contacted / count)"
    - "Clicking a chip resolves that recipient via resolveRecipients and the conversation continues; a single match is pre-selected but confirmable; same-turn valid addresses show pre-selected"
    - "A 'Searching your mailbox…' activity chip shows while the read runs; a read-auth failure surfaces the existing ReconnectBanner"
    - "Offline SMOKE:: name → resolution card → pick → PLAN, with nothing sent"
    - "The pre-existing cockpit-report E2E is updated to comma-separated recipients so the tokenizer change keeps it green"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "resolution card branch (chips per name-section) calling api.cockpit.resolveRecipients"
      contains: "resolveRecipients"
    - path: "apps/web/app/(app)/dashboard/workspace/ChatPane.tsx"
      provides: "'Searching your mailbox…' activity chip off plan candidates state"
    - path: "apps/web/e2e/cockpit-resolve.spec.ts"
      provides: "name → card → pick → PLAN E2E over the offline SMOKE:: path"
    - path: "apps/web/e2e/cockpit-report.spec.ts"
      provides: "recipients line updated to comma-separated (survives the comma/'and' tokenizer)"
  key_links:
    - from: "resolution card chip onClick"
      to: "api.cockpit.resolveRecipients"
      via: "useAction, structured pick"
      pattern: "resolveRecipients"
    - from: "CardList dispatcher"
      to: "plan.candidates"
      via: "render the card before proposed when candidates present"
      pattern: "candidates"
---

<objective>
Render the resolution card in the existing `cards.tsx` dispatcher (a new branch when
`plan.candidates` is present, BEFORE `proposed`): one section per unresolved name with clickable
contact chips carrying the header-level hint, a single-match pre-selected-but-confirmable section,
and same-turn valid addresses shown pre-selected — clicking resolves via `resolveRecipients`. Add
the "Searching your mailbox…" activity chip to `ChatPane.tsx`. Prove the whole loop with a
Playwright E2E over the offline `SMOKE::` search path, and retrofit the pre-existing
`cockpit-report` E2E for the new comma/"and" tokenizer.

Purpose: SC2's user-facing half — surfacing mailbox people into the guided conversation without
sending. Reuses the live-query reactive card pattern (no polling); the existing `ReconnectBanner`
already lights on the `gmail_reconnect` notification Plan 04 inserts, so no banner change is needed.
Output: the resolution UX + the `cockpit-resolve` spec that gates the phase.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-inbox-reading/03.2-CONTEXT.md
@.planning/phases/03.2-inbox-reading/03.2-RESEARCH.md
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@apps/web/app/(app)/dashboard/workspace/ChatPane.tsx
@apps/web/app/(app)/dashboard/workspace/page.tsx
@apps/web/e2e/cockpit-report.spec.ts

<interfaces>
<!-- Plan 02 shape on the byThread plan row: plan.candidates?: { name; matches:{ address;
     displayName?; lastSubject?; lastDateMs?; count }[] }[]; plan.pendingValid?: string[].
     Plan 04: api.cockpit.resolveRecipients({ threadId, picks:{name;address;displayName?}[] }).
     Derive the Plan type from the api as cards.tsx already does (FunctionReturnType<byThread>). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolution card in cards.tsx</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    Add a `ResolutionCard({ plan, threadId })` and dispatch it from `CardList` when
    `plan.candidates?.length` is set AND status is not yet `proposed`/`delivering` (resolution
    happens during `collecting`, before the PLAN). Render one section per `candidates[]` entry: the
    name heading + its `matches` as clickable chips, each chip labeled `address · {count} msgs, {last
    contacted}` using `lastSubject`/`lastDateMs`/`count` (USER-only hints — these are display only).
    Single-match section: pre-select the one chip but still require a confirm click (a name→address is
    an inference, never auto-assumed). Show any `plan.pendingValid` addresses as a pre-selected
    "already valid" row (confirmable, uniform with the chip sections). A single "Use these contacts"
    action calls `useAction(api.cockpit.resolveRecipients)({ threadId, picks })` where picks = the
    selected chip per section (`{name, address, displayName}`). Reuse the existing inline `box`/`chip`
    styles + teal token. Keep it a plain reactive component (no new state machinery beyond local
    selection). `ponytail:` note if single-vs-combined share one component (your discretion per
    CONTEXT).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>ResolutionCard renders sections+chips from plan.candidates, pendingValid pre-selected,
    dispatched before proposed, and clicking calls resolveRecipients with the selected picks;
    web typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: "Searching your mailbox…" activity chip in ChatPane.tsx</name>
  <files>apps/web/app/(app)/dashboard/workspace/ChatPane.tsx</files>
  <action>
    Extend the existing `activity` derivation (currently off `plan.status`) so that when
    `plan.candidates?.length` is present (resolution in flight) it shows "Searching your mailbox… /
    pick a contact →", mirroring the existing "Drafting…/Sending…" chip pattern. Purely a derived
    string off the reactive `byThread` plan — no new query, no polling. Do NOT change the composer or
    the threadId lift. (Read-auth failure needs no change here: the assistant's fallback message
    arrives on the thread and the existing ReconnectBanner lights off the gmail_reconnect
    notification.)
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>The activity chip shows a searching state while candidates are present; typecheck green;
    threadId lift + composer untouched.</done>
</task>

<task type="auto">
  <name>Task 3: cockpit-resolve E2E (offline SMOKE::) + retrofit cockpit-report recipients line</name>
  <files>apps/web/e2e/cockpit-resolve.spec.ts, apps/web/e2e/cockpit-report.spec.ts</files>
  <action>
    NEW `cockpit-resolve.spec.ts` mirroring `cockpit-report.spec.ts` (signed-in storageState,
    already-running local stack, `workspace-pane` testid). Flow: go to /dashboard/workspace; in the
    composer answer recipients with a `SMOKE::` name (e.g. `SMOKE::Sarah`) → assert the resolution
    card appears with a chip carrying `sarah@example.com` and a hint (from Plan 03's SMOKE fixture) →
    click the chip + "Use these contacts" → assert the conversation advances to the subject question.
    Continue: subject → body intent WITH `SMOKE::route=direct_llm::` (offline draft) → mode
    `individual` → assert the PLAN card shows `sarah@example.com` and exactly one Approve. Do NOT
    click Approve (this phase is about READING — nothing sends; the send E2E is cockpit-report).
    Assert the body preview opens with "Hi Sarah" (greeting personalization from the resolved display
    name). This is the new-file Wave-0 gap; `apps/web/e2e/` is already covered by watch.json (no §9
    trip).

    RETROFIT the pre-existing `cockpit-report.spec.ts` for the comma/"and" tokenizer: its recipients
    step (`await say("alice@example.com bob@example.com")`, ~line 29) is space-separated and will now
    parse as ONE malformed segment → a re-ask, breaking the currently-passing spec at step 1. Change
    it to comma-separated: `await say("alice@example.com, bob@example.com")`. This is the ONLY change
    to that spec — the rest of its flow (subject/body/mode → PLAN → approve → report) is unaffected.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test cockpit-resolve cockpit-report --list</automated>
  </verify>
  <done>`cockpit-resolve.spec.ts` is Playwright-discovered and encodes name→card→pick→PLAN over
    SMOKE::; `cockpit-report.spec.ts` recipients line is comma-separated so it survives the tokenizer
    change; the live green run of both is the phase gate (needs convex dev + next dev).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` green.
- `pnpm --filter @pikar/web exec playwright test cockpit-resolve cockpit-report --list` discovers both specs.
- Phase gate (live, not per-plan): `pnpm exec playwright test cockpit-resolve` green AND the
  retrofitted `cockpit-report` green (comma-separated recipients) against a running stack;
  cockpit-render/split still green.
</verification>

<success_criteria>
The user sees a resolution card, picks a contact, and the conversation continues to a PLAN — the
mailbox surfaced a person into the guided conversation with nothing sent (SC2), verified offline;
the pre-existing send E2E stays green under the new tokenizer.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inbox-reading/03.2-05-SUMMARY.md`.
</output>
