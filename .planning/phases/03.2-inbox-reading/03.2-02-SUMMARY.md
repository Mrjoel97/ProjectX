---
phase: 03.2-inbox-reading
plan: 02
subsystem: database
tags: [convex, schema, plans, candidates, name-resolution, content-plane]

# Dependency graph
requires:
  - phase: 03.1-cockpit-core
    provides: plans content-plane table (insertPlan/patchPlan/byThread) + PINNED lifecycle enum
provides:
  - "plans.candidates / plans.pendingValid / plans.greetingName transient fields (all optional, no migration)"
  - "internal.plans.writeCandidates — content-plane candidate writer"
  - "internal.plans.clearCandidates — wipe-on-pick (unsets both transient fields)"
  - "patchPlan accepts greetingName so the resolve path persists the drafter greeting"
affects: [03.2-04-cockpit-resolution, 03.2-05-resolution-card, drafter-greeting]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Transient content-plane store: raw candidates held on plans row, wiped on pick — 'no contacts cache at rest'"
    - "Wipe via patch({field: undefined}) removes the field in Convex (structural invariant, not a soft-delete)"

key-files:
  created: []
  modified:
    - packages/backend/convex/schema.ts
    - packages/backend/convex/plans.ts

key-decisions:
  - "candidates/pendingValid/greetingName all v.optional → Pitfall-7 optional-on-read, zero migration"
  - "clearCandidates does NOT clear greetingName — it must survive to the draft turn"
  - "No client-callable candidate reader added — the card reads candidates off the existing byThread row (Plan 05)"

patterns-established:
  - "Content-plane-only transient storage: raw fetched candidate content lives on plans, never in audit/DLQ (CLAUDE.md §4)"
  - "Shared CANDIDATES validator const in plans.ts mirrors the schema shape (single source, no duplication)"

requirements-completed: [CKPT-01]

# Metrics
duration: 3min
completed: 2026-07-12
---

# Phase 03.2 Plan 02: Transient Candidate Fields Summary

**plans-row transient name-resolution store (candidates/pendingValid/greetingName) + writeCandidates/clearCandidates internal mutations — the wipe-on-pick "no contacts cache at rest" invariant made structural.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-12T16:50:38Z
- **Completed:** 2026-07-12T16:53:27Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added three OPTIONAL transient fields to the `plans` table (`candidates` array-of-name-with-matches, `pendingValid` string array, `greetingName`) — no migration (all optional-on-read, Pitfall-7).
- `internal.plans.writeCandidates` patches candidates + pendingValid onto the content plane (cockpit calls after a name search).
- `internal.plans.clearCandidates` unsets BOTH transient fields via `patch({ candidates: undefined, pendingValid: undefined })` — the wipe-on-pick; deliberately leaves `greetingName` so it survives to the draft turn.
- Extended `patchPlan` args with `greetingName: v.optional(v.string())` so the resolve path persists the drafter greeting through the existing drop-undefined patch.

## Task Commits

1. **Task 1: transient candidate fields on the plans table** - `169c8cc` (feat)
2. **Task 2: writeCandidates + clearCandidates internal mutations** - `0d5374e` (feat)

## Files Created/Modified
- `packages/backend/convex/schema.ts` - Added candidates/pendingValid/greetingName optional fields to the `plans` table (content-plane-only comment, §4).
- `packages/backend/convex/plans.ts` - Added shared `CANDIDATES` validator const, `writeCandidates`/`clearCandidates` internal mutations, and `greetingName` to `patchPlan` args.

## Decisions Made
- All new fields `v.optional` → zero migration (matches the existing safeText/planId optional-on-read precedent).
- No index on candidates (read via the existing `by_thread` plan row, never queried standalone).
- `clearCandidates` scoped to the two transient fields only; `greetingName` survives to the draft turn.
- Kept every writer `internalMutation` — no client-callable candidate reader; the card reads candidates off the `byThread` row (Plan 05).
- Reused a single `CANDIDATES` validator const rather than duplicating the nested object shape at each call site.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The `pnpm --filter @pikar/backend test -- llmRedaction` verify command does not pass the filter through to vitest (runs the full suite). Ran `vitest run llmRedaction cockpit` directly instead — all target files green (llmRedaction 5, cockpitDraft 3, cockpit 8).
- The full suite shows one PRE-EXISTING failure: `audit.test.ts` ("auditCounts is not registered") — silently red since the Phase-2 aggregate (convex-test needs `t.registerComponent`), documented in STATE.md and unrelated to this plan's changes. Out of scope; not touched.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Schema shape + writers are ready for Plan 04 (cockpit search → writeCandidates) and Plan 05 (resolution card reading candidates off `byThread`, calling clearCandidates on pick).
- The candidate object shape mirrors @pikar/core ContactMatch/NameCandidates (Plan 01) as a Convex validator — Plan 04/05 must keep them in sync.

## Self-Check: PASSED

---
*Phase: 03.2-inbox-reading*
*Completed: 2026-07-12*
