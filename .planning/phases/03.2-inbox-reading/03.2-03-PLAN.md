---
phase: 03.2-inbox-reading
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/gmail.ts
autonomous: true
requirements: [CKPT-01]
must_haves:
  truths:
    - "The agent searches the mailbox via the already-granted gmail.modify scope (messages.list + format=metadata), HEADERS ONLY — bodies are never fetched"
    - "Each search writes exactly one mailbox.searched audit event with payload { queryHash, resultCount } — no names/addresses/subjects/message-ids"
    - "A SMOKE:: name returns deterministic offline header records so the read path runs with no live mailbox"
    - "A read-time token/refresh failure returns a reauth signal WITHOUT throwing (the caller lights the banner + asks)"
    - "send and search share ONE token-refresh root (freshAccessToken)"
  artifacts:
    - path: "packages/backend/convex/gmail.ts"
      provides: "search internalAction (explicit return type) + extracted freshAccessToken + SMOKE fixture + mailbox.searched audit"
      contains: "export const search"
  key_links:
    - from: "search"
      to: "Gmail messages.list + messages.get?format=metadata"
      via: "raw fetch, headers-only"
      pattern: "format=metadata"
    - from: "search"
      to: "internal.audit.log (mailbox.searched)"
      via: "refs-only { queryHash, resultCount }"
      pattern: "mailbox\\.searched"
    - from: "search & send"
      to: "freshAccessToken"
      via: "one shared refresh root"
      pattern: "freshAccessToken"
---

<objective>
Add a headers-only Gmail `search` action to the EXISTING `gmail.ts` `"use node"` module (never a
second node module — that re-triggers the TS circular-inference cliff, guidelines §96 / cockpit
playbook invariant). Extract the send-path token refresh into a shared `freshAccessToken` root
both actions use, add a deterministic `SMOKE::` offline fixture, and write the one refs-only
`mailbox.searched` audit event per search — co-located with the read (like the existing
`gmail.sent` write), which keeps `cockpit.ts` free of any `audit.log` call (its llmRedaction
invariant stays intact).

Purpose: delivers the SC1/CKPT-01 read capability (scoped to the requesting user, nothing sent)
and the SC3 audit shape. The action returns RAW header records; parsing/ranking is the pure
`@pikar/core` job composed by cockpit (Plan 04) — so this file has NO `@pikar/core` dependency and
stays a thin fetch adapter (§1), avoiding a same-wave import on Plan 01.
Output: `internal.gmail.search` + the SMOKE fixture Plan 04/05 drive.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-inbox-reading/03.2-CONTEXT.md
@.planning/phases/03.2-inbox-reading/03.2-RESEARCH.md
@packages/backend/convex/gmail.ts
@packages/backend/convex/gmailAuth.ts
@packages/backend/convex/audit.ts
@packages/backend/convex/lib/hash.ts

<interfaces>
<!-- The search action's return contract (Plan 04 consumes it). Explicit return type is MANDATORY
     (guidelines §96) — infer nothing through the internal graph. HeaderRecord mirrors @pikar/core
     but is defined LOCALLY here (no cross-package import) so this stays a thin fetch adapter. -->

```typescript
type HeaderRecord = { from?: string; to?: string; cc?: string; subject?: string; date?: string };
type SearchResult =
  | { ok: true; records: HeaderRecord[] }
  | { ok: false; reason: "not_connected" | "reauth" };
export const search = internalAction({ args: { tenantId, name, correlationId }, handler: ... Promise<SearchResult> });

// Existing refresh block in send() → extract:
async function freshAccessToken(ctx, tenantId: string):
  Promise<{ ok: true; token: string } | { ok: false; reason: "not_connected" | "refresh_failed" }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extract freshAccessToken (one refresh root for send + search)</name>
  <files>packages/backend/convex/gmail.ts</files>
  <action>
    Lift `send()`'s token block (getTokens → POST refresh_token grant → updateAccess) into a module
    helper `freshAccessToken(ctx, tenantId)` returning the discriminated result above (research
    §"Extracted shared refresh"). Refactor `send()` to call it: the `{ok:false, reason:"not_connected"}`
    and `{reason:"refresh_failed"}` branches keep send's EXISTING behavior verbatim —
    `internal.pipeline.setStatus(awaiting_reauth)` + `return { delivered:false, reason }` (do NOT
    throw; that burns retrier attempts and DLQs an approved draft). The `SMOKE::fail` sentinel and
    the actual send fetch stay unchanged. This is a pure refactor — send's smoke:fanout behavior
    must be identical (root-cause discipline, ponytail rung 2).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>`freshAccessToken` exists; `send` uses it and its awaiting_reauth-not-throw behavior is
    unchanged; backend test suite + typecheck green. (Live send regression = smoke:fanout, run at
    the phase gate.)</done>
</task>

<task type="auto">
  <name>Task 2: search internalAction (headers-only, SMOKE fixture, mailbox.searched audit)</name>
  <files>packages/backend/convex/gmail.ts</files>
  <action>
    Add `export const search = internalAction({ args:{ tenantId:v.string(), name:v.string(),
    correlationId:v.string() }, handler })` with the EXPLICIT `Promise<SearchResult>` return type
    (guidelines §96 — never infer through the internal graph).
    Flow:
    1. `SMOKE::` fixture FIRST: `if (name.startsWith("SMOKE::"))` return `{ ok:true, records:[...] }`
       with 2–3 deterministic HeaderRecords (e.g. `from: "Sarah Smoke <sarah@example.com>",
       subject:"Invoice", date:"..."` and a second lower-frequency match) — carries no real PII,
       real names never start with the sentinel. Still audit (step 5) so the E2E gets an audit row.
    2. `const access = await freshAccessToken(ctx, tenantId)`; on `!ok` return
       `{ ok:false, reason: access.reason === "not_connected" ? "not_connected" : "reauth" }`
       (the caller lights the banner + falls back to asking — never throw, never dead-end).
    3. `messages.list?maxResults=20&q=(from:<gq(name)> OR to:<gq(name)>) newer_than:1y` (gq = quote
       names with spaces). Parse `{ messages:[{id}] }`.
    4. ZERO-MATCH WIDEN ONCE: if empty, re-list the same `q` WITHOUT `newer_than:` a single time.
       Still empty → `records:[]` (caller falls back to asking).
    5. `Promise.all` a `messages.get/<id>?format=metadata&metadataHeaders=From&…=To&…=Cc&…=Subject
       &…=Date` per id (headers ONLY — NEVER `format=full`/`raw`; that would pull the body). Map each
       message's `payload.headers` into a HeaderRecord. `ponytail:` note that Promise.all over the
       ~20 cap is well within quota; a p-limit is the upgrade if the cap ever rises (research Open-Q 2).
    6. Audit EXACTLY ONCE: `ctx.runMutation(internal.audit.log, { tenantId, correlationId,
       eventType:"mailbox.searched", actor:"system", payload:{ queryHash: await contentHash(name),
       resultCount } })` where `resultCount` = the list hit count. Import `contentHash` from
       `./lib/hash`. Payload is refs-only (CLAUDE.md §4 / SC3) — NO name/address/subject/message-id.
       Do this on BOTH the SMOKE and live paths (records.length as resultCount for SMOKE).
    Return `{ ok:true, records }`. Add the endpoint constants (LIST/GET base) near the existing
    SEND_ENDPOINT.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- llmRedaction</automated>
  </verify>
  <done>`internal.gmail.search` compiles with an explicit return type; SMOKE:: returns deterministic
    records; a token failure returns `{ok:false, reason:"reauth"|"not_connected"}` without throwing;
    only `format=metadata` is used (no full/raw); exactly one `mailbox.searched` audit with
    `{queryHash, resultCount}`. The mailbox.searched refs-only assertion (Plan 04's llmRedaction
    extension) passes against this file.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green; backend typecheck green (explicit return type — no
  sibling action collapses to `any`; grep `draftCockpit`/`send` still typed).
- Static: `grep format=metadata` present, `grep format=full` / `format=raw` ABSENT (headers-only).
- Live regression (phase gate, not per-plan): `pnpm --filter @pikar/backend smoke:fanout` still
  green (freshAccessToken refactor did not change send).
</verification>

<success_criteria>
The agent can search the requesting user's mailbox headers-only via gmail.modify, degrade
gracefully on auth failure, run offline under SMOKE::, and record each search as a refs-only
`{queryHash, resultCount}` audit — SC1 + SC3, nothing sent.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inbox-reading/03.2-03-SUMMARY.md`.
</output>
