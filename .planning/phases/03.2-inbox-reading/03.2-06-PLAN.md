---
phase: 03.2-inbox-reading
plan: 06
type: execute
wave: 4
depends_on: [03.2-01, 03.2-02, 03.2-03, 03.2-04, 03.2-05]
files_modified:
  - docs/playbooks/cockpit.md
  - docs/playbooks/watch.json
autonomous: false
requirements: [CKPT-01]
user_setup:
  - service: google-gmail
    why: "A real mailbox read needs the human's Google OAuth (Testing-mode token) — Claude cannot complete the consent"
    dashboard_config:
      - task: "Connect Gmail in /connect-gmail, then type a real correspondent's first name in the cockpit"
        location: "app /connect-gmail then /dashboard/workspace"
must_haves:
  truths:
    - "docs/playbooks/cockpit.md documents the gmail.search read path, the resolution flow, resolveRecipients, and the transient candidate fields — with a bumped Last verified line"
    - "watch.json covers gmail.ts under cockpit.md so future changes to the search action are protected (§9)"
    - "A real mailbox read resolves a person from prior correspondence and NOTHING sends as a side effect (CKPT-01 human-verify)"
  artifacts:
    - path: "docs/playbooks/cockpit.md"
      provides: "inbox-reading additions (Data flow, Key files, Invariants) + bumped Last verified"
      contains: "gmail.search"
    - path: "docs/playbooks/watch.json"
      provides: "gmail.ts added to the cockpit.md watched prefixes"
      contains: "gmail.ts"
  key_links:
    - from: "watch.json cockpit.md"
      to: "packages/backend/convex/gmail.ts"
      via: "watched prefix (protects the search action)"
      pattern: "gmail\\.ts"
---

<objective>
Close the phase per CLAUDE.md §9: update the cockpit playbook to document the new mailbox-read path
(gmail.search, the resolution loop, resolveRecipients, the transient candidate fields, the
read-auth fallback) and bump its `Last verified`; register `gmail.ts` under cockpit.md's watch so
the search action is protected going forward. Then run the CKPT-01 human-verify — a REAL Gmail read
resolving a real correspondent with nothing sent.

Purpose: the playbook + watch update is the §9 definition-of-done and is what clears the
Stop-hook for this phase's accumulated watched-file changes (cockpit.ts/plans.ts/emailIntent.ts/
llm.ts all changed under cockpit.md's watch — this closing touch blesses them). The human-verify
is the one thing Claude cannot automate: a live mailbox read behind Google OAuth.
Output: an updated, verified playbook and a signed-off CKPT-01.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-inbox-reading/03.2-CONTEXT.md
@.planning/phases/03.2-inbox-reading/03.2-VALIDATION.md
@docs/playbooks/cockpit.md
@docs/playbooks/watch.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: document the read path in cockpit.md + register gmail.ts in watch.json</name>
  <files>docs/playbooks/cockpit.md, docs/playbooks/watch.json</files>
  <action>
    Update `docs/playbooks/cockpit.md`:
    - Key files: add `gmail.ts → search` (the headers-only mailbox read; freshAccessToken shared
      with send; mailbox.searched refs-only audit) and note the resolution additions in `cockpit.ts`
      (`resolveRecipients`, comma/"and" tokenizer) + `plans.ts` (`writeCandidates`/`clearCandidates`)
      + `emailIntent.ts` (`needs_resolution`, `parseAddress`, `rankCandidates`) + the transient
      `plans.candidates`/`pendingValid`/`greetingName` schema fields.
    - Data flow: insert the resolution step between slot-filling and propose (name segment →
      gmail.search → rankCandidates → writeCandidates → resolution card → pick → resolveRecipients →
      recipients + greetingName, candidates wiped; group names declined; zero-match / read-auth →
      ask fallback + gmail_reconnect notification lights ReconnectBanner).
    - Invariants: add "mailbox reads are headers-only (format=metadata, never bodies)"; "one
      mailbox.searched audit per search, `{queryHash, resultCount}` only — no names/addresses/
      subjects (SC3)"; "the search audit lives in gmail.ts so cockpit.ts stays audit.log-free";
      "the resolved display name is the ONLY mailbox-derived value that reaches the drafter (no
      header hints)"; "candidates are transient, wiped on pick — no contacts cache at rest";
      "no new LLM tool-loop — resolution is a deterministic Gmail query (DECISION #2)".
    - How to verify: add `pnpm exec playwright test cockpit-resolve` and the mailbox.searched
      redaction assertion.
    - Bump the `> Last verified:` line to today against this phase.
    In `docs/playbooks/watch.json`: add `"packages/backend/convex/gmail.ts"` to the `cockpit.md`
    array (research §Playbook coverage gap 1 — the search action is a cockpit-path behavior and
    should be watched). Keep JSON valid.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('docs/playbooks/watch.json','utf8')); console.log('watch.json valid')"</automated>
  </verify>
  <done>cockpit.md documents the read path + resolution flow + new invariants and its Last verified
    is bumped; watch.json is valid JSON with gmail.ts under cockpit.md. Finishing this turn does not
    trip the §9 Stop hook (the playbook touch blesses the phase's watched-file changes).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CKPT-01 human-verify — real mailbox read, nothing sends</name>
  <files>(none — human verification, no file change)</files>
  <action>
    HUMAN CHECKPOINT (no code). Present the how-to-verify steps below to the user and pause for the
    resume signal. Claude has already automated everything automatable (the offline SMOKE:: E2E in
    Plan 05); this checkpoint confirms the ONE thing Claude cannot do — a live mailbox read behind
    Google OAuth — and that nothing sends as a side effect of reading.
  </action>
  <what-built>The full inbox-reading loop over a REAL mailbox: a name in the recipients slot triggers
    a headers-only gmail.modify search, a resolution card lists the real contact(s) with hints, a
    pick folds the real address into the plan, and the greeting personalizes — audited as
    `{queryHash, resultCount}` only, with nothing sent as a side effect of reading.</what-built>
  <how-to-verify>
    1. Ensure the local stack is running (convex dev non-`--once` + next dev :3111) and Gmail is
       connected for the signed-in user (/connect-gmail).
    2. Open /dashboard/workspace. In the composer type a REAL correspondent's first name (someone you
       have prior email with), e.g. "Alex".
    3. Confirm: a "Searching your mailbox…" chip shows, then a resolution card lists that person's
       real address(es) with a hint (msg count / last contacted). Nothing has been sent.
    4. Click a contact chip + confirm → the conversation advances (subject question) and the picked
       real address appears on the forming PLAN; complete subject/body/mode → the drafted body opens
       "Hi Alex,".
    5. Confirm NO email was delivered by reading (no REPORT, no send) — reading never sends.
    6. (Optional, refs-only) inspect the audit for a `mailbox.searched` row with only
       `{queryHash, resultCount}` — no name/address/subject.
  </how-to-verify>
  <verify>Human confirms the real mailbox read resolved a real person and nothing was sent.</verify>
  <done>CKPT-01 approved by the user: a real correspondent resolved from prior mail, PLAN formed, zero sends on read.</done>
  <resume-signal>Type "approved" or describe what surfaced (wrong contact, sent something, no card, banner didn't light on a stale token).</resume-signal>
</task>

</tasks>

<verification>
- `docs/playbooks/watch.json` parses; `gmail.ts` listed under `cockpit.md`.
- `cockpit.md` Last verified bumped; read-path invariants present.
- CKPT-01 human-verify approved.
</verification>

<success_criteria>
The cockpit playbook reflects the shipped inbox-reading path and its invariants, the search action
is watch-protected, and a real mailbox read has been confirmed to resolve a person with nothing
sent — CKPT-01 done.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inbox-reading/03.2-06-SUMMARY.md`.
</output>
