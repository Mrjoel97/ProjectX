---
phase: 03.2-inbox-reading
plan: 04
type: execute
wave: 2
depends_on: [03.2-01, 03.2-02, 03.2-03]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/llmRedaction.test.ts
autonomous: true
requirements: [CKPT-01]
must_haves:
  truths:
    - "A recipients segment without @ triggers a bounded Gmail search BEFORE the PLAN is proposed — no LLM tool-loop (DECISION #2 preserved)"
    - "Recipients parse as comma/'and'-separated segments so 'Sarah Chen' stays whole; space-separated emails is an accepted behavior break with a regression test"
    - "The resolved display name (only) reaches the drafter for the greeting; header hints (subject/date/count) never do"
    - "A read-time auth failure inserts a gmail_reconnect notification and falls back to asking for the address (never dead-ends)"
    - "The only new audit is mailbox.searched (in gmail.ts); cockpit.ts still writes no audit.log"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "comma/'and' tokenizer, resolve turn (search→rank→writeCandidates), resolveRecipients action, greetingName threading, reauth fallback"
      contains: "resolveRecipients"
    - path: "packages/backend/convex/llm.ts"
      provides: "draftCockpit optional greetingName arg → 'Hi <name>,' greeting"
      contains: "greetingName"
    - path: "packages/backend/convex/cockpit.test.ts"
      provides: "parseAnswer segmentation tests incl. space-separated-emails regression + resolve fold"
    - path: "packages/backend/convex/llmRedaction.test.ts"
      provides: "mailbox.searched refs-only assertion + drafter greetingName-only assertion"
  key_links:
    - from: "sendCockpitMessage (resolve turn)"
      to: "internal.gmail.search"
      via: "ctx.runAction per unresolved name"
      pattern: "gmail\\.search|\\.search,"
    - from: "resolve turn"
      to: "internal.plans.writeCandidates / clearCandidates"
      via: "persist then wipe candidates"
      pattern: "writeCandidates"
    - from: "cockpit"
      to: "internal.llm.draftCockpit (greetingName)"
      via: "resolved display name only"
      pattern: "greetingName"
    - from: "read-auth failure"
      to: "internal.notifications.notify (gmail_reconnect)"
      via: "light the banner + ask fallback"
      pattern: "gmail_reconnect"
---

<objective>
Wire the deterministic resolution loop into the cockpit: change `parseAnswer`'s recipients
tokenizer to comma/"and" segmentation, run a bounded Gmail search when `emailIntent` flags a name,
rank the raw header records via `@pikar/core`, persist candidates transiently, add a
`resolveRecipients` action that folds a pick back into the conversation, and thread the resolved
display name (only) into the drafter's greeting. Graceful-degrade on read-auth failure. NO new LLM
tool-loop — the LLM is still called exactly once, for the body draft (DECISION #2).

Purpose: this is the SC2 seam that turns "email Sarah about the invoice" into a resolved address
without sending. Extend the two governance tests to lock the audit + greeting invariants (SC3).
Output: `sendCockpitMessage` resolve branch + `api.cockpit.resolveRecipients` Plan 05's card calls.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-inbox-reading/03.2-CONTEXT.md
@.planning/phases/03.2-inbox-reading/03.2-RESEARCH.md
@.planning/phases/03.2-inbox-reading/03.2-VALIDATION.md
@packages/backend/convex/cockpit.ts
@packages/backend/convex/cockpit.test.ts
@packages/backend/convex/llm.ts
@packages/backend/convex/llmRedaction.test.ts
@packages/backend/convex/notifications.ts
@packages/backend/convex/plans.ts

<interfaces>
<!-- From Plan 01 (@pikar/core): nextQuestion now returns { kind:"resolve_recipients"; names } and
     a group defer signal; Answer gains { slot:"resolution"; picks:{name;address;displayName?}[] };
     applyAnswer holds same-turn valids in pendingValid; parseAddress + rankCandidates(name,records)
     → ContactMatch[]. From Plan 02: internal.plans.writeCandidates({planId,candidates,pendingValid}),
     internal.plans.clearCandidates({planId}), patchPlan accepts greetingName. From Plan 03:
     internal.gmail.search({tenantId,name,correlationId}) → { ok:true; records } | { ok:false; reason }.
     Existing: internal.notifications.notify({tenantId,kind,message}); internal.llm.draftCockpit. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: comma/"and" tokenizer + toIntentState projection of candidates</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts</files>
  <behavior>
    - `parseAnswer({kind:"ask_recipients"}, "Sarah, john@x.com")` → `{slot:"recipients", value:["Sarah","john@x.com"]}`.
    - `parseAnswer(..., "Sarah and Bob")` → `["Sarah","Bob"]`. `parseAnswer(..., "Sarah Chen")` → `["Sarah Chen"]` (intra-segment spaces preserved).
    - `parseAnswer(..., "a@b.com, c@d.com")` → `["a@b.com","c@d.com"]` (two emails).
    - REGRESSION (Pitfall 3, accepted break): `"a@b.com c@d.com"` (space-separated) → ONE segment
      `["a@b.com c@d.com"]` (a malformed single address that bounces to the existing re-ask). Assert
      this explicitly so the behavior change is documented, not silent.
    - `toIntentState(plan)` maps `plan.candidates` → `pendingResolution` names and `plan.pendingValid`
      → `pendingValid`, so a resolution card persists across turns (nextQuestion keeps returning
      resolve_recipients until the pick).
  </behavior>
  <action>
    Replace the recipients `t.split(/[\s,]+/)` with comma/"and" segmentation: split on `,` and the
    word `and` (case-insensitive, word-boundary), trim, drop empties — intra-segment spaces stay.
    `ponytail:` comment naming the accepted space-separated-emails behavior break. Extend
    `toIntentState` to project the new transient fields into the pure state (per Plan 01's shape).
    Add `questionText` copy for `resolve_recipients` and the group-defer decline ("Group lists like
    'team' aren't supported yet — please give the explicit addresses.").
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpit</automated>
  </verify>
  <done>All parseAnswer cases above pass incl. the space-email regression; toIntentState projects
    candidates/pendingValid; existing cockpit.test.ts cases still green.</done>
</task>

<task type="auto">
  <name>Task 2: resolve turn (search→rank→persist), resolveRecipients action, greetingName, reauth fallback</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/llm.ts</files>
  <action>
    In `sendCockpitMessage`, after folding the answer: if `nextQuestion(newState).kind ===
    "resolve_recipients"` (name(s) present) run the resolution turn BEFORE any draft/propose:
    - For each unresolved name, `ctx.runAction(internal.gmail.search, { tenantId, name, correlationId: tid })`.
      On `{ok:false, reason:"reauth"|"not_connected"}`: `ctx.runMutation(internal.notifications.notify,
      { tenantId, kind:"gmail_reconnect", message:"…reconnect…" })` AND fall back — save an assistant
      message asking for the address directly, return (never dead-end). On `{ok:true, records}` call
      `rankCandidates(name, records)` (from `@pikar/core`).
    - `ctx.runMutation(internal.plans.writeCandidates, { planId, candidates:[{name,matches}...],
      pendingValid })` (pendingValid = the same-turn valids held by applyAnswer). Save an assistant
      message ("Searching your mailbox…" / "Found some matches — pick a contact →") and return. The
      card (Plan 05) renders off the persisted candidates. If a name search yields zero matches after
      the widen, fall back to asking for that address (still write the other sections if any).
    - Group defer (nextQuestion group signal): save the decline copy + re-ask recipients; return.
    Add `resolveRecipients` (a `tenantAction`, mirroring sendCockpitMessage's tail so it can reach
    `ready` → draft): args `{ threadId, picks: {name,address,displayName?}[] }`. Load the plan, build
    the `resolution` Answer, `applyAnswer` → recipients folded + pendingValid folded + greetingName
    captured. Persist via `patchPlan({ planId, recipients, greetingName })` then
    `clearCandidates({ planId })` (wipe on pick). Then run the SHARED advance tail (extract a small
    `advance(ctx, tid, planId, state)` helper reused by both actions): nextQuestion → ask next OR
    (ready) scanText(bodyIntent) → draftCockpit → proposeEmailPlan. Re-resolution ("no, the other
    Sarah") arrives as a normal recipients turn through sendCockpitMessage — no new machinery.
    In `llm.ts`, add an optional `greetingName: v.optional(v.string())` arg to `draftCockpit`; when
    present, prepend a greeting instruction to the prompt (e.g. open the body "Hi <greetingName>,").
    Pass ONLY the display name — NEVER lastSubject/lastDateMs/count (SC3). cockpit reads
    `plan.greetingName` at the draft step and passes it. Keep draftCockpit's SMOKE:: short-circuit +
    explicit return type intact.
    CRITICAL: cockpit.ts must NOT call `internal.audit.log` — the search audit lives in gmail.ts
    (Plan 03). Keep cockpit's "no audit.log" invariant (llmRedaction static scan).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpit llmRedaction</automated>
  </verify>
  <done>sendCockpitMessage runs the search + persists candidates on a name; resolveRecipients folds a
    pick, wipes candidates, threads greetingName, and advances to draft/propose; read-auth failure
    notifies + asks; draftCockpit takes greetingName; cockpit.ts contains no `audit.log`; backend
    typecheck green (no sibling action collapses to `any`).</done>
</task>

<task type="auto">
  <name>Task 3: governance tests — mailbox.searched refs-only + drafter greetingName-only</name>
  <files>packages/backend/convex/llmRedaction.test.ts, packages/backend/convex/cockpit.test.ts</files>
  <action>
    Extend `llmRedaction.test.ts` (static on-disk scan, the existing pattern):
    - Add `gmail.ts` to the scan and assert the `mailbox.searched` audit payload literal is EXACTLY
      `{ queryHash, resultCount }` — regex the `eventType: "mailbox.searched"` audit call's `payload:`
      object and assert it contains `queryHash`+`resultCount` and MATCHES NONE of
      `name|address|subject|messageId|from|to|cc` (SC3).
    - Assert `draftCockpit`'s call surface passes at most `greetingName` of the mailbox-derived fields
      — the `draftCockpit` args/usage must NOT reference `lastSubject|lastDateMs|count|matches` (no
      header hints to the LLM, SC3).
    - Keep/verify the existing "cockpit.ts + plans.ts emit no audit.log" test still passes (audit is
      in gmail.ts, not cockpit.ts) — if the existing regex needs a comment update, keep the assertion
      semantics: cockpit.ts must contain no `audit.log`.
    In `cockpit.test.ts` add a `resolveRecipients` unit case if convex-test can drive it to the
    pre-draft fold (assert recipients folded + candidates cleared + greetingName set); if the draft
    tail requires the action cache/agent components convex-test can't load (see the file's header
    note), assert only up to the fold via a seeded plan + `withIdentity`, mirroring the existing
    executePlan gate tests.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>llmRedaction asserts mailbox.searched is refs-only and the drafter gets greetingName only;
    cockpit.test.ts resolve fold + parseAnswer regression green; full backend suite green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green; `pnpm --filter @pikar/core test` green.
- Backend typecheck green — `search`/`draftCockpit`/`send` all keep explicit types (§96).
- Static invariants: cockpit.ts has no `audit.log`; draftCockpit receives no header hints.
</verification>

<success_criteria>
A name in the recipients slot resolves through a bounded, audited, headers-only search into a
confirmed address, the greeting personalizes from the display name only, and a read failure
degrades gracefully — SC2 + SC3, DECISION #2 preserved (one LLM call, the draft).
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inbox-reading/03.2-04-SUMMARY.md`.
</output>
