---
phase: 03.2-inbox-reading
plan: 06
subsystem: cockpit
tags: [playbook, governance, human-verify, inbox-reading, gmail]
requires: [03.2-01, 03.2-02, 03.2-03, 03.2-04, 03.2-05]
provides:
  - "cockpit.md documents the mailbox-read path (gmail.search headers-only, resolution flow, resolveRecipients, transient candidate fields) with Last verified → 03.2-06"
  - "watch.json protects packages/backend/convex/gmail.ts under cockpit.md (§9)"
  - "CKPT-01 human-verified: a real mailbox read resolves a correspondent with zero sends"
affects:
  - docs/playbooks/cockpit.md
  - docs/playbooks/watch.json
tech-stack:
  added: []
  patterns:
    - "§9 phase-closing playbook touch blesses the phase's accumulated watched-file changes"
key-files:
  created: []
  modified:
    - docs/playbooks/cockpit.md
decisions:
  - "watch.json already registered gmail.ts under cockpit.md (prior wave) — no duplicate edit; the success criterion was already met"
metrics:
  tasks: 2
  files: 1
  duration_min: 4
  completed: "2026-07-12"
---

# Phase 3.2 Plan 06: Close Inbox-Reading Phase (Playbook + CKPT-01) Summary

The §9 definition-of-done for Phase 3.2: the cockpit playbook now documents the shipped
mailbox-read path and its invariants, `gmail.ts` is watch-protected, and CKPT-01 is confirmed
by a real mailbox read that resolved a correspondent with nothing sent.

## What was done

### Task 1 — playbook + watch (commit `4348527`)

`docs/playbooks/cockpit.md`:
- Bumped `> Last verified:` to `2026-07-12 against 03.2-06`, marking the phase closed and
  blessing the phase's accumulated watched-file changes (cockpit.ts / plans.ts / emailIntent.ts
  / llm.ts / gmail.ts) so the §9 Stop hook clears.
- Added invariant: **the search audit lives in `gmail.ts`** — the sole `mailbox.searched` write
  is emitted by `gmail.search` itself (refs-only `{queryHash, resultCount}`, SC3), so `cockpit.ts`
  stays `audit.log`-free and there is exactly one audit per search.
- Noted the `mailbox.searched` refs-only redaction assertion in **How to verify** (payload is
  exactly `{queryHash, resultCount}` — no name/address/subject).

The read path itself (Key files `gmail.ts → search`, the Data-flow resolution step 2a, and the
headers-only / display-name-only / no-LLM-tool-loop / transient-candidates invariants) was already
documented in 03.2-05; this plan closed the remaining §9 gaps rather than re-authoring them
(ponytail — reuse what exists).

`docs/playbooks/watch.json`: **already registered** `packages/backend/convex/gmail.ts` under
`cockpit.md` (added in an earlier wave when the search action landed). Validated as valid JSON;
the plan's success criterion was already satisfied, so no duplicate edit was made.

### Task 2 — CKPT-01 human-verify (APPROVED)

Blocking human checkpoint — the one thing Claude cannot automate (a live mailbox read behind
Google OAuth). Approved by the user by direct observation against the live backend
(`:3210`/`:3211`, functions pushed, skills seeded):

- Typed a real correspondent's first name → the **"Searching your mailbox…"** chip showed.
- The **resolution card** listed a real contact resolved from prior correspondence.
- Picking a contact **folded the real address into the PLAN**.
- The draft opened **"Hi &lt;name&gt;,"**.
- **Nothing was sent** as a side effect of reading (no REPORT, no delivery).

The read path — `gmail.search` (headers-only) → `rankCandidates` → resolution card →
`resolveRecipients` → PLAN + greeting — is verified working end-to-end over a real mailbox
(CKPT-01, SC2/SC3).

## Deviations from Plan

**1. [Rule 1 — scope correction] watch.json already covered gmail.ts**
- **Found during:** Task 1
- **Issue:** The plan directs adding `gmail.ts` to `watch.json` under `cockpit.md`, but it was
  already registered in a prior wave.
- **Fix:** No edit — verified presence + valid JSON instead of duplicating the entry.
- **Files modified:** none (watch.json unchanged)
- **Commit:** n/a

## Authentication / human gates

CKPT-01 is a human OAuth gate by design (a live Gmail read cannot be automated). Handled as
normal checkpoint flow: presented the how-to-verify steps, paused, resumed on the user's
"APPROVED" with the observed evidence recorded above.

## Verification

- `docs/playbooks/watch.json` parses; `gmail.ts` listed under `cockpit.md`. ✓
- `cockpit.md` `Last verified` bumped to 03.2-06; read-path invariants present (incl. the new
  audit-location invariant). ✓
- CKPT-01 human-verify APPROVED against the live backend. ✓

## Self-Check: PASSED

- docs/playbooks/cockpit.md — FOUND
- .planning/phases/03.2-inbox-reading/03.2-06-SUMMARY.md — FOUND
- commit 4348527 — FOUND
