---
phase: 03.2-inbox-reading
verified: 2026-07-12T22:35:00Z
status: passed
score: 6/6 must-haves verified
---

# Phase 3.2: Inbox Reading Verification Report

**Phase Goal:** The agent can search and read the connected mailbox to find people and context, so a request can reference real recipients and prior threads instead of only user-typed input.
**Verified:** 2026-07-12
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A no-`@` recipients segment is flagged as a NAME to resolve (never a bounced address); empty/malformed segments still reject; group words defer | ✓ VERIFIED | `packages/core/src/emailIntent.ts` 5-step classification (empty→rejected, valid, malformed→rejected, group→`groupDeferred`, name→`pendingResolution`); `emailIntent.test.ts` 22 tests green (`pnpm --filter @pikar/core test`: 49/49 total incl. `fallback`/`tokenExpiry`/`validateSubmit`/`buildTelemetry`) |
| 2 | Gmail search reads the user's mailbox headers-only (no body) via the granted `gmail.modify` scope, scoped to the requesting tenant | ✓ VERIFIED | `packages/backend/convex/gmail.ts` `internal.gmail.search` — `format=metadata` on `messages.get`, `format=full`/`raw` absent; `freshAccessToken(ctx, tenantId)` shared refresh root scopes the read to the tenant's stored token |
| 3 | Mailbox reads surface people/context into the guided conversation (resolution card → pick → PLAN), and nothing is sent as a side effect | ✓ VERIFIED | `cockpit.ts` resolution turn (`sendCockpitMessage`: `gmail.search`→`rankCandidates`→`writeCandidates`, before any draft) + `resolveRecipients` tenantAction (folds pick, clears candidates, threads `greetingName`, calls shared `advance` — never `executePlan`/send); `cards.tsx` `ResolutionCard` renders chips and calls `resolveRecipients` on confirm; `cockpit-resolve.spec.ts` E2E asserts exactly one Approve and **no REPORT card** (nothing sent) |
| 4 | Read access is audited with refs/ids/counts only — no raw content in any audit/DLQ payload | ✓ VERIFIED | `gmail.ts` writes the sole `mailbox.searched` event: `{ queryHash: contentHash(name), resultCount }` — no name/address/subject/message-id. `cockpit.ts` contains **zero** `audit.log` calls (grep confirmed) — the orchestration seam never touches the audit plane. `llmRedaction.test.ts` statically asserts the payload shape and that `draftCockpit`'s code surface carries `greetingName` only (no `lastSubject`/`lastDateMs`/`count`/`matches`) |
| 5 | A resolved contact's display name personalizes the draft ("Hi <name>,") without leaking header hints to the LLM | ✓ VERIFIED | `llm.ts` `draftCockpit` optional `greetingName` arg prepends the greeting on both primary and SMOKE/fallback paths; `cockpit-resolve.spec.ts` asserts "Hi Sarah" in the offline E2E; `llmRedaction.test.ts` asserts no header-hint fields reach the drafter |
| 6 | CKPT-01 is confirmed against a real, live mailbox (not just offline fixtures) | ✓ VERIFIED (human) | 03.2-06-SUMMARY.md records the user's live-backend APPROVED checkpoint: typed a real first name → "Searching your mailbox…" chip → resolution card listed a real contact from prior correspondence → pick folded the real address into the PLAN → draft opened "Hi <name>," → nothing sent (no REPORT/delivery) |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/core/src/emailIntent.ts` | name-resolution seam + `parseAddress`/`rankCandidates` | ✓ VERIFIED | `pendingResolution`/`pendingValid`/`groupDeferred`/`greetingName` state fields; `parseAddress` (Name/bare/invalid→null, lowercased); `rankCandidates` (dedupe, count+recency sort, cap 5); `ContactMatch`/`NameCandidates`/`HeaderRecord` types exported |
| `packages/core/src/emailIntent.test.ts` | unit coverage | ✓ VERIFIED | 22 emailIntent cases, part of 49/49 green `@pikar/core` suite |
| `packages/backend/convex/gmail.ts` | `internal.gmail.search` headers-only + `mailbox.searched` audit + `freshAccessToken` | ✓ VERIFIED | Confirmed via grep + running `llmRedaction` tests; `format=metadata` present, `format=full`/`raw` absent; `SMOKE::` offline fixture present |
| `packages/backend/convex/plans.ts` | transient `candidates`/`pendingValid`/`greetingName` + `writeCandidates`/`clearCandidates` | ✓ VERIFIED | All three optional fields on `plans`; `writeCandidates` patches candidates+pendingValid; `clearCandidates` unsets both (leaves `greetingName`); `patchPlan` accepts `greetingName` |
| `packages/backend/convex/cockpit.ts` | resolve turn, `resolveRecipients`, `greetingName` threading, reauth fallback | ✓ VERIFIED | Resolution turn in `sendCockpitMessage` (search→rank→writeCandidates before draft); `resolveRecipients` tenantAction; shared `advance()` tail threads `greetingName`; read-auth failure path notifies `gmail_reconnect` + falls back to asking (no dead-end); zero `audit.log` calls |
| `apps/web` resolution card + E2E | UI surfacing + offline E2E proof | ✓ VERIFIED | `cards.tsx` `ResolutionCard` (chip-section per name + `pendingValid` row, calls `resolveRecipients`); `ChatPane.tsx` "Searching your mailbox…" chip; `apps/web/e2e/cockpit-resolve.spec.ts` exists and (per 03.2-05 summary) was confirmed discoverable by Playwright `--list` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `emailIntent.applyAnswer` (recipients) | `pendingResolution`/`pendingValid`/`groupDeferred` | per-segment classification | ✓ WIRED | 5-step ordered classification confirmed in source + 22 passing tests |
| `rankCandidates` | `parseAddress` | header parse then dedupe+rank | ✓ WIRED | `rankCandidates` calls `parseAddress` per From/To/Cc part (grep confirmed, line ~257) |
| `cockpit.sendCockpitMessage` | `internal.gmail.search` | resolution turn (before draft) | ✓ WIRED | `ctx.runAction(internal.gmail.search, ...)` at cockpit.ts:271, ahead of `proposeEmailPlan` |
| `internal.gmail.search` | `rankCandidates` → `internal.plans.writeCandidates` | cockpit orchestration | ✓ WIRED | Confirmed sequence in `cockpit.ts` around line 271–296 |
| `cockpit.resolveRecipients` | `plans.clearCandidates` + `advance()` | pick fold | ✓ WIRED | `resolveRecipients` builds `resolution` Answer, wipes candidates, calls shared `advance` (draft/propose tail) |
| `llm.draftCockpit` | greeting personalization | `greetingName` arg | ✓ WIRED | Optional arg prepends "Hi <name>," on both real and SMOKE paths; asserted by E2E + redaction test |
| `apps/web cards.tsx ResolutionCard` | `api.cockpit.resolveRecipients` | `useAction` on pick confirm | ✓ WIRED | `cards.tsx:161` `useAction(api.cockpit.resolveRecipients)`, invoked from the "Use these contacts" handler |
| `gmail.search` | `mailbox.searched` audit | refs-only payload | ✓ WIRED | `{ queryHash: contentHash(name), resultCount }` — no PII; asserted by `llmRedaction.test.ts` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|-------------|--------|----------|
| CKPT-01 | 03.2-01 through 03.2-06 (all six plans declare it) | Agent can search/read the user's mailbox via `gmail.modify`, scoped to requesting user, reads audited refs/ids/counts only, nothing sent as a side effect | ✓ SATISFIED | Full chain verified above (truths 1–6); REQUIREMENTS.md line 46 already marked `[x]`, line 152 status table says "Complete" — consistent with code + human-verify evidence |

No orphaned requirements: REQUIREMENTS.md's Phase 3.2 mapping (CKPT-01 only) matches exactly what all six plans declare in frontmatter.

### Anti-Patterns Found

None blocking. Scanned `emailIntent.ts`, `gmail.ts`, `plans.ts`, `cockpit.ts`, `cards.tsx`, `ChatPane.tsx`, `llm.ts` for TODO/FIXME/placeholder/stub markers and empty-handler patterns — none found. `ponytail:` comments present (group-word list, `parseAddress` regex ceiling, `Promise.all` cap) are intentional, documented simplifications per CLAUDE.md §8, not anti-patterns.

Minor documentation lag (non-blocking): `.planning/ROADMAP.md`'s Phase 3.2 plan checkboxes (lines 138–143) are still unchecked `[ ]` and the progress table (line 251) still reads "0/6 Planned," even though all six plans are executed, tested green, and CKPT-01 is human-verified. This is a bookkeeping gap in ROADMAP.md, not a functional gap — REQUIREMENTS.md is already correctly updated (`CKPT-01` marked `[x]`/"Complete"). Recommend updating ROADMAP.md's phase-3.2 checkboxes/progress row in a follow-up commit.

### Human Verification Required

None outstanding. CKPT-01's human gate (a live Gmail OAuth mailbox read cannot be automated) was already executed and APPROVED by the user against the running backend, per 03.2-06-SUMMARY.md:
- Typed a real correspondent's first name → "Searching your mailbox…" chip appeared.
- The resolution card listed a real contact resolved from prior correspondence.
- Picking the contact folded the real address into the PLAN.
- The draft opened "Hi &lt;name&gt;,".
- Nothing was sent (no REPORT card, no delivery) as a side effect of reading.

### Gaps Summary

No functional gaps. All 6 plans' artifacts exist, are substantive (not stubs), and are wired end-to-end: the pure `emailIntent` name-resolution seam, the headers-only Gmail search action with its `mailbox.searched` refs-only audit, the transient plans-row candidate store (wiped on pick), the cockpit orchestration (search→rank→card→resolve→greeting, zero `audit.log` calls in `cockpit.ts`), and the resolution-card UI with an offline E2E proving nothing sends during resolution. `pnpm --filter @pikar/core test` (49/49) and the backend `cockpit`/`llmRedaction`/`cockpitDraft` suites (25/25) were re-run during this verification and are green. CKPT-01 is satisfied in REQUIREMENTS.md and confirmed by the recorded live human checkpoint. The only item worth a follow-up is the stale ROADMAP.md progress bookkeeping noted above — cosmetic, does not affect goal achievement.

---

*Verified: 2026-07-12*
*Verifier: Claude (gsd-verifier)*
