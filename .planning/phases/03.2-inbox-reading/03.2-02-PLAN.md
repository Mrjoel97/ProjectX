---
phase: 03.2-inbox-reading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/plans.ts
autonomous: true
requirements: [CKPT-01]
must_haves:
  truths:
    - "Fetched candidate names/addresses/hints persist TRANSIENTLY on the plans row and are wiped on pick"
    - "Same-turn valid addresses are held (pendingValid) until the uniform confirm, not committed early"
    - "Only chosen addresses persist in recipients; candidate data never leaves the content plane"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "plans.candidates + plans.pendingValid + plans.greetingName — all optional, no migration"
      contains: "candidates: v.optional"
    - path: "packages/backend/convex/plans.ts"
      provides: "writeCandidates + clearCandidates internal mutations"
      exports: ["writeCandidates", "clearCandidates"]
  key_links:
    - from: "clearCandidates"
      to: "plans row"
      via: "patch candidates/pendingValid to undefined (wipe on pick)"
      pattern: "clearCandidates"
---

<objective>
Add the TRANSIENT candidate-holding fields to the `plans` content plane and their writer/clearer,
so a name resolution can render its card across turns and be wiped the moment the user picks.
Raw candidate content (names/addresses/subjects) is content-plane storage (CLAUDE.md §4 permits
raw content in `plans`, never in audit/DLQ) — transient, never audited, wiped on pick.

Purpose: the ephemeral store behind the resolution card (locked decision — "no contacts cache at
rest"). Only the chosen address persists in `recipients` exactly as today.
Output: the schema shape + writers Plan 04 (cockpit) and Plan 05 (card) consume.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-inbox-reading/03.2-CONTEXT.md
@.planning/phases/03.2-inbox-reading/03.2-RESEARCH.md
@packages/backend/convex/schema.ts
@packages/backend/convex/plans.ts

<interfaces>
<!-- The candidate shape MUST match @pikar/core's ContactMatch/NameCandidates (Plan 01). Mirror it
     as a Convex validator. All optional → no migration (matches the safeText optional-on-read
     precedent already in this schema). -->

```typescript
// plans table — NEW optional transient fields:
candidates: v.optional(v.array(v.object({
  name: v.string(),
  matches: v.array(v.object({
    address: v.string(),
    displayName: v.optional(v.string()),
    lastSubject: v.optional(v.string()),  // USER-only hint — never sent to the LLM
    lastDateMs: v.optional(v.number()),
    count: v.number(),
  })),
}))),
pendingValid: v.optional(v.array(v.string())),  // same-turn valid addrs awaiting uniform confirm
greetingName: v.optional(v.string()),           // resolved display name → drafter greeting
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: transient candidate fields on the plans table</name>
  <files>packages/backend/convex/schema.ts</files>
  <action>
    Add the three OPTIONAL fields above to the `plans` table definition (alongside recipients/
    subject/body). All `v.optional` → no migration needed (Pitfall-7 optional-on-read, same as the
    existing `safeText`/`planId` additions). Do NOT add an index (candidates are read via the
    existing `by_thread` plan row, never queried on their own). Add a short comment: these are
    TRANSIENT (wiped on pick) and content-plane only — never audited (CLAUDE.md §4).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- llmRedaction</automated>
  </verify>
  <done>schema.ts compiles; `plans.candidates`/`pendingValid`/`greetingName` present and optional;
    no migration file added.</done>
</task>

<task type="auto">
  <name>Task 2: writeCandidates + clearCandidates internal mutations</name>
  <files>packages/backend/convex/plans.ts</files>
  <action>
    Add two `internalMutation`s (thin content-plane writers — §1):
    - `writeCandidates({ planId, candidates, pendingValid })` — patches the two transient fields
      (validators mirroring the schema above). Called by cockpit after a search.
    - `clearCandidates({ planId })` — patches `candidates` and `pendingValid` BOTH to `undefined`
      (Convex removes a field patched to undefined — this is the wipe-on-pick). Do NOT clear
      `greetingName` here (it must survive to the draft turn).
    Also extend the existing `patchPlan` args with `greetingName: v.optional(v.string())` so the
    resolve path can persist the greeting through `patchPlan` (it already drops undefined keys).
    Keep every writer `internalMutation` (called by the agent action / resolve path only); do NOT
    add any client-callable candidate reader — the card reads candidates off the existing
    `byThread` row (Plan 05). This file must remain audit/DLQ-free (the llmRedaction static scan
    asserts plans.ts writes no log-plane row).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- llmRedaction cockpit</automated>
  </verify>
  <done>`internal.plans.writeCandidates`/`clearCandidates` exist; clearCandidates unsets both
    transient fields; patchPlan accepts greetingName; llmRedaction "plans.ts emits no audit/DLQ"
    scan still green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (redaction static scan + cockpit gate suite unaffected).
- No migration added; all new fields optional.
</verification>

<success_criteria>
The plans row can hold in-flight candidates + pendingValid transiently and wipe them on pick;
only chosen addresses ever persist — the "no contacts cache at rest" invariant is structural.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inbox-reading/03.2-02-SUMMARY.md`.
</output>
