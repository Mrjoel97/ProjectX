---
phase: 03.1-cockpit-core
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/page.tsx
  - apps/web/app/(app)/dashboard/workspace/SplitPane.tsx
  - apps/web/app/(app)/layout.tsx
  - apps/web/e2e/cockpit-render.spec.ts
  - apps/web/e2e/cockpit-split.spec.ts
autonomous: true
requirements: [INTK-01, REVW-01]

must_haves:
  truths:
    - "`/dashboard/workspace` renders a two-pane cockpit (chat ~30% left / workspace ~70% right) under the existing `(app)` auth gate (inherits the 18c8442 Connect-Gmail fix) (SC1)"
    - "The divider drags, clamps each pane to ≥20%, and the split persists across reload (localStorage, per-user) (SC1)"
    - "The divider is keyboard-accessible: role=separator, aria-valuenow, arrow-key nudge ±2% (accessibility is NOT lazy — CLAUDE.md §8)"
    - "The cockpit stops and prompts to connect when no Gmail is connected (reuses api.gmailAuth.gmailStatus) — nothing plans without a mailbox"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/page.tsx"
      provides: "Cockpit page: two-pane layout host, Gmail-connected gate, empty chat + workspace panes"
      min_lines: 40
    - path: "apps/web/app/(app)/dashboard/workspace/SplitPane.tsx"
      provides: "Native CSS-grid resizable split with role=separator drag handle + localStorage persistence"
      contains: "role=\"separator\""
    - path: "apps/web/e2e/cockpit-render.spec.ts"
      provides: "SC1 render E2E — two panes visible under auth gate"
      contains: "workspace"
    - path: "apps/web/e2e/cockpit-split.spec.ts"
      provides: "SC1 divider E2E — drag clamps ≥20% + persists across reload"
      contains: "reload"
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/page.tsx"
      to: "api.gmailAuth.gmailStatus"
      via: "useQuery gate before planning"
      pattern: "gmailStatus"
    - from: "apps/web/app/(app)/dashboard/workspace/SplitPane.tsx"
      to: "localStorage"
      via: "cockpit.split.<userId> read on mount, write on pointerUp/keydown"
      pattern: "cockpit.split"
---

<objective>
Build the cockpit's structural shell: the `/dashboard/workspace` page with a two-pane resizable layout (chat left ~30%, workspace right ~70%) and a Gmail-connected gate. Panes are empty placeholders here — the chat pane and PLAN/DRAFT/REPORT cards wire in plan 08. This depends only on the frontend harness (plan 02) and is fully testable at Wave 2 with the two SC1 E2E specs, no backend agent needed.

Purpose: SC1. Reuse the `(app)` shell (inherits auth gate + the 18c8442 Connect-Gmail fix for free — do NOT re-touch layout.tsx's auth gate), the inline-style + CSS-var convention, and native CSS grid for the divider (no new dep — ponytail rung 4 over adding react-resizable-panels).
Output: `workspace/page.tsx`, `SplitPane.tsx`, one NAV entry, `cockpit-render` + `cockpit-split` E2E specs.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-ui.md
@apps/web/app/(app)/layout.tsx
@apps/web/app/(app)/review/[id]/page.tsx
@apps/web/app/(app)/connect-gmail/page.tsx
@apps/web/app/globals.css

<interfaces>
<!-- Reused seams (RESEARCH-ui §1, §7) — do not rebuild: -->
api.gmailAuth.gmailStatus         // "No Gmail connected → stop, prompt to connect" gate
api.gmailAuth.gmailConnectUrl     // Connect/Reconnect link (already in connect-gmail/page.tsx)
// (app)/layout.tsx renders children only inside <Authenticated> (18c8442) — cockpit inherits the fix; ReconnectBanner is already global.
// review/[id]/page.tsx `box`/`btn` inline-style consts are directly reusable for cards/buttons.
// The teal CSS vars (--teal-600 etc.) exist in globals.css from plan 02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SplitPane — native resizable two-pane with a11y + persistence</name>
  <files>apps/web/app/(app)/dashboard/workspace/SplitPane.tsx</files>
  <action>
Create a client component (`"use client"`) rendering `display: grid; grid-template-columns: <chatPct>% 6px 1fr` with `{left}` `{handle}` `{right}` children (RESEARCH-ui §3). Default `chatPct = 30`.
- Drag: handle `onPointerDown` captures the pointer; `onPointerMove` sets `pct = clamp(20, (clientX - containerLeft)/containerWidth*100, 80)` (enforces ≥20% each pane); `onPointerUp` releases + writes to storage. One `useState`.
- Accessibility (NOT optional): handle is `role="separator"`, `aria-orientation="vertical"`, `aria-valuenow={Math.round(pct)}`, `aria-valuemin={20}`, `aria-valuemax={80}`, `tabIndex={0}`, `cursor: col-resize`; `onKeyDown` ArrowLeft/ArrowRight nudge ±2% (clamped).
- Persistence: `localStorage` key `cockpit.split.<userId>` (get userId from the existing identity/auth context used elsewhere in the shell). Lazy `useState` initializer reads it on mount; write on pointerUp + keydown.
- Leave: `// ponytail: localStorage split, per-user-per-browser. Upgrade to a convex userPrefs row if cross-device sync is required.`
Style with the teal vars where a handle/frame color is wanted. Do NOT add react-resizable-panels.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>SplitPane compiles; renders left/handle/right; clamp math enforces 20-80; role=separator + arrow-key nudge present; localStorage keyed by userId.</done>
</task>

<task type="auto">
  <name>Task 2: workspace page + Gmail gate + NAV entry</name>
  <files>apps/web/app/(app)/dashboard/workspace/page.tsx, apps/web/app/(app)/layout.tsx</files>
  <action>
Create `dashboard/workspace/page.tsx` (`"use client"`) rendering `<SplitPane>` with:
- LEFT (chat pane placeholder): a titled panel "Conversation" with a placeholder composer area — the real chat wires in plan 08. Mark with a `{/* chat pane — plan 08 */}` comment.
- RIGHT (workspace canvas placeholder): a titled panel "Workspace" with an empty-state ("No artifacts yet") — cards wire in plan 08. Mark `{/* card list — plan 08 */}`.
- Gmail gate: `const status = useQuery(api.gmailAuth.gmailStatus)`. While `undefined` → "Loading…"; if not connected → a teal CTA linking to `/connect-gmail` ("Connect Gmail to start planning") and DO NOT render the composer (design: "No Gmail connected → stop, prompt to connect before planning"). Reuse `gmailConnectUrl` exactly as connect-gmail/page.tsx does.
Use inline `style={}` + `var(--teal-600)` (no Tailwind). Reuse the `box` const style pattern from review/[id].

In `layout.tsx` add ONE NAV entry `{ href: "/dashboard/workspace", label: "Workspace" }` to the existing NAV array. Do NOT modify the `<Authenticated>` gate or remove existing NAV entries (the /submit and /review pages stay — retiring them is a later slice).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Page renders two panes via SplitPane; Gmail gate blocks the composer when unconnected; NAV entry added; auth gate untouched.</done>
</task>

<task type="auto">
  <name>Task 3: cockpit-render + cockpit-split E2E specs</name>
  <files>apps/web/e2e/cockpit-render.spec.ts, apps/web/e2e/cockpit-split.spec.ts</files>
  <action>
Write two Playwright specs under `apps/web/e2e/` (harness from plan 02; run against the live local dev backend + offline SMOKE:: path per the e2e README). Since the page is behind the auth gate, use the repo's established E2E auth approach — if none exists yet, add a minimal storageState/setup that signs in the test user (document it in e2e/README.md; keep it the smallest thing that reaches the authed shell).
- `cockpit-render.spec.ts` (SC1): navigate to `/dashboard/workspace`; assert both panes render (chat "Conversation" + workspace "Workspace") under the auth gate; assert no eternal spinner.
- `cockpit-split.spec.ts` (SC1): drag the `role="separator"` handle; assert the left pane clamps and never drops below ~20%; reload the page and assert the split width persisted (localStorage).
Keep assertions resilient (data-testid on panes/handle if needed — add them in Task 2 if so).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test:e2e -- cockpit-render cockpit-split</automated>
  </verify>
  <done>Both specs green against the live dev server: two panes render under auth; divider clamps ≥20% and persists across reload.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` clean.
- `test:e2e cockpit-render cockpit-split` green.
- layout.tsx auth gate + existing NAV untouched; no Tailwind/new resizable dep added.
</verification>

<success_criteria>
SC1 is fully delivered (render + drag + clamp + persist). Plan 08 drops the chat pane + card dispatcher into the existing placeholders.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-05-SUMMARY.md`.
</output>
