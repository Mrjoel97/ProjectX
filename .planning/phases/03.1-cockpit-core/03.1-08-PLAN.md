---
phase: 03.1-cockpit-core
plan: 08
type: execute
wave: 4
depends_on: [05, 07]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/page.tsx
  - apps/web/app/(app)/dashboard/workspace/ChatPane.tsx
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
autonomous: true
requirements: [AGNT-02, REVW-01, DLVR-01]

must_haves:
  truths:
    - "The chat pane renders the thread (useThreadMessages) and a composer that calls `sendCockpitMessage`; guided questions + the drafted plan appear as agent messages (SC2 render)"
    - "The workspace pane renders a card dispatcher: a PLAN card (recipient chips + mode + subject + body preview + steps + one Approve), a DRAFT card (full email), and a REPORT card (per-recipient status + message id + audit link) (SC3/SC5 render)"
    - "The single Approve button calls `executePlan`, guarded by a `busy` flag; a second click is a no-op (idempotent — double-approve sends once, enforced server-side) (SC4 UI)"
    - "The REPORT card fills live per recipient via Convex query reactivity (plans.reportForPlan) — no polling, no socket code (SC5)"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/ChatPane.tsx"
      provides: "Thread render (useThreadMessages) + composer wired to sendCockpitMessage"
      contains: "useThreadMessages"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "PLAN/DRAFT/REPORT card dispatcher fed by plans.byThread + plans.reportForPlan"
      contains: "Approve"
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx (PlanCard Approve)"
      to: "api.cockpit.executePlan"
      via: "useMutation + busy guard (mirrors review/[id] approve pattern)"
      pattern: "executePlan"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx (ReportCard)"
      to: "api.plans.reportForPlan"
      via: "useQuery reactive projection → live per-recipient rows"
      pattern: "reportForPlan"
---

<objective>
Drop the live chat pane and the PLAN/DRAFT/REPORT card dispatcher into the cockpit shell's placeholders (plan 05), wiring them to the agent + plans queries (plans 06/07). This is the UI half of SC2/SC3/SC5 — the render surface over the deterministic backend. Convex query reactivity IS the live-update mechanism (the same idiom as DeadLetterBadge/ReconnectBanner) — no polling code.

Purpose: SC3 (PLAN card shows recipients + mode + subject + body preview + steps before a single Approve), SC5 (REPORT fills per recipient live), SC2 render (guided questions as chat). REVW-01 (single Approve gate on the PLAN card, reusing the review/[id] approve pattern).
Output: `ChatPane.tsx`, `cards.tsx` (dispatcher), page.tsx wired.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-ui.md
@apps/web/app/(app)/dashboard/workspace/page.tsx
@apps/web/app/(app)/dashboard/workspace/SplitPane.tsx
@apps/web/app/(app)/review/[id]/page.tsx
@apps/web/app/(app)/_components/AttachmentPicker.tsx

<interfaces>
<!-- From plan 07: api.cockpit.sendCockpitMessage (returns threadId), api.cockpit.executePlan({ planId }) -->
<!-- From plan 06: api.plans.byThread({ threadId }) → plan row (PLAN + DRAFT cards); api.plans.reportForPlan({ planId }) → [{ recipient, status, correlationId, messageId }] -->
<!-- @convex-dev/agent/react (dep added in plan 02): useThreadMessages / useUIMessages / optimisticallySendMessage -->
<!-- review/[id]/page.tsx approve pattern: const [busy,setBusy]=useState(false); useMutation; disable while busy. `box`/`btn` inline-style consts reusable. -->
<!-- teal vars (plan 02): var(--teal-600) for the Approve/Send fills (white text). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPane — thread render + composer</name>
  <files>apps/web/app/(app)/dashboard/workspace/ChatPane.tsx</files>
  <action>
Client component. Render the thread via `useThreadMessages` (static list first — ponytail; `useUIMessages`/streaming is a later upgrade, leave a `ponytail:` note) bound to the active `threadId`. `undefined` → "Loading…"; empty → an intro prompt ("Tell me who to email and what to say"). Below the list, a composer: a text input + a teal Send button (`var(--teal-600)`, white text) that calls `api.cockpit.sendCockpitMessage` with the text (and the returned `threadId` on the first message → lift `threadId` state to the page so both ChatPane and cards share it). Reuse `AttachmentPicker` for the attach button (slice-1 upload only). Optional live activity chips ("Drafting…", "Sending to N", "Sent ✓") driven off the plan/report query status — keep minimal.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>ChatPane renders the thread + composer; Send calls sendCockpitMessage; threadId lifts to the page; attach reuses AttachmentPicker.</done>
</task>

<task type="auto">
  <name>Task 2: card dispatcher — PLAN / DRAFT / REPORT</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
A dispatcher over the plan row + report projection (RESEARCH-ui §4). Cards are plain inline-styled `<div>`s (reuse the `box` const from review/[id]):
- `PlanCard` (renders when `plan.status === "proposed"`): recipient chips, mode (individual/group) shown explicitly, subject, a body preview, a short steps list, and ONE Approve button. Approve = `useMutation(api.cockpit.executePlan)` guarded by a `busy` state (mirror review/[id] exactly); disabled while busy; a second click no-ops (idempotent server-side). Teal fill.
- `DraftCard` (EMAIL): the full drafted subject + body from the plan row. "Editable via chat" = the card just re-renders the latest plan row (no inline editor in slice 1 — `ponytail:` note).
- `ReportCard` (renders when `plan.status ∈ delivering|done`): `useQuery(api.plans.reportForPlan, { planId })` → one row per recipient: status badge (pending/delivering/sent/failed/awaiting_reauth) + Gmail message id + an audit link (the row's correlationId → the existing audit-trail view). Handle `undefined` (skeleton) / empty. This fills LIVE via reactivity — no polling.
Export a small `<CardList>` that reads `api.plans.byThread({ threadId })` and dispatches.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>PlanCard/DraftCard/ReportCard render from the live queries; Approve wired to executePlan with a busy guard; REPORT rows are per-recipient with status + messageId + audit link.</done>
</task>

<task type="auto">
  <name>Task 3: wire page.tsx — replace placeholders, share threadId</name>
  <files>apps/web/app/(app)/dashboard/workspace/page.tsx</files>
  <action>
Replace the plan-05 placeholders: LEFT pane → `<ChatPane threadId={threadId} onThread={setThreadId} />`; RIGHT pane → `<CardList threadId={threadId} />`. Hold `threadId` in page state (set from the first `sendCockpitMessage` response), pass to both. Keep the Gmail-connected gate from plan 05 (still blocks the composer when unconnected). No change to SplitPane or the auth gate.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Page composes SplitPane(ChatPane, CardList) with a shared threadId; Gmail gate intact; typecheck clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` clean.
- Approve reuses the review/[id] busy-guard pattern; REPORT is a `useQuery` projection (no polling/socket code).
- No Tailwind; teal vars used for fills; SplitPane + auth gate untouched.
</verification>

<success_criteria>
The full cockpit renders end to end: type → guided questions → PLAN/DRAFT cards → single Approve → REPORT fills live. Plan 09 drives this via Playwright over the offline SMOKE:: path and adds the human-verify checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-08-SUMMARY.md`.
</output>
