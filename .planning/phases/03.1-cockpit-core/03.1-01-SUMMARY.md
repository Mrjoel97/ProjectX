---
phase: 03.1-cockpit-core
plan: 01
subsystem: database
tags: [convex, schema, multi-tenant, cockpit, action-wrapper, biome]

# Dependency graph
requires:
  - phase: 02-backend-spine
    provides: requests content-plane table + tenantQuery/tenantMutation wrappers + requireTenant
  - phase: 03-guardrails
    provides: requests.safeText/safeTextHash rails + optional-on-read (Pitfall-7) convention
provides:
  - plans content-plane table (approve-gate object + idempotency CAS) with pinned status enum
  - requests.planId grouping column + by_plan index for the REPORT projection
  - tenantAction wrapper (tenant-scoped action builder) in lib/functions.ts
  - biome ban on raw `action` import outside lib/functions.ts
affects: [03.1-04 fan-out, 03.1-06 adapter, 03.1-07 agent/approve, delivery lane]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cockpit plans as ONE content-plane table = approve-gate + idempotency CAS; fan-out reuses requests rows grouped by planId (zero new send code)"
    - "tenantAction mirrors tenantQuery/tenantMutation — every raw builder (query/mutation/action) now import-banned outside lib/functions.ts"

key-files:
  created: []
  modified:
    - packages/backend/convex/schema.ts
    - packages/backend/convex/lib/functions.ts
    - biome.json

key-decisions:
  - "Plan-status lifecycle pinned once (collecting → proposed → approved → delivering → done), shared by agent + delivery lanes — downstream plans depend on these exact literals"
  - "REPORT is a live projection over requests rows via by_plan index, NOT a patched report[] array on plans (DECISION #1)"
  - "planId is optional → no migration (optional-on-read, Pitfall 7), same convention as safeText"

patterns-established:
  - "Raw action builder joins query/mutation in the noRestrictedImports ban; lib/functions.ts is the sole override-exempt consumer"

requirements-completed: [AGNT-02, DLVR-01]

# Metrics
duration: 6min
completed: 2026-07-12
---

# Phase 03.1 Plan 01: Cockpit Backend Contracts Summary

**`plans` content-plane table (pinned collecting→proposed→approved→delivering→done lifecycle) + `requests.planId`/`by_plan` grouping index + `tenantAction` wrapper — the two backend contracts every other cockpit plan composes against.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-07-12T03:45:08Z
- **Completed:** 2026-07-12T03:50:53Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added the `plans` table — one row per guided conversation, the approve-gate object + idempotency CAS, with the pinned plan-status enum and optional slot content (recipients/mode/subject/bodyIntent/body/correlationId/workflowId). Raw content lives here, never in audit/DLQ (CLAUDE.md §4).
- Added `requests.planId` (optional, no migration) + `by_plan` index so the REPORT projection reads every recipient row for one plan.
- Added `tenantAction` (customAction + requireTenant) to lib/functions.ts, mirroring the two existing wrappers — the cockpit agent action is now tenant-scoped without the raw `action` builder.
- Extended the biome `noRestrictedImports` ban to `action` in both `_generated/server` paths; only lib/functions.ts (override-exempt) may import it.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add plans table + planId grouping on requests** - `9e2d69e` (feat)
2. **Task 2: Add tenantAction wrapper + ban raw action import** - `732841a` (feat)

**Plan metadata:** (this commit) (docs: complete plan)

## Files Created/Modified
- `packages/backend/convex/schema.ts` - Added `plans` table (content plane + pinned status enum) and `requests.planId` optional column + `by_plan` index.
- `packages/backend/convex/lib/functions.ts` - Added `tenantAction` wrapper; imported `customAction` + raw `action`.
- `biome.json` - Added `"action"` to `importNames` in both `_generated/server` path entries; updated ban message.

## Decisions Made
- None beyond the plan — the pinned status enum, planId-as-optional (no migration), and REPORT-as-projection were all specified. Followed exactly.

## Deviations from Plan

None - plan executed exactly as written. (Pure schema + wrapper contracts; no feature logic, no bugs or missing functionality to auto-fix.)

## Issues Encountered
- The plan's verify commands (`pnpm --filter @pikar/backend typecheck` and `biome check`) surface **pre-existing, out-of-scope** noise that is not caused by this plan's changes (confirmed via `git stash` — identical without my edits, and via `git status ??` for the untracked files):
  - 15 `tsc` errors in `*.test.ts` files (`import.meta.glob` TS2339 + test-assertion narrowing) — Vitest globals plain tsc doesn't type.
  - 1 `tsc` error in **untracked** `packages/core/src/emailIntent.ts` (unrelated in-progress work from another session; `index.ts` also modified).
  - 3 biome warnings + 2 infos on `biome.json` config style (schema version, deprecated `recommended`, folder-ignore glob) — warnings, not errors; biome exits clean.
  - My three files produce **zero** new errors; `schema.ts` codegen regenerated the new `plans` types, `tenantAction` typechecks clean, and functions.ts passes biome. Logged all four to `03.1-cockpit-core/deferred-items.md`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Downstream cockpit plans (03.1-04 fan-out, 03.1-06 adapter, 03.1-07 agent/approve) can now import the `plans` / `requests.planId` types and `tenantAction` with no further schema or wrapper decisions.
- No feature behavior yet — these are pure contracts, exactly as scoped for Wave 0.

## Self-Check: PASSED

- Files verified on disk: schema.ts, lib/functions.ts, biome.json, 03.1-01-SUMMARY.md
- Commits verified: 9e2d69e (Task 1), 732841a (Task 2)
- Content proofs: schema.ts has plans/planId/by_plan (4 matches), functions.ts exports tenantAction, biome.json bans "action" in both paths (2 matches)

---
*Phase: 03.1-cockpit-core*
*Completed: 2026-07-12*
