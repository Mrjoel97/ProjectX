---
phase: 03.1-cockpit-core
plan: 09
type: execute
wave: 5
depends_on: [04, 08]
files_modified:
  - apps/web/e2e/cockpit-report.spec.ts
  - apps/web/e2e/connect-gmail.spec.ts
  - packages/backend/convex/llmRedaction.test.ts
autonomous: false
requirements: [REVW-01, DLVR-01, INTK-01]

must_haves:
  truths:
    - "A full E2E over the offline SMOKE:: path proves chat → guided questions → PLAN → single Approve → REPORT fills per recipient live (SC1-SC5 integrated)"
    - "The Connect-Gmail page resolves (no infinite spinner) on the authed session — the 18c8442 fix verified by E2E, not re-touched (Groundwork)"
    - "The redaction invariant holds end to end: no raw email content (recipient/subject/body) in any audit/DLQ/telemetry payload — asserted by the extended llmRedaction test + the smoke:fanout assertNoRawPii (CLAUDE.md §4, SC5)"
    - "A human checkpoint confirms the reassigned Phase-2 SC-1/SC-2 end-user verification (a real Gmail send to 1-2 addresses) and the divider ergonomics"
  artifacts:
    - path: "apps/web/e2e/cockpit-report.spec.ts"
      provides: "SC5 E2E — approve over SMOKE:: → REPORT fills per recipient live"
      contains: "reportForPlan"
    - path: "apps/web/e2e/connect-gmail.spec.ts"
      provides: "Groundwork E2E — connect-gmail resolves, no eternal spinner"
      contains: "connect-gmail"
    - path: "packages/backend/convex/llmRedaction.test.ts"
      provides: "Extended: cockpit plan/draft/report path leaks no raw content to audit/DLQ"
      contains: "cockpit"
  key_links:
    - from: "apps/web/e2e/cockpit-report.spec.ts"
      to: "the offline SMOKE:: delivery path"
      via: "body intent carries SMOKE::route=direct_llm:: → deterministic offline draft + fan-out"
      pattern: "SMOKE::"
---

<objective>
Prove the whole cockpit end to end and lock the governance invariant: a Playwright integration test (chat → plan → approve → live per-recipient report over the offline SMOKE:: path), an E2E verification of the already-landed Connect-Gmail fix, an extended redaction assertion covering the cockpit content plane, and the single human-verify checkpoint that carries the reassigned Phase-2 SC-1/SC-2 end-user verification.

Purpose: integrate SC1-SC5, verify the 18c8442 groundwork fix (do NOT re-touch the auth gate), and satisfy CLAUDE.md §4 across the new plan/draft/report surface. The manual checkpoint covers what automation can't (a real OAuth send + divider ergonomics — 03.1-VALIDATION Manual-Only table).
Output: `cockpit-report.spec.ts`, `connect-gmail.spec.ts`, extended `llmRedaction.test.ts`, one human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-VALIDATION.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-ui.md
@apps/web/app/(app)/dashboard/workspace/page.tsx
@apps/web/app/(app)/connect-gmail/page.tsx
@packages/backend/convex/llmRedaction.test.ts
@packages/backend/convex/smokeAssert.ts

<interfaces>
<!-- E2E harness + auth setup from plans 02/05; SMOKE:: offline path from plans 04/06 -->
<!-- A body intent containing "SMOKE::route=direct_llm::" → deterministic offline draft (no AI_GATEWAY_API_KEY) → fan-out over tenant with no token → rows hold at awaiting_reauth (the automatable half) -->
<!-- llmRedaction.test.ts is the existing no-raw-PII pattern to extend; assertNoRawPiiFanout (plan 04) covers the smoke layer -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cockpit-report E2E (chat → plan → approve → live report, offline)</name>
  <files>apps/web/e2e/cockpit-report.spec.ts</files>
  <action>
Playwright spec against the live local dev backend (auth setup from plan 05). Drive the guided conversation to 2+ recipients with a body intent containing `SMOKE::route=direct_llm::` (deterministic offline draft — no AI key). Steps: type recipients (two valid addresses), subject, the SMOKE:: body intent, choose a mode → assert the PLAN card shows recipient chips + mode + subject + body preview + one Approve. Click Approve once → assert the REPORT card appears and fills per recipient (each row reaching a terminal status via reactivity; with the tokenless smoke tenant, rows reach `awaiting_reauth` — the automatable half, real send is the manual checkpoint). Assert a second Approve click is a no-op (button disabled / idempotent). This is the SC4/SC5 integration proof.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test:e2e -- cockpit-report</automated>
  </verify>
  <done>cockpit-report green: guided flow → PLAN (recipients+mode+subject+preview+Approve) → one Approve → REPORT fills per recipient live; double-approve no-ops.</done>
</task>

<task type="auto">
  <name>Task 2: connect-gmail E2E + extended redaction assertion</name>
  <files>apps/web/e2e/connect-gmail.spec.ts, packages/backend/convex/llmRedaction.test.ts</files>
  <action>
`connect-gmail.spec.ts` (Groundwork): on the authed session, load `/connect-gmail` and `/dashboard/workspace`; assert neither strands an eternal spinner (the 18c8442 fix — verify only, do NOT touch layout.tsx's auth gate). Assert the connect CTA / status resolves to a bounded state within a short timeout (covers the `gmailConnectUrl` residual flagged in RESEARCH-ui §2.1).

`llmRedaction.test.ts` (extend, convex-test): add cockpit-path assertions — after a proposed plan + a seeded fan-out, scan every audit/deadLetters/telemetry payload for the plan's subject/body/recipient needles and assert NONE appear (CLAUDE.md §4). This complements `assertNoRawPiiFanout` (smoke layer, plan 04) at the unit layer over the plans/artifacts content plane.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test:e2e -- connect-gmail && pnpm --filter @pikar/backend exec vitest run convex/llmRedaction.test.ts</automated>
  </verify>
  <done>connect-gmail resolves with no eternal spinner; llmRedaction extended assertion green (no raw plan content in any audit/DLQ/telemetry).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — real multi-recipient send + divider ergonomics</name>
  <action>Manual verification checkpoint (carries the reassigned Phase-2 SC-1/SC-2 end-user verification). Follow the how-to-verify steps below; this is truly manual because a real Gmail send needs human OAuth consent and divider ergonomics are not fully captured by E2E assertions.</action>
  <what-built>The full Email Chat Cockpit: two-pane resizable workspace, guided slot-filling conversation, single plan-approval, hands-off multi-recipient governed fan-out, and a live per-recipient REPORT. Automated E2E covers the offline SMOKE:: path; the smoke tenant has no Gmail token so automation stops at `awaiting_reauth`. This checkpoint carries the reassigned Phase-2 SC-1/SC-2 end-user verification (a REAL send) plus divider ergonomics — the parts automation can't reach (03.1-VALIDATION Manual-Only table).</what-built>
  <how-to-verify>
1. Ensure the local dev backend is running (`convex dev` NOT `--once`, and `next`), and `npx convex run skills:seedSkills` has been run on this deployment (drafter row).
2. On a fresh REAL Google login, load `/connect-gmail` — confirm it resolves with no eternal spinner and lets you connect a real Google account (the 18c8442 fix on a real OAuth session).
3. Open `/dashboard/workspace`. Drag the divider and arrow-key it — confirm it clamps (~20% min each) and the split persists across a reload.
4. Run the cockpit end to end to 1-2 REAL email addresses (a normal body intent, no SMOKE:: sentinel): confirm the guided questions ask only for missing slots, an invalid email re-asks only that one, the PLAN card shows recipients + mode + subject + body preview before a single Approve, and after Approve the REPORT fills per recipient with a real Gmail message id + audit link. Confirm the emails actually arrive.
5. Confirm nothing sent before you clicked Approve, and a second Approve click did not double-send.
  </how-to-verify>
  <resume-signal>Type "approved" once real delivery + divider ergonomics are confirmed, or describe any issue (bad question order, a send before approve, a double-send, an unstyled/broken card, an eternal spinner).</resume-signal>
</task>

</tasks>

<verification>
- `test:e2e cockpit-report connect-gmail` green; extended `llmRedaction.test.ts` green.
- Full suite before /gsd:verify-work: `pnpm test` + `smoke:fanout` (+ regression `smoke:pipeline`/`smoke:guardrails`) + `pnpm exec playwright test` all green (03.1-VALIDATION phase gate).
- Human checkpoint confirms a real multi-recipient send + divider ergonomics.
</verification>

<success_criteria>
All five phase success criteria are demonstrated: two-pane cockpit (SC1), guided slot-filling with invalid-email re-ask (SC2), mode + PLAN card before a single Approve (SC3), idempotent hands-off fan-out with zero sends before Approve (SC4), and a live per-recipient REPORT with isolation + zero raw-content leakage (SC5). The Connect-Gmail groundwork fix is verified. Ready for /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-09-SUMMARY.md`.
</output>
