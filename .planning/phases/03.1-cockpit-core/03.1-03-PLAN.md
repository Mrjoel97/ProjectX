---
phase: 03.1-cockpit-core
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/validateSubmit.ts
  - packages/core/src/emailIntent.ts
  - packages/core/src/emailIntent.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [INTK-01, AGNT-02]

must_haves:
  truths:
    - "`emailIntent` asks only for missing slots, one topic at a time, in the fixed order recipients → (reask invalid) → subject → body intent → mode (only if >1) → ready (SC2)"
    - "An invalid recipient never enters state.recipients — it bounces back in `rejected` so the caller re-asks ONLY that one; nothing is proposed on an assumption (SC2)"
    - "`nextQuestion` NEVER returns `ready` while any required slot is missing or a recipient is invalid; all-filled returns `ready` exactly once (SC2)"
    - "The mode question (`ask_mode`) is emitted only when recipients.length > 1; a single recipient skips straight to ready (SC3)"
    - "One `isValidEmail` regex is the single source of truth, reused by both validateSubmit and emailIntent (no divergent validators)"
  artifacts:
    - path: "packages/core/src/emailIntent.ts"
      provides: "Pure slot-filling state module: EmailIntentState, nextQuestion(state), applyAnswer(state, answer)"
      exports: ["EmailIntentState", "emptyIntent", "nextQuestion", "applyAnswer", "NextQuestion", "Answer", "ApplyResult", "RecipientMode"]
    - path: "packages/core/src/emailIntent.test.ts"
      provides: "The ONE runnable check: ordering, invalid-email re-ask, mode trigger, never-ready-early, immutability/dedupe, validator reuse"
      min_lines: 60
    - path: "packages/core/src/validateSubmit.ts"
      provides: "isValidEmail promoted to a named export, reused by validateSubmit + emailIntent"
      exports: ["isValidEmail", "validateSubmit"]
  key_links:
    - from: "packages/core/src/emailIntent.ts"
      to: "packages/core/src/validateSubmit.ts (isValidEmail)"
      via: "import { isValidEmail }"
      pattern: "isValidEmail"
---

<objective>
Build the pure, tested `emailIntent` slot-filling module in `packages/core` — the deterministic brain of the guided conversation (DECISION #2: the pure module computes the next question; the LLM is used only for the body draft). It has NO send path and NO Convex import (CLAUDE.md §1), so "nothing is ever sent on an assumption" is structurally true, not conventional. Fully parallel with plans 01/02 — pure package, no external deps.

Purpose: SC2 + SC3. Clone the `validateSubmit.ts` template exactly (pure fn, discriminated-union return, side-by-side vitest). Promote the existing private `EMAIL_RE` to a shared `isValidEmail` export (one regex, two callers) rather than adding a validator.
Output: `emailIntent.ts` + `emailIntent.test.ts`, `isValidEmail` export, index re-export.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-core.md
@packages/core/src/validateSubmit.ts
@packages/core/src/index.ts
@.planning/design/email-chat-cockpit.md

<interfaces>
<!-- Existing validateSubmit.ts:37 private regex to promote (RESEARCH-core §2) — shape only, NOT deliverability: -->
const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
<!-- Recommended API shape (RESEARCH-core §3) — planner-tunable names: -->
type RecipientMode = "individual" | "group";
interface EmailIntentState { recipients: readonly string[]; subject?; bodyIntent?; mode?; attachmentIntent?; }
type NextQuestion = { kind:"ask_recipients" } | { kind:"reask_recipient"; invalid:string }
  | { kind:"ask_subject" } | { kind:"ask_body_intent" } | { kind:"ask_mode" } | { kind:"ready" };
nextQuestion(state): NextQuestion            // pure, ordered branches ARE the spec
applyAnswer(state, answer): ApplyResult       // invalid recipients bounce in `rejected`, never stored; immutable
</interfaces>
</context>

<behavior>
Tests (write FIRST — RED — then implement to GREEN) in emailIntent.test.ts, table-driven `.toEqual({...})` full-shape asserts like validateSubmit.test.ts:
- Ordering: from `emptyIntent`, nextQuestion → `ask_recipients`; after a valid recipient → `ask_subject`; then `ask_body_intent`; single recipient with all filled → `ready` (no `ask_mode`).
- Invalid-email re-ask: `applyAnswer(state, {slot:"recipients", value:["ok@x.com","nope"]})` → `{ ok:false, rejected:{ slot:"recipients", invalid:["nope"] } }`, state keeps `ok@x.com`; `nextQuestion` → `reask_recipient` with `invalid:"nope"`.
- Mode trigger: 2 valid recipients + subject + bodyIntent filled → `ask_mode`; the same with 1 recipient NEVER emits `ask_mode`.
- Never-ready-early: while any required slot missing OR any recipient rejected, `nextQuestion` never returns `ready`; all-filled returns `ready` exactly once.
- Immutability/dedupe: `applyAnswer` returns a fresh object, input `state` unchanged; a duplicate recipient (case-insensitive) is not added twice.
- Validator reuse: reuse validateSubmit's bad-email table (`"a@b"`, `"@b.com"`, `"a@.com"`, `""`, `"nope"`) against `isValidEmail` — one shared truth.
</behavior>

<tasks>

<task type="auto">
  <name>Task 1: Promote isValidEmail to a shared named export</name>
  <files>packages/core/src/validateSubmit.ts</files>
  <action>
Promote the private `EMAIL_RE` (validateSubmit.ts:37) to an exported helper and route `validateSubmit`'s own recipient check through it — one regex, one behavior, two callers (the ponytail root-cause shape):

```ts
/** Structural email check — shape only, NOT deliverability. Anchored, single address. */
export const isValidEmail = (s: string): boolean => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(s);
```

Replace the inline `EMAIL_RE.test(recipient)` in `validateSubmit` with `isValidEmail(recipient)`. Do NOT reuse the unanchored/global PII regex (`packages/pii/src/scan.ts:49`) — it accepts emails embedded in prose (wrong tool). Do NOT add zod. Existing validateSubmit.test.ts must stay green (behavior unchanged).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core exec vitest run src/validateSubmit.test.ts</automated>
  </verify>
  <done>`isValidEmail` exported; validateSubmit uses it; validateSubmit.test.ts still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: emailIntent module + the one runnable check (RED → GREEN)</name>
  <files>packages/core/src/emailIntent.ts, packages/core/src/emailIntent.test.ts, packages/core/src/index.ts</files>
  <action>
Write `emailIntent.test.ts` FIRST (see &lt;behavior&gt;), confirm it FAILS, then implement `emailIntent.ts` to pass. Model 1:1 on validateSubmit.ts: heavy doc comment stating the invariant + CLAUDE.md §1 (no Convex imports), named exports only, pure functions (no Date.now/randomness/I/O), discriminated-union returns.

- `EmailIntentState` immutable; `emptyIntent = { recipients: [] }`.
- `nextQuestion(state)`: ordered `if` branches ARE the spec — recipients → reask (if any invalid pending) → subject → bodyIntent → mode (only if `recipients.length > 1 && mode === undefined`) → `ready`. Never `ready` while a required slot is missing.
- `applyAnswer(state, answer)`: validate + merge one answer immutably. Invalid recipients are NOT stored — return them in `rejected` (trust boundary). Dedupe recipients case-insensitively. Import `isValidEmail` from validateSubmit.
- `mode` is conditional-required (only when >1 recipients); default value is `individual` but the module SURFACES `ask_mode` (does not silently default) so the PLAN card shows an explicit choice (RESEARCH-core §4).
- `attachmentIntent?` is tracked for the PLAN card but its absence NEVER gates `ready` (slice-1 attachment is upload-only, a UI affordance — do not add an `ask_attachment` question).

Re-export from `src/index.ts`: `export * from "./emailIntent";`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test</automated>
  </verify>
  <done>All &lt;behavior&gt; cases green; module is pure (no convex/date/random imports); re-exported from index; `pnpm --filter @pikar/core test` passes end-to-end.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test` green (emailIntent + validateSubmit).
- `emailIntent.ts` has zero Convex/Date/random imports (grep-clean — CLAUDE.md §1, pure module).
- Exactly one email regex in the package (isValidEmail), reused by both callers.
</verification>

<success_criteria>
SC2 + SC3 slot logic proven by the single runnable check. The agent lane (plan 07) can drive turns off `nextQuestion`/`applyAnswer` with no re-derivation; the module cannot send (no send path exists).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-03-SUMMARY.md`.
</output>
