---
phase: 03.1-cockpit-core
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/backend/convex/plans.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitDraft.test.ts
autonomous: true
requirements: [AGNT-02, DLVR-01]

must_haves:
  truths:
    - "`plans.ts` is a thin tenant-scoped adapter (CLAUDE.md §1): internal writers (insert/patch/setPlanStatus) + reactive tenantQuery readers (byThread, report projection) — raw content stays in the plans/requests content plane, never in audit/DLQ"
    - "The REPORT is a live projection: `reportForPlan` reads every `requests` row for a planId (status) joined to its `gmail.sent` audit (messageId) + correlationId (audit link) — NO report[] array, no report writer (DECISION #1, SC5)"
    - "The cockpit body draft lives INSIDE llm.ts (the ONLY \"use node\" module — a second node module re-triggers the TS circular-inference cliff, RESEARCH §6): loads the `email-drafter` skill from the registry (no hardcoded prompt, CLAUDE.md §5), honors the `SMOKE::` offline sentinel, and keeps the CHEAP_MODEL fallback"
    - "The draft reads only redacted text (safeText) — raw body-intent is scanned before the model call so no raw PII reaches the LLM or any log (GRDL-01/02, CLAUDE.md §4)"
  artifacts:
    - path: "packages/backend/convex/plans.ts"
      provides: "plans adapter: insertPlan/patchPlan/setPlanStatus (internal) + byThread/reportForPlan (tenantQuery)"
      exports: ["insertPlan", "patchPlan", "setPlanStatus", "byThread", "reportForPlan"]
    - path: "packages/backend/convex/llm.ts"
      provides: "draftCockpit action (drafter skill + parseSmoke + fallback) added inside the single use-node module"
      contains: "draftCockpit"
    - path: "packages/backend/convex/cockpitDraft.test.ts"
      provides: "Draft loads email-drafter body from the registry + honors SMOKE:: + no raw content leaks"
      contains: "email-drafter"
  key_links:
    - from: "packages/backend/convex/llm.ts (draftCockpit)"
      to: "internal.skills.getActiveSkill(EMAIL_DRAFTER_SKILL)"
      via: "registry load — no hardcoded prompt (CLAUDE.md §5)"
      pattern: "getActiveSkill"
    - from: "packages/backend/convex/plans.ts (reportForPlan)"
      to: "requests by_plan + gmail.sent audit"
      via: "reactive projection (status + messageId + correlationId)"
      pattern: "by_plan"
---

<objective>
Build the plans content-plane adapter and the cockpit body-draft step — the data + drafting layer the agent orchestration (plan 07) and the cards (plan 08) consume. Interface-first: this plan defines the plans read/write contract and the draft seam; plan 07 wires them to the conversation. Depends only on the schema contract (plan 01).

Purpose: DECISION #1 (plans table = plan/draft content plane; REPORT = live projection over requests rows, no report writer) + DECISION #2 (LLM used ONLY for the body draft) + RESEARCH §6 (draft MUST live inside llm.ts, the single use-node module). CLAUDE.md §5 (drafter loads from the registry, no hardcoded prompt), §4 (redaction-safe).
Output: `plans.ts` adapter, `draftCockpit` in llm.ts, `cockpitDraft.test.ts`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-agent.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-delivery.md
@packages/backend/convex/llm.ts
@packages/backend/convex/skills.ts
@packages/backend/convex/requests.ts
@packages/backend/convex/lib/functions.ts
@packages/contracts/src/drafting.ts
@packages/contracts/src/skill.ts

<interfaces>
<!-- Reused (RESEARCH-agent §3, §6) — do not rebuild: -->
internal.skills.getActiveSkill({ name }): { body, version, skillId }   // fails closed (throws NO_ACTIVE_SKILL)
EMAIL_DRAFTER_SKILL = "email-drafter"    // packages/contracts/src/skill.ts:42; row already seeded
draftSchema = z.object({ subject, body })  // packages/contracts/src/drafting.ts — recipient deliberately absent (never model-derived)
// llm.ts:36-57 parseSmoke(text): detects SMOKE::route=<r>::[cache=1::][fail=primary::] → deterministic offline draft { subject:"Smoke Subject", body:"Smoke draft for <hash>" }
// Existing draft path (llm.ts:217): generateObject({ model, schema: draftSchema, system: skill.body, prompt }) with CHEAP_MODEL isFallbackEligible fallback + AbortSignal.timeout
// guardrails.prepare / scanText produce safeText + safeTextHash (redacted) — reuse to redact the body-intent BEFORE the model call
// plan-status enum (plan 01): collecting | proposed | approved | delivering | done
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: plans.ts adapter — writers + reactive readers + REPORT projection</name>
  <files>packages/backend/convex/plans.ts</files>
  <action>
Create a thin adapter (CLAUDE.md §1). Internal writers (called by the agent action / executePlan / the fan-out workflow):
- `insertPlan` (internalMutation): create a `plans` row `{ tenantId, threadId, status:"collecting", recipients:[], createdAt }`; returns planId. (One per thread; the agent creates it on first turn.)
- `patchPlan` (internalMutation): patch slot fields (recipients/mode/subject/bodyIntent/body/status) by planId — used as slots fill and when the draft lands (status → "proposed").
- `setPlanStatus` (internalMutation): CAS-friendly status setter (used by executePlan and the fan-out workflow's terminal `done`).

Tenant-scoped reactive readers (client subscribes → cards update live):
- `byThread` (tenantQuery): the plans row for `{ threadId }` scoped to `ctx.tenantId` (by_thread index) → feeds the PLAN + DRAFT cards.
- `reportForPlan` (tenantQuery): the LIVE REPORT projection (DECISION #1 / RESEARCH-delivery §3) — read all `requests` rows for `planId` (by_plan index, scoped to tenant); for each return `{ recipient, status, correlationId, messageId }` where `messageId` comes from that row's `gmail.sent` audit (query the audit by correlationId). NO report[] array, NO report writer — the reactivity of `requests.status` + audit inserts makes it fill live per recipient.

Guard every reader on `ctx.tenantId` (never leak across tenants). Do NOT put recipient/subject/body into any audit call from this module — this is the content plane (§4).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec convex codegen && pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>plans.ts compiles; writers + `byThread` + `reportForPlan` present; readers tenant-scoped; REPORT is a projection (no report[] field written).</done>
</task>

<task type="auto">
  <name>Task 2: draftCockpit inside llm.ts (registry drafter + SMOKE:: + redaction)</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitDraft.test.ts</files>
  <action>
Add a thin `draftCockpit` internalAction INSIDE `llm.ts` (do NOT create a new "use node" module — RESEARCH §6). Signature takes redacted text: `{ tenantId, safeText, safeTextHash }` (RESEARCH-agent §6 — avoids fabricating a requests row per chat). It:
1. `parseSmoke(safeText)` → if the `SMOKE::` sentinel is present, return the deterministic offline draft (no model call, honors `fail=primary` to exercise fallback) — this is what the E2E offline path uses.
2. else load the drafter: `const skill = await ctx.runQuery(internal.skills.getActiveSkill, { name: EMAIL_DRAFTER_SKILL })` (no hardcoded prompt — CLAUDE.md §5), then `generateObject({ model, schema: draftSchema, system: skill.body, prompt: safeText })` with the SAME CHEAP_MODEL `isFallbackEligible` fallback + `AbortSignal.timeout(45s)` the existing draft path uses.
3. Return `{ subject, body }` (draftSchema shape; recipient is never model-derived).

Reuse/generalize the existing `draftUncached` internals rather than duplicating (ponytail — share parseSmoke + skill load + generateObject + fallback). Add explicit return-type annotations if the new action tips the use-node circular-inference limit (STATE.md Phase-02 note: gmail.ts tipped llm.ts — same remedy).

REDACTION: the CALLER (plan 07) must pass already-redacted `safeText` (scanned via the existing guardrails.prepare/scanText). Document this contract in a comment; the test below asserts no raw body-intent leaks.

`cockpitDraft.test.ts` (convex-test, extend the skills/redaction pattern): assert (a) draftCockpit loads the `email-drafter` body from the registry (not a hardcoded string), (b) a `SMOKE::route=direct_llm::` input returns the deterministic draft offline with no model call, (c) no raw content is written to any audit/log by the draft path (redaction-safe).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/cockpitDraft.test.ts</automated>
  </verify>
  <done>draftCockpit lives in llm.ts (still the only use-node module); loads the registry drafter; SMOKE:: returns offline; fallback preserved; test green; no raw content in logs.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (incl cockpitDraft.test.ts); typecheck clean.
- llm.ts remains the ONLY `"use node"` module (grep — no second node module added).
- `reportForPlan` returns a live projection; no `report[]` field exists anywhere.
</verification>

<success_criteria>
The plans adapter contract (byThread + reportForPlan + writers) and the draft seam (draftCockpit) are ready. Plan 07 wires the conversation to insertPlan/patchPlan/draftCockpit/proposeEmailPlan; plan 08 subscribes byThread + reportForPlan for the cards.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-06-SUMMARY.md`.
</output>
