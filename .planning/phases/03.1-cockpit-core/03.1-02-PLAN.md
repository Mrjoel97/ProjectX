---
phase: 03.1-cockpit-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/app/globals.css
  - apps/web/playwright.config.ts
  - apps/web/e2e/README.md
  - package.json
autonomous: true
requirements: [INTK-01]

must_haves:
  truths:
    - "`@convex-dev/agent@0.6.4` is a dependency of apps/web at the EXACT same pin as the backend (CLAUDE.md §6) so the chat pane can import `@convex-dev/agent/react` hooks"
    - "Teal design tokens (teal-900/600/400, canvas, card) exist as CSS variables in globals.css, ready for cockpit inline styles"
    - "Playwright is installed + configured (playwright.config.ts, e2e/ dir) — the first UI E2E harness in the repo — with a `test:e2e` script; feature specs land in later plans"
  artifacts:
    - path: "apps/web/package.json"
      provides: "@convex-dev/agent@0.6.4 exact pin + playwright devDependency + test:e2e script"
      contains: "0.6.4"
    - path: "apps/web/app/globals.css"
      provides: "teal cockpit CSS variables"
      contains: "--teal-600"
    - path: "apps/web/playwright.config.ts"
      provides: "Playwright config pointing at apps/web/e2e, baseURL for the local dev server"
      contains: "e2e"
  key_links:
    - from: "apps/web (chat pane, later plans)"
      to: "@convex-dev/agent/react"
      via: "package.json dependency pinned 0.6.4 (matches packages/backend)"
      pattern: "0.6.4"
---

<objective>
Land the frontend + E2E scaffolding the feature waves depend on, touching no application logic: pin `@convex-dev/agent@0.6.4` into apps/web (chat hooks), add the teal design tokens to globals.css, and install + configure Playwright (the repo's first UI E2E harness). Fully parallel with plans 01 and 03 — different files, no shared surface.

Purpose: Wave 0 scaffolding from 03.1-VALIDATION.md. The chat pane, teal-styled cards, and every SC1/SC5 E2E spec need these in place before they can be written.
Output: agent dep pinned in apps/web, teal CSS vars, `playwright.config.ts` + `e2e/` + `test:e2e` script.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-ui.md
@apps/web/package.json
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin @convex-dev/agent@0.6.4 into apps/web</name>
  <files>apps/web/package.json</files>
  <action>
Add `"@convex-dev/agent": "0.6.4"` to `apps/web/package.json` `dependencies` — EXACT pin, no caret (CLAUDE.md §6 pinned pre-1.0), and it MUST equal the backend pin (`packages/backend/package.json:25`). Run `pnpm install` at the repo root so the workspace lockfile resolves. This unlocks `import { useThreadMessages, useUIMessages } from "@convex-dev/agent/react"` in later UI plans. Do not add any other agent-related dep.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @pikar/web exec node -e "require.resolve('@convex-dev/agent/react')"</automated>
  </verify>
  <done>`@convex-dev/agent@0.6.4` resolves from apps/web; pin matches backend exactly; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add teal cockpit design tokens to globals.css</name>
  <files>apps/web/app/globals.css</files>
  <action>
Add the design doc's teal palette as CSS variables in `:root` (exact hex from `email-chat-cockpit.md` §Layout), alongside the existing amber theme (do not remove the amber vars — other pages use them):

```css
:root {
  --teal-900: #0B4F4A; /* left rail / dark frame */
  --teal-600: #009689; /* primary action: send, active tab, Approve, CTAs */
  --teal-400: #40CAD0; /* highlight / logo / glow */
  --canvas:   #F8FAFC; /* app canvas / panels */
  --card:     #FFFFFF; /* cards & bubbles */
}
```

Leave a comment noting the WCAG caveat from RESEARCH-ui §5: `--teal-600` on white is ~2.9:1 — use it for button fills / white-text CTAs, NOT small teal body text. Do not introduce Tailwind or a CSS framework (the repo uses inline `style={}` + CSS vars).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck && node -e "const c=require('fs').readFileSync('apps/web/app/globals.css','utf8'); if(!c.includes('--teal-600')||!c.includes('#009689')) process.exit(1)"</automated>
  </verify>
  <done>All five teal vars present with the design-doc hex; amber theme untouched; no CSS framework added.</done>
</task>

<task type="auto">
  <name>Task 3: Install + configure Playwright (first UI E2E harness)</name>
  <files>apps/web/playwright.config.ts, apps/web/e2e/README.md, apps/web/package.json, package.json</files>
  <action>
Install Playwright as an apps/web devDependency (`@playwright/test`, pinned) and run `pnpm exec playwright install chromium`. Create `apps/web/playwright.config.ts`:
- `testDir: "./e2e"`, `use.baseURL: "http://127.0.0.1:3111"` (the repo's local Next port — confirm against existing dev scripts / STATE.md; it is :3111),
- `webServer` OPTIONAL — the cockpit E2E runs against an already-running `convex dev` + `next` (STATE.md: background dev must stay up; the SMOKE:: offline path needs the live local backend). Prefer `reuseExistingServer: true` and document in the README that the dev backend must be running (mirrors the smokeRun.mjs live-deployment convention).
- one project: chromium.

Create `apps/web/e2e/README.md` documenting: this is the first repo E2E; specs drive the offline `SMOKE::` delivery path; the local `convex dev` + `next` must be running (per STATE.md). No spec files yet — feature specs land in plans 05 (cockpit-render/cockpit-split) and 09 (cockpit-report/connect-gmail).

Add `"test:e2e": "playwright test"` to apps/web/package.json scripts, and expose a root passthrough (`pnpm --filter @pikar/web test:e2e`) if the root package.json aggregates scripts (match the existing smoke:* passthrough pattern).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright --version</automated>
  </verify>
  <done>`playwright.config.ts` present with testDir e2e + chromium; `@playwright/test` installed; `test:e2e` script wired; `e2e/README.md` documents the live-dev-backend + SMOKE:: convention. No feature specs yet.</done>
</task>

</tasks>

<verification>
- `@convex-dev/agent@0.6.4` resolves from apps/web at the exact backend pin.
- Teal vars present; amber theme intact; no Tailwind.
- `playwright test --version` works; config points at e2e/; dev-backend convention documented.
</verification>

<success_criteria>
Later UI plans can import agent React hooks, reference `var(--teal-600)`, and add `.spec.ts` files under `e2e/` that run via `test:e2e`. No application code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-02-SUMMARY.md`.
</output>
