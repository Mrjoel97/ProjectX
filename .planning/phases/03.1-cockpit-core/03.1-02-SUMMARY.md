---
phase: 03.1-cockpit-core
plan: 02
subsystem: ui
tags: [playwright, e2e, convex-agent, css-tokens, next]

# Dependency graph
requires:
  - phase: 02-backend-spine
    provides: "@convex-dev/agent@0.6.4 backend pin (chat-hook peer to match)"
provides:
  - "@convex-dev/agent@0.6.4 pinned in apps/web — chat pane can import @convex-dev/agent/react hooks"
  - "Teal cockpit design tokens (--teal-900/600/400, --canvas, --card) in globals.css"
  - "Playwright E2E harness (first in repo): playwright.config.ts + e2e/ + test:e2e script"
affects: [03.1-cockpit-core plans 04-09, cockpit chat pane, cockpit E2E specs]

# Tech tracking
tech-stack:
  added: ["@convex-dev/agent@0.6.4 (apps/web)", "@playwright/test@1.61.1", "chromium browser"]
  patterns:
    - "E2E specs run against an already-running local stack (reuseExistingServer), mirroring smokeRun.mjs live-deployment convention"
    - "Design system stays inline style={} + CSS vars — no Tailwind/CSS framework"

key-files:
  created:
    - apps/web/playwright.config.ts
    - apps/web/e2e/README.md
  modified:
    - apps/web/package.json
    - apps/web/app/globals.css
    - package.json

key-decisions:
  - "Playwright config uses no webServer block (reuseExistingServer) — E2E needs the live convex dev + next :3111, matching the SMOKE:: offline path convention"
  - "Root test:e2e is a direct pnpm --filter passthrough (not turbo) — e2e needs a live server, not a cached turbo task; smoke:* precedent lives in packages/backend, not root"

patterns-established:
  - "First repo UI E2E harness: apps/web/e2e/, chromium-only, baseURL 127.0.0.1:3111"

requirements-completed: [INTK-01]

# Metrics
duration: 7min
completed: 2026-07-12
---

# Phase 03.1 Plan 02: Cockpit Frontend + E2E Scaffolding Summary

**Wave-0 cockpit scaffolding: @convex-dev/agent@0.6.4 pinned into apps/web for chat hooks, teal design tokens added to globals.css, and Playwright installed as the repo's first UI E2E harness — no application logic touched.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-07-12T03:45:09Z
- **Completed:** 2026-07-12T03:51:51Z
- **Tasks:** 3
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments
- `@convex-dev/agent@0.6.4` resolves from apps/web at the EXACT backend pin — `@convex-dev/agent/react` importable in later cockpit plans
- Teal cockpit palette (`--teal-900/600/400`, `--canvas`, `--card`) live in `globals.css` `:root` alongside the untouched amber theme, with the WCAG ~2.9:1 contrast caveat documented
- Playwright 1.61.1 + chromium installed; `playwright.config.ts` (testDir `e2e`, chromium, baseURL :3111, reuseExistingServer) + `e2e/README.md` documenting the live-dev-backend prerequisite; `test:e2e` script wired in apps/web and root

## Task Commits

1. **Task 1: Pin @convex-dev/agent@0.6.4 into apps/web** — `137415a` (chore) — see Deviations: this landed inside a parallel executor's commit due to a shared git index
2. **Task 2: Add teal cockpit design tokens to globals.css** — `97645d0` (feat)
3. **Task 3: Install + configure Playwright** — `daae84e` (chore)

## Files Created/Modified
- `apps/web/package.json` — `@convex-dev/agent@0.6.4` dep + `@playwright/test@1.61.1` devDep + `test:e2e` script
- `apps/web/app/globals.css` — 5 teal CSS vars in `:root` with WCAG caveat comment
- `apps/web/playwright.config.ts` — created; testDir e2e, chromium, baseURL :3111, reuseExistingServer semantics
- `apps/web/e2e/README.md` — created; documents live convex-dev + next prerequisite, SMOKE:: convention, and where specs land (plans 05, 09)
- `package.json` (root) — `test:e2e` passthrough to `@pikar/web`

## Decisions Made
- Pinned `@playwright/test` to `1.61.1` (current latest) exact, consistent with the repo's exact-pin discipline (CLAUDE.md §6 spirit) — devDep, so no pre-1.0 component-churn risk.
- No `webServer` auto-start in the Playwright config: E2E runs against the already-running `convex dev` (NOT `--once`) + `next` on :3111, because the offline `SMOKE::` delivery path needs the live local backend. This mirrors `scripts/smokeRun.mjs`.
- Root `test:e2e` is a direct `pnpm --filter @pikar/web test:e2e` passthrough rather than a turbo task — e2e requires a live server and must not be a cached turbo task; the smoke:* passthrough precedent lives in `packages/backend`, not root.

## Deviations from Plan

### Environment/Process (not a code fix)

**1. [Parallel-execution git-index collision] Task 1 landed inside a sibling executor's commit**
- **Found during:** Task 1 (pin agent dep) commit
- **Issue:** This plan (03.1-02) runs in the same wave and working tree as plan 03.1-03 (fully parallel per plan objective). Git's index/staging area is shared across the two executors. My staged `apps/web/package.json` (+`pnpm-lock.yaml`) got swept into the plan-03 executor's commit `137415a refactor(03.1-03): promote isValidEmail…` before my own `git commit` ran. A stale `.git/index.lock` from the concurrent process also blocked the first commit attempt.
- **Fix:** Verified the change is present and correct in HEAD (`git show HEAD:apps/web/package.json` shows `@convex-dev/agent: 0.6.4`). No re-commit possible (working tree matches HEAD). Subsequent tasks (2, 3) committed with a lock-retry loop to survive concurrent `.git/index.lock` contention — both landed as their own clean commits (`97645d0`, `daae84e`).
- **Verification:** `pnpm --filter @pikar/web exec node -e "require.resolve('@convex-dev/agent/react')"` → RESOLVED OK; dep present in committed HEAD.
- **Committed in:** `137415a` (shared with plan 03.1-03)

---

**Total deviations:** 1 (process/environment, no code impact)
**Impact on plan:** All three artifacts landed and verified correct. Task 1's commit message is attributed to the sibling plan due to the shared-index race, but the content (agent pin + lockfile) is in the repo and resolves. No scope creep, no functional impact.

## Issues Encountered
- **Peer-dependency warnings:** `@convex-dev/agent@0.6.4` warns on unmet peers `ai@^6.0.35` (found 7.0.20) and `@ai-sdk/provider-utils@^4.0.6` (found 5.0.7). These are pre-existing in the backend (which pins the same `ai@7.0.20` + `@convex-dev/agent@0.6.4`) and are inherent to the mandated exact pins (CLAUDE.md §6). Not introduced by this plan; agent/react still resolves and web typecheck is clean.
- **Concurrent `.git/index.lock`:** the parallel executor left a stale lock; handled with a retry-and-clear loop on commit.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Later cockpit UI plans can now `import { useThreadMessages, useUIMessages } from "@convex-dev/agent/react"`, reference `var(--teal-600)` etc. in inline styles, and add `.spec.ts` files under `apps/web/e2e/` that run via `pnpm --filter @pikar/web test:e2e`.
- Reminder for E2E authors: the local `convex dev` (NOT `--once`) + `next` on :3111 must be running before specs execute (documented in `e2e/README.md`).

---
*Phase: 03.1-cockpit-core*
*Completed: 2026-07-12*

## Self-Check: PASSED

- Files: playwright.config.ts, e2e/README.md, globals.css, package.json, SUMMARY.md all present
- Commits: 137415a, 97645d0, daae84e all in git log
- Artifact: `@convex-dev/agent@0.6.4` present in committed HEAD apps/web/package.json
