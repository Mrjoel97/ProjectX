---
phase: 03.1-cockpit-core
plan: 03
subsystem: core
tags: [slot-filling, state-machine, email, pure-ts, vitest, tdd]

# Dependency graph
requires:
  - phase: 02-guardrails
    provides: validateSubmit.ts (EMAIL_RE / isValidEmail source regex, INTK-04 trust boundary)
provides:
  - "emailIntent pure module: EmailIntentState, emptyIntent, nextQuestion(state), applyAnswer(state, answer)"
  - "isValidEmail — one shared email regex, exported from validateSubmit, reused by two callers"
  - "Deterministic next-question brain (SC2/SC3) the agent lane (plan 07) drives with no re-derivation"
affects: [03.1-07 agent lane, cockpit chat turn loop, plan-card mode/attachment surfacing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure slot-filling state module (CLAUDE.md §1): no Convex/Date/random imports; ordered if-branches ARE the spec; discriminated-union returns"
    - "Trust boundary: invalid recipients bounce in `rejected`, never stored in state.recipients — 'nothing sent on an assumption' is structural (no send path exists)"
    - "One regex, two callers: isValidEmail promoted to a named export rather than adding a divergent validator (ponytail root-cause shape)"

key-files:
  created:
    - packages/core/src/emailIntent.ts
    - packages/core/src/emailIntent.test.ts
  modified:
    - packages/core/src/validateSubmit.ts
    - packages/core/src/index.ts

key-decisions:
  - "isValidEmail is the single email regex in @pikar/core; validateSubmit + emailIntent both route through it (no PII-scan regex reuse — that one is unanchored/prose-tolerant, wrong tool)"
  - "nextQuestion order: reask (pending invalid) → ask_recipients → ask_subject → ask_body_intent → ask_mode (only if >1 recipient) → ready; never ready while a required slot is missing or a recipient is pending"
  - "attachmentIntent is tracked for the PLAN card but NEVER gates ready (slice-1 attachment is upload-only, no ask_attachment question)"

patterns-established:
  - "Slot-filling module cloned 1:1 on validateSubmit template (pure fn, doc-comment invariant, side-by-side vitest, named exports only)"
  - "applyAnswer is immutable + dedupes recipients case-insensitively; a clean recipients answer clears the prior rejection"

requirements-completed: [INTK-01, AGNT-02]

# Metrics
duration: 6min
completed: 2026-07-12
---

# Phase 03.1 Plan 03: emailIntent Slot-Filling Brain Summary

**Pure, tested `emailIntent` module — the deterministic next-question engine for the cockpit conversation (SC2/SC3): asks one missing slot at a time in fixed order, bounces invalid recipients without storing them, and surfaces `ask_mode` only when there's a real choice — with no send path and no Convex import.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-07-12T03:45:17Z
- **Completed:** 2026-07-12T03:51:01Z
- **Tasks:** 2
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- Promoted the private `EMAIL_RE` to an exported `isValidEmail` helper — one regex, two callers; `validateSubmit` unchanged in behavior (8 tests still green).
- Built `emailIntent.ts`: pure `nextQuestion`/`applyAnswer` over an immutable `EmailIntentState`, with discriminated-union `NextQuestion`/`Answer`/`ApplyResult` returns.
- Invalid recipients bounce back in `rejected` and are never stored; case-insensitive dedupe; `ask_mode` fires only for >1 recipient; `attachmentIntent` never gates `ready`.
- 8-case runnable check (`emailIntent.test.ts`) proving ordering, invalid-email re-ask, mode trigger, never-ready-early, immutability/dedupe, and validator reuse — full suite 35/35 green, typecheck clean.

## Task Commits

Each task was committed atomically:

1. **Task 1: Promote isValidEmail to a shared named export** - `137415a` (refactor)
2. **Task 2: emailIntent module + the one runnable check (TDD RED → GREEN)** - `fbc882d` (feat)

_Note: Task 2 was authored test-first (RED confirmed: module absent → import failure), then implemented to GREEN. Committed as one atomic feat since the module and its check ship together._

## Files Created/Modified
- `packages/core/src/emailIntent.ts` - Pure slot-filling state module: `EmailIntentState`, `emptyIntent`, `nextQuestion`, `applyAnswer`, plus `NextQuestion`/`Answer`/`ApplyResult`/`RecipientMode` types.
- `packages/core/src/emailIntent.test.ts` - 8 table-driven vitest cases (the one runnable check).
- `packages/core/src/validateSubmit.ts` - `EMAIL_RE` → exported `isValidEmail`; `validateSubmit` routes through it.
- `packages/core/src/index.ts` - Re-exports `./emailIntent`.

## Decisions Made
- Merged (append + case-insensitive dedupe) rather than replaced on a `recipients` answer, so the re-ask flow keeps the earlier valid recipient and appends the corrected one.
- `reask_recipient` surfaces a single `invalid` string (`rejected[0]`) so the caller re-asks exactly one at a time.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Narrowed `state.rejected[0]` for `noUncheckedIndexedAccess`**
- **Found during:** Task 2 (typecheck after tests passed)
- **Issue:** `tsc --noEmit` failed — under the strict `noUncheckedIndexedAccess` tsconfig, `state.rejected[0]` is `string | undefined` even after a `.length > 0` guard, which is not assignable to `NextQuestion.invalid: string`.
- **Fix:** Read `const pending = state.rejected?.[0]` and branch on `pending !== undefined`, which narrows cleanly.
- **Files modified:** packages/core/src/emailIntent.ts
- **Verification:** `pnpm --filter @pikar/core exec tsc --noEmit` clean; 35/35 tests green.
- **Committed in:** `fbc882d` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary for the strict typecheck to pass; no behavior change, no scope creep.

## Issues Encountered
- One test assertion I wrote was wrong (expected `ask_mode` immediately after fixing a recipient, but `subject` was still unfilled so the correct next question is `ask_subject`). The module was correct; I corrected the test expectation. No production-code change.

## User Setup Required
None - pure package, no external service configuration required.

## Next Phase Readiness
- The agent lane (plan 07) can drive conversation turns directly off `nextQuestion`/`applyAnswer` with no re-derivation of SC2/SC3 logic.
- The module structurally cannot send (no send path, no Convex import) — "nothing is ever sent on an assumption" holds by construction.

## Self-Check: PASSED

All created/modified files present on disk; both task commits (`137415a`, `fbc882d`) found in history.

---
*Phase: 03.1-cockpit-core*
*Completed: 2026-07-12*
