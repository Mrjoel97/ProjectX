---
phase: 03.1-cockpit-core
plan: 07
type: execute
wave: 3
depends_on: [03, 04, 06]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
autonomous: true
requirements: [AGNT-02, REVW-01, DLVR-01]

must_haves:
  truths:
    - "A tenant-scoped agent action drives the guided conversation deterministically: it saves the user turn, folds the answer into the plans row via emailIntent (nextQuestion/applyAnswer), and either asks the next question or drafts the body + proposes the PLAN (DECISION #2 — the LLM is only the draft) (SC2/SC3)"
    - "An invalid recipient re-asks ONLY that one and is never stored; the PLAN is proposed only when emailIntent reports `ready` — nothing is proposed on an assumption (SC2)"
    - "For >1 recipient the mode (individual/group) is asked and stored, and the proposed plan carries recipients + mode + subject + body for the PLAN card (SC3)"
    - "`executePlan` is a human-gated tenantMutation (NOT an LLM tool): a compare-and-set on plan.status (proposed→approved) makes double-approve send once and guarantees zero sends before Approve (SC4)"
    - "executePlan seeds one requests row per recipient (own server-minted correlationId, planId, body) and is the SOLE caller of workflow.start(deliverApprovedPlan) (SC4)"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "cockpit agent thread action (deterministic turn) + proposeEmailPlan + executePlan approve-gate"
      exports: ["sendCockpitMessage", "executePlan"]
    - path: "packages/backend/convex/cockpit.test.ts"
      provides: "SC4 convex-test: executePlan idempotency (double-approve = one workflow) + zero-sends-before-approve"
      contains: "executePlan"
  key_links:
    - from: "packages/backend/convex/cockpit.ts (turn action)"
      to: "@pikar/core emailIntent (nextQuestion/applyAnswer)"
      via: "deterministic next-question selection over the plans row state"
      pattern: "nextQuestion"
    - from: "packages/backend/convex/cockpit.ts (executePlan CAS)"
      to: "internal.deliverApprovedPlan.deliverApprovedPlan"
      via: "workflow.start only when status transitions proposed→approved"
      pattern: "workflow\\.start"
---

<objective>
Wire the guided conversation to the plan: a deterministic agent-thread action that turns user messages into slot updates + next questions (via the pure `emailIntent` module), drafts the body when ready (`draftCockpit`), proposes the PLAN, and a human-gated `executePlan` approve gate that idempotently starts the fan-out. This is the orchestration seam between plans 03/06 (emailIntent + adapter + draft) and plan 04 (the fan-out workflow).

Purpose: DECISION #2 (deterministic control — the thread is a message store + streaming; the LLM is only the draft; proposeEmailPlan is code-invoked when emailIntent reports `ready`; executePlan is a human gate, not an LLM tool). SC2/SC3 (guided conversation), SC4 (idempotent approve, zero sends before Approve). The Agent component's FIRST instantiation in the codebase.
Output: `cockpit.ts` (thread action + proposeEmailPlan + executePlan), `cockpit.test.ts`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-agent.md
@.planning/phases/03.1-cockpit-core/03.1-RESEARCH-delivery.md
@packages/backend/convex/convex.config.ts
@packages/backend/convex/requests.ts
@packages/backend/convex/lib/functions.ts
@packages/backend/convex/index.ts
@packages/backend/convex/plans.ts
@packages/backend/convex/llm.ts

<interfaces>
<!-- From plan 01: tenantAction; plans table + status enum collecting|proposed|approved|delivering|done -->
<!-- From plan 03: import { emptyIntent, nextQuestion, applyAnswer, isValidEmail } from "@pikar/core" -->
<!-- From plan 06: internal.plans.insertPlan/patchPlan/setPlanStatus/byThread; internal.llm.draftCockpit({ tenantId, safeText, safeTextHash }) -->
<!-- From plan 04: internal.deliverApprovedPlan.deliverApprovedPlan (workflow); onComplete = internal.deadLetter.onPipelineComplete -->
<!-- Agent 0.6.4 (RESEARCH-agent §1-2): new Agent(components.agent, { name, languageModel:"openai/gpt-4o-mini" }); createThread(ctx,{ userId }); saveMessage/listMessages; createTool uses inputSchema+execute (NOT args/handler) -->
<!-- Existing pattern to mirror: requests.submit → server-mints correlationId (never client-supplied) → workflow.start; deadLetters.markResolved CAS guard (if status !== "new" return) -->
<!-- guardrails.prepare / scanText → safeText + safeTextHash (redact body-intent before draftCockpit) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cockpit agent thread + deterministic guided turn</name>
  <files>packages/backend/convex/cockpit.ts</files>
  <action>
Instantiate the agent (first use): `const cockpitAgent = new Agent(components.agent, { name: "email-cockpit", languageModel: "openai/gpt-4o-mini" })` — OMIT a hardcoded `instructions` prompt (CLAUDE.md §5; the only LLM call is the draft, which loads the registry skill).

Export a tenant-scoped public action `sendCockpitMessage` (use `tenantAction` from plan 01). Handler (deterministic — DECISION #2):
1. Ensure a thread: create on first call (`createThread(ctx, { userId: ctx.tenantId })` so `ctx.userId === tenantId`) and a matching `plans` row (`internal.plans.insertPlan`, status `collecting`); return `threadId` so the client binds the chat hooks + `plans.byThread`.
2. `saveMessage` the user's text to the thread.
3. Load the current plans row (`byThread`), project it into `EmailIntentState`. Determine the slot being answered from the PRIOR `nextQuestion(state)` and map the free text to an `Answer`: recipients → split on comma/whitespace + `isValidEmail` each; subject/bodyIntent → the whole reply; mode → parse individual/separate vs group/together. (This free-text→Answer mapping is a small local helper — the pure slot logic stays in @pikar/core.)
4. `applyAnswer(state, answer)`. If it returns `rejected` (invalid recipients), patch only the valid ones and saveMessage an assistant `reask_recipient` for the bad one. Else `patchPlan` the new slots.
5. `nextQuestion(newState)`:
   - a question → `saveMessage` the assistant question text (deterministic copy per kind), return.
   - `ready` → redact the bodyIntent (`guardrails.prepare`/`scanText` → safeText/safeTextHash), call `internal.llm.draftCockpit({ tenantId, safeText, safeTextHash })`, then call `proposeEmailPlan` (Task 2) to write subject/body + recipients + mode and flip status → `proposed`; saveMessage an assistant "Here's your plan — review and Approve." The PLAN card renders reactively via `plans.byThread`.

Keep the whole flow inside the action; the LLM is invoked ONLY at the draft step. Add explicit return-type annotations if agent usage tips the use-node inference limit.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec convex codegen && pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>`sendCockpitMessage` compiles; drives turns via emailIntent; only the draft calls the LLM; a proposed plans row is written on `ready`; agent instantiated with no hardcoded prompt.</done>
</task>

<task type="auto">
  <name>Task 2: proposeEmailPlan (write) + executePlan (idempotent approve gate)</name>
  <files>packages/backend/convex/cockpit.ts</files>
  <action>
`proposeEmailPlan` — a code-invoked internal write (NOT an LLM-decided tool; DECISION #2): given `{ planId, recipients, mode, subject, body }`, `patchPlan` the row and set status `proposed`. (Model it as a `createTool` ONLY if streaming ergonomics require it — the deterministic default is a plain internal mutation call from Task 1. Recipient is structural, never model-invented.)

`executePlan` — the human gate, a `tenantMutation` (RESEARCH-agent §2, RESEARCH-delivery §4):
```
const plan = await ctx.db.get(planId);
if (!plan || plan.tenantId !== ctx.tenantId) throw new Error("plan not found");
if (plan.status !== "proposed") return { ok: true, alreadyStarted: true }; // idempotent no-op (double-approve)
// Gmail-connected pre-check (design: no send without a mailbox):
//   if not connected → throw / return a needs-connect result (reuse internal.gmailAuth.getTokens)
await ctx.db.patch(planId, { status: "approved" });                        // CAS: 2nd concurrent tx sees "approved"
// seed ONE requests row per recipient (individual mode) OR one row w/ comma-joined To: (group mode):
//   { tenantId, correlationId: crypto.randomUUID() /* server-minted, per row */, goal: plan.subject,
//     draft: plan.body, recipient, status: "approved", planId, attachmentRefs: [], createdAt }
const workflowId = await workflow.start(ctx, internal.deliverApprovedPlan.deliverApprovedPlan,
  { planId, tenantId: ctx.tenantId, requestIds, correlationIds },
  { onComplete: internal.deadLetter.onPipelineComplete,
    context: { tenantId: ctx.tenantId, correlationId: planCid, payload: { planId } } }); // refs only (§4)
await ctx.db.patch(planId, { status: "delivering", correlationId: planCid, workflowId });
return { ok: true, workflowId };
```
`executePlan` MUST be the SOLE `workflow.start(deliverApprovedPlan)` call site (grep-able invariant = zero sends before Approve, RESEARCH-delivery §5). correlationIds are server-minted per row (never client-supplied — mirrors requests.submit:47). Audit/DLQ context carries planId/correlationId refs ONLY.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec convex codegen && pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>proposeEmailPlan writes the proposed plan; executePlan CAS-gates on status==="proposed", seeds per-recipient rows with distinct server-minted cids, and is the only starter of the fan-out.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cockpit.test.ts — idempotency + zero-sends-before-approve</name>
  <files>packages/backend/convex/cockpit.test.ts</files>
  <action>
convex-test (unit — the CAS + seeding are pure mutation logic, testable without the component workflow). Write these first, then confirm green:
- Idempotency: seed a `proposed` plan; call `executePlan` twice; assert exactly ONE workflow started (mock/spy `workflow.start` or assert only one set of requests rows seeded + status transitioned once) — double-approve sends once.
- Zero-sends-before-approve: a plan in `collecting` (or any status !== "proposed") passed to `executePlan` → no requests rows seeded, no workflow.start; assert the guard no-ops. Also assert (grep-style or by construction) that no path other than executePlan starts deliverApprovedPlan.
- Tenant guard: a plan owned by another tenant → "plan not found" (no cross-tenant approve).
(The full fan-out delivery + isolation + redaction is covered by smoke:fanout in plan 04; this file pins the mutation-level invariants convex-test CAN run.)
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/cockpit.test.ts</automated>
  </verify>
  <done>cockpit.test.ts green: double-approve = one workflow; non-proposed status never seeds/starts; cross-tenant approve rejected.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (incl cockpit.test.ts); typecheck + codegen clean.
- `executePlan` is the sole `workflow.start(deliverApprovedPlan)` call site (grep).
- Agent instantiated with no hardcoded instructions; LLM called only at the draft.
- No recipient/subject/body in any audit/DLQ context from cockpit.ts (§4).
</verification>

<success_criteria>
SC2/SC3 backend (guided turn → proposed plan) and SC4 (idempotent approve, zero sends before Approve) delivered. Plan 08 subscribes the plan/report queries and wires the composer to `sendCockpitMessage` + the Approve button to `executePlan`.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cockpit-core/03.1-07-SUMMARY.md`.
</output>
