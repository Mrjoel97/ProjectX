---
phase: 03.1-cockpit-core
verified: 2026-07-12T15:12:18Z
status: passed
score: 12/12 must-haves verified (2 carried by already-approved human checkpoint)
---

# Phase 3.1: Cockpit Core Verification Report

**Phase Goal:** The manual `/submit` form + `/review` queue are replaced by a conversational
two-pane cockpit — a guided slot-filling conversation assembles a PLAN, the user approves once,
and execution fans out hands-off to multiple recipients through the existing governed engine
(Gmail, audit, telemetry, DLQ, tenant scoping, durable workflows), with a live per-recipient
report.

**Verified:** 2026-07-12 (static inspection only — no dev server / E2E run per instruction)
**Status:** passed
**Re-verification:** No — initial verification

**Method note:** This verification is static-only (file/content/commit inspection). The live
multi-recipient Gmail send, PLAN→single-Approve gating, no-double-send, and divider ergonomics
were already exercised and approved by the human during 03.1-09 Task 3 (user typed "approved").
That checkpoint is treated as satisfied evidence for the manual-only truths; this report confirms
the supporting code exists and is wired, not a re-run of that checkpoint.

## Goal Achievement

### Observable Truths (SC1–SC5 + Groundwork, from 03.1-VALIDATION.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1: `/dashboard/workspace` renders a two-pane cockpit (chat ~30%/workspace ~70%) under the `(app)` auth gate | ✓ VERIFIED | `apps/web/app/(app)/dashboard/workspace/page.tsx` composes `SplitPane(ChatPane, CardList)`; layout.tsx auth gate untouched (per SUMMARY, confirmed no diff outside NAV entry) |
| 2 | SC1: divider drags, clamps ≥20% each pane, persists across reload (localStorage) | ✓ VERIFIED (code) / already human-approved (ergonomics) | `SplitPane.tsx`: `role="separator"`, `aria-valuenow`, clamp logic, `localStorage.setItem` on pointerUp/keydown; human checkpoint (03.1-09 Task 3) confirmed clamp + persistence live |
| 3 | SC1: divider keyboard-accessible (role=separator, aria-valuenow, arrow-key ±2%) | ✓ VERIFIED | `SplitPane.tsx` lines ~8-100 show `role="separator"`, `aria-valuenow={Math.round(pct)}`, arrow-key nudge |
| 4 | SC2: `emailIntent` asks one missing slot at a time in fixed order; invalid recipient never enters state, bounces in `rejected`; never returns `ready` early | ✓ VERIFIED | `packages/core/src/emailIntent.ts` (108 lines) + `emailIntent.test.ts` (114 lines, 8 cases: ordering, invalid-email re-ask, mode trigger, never-ready-early, immutability/dedupe, validator reuse) |
| 5 | SC3: mode question (`ask_mode`) emitted only when recipients.length > 1 | ✓ VERIFIED | `emailIntent.ts` conditional-mode logic; test asserts single recipient skips to `ready` with no `ask_mode` |
| 6 | SC2/SC3: guided conversation is deterministic — LLM invoked ONLY at body-draft step; questions come from `nextQuestion`/`applyAnswer` | ✓ VERIFIED | `cockpit.ts` `sendCockpitMessage`: folds text via `parseAnswer`→`applyAnswer`, deterministic question copy; LLM call is the single `internal.llm.draftCockpit` invocation at `ready` |
| 7 | SC3: PLAN card shows recipients + mode + subject + body preview + one Approve, fed by `plans.byThread` | ✓ VERIFIED | `cards.tsx` `PlanCard` renders when `plan.status === "proposed"`: recipient chips, mode, subject, body preview, one Approve wired to `executePlan` with `busy` guard |
| 8 | SC4: `executePlan` is a human-gated, idempotent CAS (proposed→approved); double-approve sends once; zero sends before Approve | ✓ VERIFIED | `cockpit.ts:247` `executePlan` (tenantMutation): re-reads status, no-ops if not `"proposed"`; `cockpit.test.ts` has explicit idempotency, zero-sends-before-approve, tenant-guard, and mailbox-precheck tests (4 of 8 cases) |
| 9 | SC4: fan-out reaches every recipient reusing gmail.send/workpool/audit/telemetry/DLQ verbatim | ✓ VERIFIED | `deliverApprovedPlan.ts` loops recipient rows calling `internal.gmail.send` (reused), `smoke:fanout` assertions (`assertFanoutReachedAll`) exist and were reported green in 03.1-04 SUMMARY |
| 10 | SC5: one recipient failing dead-letters that row only; rest still send; workflow completes success | ✓ VERIFIED | `deliverApprovedPlan.ts` per-row try/catch → `internal.deadLetter.deadLetterRecipient`; `smokeAssert.ts` `assertRecipientDeadLettered` (isolation assertion) present |
| 11 | SC5: REPORT is a live per-recipient projection (no `report[]` array, no report writer) | ✓ VERIFIED | `plans.ts` `reportForPlan` (tenantQuery) reads `requests` `by_plan` joined to `gmail.sent` audit; schema.ts `plans` table has no `report` field; `cards.tsx` `ReportCard` uses `useQuery(api.plans.reportForPlan,...)` (reactive, no polling) |
| 12 | Gov: no raw email content (recipient/subject/body) in any audit/DLQ/telemetry payload | ✓ VERIFIED | `deadLetterRecipient` payload is `{requestId}` ref-only; `llmRedaction.test.ts` extended with 2 cockpit-plane assertions (`cockpit.ts`/`plans.ts` emit no audit/DLQ/telemetry write; workflow.start context payload refs-only); `assertNoRawPiiFanout` at smoke layer |
| 13 | Groundwork: connect-gmail resolves (no infinite spinner) on authed session, inherits 18c8442 fix | ✓ VERIFIED (code) / already human-approved (real session) | `connect-gmail.spec.ts` asserts bounded resolution; layout.tsx `<Authenticated>` gate not re-touched (grep confirms only NAV-entry diff per SUMMARY); human checkpoint confirmed on a real Google session |
| 14 | Manual: a real Gmail send actually delivers to 1-2 real recipients; PLAN→single-Approve gating holds; no double-send; divider ergonomics feel right | ✓ ALREADY HUMAN-APPROVED | 03.1-09 Task 3 checkpoint — user typed "approved" confirming real delivery, gating, no double-send, divider clamp/persist (per 03.1-09-SUMMARY.md) |

**Score:** 14/14 truths verified (12 by static code inspection, 2 carried by the already-approved
human checkpoint per task instructions — not re-run here).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/backend/convex/schema.ts` | `plans` table + `requests.planId` + `by_plan` index | ✓ VERIFIED | Confirmed: `plans: defineTable(...)`, `planId: v.optional(v.id("plans"))`, `.index("by_plan", ["planId"])` |
| `packages/backend/convex/lib/functions.ts` | `tenantAction` wrapper | ✓ VERIFIED | `export const tenantAction = customAction(...)` alongside `tenantQuery`/`tenantMutation` |
| `biome.json` | raw `action` import banned outside lib/functions.ts | ✓ VERIFIED | `"importNames": ["query", "mutation", "action"]` in both `_generated/server` path entries |
| `apps/web/package.json` | `@convex-dev/agent@0.6.4` exact pin + Playwright | ✓ VERIFIED | `"@convex-dev/agent": "0.6.4"`, `"@playwright/test": "1.61.1"`, `"test:e2e": "playwright test"` |
| `apps/web/app/globals.css` | teal design tokens | ✓ VERIFIED | `--teal-900`, `--teal-600: #009689`, `--teal-400`, `--canvas`, `--card` present with WCAG comment |
| `apps/web/playwright.config.ts` | Playwright config, testDir e2e, baseURL :3111 | ✓ VERIFIED | `testDir: "./e2e"`, `baseURL: "http://127.0.0.1:3111"`, chromium project |
| `packages/core/src/emailIntent.ts` + `.test.ts` | pure slot-filling module + runnable check | ✓ VERIFIED | 108-line module, 114-line test (8 cases), re-exported from `index.ts`, zero Convex/Date/random imports |
| `packages/core/src/validateSubmit.ts` | `isValidEmail` promoted export | ✓ VERIFIED | `export const isValidEmail = (s: string): boolean => ...` |
| `packages/backend/convex/deliverApprovedPlan.ts` | fan-out workflow + `markPlanDone` | ✓ VERIFIED | `workflow.define(...)`, per-row try/catch, local `markPlanDone` internalMutation |
| `packages/backend/convex/deadLetter.ts` | `deadLetterRecipient` | ✓ VERIFIED | `export const deadLetterRecipient = internalMutation(...)` |
| `packages/backend/scripts/run-smoke-fanout.mjs` + `smoke.ts`/`smokeAssert.ts` | fan-out smoke harness | ✓ VERIFIED | `seedFanout`, `assertFanoutReachedAll`, `assertRecipientDeadLettered`, `assertFanoutIdempotent`, `assertNoRawPiiFanout`, `smoke:fanout` script all present |
| `packages/backend/convex/plans.ts` | thin content-plane adapter | ✓ VERIFIED | `insertPlan`, `patchPlan`, `setPlanStatus`, `byThread`, `reportForPlan` all exported |
| `packages/backend/convex/llm.ts` | `draftCockpit` (inside sole use-node module) | ✓ VERIFIED | `export const draftCockpit = internalAction(...)`; loads `EMAIL_DRAFTER_SKILL` via `getActiveSkill` |
| `packages/backend/convex/cockpitDraft.test.ts` | drafter registry-load + SMOKE:: test | ✓ VERIFIED | Tests reference `email-drafter` registry load + `SMOKE::route=direct_llm::` offline path |
| `packages/backend/convex/cockpit.ts` | agent thread action + proposeEmailPlan + executePlan | ✓ VERIFIED | `sendCockpitMessage`, `proposeEmailPlan`, `executePlan`, `new Agent(components.agent, {...})` (no hardcoded instructions), sole production `workflow.start(deliverApprovedPlan)` call site |
| `packages/backend/convex/cockpit.test.ts` | idempotency + zero-send tests | ✓ VERIFIED | 123 lines, 8 tests: idempotent approve, zero-sends-before-approve, mailbox precheck, tenant guard, `parseAnswer` table-driven cases |
| `apps/web/.../workspace/page.tsx`, `SplitPane.tsx`, `ChatPane.tsx`, `cards.tsx` | two-pane cockpit shell + chat + cards | ✓ VERIFIED | All 4 files present; `ChatPane` uses `useThreadMessages` + `sendCockpitMessage`; `cards.tsx` has `PlanCard`/`DraftCard`/`ReportCard` with Approve→`executePlan`, `ReportCard`→`reportForPlan` |
| `apps/web/e2e/cockpit-render.spec.ts`, `cockpit-split.spec.ts` | SC1 E2E specs | ✓ VERIFIED (exists, content matches SC1) | Both present under `apps/web/e2e/` |
| `apps/web/e2e/cockpit-report.spec.ts`, `connect-gmail.spec.ts` | SC4/SC5/groundwork E2E specs | ✓ VERIFIED (exists, content matches SC) | `cockpit-report.spec.ts` drives `SMOKE::route=direct_llm::` flow to a live report; `connect-gmail.spec.ts` asserts bounded spinner resolution |
| `packages/backend/convex/llmRedaction.test.ts` | cockpit-plane redaction assertions | ✓ VERIFIED | Extended with cockpit-specific tests referencing `cockpit.ts`/`plans.ts` and refs-only `workflow.start` context |

**Note on E2E "green" status:** Per 03.1-05/08/09 SUMMARYs, the four Playwright specs compile,
typecheck clean, and are discovered by `playwright --list`, but an automated in-session live run
was consistently deferred (no persistent `convex dev` + `next :3111` + seeded auth during
execution) — documented as a carried-forward limitation, not a code defect. The 03.1-09 human
checkpoint substituted a real, more rigorous end-to-end run (actual OAuth, actual Gmail send,
actual divider drag) and the user approved it. Per this verification's instructions, that
approval is treated as satisfied evidence; this report does not re-run the specs.

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `requests.planId` | `plans` table | `v.optional(v.id("plans"))` + `by_plan` index | ✓ WIRED | Confirmed in schema.ts |
| `lib/functions.ts (tenantAction)` | `requireTenant` | `customAction(action, customCtx(...))` | ✓ WIRED | Confirmed |
| `emailIntent.ts` | `validateSubmit.ts (isValidEmail)` | `import { isValidEmail }` | ✓ WIRED | Confirmed shared regex, one source of truth |
| `deliverApprovedPlan.ts` | `internal.gmail.send` | `step.runAction` per requestId | ✓ WIRED | Confirmed |
| `deliverApprovedPlan.ts` | `internal.deadLetter.deadLetterRecipient` | catch → per-row dead-letter | ✓ WIRED | Confirmed |
| `llm.ts (draftCockpit)` | `internal.skills.getActiveSkill(EMAIL_DRAFTER_SKILL)` | registry load | ✓ WIRED | Confirmed, no hardcoded prompt |
| `plans.ts (reportForPlan)` | `requests` by_plan + `gmail.sent` audit | reactive projection | ✓ WIRED | Confirmed |
| `cockpit.ts (turn action)` | `@pikar/core emailIntent` | `nextQuestion`/`applyAnswer` | ✓ WIRED | Confirmed |
| `cockpit.ts (executePlan CAS)` | `internal.deliverApprovedPlan.deliverApprovedPlan` | `workflow.start` on proposed→approved transition | ✓ WIRED | Confirmed sole production call site (smoke.ts's `seedFanout` also calls it directly — intentional test-seeding path for `smoke:fanout`, bypassing the approve gate by design, does not affect the production invariant) |
| `cards.tsx (PlanCard Approve)` | `api.cockpit.executePlan` | `useMutation` + busy guard | ✓ WIRED | Confirmed |
| `cards.tsx (ReportCard)` | `api.plans.reportForPlan` | `useQuery` reactive | ✓ WIRED | Confirmed |
| `ChatPane.tsx` | `api.cockpit.sendCockpitMessage` / `listThreadMessages` | `useAction` / `useThreadMessages` | ✓ WIRED | Confirmed; `listThreadMessages` added as a Rule-3 prerequisite in plan 08 |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| INTK-01 | 02, 03, 05, 09 | User can submit a request (text) with optional attachments | ✓ SATISFIED | Cockpit composer (`ChatPane.tsx`) + `emailIntent` slot-filling replace the manual submit UX; attachment picker reused (upload-only, scoped) |
| AGNT-02 | 01, 03, 06, 07, 08 | User can see the plan before approving execution | ✓ SATISFIED | `PlanCard` (cards.tsx) renders recipients/mode/subject/body preview from `plans.byThread` before the single Approve |
| REVW-01 | 05, 08, 09 | Single plan-level approval gate, hands-off execution after | ✓ SATISFIED | `executePlan` CAS gate (cockpit.ts) + `PlanCard` Approve button (cards.tsx); idempotency proven in `cockpit.test.ts`; real-world gating confirmed by the 03.1-09 human checkpoint |
| DLVR-01 | 01, 04, 06, 07, 08, 09 | Approved responses delivered via Gmail through the provider-agnostic adapter | ✓ SATISFIED | `deliverApprovedPlan.ts` reuses `internal.gmail.send` verbatim per recipient; smoke-proven fan-out + isolation; real send confirmed by human checkpoint |

**Orphaned requirements check:** REQUIREMENTS.md traceability table (lines 126-143) still lists
these four IDs against "Phase 2 | Complete" — this is pre-existing bookkeeping, explicitly
annotated at line 35: REVW-01 "Redefined 2026-07-10... the plan-level gate ships with the
cockpit, Phase 3.1, whose manual checkpoint carries the end-user verification." All four IDs are
claimed by at least one 03.1 plan's `requirements:` frontmatter and are accounted for — no
orphans against this phase.

### Anti-Patterns Found

None. Grep sweep across `cockpit.ts`, `plans.ts`, `deliverApprovedPlan.ts`, `cards.tsx`,
`ChatPane.tsx`, `page.tsx`, `SplitPane.tsx` for `TODO|FIXME|XXX|HACK|not implemented|coming soon`
returned zero matches. `ponytail:` comments present are intentional, scoped simplifications with
documented upgrade paths (e.g., localStorage split persistence, static non-streaming chat list,
no inline plan editor) — consistent with CLAUDE.md §8 discipline, not gaps.

### Human Verification Required

None outstanding. The two genuinely-manual items (real multi-recipient Gmail delivery + PLAN-gate
integrity; divider drag/keyboard ergonomics) were already checkpointed and approved by the user
during 03.1-09 Task 3, per the task's own instructions. No new human verification is requested by
this report.

For completeness, the following remain soft follow-ups (not blocking phase goal achievement):
- An automated, in-CI green run of the four Playwright specs (`cockpit-render`, `cockpit-split`,
  `cockpit-report`, `connect-gmail`) against a persistent local stack has not yet happened — every
  execution attempt during 03.1-05/08/09 hit the same deployment-env/auth-harness limitation
  (documented, out of scope for cockpit code). Recommended: wire this into the repo's regular CI
  or a documented pre-release checklist so it's not perpetually deferred.

### Gaps Summary

No gaps found. All 12 statically-verifiable observable truths hold in the codebase with concrete
artifacts and wiring; the 2 remaining truths (real Gmail delivery, divider ergonomics feel) were
already exercised and explicitly approved by the human during the phase's own human-verify
checkpoint (03.1-09 Task 3), which this verification treats as satisfied per its instructions.
Requirement IDs INTK-01/AGNT-02/REVW-01/DLVR-01 all reconcile against REQUIREMENTS.md with no
orphans. No stub/placeholder/anti-pattern code was found in the touched files. All referenced
commit hashes across the nine SUMMARY.md files were confirmed present in git history.

---

*Verified: 2026-07-12T15:12:18Z*
*Verifier: Claude (gsd-verifier)*
