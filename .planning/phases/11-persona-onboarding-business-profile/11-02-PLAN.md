---
phase: 11-persona-onboarding-business-profile
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/backend/convex/onboarding.ts
  - packages/backend/convex/onboarding.test.ts
  - packages/backend/convex/profileRedaction.test.ts
  - docs/playbooks/onboarding.md
autonomous: true
requirements: [ONBD-01, ONBD-02]

must_haves:
  truths:
    - "onboarding.status returns needsOnboarding=true for a tenant with no business_profile doc, false once one exists (the first-run gate signal)"
    - "extractProfile runs the ungated business-profile skill over intake text and returns a Lean-core structured object incl. persona — it NEVER auto-commits (SC#1)"
    - "commitProfile stores the reviewed profile as a vaultDocuments row (kind business_profile) and startIngest embeds it — it becomes retrievable via searchVault/vaultGroundHydrated (SC#2, ONBD-02)"
    - "A profile committed by tenant A is never returned to tenant B (SC#3)"
    - "updateProfile re-embeds on edit so grounding stays current"
    - "No audit/telemetry/DLQ row written during onboarding carries profile prose or field values — refs/hashes/ids/counts/booleans ONLY (SC#4, §4)"
  artifacts:
    - path: "packages/backend/convex/onboarding.ts"
      provides: "status (tenantQuery), extractProfile (tenantAction), commitProfile (tenantMutation), updateProfile (tenantMutation)"
      min_lines: 60
      contains: "business_profile"
    - path: "packages/backend/convex/onboarding.test.ts"
      provides: "SC#2 embed+retrieve via SMOKE:: seam + SC#3 tenant isolation + extract-returns-persona"
    - path: "packages/backend/convex/profileRedaction.test.ts"
      provides: "SC#4 scan of audit/telemetry/deadLetters rows for profile prose"
  key_links:
    - from: "packages/backend/convex/onboarding.ts"
      to: "startIngest / vaultDocuments"
      via: "insert kind:business_profile then startIngest (persistBrief clone)"
      pattern: "startIngest"
    - from: "packages/backend/convex/onboarding.ts"
      to: "@pikar/core businessProfile"
      via: "serializeProfile for the doc text"
      pattern: "serializeProfile"
    - from: "packages/backend/convex/onboarding.ts"
      to: "internal.skills.getActiveSkill"
      via: "load BUSINESS_PROFILE_SKILL body for generateObject"
      pattern: "getActiveSkill"
    - from: "packages/backend/convex/onboarding.ts"
      to: "internal.audit.log"
      via: "refs/counts-only payload (searchVault template)"
      pattern: "queryHash|fieldCount|vaultDocId"
---

<objective>
Build the thin Convex adapter (§1) that turns intake text into a committed, embedded, tenant-scoped
business profile — reusing the persistBrief → startIngest spine verbatim. This plan delivers SC#2
(embed + retrieve), SC#3 (tenant isolation), SC#4 (redaction boundary), and the first-run gate signal
(status). It writes the two backend test files from 11-VALIDATION.

Purpose: the profile is "just another vault doc" — every hard part (embed, tenant scope, retrieval)
comes free from startIngest/searchVault; the new work is the extraction call and the §4-safe audit.
Output: onboarding.ts adapter + onboarding.test.ts + profileRedaction.test.ts.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-persona-onboarding-business-profile/11-CONTEXT.md
@.planning/phases/11-persona-onboarding-business-profile/11-RESEARCH.md
@.planning/phases/11-persona-onboarding-business-profile/11-VALIDATION.md
@CLAUDE.md
@packages/backend/convex/voice.ts
@packages/backend/convex/vaultGround.ts

<interfaces>
<!-- Exact seams read at source 2026-07-24 (11-RESEARCH.md). Use directly. -->

persistBrief TEMPLATE (voice.ts:299-324) — clone this for commitProfile:
```typescript
const vaultDocId = await ctx.db.insert("vaultDocuments", {
  tenantId: ctx.tenantId,
  title: profile.name || "Business profile",
  kind: "business_profile",                 // NEW free-string kind — ZERO schema migration
  category: categoryFor({ source: "agent" }),// → "workspace-docs"
  source: "agent",
  mimeType: "text/markdown",
  size: byteLen(text), contentHash: await contentHash(text),
  text,                                      // serializeProfile(profile) — content plane (§4)
  status: "processing", createdAt: Date.now(),
});
await startIngest(ctx, { vaultDocId, tenantId: ctx.tenantId, correlationId: crypto.randomUUID() });
```
startIngest(ctx,{vaultDocId,tenantId,correlationId}) — helper in vaultIngest.ts; runs ingestDoc → embedDoc → markReady.

extraction (llm.ts::draftCockpit pattern):
```typescript
const skill = await ctx.runQuery(internal.skills.getActiveSkill, { name: BUSINESS_PROFILE_SKILL });
const { object } = await generateObject({ model: DEFAULT_MODEL.model, system: skill.body, schema: profileSchema, prompt: intakeText });
// return object to client — NEVER into an audit/telemetry/DLQ payload (§4)
```

status query (RESEARCH Pattern 3):
```typescript
const docs = await ctx.db.query("vaultDocuments").withIndex("by_tenant", q => q.eq("tenantId", ctx.tenantId)).collect();
return { needsOnboarding: !docs.some(d => d.kind === "business_profile" && d.status !== "failed") };
```

Retrieval seam for tests (vaultGround.ts:43-55): internal.vaultGround.vaultGroundHydrated({tenantId,query})
→ {docIds,titles,chunks}; query "SMOKE::<docId>" bypasses the embedding network (offline, deterministic);
cross-tenant docs drop out via ownedDocsMeta (proves SC#3 with no network).

§4 audit template (llm.ts:1336-1342 searchVault tool):
  payload: { queryHash: await contentHash(query), resultCount } — refs + counts ONLY.

Tenant wrappers (lib/functions.ts): tenantQuery / tenantMutation / tenantAction (§2 — never raw generated fns).
PITFALL (RESEARCH §Pitfall 3): scheduler/action-invoked ingest must use INTERNAL vault.ingestExtractedText
(explicit tenantId) — the public tenant* wrappers throw UNAUTHENTICATED in a context with no identity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: status query + extractProfile action (persona inference, never auto-commit)</name>
  <files>packages/backend/convex/onboarding.ts, packages/backend/convex/onboarding.test.ts</files>
  <behavior>
    - status returns { needsOnboarding: true } for a fresh tenant; false after a business_profile doc exists (status !== "failed").
    - extractProfile over a fixed intake string returns a Lean-core object whose persona ∈ {solopreneur,startup,sme}; assert it does NOT write a vaultDocuments row (no auto-commit — SC#1). Use an offline/mock model seam so the test carries no network.
  </behavior>
  <action>
    Create packages/backend/convex/onboarding.ts. Add `status` as a tenantQuery implementing the existence
    check above (refs/booleans only — §4). Add `extractProfile` as a tenantAction that loads
    BUSINESS_PROFILE_SKILL via internal.skills.getActiveSkill and calls generateObject over the intake text
    (draftCockpit pattern), returning the structured object (incl. persona) to the caller ONLY — it must not
    insert any doc and must not put the object into any audit payload (SC#1 + §4). Reuse the profile schema
    from @pikar/core if Task-1-of-plan-01 exported one, else build the generateObject schema from the
    BusinessProfile type. Write the failing test first against the SMOKE/mock-model seam used elsewhere in
    the backend suite, then implement.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- onboarding</automated>
  </verify>
  <done>onboarding.test.ts covers status + extract-returns-persona + no-auto-commit, green; §2 wrappers used.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commitProfile + updateProfile (persistBrief clone, embed + re-embed) — SC#2/#3</name>
  <files>packages/backend/convex/onboarding.ts, packages/backend/convex/onboarding.test.ts</files>
  <behavior>
    - commitProfile(profile) inserts a vaultDocuments row kind:"business_profile", calls startIngest; after driving ingestDoc to ready, vaultGroundHydrated({tenantId, query:"SMOKE::<docId>"}) returns the doc (SC#2).
    - Tenant B's vaultGroundHydrated over tenant A's SMOKE::<A-docId> returns empty — isolation (SC#3).
    - updateProfile re-embeds: after edit, the doc's rag entry reflects the new serialized text (old entry replaced), and searchVault/vaultGroundHydrated returns the updated content.
  </behavior>
  <action>
    Add `commitProfile` (tenantMutation): serializeProfile(profile) from @pikar/core → insert the
    vaultDocuments row (persistBrief clone above, kind "business_profile", source "agent") → startIngest.
    Emit ONE audit event (internal.audit.log, insert-only §3) with payload {vaultDocId, fieldCount,
    personaConfirmed} — refs/counts/booleans ONLY, NEVER the profile fields (§4, SC#4). Add `updateProfile`
    (tenantMutation): re-embed on edit — either patch vaultDocuments.text + re-startIngest, or
    deleteVaultDoc (cascades rag+graph) then re-commit; pick the smaller correct path (ponytail: patch+
    re-ingest if ingestDoc is idempotent on contentHash change, else delete+re-ingest). Extend
    onboarding.test.ts with the SC#2 embed+retrieve (SMOKE seam), SC#3 cross-tenant isolation, and the
    re-embed assertions. RED then GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- onboarding</automated>
  </verify>
  <done>commit→ingest→retrieve green via SMOKE seam (SC#2); cross-tenant query empty (SC#3); updateProfile re-embeds; audit payload is refs/counts only.</done>
</task>

<task type="auto">
  <name>Task 3: §4 redaction scan test (SC#4) + playbook bump</name>
  <files>packages/backend/convex/profileRedaction.test.ts, docs/playbooks/onboarding.md</files>
  <action>
    Create packages/backend/convex/profileRedaction.test.ts mirroring the existing llmRedaction.test.ts /
    auditImmutability.test.ts pattern. Run a full onboarding commit with a fixture profile carrying
    recognizable sentinel strings in every field (e.g. name "ACME_SENTINEL", constraint "CONSTRAINT_SENTINEL").
    Then read EVERY audit, telemetry, and deadLetters row written during the flow and assert NONE of the
    sentinel strings (nor the serialized profile prose) appear in any payload — payloads contain only
    refs/hashes/ids/counts/booleans. Because onboarding.ts (a watched path) was created this phase, bump
    docs/playbooks/onboarding.md `Last verified` to today and note the commitProfile/audit §4 contract in
    its verify section (Stop hook §9 requires the playbook touched in the same turn as watched-path changes).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- profileRedaction</automated>
  </verify>
  <done>profileRedaction test green — no sentinel/profile prose in any audit/telemetry/DLQ row (SC#4); onboarding.md Last verified bumped; check-playbooks passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- onboarding` green (SC#1 no-auto-commit, SC#2 embed+retrieve, SC#3 isolation).
- `pnpm --filter @pikar/backend test -- profileRedaction` green (SC#4).
- onboarding.ts uses tenantQuery/tenantMutation/tenantAction (§2) and internal.audit.log insert-only (§3).
- `node scripts/check-playbooks.mjs` passes.
</verification>

<success_criteria>
- A committed profile is embedded and retrievable via the SMOKE seam; cross-tenant retrieval is empty.
- Extraction never auto-commits; persona is returned for confirmation only.
- Every onboarding audit/telemetry/DLQ payload is refs/counts/booleans only.
</success_criteria>

<output>
After completion, create `.planning/phases/11-persona-onboarding-business-profile/11-02-SUMMARY.md`
</output>
