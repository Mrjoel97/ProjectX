---
phase: 11-persona-onboarding-business-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/playbooks/onboarding.md
  - docs/playbooks/watch.json
  - docs/playbooks/skill-registry.md
  - packages/core/src/businessProfile.ts
  - packages/core/src/businessProfile.test.ts
  - packages/contracts/src/skill.ts
  - packages/contracts/src/skills/businessProfile.ts
  - packages/backend/convex/skills.ts
autonomous: true
requirements: [ONBD-01, ONBD-02]

must_haves:
  truths:
    - "The Lean-core profile shape (name, one-line description, persona, stage, offering, target customer, primary goals, known constraints) exists as a pure, Convex-free module"
    - "Persona is one of solopreneur | startup | sme — enterprise is not an emittable value"
    - "Every persona inference returns a needs-confirm state — no code path auto-commits a persona (SC#1)"
    - "A profile serializes to deterministic markdown for storage as a vault doc"
    - "An UNGATED business-profile extraction skill is seeded active v1 and loadable at runtime (§5)"
    - "The onboarding subsystem has a registered playbook so downstream code paths are protected (§9)"
  artifacts:
    - path: "packages/core/src/businessProfile.ts"
      provides: "Pure profile type/schema, persona enum, serializeProfile, always-confirm decision fn, field validators"
      min_lines: 40
    - path: "packages/core/src/businessProfile.test.ts"
      provides: "SC#1 unit test — always-confirm + enterprise-not-emittable + serializer roundtrip"
    - path: "packages/contracts/src/skills/businessProfile.ts"
      provides: "Extraction prompt body (markdown seed) exported as businessProfileSkillBody"
    - path: "packages/contracts/src/skill.ts"
      provides: "BUSINESS_PROFILE_SKILL const — NOT in GATED_SKILLS"
      contains: "BUSINESS_PROFILE_SKILL"
    - path: "packages/backend/convex/skills.ts"
      provides: "business-profile row in seedSkills[]"
      contains: "BUSINESS_PROFILE_SKILL"
    - path: "docs/playbooks/onboarding.md"
      provides: "Onboarding subsystem playbook + watched-path registration"
  key_links:
    - from: "packages/backend/convex/skills.ts"
      to: "packages/contracts/src/skills/businessProfile.ts"
      via: "import businessProfileSkillBody"
      pattern: "businessProfileSkillBody"
    - from: "docs/playbooks/watch.json"
      to: "onboarding.md"
      via: "watched path prefixes for new onboarding code"
      pattern: "onboarding"
---

<objective>
Land the Phase 11 foundation: the pure business-profile domain module (§1, Convex-free),
the new UNGATED extraction skill (§5), and the onboarding subsystem playbook (§9). This is
the substrate every later plan imports — it must exist first so Waves 2-4 have contracts,
a seeded skill, and playbook coverage to build against without racing.

Purpose: encode SC#1 (persona is always confirmed, never assumed) as a pure, unit-tested
decision function, and give the extraction prompt a versioned registry home.
Output: `@pikar/core` profile module + test, `business-profile` seeded skill, onboarding playbook.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-persona-onboarding-business-profile/11-CONTEXT.md
@.planning/phases/11-persona-onboarding-business-profile/11-RESEARCH.md
@.planning/phases/11-persona-onboarding-business-profile/11-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the tree 2026-07-24. Use directly — no codebase exploration needed. -->

Persona locked set (CONTEXT §Persona detection): solopreneur | startup | sme. Enterprise is OUT.

Lean-core fields (CONTEXT §Business profile structure — exact names Claude's discretion,
anchor to what the Phase 12 eval engine reads):
  name, one-line description, persona, stage, offering, target customer, primary goals, known constraints.

Skill registration pattern (packages/backend/convex/skills.ts:235-250):
```typescript
import { voiceBriefSkillBody } from "@pikar/contracts/skills/voiceBrief";
export const seedSkills = internalMutation({ ... rows: [
  { name: VOICE_BRIEF_SKILL, body: voiceBriefSkillBody },   // ungated — first boot seeds v1/active
] });
```
Skill const pattern (packages/contracts/src/skill.ts:61-62):
```typescript
export const VOICE_BRIEF_SKILL = "voice-brief" as const;   // NOT added to GATED_SKILLS (line 76)
```
voice-brief is the exact ungated precedent (skills.ts:247-250 rationale) — mirror it.

Playbook infra: create from docs/playbooks/TEMPLATE.md; register watched prefixes in
docs/playbooks/watch.json. `packages/contracts/src/skills/` and `packages/backend/convex/skills.ts`
+ `packages/contracts/src/skill.ts` are ALREADY watched by skill-registry.md → editing them
requires bumping skill-registry.md `Last verified` in this same turn (Stop hook §9).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding playbook and register watched paths</name>
  <files>docs/playbooks/onboarding.md, docs/playbooks/watch.json, docs/playbooks/skill-registry.md</files>
  <action>
    Create docs/playbooks/onboarding.md from docs/playbooks/TEMPLATE.md. Document the onboarding
    subsystem invariants (do NOT restate CLAUDE.md — reference it): profile-as-vault-doc (persistBrief
    clone, kind:"business_profile", zero migration); persona ALWAYS confirmed (SC#1); §4 redaction
    boundary (refs/counts only in audit/telemetry/DLQ — SC#4); extraction skill is UNGATED and separate
    from the gated cockpit-agent; first-run gate lives in the client AppShell, never middleware.ts.
    Include "How to change safely", "How to verify" (the pnpm test commands from 11-VALIDATION), and set
    the `Last verified` line to today.

    In docs/playbooks/watch.json add an "onboarding.md" entry registering these NEW path prefixes (these
    are the paths later plans create; registering now prevents the Stop hook blocking Waves 2-4):
      - "packages/core/src/businessProfile.ts"
      - "packages/backend/convex/onboarding.ts"
      - "apps/web/app/(app)/dashboard/onboarding/"
      - "apps/web/app/(app)/dashboard/profile/"
    Do NOT add layout.tsx (it is uncovered + shared with cockpit/vault bleed logic — watching it would
    force every future layout edit to touch this playbook).

    Because this plan also edits skills.ts / skill.ts / adds a contracts/src/skills/ file (all watched by
    skill-registry.md), bump docs/playbooks/skill-registry.md `Last verified` line to today so the Stop
    hook passes this turn.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>onboarding.md exists and is registered in watch.json with the four new prefixes; skill-registry.md Last verified bumped; check-playbooks passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure business-profile domain module (§1) — always-confirm + serializer</name>
  <files>packages/core/src/businessProfile.ts, packages/core/src/businessProfile.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - decideConfirm(inference) ALWAYS returns { persona, needsConfirm: true } — assert no input yields an auto-committed persona (SC#1).
    - The persona type/enum admits exactly solopreneur | startup | sme; assert "enterprise" is not a member / is rejected by the validator.
    - serializeProfile(profile) produces stable deterministic markdown containing every Lean-core field; assert a fixed input maps to a fixed string (snapshot-free equality).
    - validateProfile rejects a missing required field (e.g. empty name) and accepts a complete Lean-core object.
  </behavior>
  <action>
    Create packages/core/src/businessProfile.ts as a PURE module (no Convex, no network — §1 thin-adapter
    rule). Export: a Persona union/const (solopreneur | startup | sme); a BusinessProfile type covering the
    Lean-core fields (name, oneLineDescription, persona, stage, offering, targetCustomer, primaryGoals,
    knownConstraints — pick concrete field names/types anchored to what Phase 12 eval frameworks read);
    serializeProfile(profile): string → deterministic markdown for the vault doc text; decideConfirm(): the
    SC#1 always-confirm decision fn; and lightweight field validators. Prefer plain TS + a small validator
    over adding Zod unless a Zod schema is genuinely reused downstream (ponytail: rung 5 — only if the
    backend extraction action needs the same schema object; if so export it, else keep plain). Re-export
    from packages/core/src/index.ts. Write businessProfile.test.ts first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- businessProfile</automated>
  </verify>
  <done>businessProfile.test.ts green; module exports Persona, BusinessProfile, serializeProfile, decideConfirm, validators; no path auto-commits a persona; enterprise not emittable.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the UNGATED business-profile extraction skill (§5)</name>
  <files>packages/contracts/src/skill.ts, packages/contracts/src/skills/businessProfile.ts, packages/backend/convex/skills.ts</files>
  <action>
    Mirror the voice-brief ungated precedent exactly. In packages/contracts/src/skill.ts add
    `export const BUSINESS_PROFILE_SKILL = "business-profile" as const;` and do NOT add it to GATED_SKILLS.
    Create packages/contracts/src/skills/businessProfile.ts exporting `businessProfileSkillBody` (a markdown
    system prompt, §5 — no hardcoded prompt in source): instruct the model to read the user's business
    intake (paste / extracted file text / spoken brief) and emit the Lean-core structured profile INCLUDING
    an inferred persona (solopreneur | startup | sme only — never enterprise), returning fields it cannot
    determine as empty rather than inventing them. In packages/backend/convex/skills.ts import
    businessProfileSkillBody and add `{ name: BUSINESS_PROFILE_SKILL, body: businessProfileSkillBody }` to the
    seedSkills[] rows. Being ungated, first boot seeds v1/active cleanly via the rows.length===0 bootstrap
    (per skill-version-collision-gotcha: do NOT pin a version — let seedSkills own it).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec tsc --noEmit</automated>
  </verify>
  <done>BUSINESS_PROFILE_SKILL exported and absent from GATED_SKILLS; businessProfileSkillBody authored; seedSkills[] includes the row; backend typechecks.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- businessProfile` green (SC#1).
- `pnpm --filter @pikar/backend exec tsc --noEmit` clean (skill wiring compiles).
- `node scripts/check-playbooks.mjs` passes (playbook + watch.json + skill-registry bump).
- BUSINESS_PROFILE_SKILL is NOT in GATED_SKILLS (grep).
</verification>

<success_criteria>
- Pure profile module encodes SC#1 (always-confirm) with a passing unit test.
- Extraction skill seeded ungated; the gated cockpit-agent body is untouched.
- Onboarding playbook created and watching the four new path prefixes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-persona-onboarding-business-profile/11-01-SUMMARY.md`
</output>
