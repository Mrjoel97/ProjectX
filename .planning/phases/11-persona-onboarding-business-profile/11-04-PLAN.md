---
phase: 11-persona-onboarding-business-profile
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - apps/web/app/(app)/dashboard/profile/page.tsx
  - docs/playbooks/onboarding.md
autonomous: false
requirements: [ONBD-02]

must_haves:
  truths:
    - "The user has a dedicated profile page showing the committed Lean-core fields"
    - "Each field is editable and saving persists the change"
    - "Saving re-embeds the profile doc so grounding stays current (old rag entry replaced) — searchVault returns the updated text"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/profile/page.tsx"
      provides: "Editable business-profile page; save → api.onboarding.updateProfile (re-embed)"
      min_lines: 40
      contains: "updateProfile"
  key_links:
    - from: "apps/web/app/(app)/dashboard/profile/page.tsx"
      to: "api.onboarding.updateProfile"
      via: "useMutation on save → re-embed"
      pattern: "updateProfile"
---

<objective>
Ship the dedicated editable profile page. Saving re-embeds the profile doc (Plan 02's updateProfile)
so downstream grounding (Phase 12 eval, Phase 14 flagship) always reads the current business profile.
Separate wave from Plan 03 because both must bump the shared onboarding.md playbook (wave-serialization
avoids the file race).

Purpose: later editability is a locked decision — the profile must stay correctable without re-onboarding.
Output: dashboard/profile/page.tsx.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-persona-onboarding-business-profile/11-CONTEXT.md
@.planning/phases/11-persona-onboarding-business-profile/11-RESEARCH.md
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/vault/page.tsx

<interfaces>
<!-- Backend from Plan 02; UI patterns from the vault dashboard. -->
api.onboarding.status → { needsOnboarding, profileDocId? } — load the current profile for display/edit.
api.onboarding.updateProfile({ profile }) → re-embeds (delete/replace the old rag entry, re-startIngest).
Reuse the vault dashboard card/form patterns (apps/web/.../dashboard/vault/) — no new component library.
§10: read docs/design/BRAND.md; use globals.css tokens; match dashboard patterns.
The review-gate card built in Plan 03 (dashboard/onboarding/page.tsx) is the editable-card precedent —
reuse its field-form shape here rather than inventing a second layout (ponytail rung 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editable profile page with re-embed on save</name>
  <files>apps/web/app/(app)/dashboard/profile/page.tsx, docs/playbooks/onboarding.md</files>
  <action>
    Create dashboard/profile/page.tsx. Load the current profile (via onboarding.status / a getter for the
    profileDocId's fields) and render the Lean-core fields as an editable form reusing the Plan-03 review
    card shape and vault dashboard patterns. On save, call api.onboarding.updateProfile({ profile }) which
    re-embeds. Show saving/success/error states; persona stays one of solopreneur|startup|sme (no
    enterprise). Follow BRAND.md + globals.css tokens (§10), no new component library. Because
    dashboard/profile/ is a watched path, bump docs/playbooks/onboarding.md `Last verified` and note the
    edit/re-embed flow in the same turn (§9).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit</automated>
  </verify>
  <done>Profile page renders editable Lean-core fields; save calls updateProfile; typechecks; onboarding.md bumped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify profile edit re-embeds grounding</name>
  <action>Human verification only — Task 1 automated the editable profile page; this task confirms save re-embeds. Follow how-to-verify.</action>
  <what-built>Dedicated editable profile page; saving a field change re-embeds the profile doc so grounding stays current.</what-built>
  <how-to-verify>
    1. Open /dashboard/profile → confirm the committed Lean-core fields are shown and editable.
    2. Edit a field (e.g. offering) and save → confirm success state.
    3. Confirm the change grounds: the cockpit's searchVault / vault browse returns the UPDATED text (old entry replaced, not duplicated).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web exec tsc --noEmit` clean.
- Save path calls api.onboarding.updateProfile (re-embed), not a bespoke rag write.
- `node scripts/check-playbooks.mjs` passes.
</verification>

<success_criteria>
- The profile is editable post-onboarding and saving re-embeds so grounding reflects the edit.
</success_criteria>

<output>
After completion, create `.planning/phases/11-persona-onboarding-business-profile/11-04-SUMMARY.md`
</output>
