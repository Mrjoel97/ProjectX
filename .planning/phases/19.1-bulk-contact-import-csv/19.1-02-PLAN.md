---
phase: 19.1-bulk-contact-import-csv
plan: 02
type: execute
wave: 2
depends_on: ["19.1-01"]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/contacts.ts
  - packages/backend/convex/contacts.test.ts
  - packages/core/src/contacts.test.ts
  - apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx
  - apps/web/app/(app)/dashboard/pipeline/pipelineView.test.ts
  - docs/playbooks/contacts-crm.md
autonomous: true
requirements: [ACTN-05]

must_haves:
  truths:
    - "A contact row can record that it came from a file (`origin: \"imported\"`) and that its consent came from a batch attestation (`consentSource: \"imported-attested\"`)"
    - "A contact row can carry a company, a phone and a title"
    - "The Pipeline shows an imported contact as imported — origin label AND consent chip"
    - "The hand-add form still cannot claim imported provenance"
    - "The agent still cannot stage an imported-origin contact"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "the widened unions and the three content-plane columns"
      contains: "imported-attested"
    - path: "packages/backend/convex/contacts.ts"
      provides: "ContactOrigin + one shared ConsentSource used by BOTH consent types"
      contains: "export type ConsentSource"
    - path: "apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx"
      provides: "an ORIGIN_LABELS entry for imported, bound by `satisfies`, and a consent chip that reads `source`"
      contains: "satisfies Record<"
  key_links:
    - from: "apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx"
      to: "row.consent.source"
      via: "the Consent cell"
      pattern: "consent\\.source"
    - from: "packages/backend/convex/contacts.ts"
      to: "packages/backend/convex/schema.ts"
      via: "hand-typed mirrors that tsc binds to the schema at the listContacts assignment"
      pattern: "ContactOrigin|ConsentSource"
---

<objective>
Widen `origin` with `"imported"` and `consentSource` with `"imported-attested"`, add `company` /
`phone` / `title`, and register every consuming site — **in ONE commit**. A partially-registered
union is worse than none: 16 of the 19 sites are compile errors, and the three that are not are
exactly where a whole phase has shipped invisible before.

Purpose: this is the riskiest part of the build (the design spec says so, and 19-06's `media`
failure is the precedent). It lands alone, in one commit, with a red-able test on each silent site.
Output: a schema that can represent an imported, batch-attested contact, and a Pipeline that shows
the difference.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-bulk-contact-import-csv/19.1-CONTEXT.md
@.planning/phases/19.1-bulk-contact-import-csv/19.1-RESEARCH.md
@packages/backend/convex/contacts.ts
@apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx
@docs/playbooks/contacts-crm.md

<interfaces>
<!-- The 19-site walk, already done in research. This is the executor's checklist. -->

**Widen (2 sites — the source):** `schema.ts:1492` `contacts.origin`, `schema.ts:1496`
`contacts.consentSource`. Widening a union and adding optional fields are BOTH no-migration changes
(`convex-migration-helper` skill, "Safe Changes"). NARROWING later would need one — say so in a
comment. No `npx convex codegen` needed: a schema-only change derives generically (19-01 finding).

**Widen (3 sites — tsc will force you, both directions):**
```ts
// contacts.ts:163
export type ContactOrigin = "mailbox-resolved" | "user-entered" | "inbound";  // + "imported"
// contacts.ts:284  ConsentRecord.source   — inline literal pair
// contacts.ts:764  PipelineContactRow.consent.source — a SECOND independent copy of the same pair
```
Extract ONE `export type ConsentSource` and use it at both, so the pair exists once.

**Do NOT widen (3 sites — deliberate, and each needs a test because tsc is silent):**
| Site | Why not |
|---|---|
| `contacts.ts:240` `upsertContact` args `v.union(...)` | The hand-add form must not be able to claim imported provenance. Hand-maintained validator, nothing binds it to the schema — the `patchPlan` Pitfall-1 class. |
| `core/src/contacts.ts:107` `CrmContactOrigin` | Human-only import is a LOCKED decision; the agent may not stage one. |
| `core/src/contacts.ts:126` `const ORIGINS: readonly string[]` | Typed `readonly string[]`, structurally decoupled from `CrmContactOrigin` — the exact `PlanRow`-never-declares-`kind` shape. |

**The third silent site is the UI** (`PipelineView.tsx:279-283`): the Consent cell renders
`Consented {formatDay(row.consent.at)}` and never reads `row.consent.source`. Owner decision
2026-08-10: **render it.**

**Unaffected (no edit):** `llm.ts:2584` and `contacts.ts:200` hardcode `"mailbox-resolved"`;
`contacts.ts:272` `assertConsent` hardcodes `"asserted-by-user"` — correctly, leave it. ~30 narrow
literal fixtures across `contacts.test.ts`, `cockpit.test.ts`, `cockpitTools.test.ts`,
`plans.test.ts`, `crmCard.test.ts`, `core/contacts.test.ts` all remain valid under a wider union.

**Do NOT copy** the `?? "asserted-by-user"` fallback (`contacts.ts:322`, `:837`) into new code — the
import always writes both fields, so a defaulted source there would be an invented fact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: widen the schema and the type mirrors</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/contacts.ts</files>
  <action>
FIRST measure the typecheck baseline so deltas are comparable, not absolutes (`cash.ts` may carry 2
pre-existing `TS2739` from the finance lane):
`cd packages/backend && npx tsc --noEmit 2>&1 | tail -5` — record the number in the summary.

`schema.ts`, the `contacts` table:
- `origin`: add `v.literal("imported")`. Comment: `imported` = a row that came out of a CSV the user
  uploaded (19.1); widening a union is backward-compatible for rows at rest, NARROWING would need a
  migration.
- `consentSource`: add `v.literal("imported-attested")`. Comment: deliberately DISTINCT from
  `asserted-by-user` — one attestation over 500 rows is weaker evidence than consent recorded for
  one person, and the schema must not flatten that difference.
- Add `company`, `phone`, `title`, all `v.optional(v.string())`, with the CLAUDE.md §4
  content-plane comment the way `consentWording` already carries one. THREE, and no more — no
  custom fields, no tags, no key-value.

`contacts.ts`:
- `ContactOrigin` gains `"imported"`.
- Add `export type ConsentSource = "asserted-by-user" | "inbound-form" | "imported-attested";` with
  a comment that it mirrors the schema union and exists ONCE so the two consumers cannot drift.
  Use it at `ConsentRecord.source` and at `PipelineContactRow.consent.source`, replacing both inline
  literal pairs.
- Leave `upsertContact`'s arg validator, `assertConsent`'s hardcoded source, and the two hardcoded
  `"mailbox-resolved"` sites EXACTLY as they are.
- Leave `packages/core/src/contacts.ts` untouched entirely.

Do NOT commit yet — this plan commits once, in task 3.
  </action>
  <verify>
    <automated>cd packages/backend && npx tsc --noEmit 2>&1 | tail -3 && npx vitest run convex/contacts.test.ts</automated>
  </verify>
  <done>Typecheck error count equals the recorded baseline (0 new); `contacts.test.ts` still green; `grep -c "imported" packages/backend/convex/schema.ts` finds both new literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the three silent sites and their red-able tests</name>
  <files>packages/backend/convex/contacts.test.ts, packages/core/src/contacts.test.ts, apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx, apps/web/app/(app)/dashboard/pipeline/pipelineView.test.ts</files>
  <behavior>
    - `upsertContact({ email, origin: "imported" })` is REJECTED by the arg validator (the hand-add form cannot claim imported provenance)
    - `parseCrmOperations([{ op: "addContact", email: "a@b.co", origin: "imported" }])` throws `CRM_CONTACT_ORIGIN_INVALID` (the agent cannot either)
    - `ORIGIN_LABELS` has an entry for every origin the backend can return, enforced at the DECLARATION
    - A row with `origin: "imported"` renders "Imported from a file", not a blank and not "Unknown"
    - A row with `consent.source === "imported-attested"` renders a chip that is textually DIFFERENT from an `asserted-by-user` chip
    - A row with `consent: null` still reads "none on record"
  </behavior>
  <action>
**Silent site 1 — `packages/backend/convex/contacts.test.ts`.** In the existing describe block that
covers `upsertContact`, add: `await expect(h.asA.mutation(api.contacts.upsertContact, { email: "a@b.co", origin: "imported" as never })).rejects.toThrow();` with a comment naming WHY the validator
is deliberately narrower than the schema (the hand-add form must not claim imported provenance) and
that nothing but this test binds it.

**Silent site 2 — `packages/core/src/contacts.test.ts`.** Add a case to the `parseCrmOperations`
refusal block asserting `origin: "imported"` throws `CRM_CONTACT_ORIGIN_INVALID`, with a comment
that `ORIGINS` is typed `readonly string[]` and is therefore structurally decoupled from
`CrmContactOrigin` — this test is the only thing holding them together.

**Silent site 3 — `PipelineView.tsx`.**
- Add `imported: "Imported from a file"` to `ORIGIN_LABELS`, and add the compile-time bind that does
  not exist today: derive the origin type from the read model
  (`type ContactRow = FunctionReturnType<typeof api.contacts.listContacts>["contacts"][number];`
  then `as const satisfies Record<ContactRow["origin"], string>`), so a future origin fails at the
  DECLARATION rather than inside a JSX index expression.
- Change the Consent cell to render the source:
  `Consented {formatDay(row.consent.at)}{row.consent.source === "imported-attested" ? " · imported" : ""}`.
  One expression. Keep `chip` / `quietChip` — BRAND §6, teal is a FILL, no amber. Add a comment
  naming the owner decision (2026-08-10) and the reason: the schema preserves consent STRENGTH and
  the only surface anyone looks at was flattening it, invisibly to `tsc`.

**`pipelineView.test.ts`.** Add cases for every behaviour above. The consent case must assert BOTH
chips and that they differ — an assertion that only reads the imported one would pass if the code
appended " · imported" to every chip.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test && pnpm --filter @pikar/core test contacts && cd packages/backend && npx vitest run convex/contacts.test.ts</automated>
  </verify>
  <done>All three silent sites carry a test; the web, core and backend suites are green; `grep -n "consent.source" apps/web/app/\(app\)/dashboard/pipeline/PipelineView.tsx` finds the render.</done>
</task>

<task type="auto">
  <name>Task 3: mutation-proof the three guards, bump the playbook, ONE commit</name>
  <files>docs/playbooks/contacts-crm.md</files>
  <action>
**Mutation-proof each of the three silent-site guards** — break it, observe red, record the exact
failing text in the summary, revert:
1. Add `v.literal("imported")` to `upsertContact`'s arg validator → the backend test goes red.
2. Add `"imported"` to `ORIGINS` in `core/src/contacts.ts` → the core test goes red.
3. Drop the ` · imported` suffix from the consent chip → the web test goes red.
A guard never observed failing is not proven (19-06 / 19-11 both depended on this).

Bump `docs/playbooks/contacts-crm.md` — a new `> Last verified:` line at the top (SINGLE-LINE
anchor; the file is CRLF) recording: the two widened unions and the three columns; the three sites
deliberately NOT widened and why; that the Pipeline consent chip now renders `source` (owner
decision 2026-08-10) because the schema's distinction was invisible at the only surface anyone
looks at; and that widening is no-migration while narrowing is not.

Then **ONE pathspec commit for the entire union walk** — checking `.git/MERGE_HEAD` first, never
`git add -A`:

```
git commit -m "feat(19.1-02): origin +imported and consentSource +imported-attested, every registration site in one commit" -- packages/backend/convex/schema.ts packages/backend/convex/contacts.ts packages/backend/convex/contacts.test.ts packages/core/src/contacts.test.ts "apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx" "apps/web/app/(app)/dashboard/pipeline/pipelineView.test.ts" docs/playbooks/contacts-crm.md
```
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Three mutation-proofs run and reverted with their failing text recorded; the playbook carries a new Last verified line; `git show --stat HEAD` shows all seven files in ONE commit; typecheck delta vs the recorded baseline is 0.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` — delta 0 against the baseline recorded in task 1
- `pnpm test` — core, backend and web suites green
- `git show --stat HEAD` — the widening and every registration site in a single commit
- `node scripts/check-playbooks.mjs` exits 0
</verification>

<success_criteria>
The schema can represent an imported, batch-attested contact with a company, phone and title. All 19
consuming sites are accounted for: 5 widened, 3 deliberately not widened and each pinned by a
red-able test, the UI registration landed, the rest verified unaffected. One commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-bulk-contact-import-csv/19.1-02-SUMMARY.md`
</output>
