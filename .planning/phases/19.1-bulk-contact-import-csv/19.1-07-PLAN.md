---
phase: 19.1-bulk-contact-import-csv
plan: 07
type: execute
wave: 7
depends_on: ["19.1-06"]
files_modified:
  - apps/web/e2e/pipeline-uat.spec.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/contacts-crm.md
  - .planning/phases/19.1-bulk-contact-import-csv/19.1-VALIDATION.md
autonomous: false
requirements: [ACTN-05]

must_haves:
  truths:
    - "A REAL browser uploads a real CSV, sees the four counts, confirms, and the rows appear in the table"
    - "Confirm is disabled until the attestation is ticked, observed in the browser"
    - "A hand-typed name survives the import, asserted byte-for-byte against stored state"
    - "A file over 1,000 rows is refused inline with zero browser dialogs"
    - "The import panel does not break the 390px phone width or the tiles' geometry"
  artifacts:
    - path: "apps/web/e2e/pipeline-uat.spec.ts"
      provides: "the import UAT step, placed after step 3, plus the phone-width extension"
      contains: "setInputFiles"
    - path: "docs/playbooks/cockpit.md"
      provides: "the WATCH-GATE bump required because cockpit.md watches apps/web/e2e/"
    - path: ".planning/phases/19.1-bulk-contact-import-csv/19.1-VALIDATION.md"
      provides: "the Per-Task Verification Map, filled with observed results"
  key_links:
    - from: "apps/web/e2e/pipeline-uat.spec.ts"
      to: "the import panel's file input"
      via: "setInputFiles with an in-memory buffer"
      pattern: "setInputFiles"
    - from: "apps/web/e2e/pipeline-uat.spec.ts"
      to: "api.contacts.listContacts"
      via: "fetchQuery read-back — the preview is under test, so it cannot also be the oracle"
      pattern: "fetchQuery"
---

<objective>
Prove the whole thing in a real browser, and close the two playbook gates.

Purpose: **this row is a requirement, not a nicety.** Phase 19 shipped a capability that unit tests,
SMOKE tests and a paid eval gate all certified while it was completely unreachable from the product,
because each of those supplies context a browser never provides. An import feature has the identical
shape: a `tenantMutation` passing every backend test is still unreachable if the button is not wired.
Output: one new UAT step, an extended phone-width step, two playbook bumps, and a filled validation
map.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-bulk-contact-import-csv/19.1-CONTEXT.md
@.planning/phases/19.1-bulk-contact-import-csv/19.1-RESEARCH.md
@.planning/phases/19.1-bulk-contact-import-csv/19.1-VALIDATION.md
@apps/web/e2e/pipeline-uat.spec.ts

<interfaces>
**HOW TO RUN — verbatim from the spec's own header:**
```
# from apps/web, against an ALREADY-RUNNING stack:
#   convex dev on :3210 (NOT --once) + a PRODUCTION build on :3111 (next dev OOMs on /workspace)
npx playwright test e2e/pipeline-uat.spec.ts --no-deps --workers=1
```
`pnpm --filter @pikar/web test:e2e -- <file>` **DOES NOT FILTER** — pnpm forwards the literal `--`,
Playwright matches nothing, and all 25 specs run for ~8 minutes. 19-10 hit this.

**Harness contract (do not bypass):** `test.describe.configure({ mode: "serial" })`; `afterEach`
re-saves `storageState` because Convex Auth ROTATES its refresh token — use the `page` fixture, never
a hand-made context. Step 3 has already created `JANE` ("Jane Doe") and a NAMELESS contact, so a
step placed after it can prove all four counts at once. Existing idioms in the file: `shot(page, "step-XX-name")`, `tileValue(page, id)`, `fetchQuery`/`fetchMutation` from `convex/nextjs` with
`auth` (`:336, :556, :571`), and the dialog counter (`:370-374`) that proves a refusal is inline.

**Playwright's upload form** (`vault.spec.ts:139`, `intake.spec.ts:48`):
```ts
await page.getByTestId("import-file-input").setInputFiles({
  name: "contacts.csv", mimeType: "text/csv", buffer: Buffer.from(FIXTURE_CSV),
});
```

**The fixture is an inline TS constant, NOT a committed .csv file.** `core.autocrlf=true` +
`* text=auto` would normalize a committed file's CRLF and could strip or mangle the BOM — which are
two of the five hazards the fixture exists to prove. Build it with explicit escapes (`"\uFEFF"`,
`"\r\n"`) so it is byte-stable in every checkout. This satisfies the VALIDATION Wave-0 fixture item.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the import UAT step, after step 3</name>
  <files>apps/web/e2e/pipeline-uat.spec.ts</files>
  <action>
Add `test("step 3b: a real CSV imports — preview counts, the attestation gate, and nothing typed by hand is overwritten", ...)` immediately AFTER step 3 (serial mode: ordering is the contract).
`test.setTimeout(180_000)` — the batch loop plus a reactive re-render is slower than the 30s default.

Build `FIXTURE_CSV` inline, with a BOM, CRLF line endings, and these rows:
- a NEW address with a quoted comma company (`"Acme, Inc."`) → `created`
- `JANE` again with a `company` the hand-add never set, and a name spelled DIFFERENTLY → must count
  as `enriched` AND must not overwrite "Jane Doe"
- a row with an embedded newline inside a quoted field and a doubled quote in the name
  (`"O""Neil, Ann"`) → `created`, and proves the physical-line accounting
- the NAMELESS contact's address with every mappable field blank → `unchanged`
- `not-an-address` → `rejected`

Assertions, in order:
1. Count dialogs from the top (`page.on("dialog", ...)`) and assert `dialogs === 0` at the end —
   every refusal must be inline.
2. Open the panel (the empty state is gone by step 3, so use the panel's own control), upload via
   `setInputFiles`, wait for `import-preview`.
3. `import-counts` contains the four numbers — assert the NUMBERS, not just that text rendered.
4. `import-rejected` names the malformed row's PHYSICAL file line and a reason.
5. `import-confirm` is `disabled` BEFORE the tick and enabled after — this is the consent contract,
   observed rather than assumed.
6. `shot(page, "step-03b-import-preview")` — the preview screen has to earn trust and is an owner
   judgement call, like steps 9b/10/13.
7. Tick, fill the "where did these come from?" box, Confirm, wait for `import-done`.
8. The new rows appear in `pipeline-contact-row`, and the imported one's Origin chip reads
   "Imported from a file" and its Consent chip carries the imported marker.
9. **Read back with `fetchQuery(api.contacts.listContacts, {}, auth)` and assert Jane's name is
   still byte-for-byte "Jane Doe"** — the preview is the thing under test, so it cannot also be the
   oracle.
10. A second upload of a >`IMPORT_ROW_MAX` CSV (generate the string in-memory) is refused inline in
    `import-refusal`, naming 1,000, with the dialog count still 0 and no rows written.
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test e2e/pipeline-uat.spec.ts --no-deps --workers=1 --grep "step 3b"</automated>
  </verify>
  <done>The new step passes against a running stack; the screenshot is on disk; the read-back assertion proves the un-overwritten name from stored state, not from the preview.</done>
</task>

<task type="auto">
  <name>Task 2: the phone-width step, both playbook gates, the validation map</name>
  <files>apps/web/e2e/pipeline-uat.spec.ts, docs/playbooks/cockpit.md, docs/playbooks/contacts-crm.md, .planning/phases/19.1-bulk-contact-import-csv/19.1-VALIDATION.md</files>
  <action>
1. **Extend step 14** (390px): assert the import panel is visible, that
   `document.documentElement.scrollWidth <= 391` still holds with the panel and its preview table on
   screen, and that the four `stat-tile`s are still >80px wide. A file-picker panel with a wide fixed
   element or a non-scrolling preview table is exactly what breaks this.
2. **Stop-hook gate 2 — `docs/playbooks/cockpit.md`.** It watches `apps/web/e2e/` (line 109), so
   touching this spec REQUIRES a `cockpit.md` bump too. Add a `> Last verified:` line marked
   **WATCH-GATE BUMP ONLY** (19-07's note is the precedent): no cockpit behaviour changed; the e2e
   suite gained a contacts-import step. SINGLE-LINE anchor — the file is CRLF.
3. **`docs/playbooks/contacts-crm.md`** final bump: the phase's closing invariants — an import fills
   blanks and destroys nothing (fields OR consent); provenance is set once; the browser step is what
   proves the capability is reachable; the ceilings paragraph names 1,000 rows per import, 100 rows
   per mutation, 500 addresses per match, and that nothing caps a tenant's total.
4. **Fill `19.1-VALIDATION.md`'s Per-Task Verification Map** with the observed result for every task
   across plans 01-07 (the planner pre-filled the rows; replace ⬜ with ✅/❌ and record the actual
   commands run). Tick the Wave 0 boxes — the fixture item is satisfied by the inline constant, and
   the reason (autocrlf would mangle a committed CRLF/BOM file) goes in the box. Set
   `wave_0_complete: true` and `status: complete` in its frontmatter.
5. Commit (check `.git/MERGE_HEAD`, pathspec only):
```
git commit -m "test(19.1-07): the browser proves the import is reachable" -- apps/web/e2e/pipeline-uat.spec.ts docs/playbooks/cockpit.md docs/playbooks/contacts-crm.md .planning/phases/19.1-bulk-contact-import-csv/19.1-VALIDATION.md
```
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs && pnpm test && pnpm typecheck</automated>
  </verify>
  <done>Step 14 covers the panel; both playbooks carry a new Last verified line; the validation map has no ⬜ rows left; `check-playbooks.mjs` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: owner runs the full browser suite and judges the preview screen</name>
  <what-built>
The complete CSV import: a pure parser in `@pikar/core`, two widened schema unions with all 19
registration sites walked, fill-empty-only as a flag on the ONE contact writer, `matchExisting` +
`importContacts`, an honest `pipelineTiles` bound, the three-step panel on `/dashboard/pipeline`, and
a browser step that uploads a real CSV end to end.
  </what-built>
  <how-to-verify>
1. Bring the stack up (the spec's own header is the contract):
   - `cd packages/backend && npx convex dev` on :3210 — **not** `--once`, and **not** with
     `--typecheck=disable`, so a stale build cannot be served silently
   - `cd apps/web && pnpm build && pnpm start -p 3111` — a PRODUCTION build; `next dev` OOMs on
     `/workspace`
2. `cd apps/web && npx playwright test e2e/pipeline-uat.spec.ts --no-deps --workers=1`
   Expect **16/16** (15 pre-existing + the new import step) and `uat/spend.json` unchanged — the new
   step costs $0.
3. Open `apps/web/uat/step-03b-import-preview.png` and judge, as the owner:
   - Does the preview read as trustworthy? Are the counts, the mapping and the rejected-row reasons
     legible without explanation? It is the last screen before a bulk write.
   - Is the attestation sentence one you would stand behind on record?
4. On `/dashboard/pipeline`, compare a hand-added contact's Consent chip against an imported one —
   is the distinction legible, or does it read as noise?
  </how-to-verify>
  <action>PAUSE. Do not proceed past this task without the owner. Bring the stack up as described
  above, run the full browser suite, put the screenshot in front of the owner, and wait for the
  resume signal. Record the owner words verbatim in the summary — a paraphrased sign-off is not
  one (19-10 established this).</action>
  <done>The suite ran 16/16 against a real stack, the owner looked at the preview screenshot
  and the two consent chips, and the verdict is recorded verbatim in the plan summary.</done>
  <resume-signal>Type "approved", or describe what reads wrong on the preview or the chips</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/web && npx playwright test e2e/pipeline-uat.spec.ts --no-deps --workers=1` — 16/16
- `pnpm test` + `pnpm typecheck` — green, delta 0 against the plan-02 baseline
- `node scripts/check-playbooks.mjs` exits 0 with BOTH playbooks bumped
- `.planning/phases/19.1-bulk-contact-import-csv/19.1-VALIDATION.md` — every map row resolved
</verification>

<success_criteria>
A real browser has picked a real CSV, seen honest counts, been refused a Confirm until the
attestation was ticked, written the contacts, and left a hand-typed name untouched — and the owner
has looked at the screen and said so.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-bulk-contact-import-csv/19.1-07-SUMMARY.md`
</output>
