---
phase: 25-private-beta-productionization
plan: 04
type: execute
wave: 3
depends_on: [25-01]
files_modified:
  - packages/backend/convex/onboarding.ts
  - packages/backend/convex/onboarding.test.ts
  - packages/backend/convex/cockpit.test.ts
  - apps/web/app/(app)/dashboard/onboarding/page.tsx
  - apps/web/app/(app)/dashboard/workspace/page.tsx
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/onboarding-first-send.spec.ts
  - docs/playbooks/onboarding.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [BETA-03]

must_haves:
  truths:
    - "Completing the existing conversational onboarding leads to one concrete first-send offer in the existing cockpit; no second wizard, route, state table, or persistent checklist exists"
    - "The offer works for a thin idea-stage profile with only oneLineDescription and persona"
    - "A missing postal address is collected inline through the existing tenantProfile.saveFacts boundary and remains outside missingSlots/canComplete"
    - "A missing mailbox prompts the existing connect/reconnect flow at first Approve and resumes the same plan afterward"
    - "One tap begins the existing resolve -> PLAN -> Approve -> governed delivery path to the authenticated user's own email"
  artifacts:
    - path: "packages/backend/convex/onboarding.ts"
      provides: "Authenticated first-send recipient/eligibility projection without new persisted onboarding state"
    - path: "apps/web/app/(app)/dashboard/workspace/page.tsx"
      provides: "One-time first-run cockpit opener wired to existing agent send behavior"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "Inline postal-address and mailbox recovery at the existing Approve refusal seam"
    - path: "apps/web/e2e/onboarding-first-send.spec.ts"
      provides: "Thin-profile one-tap path through PLAN and Approve using SMOKE fixtures"
  key_links:
    - from: "apps/web/app/(app)/dashboard/onboarding/page.tsx"
      to: "apps/web/app/(app)/dashboard/workspace/page.tsx"
      via: "after saveFacts then commitProfile succeed, redirect with an ephemeral first-run marker"
      pattern: "firstRun"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "packages/backend/convex/tenantProfile.ts"
      via: "no_postal_address recovery calls the existing validated saveFacts write boundary"
      pattern: "saveFacts"
---

<objective>
Extend the shipped Phase 11/15.1 onboarding into a first real governed email without reopening its
completion contract or discovering the postal-address and mailbox blockers during live UAT.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/11-persona-onboarding-business-profile/11-CONTEXT.md
@.planning/phases/15.1-fact-derived-tier-conversational-onboarding/15.1-CONTEXT.md
@CLAUDE.md
@docs/design/BRAND.md
@docs/playbooks/onboarding.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose a bounded first-send offer without adding onboarding state</name>
  <files>packages/backend/convex/onboarding.ts, packages/backend/convex/onboarding.test.ts, apps/web/app/(app)/dashboard/onboarding/page.tsx, apps/web/app/(app)/dashboard/workspace/page.tsx</files>
  <behavior>
    - The authenticated projection returns the current user's own email and whether the first-send offer is eligible; it never accepts a client-provided email/tenant id.
    - A thin idea-stage profile is eligible after commitProfile; incomplete onboarding is not.
    - After saveFacts then commitProfile, onboarding redirects to the existing cockpit with a transient marker; refresh/back navigation does not create a persistent checklist.
    - The cockpit renders one seeded opener and a single concrete action such as “Send a short introduction to myself”; it does not change a registry skill body.
  </behavior>
  <action>
    Read the final Phase 11/15.1 implementations before editing. Add the smallest authenticated query
    needed to derive the user's own address and eligibility from existing rows. Preserve
    `missingSlots`, `canComplete`, `converse.done`, saveFacts→commitProfile ordering, and the absence
    of an onboarding-state table byte-for-byte. Use a query parameter/session-local marker plus a
    seeded UI opener, not a `cockpit-agent` skill edit; this avoids an unnecessary eval gate and is
    content, not a hardcoded system prompt. The single action must call the existing chat/resolve
    seam rather than fabricating a plan client-side.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/onboarding.test.ts</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>The offer appears only after the existing completion transaction, uses the signed-in user's own address, and works with the minimum idea-stage profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover postal address and mailbox inline at first Approve</name>
  <files>packages/backend/convex/cockpit.test.ts, apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <behavior>
    - Approve returning `no_postal_address` reveals an inline textarea, saves through tenantProfile.saveFacts, and retries the same plan only after a valid nonblank at-most-500-character address succeeds.
    - Postal address remains absent from missingSlots/canComplete; existing tenants are not forced back through onboarding.
    - Approve with no selected provider connection uses the existing connect/reconnect affordance and preserves the proposed plan for resume.
    - The first-send plan targets only the authenticated user's address and uses the real executePlan mutation, audit, footer, telemetry, and per-recipient report.
  </behavior>
  <action>
    Treat `no_postal_address` as a recoverable first-Approve state, not a bypass. Reuse the existing
    write validator and inline error style; never exempt the send from CAN-SPAM or call
    `notifyExternal`. For a missing mailbox, reuse the current connect flow/ReconnectBanner and keep
    plan state proposed so return can resume safely. Add explicit backend tests for a thin profile
    whose postal address starts absent and succeeds only after the same saveFacts boundary.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/cockpit.test.ts convex/onboarding.test.ts</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>A new thin-profile user can recover both missing prerequisites without leaving the cockpit, weakening the footer guard, or changing onboarding completeness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Automate the first-run cockpit path with SMOKE fixtures</name>
  <files>apps/web/e2e/onboarding-first-send.spec.ts, docs/playbooks/onboarding.md, docs/playbooks/cockpit.md</files>
  <behavior>
    - The e2e starts from incomplete thin-profile onboarding, completes it, sees exactly one offer, taps it, reaches a PLAN card, handles postal/mailbox recovery, approves, and observes terminal delivered/report state.
    - The test does not claim a real mailbox delivery; that remains Plan 09's live checkpoint.
  </behavior>
  <action>
    Use the existing SMOKE fixture spine and authenticated storage setup. Assert semantic text/roles and
    terminal state, not timing sleeps. Update both playbooks with the attach point, the explicitly
    preserved completion contract, postal/mailbox recovery, and the split between automated fixture
    proof and live wall-clock proof.
  </action>
  <verify>
    <automated>pnpm test:e2e -- e2e/onboarding-first-send.spec.ts</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The scripted fixture path reaches delivered, and the documentation does not overclaim the later real-email checkpoint.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/onboarding.test.ts convex/cockpit.test.ts`
- `pnpm test:e2e -- e2e/onboarding-first-send.spec.ts`
- `pnpm --filter @pikar/web build`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- One existing onboarding path flows into one cockpit offer
- Thin profiles pass without widening completeness
- Postal address and mailbox absence recover inline at Approve
- Automated fixture proof reaches the real governed terminal path
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-04-SUMMARY.md`.</output>
