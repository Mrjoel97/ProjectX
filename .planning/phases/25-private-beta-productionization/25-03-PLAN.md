---
phase: 25-private-beta-productionization
plan: 03
type: execute
wave: 4
depends_on: [25-01, 25-02]
files_modified:
  - packages/backend/convex/isolation.test.ts
  - packages/backend/convex/skilloptExport.ts
  - packages/backend/convex/skilloptExport.test.ts
  - docs/playbooks/authorization.md
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [BETA-02, BETA-05]

must_haves:
  truths:
    - "Every runtime table is classified as tenant-covered, Convex-Auth-exempt, or deliberately global; adding an unclassified table fails the suite"
    - "Every index on a tenant-owned table leads with tenantId unless a named, reviewed exception proves why it is safe"
    - "One two-identity harness proves cross-tenant reads and writes fail across the complete covered surface and the owner's pre-existing rows remain attributed"
    - "A non-owner receives OWNER_REQUIRED from all three owner-gated Phase-8 functions"
    - "Grounded-prose export remains owner-only and the packages/pii names-in-prose scrub gap is the named unlock"
  artifacts:
    - path: "packages/backend/convex/isolation.test.ts"
      provides: "Schema-derived table/index classification, public-surface coverage scan, two-user matrix, and owner-boundary assertions"
    - path: "packages/backend/convex/skilloptExport.test.ts"
      provides: "Regression proof that grounded prose cannot be exported by a non-owner"
  key_links:
    - from: "packages/backend/convex/isolation.test.ts"
      to: "packages/backend/convex/schema.ts"
      via: "Object.keys(schema.tables) plus TableDefinition[' indexes']() runtime enumeration"
      pattern: "schema.tables"
    - from: "packages/backend/convex/isolation.test.ts"
      to: "packages/backend/convex/contacts.test.ts"
      via: "reuse the established two-user identity and import.meta.glob coverage pattern"
      pattern: "withIdentity"
---

<objective>
Create the phase's culminating isolation gate as an executable proof that expands automatically with
the schema and public tenant-wrapper surface instead of freezing today's table list in prose.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-01-SUMMARY.md
@CLAUDE.md
@packages/backend/convex/schema.ts
@packages/backend/convex/contacts.test.ts
@packages/backend/convex/lib/functions.ts
@docs/playbooks/authorization.md
@docs/playbooks/skill-registry.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive complete table and index coverage from the runtime schema</name>
  <files>packages/backend/convex/isolation.test.ts</files>
  <behavior>
    - `COVERED + EXEMPT_AUTH + EXEMPT_GLOBAL` sorted equals `Object.keys(schema.tables).sort()` exactly.
    - A deliberately injected unknown table/classification mismatch makes the assertion red.
    - Every exempt-global table lacks a tenantId validator field; every covered table has one.
    - Every index on every covered table has tenantId as its first field, with narrow commented exceptions only where a cross-tenant range is impossible by construction.
    - The experimental quoted `" indexes"()` accessor sees at least one known index, so API removal cannot turn the scan vacuously green.
    - An import.meta.glob source scan classifies every exported tenantQuery/tenantMutation/tenantAction/ownerQuery/ownerMutation surface or fails on an unlisted addition.
  </behavior>
  <action>
    Build pure static tests first, with explicit reasons beside every Auth/global exemption. Derive
    ALL from `schema.tables`; do not hand-author ALL. Use validator fields to catch a tenant table
    incorrectly parked in EXEMPT_GLOBAL. Use the quoted public index accessor and assert a known
    index is visible. For edge-runtime source coverage use eager `import.meta.glob(..., '?raw')`,
    never node:fs. When the first run surfaces a legitimate non-tenant-leading index, prove and
    name the exception rather than weakening the rule globally.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/isolation.test.ts</automated>
  </verify>
  <done>Static coverage is non-vacuous and fails on an unclassified table, uncovered wrapper export, or unsafe tenant-table index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the full two-user behavioral matrix in one backend harness</name>
  <files>packages/backend/convex/isolation.test.ts</files>
  <behavior>
    - Seed owner A before user B exists; after B is added, every A row remains owned by A and every tenant reader returns only its own rows.
    - B cannot read, patch, delete, approve, send, or otherwise act on A-owned ids; rejected operations leave A rows and audit counts unchanged.
    - B's valid writes always carry B's tenant id and remain invisible to A unless an intentionally global owner-only projection is involved.
    - Non-owner calls to optimizerConfig.setOptimizerEnabled, skills.activateCandidate, and skills.candidatesForReview reject with OWNER_REQUIRED and mutate nothing.
  </behavior>
  <action>
    Reuse the 19-02 identity shape `${userId}|session_x` and its byte-unchanged assertions. Use ONE
    convexTest instance for the driven table/function matrix; registering a component per case is
    forbidden because the established shared-worker memory failure makes unrelated tests flake.
    Register aggregate components only for cases that actually reach them. Separate structural
    coverage from behavioral samples, and require every covered surface to name its behavioral
    probe or explicit safe structural proof.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/isolation.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>The two-user suite covers every classified tenant surface, preserves owner attribution, and proves all three owner APIs reject a non-owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin grounded-prose export behind requireOwner</name>
  <files>packages/backend/convex/skilloptExport.ts, packages/backend/convex/skilloptExport.test.ts, docs/playbooks/authorization.md, docs/playbooks/skill-registry.md</files>
  <behavior>
    - Owner export behavior remains available and unchanged.
    - Non-owner direct invocation rejects OWNER_REQUIRED before any grounded prose is read or serialized.
    - No test claims packages/pii scrubs names in free prose.
  </behavior>
  <action>
    Extend the existing export test at the current public boundary; change `skilloptExport.ts` only if
    the test finds it is not already owner-wrapped. Record the exact ceiling and unlock: grounded prose stays owner-only
    until packages/pii gains tested names-in-prose scrubbing. Do not attempt that scrub here.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/skilloptExport.test.ts convex/isolation.test.ts</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Owner export stays functional, non-owner export fails before content access, and both playbooks name the scrub ceiling honestly.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/isolation.test.ts convex/skilloptExport.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- `pnpm test`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- The suite fails automatically when a table, index, or tenant wrapper appears without a classification/probe
- Two real identities cannot cross-read or cross-write and owner data remains correctly attributed
- All three owner functions reject a non-owner
- Grounded-prose export is owner-only with the unlock documented
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-03-SUMMARY.md`.</output>
