---
phase: 25-private-beta-productionization
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-private-beta-productionization/25-PREREQUISITE-EVIDENCE.md
autonomous: false
requirements: [BETA-01, BETA-02, BETA-03, BETA-05, DLVR-02]
amended: "2026-08-16 at owner direction. THE PREREQUISITE SET IS NARROWED TO THE BETA-CRITICAL LANES.
  As written, Task 1 required completion evidence for Phases 14, 15.3, 17, 17.1, 18, 19, 20, 20.1,
  21, 22.1, 23, 24, 26 and 31 -- effectively the whole v2.0 milestone -- before Phase 25 could edit
  application code. Two consequences made that unworkable. (1) IT CREATED A CYCLE: Phase 23's own
  first must-have truth blocks on GOVN-01 closure, and GOVN-01's closure was scheduled inside 25-02,
  while this plan blocked 25-02 behind Phase 23. The cycle is now broken from the other end --
  `22-VERIFICATION.md` is `status: passed` 5/5 and REQUIREMENTS.md records GOVN-01 Complete
  (2026-08-16) -- so Phase 22 closed INDEPENDENTLY, exactly as the 2026-08-16 milestone audit's
  dependency-safe order step 1 requires. (2) IT CONFLATED TWO REQUIREMENT LANES. Phase 25 carries
  BETA-01/02/03/05 and DLVR-02 -- admission, isolation, first-result onboarding and Outlook
  delivery. Phases 23, 24, 26 and 31 carry SKILL-02, GOVN-02, product-page and marketing scope that
  no Phase 25 requirement consumes. They share FILES with Phase 25, which is a working-tree
  sequencing problem, not a requirement dependency.

  WHAT STAYS A PREREQUISITE is now derived per requirement rather than per phase, and every one of
  them is already closed: BETA-01 needs Phase 22's requireOwner for the admin/ops surface (passed
  5/5); BETA-02/BETA-05 need Phase 22.1's table registry and erasure work (GOVN-03 Complete
  2026-08-16, live-proven on production SHA `1ca7c6f`); BETA-03 needs Phase 11/15.1 onboarding
  (both passed); DLVR-02 needs the ONE shared Microsoft connection from 17-06 under ADR-018
  (landed and browser-verified at `a36c641`/`72ff6bc`/`d783479`). 17-07..17-11 are calendar
  MANAGEMENT (ACTN-02) and are NOT on the mail path.

  WHAT THIS AMENDMENT DOES NOT DO: it does not weaken Task 2 or Task 3. The quiescent-baseline
  requirement, the foreign-lane stop rule, and the full Plan 01-13 structural + goal-backward
  re-verification all stand unchanged. Excluded lanes are DEFERRED, NOT DECLARED COMPLETE -- Phase
  23 remains blocked on Phase 21's unrun live gate (21-06/21-07 have no SUMMARY, no
  21-LIVE-RESULT.json exists, SKILL-01 Pending) and must not be recorded as passed by this narrowing."

must_haves:
  truths:
    - "Every lane that a Phase 25 requirement actually consumes -- Phase 22 for owner authorization, Phase 22.1 for the table registry and erasure, Phase 11/15.1 for onboarding, 17-06 for the shared Microsoft grant -- is complete before Phase 25 edits application code"
    - "Lanes that share files with Phase 25 but feed no Phase 25 requirement (23, 24, 26, 31) are sequenced in the working tree, not gated on, and are recorded as deferred rather than complete"
    - "Phase 32 is not silently added as a prerequisite; its legal-entity-blocked verification track stays off the private-beta critical path"
    - "The final schema, tenant-wrapper export surface, Gmail callers, onboarding flow, deployment gates, and dirty-worktree state are re-inventoried after prerequisites land"
    - "No Phase 25 plan executes while overlapping foreign-lane edits remain in its owned files"
    - "Material baseline drift stops execution and returns to `$gsd-plan-phase 25`; Plan 00 never rewrites downstream plans"
  artifacts:
    - path: ".planning/phases/25-private-beta-productionization/25-PREREQUISITE-EVIDENCE.md"
      provides: "Auditable prerequisite completion matrix and stable-baseline inventory for all later plans"
  key_links:
    - from: ".planning/phases/25-private-beta-productionization/25-PREREQUISITE-EVIDENCE.md"
      to: ".planning/ROADMAP.md"
      via: "each required lane is tied to completed plan/UAT/verification evidence, not a status assertion"
      pattern: "Phase 31"
---

<objective>
Stop Phase 25 before application changes until the milestone's pre-beta work has produced a stable,
non-overlapping baseline. Re-run the volatile inventories after that gate instead of planning from
today's in-progress schema and call graph.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prove every pre-beta prerequisite lane is complete</name>
  <files>.planning/phases/25-private-beta-productionization/25-PREREQUISITE-EVIDENCE.md</files>
  <action>
    AMENDED 2026-08-16 -- see the `amended:` frontmatter for the full rationale. Require completion
    evidence ONLY for the lanes a Phase 25 requirement actually consumes:

      - BETA-01 (invite admission, admin/ops surface) <- Phase 22 `requireOwner`
      - BETA-02 / BETA-05 (isolation across every table and index) <- Phase 22.1 table registry,
        export and erasure
      - BETA-03 (first delivered result via guided onboarding) <- Phase 11 and Phase 15.1
      - DLVR-02 (Outlook delivery through the same adapter) <- 17-06's ONE shared Microsoft
        connection under ADR-018

    For each cite its SUMMARY/VERIFICATION/UAT artifact and the commit containing its final code.
    A roadmap checkbox alone is not evidence.

    EXPLICITLY EXCLUDED, AND RECORDED AS DEFERRED RATHER THAN COMPLETE -- state each one's real
    status so the narrowing can never be misread as closure:

      - Phase 23 (SKILL-02) -- blocked on Phase 21's unrun live gate; 21-06/21-07 have no SUMMARY,
        no `21-LIVE-RESULT.json` exists, SKILL-01 Pending. Shares nine files with Phase 25.
      - Phase 24 (GOVN-02), Phase 26, Phase 31 (MKTG-01..03) -- no Phase 25 requirement consumes them.
      - Phase 14, 15.2, 15.3, 17-07..17-11, 17.1, 18, 20, 20.1, 20.2 -- open lanes on other
        requirement tracks; none is on an admission, isolation, onboarding or mail path.
      - Phase 32 -- unchanged: legally blocked, its verification/legal-entity track is expressly off
        Phase 25's critical path.

    The exclusions are a SCOPE decision about what Phase 25 waits for. They are NOT a claim that the
    excluded work is done, and this matrix must say so in the same row it excludes them.
  </action>
  <verify>
    <manual>Owner reviews the matrix and confirms every included lane is complete, or execution stops with the exact unfinished lane named.</manual>
  </verify>
  <done>Owner types `approved` only when every included prerequisite has evidence and Phase 32 is visibly excluded with the governing rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze and record the actual post-prerequisite baseline</name>
  <files>.planning/phases/25-private-beta-productionization/25-PREREQUISITE-EVIDENCE.md</files>
  <action>
    After approval, run `graphify update .` then `node scripts/extract-convex-edges.mjs`, and inventory:
    `Object.keys(schema.tables)` plus all index descriptors; every tenant/owner wrapper export; every
    non-test `internal.gmail.*` caller; all `process.env` references plus Convex Auth implicit keys;
    the current `gmailTokens`, `plans`, and `requests` validators; the onboarding redirect/commit and
    first Approve path; every source/UI path that generates, displays, sends, or copies a URL a beta
    user or their client could receive (including unsubscribe links); the current playbook watch map;
    and exact files with uncommitted changes.
    Record counts and commit SHA in the evidence file. If a foreign lane still modifies any Phase 25
    owned file (especially schema.ts, cockpit.ts, plans, contacts, approvals, actionType, or workspace),
    stop—do not stash, reset, overwrite, or absorb it. Compare the recorded inventory with every
    assumption, symbol, path, file-ownership list, and verification command in Plans 01-13 and
    25-VALIDATION. If any material drift exists, STOP the Phase 25 execution lane after this task,
    record the exact affected assumptions/plans, and return to `$gsd-plan-phase 25` for planning-only
    revision and checker verification. Plan 00 MUST NOT rewrite any PLAN/VALIDATION file; do not begin
    Plan 01 or edit application source. Line numbers in research are advisory only.
  </action>
  <verify>
    <automated>git status --short</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The evidence artifact names a clean/stable SHA, current inventories/gates, no overlapping foreign edits, and either `drift: none` or `replanning required` with the exact affected plans; drift cannot advance directly to Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the reconciled executable plan set before Plan 01</name>
  <files>.planning/phases/25-private-beta-productionization/25-PREREQUISITE-EVIDENCE.md</files>
  <action>
    For every Plan 01-13 file, run
    `node C:/Users/expert/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure <plan-file>`.
    Then rerun the Phase 25 `gsd-plan-checker` against the complete plan set, ROADMAP, REQUIREMENTS,
    CONTEXT, and RESEARCH; require both structural verification and goal-backward verification to
    pass. If Task 2 recorded drift, this checkpoint may run only after `$gsd-plan-phase 25` has
    produced revised files and a fresh checker verdict against the final-baseline inventory. Record
    each command/result, checker verdict, affected files,
    and reviewer in 25-PREREQUISITE-EVIDENCE.md. Any failure returns to planning-only reconciliation;
    application execution remains stopped.
  </action>
  <verify>
    <manual>Owner confirms all thirteen Plan 01-13 structure checks are green, the complete Phase 25 goal-backward checker passes, every material inventory delta is incorporated, and the evidence records the reconciled plan SHA.</manual>
  </verify>
  <done>Owner types `approved` on the reconciled plan set; only this approval releases Plan 25-01.</done>
</task>

</tasks>

<verification>
- Blocking owner approval of the prerequisite matrix
- `pnpm test`, `pnpm typecheck`, and `node scripts/check-playbooks.mjs` green at the recorded SHA
- No Phase 25-owned path has unresolved foreign-lane edits
- Phase 32 is explicitly excluded rather than accidentally treated as complete
- All Plan 01-13 structures and the complete goal-backward plan set are re-verified against the final inventory, with blocking owner approval
</verification>

<success_criteria>
- Phase 25 starts only after all real pre-beta prerequisites finish
- Later plan assumptions are refreshed from the final code rather than today's moving baseline
- Shared-worktree ownership is safe before the first application edit
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-00-SUMMARY.md`.</output>
