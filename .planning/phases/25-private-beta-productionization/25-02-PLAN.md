---
phase: 25-private-beta-productionization
plan: 02
type: execute
wave: 3
depends_on: [25-01]
files_modified:
  - apps/web/app/(auth)/signup/page.tsx
  - apps/web/app/(app)/admin/page.tsx
  - apps/web/app/(app)/admin/AdminView.tsx
  - apps/web/app/(app)/ops/page.tsx
  - apps/web/e2e/admin.spec.ts
  - docs/playbooks/beta-admission.md
  - docs/playbooks/authorization.md
autonomous: true
requirements: [BETA-01]

must_haves:
  truths:
    - "The existing public signup page accepts an invite query parameter or pasted code and also hosts the waitlist request path without adding another public route"
    - "The owner can review pending waitlist entries, approve exactly one invite, copy its signup link, and operate the three already-owner-gated optimizer controls from one admin surface"
    - "A non-owner cannot render the admin controls even though the backend remains the authoritative boundary"
    - "OAuth refusal returns to signup with truthful generic copy instead of a silent bounce"
  artifacts:
    - path: "apps/web/app/(auth)/signup/page.tsx"
      provides: "Invite preflight, waitlist request, editable prefilled code, and Google/Microsoft/password signup affordances"
    - path: "apps/web/app/(app)/admin/page.tsx"
      provides: "Owner-confirmed route gate"
    - path: "apps/web/app/(app)/admin/AdminView.tsx"
      provides: "Waitlist approval and existing optimizer controls"
    - path: "apps/web/e2e/admin.spec.ts"
      provides: "Owner/non-owner DOM and issuance flow coverage"
  key_links:
    - from: "apps/web/app/(auth)/signup/page.tsx"
      to: "packages/backend/convex/invites.ts"
      via: "public waitlist and invite preflight calls"
      pattern: "api.invites"
    - from: "apps/web/app/(app)/admin/page.tsx"
      to: "packages/backend/convex/owner.ts"
      via: "owner.viewer controls route rendering; owner wrappers still protect every operation"
      pattern: "owner.viewer"
---

<objective>
Expose the already-proven admission boundary through one public signup/waitlist page and one
owner-only admin page, while closing Phase 22's outstanding owner/non-owner DOM residue.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-01-SUMMARY.md
@.planning/phases/22-owner-authorization-primitive-requireowner/22-UAT-EVIDENCE.md
@CLAUDE.md
@docs/design/BRAND.md
@docs/playbooks/beta-admission.md
@docs/playbooks/authorization.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn signup into the invite and waitlist front door</name>
  <files>apps/web/app/(auth)/signup/page.tsx</files>
  <behavior>
    - `/signup?invite=CODE` pre-fills an editable visible code field; no parameter leaves the same field blank for paste.
    - Invalid/used code never enables provider/password submission and renders generic copy that does not reveal another invite's full email.
    - The no-code state offers a waitlist form on `/signup`; a successful request confirms receipt without promising approval timing.
    - OAuth starts with `redirectTo=/signup?invite=CODE&amp;r=1`; returning signed-out with `r=1` renders copy true for cancellation or failed redemption.
    - Google, Microsoft, and password signup all use the same preflight state; password passes the code into its credentials profile.
  </behavior>
  <action>
    Extend the existing signup page rather than creating a waitlist route or changing middleware.
    Reuse current auth components and global CSS tokens; add no component library. Do not treat
    preflight as authorization—the Plan 01 callback is authoritative. Mask the invited email and
    avoid account-enumeration copy. Preserve sign-in for returning users separately from new signup.
    Keep the code visible/auditable as locked by CONTEXT.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Invite links, pasted codes, waitlist requests, and truthful OAuth-return errors work on the one existing public route with brand tokens and no middleware widening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the consolidated owner-admin surface</name>
  <files>apps/web/app/(app)/admin/page.tsx, apps/web/app/(app)/admin/AdminView.tsx, apps/web/app/(app)/ops/page.tsx, apps/web/e2e/admin.spec.ts</files>
  <behavior>
    - Confirmed owner sees pending waitlist rows, approves one, receives a copyable `/signup?invite=...` link, and approval replay returns the same invite.
    - Confirmed owner sees and can use setOptimizerEnabled, activateCandidate, and candidatesForReview using their existing APIs.
    - Non-owner and owner-loading states render none of those controls and never flash protected content.
    - Direct non-owner API rejection remains separately covered by Plan 03; this e2e proves presentation only.
  </behavior>
  <action>
    Create `/admin` inside the existing default-deny `(app)` route group. Use `owner.viewer` before
    mounting the client view. Consolidate or link the existing `/ops` controls without duplicating
    backend state or wrappers; keep one canonical implementation of each control. Add loading,
    empty, refusal, copy-success, and mutation-error states. The invite code may be displayed once
    to the owner but must never enter logs/audit payloads. Make the authenticated Playwright spec
    exercise owner and controlled non-owner storage states using the established auth setup.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm test:e2e -- e2e/admin.spec.ts</automated>
    <automated>pnpm --filter @pikar/web build</automated>
  </verify>
  <done>The admin DOM gate passes for both identities, invite approval works, and Phase 22's outstanding presentation proof is recorded by an executed e2e result rather than asserted from source.</done>
</task>

<task type="auto">
  <name>Task 3: Update admission and authorization playbooks</name>
  <files>docs/playbooks/beta-admission.md, docs/playbooks/authorization.md</files>
  <action>
    Document the signup return/error semantics, admin route, one-time owner handling of raw codes,
    and the distinction between DOM hiding and backend authorization. Bump both Last verified lines
    and record the executed admin e2e evidence. Do not mark the real hosted OAuth gate complete here.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Both playbooks describe the shipped UI and retain the server boundary as the source of truth.</done>
</task>

</tasks>

<verification>
- `pnpm test:e2e -- e2e/admin.spec.ts`
- `pnpm --filter @pikar/web typecheck`
- `pnpm --filter @pikar/web build`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Signup and waitlist share one public route
- Invite code remains editable and visible without becoming the OAuth authorization mechanism
- Owner admin combines issuance and the three existing controls
- Non-owner protected DOM is absent and the admin e2e actually ran
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-02-SUMMARY.md`.</output>
