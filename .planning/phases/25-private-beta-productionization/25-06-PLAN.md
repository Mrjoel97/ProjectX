---
phase: 25-private-beta-productionization
plan: 06
type: execute
wave: 6
depends_on: [25-05]
files_modified:
  - packages/backend/convex/microsoftAuth.ts
  - packages/backend/convex/microsoftAuth.test.ts
  - packages/backend/convex/crons.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/mail-provider.spec.ts
  - docs/playbooks/cockpit.md
  - .planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [DLVR-02]
amended: "2026-08-14 by ADR-018 (docs/decisions/018-one-microsoft-connection-not-two.md). This plan
  and 17-06 each specified an independent Microsoft OAuth flow for the SAME account, colliding on
  http.ts, ReconnectBanner.tsx and cockpit.md. Both were unexecuted, so nothing is unwound. 17-06 NOW
  BUILDS THE ONE SHARED MICROSOFT CONNECTION -- microsoftAuth.ts, /microsoft/callback,
  /connect-microsoft, one token row, one disconnect, one reconnect state -- with the UNION grant
  `offline_access Calendars.ReadWrite Mail.Send Mail.Read` plus identity scopes. THIS PLAN NO LONGER
  MINTS A MICROSOFT GRANT, CALLBACK, CONSENT SURFACE OR TOKEN ROW. It builds the mail send/read
  ADAPTER ONLY, over the already-consented connection, and its DLVR-02 lifecycle work (connect,
  per-send choice, warn/re-auth, independent disconnect) operates on that shared row rather than a
  second one. DO NOT re-add a Microsoft callback to http.ts and DO NOT re-generalize
  ReconnectBanner.tsx by provider -- 17-06 does both, once. Verify 17-06 has landed before executing;
  if it has not, this plan's Microsoft prerequisites do not exist yet. The Microsoft revocation
  limitation is UNCHANGED and still owed honestly: the v2 flow used here has no Google-style
  revocation endpoint, so 'disconnect' deletes the local row and removing consent stays a separate
  owner action in Microsoft My Apps or Entra.

  RE-CUT 2026-08-15 at owner direction, against the LANDED 17-05/17-06 code. The amendment above was
  correct in PROSE but its machine-readable fields were never updated to match, and an executor reads
  those. They said this plan modifies `http.ts`, `gmailAuth.ts`, `connect-gmail/page.tsx` and
  `ReconnectBanner.tsx`, that http.ts 'provides Microsoft mailbox authorization/callback', and that
  the key link keys on a `by_tenant_provider` index -- all four contradict the amendment, and the
  index DOES NOT EXIST and must not (schema.ts explains why). Fixed now: files_modified, artifacts,
  key_links and truths all describe adapter-and-surface work over the ONE existing grant.

  ALSO CORRECTED: 17-06 already requests the mail scopes. MICROSOFT_SCOPES is the UNION grant
  (offline_access + identity + Calendars.ReadWrite + Mail.Send + Mail.Read), so there is no Azure
  registration or scope work left here -- the old truth naming only Mail.Send/Mail.Read as if they
  were this plan's to request is dropped. What IS still owed: the mail HALF may be unready on a grant
  consented before the union landed, so read `microsoftMailReady(scope)` and offer re-consent;
  `microsoftAuth.ts` already anticipates this ('a narrower scope string that would otherwise keep
  mailReady false forever'). GOVN-03 NOTE: `disconnectMicrosoft` returns revokedAtProvider:false and
  is documented in-source as NOT parity with disconnectGoogle. This plan must SURFACE that
  asymmetry honestly; it does not close it, and GOVN-03's provider-revocation clause stays open."

must_haves:
  truths:
    - "NO Microsoft grant, callback, consent page, token row or ReconnectBanner generalization is created here — 17-06 built all of them, once. A grep proves this plan adds no new http.route and no second consent surface"
    - "One tenant can connect Google and Microsoft simultaneously, see each status independently, disconnect either without deleting the other, and choose a provider per send"
    - "Mail availability is read as microsoftMailReady(scope), never as a bare `connected` boolean, so a legacy calendar-only grant presents as 'reconnect to send mail' rather than as a working mail provider"
    - "The Microsoft disconnect asymmetry is stated to the user, not hidden: disconnectMicrosoft returns revokedAtProvider:false by design and the surface must not imply Google-equivalent revocation"
    - "Microsoft reauth timing is provider-aware; the Google testing-mode seven-day warning remains Google-only"
  artifacts:
    - path: "packages/backend/convex/microsoftAuth.ts"
      provides: "The mail-half readiness surface over the EXISTING shared grant, and the re-consent path for a narrower legacy scope — no new callback"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "Per-send provider choice, gated on mailReady"
    - path: "apps/web/e2e/mail-provider.spec.ts"
      provides: "Offline coexistence, selection, mailReady-false refusal, and independent disconnect coverage"
  key_links:
    - from: "packages/backend/convex/microsoftAuth.ts"
      to: "packages/backend/convex/schema.ts"
      via: "reads the single shared grant; never mints a second row"
      pattern: "microsoftCalendarTokens"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "packages/backend/convex/cockpit.ts"
      via: "provider choice is saved on the plan before executePlan seeds requests"
      pattern: "mailProvider"
---

<objective>
Complete provider lifecycle and selection: connect both accounts, choose per send, warn/re-auth per
provider, and disconnect independently with an explicit Microsoft remote-invalidation posture.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-05-SUMMARY.md
@CLAUDE.md
@docs/design/BRAND.md
@packages/backend/.agents/skills/convex-migration-helper/SKILL.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface the MAIL HALF of the existing grant — no new connect flow</name>
  <files>packages/backend/convex/microsoftAuth.ts, packages/backend/convex/microsoftAuth.test.ts, packages/backend/convex/crons.ts</files>
  <behavior>
    - NOTHING here creates an authorize URL, a callback route, a consent page or a token row. 17-06 built all of them and `http.ts` carries the in-source instruction "must NOT add a second callback here". A test greps that this plan added no new `http.route`.
    - `microsoftStatus` already returns `calendarReady`/`mailReady` from the stored scope. This task makes the MAIL half consumable: a caller can ask "can this tenant send mail via Microsoft?" and get `mailReady`, never a bare `connected`.
    - A grant consented BEFORE the union scope set reads `mailReady:false`. That state offers RE-CONSENT through the existing 17-06 connect URL — it does not mint a parallel flow, and it does not silently present as a working mail provider.
    - A Microsoft mail send never touches `gmailTokens`; a Google send never touches `microsoftCalendarTokens`. Asserted both ways.
    - Reauth timing is provider-aware. Google keeps the seven-day testing-mode rule; Microsoft uses refresh-failure semantics and does not nag weekly.
  </behavior>
  <action>
    READ `microsoftAuth.ts` AND `schema.ts` BEFORE WRITING ANYTHING. The grant, the callback, the
    consent page, the disconnect and the reconnect state all exist. Your job is the mail-half
    READINESS surface over that row, plus the re-consent path for a narrower legacy scope —
    `microsoftAuth.ts` already anticipates this case in a comment ("a narrower scope string that
    would otherwise keep mailReady false forever").

    Do NOT re-request scopes: `MICROSOFT_SCOPES` is already the union grant
    (offline_access + identity + Calendars.ReadWrite + Mail.Send + Mail.Read), so there is no Azure
    registration work and no scope widening in this phase. Do NOT re-generalize `ReconnectBanner.tsx`
    by provider — 17-06 did it once.

    Leave `disconnectMicrosoft` returning `revokedAtProvider:false`. It is correct and deliberately
    documented as NOT parity with `disconnectGoogle`. Task 2 decides how that is presented; this task
    must not soften, hide or conditionally re-word it in source.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/microsoftAuth.test.ts convex/graph.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>The mail half of the ONE shared grant is readable as `mailReady`, a legacy calendar-only grant routes to re-consent, no second OAuth surface was added, and neither provider's send path can write the other's token row.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the Microsoft remote-invalidation support posture</name>
  <files>.planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md, docs/playbooks/cockpit.md</files>
  <action>
    Present the exact platform distinction with official-current evidence: Microsoft Entra exposes no
    Google-style endpoint under the existing delegated `Mail.Read Mail.Send offline_access` grant that
    revokes only this application's refresh grant; `/common/oauth2/v2.0/logout` ends the browser SSO
    session only; Graph `revokeSignInSessions` invalidates all refresh sessions for a work/school user,
    requires admin-consented `User.RevokeSessions.All`, and is unsupported for personal accounts.
    Owner chooses one posture: (A) accept honest local
    disconnect for beta, show explicit instructions/link for the user or tenant admin to remove the app
    grant remotely, and never label it remote revocation; or (B) STOP this lane and authorize a separate
    security/consent review for a specific broader session-invalidation mechanism before source work
    continues. Record the decision, evidence URL/date, UI wording, and consequence. Do not overstate
    Google/Microsoft parity.
  </action>
  <verify>
    <manual>Owner approves posture A's explicit limitation and remote-removal instructions, or posture B's blocked follow-up with a named supported mechanism and consent cost.</manual>
  </verify>
  <done>The Microsoft disconnect contract is an explicit owner-approved support posture, not a conditional claim in source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose connect-both and choose-per-send without one-active-provider state</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, apps/web/e2e/mail-provider.spec.ts</files>
  <behavior>
    - A plan with both providers available requires/retains an explicit per-send choice; one available defaults honestly; none routes to connect recovery.
    - The Microsoft option is offered only when `mailReady` is true. A connected-but-calendar-only grant shows re-consent, NOT a selectable mail provider.
    - Connecting or disconnecting one provider never changes the other's status.
    - Microsoft disconnect copy matches Task 2's approved support posture and never implies Google-equivalent revocation.
  </behavior>
  <action>
    SURFACE NOTE, unresolved and deliberately not decided here: 17-06 shipped `/connect-microsoft`
    as its OWN page alongside the existing `/connect-gmail`. This plan was written when one page was
    expected to manage both. TWO PAGES IS THE LANDED REALITY — do not merge them as a side effect of
    this task. If the owner wants one connections surface, that is a separate UX decision with its
    own plan; note it and move on.

    So this task touches the CHOICE surface only, not the connection pages. Reuse brand tokens and
    current card controls. Save provider on the plan before approval; never store a deployment-wide
    or tenant-wide "active provider." Gate the Microsoft option on `mailReady`, not on `connected`.
    Exercise coexistence, selection persistence, the mailReady-false path, and independent
    disconnect with fixture-backed e2e.
  </action>
  <verify>
    <automated>pnpm test:e2e -- e2e/mail-provider.spec.ts</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Connect-both and choose-per-send are visible and tested; no action silently overwrites the other provider.</done>
</task>

</tasks>

<verification>
- Focused backend auth/Graph tests (NOT migration tests — there is no migration in this phase)
- A grep proving this plan added no new `http.route` and no second consent surface
- `pnpm test:e2e -- e2e/mail-provider.spec.ts`
- Explicit owner decision on the exact Microsoft remote-invalidation limitation or a separately authorized supported mechanism
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Choose-per-send works without overwrites, gated on `mailReady` rather than `connected`
- No second Microsoft OAuth surface, callback or token row was created
- Disconnect and reauth are provider-aware; Google remote revocation remains verified and Microsoft remote invalidation follows the explicit owner-approved support posture
- Provider lifecycle and UI are ready for the live-provider plans (25-07 continuity, 25-08 threading, 25-09 parity)
- GOVN-03's provider-revocation clause is NOT closed by this plan and is not claimed to be
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-06-SUMMARY.md`.</output>
