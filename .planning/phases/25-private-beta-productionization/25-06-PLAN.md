---
phase: 25-private-beta-productionization
plan: 06
type: execute
wave: 6
depends_on: [25-05]
files_modified:
  - packages/backend/convex/http.ts
  - packages/backend/convex/gmailAuth.ts
  - packages/backend/convex/gmailAuth.test.ts
  - packages/backend/convex/crons.ts
  - apps/web/app/(app)/connect-gmail/page.tsx
  - apps/web/app/(app)/_components/ReconnectBanner.tsx
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/mail-provider.spec.ts
  - docs/playbooks/cockpit.md
  - .planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [DLVR-02]

must_haves:
  truths:
    - "One tenant can connect Google and Microsoft simultaneously, see each status independently, disconnect either without deleting the other, and choose a provider per send; Google remote revocation remains verified while Microsoft remote invalidation is represented only to the extent the chosen platform-supported posture proves"
    - "The Azure registration supports personal and work/school accounts through /common and requests offline_access Mail.Send Mail.Read plus identity scopes"
    - "Microsoft refresh rotation and reauth timing are provider-aware; the Google testing-mode seven-day warning remains Google-only"
  artifacts:
    - path: "packages/backend/convex/http.ts"
      provides: "Microsoft mailbox authorization/callback and independently scoped disconnect route with owner-approved remote-invalidation wording"
    - path: "apps/web/app/(app)/connect-gmail/page.tsx"
      provides: "Connect-both status, independent connect/disconnect, work-admin-consent errors, and provider choice"
    - path: "apps/web/e2e/mail-provider.spec.ts"
      provides: "Offline coexistence, selection, and independent disconnect coverage"
  key_links:
    - from: "packages/backend/convex/http.ts"
      to: "packages/backend/convex/schema.ts"
      via: "OAuth callback upserts only the tenant+Microsoft token row"
      pattern: "by_tenant_provider"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "packages/backend/convex/cockpit.ts"
      via: "provider choice is saved on the plan before executePlan seeds requests"
      pattern: "mailProvider"
---

<objective>
Complete provider lifecycle and selection: connect both accounts, choose per send, warn/re-auth per
provider, and disconnect independently with an explicit Microsoft remote-invalidation posture.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-05-SUMMARY.md
@CLAUDE.md
@docs/design/BRAND.md
@packages/backend/.agents/skills/convex-migration-helper/SKILL.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Microsoft connect, provider-aware status/reauth, and independent disconnect</name>
  <files>packages/backend/convex/http.ts, packages/backend/convex/gmailAuth.ts, packages/backend/convex/gmailAuth.test.ts, packages/backend/convex/crons.ts</files>
  <behavior>
    - Microsoft authorize/callback uses `/common`, PKCE/state protections matching the existing Google flow, and requested offline/read/send scopes.
    - Callback upserts only the Microsoft row; an existing Google row is byte-unchanged.
    - Disconnect Microsoft always deletes only the Microsoft local credential row with refs-only audit; it claims remote revocation/session invalidation only if Task 2 selects and verifies a supported mechanism.
    - Status returns both provider states. Google expiry warning retains the seven-day testing rule; Microsoft uses its verified inactivity/refresh failure semantics and does not nag weekly.
    - Work account “admin approval required” and personal-account errors return truthful provider-specific copy without leaking tokens.
  </behavior>
  <action>
    Mirror the proven Google OAuth lifecycle only where protocols match. Use the `/common` authority
    and single Azure registration configured for `AzureADandPersonalMicrosoftAccount`; request
    offline_access Mail.Send Mail.Read plus identity scopes. Persist returned refresh rotation.
    Implement independent status and disconnect paths. Preserve Google's verified
    `oauth2.googleapis.com/revoke` behavior. Do not invent a Microsoft equivalent: the v2 logout
    endpoint clears browser SSO but does not revoke this app's refresh grant, while Graph
    `revokeSignInSessions` invalidates all of a work/school user's refresh sessions, requires the
    admin-consented `User.RevokeSessions.All` permission, and is unsupported for personal Microsoft
    accounts. Keep the Microsoft UI/API wording local-only until
    Task 2 records the owner posture. Do not generalize Gmail's seven-day
    testing expiry or widen the seven-day scheduled-send horizon. Add callback route tests for state,
    coexistence, errors, and provider-local deletion.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/gmailAuth.test.ts convex/graph.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Both providers connect and delete their own local credentials independently, Google still revokes remotely, Microsoft makes no unverified remote-revocation claim, and refresh/account posture is represented truthfully.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the Microsoft remote-invalidation support posture</name>
  <files>.planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md, docs/playbooks/cockpit.md</files>
  <action>
    Present the exact platform distinction with official-current evidence: Microsoft Entra exposes no
    Google-style endpoint under the existing delegated `Mail.Read Mail.Send offline_access` grant that
    revokes only this application's refresh grant; `/common/oauth2/v2.0/logout` ends the browser SSO
    session only; Graph `revokeSignInSessions` invalidates all refresh sessions for a work/school user,
    requires admin-consented `User.RevokeSessions.All`, and is unsupported for personal accounts.
    Owner chooses one posture: (A) accept honest local
    disconnect for beta, show explicit instructions/link for the user or tenant admin to remove the app
    grant remotely, and never label it remote revocation; or (B) STOP this lane and authorize a separate
    security/consent review for a specific broader session-invalidation mechanism before source work
    continues. Record the decision, evidence URL/date, UI wording, and consequence. Do not overstate
    Google/Microsoft parity.
  </action>
  <verify>
    <manual>Owner approves posture A's explicit limitation and remote-removal instructions, or posture B's blocked follow-up with a named supported mechanism and consent cost.</manual>
  </verify>
  <done>The Microsoft disconnect contract is an explicit owner-approved support posture, not a conditional claim in source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose connect-both and choose-per-send without one-active-provider state</name>
  <files>apps/web/app/(app)/connect-gmail/page.tsx, apps/web/app/(app)/_components/ReconnectBanner.tsx, apps/web/app/(app)/dashboard/workspace/cards.tsx, apps/web/e2e/mail-provider.spec.ts</files>
  <behavior>
    - Connection page independently renders Google and Outlook connected/disconnected/reauth/error states and actions, and Microsoft disconnect copy matches Task 2's approved support posture.
    - Connecting or disconnecting one provider never changes the other's DOM status.
    - A plan with both available requires/retains an explicit per-send choice; one available defaults honestly; none routes to connect recovery.
    - ReconnectBanner names the affected provider and links to the shared connection page.
  </behavior>
  <action>
    Keep the existing route for compatibility even though it now manages both providers; changing the
    URL buys nothing. Reuse brand tokens and current card controls. Save provider on the plan before
    approval; never store a deployment-wide or tenant-wide “active provider.” Exercise coexistence,
    selection persistence, and independent disconnect with fixture-backed e2e.
  </action>
  <verify>
    <automated>pnpm test:e2e -- e2e/mail-provider.spec.ts</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
  </verify>
  <done>Connect-both and choose-per-send are visible and tested; no action silently overwrites the other provider.</done>
</task>

</tasks>

<verification>
- Focused backend auth/Graph/migration tests
- `pnpm test:e2e -- e2e/mail-provider.spec.ts`
- Explicit owner decision on the exact Microsoft remote-invalidation limitation or a separately authorized supported mechanism
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Connect both and choose per send work without overwrites
- Disconnect and reauth are provider-aware; Google remote revocation remains verified and Microsoft remote invalidation follows the explicit owner-approved support posture
- Provider lifecycle and UI are ready for the separately gated migration and live-provider plans
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-06-SUMMARY.md`.</output>
