---
phase: 25-private-beta-productionization
plan: 09
type: execute
wave: 9
depends_on: [25-08]
files_modified:
  - packages/backend/convex/graph.ts
  - packages/backend/convex/graph.test.ts
  - packages/backend/convex/delivery.ts
  - packages/backend/convex/delivery.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/llm.test.ts
  - packages/backend/convex/briefings.test.ts
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [DLVR-02]

must_haves:
  truths:
    - "Outlook matches Gmail for contact search, inbox listing, body fetch, briefing, reply target resolution, and governed reply delivery"
    - "Every mailbox read caller routes through the provider adapter; no non-test llm/cockpit caller reaches internal.gmail read actions directly"
    - "Graph search and range listing keep $search and $filter on separate endpoint shapes, body reads request plain text, and all outputs retain existing caps/redaction boundaries"
    - "SMOKE fixtures are checked before credentials for every Graph read action so offline eval/e2e remains deterministic"
    - "The Azure registration's `/common` authority plus `AzureADandPersonalMicrosoftAccount` audience supports personal and work/school account classes; a work tenant may still require tenant-admin consent, which is recorded as an external support restriction rather than falsely claimed as a verified mailbox"
    - "At least one consent-capable real Outlook account completes send, search, briefing, contact resolution, body fetch, and reply; work-account parity is claimed only when a controlled work tenant actually consents and passes"
  artifacts:
    - path: "packages/backend/convex/graph.ts"
      provides: "Fixture-first Graph search/listInbox/fetchInboxBodies/getReplyTarget parity"
    - path: "packages/backend/convex/delivery.ts"
      provides: "Provider routing for the complete mailbox read and send surface"
    - path: "packages/backend/convex/delivery.test.ts"
      provides: "Per-operation routing and zero direct-Gmail caller scans"
  key_links:
    - from: "packages/backend/convex/llm.ts"
      to: "packages/backend/convex/delivery.ts"
      via: "all contact, briefing, and reply tool calls use provider-aware actions"
      pattern: "internal.delivery"
    - from: "packages/backend/convex/graph.ts"
      to: "packages/backend/convex/smoke.ts"
      via: "fixtures return before token lookup and network fetch"
      pattern: "getInboxFixture"
---

<objective>
Finish DLVR-02's knowingly heavy lane: route every Gmail read-plane consumer through the same
two-provider adapter and prove full Outlook parity offline and against real personal/work posture.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-08-SUMMARY.md
@CLAUDE.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fixture-first Microsoft read-plane parity</name>
  <files>packages/backend/convex/graph.ts, packages/backend/convex/graph.test.ts</files>
  <behavior>
    - search uses Graph message `$search` with a bounded `$select/$top`, never combines `$filter`, and normalizes results to the existing contact-resolution contract.
    - listInbox uses inbox messages with ISO receivedDateTime `$filter`, `$orderby`, bounded `$top`, and bodyPreview.
    - fetchInboxBodies requests each bounded id with `Prefer: outlook.body-content-type="text"`, applies the existing body truncation cap, and never ports Gmail MIME walking.
    - getReplyTarget returns from/subject/internetMessageId/conversationId normalized to existing reply fields and provider-qualifies the thread handle.
    - All four operations return SMOKE fixture results before token mint; absent/expired Microsoft credentials return the provider-specific reconnect outcome.
  </behavior>
  <action>
    Implement exactly the endpoint mappings in RESEARCH §4.4 with bare fetch and no Graph SDK. Preserve
    existing caps, timeout/error behavior, refs-only audit, and toolless body-ingestion boundary.
    Escape KQL safely; never combine `$search` with `$filter`. Use internetMessageId for RFC reply
    headers and provider-qualified conversationId only as stored provider context. Add focused tests
    for wire query/header/body shapes, caps, fixture-before-token ordering, and failure taxonomy.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/graph.test.ts</automated>
  </verify>
  <done>All Graph read operations satisfy the same normalized contracts and run offline through fixtures without credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every mailbox-read consumer through the two-provider adapter</name>
  <files>packages/backend/convex/delivery.ts, packages/backend/convex/delivery.test.ts, packages/backend/convex/llm.ts, packages/backend/convex/llm.test.ts, packages/backend/convex/briefings.test.ts</files>
  <behavior>
    - Name/contact resolution, inbox digest listing/body fetch, and reply target/body fetch route by selected/connected provider.
    - Google output remains byte/shape compatible for existing tests.
    - A static import.meta.glob scan finds zero non-test direct `internal.gmail.search|listInbox|fetchInboxBodies|getReplyTarget` callers outside delivery.ts/tests.
    - Briefing body content remains toolless-ingested and never enters tool-bearing model context differently by provider.
  </behavior>
  <action>
    Extend the existing small delivery module with one switch per current normalized mailbox operation;
    do not introduce interfaces, registries, or a package. Rewrite every final-baseline caller found
    by Plan 00's inventory, not only the line list in research. The selected plan provider governs
    reply/contact context; provider-neutral briefing may use the user's explicit mailbox choice or a
    deterministic connected-provider choice exposed in the UI, never a deployment-global default.
    Keep Gmail tests green and add Microsoft fixture cases through the real llm tool seams.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/delivery.test.ts convex/llm.test.ts convex/briefings.test.ts convex/graph.test.ts convex/gmail.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Every read consumer routes through delivery, Gmail has no regression, and the static scan proves no provider-hardcoded caller remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-verify full Outlook parity and personal/work posture</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
    After Plan 08's threading checkpoint is approved, first verify the Azure registration uses
    `signInAudience: AzureADandPersonalMicrosoftAccount` and `/common`; those facts establish supported
    account classes but do not override a tenant's consent policy. Run the complete live matrix with a
    consent-capable personal account and, when available, a controlled work/school account. If the work
    tenant returns “Need admin approval,” record that exact tenant-admin restriction and do not call it
    an app failure or claim that work mailbox was live-verified;
    keep Google connected simultaneously; search a known sender; resolve a contact; list a date-bounded
    inbox; fetch bodies; generate a briefing; approve a new Outlook send and confirm arrival/footer;
    resolve and send a reply and reconfirm in-thread grouping; disconnect/revoke Microsoft and prove
    Google remains connected. Record response shapes, account class, timestamps, and hashed refs only.
  </action>
  <verify>
    <manual>Owner confirms a consent-capable real Outlook account passes send/read/reply parity; `/common` + audience configuration is evidenced, and any work-tenant admin-consent denial is recorded as an external restriction without a false work-mailbox pass.</manual>
  </verify>
  <done>Real Outlook send and full read plane pass on a consent-capable account; personal/work support configuration and any tenant-admin consent restriction are evidenced honestly.</done>
</task>

<task type="auto">
  <name>Task 4: Seal the mailbox-provider playbook</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
    Record endpoint distinctions, caps, provider-qualified thread handling, reconnect/disconnect rules,
    live matrix outcome, admin-consent support outcome, and rollback instructions. Bump Last verified
    and name Plan 08's threading evidence separately from this plan's full parity evidence.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The playbook is sufficient to change or diagnose either provider without rediscovering protocol differences.</done>
</task>

</tasks>

<verification>
- Full Graph/Gmail/delivery/llm focused test set
- Static zero-direct-Gmail read-caller scan
- Backend typecheck and full `pnpm test`
- Blocking real Outlook full-parity approval
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Microsoft supports send and the complete Gmail-equivalent read plane
- Every consumer uses the provider adapter
- Fixture/eval paths remain credential-free and deterministic
- `/common` + Azure audience support both account classes; live work-mailbox parity is claimed only when tenant-admin consent permits and the matrix actually passes
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-09-SUMMARY.md`.</output>
