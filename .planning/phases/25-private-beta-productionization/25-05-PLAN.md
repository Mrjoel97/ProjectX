---
phase: 25-private-beta-productionization
plan: 05
type: execute
wave: 5
depends_on: [25-02, 25-03, 25-04]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/graph.ts
  - packages/backend/convex/graph.test.ts
  - packages/backend/convex/delivery.ts
  - packages/backend/convex/delivery.test.ts
  - packages/backend/convex/deliverApprovedPlan.ts
  - packages/backend/convex/pipeline.ts
  - packages/backend/convex/cockpit.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [DLVR-02]
amended: "2026-08-14 by ADR-018 (light touch). RE-CUT 2026-08-15 against the LANDED code, at owner
  direction. NOTE ON AUTHORITY: 25-PREREQUISITE-EVIDENCE.md lists 'downstream plan reconciliation'
  among the things prohibited before Task 1 approval; the owner lifted that for this re-cut
  explicitly. Recorded, not silently bypassed.

  WHAT CHANGED, AND WHY THE OLD PLAN WOULD HAVE UNWOUND SHIPPED CODE. This plan was written to widen
  `gmailTokens` with an optional `provider` column, add a `by_tenant_provider` index and run a
  tracked backfill migration. NONE OF THAT MAY HAPPEN. Plan 17-05 landed a SECOND table,
  `microsoftCalendarTokens`, and schema.ts records the reasoning in-source: `gmailTokens` IS the
  Google grant, `freshAccessToken` is 'the ONE token-refresh root' over it, and a discriminator
  column 'would make every existing by_tenant .unique() read ambiguous' while the two grants have
  genuinely different refresh endpoints, scope strings and expiry behaviour. `gmailTokens` is
  UNCHANGED and stays that way.

  CONSEQUENCES: (1) NO provider column, NO by_tenant_provider index, NO migration -- the two tables
  are already keyed by tenant, so coexistence is structural and needs no backfill. `migrations.ts`
  and `migrations.test.ts` are REMOVED from files_modified. (2) The Microsoft credential already
  exists and is already consented for mail: ADR-018 made MICROSOFT_SCOPES a UNION grant carrying
  Calendars.ReadWrite, Mail.Send and Mail.Read together, so there is no scope work here either.
  schema.ts states it directly -- 'Phase 25-06 CONSUMES this row for Outlook; it must not mint a
  second.' (3) `graph.ts` MUST NOT re-implement token refresh: `microsoftCalendar.ts` already
  exports `freshGraphToken`, which is the refresh root for this row. Reuse it; a second refresh path
  over one row is a token-rotation race. (4) Read per-half readiness with
  `microsoftCalendarReady`/`microsoftMailReady` from @pikar/core, NEVER a bare `connected` boolean.

  WHAT SURVIVES UNCHANGED: the `mailProvider` field on saved plan/request state (still needed to
  pick an arm), `graph.ts` as the Graph SEND adapter, `delivery.ts` as the two-arm dispatcher, and
  moving BOTH production callers onto it in the same commit. That was always the real work."

must_haves:
  truths:
    - "`gmailTokens` is NOT modified: no provider column, no by_tenant_provider index, no migration. A test asserts the table's shape is unchanged, so a future widening fails loudly instead of silently reintroducing the ambiguity schema.ts warns about"
    - "The mailProvider plan/request field, the Graph send adapter, and the two-arm delivery switch land in the same production-code commit; no one-provider abstraction commit exists"
    - "graph.ts obtains its access token ONLY via microsoftCalendar.freshGraphToken; a grep proves no second refresh path writes microsoftCalendarTokens"
    - "Both production send callers route through the provider switch, while notifyExternal deliberately remains Gmail/service-notice specific"
    - "Graph send reuses the exact MIME builder, enforces its lower provider cap, applies suppression/footer guards before credential mint, and classifies transient versus terminal failures"
    - "Connect-both coexistence is structural — the two grants live in two tenant-keyed tables — so selecting one provider for a send cannot overwrite the other, and no backfill is required to make that true"
    - "A send routed to Microsoft refuses with a clear reason when microsoftMailReady(scope) is false, rather than attempting Graph with a calendar-only grant"
  artifacts:
    - path: "packages/backend/convex/graph.ts"
      provides: "Graph MIME send over the EXISTING microsoftCalendarTokens row, reusing freshGraphToken — send only, no refresh, no token write"
    - path: "packages/backend/convex/delivery.ts"
      provides: "Minimal two-arm provider dispatcher"
    - path: "packages/backend/convex/delivery.test.ts"
      provides: "Routing, the mailReady refusal, and zero remaining non-test internal.gmail.send scan"
  key_links:
    - from: "packages/backend/convex/delivery.ts"
      to: "packages/backend/convex/graph.ts"
      via: "request.mailProvider switch with two arms from first commit"
      pattern: "internal.graph.send"
    - from: "packages/backend/convex/deliverApprovedPlan.ts"
      to: "packages/backend/convex/delivery.ts"
      via: "workflow retries the provider dispatcher rather than Gmail directly"
      pattern: "internal.delivery.send"
    - from: "packages/backend/convex/pipeline.ts"
      to: "packages/backend/convex/delivery.ts"
      via: "the second production send caller moves in the same commit"
      pattern: "internal.delivery.send"
---

<objective>
Add the Microsoft send provider and the smallest two-provider routing seam in one atomic code change,
over the ALREADY-CONSENTED shared Microsoft grant. No token-table migration exists or is needed:
17-05 landed `microsoftCalendarTokens` as a separate tenant-keyed table, so the two providers already
coexist structurally.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-00-SUMMARY.md
@CLAUDE.md
@packages/backend/convex/schema.ts
@packages/backend/convex/microsoftCalendar.ts
@packages/core/src/microsoft.ts
@docs/decisions/018-one-microsoft-connection-not-two.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive red contracts to one atomic mailProvider, Graph-send, and dispatcher commit</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/gmail.ts, packages/backend/convex/graph.ts, packages/backend/convex/graph.test.ts, packages/backend/convex/delivery.ts, packages/backend/convex/delivery.test.ts, packages/backend/convex/deliverApprovedPlan.ts, packages/backend/convex/pipeline.ts, packages/backend/convex/cockpit.ts, docs/playbooks/cockpit.md, docs/playbooks/watch.json</files>
  <action>
    Within this one task, write and run the two new focused test files first. Pin MIME byte parity,
    Graph suppression/footer-before-credential ordering, provider cap, empty-202 success, 4xx/5xx
    taxonomy, Google/Microsoft/legacy routing, both production callers, the microsoftMailReady
    refusal on a calendar-only grant, and same-tenant token coexistence across the two tables. Also
    pin that `gmailTokens`' definition is unchanged. Do NOT test refresh rotation or a backfill here
    — rotation belongs to `freshGraphToken` and its existing `microsoftCalendar.test.ts` coverage,
    and there is no backfill. Confirm their failures are
    the intended missing behaviors, then continue immediately to implementation; do not commit or
    end the task red. Leave existing gmail.test.ts and cockpit.test.ts unchanged and rely on their
    full focused/full-wave regression coverage below.

    This task's production files MUST be one path-scoped commit. In that single commit: add optional
    `mailProvider` to saved plan/request state with existing-row Google fallback; make every new plan
    and request write it explicitly; add `graph.ts`; add `delivery.ts` with both arms; and move BOTH
    deliverApprovedPlan and pipeline. Do not land delivery.ts alone in an earlier commit.

    DO NOT TOUCH `gmailTokens`. There is no provider column, no `by_tenant_provider` index and no
    migration in this plan — 17-05 already landed `microsoftCalendarTokens` as a separate
    tenant-keyed table, and schema.ts records why a discriminator column is the wrong shape (it
    would make every existing `by_tenant` `.unique()` read ambiguous). The only schema edit here is
    `mailProvider` on plan/request state. If you find yourself writing a backfill, stop: coexistence
    is already structural.

    `graph.ts` IS A SEND ADAPTER ONLY. Get the access token from
    `internal.microsoftCalendar.freshGraphToken` — the documented refresh root over that row — and
    do not refresh, rotate or write `microsoftCalendarTokens` from here; a second refresh path over
    one row is a token-rotation race. Before sending, check `microsoftMailReady(scope)` from
    @pikar/core and refuse with a named reason if false, so a calendar-only legacy grant fails
    honestly instead of erroring inside Graph.

    Reuse buildMime exactly and keep CAN-SPAM footer concatenation at each provider send call site.
    Graph uses standard base64 and `/me/sendMail`, returns success without fabricating an id, and
    refuses its provider-specific size ceiling before fetch. Keep the `gmailTokens` and
    `microsoftCalendarTokens` table names with a ponytail comment — both are deliberately narrower
    than their contents and renaming a Convex table is a migration for cosmetic gain. Leave
    notifyExternal on its current service-notice path and document why. Register graph/delivery paths
    under cockpit.md and bump the playbook in this same commit.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/graph.test.ts convex/delivery.test.ts convex/gmail.test.ts convex/cockpit.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The first commit containing the adapter already has Google and Microsoft arms, legacy rows remain readable, both callers route through it, and all focused tests are green.</done>
</task>

</tasks>

<verification>
- Inspect the path-scoped production commit and prove mailProvider + graph.ts + delivery.ts + both caller rewrites coexist
- Prove `gmailTokens` is byte-unchanged in that commit (`git show <sha> -- packages/backend/convex/schema.ts` must show no gmailTokens hunk)
- `pnpm --filter @pikar/backend exec vitest run convex/graph.test.ts convex/delivery.test.ts convex/gmail.test.ts convex/cockpit.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- `pnpm test`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Two providers exist from the adapter's first commit
- Existing Gmail connections are untouched: no provider column, no index, no backfill
- Both delivery callers route by request provider
- Graph send preserves all governance guards and error semantics
- Same-tenant Google and Microsoft credentials coexist
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-05-SUMMARY.md`.</output>
