---
phase: 25-private-beta-productionization
plan: 05
type: execute
wave: 5
depends_on: [25-02, 25-03, 25-04]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/migrations.ts
  - packages/backend/convex/migrations.test.ts
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/graph.ts
  - packages/backend/convex/graph.test.ts
  - packages/backend/convex/delivery.ts
  - packages/backend/convex/delivery.test.ts
  - packages/backend/convex/deliverApprovedPlan.ts
  - packages/backend/convex/pipeline.ts
  - packages/backend/convex/cockpit.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [DLVR-02]

must_haves:
  truths:
    - "The provider field/index widening, Microsoft Graph implementation, and two-arm delivery switch land in the same production-code commit; no one-provider abstraction commit exists"
    - "Existing Gmail token and request rows remain readable during the migration window and every new row writes an explicit provider"
    - "Both production send callers route through the provider switch, while notifyExternal deliberately remains Gmail/service-notice specific"
    - "Graph send reuses the exact MIME builder, enforces its lower provider cap, applies suppression/footer guards before credential mint, persists refresh rotation, and classifies transient versus terminal failures"
    - "Connect-both data coexistence is possible because tokens are keyed by tenant and provider; selecting one provider for a send never overwrites the other"
  artifacts:
    - path: "packages/backend/convex/graph.ts"
      provides: "Microsoft token refresh and Graph MIME send implementation"
    - path: "packages/backend/convex/delivery.ts"
      provides: "Minimal two-arm provider dispatcher"
    - path: "packages/backend/convex/migrations.ts"
      provides: "Tracked resumable Gmail-token provider backfill"
    - path: "packages/backend/convex/delivery.test.ts"
      provides: "Routing and zero remaining non-test internal.gmail.send scan"
  key_links:
    - from: "packages/backend/convex/delivery.ts"
      to: "packages/backend/convex/graph.ts"
      via: "request.mailProvider switch with two arms from first commit"
      pattern: "internal.graph.send"
    - from: "packages/backend/convex/deliverApprovedPlan.ts"
      to: "packages/backend/convex/delivery.ts"
      via: "workflow retries the provider dispatcher rather than Gmail directly"
      pattern: "internal.delivery.send"
    - from: "packages/backend/convex/pipeline.ts"
      to: "packages/backend/convex/delivery.ts"
      via: "the second production send caller moves in the same commit"
      pattern: "internal.delivery.send"
---

<objective>
Add the Microsoft send provider and the smallest two-provider routing seam in one atomic code change,
using a safe optional-field migration window so the owner's existing Gmail connection cannot vanish.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-00-SUMMARY.md
@CLAUDE.md
@packages/backend/.agents/skills/convex-migration-helper/SKILL.md
@packages/backend/.agents/skills/convex-migration-helper/references/migrations-component.md
@packages/backend/.agents/skills/convex-migration-helper/references/migration-patterns.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive red contracts to one atomic optional-widening, Graph, and adapter commit</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/migrations.ts, packages/backend/convex/migrations.test.ts, packages/backend/convex/gmail.ts, packages/backend/convex/graph.ts, packages/backend/convex/graph.test.ts, packages/backend/convex/delivery.ts, packages/backend/convex/delivery.test.ts, packages/backend/convex/deliverApprovedPlan.ts, packages/backend/convex/pipeline.ts, packages/backend/convex/cockpit.ts, docs/playbooks/cockpit.md, docs/playbooks/watch.json</files>
  <action>
    Within this one task, write and run the three new focused test files first. Pin MIME byte parity,
    Graph suppression/footer-before-credential ordering, provider cap, empty-202 success, 4xx/5xx
    taxonomy, refresh-token rotation, Google/Microsoft/legacy routing, both production callers,
    idempotent legacy-Google backfill, and same-tenant token coexistence. Confirm their failures are
    the intended missing behaviors, then continue immediately to implementation; do not commit or
    end the task red. Leave existing gmail.test.ts and cockpit.test.ts unchanged and rely on their
    full focused/full-wave regression coverage below.

    This task's production files MUST be one path-scoped commit. In that single commit: widen
    `gmailTokens.provider` to optional `google|microsoft`, add `by_tenant_provider`; add optional
    mailProvider to saved plan/request state with existing-row Google fallback; make every new token,
    plan, and request write explicit; define the tracked @convex-dev/migrations backfill; add
    `graph.ts`; add `delivery.ts` with both arms; and move BOTH deliverApprovedPlan and pipeline.
    Do not land delivery.ts or the schema widening alone in an earlier commit.

    Reuse buildMime exactly and keep CAN-SPAM footer concatenation at each provider send call site.
    Graph uses standard base64 and `/me/sendMail`, returns success without fabricating an id, refuses
    its provider-specific size ceiling before fetch, and persists rotated refresh tokens. Keep
    `gmailTokens` table name with a ponytail comment; renaming is a needless data migration. Leave
    notifyExternal on its current service-notice path and document why. Register graph/delivery paths
    under cockpit.md and bump the playbook in this same commit.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/graph.test.ts convex/delivery.test.ts convex/migrations.test.ts convex/gmail.test.ts convex/cockpit.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The first commit containing the adapter already has Google and Microsoft arms, legacy rows remain readable, both callers route through it, and all focused tests are green.</done>
</task>

</tasks>

<verification>
- Inspect the path-scoped production commit and prove schema widening + graph.ts + delivery.ts + both caller rewrites coexist
- `pnpm --filter @pikar/backend exec vitest run convex/graph.test.ts convex/delivery.test.ts convex/migrations.test.ts convex/gmail.test.ts convex/cockpit.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- `pnpm test`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- Two providers exist from the adapter's first commit
- Existing Gmail connections survive the optional-field migration window
- Both delivery callers route by request provider
- Graph send preserves all governance guards and error semantics
- Same-tenant Google and Microsoft credentials coexist
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-05-SUMMARY.md`.</output>
