---
phase: 25-private-beta-productionization
plan: 07
type: execute
wave: 7
depends_on: [25-06]
files_modified:
  - docs/playbooks/cockpit.md
  - .planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [DLVR-02]
amended: "2026-08-14 by ADR-018. RE-CUT 2026-08-15 at owner direction, against the LANDED code.
  NOTE ON AUTHORITY: 25-PREREQUISITE-EVIDENCE.md lists 'downstream plan reconciliation' among the
  things prohibited before Task 1 approval; the owner lifted that for this re-cut explicitly.

  THIS PLAN WAS ~2/3 OBSOLETE AND IS NOW REBUILT AROUND THE THIRD. Its original Tasks 1 and 2 --
  dry-run and execute a tracked `gmailTokens.provider` backfill, then narrow that column to required
  and strip the undefined=>google fallbacks -- describe work on a column THAT WILL NEVER EXIST.
  Plan 17-05 landed `microsoftCalendarTokens` as a SEPARATE tenant-keyed table and schema.ts records
  why a discriminator on `gmailTokens` is the wrong shape (it would make every existing `by_tenant`
  `.unique()` read ambiguous). 25-05 is re-cut to match, so there is no widening, no backfill, no
  narrowing and no fallback to remove. Tasks 1 and 2 are DELETED, not deferred -- there is nothing
  left for them to act on. `migrations.ts`/`migrations.test.ts` leave files_modified.

  WHAT SURVIVES, AND WHY IT MATTERS MORE THAN BEFORE: the original Task 3, live Gmail continuity.
  25-05 moves BOTH production send callers off `internal.gmail.send` onto `internal.delivery.send`.
  That is a real regression surface on the one delivery path the product already depends on, and
  no offline test proves a live Gmail send still works through a new dispatcher. The gate is now
  ABOUT the dispatcher rather than about a migration, so pre-25-05 evidence is not reusable.

  SCOPE BOUNDARY, so this does not duplicate its neighbours: 25-08 owns Outlook reply THREADING and
  25-09 owns full read-plane parity plus real personal/work posture. This plan proves only the two
  things that must hold the moment the dispatcher lands -- Gmail did not regress, and the Microsoft
  arm can complete ONE governed send end to end."

must_haves:
  truths:
    - "A real Gmail send and a bounded real Gmail read both succeed on the deployed dispatcher SHA, proving the move from internal.gmail.send to internal.delivery.send did not regress the existing delivery path"
    - "One governed Outlook send completes end to end over the shared 17-06 grant, proving the Graph arm works against a real mailbox rather than only against a mocked fetch"
    - "The Google grant and the Microsoft grant are both intact after the run — neither send path wrote, rotated or deleted the other's token row"
    - "Evidence is refs, ids, hashes, counts and timestamps only; no message bodies, addresses or token material reach the evidence file (CLAUDE.md §4)"
    - "Pre-dispatcher evidence is explicitly invalid and is not reused: every observation is timestamped against the recorded SHA"
  artifacts:
    - path: ".planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md"
      provides: "Deployed SHA/deployment id, the Gmail read+send observations, the Outlook send observation, and the both-grants-intact readback"
  key_links:
    - from: ".planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md"
      to: "packages/backend/convex/delivery.ts"
      via: "live evidence is recorded against the dispatcher commit, not against a migration"
      pattern: "internal.delivery.send"
---

<objective>
Prove, live and on one recorded SHA, that routing both send callers through the new two-arm
dispatcher did not regress Gmail, and that the Microsoft arm can complete one governed send over the
single shared grant.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-05-SUMMARY.md
@.planning/phases/25-private-beta-productionization/25-06-SUMMARY.md
@CLAUDE.md
@docs/decisions/018-one-microsoft-connection-not-two.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gmail continuity on the deployed dispatcher SHA</name>
  <files>.planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md, docs/playbooks/cockpit.md</files>
  <action>
    Deploy the 25-05/25-06 code and record the exact SHA and deployment id FIRST, so every
    observation below is anchored to it. Then, against the owner's existing Google connection:
    refresh status, perform ONE bounded real Gmail read and ONE governed self-send through the
    cockpit's normal approve path — not by calling an internal function directly, since the point is
    that the PRODUCTION caller still reaches Gmail through `internal.delivery.send`.

    Confirm the send is attributed to the google arm. Then read back BOTH token rows and confirm the
    Microsoft row's `updatedAt` and scope are unchanged — a Gmail send must not touch
    `microsoftCalendarTokens`.

    Record commands, ids, counts, SHA, deployment id and timestamps. Refs and hashes only: no
    message bodies, no recipient addresses, no token material. Pre-dispatcher evidence is invalid
    and must not be copied forward.
  </action>
  <verify><manual>Owner confirms a real Gmail read and a real governed send both succeeded on the recorded SHA, and that the Microsoft grant is untouched.</manual></verify>
  <done>The pre-existing Gmail delivery path is proven intact behind the new dispatcher.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: One governed Outlook send over the shared grant</name>
  <files>.planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md, docs/playbooks/cockpit.md</files>
  <action>
    On the SAME recorded SHA, select Microsoft for one governed self-send through the normal approve
    path. Before sending, record what `microsoftMailReady(scope)` reports for the grant in use: if it
    is false the grant predates the union scope set and the correct outcome is the re-consent path
    from 25-06, NOT a Graph attempt — record that as the observed result rather than forcing a send.

    On success, record the Graph response shape actually observed (a 202 with an empty body is
    success; do not fabricate a message id the API did not return). Then read back BOTH token rows
    again and confirm the GOOGLE row is unchanged.

    If the send fails, record the classification the dispatcher assigned (transient vs terminal) and
    stop — do not retry blind. A first live Graph send failing is information, and 25-08/25-09 depend
    on knowing which half broke.
  </action>
  <verify><manual>Owner confirms either a completed Outlook send, or a correctly-refused send with mailReady false and the re-consent path offered.</manual></verify>
  <done>The Microsoft arm is proven against a real mailbox, or is proven to refuse honestly when the grant lacks mail scope.</done>
</task>

</tasks>

<verification>
- The evidence file records ONE SHA and one deployment id, and every observation is stamped against it
- Gmail: bounded real read + governed real send, both through the production caller
- Outlook: one governed send, or a recorded mailReady-false refusal with re-consent offered
- Both token rows read back intact after each arm ran
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- The dispatcher rewrite is proven not to have regressed live Gmail delivery
- The Microsoft send arm is proven live, or proven to refuse honestly on a calendar-only grant
- Neither provider's credential row was written by the other provider's send
- The evidence file carries refs/hashes/counts only, per CLAUDE.md §4
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-07-SUMMARY.md`.</output>
