---
phase: 25-private-beta-productionization
plan: 07
type: execute
wave: 7
depends_on: [25-06]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/migrations.ts
  - packages/backend/convex/migrations.test.ts
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/gmailAuth.ts
  - docs/playbooks/cockpit.md
  - .planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [DLVR-02]
amended: "2026-08-14 by ADR-018. The provider schema this plan narrows was widened ONCE, by 17-06,
  not twice. There is no duplicate Microsoft token row, callback or consent surface to reconcile --
  that reconciliation was the cost this ADR removed before either plan ran. Narrow against the single
  shared Microsoft connection."

must_haves:
  truths:
    - "The hosted migration checkpoint changes remote data only and verifies zero undefined provider rows"
    - "Only after migration approval does an automatic task require provider, remove temporary token fallbacks, test, and deploy an exact clean SHA"
    - "A fresh live Gmail read/send proves continuity after the narrowed deployment; pre-narrow evidence is not reusable"
  artifacts:
    - path: ".planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md"
      provides: "Migration-only checkpoint, narrow-deploy SHA, and post-deploy Gmail evidence"
  key_links:
    - from: "packages/backend/convex/migrations.ts"
      to: "packages/backend/convex/schema.ts"
      via: "tracked backfill completes before provider becomes required"
      pattern: "provider"
---

<objective>
Migrate hosted legacy Gmail tokens, narrow the provider schema/fallbacks in a separate automatic
deployment, and freshly prove Gmail continuity on that narrowed SHA.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-05-SUMMARY.md
@.planning/phases/25-private-beta-productionization/25-06-SUMMARY.md
@CLAUDE.md
@packages/backend/.agents/skills/convex-migration-helper/SKILL.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Dry-run, execute, and verify the hosted token data migration</name>
  <files>.planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md, docs/playbooks/cockpit.md</files>
  <action>
    Deploy Plan 05's optional-schema/dual-read code. Run the tracked provider migration with
    `dryRun:true`, inspect its sample/count, run it normally, monitor component status, and use a
    bounded query to prove zero `gmailTokens.provider === undefined` rows. Record commands, counts,
    deployment id, SHA, and timestamps. Never use `.collect()` or an ad-hoc mutation. This checkpoint
    performs remote migration/evidence only and MUST NOT edit schema, fallbacks, tests, or deploy code.
  </action>
  <verify><manual>Owner confirms dry-run output, completed migration status, and zero legacy token rows before authorizing narrowing.</manual></verify>
  <done>The hosted data postcondition is approved while source remains on the optional schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow provider schema/fallbacks and deploy the green exact commit</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/migrations.ts, packages/backend/convex/migrations.test.ts, packages/backend/convex/gmail.ts, packages/backend/convex/gmailAuth.ts, docs/playbooks/cockpit.md, .planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md</files>
  <action>
    Only after Task 1 approval, require `gmailTokens.provider` and remove the temporary
    undefined=>google token-row fallbacks found by Plan 00. Preserve the intentional fallback for old
    plan/request `mailProvider` rows unless separately migrated. Retain migration history and update
    its tests. Run the focused suite first, then typecheck/full tests, deploy the exact clean commit,
    and record SHA/deployment id. Do not fold live Gmail observation into this task.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/migrations.test.ts convex/gmailAuth.test.ts convex/gmail.test.ts convex/delivery.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
    <automated>pnpm test</automated>
  </verify>
  <done>The required-provider schema and fallback removal are green and deployed from the recorded SHA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real Gmail after the narrowed deployment</name>
  <files>.planning/phases/25-private-beta-productionization/25-MAIL-MIGRATION-EVIDENCE.md, docs/playbooks/cockpit.md</files>
  <action>
    Against Task 2's deployed SHA, refresh the owner's existing Gmail status, perform one bounded real
    Gmail read and one governed self-send, confirm provider=`google`, and prove the Microsoft row is
    unchanged. Record timestamps, deployment id/SHA, and hashed refs. Pre-narrow evidence is invalid.
  </action>
  <verify><manual>Owner confirms Gmail reads and sends on the narrowed SHA with Microsoft unchanged.</manual></verify>
  <done>The final required-provider deployment has fresh Gmail continuity evidence.</done>
</task>

</tasks>

<verification>
- Migration-only checkpoint precedes source narrowing
- Focused migration/Gmail/auth/delivery suite, typecheck, then full tests
- Exact-SHA narrow deployment followed by fresh live Gmail proof
</verification>

<success_criteria>
- Hosted legacy rows are migrated before provider becomes required
- Narrowing/fallback removal is automatic, tested, and separately deployed
- Gmail remains live after the narrow deploy
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-07-SUMMARY.md`.</output>
