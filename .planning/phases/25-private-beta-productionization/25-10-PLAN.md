---
phase: 25-private-beta-productionization
plan: 10
type: execute
wave: 10
depends_on: [25-02, 25-09]
files_modified:
  - packages/backend/convex/lib/env.ts
  - packages/backend/convex/env.test.ts
  - packages/backend/convex/ops.ts
  - apps/web/app/(app)/admin/AdminView.tsx
  - docs/playbooks/production-beta.md
  - docs/playbooks/watch.json
  - docs/decisions/017-beta-domain-posture.md
  - .planning/phases/25-private-beta-productionization/25-DEPLOY-EVIDENCE.md
autonomous: false
requirements: [BETA-01, DLVR-02]

must_haves:
  truths:
    - "One runtime manifest covers every load-bearing hosted key and owner-only envCheck exposes missing names, never values"
    - "Phase completion requires Branch A: durable custom app/API/HTTP origins with DNS and TLS; choosing Branch B records the decision and BLOCKS Phase 25"
    - "No beta deployment or mandatory self/Outlook send proceeds from an ephemeral user-shareable URL"
  artifacts:
    - path: "packages/backend/convex/lib/env.ts"
      provides: "Canonical hosted environment manifest and durable-origin validation"
    - path: "docs/decisions/017-beta-domain-posture.md"
      provides: "Immutable A/B decision whose B outcome explicitly blocks the phase"
  key_links:
    - from: "apps/web/app/(app)/admin/AdminView.tsx"
      to: "packages/backend/convex/ops.ts"
      via: "owner readiness shows missing key names without values"
      pattern: "envCheck"
---

<objective>
Build hosted readiness visibility, decide the domain posture, and automatically enforce the only
completion-capable outcome: durable Branch-A origins. Branch B is a recorded phase blocker.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-private-beta-productionization/25-CONTEXT.md
@.planning/phases/25-private-beta-productionization/25-RESEARCH.md
@.planning/phases/25-private-beta-productionization/25-VALIDATION.md
@.planning/phases/25-private-beta-productionization/25-09-SUMMARY.md
@README.md
@CLAUDE.md
@docs/design/BRAND.md
@docs/playbooks/ci-gate.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build one fail-closed hosted environment manifest and owner status</name>
  <files>packages/backend/convex/lib/env.ts, packages/backend/convex/env.test.ts, packages/backend/convex/ops.ts, apps/web/app/(app)/admin/AdminView.tsx, docs/playbooks/production-beta.md, docs/playbooks/watch.json</files>
  <action>
    Re-run Plan 00's source/env inventory. Cover final process.env consumers, Convex Auth implicit
    keys, both sign-in/mailbox OAuth pairs, unsubscribe, skillopt, model/research/media/WORM keys, and
    later additions. Keep one array/filter; never throw at module load. envCheck is owner-only and
    returns `{missing:string[]}` without values. Register production-beta.md and seed invariants.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/env.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The source-complete manifest and owner UI fail visibly without exposing values.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide durable-domain posture and block on Branch B</name>
  <files>docs/decisions/017-beta-domain-posture.md, .planning/phases/25-private-beta-productionization/25-DEPLOY-EVIDENCE.md</files>
  <action>
    Present RESEARCH's domain facts and current Convex plan support. Owner chooses exactly one:
    (A) durable app/API/HTTP-action hostnames with DNS/TLS and stable OAuth/unsubscribe origins; or
    (B) no user-shareable URL ships until those exist. Write ADR 017. Branch B is NOT a completion
    path because disabling governed external sends would make mandatory BETA-03 self-send and DLVR-02
    Outlook send impossible: record the blocker and STOP Phase 25 before Task 3/Plan 11. Only Branch A
    may be approved to continue. “Decide later” and ephemeral deployment URLs are forbidden.
  </action>
  <verify><manual>Owner approves Branch A, or acknowledges Branch B leaves Phase 25 blocked with no deployment/live-send completion claim.</manual></verify>
  <done>Branch A releases Task 3; Branch B records an explicit unresolved phase blocker and stops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the selected durable origins before production deployment</name>
  <files>packages/backend/convex/lib/env.ts, packages/backend/convex/env.test.ts, packages/backend/convex/ops.ts, apps/web/app/(app)/admin/AdminView.tsx, docs/playbooks/production-beta.md, .planning/phases/25-private-beta-productionization/25-DEPLOY-EVIDENCE.md</files>
  <action>
    Require an approved Branch-A ADR. Add narrow configuration/origin validation proving every
    generated user-shareable URL and both OAuth redirect sets derive from the named durable
    SITE_URL/CONVEX_SITE_URL. Surface invalid/non-durable origin readiness without values. If ADR 017
    says B or validation cannot prove the named origins, fail without source/deploy progression.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/env.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Only the named durable Branch-A origin configuration can report deployment-ready.</done>
</task>

</tasks>

<verification>
- Focused env/readiness tests precede build/typecheck/playbook checks
- Blocking A/B decision; Branch B stops Phase 25
- Branch-A durable-origin validation must pass before Plan 11
</verification>

<success_criteria>
- Hosted readiness exposes missing names only
- Durable domain/DNS/TLS is mandatory for phase completion
- No ephemeral user-shareable URL can reach production qualification
</success_criteria>

<output>After completion, create `.planning/phases/25-private-beta-productionization/25-10-SUMMARY.md`.</output>
