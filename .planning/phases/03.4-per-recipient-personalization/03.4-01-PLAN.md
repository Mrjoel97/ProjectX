---
phase: 03.4-per-recipient-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/plans.test.ts
  - docs/playbooks/cockpit.md
requirements: [CKPT-03]
autonomous: true
must_haves:
  truths:
    - "A tailored body can be stored per recipient, keyed by lowercased address, on the plan row"
    - "Writing an override never clobbers other filled slots (drop-undefined patch discipline holds)"
    - "recipientBodies lives only on the content-plane plans row — never in audit/DLQ/telemetry"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "plans.recipientBodies optional field (address -> tailored body), no migration"
      contains: "recipientBodies"
    - path: "packages/backend/convex/plans.ts"
      provides: "patchPlan gains recipientBodies arg (drop-undefined pattern already present)"
      contains: "recipientBodies"
    - path: "packages/backend/convex/plans.test.ts"
      provides: "coverage: patchPlan writes recipientBodies keyed by address; partial patch leaves it untouched"
  key_links:
    - from: "packages/backend/convex/plans.ts patchPlan"
      to: "plans.recipientBodies column"
      via: "Object.fromEntries drop-undefined patch"
      pattern: "recipientBodies"
---

<objective>
Add the single content-plane field that every later wave consumes: `plans.recipientBodies`,
an optional map keyed by lowercased recipient ADDRESS whose value is that recipient's tailored
body string. This is an append-only optional field (NO migration — exactly how
`attachments`/`candidates`/`greetingName` were added, schema.ts:150-185). Extend `patchPlan`
so the personalization tool (Wave 2) and the seed loop (Wave 3) have a write/read surface.

Purpose: Establish the foundation contract. Personalization is the OPPOSITE of the 3.3
attachment fan-out — each recipient carries a DISTINCT body while sharing subject/mode/attachments.
Keying by address (not index) survives mid-conversation recipient edits (Pitfall 2) and orphaned
keys filter harmlessly at seed time.
Output: schema field + patchPlan arg + plans.test.ts coverage. NO tools/seed/UI (later waves).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-per-recipient-personalization/03.4-RESEARCH.md
@.planning/phases/03.4-per-recipient-personalization/03.4-VALIDATION.md
@docs/playbooks/cockpit.md

<interfaces>
<!-- The append-only optional-field precedent this task mirrors. Executor: match this shape. -->

plans table block (schema.ts:150-188) already carries these optional additive fields:
```typescript
attachments: v.optional(v.array(v.object({ storageId, filename, mimeType, size }))),
attachmentError: v.optional(v.string()),
candidates: v.optional(v.array(...)),
pendingValid: v.optional(v.array(v.string())),
greetingName: v.optional(v.string()),
```

patchPlan (plans.ts:71-87) — the drop-undefined writer to extend:
```typescript
export const patchPlan = internalMutation({
  args: { planId: v.id("plans"), recipients, mode, subject, bodyIntent, body, status, greetingName },
  handler: async (ctx, { planId, ...patch }) => {
    const fields = Object.fromEntries(Object.entries(patch).filter(([, val]) => val !== undefined));
    await ctx.db.patch(planId, fields);
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add plans.recipientBodies field + patchPlan arg</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/plans.ts, packages/backend/convex/plans.test.ts</files>
  <behavior>
    - patchPlan({ planId, recipientBodies: { "bob@x.com": "Hi Bob..." } }) writes the map to the row; byThread/getById reads it back verbatim.
    - patchPlan({ planId, subject: "S" }) with NO recipientBodies leaves an existing recipientBodies map untouched (drop-undefined).
    - The map is keyed by lowercased address; a second patch replaces the whole map wholesale (the tool passes the full merged map — see Wave 2).
  </behavior>
  <action>
    In schema.ts, inside the EXISTING `plans` table block (append after `greetingName`, schema.ts:185 — own additive line, never reorder others'):
    `recipientBodies: v.optional(v.record(v.string(), v.string())), // address(lowercased) -> tailored body; overrides only, missing = shared body (no migration, content-plane only §4)`

    CONFIRM `v.record(v.string(), v.string())` is accepted by the pinned Convex version:
    run `pnpm --filter @pikar/backend exec convex codegen` (or observe `npx convex dev` codegen) — if `v.record` is rejected, FALL BACK to
    `v.array(v.object({ address: v.string(), body: v.string() }))` and adjust the Wave-2/Wave-3 lookup to `.find(e => e.address === addr)?.body`. Record which shape you used in the SUMMARY (the record shape is standard current Convex and strongly preferred — one lookup, no `.find`).

    In plans.ts `patchPlan` args (plans.ts:72-81), add one optional arg mirroring the field:
    `recipientBodies: v.optional(v.record(v.string(), v.string())),`
    The handler is UNCHANGED — the existing `Object.fromEntries(...filter(v !== undefined))` drop-undefined pattern (plans.ts:83-84) already handles the new key correctly (a partial patch without it leaves the stored map intact).

    Do NOT add a new mutation — `patchPlan` IS the writer (ponytail rung 2: reuse the drop-undefined surface; the tool spreads `{...existing, [address]: body}` and passes the full map). Do NOT touch send/tools/UI.

    Add the plans.test.ts cases described in <behavior> (extend the existing suite; seed a plan via insertPlan, patch recipientBodies, assert via getById/byThread; a second subject-only patch preserves the map).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test plans</automated>
  </verify>
  <done>plans.recipientBodies exists (or documented array fallback); patchPlan accepts + persists it; partial patch preserves it; plans.test.ts green; backend typecheck clean (the 1 pre-existing audit.test.ts red is NOT a regression).</done>
</task>

<task type="auto">
  <name>Task 2: §9 playbook bump — recipientBodies content-plane note</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
    plans.ts + schema.ts are watch-protected under cockpit.md, so this turn's code change requires the playbook be touched in the same phase (CLAUDE.md §9; else the Stop hook blocks).
    Bump the `> Last verified:` line to `03.4-01` and add a one-line note under the plans.ts Key-files entry: `recipientBodies` is an optional address-keyed map (per-recipient body override, no migration) — content-plane only, NEVER audited (§4), written via `patchPlan`. Keep the edit minimal and additive; the full personalization invariant lands in the phase-close plan (Wave 4).
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>cockpit.md Last verified = 03.4-01; recipientBodies noted; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test plans` green (schema field + patchPlan write/read/preserve).
- Backend typecheck clean; `v.record` accepted (or array fallback documented).
- `node scripts/check-playbooks.mjs` exits 0.
</verification>

<success_criteria>
plans.recipientBodies (address -> body, optional, no migration) exists and is written/read via patchPlan; a partial patch never clobbers it; the field is content-plane only. Foundation ready for Wave 2 (tool) and Wave 3 (seed).
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-per-recipient-personalization/03.4-01-SUMMARY.md`.
</output>
