---
phase: 03.4-per-recipient-personalization
verified: 2026-07-14T15:36:54Z
status: passed
score: 5/5 must-haves verified
---

# Phase 03.4: Per-Recipient Personalization Verification Report

**Phase Goal:** A multi-recipient send can tailor wording per recipient, moving beyond
slice-1 same-content-to-all while keeping the single plan-approval gate.
**Verified:** 2026-07-14T15:36:54Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A tailored body can be stored per recipient, keyed by address, on the plan row | ✓ VERIFIED | `schema.ts:190` `recipientBodies: v.optional(v.record(v.string(), v.string()))`; `plans.ts:81` `patchPlan` accepts the same arg; `plans.test.ts` 7/7 green (write/read-back, partial-patch preserves map, wholesale replace) |
| 2 | The agent can tailor ONE recipient's wording by #index, address resolved server-side, reusing draftBody's exact redact-then-draft path (guardrail parity, SC2) | ✓ VERIFIED | `llm.ts:750` `personalizeRecipient` tool: resolves `recipients[index-1]` server-side (§2-D), `scanText` fail-closed before `draftCockpit` (llm.ts:786-788), merge-spread into `recipientBodies[address]` via `patchPlan`; `cockpitTools.test.ts` 24/24 green (index-bounce no-write, merge preserves prior overrides, scan-before-draft) |
| 3 | proposePlan refuses group-mode + personalization (personalization requires individual mode) | ✓ VERIFIED | `llm.ts:909-911` `hasPersonalization && plan.mode === "group"` refusal, facts-from-the-row; covered in `cockpitTools.test.ts` |
| 4 | On Approve, each recipient's `requests.draft` carries its distinct tailored body (`recipientBodies[recipient] ?? body`); shared subject; single `workflow.start` intact | ✓ VERIFIED | `cockpit.ts:257` seed line; `cockpit.ts:268` sole `workflow.start(` call site (grep-verified, only one); `cockpit.test.ts` distinct-draft case green |
| 5 | The PLAN card shows each recipient's tailored/shared body before the single Approve (SC1) | ✓ VERIFIED | `cards.tsx:159-189` `PlanRecipientBodies` (mirrors `PlanAttachments`, rendered at cards.tsx:228 before Approve); renders "tailored"/"shared body" tag per recipient |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/backend/convex/schema.ts` | `plans.recipientBodies` optional field | ✓ VERIFIED | line 190, additive, no migration |
| `packages/backend/convex/plans.ts` | `patchPlan` recipientBodies arg | ✓ VERIFIED | line 81, drop-undefined pattern unchanged |
| `packages/backend/convex/llm.ts` | `personalizeRecipient` tool + `buildAgentContext` surfacing + `proposePlan` gate + SMOKE op | ✓ VERIFIED | tool at 750, context surfacing 434/453/458/466, gate 909-911, SMOKE `personalize` op at 1103 |
| `packages/backend/convex/cockpit.ts` | executePlan seed override | ✓ VERIFIED | line 257 `draft: (plan.recipientBodies ?? {})[recipient] ?? body`; single `workflow.start` at line 268 |
| `apps/web/app/(app)/dashboard/workspace/cards.tsx` | `PlanRecipientBodies` PLAN-card section | ✓ VERIFIED | lines 159-189, wired into `PlanCard` at line 228 |
| `packages/contracts/skills/cockpit-agent.md` + derived `cockpitAgent.ts` | Personalization section, no hardcoded prompt (§5) | ✓ VERIFIED | skills.test.ts 12/12 green (drift test passes) |
| `apps/web/e2e/cockpit-personalize.spec.ts` | offline E2E, 2 recipients, distinct bodies pre-Approve | ✓ VERIFIED | file exists, Playwright-discovered (per 03.4-04 SUMMARY; live run deferred to verify-work per phase-wide convention) |
| `packages/backend/scripts/run-smoke-fanout.mjs` / `smokeAssert.ts` | `assertFanoutBodiesDistinct` | ✓ VERIFIED | grep-confirmed in both files |
| `docs/playbooks/cockpit.md` | personalization invariants + gap-closure invariants, Last verified bumped | ✓ VERIFIED | "Contact resolution is ADDITIVE (3.4 gap-closure)" + "The agent's view of the shared plan is COMPLETE (3.4 gap-closure)" invariants present; Last verified header dated 2026-07-14 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `llm.ts personalizeRecipient` | `plans.recipientBodies[address]` | `internal.plans.patchPlan { recipientBodies: {...existing,[address]:body} }` | ✓ WIRED | llm.ts:787-788 |
| `llm.ts personalizeRecipient` | `scanText -> draftCockpit` | redact-then-draft, fail-closed | ✓ WIRED | llm.ts:786 scan before draftCockpit call; `llmRedaction.test.ts` static scan (11/11 green) |
| `llm.ts proposePlan` | group+personalization refusal | facts-from-the-row gate | ✓ WIRED | llm.ts:909-911 |
| `cockpit.ts executePlan seed loop` | `requests.draft` | `recipientBodies[recipient] ?? body` | ✓ WIRED | cockpit.ts:257; `cockpit.test.ts` 7/7 green |
| `cards.tsx PlanCard` | `plan.recipientBodies` | reactive per-recipient render pre-Approve | ✓ WIRED | cards.tsx:228 renders `PlanRecipientBodies` before `PlanAttachments` |
| `plans.ts writeCandidates` (gap-closure) | `plans.candidates`/`pendingValid` | upsert-by-name + union (additive, not wholesale patch) | ✓ WIRED | plans.ts:111-134; a multi-name turn keeps every name |
| `llm.ts buildAgentContext` (gap-closure) | pending contact picks | "Awaiting the user's contact pick" surfacing (name+count only) | ✓ WIRED | llm.ts:476-491 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| CKPT-03 | 03.4-01/02/03/04 | Multi-recipient send tailors wording per recipient behind single plan approval, per-recipient content shown on PLAN card before approval, same PII/cost guardrails + per-recipient audit/telemetry | ✓ SATISFIED | SC1 (per-recipient tailoring shown pre-Approve) is human-verified APPROVED per task context, with the live-only agent↔workspace disconnect (multi-name contact resolution) found and fixed (591f88b, 43614a8, 941ee01, merged to main via 2e79fff) and re-tested live (both names now shown on ResolutionCard). SC2 (guardrail parity) is satisfied by construction — `personalizeRecipient` reuses `draftBody`'s exact scan→draft path — and proven by `llmRedaction.test.ts` static scans (recipientBodies absent from every audit/DLQ/telemetry write) + `smoke:fanout`'s `assertFanoutBodiesDistinct` + `assertNoRawPiiFanout` needle checks (per 03.4-04 SUMMARY; live smoke run deferred to verify-work per phase-wide convention, source verified clean). |

Note: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` in this worktree (and on `main`) still show CKPT-03 as "In Progress (03.4-01 foundation...)" — this is stale bookkeeping left over from before the Wave-4 phase-close commits and the subsequent human-verify/gap-closure session; it is not a code gap. The orchestrator should update these two files to reflect CKPT-03 Complete now that this verification confirms the goal is achieved.

### Anti-Patterns Found

None. Grep for TODO/FIXME/XXX/HACK/PLACEHOLDER across the phase's modified files (llm.ts, cockpit.ts, plans.ts, cards.tsx) returned no matches. `node scripts/check-playbooks.mjs` exits 0.

### Automated Test Results

- `pnpm --filter @pikar/backend vitest run convex/cockpitTools.test.ts` — **24/24 green** (includes the resolution-fix additive/pending-candidate coverage + all personalization tool cases).
- `pnpm --filter @pikar/backend test` (full suite) — **128/129 green**. The 1 red is `audit.test.ts` "audit.log inserts exactly one row that round-trips" (`Component "auditCounts" is not registered`) — this is the documented pre-existing red (known since Phase 2, unrelated to this phase's changes: audit.ts's aggregate-component registration issue in the test harness). The previously-documented `runCockpitAgent.test.ts` mock-loop cold-start timeout flake did NOT reproduce in this run (completed in 5158ms, under the flake's usual trigger threshold) — consistent with it being a load-dependent flake, not a regression.
- `node scripts/check-playbooks.mjs` — exit 0.

### Human Verification Required

None outstanding for this verification pass — per the task context, CKPT-03's human-verify was already performed on the live Gmail-connected backend and APPROVED by the user, with the live-only multi-name contact-resolution gap found and fixed during that session (verified in code above: `writeCandidates` additive upsert + `buildAgentContext` pending-candidate surfacing).

### Gaps Summary

No code gaps found. All four plans' must-haves are present and wired: the `recipientBodies` content-plane field (Wave 1), the `personalizeRecipient` governed tool with guardrail parity by construction (Wave 2), the `executePlan` seed override + PLAN card per-recipient section (Wave 3), and the E2E/smoke closer + playbook invariants (Wave 4). The CKPT-03 human-verify gap-closure (additive contact resolution + complete agent context) is present in code, tested, and documented in the playbook. The only outstanding item is stale status text in `REQUIREMENTS.md`/`ROADMAP.md` ("In Progress") that the orchestrator should flip to Complete as part of closing this phase — not a functional gap.

---

_Verified: 2026-07-14T15:36:54Z_
_Verifier: Claude (gsd-verifier)_
