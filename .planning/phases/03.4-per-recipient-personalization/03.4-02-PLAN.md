---
phase: 03.4-per-recipient-personalization
plan: 02
type: execute
wave: 2
depends_on: ["03.4-01"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/llmRedaction.test.ts
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/skill-registry.md
requirements: [CKPT-03]
autonomous: true
must_haves:
  truths:
    - "The agent can tailor ONE recipient's wording by 1-based #index; the address is resolved server-side (never handled by the model, §2-D)"
    - "personalizeRecipient runs scanText fail-closed BEFORE draftCockpit (redact-then-draft), the identical guardrail draftBody uses"
    - "An out-of-range index is rejected and does NOT patch the row"
    - "The tailored body lands in recipientBodies[address], NOT the shared body; the shared body is unchanged"
    - "proposePlan refuses a group-mode plan that carries any personalization (individual required)"
    - "buildAgentContext shows which recipients are personalized by #index only, never by address"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "personalizeRecipient tool in buildCockpitTools + buildAgentContext personalization surfacing + proposePlan group gate + SMOKE::agent::personalize op"
      contains: "personalizeRecipient"
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "Personalization section (SUGGEST-then-confirm; individual-mode requirement)"
      contains: "Personaliz"
    - path: "packages/backend/convex/cockpitTools.test.ts"
      provides: "governance cases: index bounce (no patch), scan-before-draft, patches recipientBodies not body, group-mode propose refusal"
    - path: "packages/backend/convex/llmRedaction.test.ts"
      provides: "static scan: personalizeRecipient scanText-before-draftCockpit + recipientBodies never in an audit/DLQ write"
  key_links:
    - from: "llm.ts personalizeRecipient"
      to: "plans.recipientBodies[address]"
      via: "internal.plans.patchPlan { recipientBodies: {...existing, [address]: draft.body} }"
      pattern: "recipientBodies"
    - from: "llm.ts personalizeRecipient"
      to: "scanText -> internal.llm.draftCockpit"
      via: "redact-then-draft (fail-closed)"
      pattern: "scanText"
    - from: "llm.ts proposePlan"
      to: "group-mode + recipientBodies refusal"
      via: "facts-from-the-row gate"
      pattern: "recipientBodies"
---

<objective>
Add the governed `personalizeRecipient({ index, instructions })` tool that MIRRORS `draftBody`
verbatim (scanText fail-closed -> `draftCockpit` -> patch) but targets ONE recipient resolved
server-side from a 1-based `#index` (exactly like `removeRecipient`, llm.ts:644-656), writing the
tailored body into `recipientBodies[address]` (Wave 1) instead of the shared `body`. Surface
personalization state in `buildAgentContext` by #index only. Add the `proposePlan` group-mode
refusal gate (personalization is exclusive with a combined group send). Add the
`SMOKE::agent::personalize=<i>:<intent>` offline driver. Teach the `cockpit-agent` skill a
"Personalization" section via the 5-file mirror.

Purpose: This is the whole feature's engine. Guardrail parity is FREE by construction — reusing
the identical `scanText -> draftCockpit` path means PII redaction + cost preCall/recordSpend are
the SAME code (SC2). No new LLM action, no new "use node" module (playbook invariant).
Output: the tool + context + gate + skill + governance tests. NO seed/UI (Wave 3/4).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-per-recipient-personalization/03.4-RESEARCH.md
@.planning/phases/03.4-per-recipient-personalization/03.4-VALIDATION.md
@docs/playbooks/cockpit.md

<interfaces>
<!-- The two existing patterns personalizeRecipient composes. Executor: mirror these EXACTLY. -->

draftBody (llm.ts:684-718) — the scan->draftCockpit->patch shape to copy:
```typescript
const scan = scanText(intent);                              // redact BEFORE the model (§4). Fail closed.
if (!scan.ok) throw new Error("cockpit: body-intent scan failed");
const plan = await readPlan();
const draft = await ctx.runAction(internal.llm.draftCockpit, {
  tenantId, safeText: scan.value.safeText, safeTextHash: await contentHash(scan.value.safeText),
  greetingName: plan.greetingName,
});
await ctx.runMutation(internal.plans.patchPlan, { planId, bodyIntent: intent, body: draft.body });
```

removeRecipient (llm.ts:644-656) — the server-side #index -> address resolution:
```typescript
// 1-based #index only; the model never handles the address (§2-D)
inputSchema: jsonSchema<{ index: number }>({ ... required: ["index"] }),
```

proposePlan (llm.ts:804-841) — the facts-from-the-row gate to extend (currently guards
recipients/subject/body/attachmentError; add the group+personalization refusal alongside).

buildAgentContext (llm.ts:441-471) — currently takes { recipients, subject, body, mode, attachments, attachmentError }; widen to also accept recipientBodies so index<->address alignment is computed HERE, never exposed.

AgentSmokeOp (llm.ts:934-1015) — the offline sentinel grammar; `regenerate` (llm.ts:976-981) is the
`<i>:<topic>` split-on-first-colon precedent for the new `personalize=<i>:<intent>` op.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: personalizeRecipient tool + buildAgentContext surfacing + SMOKE op</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - personalizeRecipient({ index: 1, instructions }) resolves recipients[0] server-side, scans instructions (fail-closed), calls draftCockpit, patches recipientBodies[address] = draft.body; the shared plan.body is UNCHANGED.
    - personalizeRecipient({ index: 99, ... }) on a 2-recipient plan returns a rejection string ("no recipient #99...") and does NOT call patchPlan (no write).
    - A merge (not replace): personalizing #2 after #1 leaves #1's override intact — the tool spreads `{...(plan.recipientBodies ?? {}), [address]: draft.body}`.
    - buildAgentContext annotates each recipient line " — personalized" / " — shared body" by #index; NO address appears.
    - SMOKE::agent::personalize=1:make it warmer drives the tool offline via one governed call.
  </behavior>
  <action>
    In `buildCockpitTools` (llm.ts), add `personalizeRecipient` alongside `draftBody`/`removeRecipient` (place near draftBody, llm.ts:718). Compose it from the two interfaces above:
    - inputSchema `jsonSchema<{ index: number; instructions: string }>` (index = 1-based #index; instructions = how the wording should differ, plain language). Description: "Tailor the email wording for ONE recipient at the given 1-based #index, from a plain-language instruction. The shared body is unchanged; only that recipient gets the tailored version."
    - execute: `const plan = await readPlan(); const recipients = plan.recipients ?? []; const i = index - 1;` — if `i < 0 || i >= recipients.length` return `` `Rejected: there is no recipient #${index}. The plan has ${recipients.length}.` `` (NO patch). Else `const address = recipients[i]!;` (server-side, §2-D).
    - `const scan = scanText(instructions); if (!scan.ok) throw new Error("cockpit: personalize-intent scan failed");` — redact-then-draft, identical to draftBody.
    - `const draft = await ctx.runAction(internal.llm.draftCockpit, { tenantId, safeText: scan.value.safeText, safeTextHash: await contentHash(scan.value.safeText) });` — DECISION (Pitfall 4): OMIT `greetingName` here. The shared `greetingName` is pick-#1's name and would open a DIFFERENT recipient's tailored body with the wrong "Hi <name>,". Let the `instructions` carry any greeting intent. Note per-recipient greeting as a ceiling in the SUMMARY.
    - `const next = { ...(plan.recipientBodies ?? {}), [address]: draft.body }; await ctx.runMutation(internal.plans.patchPlan, { planId, recipientBodies: next }); return \`Tailored the wording for recipient #${index}.\`;` — return a LABEL only, never the address/body (§2-D/§4).
    - (Array-fallback branch only if Wave 1 used the array shape: build the next array by replacing/appending the {address, body} entry.)

    Widen `buildAgentContext` (llm.ts:441-471): add `recipientBodies?: Record<string,string>` to the param type; in the recipients `.map`, align each view row to `(plan.recipients ?? [])[i]` and append `" — personalized"` when `recipientBodies[addr]` exists, else `" — shared body"` (see RESEARCH Pattern 3). The address is used ONLY to look up the flag here — it is never emitted. Update the two `buildAgentContext(plan)` call sites to pass `recipientBodies` (the plan row already carries it).

    Add the SMOKE op (llm.ts:934-1015): extend `AgentSmokeOp` union with `| { kind: "personalize"; index: number; instructions: string }`; in `parseAgentSmoke` add `case "personalize"` splitting on the FIRST colon (mirror `regenerate`, llm.ts:976-981) -> `{ kind, index: Number(...), instructions: rest }`; in `runAgentSmokeOp` add `case "personalize": return invokeTool(tools, "personalizeRecipient", { index: op.index, instructions: op.instructions });`. Update the grammar comment (llm.ts:930-933).

    Add cockpitTools.test.ts cases for the <behavior> (drive via the `__invokeCockpitTool` shim against REAL primitives offline as the existing attachment/draft cases do; SMOKE:: instructions so draftCockpit returns fixed text with no model call).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpitTools</automated>
  </verify>
  <done>personalizeRecipient patches recipientBodies[address] (not body); out-of-range index bounces without a patch; merge preserves prior overrides; buildAgentContext shows personalized/shared by #index address-free; SMOKE personalize op drives the tool; cockpitTools.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: proposePlan group-mode refusal gate + redaction static scan</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts, packages/backend/convex/llmRedaction.test.ts</files>
  <behavior>
    - proposePlan on a plan with mode "group" AND a non-empty recipientBodies returns a refusal telling the agent to switch to individual; the plan is NOT proposed.
    - proposePlan on an individual plan with recipientBodies proceeds normally (personalization allowed).
    - proposePlan on a group plan with NO recipientBodies is unaffected (today's behavior).
    - Static scan: the personalizeRecipient tool surface runs scanText BEFORE draftCockpit (redact-then-draft), and recipientBodies never appears in any audit/DLQ payload write in llm.ts/cockpit.ts/plans.ts.
  </behavior>
  <action>
    In `proposePlan` (llm.ts:812-841), after the existing recipients/subject/body/attachmentError guards and BEFORE the `proposeEmailPlan` call, add the group-mode refusal (facts from the ROW, like the attachmentError gate at llm.ts:826):
    `const hasPersonalization = Object.keys(plan.recipientBodies ?? {}).length > 0;` (array fallback: `(plan.recipientBodies ?? []).length > 0`)
    `if (hasPersonalization && plan.mode === "group") return "Cannot propose yet — this plan tailors wording per recipient, which requires individual sends (a group send is one combined email). Switch the mode to individual, then propose.";`
    Rationale (locked decision (a) from RESEARCH Open Questions): REFUSE in proposePlan rather than silently force-individual — explicit user consent to individual sends. Do NOT edit executePlan for this (Wave 3 keeps the seed change to one line).

    Add the cockpitTools.test.ts group-refusal cases from <behavior>.

    Extend llmRedaction.test.ts: (1) a static scan asserting the `personalizeRecipient` code surface calls `scanText(` before `internal.llm.draftCockpit` (mirror the draftBody redact-before-draft assertion); (2) extend the existing cockpit-plane refs-only assertion so `recipientBodies` is in the set of content-plane fields that MUST NOT appear in any `audit`/`deadLetters`/telemetry write in llm.ts/cockpit.ts/plans.ts (Pitfall 5, §4).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpitTools llmRedaction</automated>
  </verify>
  <done>Group+personalization proposePlan refuses; individual+personalization proceeds; personalizeRecipient proven to scan-before-draft; recipientBodies proven absent from every audit/DLQ write; both suites green.</done>
</task>

<task type="auto">
  <name>Task 3: cockpit-agent skill Personalization section (5-file mirror) + §9 playbook bumps</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md</files>
  <action>
    Edit the CANONICAL `packages/contracts/skills/cockpit-agent.md` — add a "## Personalization" section (place after "## Attachments"): the agent has `personalizeRecipient` to tailor ONE recipient's wording by `#index`; SUGGEST-then-confirm before tailoring (never personalize unasked); reason about recipients by `#index` only (never an address); personalization requires INDIVIDUAL mode — if the user wants per-recipient wording, ensure `setMode("individual")` (a group send is one combined email and `proposePlan` will refuse a group plan that carries tailoring); the shared body still covers any recipient you do not personalize. Keep the prose consistent with the existing address-free / no-hardcoded-send voice.

    Regenerate the DERIVED `packages/contracts/src/skills/cockpitAgent.ts` `cockpitAgentSkillBody` constant so it is BYTE-IDENTICAL (LF-normalized) to the .md — the drift `test.each` row (`skills.test.ts:152`) enforces this. `seedSkills` publish-on-change (no version literal in the row, skills.ts:101) ships v-next automatically when the active body drifts; the `COCKPIT_AGENT_SKILL` name const and the seed row need NO edit. Bump the `(v1)` header token in BOTH files if the existing convention increments it — keep the two byte-identical either way.

    §9: cockpit-agent skill sources are watch-protected under skill-registry.md AND cockpit.md. Bump BOTH playbooks' `Last verified` to `03.4-02`; in skill-registry.md note the cockpit-agent Personalization section; in cockpit.md note the personalizeRecipient tool alongside the tool set. Keep edits minimal + additive.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test skills && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>cockpit-agent.md has a Personalization section; derived cockpitAgent.ts is byte-identical (drift test green); seedSkills publishes v-next on change (skills.test.ts green); both playbooks bumped to 03.4-02; check-playbooks exits 0. No hardcoded prompt in convex source (§5).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test cockpitTools llmRedaction skills` green.
- personalizeRecipient: index-bounce no-patch, scan-before-draft, patches recipientBodies[address] not body, merge preserves prior overrides.
- proposePlan refuses group+personalization; allows individual+personalization.
- recipientBodies proven absent from every audit/DLQ write (llmRedaction static scan).
- Skill drift test green (5-file mirror intact); both playbooks bumped; check-playbooks exits 0.
</verification>

<success_criteria>
The agent can tailor one recipient's body by #index through a governed tool that reuses draftBody's
exact redact-then-draft + cost path (SC2 parity free); the override lands in recipientBodies[address]
not the shared body; group+personalization is refused; the model never sees an address; the
cockpit-agent skill teaches SUGGEST-then-confirm + individual-mode. Ready for Wave 3 (executePlan seed + card).
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-per-recipient-personalization/03.4-02-SUMMARY.md`.
</output>
