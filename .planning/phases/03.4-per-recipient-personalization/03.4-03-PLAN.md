---
phase: 03.4-per-recipient-personalization
plan: 03
type: execute
wave: 3
depends_on: ["03.4-01", "03.4-02"]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
requirements: [CKPT-03]
autonomous: true
must_haves:
  truths:
    - "On Approve, each recipient's requests.draft carries its DISTINCT tailored body (recipientBodies[recipient] ?? body); the goal (subject) is SHARED"
    - "A recipient with no override falls back to the shared body — no duplication, no error"
    - "An orphaned recipientBodies key (address no longer in recipients) is ignored harmlessly at seed"
    - "The PLAN card shows each recipient's tailored body (or the shared body for a non-personalized recipient) before the single Approve"
    - "The send/audit/telemetry/DLQ spine below executePlan is UNCHANGED — single workflow.start call site intact"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "executePlan seed loop reads the per-recipient override (ONE line changed)"
      contains: "recipientBodies"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "PLAN card per-recipient body section (mirror PlanAttachments)"
      contains: "recipientBodies"
    - path: "packages/backend/convex/cockpit.test.ts"
      provides: "executePlan distinct-draft assertion (distinct drafts, shared goal, non-personalized fallback)"
  key_links:
    - from: "cockpit.ts executePlan seed loop"
      to: "requests.draft"
      via: "recipientBodies[recipient] ?? body"
      pattern: "recipientBodies"
    - from: "cards.tsx PlanCard"
      to: "plan.recipientBodies"
      via: "reactive per-recipient render before Approve"
      pattern: "recipientBodies"
---

<objective>
Wire the override into delivery and surface it pre-Approve — the two consumer ends of the Wave-1
field. Backend: change the ONE `draft:` line in `executePlan`'s existing per-recipient seed loop
(cockpit.ts:244-259) to `draft: (plan.recipientBodies ?? {})[recipient] ?? body`. Frontend: add a
per-recipient body section to the PLAN card (mirror `PlanAttachments`, cards.tsx:72-150) so the user
sees each tailored body (or the shared body) before the single Approve.

Purpose: The pivotal research finding — the delivery spine is ALREADY per-recipient
(`gmail.getForDelivery` reads `body: r.editedBody ?? r.draft ?? ""` PER request row, gmailAuth.ts:138),
so a recipient's tailored wording only has to land in ITS `requests.draft`. `deliverApprovedPlan`,
`gmail.send`, the CAS, and the single `workflow.start` need ZERO changes. This is the OPPOSITE of the
3.3 attachment fan-out (one shared ref to all); here each row gets a DISTINCT body, shared subject.
Output: the seed line + PLAN card section + executePlan distinct-draft test. NO smoke/E2E/close (Wave 4).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-per-recipient-personalization/03.4-RESEARCH.md
@.planning/phases/03.4-per-recipient-personalization/03.4-VALIDATION.md
@docs/playbooks/cockpit.md

<interfaces>
<!-- The seed loop to change (ONE line) and the card section to mirror. -->

executePlan seed loop (cockpit.ts:244-259) — only the `draft:` line changes:
```typescript
const targets = mode === "group" ? [recipients.join(", ")] : recipients; // group => join-miss => shared body
for (const recipient of targets) {
  const correlationId = crypto.randomUUID();
  const requestId = await ctx.db.insert("requests", {
    tenantId: ctx.tenantId, correlationId,
    goal: subject,                                          // SHARED subject (unchanged)
    recipient,
    draft: body,                                            // <- becomes: (plan.recipientBodies ?? {})[recipient] ?? body
    status: "approved", attachmentRefs, planId, createdAt: Date.now(),
  });
}
```
Note: group mode joins recipients into ONE comma-joined `recipient` string, so the address-key
lookup MISSES and falls back to the shared body — correct (a group send is one combined email).
proposePlan (Wave 2) already refuses group+personalization, so a group plan never carries overrides.

PlanAttachments (cards.tsx:72-150) — the per-recipient/list section template to mirror; PlanCard
renders it at cards.tsx:189 between PREVIEW and the steps list. Plan type already exposes
`recipients?: string[]` and (Wave 1) `recipientBodies` on the reactive `api.plans.byThread` row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: executePlan seed override + distinct-draft test</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts</files>
  <behavior>
    - executePlan on an individual plan with recipientBodies = { "a@x.com": "Dear A", "b@x.com": "Yo B" } and recipients [a@x.com, b@x.com, c@x.com] seeds three requests: a's draft = "Dear A", b's = "Yo B", c's = plan.body (fallback); all three share goal = subject.
    - An orphaned key ("z@x.com" not in recipients) is simply never looked up — no error, no extra row.
    - The single workflow.start call site is intact (grep-verifiable, one call).
  </behavior>
  <action>
    In `executePlan` (cockpit.ts:251), change the single `draft: body,` line to:
    `draft: (plan.recipientBodies ?? {})[recipient] ?? body, // tailored per recipient if present, else the shared body`
    (Array fallback from Wave 1: `plan.recipientBodies?.find((e) => e.address === recipient)?.body ?? body`.)
    Change NOTHING else — `goal: subject` stays shared, `attachmentRefs` stays shared (3.3), the CAS/token-check/`workflow.start` are untouched. Group mode's comma-joined `recipient` misses the address key and falls back to `body` by construction (proposePlan already blocks group+personalization). Orphaned keys are ignored (Pitfall 2) — no filter needed since a missing recipient is simply never read.

    Extend cockpit.test.ts with the <behavior> case. This reuses the `workflow` + `workflow/workpool` component registration 3.3 added (03.3-05) so executePlan runs the successful proposed->delivering path end-to-end; assert each seeded `requests` row's `draft` and shared `goal` via the `by_plan` index.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpit</automated>
  </verify>
  <done>Each recipient's requests.draft is its distinct override; non-personalized recipient falls back to shared body; goal shared; single workflow.start intact; cockpit.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: PLAN card per-recipient body section + §9 playbook bump</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, docs/playbooks/cockpit.md</files>
  <action>
    In cards.tsx, add a `PlanRecipientBodies({ plan })` section mirroring `PlanAttachments` (cards.tsx:72-150) and render it in `PlanCard` (near cards.tsx:189, after PREVIEW). For each recipient in `plan.recipients ?? []`, show a labeled row: the recipient chip/label + its body — `(plan.recipientBodies ?? {})[recipient]` when present (tag it "tailored"), else the shared `plan.body` (tag it "shared body"). Keep it read-only (slice 1 — tailoring happens in chat via the agent; no inline editor, matching DraftCard's ponytail note). Reuse the existing `label`/`dim`/`chip`/`box` style tokens; truncate long bodies like the PREVIEW (`.slice(0, 240)`). If NO recipient has an override, you MAY render nothing (the shared PREVIEW already covers the same-content case) — but when any override exists, show the per-recipient breakdown so the user sees each tailored body BEFORE Approve (SC1).
    (Array fallback: look up via `.find((e) => e.address === recipient)?.body`.)

    §9: cards.tsx + cockpit.ts are watch-protected under cockpit.md. Bump `Last verified` to `03.4-03` and note the per-recipient draft override (executePlan) + the PLAN card per-recipient body section. Minimal + additive; full invariant lands in Wave 4.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>PLAN card shows each recipient's tailored/shared body before Approve; web typecheck clean; cockpit.md bumped to 03.4-03; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test cockpit` green (distinct drafts, shared goal, fallback).
- `pnpm --filter @pikar/web typecheck` clean.
- Single `workflow.start(` call site intact (grep cockpit.ts).
- `node scripts/check-playbooks.mjs` exits 0.
</verification>

<success_criteria>
On Approve, each recipient's request carries its distinct tailored body (shared subject); a
non-personalized recipient falls back to the shared body; the PLAN card shows each recipient's body
before the single Approve; the delivery spine is untouched. Ready for Wave 4 (E2E + smoke + close + human-verify).
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-per-recipient-personalization/03.4-03-SUMMARY.md`.
</output>
