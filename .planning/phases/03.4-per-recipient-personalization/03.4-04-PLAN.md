---
phase: 03.4-per-recipient-personalization
plan: 04
type: execute
wave: 4
depends_on: ["03.4-03"]
files_modified:
  - apps/web/e2e/cockpit-personalize.spec.ts
  - packages/backend/scripts/run-smoke-fanout.mjs
  - packages/backend/convex/smoke.ts
  - docs/playbooks/cockpit.md
requirements: [CKPT-03]
autonomous: false
must_haves:
  truths:
    - "The offline E2E drives chat -> 2 recipients -> subject -> body -> personalize #1 -> PLAN card shows 2 DISTINCT bodies -> Approve -> REPORT (nothing sent before Approve)"
    - "smoke:fanout proves DISTINCT per-recipient drafts with a SHARED subject (the inverse of 3.3's shared-attachment assertion)"
    - "Each distinct tailored body needle is proven ABSENT from every audit/DLQ/telemetry log plane (§4)"
    - "A real 2-recipient send lands DISTINCT wording in each inbox under a shared subject, audit refs-only (human-verify, CKPT-03)"
  artifacts:
    - path: "apps/web/e2e/cockpit-personalize.spec.ts"
      provides: "the CKPT-03 personalization E2E (mirror cockpit-attachment.spec.ts)"
    - path: "packages/backend/scripts/run-smoke-fanout.mjs"
      provides: "assertFanoutBodiesDistinct + distinct-body needles in assertNoRawPiiFanout"
      contains: "assertFanoutBodiesDistinct"
    - path: "docs/playbooks/cockpit.md"
      provides: "personalization invariants + Last verified -> 03.4-04 (phase close)"
  key_links:
    - from: "run-smoke-fanout.mjs"
      to: "smoke:seedFanout per-recipient bodies + assertFanoutBodiesDistinct"
      via: "distinct drafts, shared subject"
      pattern: "assertFanoutBodiesDistinct"
---

<objective>
Close the phase: prove the distinct-body fan-out end to end and verify a real send. Add the
`cockpit-personalize` Playwright spec (mirror `cockpit-attachment.spec.ts`); extend `smoke:fanout`
to seed DISTINCT per-recipient bodies and assert them (the INVERSE of 3.3's
`assertFanoutAttachmentShared` — distinct drafts, shared subject) plus prove each body needle is
absent from every log plane; update the cockpit playbook with the personalization invariants and
bump `Last verified`; run the CKPT-03 human-verify (the sole live-only proof).

Purpose: SC1 (each recipient tailored, shown pre-Approve) closes on the E2E + human-verify; SC2
(same guardrails + per-recipient audit/telemetry) closes on the smoke distinct-body + refs-only
assertions + the live audit inspection. This mirrors the proven 3.3 Wave-4 closer (03.3-06).
Output: E2E spec + smoke extension + playbook close + CKPT-03 sign-off.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-per-recipient-personalization/03.4-RESEARCH.md
@.planning/phases/03.4-per-recipient-personalization/03.4-VALIDATION.md
@docs/playbooks/cockpit.md

<interfaces>
<!-- The smoke driver to extend and the E2E to mirror. -->

run-smoke-fanout.mjs (full file, 60 lines) currently seeds ONE shared bodyNeedle for all recipients
and asserts a SHARED attachment (assertFanoutAttachmentShared, line 44). The seed helper
`smoke:seedFanout` (a Convex function in convex/smoke.ts) takes { correlationIds, recipients,
failIndex, subjectNeedle, bodyNeedle, attachment } and inserts requests rows directly.
The refs-only scan `assertNoRawPiiFanout` (line 57) takes { correlationIds, needles }.

cockpit-attachment.spec.ts (apps/web/e2e/) is the SMOKE::agent:: E2E template: it types composer
sentinels turn-by-turn (add/subject/body/attach/propose), waits on the composer clearing between
turns, and asserts plan-row-derived CARD content (not agent prose) then Approve -> REPORT.
The new personalize op is `SMOKE::agent::personalize=<1-based i>:<intent>` (Wave 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cockpit-personalize E2E spec</name>
  <files>apps/web/e2e/cockpit-personalize.spec.ts</files>
  <action>
    Create `apps/web/e2e/cockpit-personalize.spec.ts` mirroring `cockpit-attachment.spec.ts`. Drive the offline SMOKE::agent:: flow turn-by-turn on the composer (wait on the composer clearing between turns):
    1. `SMOKE::agent::add=alice@example.com,bob@example.com` (two literal recipients)
    2. `SMOKE::agent::subject=Q3 sync` (shared subject)
    3. `SMOKE::agent::body=SMOKE::route=direct_llm::the shared body` (the base body via the offline draft path)
    4. `SMOKE::agent::mode=individual`
    5. `SMOKE::agent::personalize=1:make Alice's note warmer and personal` (tailor recipient #1)
    Then assert the PLAN card shows TWO recipients with DISTINCT bodies (alice's tailored text differs from bob's shared body) BEFORE the single Approve — assert plan-row-derived card content, not agent prose. Then Approve -> REPORT fills per-recipient (offline lands awaiting_reauth by design — the harness user has a stale token, per 03.1-VALIDATION); assert nothing is sent before Approve (no REPORT until Approve). Playwright-discovered + type-loads; the live green run is deferred to verify-work (needs `next dev :3111` + seeded E2E user, the phase-wide convention).
  </action>
  <verify>
    <automated>pnpm exec playwright test cockpit-personalize --list</automated>
  </verify>
  <done>cockpit-personalize.spec.ts type-loads and is Playwright-discovered; drives 2 recipients + one personalization -> asserts 2 distinct PLAN-card bodies pre-Approve -> Approve -> REPORT; nothing sent before Approve.</done>
</task>

<task type="auto">
  <name>Task 2: smoke:fanout distinct-body extension</name>
  <files>packages/backend/scripts/run-smoke-fanout.mjs, packages/backend/convex/smoke.ts</files>
  <action>
    Extend the live fan-out smoke to prove the distinct-body path (the INVERSE of the shared-attachment assertion). In `convex/smoke.ts`:
    - Extend `smoke:seedFanout` with an OPTIONAL additive arg `recipientBodies` (array parallel to `recipients`, or a map) — when present, each seeded `requests` row's `draft` is that recipient's distinct body; `goal` (subject) stays SHARED (append-only optional arg — do not change existing behavior when absent).
    - Add `smokeAssert:assertFanoutBodiesDistinct({ correlationIds })` — reads the `requests` rows for the cids and asserts every row's `draft` is DISTINCT (no two equal) AND every row's `goal` is IDENTICAL (shared subject). This is the mirror-opposite of `assertFanoutAttachmentShared`.
    In `run-smoke-fanout.mjs`:
    - Generate N distinct body needles (e.g. `BODY-SECRET-<i>-<uuid8>`), pass them as `recipientBodies` to `smoke:seedFanout`.
    - Call `assertFanoutBodiesDistinct` after the seed (alongside the existing `assertFanoutAttachmentShared`).
    - Add EVERY distinct body needle to the `needles` array fed to `assertNoRawPiiFanout` (line 56) so each tailored body is proven absent from any audit/deadLetters/telemetry row (§4, Pitfall 5).
    Keep the forced-fail-row isolation + write-once + reached-all assertions intact.
    §9: run-smoke-fanout.mjs is watch-listed under cockpit.md — the playbook bump in Task 3 covers this turn's change.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend smoke:fanout</automated>
  </verify>
  <done>smoke:fanout seeds distinct per-recipient bodies; assertFanoutBodiesDistinct proves distinct drafts + shared subject; each body needle proven absent from every log plane; forced-fail isolation + write-once still green against a live convex dev.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — cockpit.md personalization invariants</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
    Update `docs/playbooks/cockpit.md` for the phase close (CLAUDE.md §9). Bump `Last verified` to `03.4-04`. Add the personalization invariants to the "Invariants — what must never break" section:
    - **Per-recipient wording rides the existing per-recipient spine (3.4, CKPT-03)**: a tailored body lives in `plans.recipientBodies` (address-keyed, content-plane only, never audited §4); `executePlan` seeds each `requests.draft` as `recipientBodies[recipient] ?? body` (distinct per recipient, SHARED subject) — the delivery spine (`deliverApprovedPlan`/`gmail.send`/`getForDelivery`/CAS/single `workflow.start`) is UNCHANGED. Inverse of the 3.3 shared-attachment fan-out.
    - **Personalization requires individual mode (3.4)**: `proposePlan` refuses a `group` plan that carries any `recipientBodies` (a group send is one combined email); the `cockpit-agent` skill teaches SUGGEST-then-confirm + individual-mode.
    - **Guardrail parity is by construction (3.4)**: `personalizeRecipient` reuses `draftBody`'s exact `scanText`(fail-closed)->`draftCockpit` path, so PII redaction + `preCall`/`recordSpend` cost governance are the SAME code; per-recipient `correlationId` -> `gmail.sent` audit + terminal telemetry already isolate per recipient (no new audit event).
    Note the new tests in the How-to-verify / Tests lists: `cockpit-personalize.spec.ts`, the `assertFanoutBodiesDistinct` smoke assertion, and the cockpitTools/llmRedaction personalization cases. Update the Key-files entries for llm.ts (personalizeRecipient), cockpit.ts (seed override), cards.tsx (per-recipient body section), plans.ts (recipientBodies). Keep prose consistent with the existing voice.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>cockpit.md documents the personalization invariants + updated Key-files/Tests; Last verified = 03.4-04; check-playbooks exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CKPT-03 human-verify — real distinct-wording send</name>
  <action>Manual verification checkpoint (the sole live-only proof, mirrors 3.3's V10) — truly manual because a real Gmail send needs human OAuth consent and human inboxes to confirm distinct wording arrives. Complete Task 3 (playbook close) and confirm the full backend + core suites are green first, then follow the how-to-verify steps and sign off.</action>
  <what-built>
    Per-recipient personalization end to end: the agent tailors one recipient's wording
    (`personalizeRecipient`), the PLAN card shows each recipient's distinct body before the single
    Approve, and on Approve each recipient's request carries its own body under a shared subject
    through the unchanged governed send (audit/telemetry/DLQ), with the same PII/cost guardrails.
  </what-built>
  <how-to-verify>
    On the live Gmail-connected backend (this worktree's own `npx convex dev` + `pnpm dev`, skills seeded):
    1. Connect Gmail (if not already). Start a cockpit conversation.
    2. Compose to TWO real recipients you control (two inboxes you can open).
    3. Give a shared subject and a base body.
    4. Ask the agent to tailor recipient #1's wording distinctly (e.g. "make the note to the first person warmer and mention the demo"), leave #2 on the shared body.
    5. Confirm the PLAN card shows TWO DISTINCT bodies (recipient #1 tailored, recipient #2 shared) BEFORE you Approve; confirm nothing was sent yet.
    6. Approve ONCE.
    7. Open BOTH inboxes: confirm each received its OWN wording (distinct bodies) under the SHARED subject.
    8. Inspect the audit/deadLetters rows (`npx convex run` or dashboard): confirm they are refs-only — storageId/filename/messageId/correlationId/counts, NO raw body/subject/address (§4). Confirm `recipientBodies` content appears in NO audit/DLQ/telemetry row.
    Note: if a group-mode send is attempted with personalization, the agent should refuse and ask to switch to individual.
  </how-to-verify>
  <resume-signal>Type "approved" if both inboxes received distinct wording under a shared subject with audit refs-only; otherwise describe what differed (wrong body, shared where distinct expected, any raw content in a log).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec playwright test cockpit-personalize --list` discovers the spec (type-loads).
- `pnpm --filter @pikar/backend smoke:fanout` green with distinct-body assertion + refs-only needles.
- `node scripts/check-playbooks.mjs` exits 0.
- Full backend + core suites green before verify-work: `pnpm --filter @pikar/core test && pnpm --filter @pikar/backend test` (the 1 pre-existing audit.test.ts red is NOT a regression).
- CKPT-03 human-verify APPROVED (distinct inbox wording, shared subject, audit refs-only).
</verification>

<success_criteria>
CKPT-03 satisfied and human-verified: a multi-recipient send tailors wording per recipient behind
the single plan approval, per-recipient content shown on the PLAN card before Approve, passing the
same PII/cost guardrails and per-recipient audit/telemetry as same-content sends. Phase 3.4 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-per-recipient-personalization/03.4-04-SUMMARY.md`.
</output>
