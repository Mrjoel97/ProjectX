---
phase: 3.4
slug: per-recipient-personalization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-14
---

# Phase 3.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> SC1: each recipient CAN get tailored wording shown pre-Approve.
> SC2: personalized content passes the SAME PII/cost guardrails + per-recipient audit/telemetry as same-content sends.
> Inverts Phase 3.3's fan-out assertion — 3.3 proved ONE shared attachment ref fanned to ALL; here we prove DISTINCT bodies per recipient with a SHARED subject.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `vitest` + `convex-test` (backend, offline); Playwright 1.61.1 (E2E); node smoke scripts (live dev deployment) |
| **Config file** | `packages/backend` vitest (existing); `apps/web/playwright.config.ts` (existing) |
| **Quick run command** | `pnpm --filter @pikar/backend test` |
| **Full suite command** | `pnpm --filter @pikar/core test && pnpm --filter @pikar/backend test && pnpm exec playwright test` |
| **Estimated runtime** | ~30s backend; ~60s full incl. Playwright discovery |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @pikar/backend test` (unit governance + executePlan seed)
- **After every plan wave:** Full backend + core suites; `smoke:fanout` against a live `convex dev` if the wave touched the seed/fan-out
- **Before `/gsd:verify-work`:** Full suite green + `smoke:fanout` (distinct-body assertion) green + `cockpit-personalize` E2E Playwright-discovered
- **Max feedback latency:** ~30 seconds (quick backend run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 3.4-01-* | 01 | 1 | CKPT-03 (SC1) | unit (schema+writer) | `pnpm --filter @pikar/backend test plans` | ✅ (extend) | ⬜ pending |
| 3.4-02-* | 02 | 2 | CKPT-03 (SC1+SC2) | unit (governance) | `pnpm --filter @pikar/backend test cockpitTools` | ❌ W0 | ⬜ pending |
| 3.4-02-* | 02 | 2 | CKPT-03 (SC2) | unit (static scan) | `pnpm --filter @pikar/backend test llmRedaction` | ✅ (extend) | ⬜ pending |
| 3.4-03-* | 03 | 3 | CKPT-03 (SC1) | unit (executePlan) | `pnpm --filter @pikar/backend test cockpit` | ✅ (extend) | ⬜ pending |
| 3.4-04-* | 04 | 4 | CKPT-03 (SC1) | e2e | `pnpm exec playwright test cockpit-personalize` | ❌ W0 | ⬜ pending |
| 3.4-04-* | 04 | 4 | CKPT-03 (SC2) | smoke (live) | `pnpm --filter @pikar/backend smoke:fanout` | ✅ (extend) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/backend/convex/cockpitTools.test.ts` — new cases for `personalizeRecipient`: index bounce (no patch), scan-before-draft, patches `recipientBodies[address]` not shared `body`
- [ ] `packages/backend/convex/cockpit.test.ts` — new `executePlan` case: distinct `requests.draft` per recipient + shared subject + shared-body fallback for a non-personalized recipient (reuses the `workflow`/`workflow/workpool` registration 3.3 added)
- [ ] `packages/backend/convex/llmRedaction.test.ts` — extend static scan for the new tool (scanText-before-draftCockpit) + assert `recipientBodies` never reaches an audit/DLQ payload
- [ ] `apps/web/e2e/cockpit-personalize.spec.ts` — new: chat → resolve/add 2 recipients → subject → body → `SMOKE::agent::personalize=1:<intent>` → PLAN card shows 2 distinct bodies → Approve → REPORT
- [ ] `packages/backend/scripts/run-smoke-fanout.mjs` — extend: seed distinct per-recipient bodies, add `assertFanoutBodiesDistinct` (each row's `draft` differs, subject shared), add each body needle to `assertNoRawPiiFanout`

*No framework install needed — vitest/convex-test/Playwright/smoke scripts all exist.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real 2-recipient send where each inbox receives its OWN tailored wording under a shared subject | CKPT-03 (SC1+SC2) | Requires a live Gmail-connected backend + real inbox delivery; the offline suite cannot observe real inbox arrival (mirrors Phase 3.3 V10) | Connect Gmail; compose to 2 real recipients; personalize recipient #1 with a distinct instruction; Approve ONCE; confirm both inboxes receive distinct bodies + shared subject; confirm audit rows are refs-only (no raw body/address) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
