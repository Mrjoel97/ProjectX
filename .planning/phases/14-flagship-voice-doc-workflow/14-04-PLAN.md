---
phase: 14-flagship-voice-doc-workflow
plan: 04
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - packages/backend/convex/voiceToken.ts
  - packages/backend/convex/voiceToken.test.ts
  - docs/playbooks/voice.md
autonomous: true
requirements: [DOCV-01]

must_haves:
  truths:
    - "A doc-scoped mint bakes the document-analyst persona AND a bounded, fenced digest of the report into the ephemeral session"
    - "The retrieval tool is declared in the flat Realtime shape, and exactly one read-only tool is ever declared"
    - "A mint rejected for carrying tools still produces a working session — the browser is told to declare them over the data channel instead"
    - "The persona still loads from the registry and fails closed when unseeded"
    - "OPENAI_API_KEY is structurally absent from the mint's return value"
  artifacts:
    - path: "packages/backend/convex/voiceToken.ts"
      provides: "mintClientSecret({docId?}) — persona + digest + tool declaration + the session.update fallback"
      contains: "toolsAtMint"
    - path: "packages/backend/convex/voiceToken.test.ts"
      provides: "Mock-fetch body capture asserting persona, digest, flat tool shape, the 400 fallback, and fail-closed"
  key_links:
    - from: "packages/backend/convex/voiceToken.ts"
      to: "internal.skills.getActiveSkill"
      via: "DOCUMENT_ANALYST_SKILL when docId is present, VOICE_SESSION_SKILL otherwise — fail-closed"
      pattern: "DOCUMENT_ANALYST_SKILL"
    - from: "packages/backend/convex/voiceToken.ts"
      to: "packages/voice/src/docSession.ts"
      via: "buildDocDigest + SEARCH_DOCUMENT_TOOL imported from @pikar/voice"
      pattern: "SEARCH_DOCUMENT_TOOL"
---

<objective>
Teach `mintClientSecret` to open a *doc-grounded* session: the document-analyst persona from the
registry, a bounded fenced digest of the report, and the `search_document` tool — with a
pre-written fallback for the one API shape this repo has been wrong about twice.

Purpose: this is the other half of Success Criterion 1. The digest is why the agent is fluent from
the first second and can open with something specific about the report (the flagship moment); the
tool declaration is what makes the mid-call drill-in reachable at all.
Output: `voiceToken.mintClientSecret({ docId? })` → `{ clientSecret, expiresAt, toolsAtMint }`.

**Playbook serialization (wave contract — read this before you plan your commits).** EVERY plan in
Phase 14 appends to `docs/playbooks/voice.md`. Two plans appending to one file in one worktree can
silently lose an update, and there is no git "keep both" recovery for that — so the phase's waves
are deliberately **one plan per wave**: this plan has NO wave-mate and nothing else is editing
`voice.md` while you run. Do not assume a sibling is running beside you, and do not defer your
playbook edit to "the plan that owns it" — you own it for your wave. Append inside the `Phase 14`
subsection only and never reformat another plan's block.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-flagship-voice-doc-workflow/14-RESEARCH.md
@.planning/phases/14-flagship-voice-doc-workflow/14-VALIDATION.md
@.planning/phases/14-flagship-voice-doc-workflow/14-02-SUMMARY.md
@packages/backend/convex/voiceToken.ts
@packages/backend/convex/voiceToken.test.ts
@packages/voice/src/realtime.ts

<interfaces>
<!-- Extracted from the codebase. Use these directly — do not go exploring. -->

`packages/backend/convex/voiceToken.ts:39` — the ONE seam (full current body):
```ts
export const mintClientSecret = tenantAction({
  args: {},
  handler: async (ctx): Promise<{ clientSecret: string; expiresAt: number }> => {
    const skill = await ctx.runQuery(internal.skills.getActiveSkill, { name: VOICE_SESSION_SKILL });
    const res = await fetch(CLIENT_SECRETS_URL, {
      method: "POST",
      headers: { Authorization: `Bearer ${process.env.OPENAI_API_KEY ?? ""}`,
                 "Content-Type": "application/json" },
      body: JSON.stringify({
        session: {
          type: "realtime",
          model: DEFAULT_REALTIME_MODEL,
          instructions: skill.body,
          // LIVE-VERIFIED 2026-07-20: turn_detection + transcription nest under `audio.input`;
          // a top-level session.turn_detection 400s "unknown parameter".
          audio: { input: { transcription: { model: TRANSCRIPTION_MODEL },
                            turn_detection: { type: TURN_DETECTION_TYPE } },
                   output: { voice: REALTIME_VOICE } },
        },
      }),
    });
    if (!res.ok) throw new Error(`mintClientSecret: ${res.status}`);
    const body = (await res.json()) as MintResponse;   // { value: string; expires_at: number }
    return { clientSecret: body.value, expiresAt: body.expires_at };
  },
});
```

`packages/backend/convex/voiceToken.test.ts:26-70` — the mock-fetch **body capture** pattern to
extend (stubs `globalThis.fetch`, keeps the request body, asserts on the parsed JSON).
`voiceToken.test.ts:86` — the existing **fail-closed-when-unseeded** pattern to mirror.

From `@pikar/voice` (14-01 + 14-02): `buildDocDigest({title, text, truncated})`,
`SEARCH_DOCUMENT_TOOL`, `SESSION_TOOL_KEYS`, `TOOL_CHOICE_AUTO`, `DIGEST_CHAR_CAP`.
From `@pikar/contracts/skill`: `DOCUMENT_ANALYST_SKILL`, `VOICE_SESSION_SKILL`.
`internal.vault.getDoc({ vaultDocId, tenantId })` — fail-closed tenant-scoped doc read returning
`{ title, text?, status, extractionTruncated? }`.

**Realtime tool shape is FLAT** — `{type,name,description,parameters}`, NOT the Chat-Completions
`{type, function:{...}}` nesting. Getting it wrong 400s the mint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mintClientSecret gains a doc scope, the tool array, and the session.update fallback</name>
  <files>packages/backend/convex/voiceToken.ts</files>
  <action>
Extend `mintClientSecret` to `args: { docId: v.optional(v.id("vaultDocuments")) }` and an explicit
`Promise<{ clientSecret: string; expiresAt: number; toolsAtMint: boolean }>` return type.

1. **Persona selection (§5).** `docId` present → `internal.skills.getActiveSkill({ name:
   DOCUMENT_ANALYST_SKILL })`; absent → `VOICE_SESSION_SKILL`, byte-unchanged from today. Keep the
   fail-closed behaviour (`getActiveSkill` throws `NO_ACTIVE_SKILL` when unseeded) — never a
   hardcoded fallback prompt. The Phase-6 `voice-session` persona is not touched, so a Phase-14
   prompt change cannot regress Phase 6.
2. **Digest.** `docId` present → `internal.vault.getDoc({ vaultDocId: docId, tenantId: ctx.tenantId })`
   (fail-closed; throw `"voicedoc: document not found"` on null and `"voicedoc: document not ready"`
   when `status !== "ready"` — the mint is a second trust boundary alongside `startSession`). Then
   `instructions: `${skill.body}\n\n${buildDocDigest({ title: doc.title, text: doc.text,
   truncated: Boolean(doc.extractionTruncated) })}``. The digest is already fenced and capped by
   `@pikar/voice` — do NOT re-slice or re-fence here, and do not add any behavioural instruction
   inline (that lives in the registry skill body, §5).
3. **Tool declaration.** `docId` present → add `tools: [SEARCH_DOCUMENT_TOOL]` and
   `tool_choice: TOOL_CHOICE_AUTO` to the `session` object, using the `SESSION_TOOL_KEYS` names.
   Exactly ONE tool, and it is READ-ONLY: no write, no send, no plan mutation is reachable from a
   voice session, so an instruction injected into the report has nothing to actuate (Pitfall 10 —
   the tool-set-level version of the toolless-ingestion firewall). Add a `ponytail:` comment
   recording the residual prompt-injection ceiling (a digest sits in SYSTEM instructions, a
   materially stronger exposure than ADR-006's tool-return case) and the upgrade path.
4. **The contingency, pre-written in this same task (Open Question 3).** Build the body ONCE via a
   local helper `sessionBody(withTools: boolean)`. POST with `withTools = true` when `docId` is
   present. If the response status is **400**, POST again with `withTools = false` and set
   `toolsAtMint = false`; any other non-OK status throws as today (`mintClientSecret: ${status}` —
   a status only, never the key). Return `toolsAtMint: true` when the first POST succeeded (and
   `true` trivially when there is no doc scope and therefore no tool to declare — document that
   choice in a comment so the browser's branch stays simple). Plan 14-06's relay reads this flag and
   sends `{type:"session.update", session:{tools, tool_choice}}` over the data channel when it is
   false. Reference the dated `LIVE-VERIFIED ____-__-__` line 14-02 added to
   `packages/voice/src/realtime.ts` and note that live-verify fills it in.
5. Return shape stays free of key material. `OPENAI_API_KEY` is structurally absent from the
   returned object, from every thrown message, and from every log line (Pitfall 4). The added
   `toolsAtMint` boolean is the ONE deliberate extension to the Phase-6 `{clientSecret, expiresAt}`
   contract; comment why (it is transport control, not a secret).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voiceToken</automated>
  </verify>
  <done>`mintClientSecret` accepts an optional `docId`, selects the persona from the registry accordingly, bakes the fenced digest, declares exactly one flat read-only tool, retries once without tools on a 400, and returns `{clientSecret, expiresAt, toolsAtMint}` with an explicit return type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voiceToken.test.ts — body capture, fallback, fail-closed; then the playbook</name>
  <files>packages/backend/convex/voiceToken.test.ts, docs/playbooks/voice.md</files>
  <behavior>
    - With a `docId`, the captured request body's `session.instructions` starts with the document-analyst skill body and then contains the digest fence markers and the document title
    - With a `docId`, `session.tools` has length exactly 1, its `[0].name === "search_document"`, it has a top-level `parameters` key and NO `function` key (the flat Realtime shape), and `session.tool_choice === "auto"`
    - Without a `docId`, the captured body is unchanged from Phase 6: `session.instructions === voice-session skill body`, and the body has NO `tools` / `tool_choice` keys at all
    - A stubbed fetch returning 400 on the first POST and 200 on the second yields `toolsAtMint === false`, and the SECOND captured body carries no `tools` key; a stubbed 500 still throws
    - Unseeded `document-analyst` → the doc-scoped mint rejects (mirrors the existing fail-closed test at line 86); the returned object's keys are exactly `clientSecret`, `expiresAt`, `toolsAtMint` and no value in it matches the fake API key
    - The captured `session.instructions` length stays under a sane ceiling for the digest branch (assert `instructions.length < skill.body.length + DIGEST_CHAR_CAP + 500`) so a future digest change cannot silently blow the instruction budget
  </behavior>
  <action>
Extend `voiceToken.test.ts` using the existing mock-fetch body-capture pattern at lines 26-70 and
the fail-closed pattern at line 86 — do not invent a new harness. Seed the `document-analyst` skill
row inside the test the way the existing tests seed `voice-session`.

Then add a `### Phase 14 — doc-grounded mint` subsection to `docs/playbooks/voice.md` recording:
the persona switch (document-analyst for a doc-scoped session, voice-session otherwise, both
registry-loaded and fail-closed); the digest budget and its fence; the ONE read-only tool as the
prompt-injection containment at the tool-SET level; the mint-time-then-`session.update` fallback and
the fact that the accepted branch is **not yet live-verified** (pointing at `realtime.ts`'s dated
line); and the `toolsAtMint` return-contract extension. Bump `Last verified`. Append only — do not
reformat another plan's block.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test voiceToken</automated>
    <automated>node scripts/check-playbooks.mjs check</automated>
  </verify>
  <done>All six behaviours green; the no-doc branch is asserted byte-equivalent to the Phase-6 body; `voice.md` carries the doc-grounded-mint subsection with a bumped `Last verified`; `check-playbooks.mjs` exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test voiceToken` — green, including the untouched Phase-6 cases.
- `pnpm test` — backend holds 494/495.
- `pnpm --filter @pikar/backend exec tsc --noEmit` — +0 new errors; `pnpm --filter @pikar/web typecheck` — exit 0.
- `git diff --stat` shows ZERO changes to `convex/llm.ts` and to the `voice-session` skill body.
- `node scripts/check-playbooks.mjs check` — exit 0.
</verification>

<success_criteria>
A doc-scoped mint produces an ephemeral session carrying the document-analyst persona, a bounded
fenced digest of that specific report, and exactly one flat read-only retrieval tool — and if the
API refuses tools in the mint body, the session still opens and the browser is told to declare them
over the data channel instead. The undecided branch is recorded, dated and blank, not guessed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-flagship-voice-doc-workflow/14-04-SUMMARY.md`
</output>
