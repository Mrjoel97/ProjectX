---
phase: 14-flagship-voice-doc-workflow
plan: 09
type: execute
wave: 9
depends_on: ["14-05", "14-07", "14-08"]
files_modified:
  - packages/backend/convex/llmRedaction.test.ts
  - packages/voice/src/realtime.ts
  - docs/playbooks/voice.md
autonomous: false
requirements: [DOCV-01]

must_haves:
  truths:
    - "No report text, passage, quoted excerpt, finding label, citation title, transcript turn or query string can reach an audit / telemetry / dead-letter / agentSteps payload"
    - "The voice-doc module's log-plane surface is pinned by count, so a future audit call is a failing test rather than a silent leak"
    - "Citations are welded in code — the model output schema provably has no citation field"
    - "The behaviours only a real call can prove are verified by a human on integrated main, not faked as automated"
  artifacts:
    - path: "packages/backend/convex/llmRedaction.test.ts"
      provides: "The Phase-14 §4 static scans, each mutation-verified"
      contains: "voiceDoc.ts"
    - path: "packages/voice/src/realtime.ts"
      provides: "The now-dated record of which tool-declaration branch the API accepted"
      contains: "LIVE-VERIFIED"
  key_links:
    - from: "packages/backend/convex/llmRedaction.test.ts"
      to: "packages/backend/convex/voiceDoc.ts"
      via: "on-disk readSource scan over every payload: object"
      pattern: "readSource\\(\"voiceDoc.ts\"\\)"
---

<objective>
Prove Success Criterion 4 statically, then have the owner verify by voice everything a static test
cannot reach.

Purpose: SC4 ("no report content leaks into audit/telemetry/step rows") is not observable at
runtime in a passing system — it only shows up after it has already leaked. This repo's answer is a
mutation-verified source scan, and this plan writes Phase 14's. The remaining SC-critical
behaviours — a real drill-in, a real interruption, a spoken "no gaps", the mint's actual tool
branch — need a microphone and are recorded as an explicit human checkpoint instead of pretended.
Output: the §4 scans, the dated tool-branch record, and a signed-off live verification.

**Playbook serialization (wave contract — read this before you plan your commits).** EVERY plan in
Phase 14 appends to `docs/playbooks/voice.md`. Two plans appending to one file in one worktree can
silently lose an update, and there is no git "keep both" recovery for that — so the phase's waves
are deliberately **one plan per wave**: this plan has NO wave-mate and nothing else is editing
`voice.md` while you run. Do not assume a sibling is running beside you, and do not defer your
playbook edit to "the plan that owns it" — you own it for your wave. Append inside the `Phase 14`
subsection only and never reformat another plan's block.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PARALLELIZATION.md
@.planning/phases/14-flagship-voice-doc-workflow/14-VALIDATION.md
@.planning/phases/14-flagship-voice-doc-workflow/14-RESEARCH.md
@.planning/phases/14-flagship-voice-doc-workflow/14-05-SUMMARY.md
@.planning/phases/14-flagship-voice-doc-workflow/14-08-SUMMARY.md
@packages/backend/convex/llmRedaction.test.ts
@packages/backend/convex/voiceDoc.ts
@packages/voice/src/realtime.ts

<interfaces>
<!-- The house scan idiom, from llmRedaction.test.ts (784 lines, @vitest-environment node). -->

```ts
const readSource = (file: string): string => readFileSync(join(convexDir, file), "utf8");

// payload extraction by eventType (line ~87, the mailbox.searched precedent):
const m = src.match(/eventType:\s*["']mailbox\.searched["'][\s\S]*?payload:\s*(\{[^}]*\})/);
expect(m, "mailbox.searched audit not found").toBeTruthy();      // presence assertion
expect(m![1]).toMatch(/queryHash/);
expect(m![1]).not.toMatch(/\bquery\b(?!Hash)/);

// content-plane "no log-plane write" scan (line ~54):
expect(src, `${file} writes a log-plane row`).not.toMatch(/audit\.log\b/);

// comment-stripping when prose may legitimately name a banned identifier (line ~125):
const src = readSource("gmail.ts").replace(/\/\/[^\n]*/g, "");   // prose may; CODE may not

// STRICT-mode schema scan (line ~236): every key in `properties` also appears in `required`.
```
An existing test at ~line 706 already scans `voice.ts` session-audit payloads for
`callId|transcript|clientSecret|secret|apiKey|OPENAI_API_KEY|markdown|briefBody|\btext\b|\bbody\b` —
it must still pass after 14-03's `docRef` change.

Phase-14 log-plane surface as built:
- `voiceDoc.ts` → exactly two `audit.log` call sites: `voicedoc.searched`
  `{sessionId, queryHash, resultCount}` and `voicedoc.reviewed`
  `{sessionId, findingCount, gapCount, verdict}`.
- `voice.ts` → the pre-existing `voice.session_started` / `voice.session_ended`, the first of which
  may now carry `docRef`.
- `agentSteps` has no text field by construction (`schema.ts:394`) and its closed allow-list scan
  already lives in this file.

`packages/voice/src/realtime.ts` carries the blank dated line 14-02 added:
`// LIVE-VERIFIED ____-__-__: <mint-time | session.update>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The Phase-14 §4 static scans, every one mutation-verified</name>
  <files>packages/backend/convex/llmRedaction.test.ts</files>
  <behavior>
    - `voicedoc.searched`'s payload object contains `queryHash` and `resultCount` and does NOT contain a bare `query`, `passages`, `chunks`, `text`, `label`, `citationTitle`, `citationExcerpt`, `excerpt` or `transcript`
    - `voicedoc.reviewed`'s payload object contains `findingCount`, `gapCount`, `verdict` and none of the banned content identifiers above
    - Every `payload:` object literal anywhere in `voiceDoc.ts` is free of `passages|chunks|\btext\b|\blabel\b|citationTitle|citationExcerpt|\bexcerpt\b|transcript|\bquery\b(?!Hash)` — not just the two known ones. **`excerpt` is the newest and most dangerous of these**: 14-01 widened `evaluations.findings[]` with `citationExcerpt` so the LOCKED "quoted passage where available" decision could reach the product surface, and a quoted passage is verbatim report content — it is legal in the `evaluations` row and on screen, and illegal in every `payload:` / `agentSteps` row
    - The same forbidden-token scan runs over `voice.ts`'s Phase-14 payloads and over `PostCall.tsx`'s and `cards.tsx`'s telemetry-shaped calls if any exist — a finding excerpt must not become a log field on the way to the screen either
    - `voiceDoc.ts` contains exactly TWO `audit.log` call sites and ZERO `.insert("telemetry"` / `.insert("deadLetters"` / `agentSteps` writes — the count is pinned so a third audit row fails the test rather than leaking quietly
    - The `docReviewSchema` block in `voiceDoc.ts` contains no `citationDocId` / `citationTitle` / `verdict` / `route` / `playbook` / `leverageRank` key (those are welded in code — Success Criterion 2 by construction)
    - The `docReviewSchema` block DOES contain `excerpt` — the one deliberate model-authored citation input (14-CONTEXT.md's "quoted passage where available"). Assert its PRESENCE so a future "cleanup" that deletes it fails loudly rather than silently dropping half the locked citation decision
    - Every `jsonSchema(` block in `voiceDoc.ts` is STRICT-mode legal (every `properties` key repeated in `required`), extending the existing `llm.ts`-scoped scan to the new module
    - Each scan asserts its target identifier is PRESENT before asserting the absence, so a rename fails loudly instead of passing vacuously
  </behavior>
  <action>
Add a Phase-14 block to `packages/backend/convex/llmRedaction.test.ts` following the file's own
idiom exactly (on-disk `readSource`, `@vitest-environment node`, comment-stripping where prose may
legitimately name a banned identifier).

**Mutation-verify every single scan before you finish — this is the house rule, not a suggestion.**
For each new scan: deliberately interpolate the forbidden value into `voiceDoc.ts` (e.g. add
`query` to the `voicedoc.searched` payload; add `citationTitle` to `docReviewSchema`; add a third
`audit.log` call), run the test, CONFIRM IT GOES RED, then revert. A scan that cannot go red is
theatre. Record in the plan summary which mutation you used for each scan and that it went red.

**The excerpt scan is mutation-verified like every other one, no exceptions.** Its planted value is
`citationExcerpt: review.findings[0].citationExcerpt` inside the `voicedoc.reviewed` payload (and,
for the file-wide scan, a bare `excerpt` key in any `payload:` literal). Confirm RED, then revert.
This is the scan that keeps the Phase-14 excerpt feature from turning the audit log into the PII
honeypot CLAUDE.md §4 exists to prevent — the excerpt is the only verbatim report content the
phase persists, so it is the highest-value thing to keep out of the log plane.

Also confirm the pre-existing `voice.ts` session-audit scan (~line 706) still passes now that the
payload may carry `docRef` — if the regex bans a substring that `docRef` trips, widen it precisely
(an id IS a ref and is §4-legal), never by deleting the assertion.

Do NOT extend these scans to `llm.ts` or any Lane-A/Lane-B file.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test llmRedaction</automated>
  </verify>
  <done>Seven new scans green (including the `citationExcerpt` / `excerpt` forbidden-token scan and the `docReviewSchema` excerpt-presence assertion); each was individually confirmed RED under a planted forbidden value and then reverted; the `audit.log` call-site count in `voiceDoc.ts` is pinned; the pre-existing `voice.ts` payload scan still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the voice playbook and stage the live-verify record</name>
  <files>docs/playbooks/voice.md, packages/voice/src/realtime.ts</files>
  <action>
`docs/playbooks/voice.md` has been appended to by six plans. Read the whole Phase-14 section and
consolidate it into a coherent subsystem record — invariants, how to change it safely, how to
verify, operations — without deleting any invariant another plan recorded. It must state at minimum:

- the entry contract (`/dashboard/voice?doc=<id>`, ready-only, failed refused, live wait state);
- the hybrid grounding shape (bounded fenced digest at mint + ONE read-only relayed retrieval tool)
  and why each half exists;
- the §4 split, quoting the two audit payloads verbatim, and the standing rule that the retrieval
  relay never writes an `agentSteps` row;
- the honesty invariants: zero findings ⇒ `insufficient` with gaps force-cleared; findings + zero
  gaps ⇒ `healthy`; the anti-vacuous pairing any future assertion must keep;
- citations / gap route / gap playbook are welded in code — the model schema has no such field;
- the ONE model-authored citation input is `citationExcerpt` (the locked "quoted passage where
  available" half): capped at `EXCERPT_CHAR_CAP`, substring-verified against the document text,
  optional by design, rendered only when present — and banned from every `payload:` because it is
  verbatim report content;
- the persona is ungated by decision, with the `run-eval-golden.mjs` reason, so nobody "fixes" it
  by adding it to `GATED_SKILLS` later;
- the standing traps: never call `runEvaluation` with `document-review`; never route a voice-doc
  synthetic thread to the workspace composer; never add a second `"use node"` Convex module;
- the accepted ceilings, each with its upgrade path: the doc-scoped retrieval top-K filter, prompt
  injection via a digest in system instructions, and `listVaultDocs` returning full rows
  (an observation, deliberately not fixed in this lane).

Bump `Last verified`.

In `packages/voice/src/realtime.ts`, leave the `LIVE-VERIFIED ____-__-__:` line BLANK for now —
Task 3's checkpoint fills it in with the branch the API actually accepted and the date, exactly as
Phase 6 did after its own live verification. Do not guess it.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs check</automated>
    <automated>pnpm --filter @pikar/voice test</automated>
  </verify>
  <done>`voice.md` reads as one coherent Phase-14 record covering all nine bullets with a bumped `Last verified`; the dated tool-branch line is present and still blank; `check-playbooks.mjs` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification on integrated main — the microphone half</name>
  <files>packages/voice/src/realtime.ts, docs/playbooks/voice.md</files>
  <action>
Pause here and hand the checklist below to the owner. This is a BLOCKING checkpoint: do not mark
the phase done on automated gates alone. Once the owner reports back, land their findings — fill in
the dated `LIVE-VERIFIED` line in `packages/voice/src/realtime.ts` with the tool-declaration branch
the API actually accepted, and record the measured retrieval round-trip latency in
`docs/playbooks/voice.md` (bumping its `Last verified`). If a step failed, capture it as a gap for
`/gsd:plan-phase 14 --gaps` rather than patching around it here.
  </action>
  <what-built>
The complete voice-doc flagship flow, fully automated up to the point where a real microphone,
a real `OPENAI_API_KEY` and real duplex audio are required. Everything listed below is a behaviour
that `14-VALIDATION.md` classifies as **manual-only**: `apps/web/e2e/voice.spec.ts` is deliberately
offline, and the golden-eval runner structurally cannot drive a Realtime voice persona.

Per `.planning/PARALLELIZATION.md` Stage 3, this runs ONCE on integrated `main` after all three
lanes have landed — lanes merge on automated gates alone and do not block on sign-off.
  </what-built>
  <how-to-verify>
Prerequisites: on integrated `main`, `pnpm install` → `npx convex dev` → `pnpm dev`, with a seeded
skills registry (`document-analyst` must be active — confirm via `getActiveSkill`) and a real
`OPENAI_API_KEY` in the Convex env.

1. **Entry gating.** Upload a multi-page report at `/dashboard/vault`. While it ingests, confirm
   "Discuss by voice" is disabled and reads "Still reading your document…", and that it
   **enables itself** when the status flips to ready — with no page refresh.
   Then upload a deliberately unreadable file (e.g. a scanned image-only PDF) and confirm it is
   refused with an honest reason and offers no voice action.
2. **The flagship opening.** Start the session on the ready report. Confirm within the first ~15
   seconds the agent says something **specific and non-obvious about that report** — not a generic
   greeting. Confirm the doc strip names the document.
3. **Interrupt and redirect.** Talk over the agent mid-sentence. Confirm it yields immediately and
   follows the redirect.
4. **Drill in (the one that matters).** Ask about a detail that appears only deep in the document —
   something that cannot have been in the opening digest. Confirm you get a grounded answer from
   THIS document. **Time the pause** between your question and the answer and note it (RESEARCH
   Open Question 5 — nothing in-repo measures this).
5. **Tool branch (Open Question 3).** In the browser console filtered by `[voice]`, and in the
   Convex logs, determine whether the mint accepted `tools` or whether the 400 fallback fired and
   the browser sent `session.update`. Also watch the `default:` diagnostic branch for any unhandled
   `function_call*` event name.
6. **Truncated read.** Upload a document large enough to set `extractionTruncated` and confirm the
   agent discloses the partial read **up front** and the strip shows the "partial" badge.
7. **Honest no gaps.** Run a session on a healthy document (one with no real problems). Ask "what
   are the gaps?" and confirm it says there are none and names strengths instead of inventing one.
   Confirm the post-call screen shows the affirmative healthy state in DOCUMENT language.
8. **Outcome choice, both paths.** On one session save the memo; confirm exactly ONE new artifact
   in `/dashboard/vault` (not two). On another, tap a gap → confirm a NEXT-STEP MEMO plan card with
   a single Approve → approve it → confirm the memo lands in the vault and **no email is sent**.
9. **Dropped call.** Close the tab mid-session; on next open confirm the abnormal path still
   produced its artifact and the post-call outcome is still reachable.
10. **Accessibility.** Tab through the post-call findings and gap controls; confirm every control is
    reachable and labelled, and that the text-input fallback still works during a live call.

**Then record the results:**
- Fill in `packages/voice/src/realtime.ts`'s `// LIVE-VERIFIED ____-__-__:` line with the branch the
  API actually accepted and today's date (the Phase-6 precedent).
- Add the measured retrieval round-trip latency to `docs/playbooks/voice.md` and bump
  `Last verified`.
- If step 5 shows the mint refused tools, say so explicitly — that resolves Open Question 3 and the
  fallback becomes the documented primary path.
  </how-to-verify>
  <done>The owner has run all ten steps and reported back; the `LIVE-VERIFIED ____-__-__:` line in `packages/voice/src/realtime.ts` names a branch and a date; the measured retrieval latency is recorded in `docs/playbooks/voice.md` with a bumped `Last verified`.</done>
  <resume-signal>Type "approved" (and paste the tool-branch + latency findings), or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test llmRedaction` — green, with every new scan individually
  proven to go RED under a planted value.
- `pnpm test` — full suite; backend holds 494/495 (sole red = the pre-existing `audit.test.ts`
  `auditCounts` row).
- `pnpm --filter @pikar/backend exec tsc --noEmit` — +0 new over the 52 pre-existing.
- `pnpm --filter @pikar/web typecheck` — exit 0.
- `node scripts/check-playbooks.mjs check` — exit 0.
- The four automated gates above ARE the `PARALLELIZATION.md` Stage-3 lane merge gate; the human
  checkpoint is the once-per-integration verification that happens after all three lanes land.
</verification>

<success_criteria>
Success Criterion 4 is enforced by scans that provably fail when violated; the tool-declaration
branch is recorded with a date rather than guessed; the retrieval latency is measured rather than
assumed; and the owner has confirmed by voice the four behaviours that no automated harness in this
repo can reach — a real drill-in, a real interruption, a spoken honest "no gaps", and the
memo-vs-plan outcome crossing the single Approve gate.
</success_criteria>

<output>
After completion, create `.planning/phases/14-flagship-voice-doc-workflow/14-09-SUMMARY.md`
</output>
