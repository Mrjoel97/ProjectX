---
phase: 03.9-agent-activity-streaming
plan: 04
type: execute
wave: 4
depends_on: ["03.9-03"]
files_modified:
  - apps/web/e2e/cockpit-activity.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/03.9-agent-activity-streaming/03.9-VALIDATION.md
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [CKPT-05]

must_haves:
  truths:
    - "The whole spine is green offline: rows, emission, terminal states, §4 scans, both typechecks"
    - "A human watched a real 10-30s turn and the wait no longer reads as broken"
    - "A human watched a brand-NEW thread's FIRST turn and the trace was not blank"
    - "A failed turn left no step spinning forever"
    - "CKPT-05 is Complete only after the human said so"
  artifacts:
    - path: "apps/web/e2e/cockpit-activity.spec.ts"
      provides: "offline E2E over the SMOKE path — steps render in the workspace"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CKPT-05 flipped to Complete after human-verify"
  key_links:
    - from: "apps/web/e2e/cockpit-activity.spec.ts"
      to: "SMOKE::agent:: sentinel path"
      via: "the Plan 02 smoke-site emission (Pitfall 4) — without it the spec asserts on an empty surface"
      pattern: "SMOKE::agent"
---

<objective>
Close the phase: the offline E2E, the full sweep, and the human-verify that is the ONLY thing that
can actually confirm this phase worked.

Purpose: CKPT-05 is a *perceived-latency* requirement. No automated test can tell us the wait stopped
feeling broken — that is why the checkpoint is non-negotiable and why the phase is not done without
it. Two of the three manual items (the first-turn window; a live failure's terminal state) are
structurally outside the E2E's reach.

Output: `cockpit-activity.spec.ts`, a green sweep, a human sign-off, and CKPT-05 marked Complete.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.9-agent-activity-streaming/03.9-RESEARCH.md
@.planning/phases/03.9-agent-activity-streaming/03.9-VALIDATION.md
@.planning/phases/03.9-agent-activity-streaming/03.9-03-SUMMARY.md
@.planning/phases/03.7-inbox-briefing/03.7-UAT.md

# The E2E precedent to copy — the JWT tenantId decode, the smokeRun.mjs CLI seam, the SMOKE:: drive.
# It ALSO has never run, for the same auth reason. Read its STATE.md entry (03.7-04) first.
@apps/web/e2e/cockpit-briefing.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline activity E2E + the full phase sweep</name>
  <files>apps/web/e2e/cockpit-activity.spec.ts, .planning/phases/03.9-agent-activity-streaming/03.9-VALIDATION.md</files>
  <action>
    **The spec.** Mirror `cockpit-briefing.spec.ts` structurally (ponytail rung 2 — it already solved
    every hard part of this harness; copy it, do not redesign):
    - Sign in, land on the workspace, drive a `SMOKE::agent::subject=Hello` (or `brief=today`) turn.
      That path is `generateText`-free, so it only produces steps because Plan 02 emitted around the
      smoke call site — this spec is the regression test for Pitfall 4.
    - Assert the ActivityCard's `LATEST TRACE` label is present and at least one `.trace-line` step
      row rendered with a real verb.
    - Assert the card contains ZERO subject/sender/body needles (reuse the fixture's
      `attacker@evil.example` needle if driving `brief=today`) — the UI-visible half of the §4 story
      that Plan 02's scans enforce structurally.
    - Windows: drive any `convex` CLI through `node` via the `smokeRun.mjs` convention, NEVER a shell
      — the tenantId contains a `|` that cmd.exe reads as a pipe. Read success from OUTPUT, not the
      exit code. (03.7-04's entry documents both.)

    **BE HONEST ABOUT WHAT THIS SPEC PROVES, and say it in the spec's own header comment.** A SMOKE
    turn is instant. This spec proves *steps render*; it CANNOT prove *steps render progressively
    during a wait* — the entire point of the phase. That belongs to Task 2's human-verify and to
    `runCockpitAgent.test.ts` (Plan 02), which is the LOAD-BEARING automated proof that the emitter
    fires at all. Do not let a green E2E here be mistaken for the phase being verified.

    **The carried-in auth blocker (03.7-04 / 03.7-UAT — read it, do not re-derive it).**
    `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` are absent from the env and every `.env*` in the repo and
    worktrees, AND `page.tsx:221` gates the composer on `gmailAuth.status.connected` with no smoke
    helper able to seed a Gmail token (it only arrives via real OAuth consent). So this spec needs a
    Gmail-CONNECTED seeded user. `cockpit-briefing.spec.ts` has never run for exactly this reason.
    - **DO NOT weaken the auth gate to force a pass.** Explicit standing instruction.
    - **DO NOT fabricate a workaround.**
    - Plan of record: run it in Task 2's live human-verify session, which has a connected user BY
      CONSTRUCTION. Run `pnpm --filter @pikar/web test:e2e -- cockpit-activity` there — and run
      `cockpit-briefing` too, which has been waiting on the same two blockers since 03.7-04 and gets
      satisfied for free.
    - If it still cannot run: the spec ships written-but-deferred and the SUMMARY says so plainly,
      naming `runCockpitAgent.test.ts` as the load-bearing automated proof. That is an acceptable,
      already-precedented outcome. Silently claiming E2E coverage is not.

    **Fill in `03.9-VALIDATION.md`'s Per-Task Verification Map** with every task across plans 01-04:
    task id, plan, wave, CKPT-05, test type, automated command, file, status. Set
    `nyquist_compliant: true` and `wave_0_complete: true` in its frontmatter once true (Plan 01
    created `agentSteps.test.ts` and this task creates the spec — the two Wave-0 gaps; watch.json was
    the third and landed in Plan 01 Task 1).

    **The full sweep** (research §Sampling rate, Phase gate). Run with the Next dev server DOWN —
    heavy vitest under a live dev server has twice OOM-killed it:
    - `pnpm --filter @pikar/backend test` — full suite. SOLE accepted red: the documented
      pre-existing `audit.test.ts` (STATE.md records it across 5+ plans — do NOT chase it).
      `runCockpitAgent.test.ts` 5000ms timeouts are machine-load noise that reproduce on base
      commits; re-run at `--testTimeout=30000` before believing any red there.
    - `pnpm --filter @pikar/backend typecheck` — source clean. Baseline noise: exit 2, ~29-30 errors,
      ALL in `.test.ts`. Diff against base; do not chase.
    - `pnpm --filter @pikar/web typecheck` — exit 0.
    - `node ./scripts/run-eval-golden.mjs --self-check` — exit 0 (offline, zero convex calls).
    - `node scripts/check-playbooks.mjs </dev/null` — exit 0.
    - `git diff --stat packages/backend/package.json packages/contracts/skills/` — EMPTY. Zero
      dependencies added, zero skill bodies touched ⇒ **no eval gate cycle owed** (research
      §Skill gate). If either is non-empty the phase drifted — STOP and report.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test && pnpm --filter @pikar/web typecheck && node scripts/check-playbooks.mjs </dev/null</automated>
    E2E: `pnpm --filter @pikar/web test:e2e -- cockpit-activity` — expected to be BLOCKED on the
    carried-in auth blocker until Task 2's live session. Playwright must at minimum type-load and
    DISCOVER the spec (the 03.7-04 bar): `pnpm --filter @pikar/web exec playwright test --list`.
  </verify>
  <done>
    The spec exists, type-loads, is discovered by Playwright, and its header states honestly what it
    does and does not prove. The sweep is green modulo the two documented pre-existing reds.
    VALIDATION.md's map is filled and `nyquist_compliant: true`. Zero deps, zero skill edits.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — the wait no longer reads as broken</name>
  <files>(none — verification only)</files>
  <action>PAUSE and hand the session to the user. Present <what-built>, then walk the numbered
  <how-to-verify> steps together on a live stack. This is a BLOCKING gate: CKPT-05 is a
  perceived-latency requirement and no automated test can confirm it, so Task 3 must not run until
  the user responds. Do not self-approve, do not infer approval from a green suite.</action>
  <verify>User types "approved" (or reports what they saw). Task 3 is blocked until then.</verify>
  <done>The user has explicitly approved, or has reported failures for gap-closure planning.</done>
  <what-built>
    The cockpit now shows what the agent is doing while it does it:
    - An append-only `agentSteps` row per tool call, emitted natively by `ai@7`'s
      `onToolExecutionStart`/`onToolExecutionEnd` (zero tool-wrapper edits, zero new dependencies),
      plus one driver-owned `thinking` row per turn so a turn that calls no tools still shows life.
    - A `LATEST TRACE` card in the workspace and an in-progress bubble in the chat, both fed by ONE
      tenant-scoped `agentSteps.latestTurn` query over Convex reactivity — no polling, no new transport.
    - Both agent entry points traced: `sendCockpitMessage` AND `resolveRecipients` (the 10–30s silent
      wait after a contact-chip click).
    - Step rows carry a closed tool-name union + a phase enum + numbers. There is NO free-text field
      — §4 is enforced by schema absence, and the display verbs live in the UI.
  </what-built>
  <how-to-verify>
    **Setup.** Start a persistent `npx convex dev` (127.0.0.1:3210) — required for anything live; a
    per-command cold start yields InternalServerError + banner-contaminated output. Then `pnpm dev`.
    If the dev server dies with exit 134 after ~45 min, that is V8's ~4GB heap leak — restart it, it
    is not a code bug.

    1. **The first-turn case — this one matters most, and needs a genuinely NEW thread.** Start a
       BRAND-NEW chat (not an existing thread) and send a first message that makes the agent work,
       e.g. "brief me on my inbox today". WATCH the workspace during the wait.
       - EXPECT: "Thinking…" appears within about a second, then step rows appear ONE BY ONE as the
         agent works — not all at once when the answer lands.
       - This is the exact case that fails if the trace is keyed on a threadId (there is none until
         the loop returns). If it is blank here, research Pitfall 1 is live — report it.
    2. **Progressive, not retroactive.** Same again on turn 2+. The steps must appear DURING the wait.
       If they all pop at the end, the surface is decorative and the phase did not work.
    3. **The contact-chip wait.** Trigger a name resolution ("email Sarah"), click a contact chip, and
       watch. That re-entry turn must show a trace too — not freeze silently.
    4. **The chat bubble.** The in-progress agent bubble should agree with the workspace card at all
       times (they share one query, so a disagreement is a real bug). The brain button in the composer
       should toggle the collapsible "Thought Process" trace — and must no longer say "coming soon".
    5. **Terminal state on failure.** Induce a failure (easiest: temporarily unset `OPENAI_API_KEY` in
       the **deployment env** — not `.env.local` — and send a message). EXPECT: the turn ends with an
       error reply and NO step is left spinning forever. Restore the key afterward.
    6. **Run the two E2Es** — this session has a Gmail-connected user by construction, which satisfies
       BOTH carried-in blockers for free:
       `pnpm --filter @pikar/web test:e2e -- cockpit-activity`
       `pnpm --filter @pikar/web test:e2e -- cockpit-briefing`   ← owed since 03.7-04
    7. **Taste calls to rule on** (research Open Questions — your call, not governance):
       - Does the settled trace persisting after the turn help ("what did it just do?") or is it clutter?
       - Are VERBS enough ("Reading your inbox…"), or do you want COUNTS ("Read 12 messages")? Counts
         are deliberately deferred: they exist only inside a tool's §4-hazardous return string, so
         they need an explicit per-tool count recorder, not a parser. The schema has an unwritten
         `count` field reserved for it.
       - Are the verbs the right words?
  </how-to-verify>
  <resume-signal>Type "approved" — or describe what you saw (especially: was the FIRST turn blank?)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark CKPT-05 Complete + close the phase</name>
  <files>.planning/REQUIREMENTS.md, docs/playbooks/cockpit.md</files>
  <action>
    ONLY after Task 2 is approved:
    - `.planning/REQUIREMENTS.md`: check the CKPT-05 box (`- [x]`) and flip its traceability row to
      `| CKPT-05 | Phase 3.9 | Complete |` with the human-verify date, following the CKPT-03 row's
      wording precedent.
    - `docs/playbooks/cockpit.md`: bump `Last verified` to `03.9-04` and record any gap the
      human-verify surfaced under its Gaps section (e.g. counts deferred to a follow-up; a verb the
      user wants reworded).
    - If the human-verify turned up FAILURES rather than taste notes, do NOT mark Complete. Report
      them for gap-closure planning (`/gsd:plan-phase 03.9 --gaps`) instead.

    **Windows tooling gotchas — use the workarounds, do not fight the tools** (recorded in memory
    and STATE.md): `gsd-tools commit` has broken quoting → use plain `git commit -m`.
    `roadmap update-plan-progress` and `state advance-plan` misbehave on decimal phases → hand-edit
    the ROADMAP's `**Plans:**` line and STATE.md.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs </dev/null</automated>
    Plus: `grep -n "CKPT-05" .planning/REQUIREMENTS.md` shows the box checked and the row Complete.
  </verify>
  <done>
    CKPT-05 is Complete and dated, cockpit.md's Last verified reads 03.9-04, and any taste-note
    follow-ups are recorded as gaps rather than silently dropped.
  </done>
</task>

</tasks>

<verification>
- Full backend suite green modulo the documented pre-existing `audit.test.ts`.
- Both typechecks exit 0 (backend source-clean; web fully clean).
- `run-eval-golden.mjs --self-check` exit 0; `check-playbooks` exit 0.
- Zero new dependencies; zero skill-body edits; no eval gate cycle owed.
- Human approved the perceived-latency behavior, INCLUDING the first-turn case on a new thread.
</verification>

<success_criteria>
- A human watched a real agent turn and the steps appeared progressively, during the wait.
- The FIRST turn of a NEW thread was not blank.
- A failed turn left nothing spinning.
- CKPT-05 is Complete.
- Honest accounting in the SUMMARY of what remains unproven and why (research §Metadata bar).
</success_criteria>

<output>
After completion, create `.planning/phases/03.9-agent-activity-streaming/03.9-04-SUMMARY.md`
</output>
