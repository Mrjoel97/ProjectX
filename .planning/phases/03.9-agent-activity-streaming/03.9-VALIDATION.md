---
phase: 03.9
slug: agent-activity-streaming
status: active
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-17
updated: 2026-07-17
---

# Phase 03.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (workspace-wide, installed) + Playwright (apps/web e2e) |
| **Config file** | packages/backend/vitest.config.ts, packages/core/vitest.config.ts, apps/web/playwright.config.ts |
| **Quick run command** | `pnpm --filter @pikar/backend test -- <file>` |
| **Full suite command** | `pnpm test` (workspace) |
| **Estimated runtime** | ~30–60 seconds (unit); E2E gated on live stack |

---

## Sampling Rate

- **After every task commit:** scoped vitest file for the touched package
- **After every plan wave:** `pnpm test` (workspace full suite)
- **Before `/gsd:verify-work`:** full suite green
- **Max feedback latency:** 60 seconds (unit); E2E excluded per the carried-in auth blocker below

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File | Status |
|---------|------|------|-------------|-----------|-------------------|------|--------|
| 01-T1 — `agentSteps` table + adapter (`record`/`finish`/`latestTurn`) + watch.json | 01 | 1 | CKPT-05 | unit | `pnpm --filter @pikar/backend test -- agentSteps` | `convex/agentSteps.test.ts` (new) | ✅ green (10/10) |
| 01-T2 — mint CKPT-05 (count 47→48) | 01 | 1 | CKPT-05 | docs/grep | `grep -c "CKPT-05" .planning/REQUIREMENTS.md` → 4 | `.planning/REQUIREMENTS.md` | ✅ green |
| 02-T1 — SDK-callback emission + the SMOKE call site | 02 | 2 | CKPT-05 | integration | `pnpm --filter @pikar/backend test -- runCockpitAgent` | `convex/runCockpitAgent.test.ts` | ✅ green (16/16) |
| 02-T2 — turn lifecycle in both drivers (`thinking` row + `finally`) | 02 | 2 | CKPT-05 | integration | `pnpm --filter @pikar/backend test -- runCockpitAgent` | `convex/runCockpitAgent.test.ts` | ✅ green |
| 02-T3 — §4 static scans (all mutation-checked) | 02 | 2 | CKPT-05 | static scan | `pnpm --filter @pikar/backend test -- llmRedaction` | `convex/llmRedaction.test.ts` | ✅ green (26/26) |
| 03-T1 — `ActivityCard` + the third independent `useQuery` | 03 | 3 | CKPT-05 | typecheck + structural grep | `pnpm --filter @pikar/web typecheck` | `workspace/cards.tsx` | ✅ green (exit 0) |
| 03-T2 — in-progress bubble + brain button + streaming-note fix | 03 | 3 | CKPT-05 | typecheck + grep guards | `pnpm --filter @pikar/web typecheck` | `workspace/ChatPane.tsx` | ✅ green (exit 0) |
| 04-T1 — offline activity E2E + the full phase sweep | 04 | 4 | CKPT-05 | e2e | `pnpm --filter @pikar/web test:e2e -- cockpit-activity` | `apps/web/e2e/cockpit-activity.spec.ts` (new) | ⚠️ **written, type-loads, DISCOVERED by Playwright — but BLOCKED from running** on the carried-in auth blocker below. Deferred to 04-T2's live session. |
| 04-T2 — human-verify (perceived latency; **blocking gate**) | 04 | 4 | CKPT-05 | manual only | none possible — see Manual-Only Verifications | — | ⬜ pending — awaiting the user |
| 04-T3 — mark CKPT-05 Complete + close the phase | 04 | 4 | CKPT-05 | playbook gate + grep | `node scripts/check-playbooks.mjs </dev/null` | `.planning/REQUIREMENTS.md`, `docs/playbooks/cockpit.md` | ⬜ blocked on 04-T2 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky/blocked*

**Phase-gate sweep at 04-T1** (research §Sampling rate): backend suite **271/272** (sole red = the
documented pre-existing `audit.test.ts`, unchanged across 6+ plans — do not chase); backend source
typecheck **clean** (30 errors, ALL in `.test.ts` = the documented baseline); web typecheck **exit
0**; `run-eval-golden.mjs --self-check` **exit 0** (the runner lives at
`packages/backend/scripts/`, not the repo root as the plan stated); `check-playbooks` **exit 0**;
`git diff` over `packages/backend/package.json` + `packages/contracts/skills/` across the whole
phase (`c014f9c..HEAD`) **EMPTY** ⇒ zero new dependencies, zero skill-body edits, **no eval gate
cycle owed**.

**Which automated proof is load-bearing, and which is not.** `runCockpitAgent.test.ts` (02-T1/T2)
is the load-bearing one: `ai@7`'s `notify` **swallows callback exceptions**
(`dist/index.js:2636-2639`), so a broken emitter produces no error, no log and no red test — it
just silently renders nothing in production. Only a test that drives real `generateText` and
asserts the rows exist can catch that. The E2E (04-T1) proves something narrower: that step rows
reach the browser and render as words, and that the SMOKE call site still emits (research Pitfall
4 — every offline E2E in the repo drives the `generateText`-free SMOKE path). **A green E2E is not
this phase verified**; a SMOKE turn is instant, so it only ever sees a settled trace.

---

## Wave 0 Requirements

**Complete** (`wave_0_complete: true`). Three gaps were identified; all three landed:

- [x] `packages/backend/convex/agentSteps.test.ts` — the adapter's unit tests (Plan 01 Task 1).
- [x] `docs/playbooks/watch.json` — `agentSteps.ts` + `.test.ts` registered under `cockpit.md`
      (Plan 01 Task 1), or §9's Stop hook blocks the turn that creates them. `apps/web/e2e/` was
      already a `cockpit.md` prefix, so the new spec needed no entry (the 03.7-04 precedent).
- [x] `apps/web/e2e/cockpit-activity.spec.ts` — the offline E2E (Plan 04 Task 1). Depended on
      Pitfall 4 being solved first (the SMOKE path must emit — Plan 02 Task 1) or it would have
      asserted on an empty surface.

No framework install was needed — vitest is installed in every touched package, the
`llmRedaction.test.ts` static-scan family existed to extend, and the Playwright harness existed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Steps appear DURING a real 10–30s agent turn | CKPT-05 | Perceived-latency behavior — a passing unit test cannot show that the wait stopped feeling broken; this is the whole point of the phase | Ask for a briefing in the cockpit and WATCH the workspace during the wait: steps must appear progressively, not all at once on completion |
| **First-turn** trace renders (research Pitfall: no `threadId` until the loop returns) | CKPT-05 | The E2E signs in and reuses a thread; only a genuinely NEW thread exercises the no-threadId window | Start a BRAND-NEW thread and send the first message — the trace must appear during that first wait, not stay blank |
| Trace terminal state on failure | CKPT-05 | Requires inducing a live tool failure | A failed/errored turn must leave no step spinning forever |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — the sole exception is 04-T2, the
      human-verify, which is manual **by nature**: CKPT-05 is a perceived-latency requirement and no
      automated test can say the wait stopped feeling broken. That is why it is a blocking gate.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — all three gaps closed (above)
- [x] No watch-mode flags
- [x] Feedback latency < 60s (unit ~40s full-suite; E2E excluded per the carried-in blocker)
- [x] `nyquist_compliant: true` set in frontmatter

**Known carried-in blocker (from 03.7-04 / 03.7-UAT):** any new cockpit E2E needs a **Gmail-connected**
seeded user (`E2E_USER_EMAIL`/`E2E_USER_PASSWORD`); the composer is gated on
`gmailAuth.status.connected` and no smoke helper can seed a token. Do NOT weaken the auth gate to
force a pass. Either run it in the same live session as `cockpit-briefing.spec.ts`, or make the
offline integration test load-bearing and SAY SO explicitly in the plan.

**Status of that blocker at 04-T1 (re-checked, not re-derived): UNRESOLVED, unchanged.**
`E2E_USER_EMAIL`/`E2E_USER_PASSWORD` are still absent, and the composer gate is untouched. Nothing
was weakened and no workaround was fabricated. Both dispositions the plan allowed are therefore in
play, in this order:
1. **Plan of record** — run both specs in 04-T2's live human-verify session, which has a
   Gmail-connected user BY CONSTRUCTION and satisfies both blockers for free:
   `pnpm --filter @pikar/web test:e2e -- cockpit-activity` and `-- cockpit-briefing` (the latter
   owed since 03.7-04). **NOTE:** Playwright's `baseURL` is `http://127.0.0.1:3111`, but the live
   dev server for the human-verify runs on **:3000** — the E2E needs a :3111 instance or a
   `baseURL` override, or it will fail for a reason that has nothing to do with the feature.
2. **Fallback, already precedented and acceptable** — the spec ships written-but-deferred, and
   `runCockpitAgent.test.ts` is named as the load-bearing automated proof. Silently claiming E2E
   coverage is not acceptable.

**Approval:** pending — 04-T2's human-verify has not been run. CKPT-05 stays **Pending**.
