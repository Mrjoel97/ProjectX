---
phase: 03.9-agent-activity-streaming
verified: 2026-07-17T00:00:00Z
status: passed
score: 20/20 must-haves verified (4 plans, all truths/artifacts/key-links)
---

# Phase 03.9: Agent Activity Streaming Verification Report

**Phase Goal:** The cockpit stops looking frozen — while the Executive Agent runs its tool loop, the
workspace shows the steps it is taking as they happen (reading the inbox, summarizing, drafting), and
the chat renders in-progress bubbles, so a user never mistakes a working agent for a hung system.
Cross-cutting: applies to every agent flow (briefing, send, attachment, personalization), not one
feature.

**Verified:** 2026-07-17
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (by plan)

**03.9-01 — the data plane**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A step row can be recorded as running and later patched terminal, scoped to one tenant | ✓ VERIFIED | `agentSteps.ts` `record`/`finish`, both `internalMutation`s writing/patching by `(tenantId, turnId, stepKey)`; `agentSteps.test.ts` 10/10 green (re-run live) |
| 2 | The newest turn's steps can be read with no threadId argument (the first-turn window) | ✓ VERIFIED | `latestTurn = tenantQuery({ args: {} ... })` in `agentSteps.ts:103` — no threadId param; uses `by_tenant` index (`["tenantId"]` alone — a deliberate fix over the research draft's `by_thread` prefix bug, documented in schema.ts comment) |
| 3 | Tenant B can never read tenant A's steps | ✓ VERIFIED | `latestTurn` is a `tenantQuery` scoped via `ctx.tenantId` (CLAUDE.md §2 wrapper); cross-tenant test in `agentSteps.test.ts` |
| 4 | CKPT-05 exists as a v1 requirement traceable to Phase 3.9 | ✓ VERIFIED | `REQUIREMENTS.md:50` (definition, now `[x]`), `:166` (traceability row → Phase 3.9), count line `:196` reads 48 |

**03.9-02 — loop emission + turn lifecycle**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 5 | A real agent turn leaves step rows — one thinking row plus one row per tool call | ✓ VERIFIED | `llm.ts:1280-1300` `onToolExecutionStart`/`onToolExecutionEnd` call `internal.agentSteps.record`/`finish`; `cockpit.ts:78-113` mints `turnId` + records/finishes a `thinking` row in both drivers; `runCockpitAgent.test.ts` 16/16 green live |
| 6 | A step that starts always ends: success, tool error, model failure, governed stop | ✓ VERIFIED | `cockpit.ts` both drivers wrap the agent call in `try/catch/finally`, finishing in `finally` (covers the governed-stop early-return, not just the catch) — grep-confirmed at `:104` and `:194` |
| 7 | A fallback retry's re-run appears honestly as its own steps rather than being hidden | ✓ VERIFIED | `record` is insert-only/append-only (no de-dup); SUMMARY documents the fallback test's scope honestly |
| 8 | No mail content can reach a step row — enforced by a mutation-checked static scan | ✓ VERIFIED | `llmRedaction.test.ts` 26/26 green live (incl. 3 new §4 scans: schema allow-list, callback-hazard scan, log-plane-free scan); SUMMARY records all 3 independently mutation-checked to RED then restored byte-identical |
| 9 | The offline SMOKE path emits steps too | ✓ VERIFIED | `llm.ts:1533-1556` — SMOKE call site records/finishes in a `finally`; live E2E run confirms 2 rows for `brief=today` |

**03.9-03 — the render surfaces**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 10 | Workspace lists the steps the agent is taking, in plain verbs | ✓ VERIFIED | `cards.tsx` `VERB`/`stepText` + `ActivityCard` rendering `.trace-line` per step; `data-testid="activity-card"` present |
| 11 | The trace renders on the FIRST message of a brand-new thread (no threadId yet) | ✓ VERIFIED | `cards.tsx:804` `useQuery(api.agentSteps.latestTurn)` (no args) rendered above `rest()` which contains the `!threadId` (`:819`) and `plan === null` (`:823`) returns — structurally outside both early-return gates |
| 12 | The trace renders while plan status is 'collecting' | ✓ VERIFIED | Trace (`{trace}`) renders unconditionally in `CardList`'s return (`:829`), independent of `PlanCards`' status dispatch (extracted to a sibling function so the status branch cannot gate it) |
| 13 | Chat shows an in-progress agent bubble sourced from the same rows as the workspace | ✓ VERIFIED | `ChatPane.tsx:112` — the identical `useQuery(api.agentSteps.latestTurn)` call, `data-testid="agent-activity"` bubble |
| 14 | A screen-reader user hears progress; nothing encoded in color alone | ✓ VERIFIED | `ActivityCard` carries `aria-live="polite"`; `error` steps render as text ("— couldn't complete this step"), no color-only state, no amber (reserved for the approval gate) |
| 15 | A step stuck running for ~90s reads as possibly stalled | ✓ VERIFIED | `stepText` compares `Date.now()` against `startedAt` with a `STALE_MS` (90s) threshold, computed at render (no ticker/watchdog) |

**03.9-04 — the phase gate**

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 16 | Whole spine green offline: rows, emission, terminal states, §4 scans, both typechecks | ✓ VERIFIED | Live re-run: backend 271/272 (sole red = documented pre-existing `audit.test.ts` — `Component "auditCounts" is not registered`, unrelated to this phase); `pnpm --filter @pikar/web typecheck` exit 0; `check-playbooks.mjs` exit 0 |
| 17 | A human watched a real 10-30s turn and the wait no longer reads as broken | ✓ VERIFIED (human) | User explicitly approved 2026-07-17, after 4 checkpoint-feedback fixes for a WorkspacePage crash the verify uncovered (not the trace feature) |
| 18 | A human watched a brand-NEW thread's FIRST turn and the trace was not blank | ✓ VERIFIED (human) | Same approval; SUMMARY records the trace observed live rendering ("Briefed your inbox · 43.6s") |
| 19 | A failed turn left no step spinning forever | ✓ VERIFIED | Structural guarantee (`finally` in both drivers, terminal on tool-error) + human-verify step 5 in the approved checkpoint |
| 20 | CKPT-05 is Complete only after the human said so | ✓ VERIFIED | `REQUIREMENTS.md:50` checkbox is `[x]`; `:166` traceability row reads `Complete (human-verified 2026-07-17, after 4 checkpoint-feedback fixes...)` |

**Score:** 20/20 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/backend/convex/schema.ts` | `agentSteps` table, closed tool union, phase enum, numbers, NO free-text field | ✓ VERIFIED | Fields: tenantId, threadId, turnId, stepKey, tool (closed union), phase (enum), startedAt, endedAt, durationMs, count — nothing else. Indexes `by_turn` and `by_tenant` (not `by_thread` — deliberate correction, documented) |
| `packages/backend/convex/agentSteps.ts` | `record`/`finish` internalMutations + `latestTurn` tenantQuery | ✓ VERIFIED | All three exported exactly as specified; `latestTurn` takes `args: {}` |
| `packages/backend/convex/agentSteps.test.ts` | adapter unit tests incl. cross-tenant isolation | ✓ VERIFIED | 10/10 green (live re-run) |
| `.planning/REQUIREMENTS.md` | CKPT-05 minted, then flipped Complete | ✓ VERIFIED | Checkbox `[x]`, traceability row `Complete`, count 48 |
| `packages/backend/convex/llm.ts` | emission from `onToolExecutionStart`/`onToolExecutionEnd` via `ctx.runMutation`; SMOKE-path emission | ✓ VERIFIED | Both callbacks present at `:1280`/`:1295`; SMOKE emission at `:1533-1556`; grep confirms zero hits inside `invokeTool`/`buildCockpitTools` |
| `packages/backend/convex/cockpit.ts` | turn lifecycle in both drivers, terminal via `finally`; `listThreads` numItems 10 | ✓ VERIFIED | `sendCockpitMessage` (`:78-113`) and `resolveRecipients` (`:173-198`) both mint `turnId` + `finally`-terminalize; `listThreads` (`:256`) `numItems: 10` |
| `apps/web/.../cards.tsx` | ActivityCard/LATEST TRACE above `!threadId` and `plan === null` early returns | ✓ VERIFIED | `activity` query + `trace` render at `:794-830`, structurally above `rest()`'s early returns |
| `apps/web/.../ChatPane.tsx` | `setText("")` before await; Send-button busy spinner | ✓ VERIFIED | `:136` clears before `:138` await, restores on catch (`:141`); spinner `.btn-spinner` + `aria-busy` at `:314-332` |
| `apps/web/.../ErrorBoundary.tsx` | exists; isolates `listThreads` | ✓ VERIFIED | Minimal class component; wraps `PastChats` in `page.tsx:207-209`, degrades to `PastChatsFallback` |
| `apps/web/.../page.tsx` | single `sending` flag passed to both ChatPane and CardList | ✓ VERIFIED | `page.tsx:148` single `useState`, passed at `:257` and `:309` |
| `docs/playbooks/agent-runtime.md` | invariants 10 + 11 | ✓ VERIFIED | Invariant 10 (toolless-ingestion, pre-existing, carried from 03.7) and invariant 11 (activity-trace, new, `:84`) both present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `agentSteps.ts` | `convex/lib/functions.ts` | `tenantQuery` import | ✓ WIRED | `import { tenantQuery } from "./lib/functions"` |
| `docs/playbooks/watch.json` | `agentSteps.ts` | cockpit.md registration | ✓ WIRED | Both `agentSteps.ts` and `.test.ts` registered |
| `llm.ts` | `internal.agentSteps.record`/`finish` | `ctx.runMutation` in callbacks | ✓ WIRED | 4 call sites (2 callbacks + 2 smoke) |
| `cockpit.ts` | `internal.agentSteps.finish` | `finally` block, both drivers | ✓ WIRED | 2 sites, each in `finally` |
| `cards.tsx` | `api.agentSteps.latestTurn` | `useQuery`, no args, above guards | ✓ WIRED | Confirmed structurally above both early returns |
| `ChatPane.tsx` | `api.agentSteps.latestTurn` | same query as cards.tsx | ✓ WIRED | Identical query, `data-testid="agent-activity"` bubble |
| `apps/web/e2e/cockpit-activity.spec.ts` | `SMOKE::agent::` sentinel | Plan 02 smoke-site emission | ✓ WIRED (structurally; not executed) | Spec type-loads, Playwright discovers it (`--list` confirms), correct test-ids referenced; blocked from running by the pre-existing (03.7-04) Gmail-auth-seed gap, not by this phase |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|--------------|--------|----------|
| CKPT-05 | 03.9-01 (minted), 03.9-02/03 (implementation), 03.9-04 (closed) | Workspace shows agent steps live; chat shows in-progress bubble; append-only step rows, no free text; terminal on all outcomes | ✓ SATISFIED | `REQUIREMENTS.md` checkbox `[x]`, traceability row `Complete (human-verified 2026-07-17)`; no orphaned requirements found for Phase 3.9 |

No orphaned requirements found — CKPT-05 is the sole ID mapped to Phase 3.9, and it appears in all 4 plans' `requirements:` frontmatter.

### Anti-Patterns Found

None found in the phase's modified files. Specifically checked and clear:
- No `TODO`/`FIXME`/`PLACEHOLDER` in `agentSteps.ts`, `ErrorBoundary.tsx`, or the new callback blocks in `llm.ts`.
- No stub returns (`return null`/`return {}` as a full implementation) in any of the phase's artifacts.
- The one item that historically would have been a "coming soon" anti-pattern (`ChatPane.tsx`'s brain button) was fixed in this phase — `grep -c "coming soon"` confirmed 0.
- Two stale ponytail comments (welded-shut streaming-upgrade doors in `cockpit.ts` and `ChatPane.tsx`) were corrected per plan, confirmed by `grep -c "useUIMessages\|vStreamArgs"` = 0 in both.

### Human Verification Required

None outstanding. The phase's single human-verify checkpoint (03.9-04 Task 2) was already run and explicitly approved by the user on 2026-07-17, after four checkpoint-feedback fixes addressed a WorkspacePage crash the verify uncovered (a slow `listThreads` query throwing inside the page — not the trace feature, which was independently observed rendering correctly live). No further human action is required for this phase to be considered goal-achieved.

### Known, Documented Gap (not a failure)

`apps/web/e2e/cockpit-activity.spec.ts` exists, type-loads, and is discovered by Playwright (`--list` confirms it), but has never executed — a carried-in blocker since 03.7-04 (needs a Gmail-connected seeded user; `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` absent from the environment; the composer is gated on a token no smoke helper can seed). This was not weakened or worked around — the spec's own header comment states this honestly and names `runCockpitAgent.test.ts` as the load-bearing automated proof that the emitter fires. This is consistent with the phase's own SUMMARY accounting and does not block the "passed" status.

### Gaps Summary

None. All must-haves across the four plans are verified live against the current codebase (not merely against SUMMARY claims): the schema enforces §4 by field absence, the emitter fires from native SDK callbacks with zero tool-wrapper edits, both cockpit drivers terminalize step state in `finally` blocks covering all reachable death modes, the first-turn and plan-status-gating traps are both structurally avoided in the UI, the WorkspacePage crash discovered during human-verify was fixed and confirmed in source, and CKPT-05 is marked Complete with a valid traceability row. The sole known gap (the offline E2E never executing) is pre-existing, honestly documented, and explicitly out of this phase's control.

---

_Verified: 2026-07-17_
_Verifier: Claude (gsd-verifier)_
