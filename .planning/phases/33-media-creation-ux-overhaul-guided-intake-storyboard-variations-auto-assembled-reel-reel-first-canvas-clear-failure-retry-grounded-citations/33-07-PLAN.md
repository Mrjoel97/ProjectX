---
phase: 33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations
plan: 07
type: execute
wave: 7
depends_on: ["33-06"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/mediaCanvasView.ts
  - apps/web/app/(app)/dashboard/workspace/mediaCanvas.test.ts
  - apps/web/app/(app)/dashboard/workspace/MediaCanvas.tsx
  - docs/playbooks/media.md
autonomous: true
requirements: [33-INTAKE, 33-VARIA]

must_haves:
  truths:
    - "The canvas shows the parsed brief as editable chips before any storyboard — two surfaces, one brief, and the canvas is the source of truth for what was captured"
    - "Only topic + duration are required; audience/tone/brand chips render as 'defaulted' when the profile is thin (the Phase-11 rule) — nothing blocks on fields an idea-stage tenant can't fill"
    - "Duration is a 15/30/60 preset chip; 60s shows its higher cost up front; free entry is impossible"
    - "Editing a chip after storyboards exist shows the 'brief changed' badge and a free re-propose button; nothing auto-fires"
    - "Two variations render side by side, switchable until Generate; after Generate the switcher is gone"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/mediaCanvasView.ts"
      provides: "briefChips, deckStale, variationView derivations"
      exports: ["briefChips", "deckStale", "variationView"]
    - path: "apps/web/app/(app)/dashboard/workspace/MediaCanvas.tsx"
      provides: "chips row, stale badge + re-propose button, A/B variation switcher"
  key_links:
    - from: "re-propose button"
      to: "cockpit dispatch"
      via: "the existing chat send path (useSendCockpitMessage) with a canned user message — never a second dispatch entry"
      pattern: "useSendCockpitMessage"
    - from: "duration chip"
      to: "TARGET_DURATIONS"
      via: "renders the exported preset constant — no free-entry field"
      pattern: "TARGET_DURATIONS"
---

<objective>
Build the guided-intake and variation surfaces on the canvas: brief chips (with defaulted
markers and preset durations), the stale-deck badge with free re-propose, and the side-by-side
two-deck switcher.

Purpose: 33-INTAKE and 33-VARIA's user-facing halves, over the state and mutations shipped in
33-02/33-03.

Output: chips + switcher live on the canvas, derivations tested by calling.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-CONTEXT.md
@docs/design/BRAND.md
@docs/playbooks/media.md
@apps/web/app/(app)/dashboard/workspace/mediaCanvasView.ts
@apps/web/app/(app)/dashboard/workspace/useSendCockpitMessage.ts

<interfaces>
Available state and mutations (from 33-02/33-03):

```typescript
// plans row: brief { topic, durationSeconds, audience?, tone?, brandVoice?, defaulted: string[] },
//   briefChangedAt?, deckProposedAt?, altShots?, altTargetDurationSeconds?, deckLockedAt?
// media.editBrief(planId, patch)  → { ok } | { ok:false, reason: "illegal_duration"|"deck_locked" }
// media.switchDeck(planId)       → { ok } | { ok:false, reason: "no_alternate"|"deck_locked" }
// @pikar/core: TARGET_DURATIONS = [15, 30, 60]
// apps/web workspace: useSendCockpitMessage.ts — the chat send hook the cockpit input uses;
//   the re-propose button reuses it (token spend stays literally user-initiated).
// mediaCanvasView: KIND_COST_NOTE — the cost-discoverability copy idiom for the 60s note.
```
</interfaces>
</context>

<constraints>
- Chip visual design is Claude's discretion — but only globals.css tokens, BRAND.md patterns, no
  component library, no new dependency.
- The re-propose turn MUST go through the existing chat send path with a canned message (e.g.
  "Re-propose storyboards for the updated brief") — a second UI→dispatch entry point is the
  named anti-pattern (research Open Question 3).
- Editing a chip fires editBrief ONLY — never a proposal; the badge + button are the whole
  affordance (nothing auto-fires).
- No logic in the .tsx; derivations + copy in mediaCanvasView.ts.
- Commit only owned files with explicit paths; check `.git/MERGE_HEAD` first.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: briefChips, deckStale, variationView derivations</name>
  <files>apps/web/app/(app)/dashboard/workspace/mediaCanvasView.ts, apps/web/app/(app)/dashboard/workspace/mediaCanvas.test.ts</files>
  <behavior>
    - `briefChips(brief, deckLocked)` → ordered chip models: topic (required, editable), duration
      (preset options TARGET_DURATIONS, current value, a cost note on 60s via the KIND_COST_NOTE
      idiom), audience/tone/brandVoice (each { value, defaulted: boolean } — defaulted chips show
      a "from your profile" marker and are still editable; absent + not defaulted renders an
      empty-but-never-blocking chip per the Phase-11 rule). All chips read-only when deckLocked.
    - `deckStale(briefChangedAt, deckProposedAt)` → boolean: true iff both present and
      briefChangedAt > deckProposedAt. Copy constant for the badge ("Brief changed — storyboards
      may be out of date") and for the free re-propose button label.
    - `variationView(plan)` → { hasAlternate: boolean, locked: boolean, canSwitch: boolean } +
      the side-by-side summary model for the alternate deck (scene count, kind mix, duration —
      derived from altShots the same way the picked deck's summary derives from shots).
  </behavior>
  <action>
    Pure functions + copy constants in mediaCanvasView.ts. Tests: defaulted marker logic, locked
    read-only state, stale boundary conditions (absent stamps → false), alternate summary parity.
  </action>
  <verify>
    <automated>pnpm --filter web test -- mediaCanvas</automated>
  </verify>
  <done>All three derivations fully covered by calling; no string literals for these surfaces appear in the .tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Chips row + stale badge + re-propose in MediaCanvas.tsx</name>
  <files>apps/web/app/(app)/dashboard/workspace/MediaCanvas.tsx</files>
  <action>
    Render the chips row above the hero (the brief is what the deck answers — it reads first):
    inline edit per chip (text input for topic/audience/tone/brandVoice; a three-option preset
    control for duration built from TARGET_DURATIONS) committing via media.editBrief; surface the
    refusal reasons as the existing sentence pattern. Show the stale badge + "Re-propose (free)"
    button when deckStale — the button sends the canned message through useSendCockpitMessage
    (same hook instance/path the chat input uses; if the hook lives in the page, lift the send
    callback down as a prop rather than instantiating a second sender). Keyboard/label
    accessibility per BRAND §6.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && pnpm --filter web test -- mediaCanvas</automated>
  </verify>
  <done>Chips render and edit against the live mutation; stale badge appears after a chip edit; re-propose sends one canned chat message and nothing else.</done>
</task>

<task type="auto">
  <name>Task 3: Side-by-side variation switcher</name>
  <files>apps/web/app/(app)/dashboard/workspace/MediaCanvas.tsx</files>
  <action>
    When variationView.hasAlternate: render an A/B compare region between chips and hero — the
    PICKED deck (full existing strip) beside/above a compact alternate summary card (from
    variationView's model: concept line from the alternate's first scene description, scene
    count, kind mix, duration) with a "Switch to this storyboard" action calling
    media.switchDeck. After Generate (locked), the region collapses to nothing — the switcher
    and summary are simply absent. No cross-deck scene mixing UI (deferred by decision — the
    per-scene editor covers it by hand). Estimate reactivity needs no work: switching swaps
    plans.shots and jobEstimate re-derives.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && pnpm --filter web test -- mediaCanvas</automated>
  </verify>
  <done>Two decks visible side by side pre-Generate; switch works and the estimate follows; post-Generate the alternate is gone from state AND from the UI.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web test -- mediaCanvas` green; `npx tsc --noEmit` in apps/web and packages/backend.
- Playbook same-commit: chips contract (required fields, defaulted markers, preset-only
  duration), re-propose path, switcher lifecycle; `Last verified` bumped.
</verification>

<success_criteria>
- The full intake loop works without a storyboard existing yet, never blocks a thin profile, and
  never fires a token-spending turn without a click.
- Variations are visible, comparable, switchable, and gone after Generate.
</success_criteria>

<output>
After completion, create `.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-07-SUMMARY.md`
</output>
