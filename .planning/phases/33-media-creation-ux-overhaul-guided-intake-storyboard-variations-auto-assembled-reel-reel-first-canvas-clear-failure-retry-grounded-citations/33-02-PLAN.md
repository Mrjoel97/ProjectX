---
phase: 33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/media.ts
  - packages/backend/convex/media.test.ts
  - docs/playbooks/media.md
autonomous: true
requirements: [33-INTAKE, 33-VARIA, 33-CITE]

must_haves:
  truths:
    - "Editing a brief chip patches only the brief plane and stamps briefChangedAt — the deck's own money contract (targetDurationSeconds, shots) is untouched"
    - "Switching decks swaps shots and altShots atomically, stamps shotsChangedAt, and is refused after Generate locks the choice"
    - "Confirming a claim writes confirmedAt from the authenticated context; the mutation accepts no actor or timestamp argument"
    - "Editing a confirmed scene's narration clears its confirmation — a changed claim is unconfirmed"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "brief plane, variation plane, per-shot citation fields, renderRetriedAt, reelVaultDocId — all optional, widen-only"
      contains: "briefChangedAt"
    - path: "packages/backend/convex/media.ts"
      provides: "editBrief, switchDeck, confirmClaim tenant mutations"
      exports: ["editBrief", "switchDeck", "confirmClaim"]
  key_links:
    - from: "confirmClaim"
      to: "plans.shots[i].confirmedAt"
      via: "ctx-derived identity, args carry only planId + sceneIndex"
      pattern: "confirmClaim"
    - from: "switchDeck"
      to: "shotsChangedAt"
      via: "structural-edit stamp on swap"
      pattern: "shotsChangedAt"
---

<objective>
Land the new plan-row state Phase 33 needs (brief plane, two-deck variation plane, per-scene
citation/confirmation fields, render-retry marker, saved-reel ref) plus the three governed tenant
mutations that write it: `editBrief` (chip edits), `switchDeck` (variation switch until Generate),
`confirmClaim` (the provenance rule's front door).

Purpose: every later plan reads or writes this state. The critical invariants are (a) the money
path never learns variations exist — `plans.shots` stays "the picked deck"; (b) the model has no
write path to a confirmation.

Output: widened schema + three mutations + mutation-observed tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-RESEARCH.md
@docs/playbooks/media.md
@packages/backend/convex/schema.ts
@packages/backend/convex/media.ts

<interfaces>
Verified on disk:

```typescript
// schema.ts plans row (media plane, ~:500-613):
targetDurationSeconds: v.optional(v.number());     // scene-deck discriminator + money contract
shots: v.optional(v.array(v.object({ index, type?, visual?, seconds, windowStartMs, description, overlay?, prompt, narration, asset? })));
shotsChangedAt: v.optional(v.number());            // structural-writes-only staleness stamp
renderStatus / renderStorageId / captionStatus ... // render + caption planes

// schema.ts:120 provenance idiom (verbatim comment): "Required when author === 'user';
// derived from authenticated identity, never from args."

// media.ts:
export function sceneDeckOf(plan): { targetDurationSeconds, scenes: Scene[] } | null; // :1798 — reads plan.shots ONLY
async function patchShots(ctx, planId, next): Promise<void>; // :2492 — the five editor mutations' shared write path; renumbers + running-sum offsets; stamps structural edits
```

Tenant wrappers come from `convex/lib/functions.ts` (`tenantMutation`/`tenantQuery`) — raw
query/mutation imports are BANNED (CLAUDE.md §2).
</interfaces>
</context>

<constraints>
- Schema changes are WIDEN-ONLY (every new field optional) — required members fail the push
  against existing rows (the renderSummary precedent).
- Never patch `plans.targetDurationSeconds` from a brief edit: the deck keeps its own validated
  contract; a mismatch between brief and deck is exactly what the stale badge expresses.
- CRLF playbook edit discipline; commit only owned files; `graphify update .` + extract-convex-edges after.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Widen the plans schema (brief / variation / citation / retry / vault-ref planes)</name>
  <files>packages/backend/convex/schema.ts</files>
  <action>
    On the `plans` table, all optional:
    - Brief plane: `brief: v.optional(v.object({ topic: v.string(), durationSeconds: v.number(), audience: v.optional(v.string()), tone: v.optional(v.string()), brandVoice: v.optional(v.string()), defaulted: v.array(v.string()) }))`,
      `briefChangedAt: v.optional(v.number())`, `deckProposedAt: v.optional(v.number())`.
      Comment: brief.durationSeconds is the USER'S ask (a TARGET_DURATIONS preset); the deck's own
      `targetDurationSeconds` remains the money contract — divergence renders as the stale badge,
      never as an estimate refusal.
    - Variation plane: extract the existing `shots` array-element validator into a shared const
      (single source — do not hand-copy the object) and add `altShots` using it, plus
      `altTargetDurationSeconds: v.optional(v.number())` and `deckLockedAt: v.optional(v.number())`.
      Comment the anti-pattern refused: no `decks[]` array with an index the money path reads —
      `plans.shots` IS the picked deck and `sceneDeckOf` stays untouched.
    - Citation fields ON THE SHOT ELEMENT (so reorder/delete/switch carry them for free):
      `source: v.optional(v.object({ docId: v.string(), title: v.string() }))`,
      `needsConfirmation: v.optional(v.boolean())`, `confirmedAt: v.optional(v.number())`.
      Comment: `source.docId` is MODEL-AUTHORED text — ownership is checked where consumed (the
      `asset.docId` precedent); `confirmedAt` is written ONLY by `confirmClaim` (ctx-derived).
    - Retry + vault-ref: `renderRetriedAt: v.optional(v.number())`,
      `reelVaultDocId: v.optional(v.id("vaultDocuments"))`.
  </action>
  <verify>
    <automated>cd packages/backend && npx tsc --noEmit -p . && pnpm test -- media</automated>
  </verify>
  <done>Schema typechecks; every field optional; shots element validator is a single shared const used by both shots and altShots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: editBrief + switchDeck mutations</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - editBrief(planId, patch:{topic?, durationSeconds?, audience?, tone?, brandVoice?}):
      merges into plan.brief, removes edited fields from brief.defaulted, stamps briefChangedAt;
      refuses `illegal_duration` when durationSeconds not in TARGET_DURATIONS; refuses
      `deck_locked` when deckLockedAt set (post-Generate edits are canvas-only via the paid rail);
      NEVER touches plans.targetDurationSeconds or shots.
    - switchDeck(planId): refuses `no_alternate` when altShots absent, `deck_locked` when locked;
      otherwise swaps shots↔altShots and targetDurationSeconds↔altTargetDurationSeconds in ONE
      patch and stamps shotsChangedAt (structural — landed assets belong to the deck that bought
      them, so invalidation is CORRECT).
    - Mutation-observed: after switchDeck, sceneDeckOf(plan) returns the formerly-alternate deck;
      switch twice round-trips; jobEstimate-visible deck is always plans.shots.
  </behavior>
  <action>
    Implement both as `tenantMutation`s in media.ts beside the existing free-editor mutations
    (the D7 five-affordance comment block). Use TARGET_DURATIONS from @pikar/core. Return the
    `{ ok, reason }` shape the existing editor mutations use. Test in media.test.ts via
    convex-test, asserting OUTCOMES (what sceneDeckOf/plan row shows), not call arguments.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>Both mutations behave per the behavior list; a test deleting the deck_locked guard goes red (observed RED); brief edits provably never move money-contract fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: confirmClaim — the provenance front door</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - confirmClaim(planId, sceneIndex) — those two args ONLY. Actor and timestamp derive from the
      authenticated tenant context (schema.ts:120 idiom); the model has NO mutation that can set
      confirmedAt, and the arg validator structurally cannot carry one.
    - Requires: shot at sceneIndex exists and has needsConfirmation === true. Writes
      confirmedAt: Date.now() on that shot element (via a targeted shots patch that does NOT stamp
      shotsChangedAt — confirmation is not a structural deck edit). Idempotent re-confirm is OK.
    - One insert-only audit row, refs/counts only: { planId, sceneIndex } — never the claim text.
    - Content edits invalidate confirmation: locate the existing narration/overlay editor
      mutation(s) (grep the D7 editor block for the narration edit path through patchShots) and
      clear confirmedAt + keep needsConfirmation on the EDITED scene only. Reorder/delete leave
      other scenes' confirmations intact (they ride the shot element through patchShots renumbering).
  </behavior>
  <action>
    Implement confirmClaim as a tenantMutation. Wire the invalidation into the shared editor
    write path narrowly (only when the narration/overlay content of a confirmed scene changes).
    Tests: confirm → confirmedAt set with no actor arg anywhere in the call; editing that scene's
    narration clears it; reordering leaves a sibling's confirmation standing; audit payload
    contains ids/counts only.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>Confirmation is writable only through the authenticated mutation, survives reorder, dies on content edit of its own scene, and audits refs-only.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- media` green; `npx tsc --noEmit` green in packages/backend.
- Playbook updated same-commit: new plan-row planes + the "model can never write confirmedAt"
  invariant documented; `Last verified` bumped.
- Ask of every new stored field: "who does the STORED row say wrote this, and can the model
  influence it?" (provenance-laundering defect class) — answer recorded in the playbook.
</verification>

<success_criteria>
- The money path (`sceneDeckOf`, `jobEstimate`, `reserveSceneJobInner`) is textually UNTOUCHED by
  this plan yet correctly prices whichever deck is picked, because shots stays "the picked deck".
- All new state optional; no migration; existing rows read correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-02-SUMMARY.md`
</output>
