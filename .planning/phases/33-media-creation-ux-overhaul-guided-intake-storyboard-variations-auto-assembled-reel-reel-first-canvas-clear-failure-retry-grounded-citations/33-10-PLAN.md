---
phase: 33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations
plan: 10
type: execute
wave: 10
depends_on: ["33-08", "33-09"]
files_modified:
  - apps/web/e2e/media-canvas.spec.ts
  - docs/playbooks/media.md
autonomous: false
requirements: [33-INTAKE, 33-VARIA, 33-REEL, 33-CANVAS, 33-FAIL, 33-CITE]

must_haves:
  truths:
    - "The live deployment's ACTIVE media-director row carries the v3 body — verified by read-back, not assumed from seeding"
    - "A production-build browser run proves the new canvas: chips, two variations, switch, confirm-blocks-Generate, hero layout, estimate breakdown"
    - "The full repository suite and both typechecks are green before the owner gate"
    - "The owner has walked the whole experience and issued a verdict"
  artifacts:
    - path: "apps/web/e2e/media-canvas.spec.ts"
      provides: "extended live-browser assertions for the phase-33 canvas"
  key_links:
    - from: "seedSkills"
      to: "live media-director ACTIVE row"
      via: "pnpm dev seed + version read-back (collision gotcha)"
      pattern: "media-director"
---

<objective>
Close the phase live: seed media-director v3 and read back the ACTIVE version, extend the
Playwright media-canvas spec for the new experience, run the full gates, and hold the owner
verification checkpoint.

Purpose: the phase's two unverifiable-offline items (is v3 seeded? does the whole experience
hold in a real browser?) plus the repository-wide green gate before /gsd:verify-work.

Output: live-verified phase + owner verdict recorded.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-RESEARCH.md
@apps/web/e2e/README.md
@apps/web/e2e/media-canvas.spec.ts
@docs/playbooks/media.md
</context>

<constraints>
- Live-stack gotchas (recorded memories): `pnpm dev` seeds, `npx convex dev` alone does NOT;
  convex CLI runs from packages/backend only; `convex run` from the wrong place can kill the dev
  session; Next DEV OOMs on the workspace page — Playwright runs against a PROD BUILD; the
  e2e harness traps are documented in `apps/web/e2e/README.md`; seeded e2e user + onboarding
  gate apply.
- Skill-version-collision gotcha: seedSkills writes maxVersion+1 and optimizer dry-run candidates
  occupy version numbers — read back WHICH version is ACTIVE and confirm its body contains the
  v3 markers (a VARIATION A heading), never trust the seed log line alone.
- This gate spends nothing by default: proposals are token-only. Actually clicking Generate
  spends real media dollars — that is the OWNER'S choice inside the checkpoint, with the price
  on screen.
- Commit only owned files with explicit paths; check `.git/MERGE_HEAD` first.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Seed v3 and read back the live version</name>
  <files>docs/playbooks/media.md</files>
  <action>
    With a live deployment available: run `pnpm dev` long enough for seedSkills to publish (or
    invoke the seed path directly from packages/backend if a narrower entry exists — grep
    seedSkills). Then READ BACK: from packages/backend, query the skills registry for the ACTIVE
    media-director row (the getActiveSkill read or equivalent `npx convex run` — run it from
    packages/backend, and prefer a one-off query over anything that disturbs the dev session).
    Confirm (a) which version number is ACTIVE, (b) the body contains "VARIATION A" and the
    Source-line teaching. Record version + date in the playbook's seeding note.
    A/B discipline: if any existing media fixture or live proposal behaves oddly after v3, A/B
    the prior body version on that one fixture (~$0.01) BEFORE theorizing (recorded memory).
    If no live deployment is reachable, STOP and surface a checkpoint rather than marking this
    done — "autonomous: true ≠ runnable now" is a recorded failure mode.
  </action>
  <verify>
    <automated>cd packages/backend && npx convex run skills:getActiveSkill '{"name":"media-director"}' 2>/dev/null || echo "MANUAL: paste read-back output into SUMMARY"</automated>
  </verify>
  <done>Playbook records the ACTIVE version number, the read-back evidence, and the date; the live specialist provably writes v3 proposals.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the media-canvas e2e + full gates</name>
  <files>apps/web/e2e/media-canvas.spec.ts</files>
  <action>
    Extend the existing spec (respecting both harness traps in e2e/README.md) with the phase-33
    assertions, token-only (no Generate click): brief chips render with the three duration
    presets; the hero slot exists before any reel (tracker or placeholder visible above the
    scene strip); with a two-variation plan seeded/proposed, both decks visible and Switch swaps
    the strip; a scene with an unconfirmed claim shows the badge AND the Generate control is
    disabled with the unconfirmed-claims sentence; confirming clears it; the estimate headline
    renders with an expandable breakdown listing per-kind lines and remaining budget.
    Then run the full gates against a production build:
    1. `pnpm build` (or the repo's prod-build invocation for apps/web)
    2. `npx playwright test media-canvas`
    3. `turbo run test --concurrency=1` (concurrency 1 — vitest spawn-fail gotcha)
    4. `npx tsc --noEmit` in packages/backend AND apps/web (turbo cache can hide red typechecks
       — run them directly)
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test media-canvas && cd ../.. && turbo run test --concurrency=1</automated>
  </verify>
  <done>New assertions pass against a prod build; full suite green; both typechecks green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner walk-through of the whole experience</name>
  <action>Present the walk-through below to the owner and BLOCK until a verdict arrives; record the verbatim verdict in the SUMMARY. Do not proceed on any other signal.</action>
  <done>Owner verdict recorded verbatim; any reported issues captured as gaps for /gsd:plan-phase 33 --gaps.</done>
  <what-built>
    The Phase 33 media-creation experience: guided intake (chat + chips), two storyboard
    variations, reel-first canvas with live tracker, one-click auto-assembled captioned reel with
    vault auto-save, plain-language failure cards with retry/fix menu, and per-scene grounded
    citations with the confirm-before-Generate gate.
  </what-built>
  <how-to-verify>
    In the workspace (media plan thread):
    1. Ask for a reel in plain words (topic + rough intent). Confirm the canvas shows brief
       chips (topic + a 15/30/60 preset; audience/tone marked "from your profile" if defaulted)
       BEFORE any storyboard.
    2. Confirm TWO storyboards render side by side as genuinely different concepts; switch once;
       edit a brief chip and confirm the "brief changed" badge + free Re-propose button (nothing
       fires on its own).
    3. Check citations: cited lines name a vault doc (click one → preview opens); any flagged
       claim shows "needs your confirmation" and Generate stays disabled with the price visible
       until you confirm each.
    4. OPTIONAL, spends real money (price shown on the button, expandable breakdown): click
       Generate once. The tracker should carry generate → voice → assemble → captions with zero
       further clicks, then the finished reel plays muted-looping in the hero (tap for sound).
       Check the vault: the reel is saved as a document with its transcript and citations.
    5. If anything fails mid-run, judge the failure card: plain words, which scene/stage, free
       vs $ fix, code underneath.
  </how-to-verify>
  <resume-signal>
    Type "approved" or describe issues. Two adjacent owner decisions to note (not blockers of
    this phase): (a) 20-12's cockpit-agent media-route certification (~$0.35) remains open;
    (b) Sora 2's Videos API shuts down 2026-09-24 with no announced replacement — a provider
    migration phase is REQUIRED before that date regardless of this phase's outcome.
  </resume-signal>
</task>

</tasks>

<verification>
- Live read-back evidence in the playbook; e2e green on prod build; `turbo run test
  --concurrency=1` green; both tsc runs green; owner verdict recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
- All six phase requirements demonstrated live or owner-accepted; no gate weakened; the two
  adjacent owner decisions surfaced in writing.
</success_criteria>

<output>
After completion, create `.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-10-SUMMARY.md`
</output>
