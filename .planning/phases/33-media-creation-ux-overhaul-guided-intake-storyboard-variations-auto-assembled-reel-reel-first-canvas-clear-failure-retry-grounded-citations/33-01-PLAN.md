---
phase: 33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/storyboard.ts
  - packages/core/src/storyboard.test.ts
  - docs/playbooks/media.md
autonomous: true
requirements: [33-INTAKE, 33-VARIA, 33-CITE]

must_haves:
  truths:
    - "A specialist body carrying a BRIEF section parses into topic + duration-preset + defaulted-field markers, free, before any money moves"
    - "A body carrying VARIATION A / VARIATION B headings parses into two independent scene decks; a body without them still parses exactly as before"
    - "A scene's Source line parses into a document-level citation; 'Source: unverified' marks the scene as needing owner confirmation; an absent Source line means creative copy"
    - "A malformed variation refuses the whole proposal — it never silently falls back to a single deck"
  artifacts:
    - path: "packages/core/src/storyboard.ts"
      provides: "parseBrief, parseVariations, per-scene citation fields on Scene / ParsedSceneDeck"
      exports: ["parseBrief", "parseVariations"]
    - path: "packages/core/src/storyboard.test.ts"
      provides: "Round-trip + refusal tests for all three new contracts, observed RED under mutation"
  key_links:
    - from: "parseVariations"
      to: "parseSceneDeck"
      via: "each variation slice is parsed by the existing scene-deck parser, unchanged"
      pattern: "parseSceneDeck\\("
    - from: "Scene type"
      to: "citation fields"
      via: "optional source/needsConfirmation members on the parsed scene"
      pattern: "needsConfirmation"
---

<objective>
Extend the pure-TS storyboard contract (`packages/core/src/storyboard.ts`) with the three parse
surfaces Phase 33 needs: a BRIEF section (guided intake), a two-variation deck body (storyboard
variations), and per-scene document-level citations (grounded citations). All free at parse time,
all refusing before a cent moves — the same posture `parseSceneDeck` already holds.

Purpose: every downstream plan (schema, persist terminal, money gates, canvas, skill body v3)
builds on these contracts. Getting them right first means the money path never has to interpret
model text.

Output: extended `storyboard.ts` + tests. No backend, no UI.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-CONTEXT.md
@.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-RESEARCH.md
@docs/playbooks/media.md
@packages/core/src/storyboard.ts

<interfaces>
Existing exports in `packages/core/src/storyboard.ts` (verified on disk):

```typescript
export const TARGET_DURATIONS = [15, 30, 60] as const;          // :406
export type TargetDuration = (typeof TARGET_DURATIONS)[number]; // :407
export const VISUAL_KINDS = [...] as const;                     // :411
export type Scene = { index; startMs; durationMs; visual: VisualKind; asset?; description; narration; overlay?; prompt }; // :439
export type ParsedSceneDeck = /* ok | refusal union with reason codes incl. "no_deck" */; // :513
export function parseSceneDeck(body: string): ParsedSceneDeck;  // :583
export const parseScript = (body: string): string;              // :213 — sectionOf(body, "SCRIPT") idiom
export function parseArtDirection(body: string): ArtDirection | null; // :257 — null-when-incomplete precedent
```

The section-parser idiom to follow: `parseScript` / `parseArtDirection` read a `## HEADING`-style
section via a shared `sectionOf` helper; `parseArtDirection` returns `null` (not a refusal) when
the section is absent or incomplete.
</interfaces>
</context>

<constraints>
- CRLF: `docs/playbooks/media.md` is CRLF — normalize/assert-unique anchors before editing;
  multi-line `\n` anchors silently no-op.
- Commit ONLY the three files this plan owns (plain `git commit -- <paths>`, never `-A`; check
  `.git/MERGE_HEAD` first — lanes share one working tree and foreign media files are modified).
- After code changes run `graphify update .` then `node scripts/extract-convex-edges.mjs`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BRIEF section parser</name>
  <files>packages/core/src/storyboard.ts, packages/core/src/storyboard.test.ts</files>
  <behavior>
    - `parseBrief(body)` returns `{ topic: string; durationSeconds: TargetDuration; audience?: string; tone?: string; brandVoice?: string; defaulted: string[] } | null`
    - Reads a `BRIEF` section via the same `sectionOf` idiom as SCRIPT/ART DIRECTION. Lines:
      `Topic:`, `Duration:` (e.g. "30s" or "30"), `Audience:`, `Tone:`, `Brand voice:`.
    - A field suffixed `(defaulted)` (case-insensitive) strips the marker and appends the field
      name ("audience" | "tone" | "brandVoice" | "duration") to `defaulted`.
    - Returns null when the section is absent, when Topic is empty, or when Duration is not one
      of TARGET_DURATIONS (15/30/60 — presets only, per locked decision; never free entry).
    - Backward compat: a v2 body with no BRIEF section → null, and nothing else changes.
  </behavior>
  <action>
    Add `parseBrief` to `packages/core/src/storyboard.ts` following the `parseArtDirection`
    null-when-incomplete precedent (NOT a refusal union — a missing brief is a worse UX, not an
    unusable proposal; the deck still carries its own validated `targetDurationSeconds`).
    Export a `BriefFields` type. Keep it dependency-free pure TS.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- storyboard</automated>
  </verify>
  <done>parseBrief round-trips a well-formed BRIEF, strips defaulted markers into the array, and returns null for absent section / bad duration / empty topic — each case covered by a test that fails if the check is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-scene citation lines (document-level, model-declared)</name>
  <files>packages/core/src/storyboard.ts, packages/core/src/storyboard.test.ts</files>
  <behavior>
    - Each scene in a SCENE DECK may carry ONE optional `Source:` line, three forms:
      1. `Source: <vault doc title> [doc:<docId>]` → parsed scene gains `source: { docId, title }`
      2. `Source: unverified` → parsed scene gains `needsConfirmation: true` (a factual claim the
         specialist could not ground in the vault)
      3. line absent → neither field (creative copy — hooks/CTAs need no citation, per locked decision)
    - A `Source:` line with a `[doc:...]` token whose id is empty refuses the deck (same refusal
      posture as other malformed scene lines — never silently dropped).
    - Document-level only (the Phase-14 locked idiom): one doc per scene, no chunk refs.
  </behavior>
  <action>
    Widen the `Scene` type with optional `source?: { docId: string; title: string }` and
    `needsConfirmation?: true`. Extend the scene-line parsing inside `parseSceneDeck` (and its
    per-scene block reader) to recognize the `Source:` line. IMPORTANT: the parser output must
    have NO field for confirmation timestamps — `confirmedAt` is written only by an authenticated
    tenant mutation later (provenance rule: the model must have no path to writing a
    confirmation; the type system is the first door).
    Keep every existing fixture green: decks without Source lines parse identically.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- storyboard</automated>
  </verify>
  <done>Cited, unverified, and uncited scenes parse into the three distinct shapes; malformed doc token refuses; the Scene type carries no confirmation field; all pre-existing fixtures still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two-variation body parser</name>
  <files>packages/core/src/storyboard.ts, packages/core/src/storyboard.test.ts</files>
  <behavior>
    - `parseVariations(body)` returns a union:
      `{ kind: "two"; a: <ok scene deck + its brief-adjacent script/artDirection slice>; b: <same> }`
      | `{ kind: "one" }` (no VARIATION headings — caller falls through to the existing single-deck path)
      | `{ kind: "refused"; variation: "a" | "b"; reason: <the inner ParsedSceneDeck refusal reason> }`
    - Implementation: split the body at `VARIATION A` / `VARIATION B` headings (same heading style
      the SCENE DECK heading uses); run the EXISTING `parseSceneDeck` on each slice unchanged.
    - A variation slice whose deck refuses for any reason (including `no_deck` inside a declared
      variation) refuses the WHOLE proposal — no silent fallback to one deck (mirrors the
      persistStoryboard rule: a deck nobody wrote must never be proposed).
    - Both decks must be complete independent scene decks (own target duration line, own scenes).
  </behavior>
  <action>
    Add `parseVariations` to storyboard.ts. Keep it a thin splitter over `parseSceneDeck` — no
    duplicate scene parsing logic. Tests must include: two valid distinct decks parse into a+b;
    body without headings returns kind:"one"; variation B with a duration-sum mismatch refuses the
    whole body (observed RED: removing the refusal branch must fail the test); Source lines inside
    variations survive the split (compose with Task 2).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- storyboard</automated>
  </verify>
  <done>parseVariations handles the three-way contract with refusal-over-fallback proven by a mutation-observed test; single-deck bodies are byte-for-byte unaffected.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- storyboard` fully green (936+ pre-existing tests intact).
- `docs/playbooks/media.md` updated in the same commit (new parse contracts documented under the
  scene-deck section; bump `Last verified`). The Stop hook enforces this.
- `graphify update .` + `node scripts/extract-convex-edges.mjs` run after the code change.
</verification>

<success_criteria>
- Three new parse surfaces exported from @pikar/core with closed, tested contracts.
- Zero behavior change for v2 single-deck bodies (backward compat proven by existing fixtures).
- No confirmation-timestamp field anywhere in parser output types.
</success_criteria>

<output>
After completion, create `.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-01-SUMMARY.md`
</output>
