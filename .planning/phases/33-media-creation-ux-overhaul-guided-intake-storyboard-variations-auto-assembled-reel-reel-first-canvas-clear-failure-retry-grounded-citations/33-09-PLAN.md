---
phase: 33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations
plan: 09
type: execute
wave: 9
depends_on: ["33-01", "33-03"]
files_modified:
  - packages/contracts/skills/media-director.md
  - packages/core/src/storyboard.test.ts
  - docs/playbooks/media.md
autonomous: true
requirements: [33-INTAKE, 33-VARIA, 33-CITE]

must_haves:
  truths:
    - "The media-director v3 body teaches: echo the brief, propose TWO distinct-concept variations, cite every factual claim with a vault Source line or mark it unverified"
    - "The body's own worked example round-trips through parseBrief + parseVariations + the citation parse — the parser and the prompt cannot drift"
    - "Prior single-deck fixtures still parse — a rollback to v2 keeps working"
  artifacts:
    - path: "packages/contracts/skills/media-director.md"
      provides: "v3 skill body (brief echo, two variations, Source lines)"
      contains: "VARIATION A"
    - path: "packages/core/src/storyboard.test.ts"
      provides: "round-trip test reading the v3 worked example"
  key_links:
    - from: "media-director.md worked example"
      to: "parseVariations / parseBrief"
      via: "round-trip test parses the body's own example text"
      pattern: "media-director"
---

<objective>
Author media-director v3: the skill-body half of guided intake, variations, and citations. The
parsers (33-01) and the terminal (33-03) are already live; this teaches the specialist to emit
what they read.

Purpose: no hardcoded prompts (CLAUDE.md §5) — the behavior change ships as a versioned registry
row. The round-trip test is the drift guard between prompt and parser.

Output: v3 body on disk + round-trip test. Seeding and live verification happen in 33-10.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-RESEARCH.md
@packages/contracts/skills/media-director.md
@packages/core/src/storyboard.ts
@docs/playbooks/media.md
</context>

<constraints>
- media-director is DELIBERATELY UNGATED — body edits activate on seed with no eval. That makes
  the round-trip test and the worked example the ONLY pre-live safety net: they must be strict.
- Skill-body edits can shift model tool args on unrelated paths (recorded memory). Do not remove
  or reword the existing searchVault guidance, the scene-deck rules, or any tool-adjacent
  instruction unless the change is the point. Keep the v2 sections' structure; ADD the three new
  sections.
- The body must keep teaching: mixed kinds (an all-generated deck cannot hit any target —
  ADR-019 feasibility), exact duration sums, silent scenes allowed, the closed VISUAL_KINDS set.
- CRLF check before editing (contracts .md files may be CRLF); commit only owned files.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author the v3 body</name>
  <files>packages/contracts/skills/media-director.md</files>
  <action>
    Extend the v2 scene-author body with three teachings:
    1. BRIEF echo — before the variations, emit a BRIEF section (Topic, Duration as one of
       15/30/60s, Audience/Tone/Brand voice) echoing what the user asked; mark any field taken
       from the business profile rather than the user's words with "(defaulted)". Only topic and
       duration are required; never invent specifics for a thin profile (Phase-11 rule) — default
       and mark instead.
    2. TWO VARIATIONS — emit VARIATION A and VARIATION B headings, each a COMPLETE scene deck
       (own Target duration line, own scenes). The two must be DISTINCT CONCEPTS: different
       angle AND different visual treatment (e.g. testimonial-style vs data-driven hook,
       different kind mixes) — never the same script with different visuals.
    3. SOURCE lines — for every scene whose narration or text card states a fact, figure, or
       business-specific claim: cite the vault document it came from as
       `Source: <doc title> [doc:<id>]` using ONLY documents returned by searchVault this
       conversation; if no vault document grounds the claim, write `Source: unverified` — the
       owner will be asked to confirm it. Creative copy (hooks, CTAs) carries no Source line.
       Never fabricate a doc id.
    Update the worked example to demonstrate ALL of: the brief echo with one defaulted field,
    two distinct-concept decks with different kind mixes, at least one silent scene, at least
    one cited line, one `Source: unverified` line, and uncited creative copy. Bump the body's
    version marker to v3 per the file's own convention.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- storyboard</automated>
  </verify>
  <done>v3 body on disk with the three new sections and a worked example exercising every new contract; no v2 rule weakened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip test against the body's own example</name>
  <files>packages/core/src/storyboard.test.ts</files>
  <behavior>
    - The existing round-trip test (which reads media-director.md's worked example — find it by
      grepping storyboard.test.ts for the body path) is extended to assert against the v3
      example: parseBrief returns the echoed brief with the defaulted field in `defaulted`;
      parseVariations returns kind:"two" with both decks ok; deck A and B have different kind
      mixes (assert the mixes differ — the distinct-concept contract's testable shadow); the
      cited scene carries source.docId, the unverified scene carries needsConfirmation, the
      creative scene carries neither.
    - Prior fixtures for v2 single-deck bodies remain untouched and green (rollback safety).
  </behavior>
  <action>
    Extend the round-trip test. If the current test reads the .md from disk, keep that mechanism
    (it is the drift guard); do not inline a copy of the example.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- storyboard</automated>
  </verify>
  <done>Prompt and parser are pinned together: editing either side incompatibly turns the suite red.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- storyboard` green; full backend media suite still green
  (`pnpm --filter @pikar/backend test -- media` — the terminal consumes the same contracts).
- Playbook same-commit: v3 body summary + the seeding requirement forward-pointer ("on disk is
  not live — see 33-10"); `Last verified` bumped.
- NOTE for the SUMMARY: the LIVE deployment still runs the old body until 33-10 seeds and
  reads back — say so explicitly.
</verification>

<success_criteria>
- v3 authored, round-trip pinned, v2 rollback path intact, nothing live yet by design.
</success_criteria>

<output>
After completion, create `.planning/phases/33-media-creation-ux-overhaul-guided-intake-storyboard-variations-auto-assembled-reel-reel-first-canvas-clear-failure-retry-grounded-citations/33-09-SUMMARY.md`
</output>
