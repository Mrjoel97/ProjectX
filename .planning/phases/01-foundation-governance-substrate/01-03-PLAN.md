---
phase: 01-foundation-governance-substrate
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/backend/convex/audit.ts
  - packages/backend/convex/audit.test.ts
  - packages/backend/convex/auditImmutability.test.ts
  - packages/audit/package.json
  - packages/audit/src/index.ts
  - packages/audit/src/eventTypes.ts
  - packages/contracts/src/audit.ts
autonomous: true
requirements: [OPSG-02]

must_haves:
  truths:
    - "audit.log inserts a row with tenantId, correlationId, eventType, actor, payload, ts"
    - "The audit module exposes ONLY an insert surface — no patch/replace/delete functions exist"
    - "No public (client-callable) function writes the audit table"
    - "Audit payloads are redaction-safe (refs/hashes/metadata only, never raw content)"
  artifacts:
    - path: "packages/backend/convex/audit.ts"
      provides: "internalMutation log() — the sole audit write surface"
      contains: "internalMutation"
    - path: "packages/backend/convex/audit.test.ts"
      provides: "OPSG-02 insert-path unit test (convex-test)"
    - path: "packages/backend/convex/auditImmutability.test.ts"
      provides: "static scan: no db.patch/replace/delete in audit.ts; no public writer of audit table"
    - path: "packages/audit/src/eventTypes.ts"
      provides: "audit event-type taxonomy (pure TS)"
  key_links:
    - from: "packages/backend/convex/audit.ts"
      to: "packages/audit / packages/contracts"
      via: "event-type taxonomy + payload contract import"
      pattern: "@pikar/audit|@pikar/contracts"
---

<objective>
Build the insert-only audit module (OPSG-02): a single `internalMutation` write surface backed by the `audit` table, with the event-type taxonomy and redaction-safe payload contract living in pure-TS `packages/audit` + `packages/contracts`. Enforce immutability by convention + a static-scan test (Convex has no append-only primitive).

Purpose: Satisfy requirement OPSG-02 and Success Criterion 3 (audit half).
Output: `convex/audit.ts` (insert only), the audit taxonomy package, and passing insert-path + immutability-scan tests.

Requirement mapping: OPSG-02.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-governance-substrate/01-RESEARCH.md
@.planning/phases/01-foundation-governance-substrate/01-VALIDATION.md
@packages/backend/convex/schema.ts

<interfaces>
<!-- audit table (from plan 01 schema): tenantId, correlationId, eventType, actor, payload(v.any, refs/hashes only), ts -->
```typescript
// packages/backend/convex/audit.ts — the ONLY write surface; NO update/delete functions here
import { internalMutation } from "./_generated/server";
import { v } from "convex/values";
export const log = internalMutation({
  args: { tenantId: v.string(), correlationId: v.string(), eventType: v.string(), actor: v.string(), payload: v.any() },
  handler: async (ctx, args) => { await ctx.db.insert("audit", { ...args, ts: Date.now() }); },
});
```
Enforcement = convention + verification: (a) audit.ts exports insert only; (b) static-scan test asserts no db.patch/replace/delete in audit.ts; (c) no public function writes `audit`.
audit.ts is an allow-listed internal-builder module (see plan 02 lib/allowlist.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing audit insert + immutability-scan tests (RED)</name>
  <files>packages/backend/convex/audit.test.ts, packages/backend/convex/auditImmutability.test.ts</files>
  <behavior>
    audit.test.ts (convex-test): calling internal.audit.log with {tenantId, correlationId, eventType, actor, payload} inserts exactly one audit row whose fields round-trip and whose ts is a number.
    auditImmutability.test.ts (static scan): reads convex/audit.ts source and asserts it contains NO db.patch, db.replace, or db.delete; asserts audit.ts exports no non-`internal` (client-callable) function; scans all convex/*.ts and asserts no PUBLIC query/mutation writes the `audit` table (only internalMutation may).
  </behavior>
  <action>Write both test files. They MUST fail (audit.ts absent). Command: `pnpm --filter backend vitest run convex/audit.test.ts convex/auditImmutability.test.ts`.</action>
  <verify>
    <automated>cd packages/backend && npx convex codegen && pnpm vitest run convex/audit.test.ts convex/auditImmutability.test.ts || echo "RED as expected"</automated>
  </verify>
  <done>Both audit tests exist and fail (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit taxonomy package + insert-only module (GREEN)</name>
  <files>packages/audit/package.json, packages/audit/src/index.ts, packages/audit/src/eventTypes.ts, packages/contracts/src/audit.ts, packages/backend/convex/audit.ts</files>
  <action>
    `packages/audit` (source-export package, name `@pikar/audit`): `eventTypes.ts` defines the audit event-type union taxonomy ("request.received", "routing.decided", "model.called", "review.action", "delivery.sent", "deadletter.written", ...) as pure TS; `index.ts` re-exports plus any formatting helpers. `packages/contracts/src/audit.ts` defines the redaction-safe `AuditPayload` shape (refs/hashes/metadata only — add a top comment: "payload MUST be redaction-safe; never raw content — Pitfall 4"). Implement `convex/audit.ts` exactly per the interfaces block (insert only), importing the taxonomy/type. Keep it a thin adapter.
  </action>
  <verify>
    <automated>cd packages/backend && npx convex codegen && pnpm vitest run convex/audit.test.ts convex/auditImmutability.test.ts</automated>
  </verify>
  <done>Insert-path + immutability-scan tests pass (GREEN); audit.ts is insert-only and imports the pure-TS taxonomy/contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend vitest run convex/audit.test.ts convex/auditImmutability.test.ts` green.
- audit.ts contains no patch/replace/delete and exports only an internalMutation.
- No public function writes the audit table.
</verification>

<success_criteria>
OPSG-02 met: any state change can append to the insert-only audit module; no update/delete functions exist; immutability enforced by convention + static scan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance-substrate/01-03-SUMMARY.md`
</output>
