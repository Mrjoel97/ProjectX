---
phase: 01-foundation-governance-substrate
plan: 09
type: execute
wave: 4
depends_on: ["01", "08"]
files_modified:
  - packages/backend/convex/gmailOAuth.ts
  - packages/backend/convex/http.ts
  - apps/web/app/connect-gmail/page.tsx
autonomous: false
requirements: [SC-5]
user_setup:
  - service: google-cloud
    why: "OAuth consent screen, gmail.send scope, client credentials, and verification submission all live in the user's Google Cloud project"
    env_vars:
      - name: GOOGLE_OAUTH_CLIENT_ID
        source: "GCP Console -> APIs & Services -> Credentials -> OAuth 2.0 Client (Web application); set via `npx convex env set` (NOT Vercel)"
      - name: GOOGLE_OAUTH_CLIENT_SECRET
        source: "Same OAuth client; set via `npx convex env set`"
    dashboard_config:
      - task: "Create GCP project, enable Gmail API, configure consent screen (Branding: app name, support email, authorized domain, privacy policy URL), publishing status Testing with the owner as test user, scope gmail.send ONLY"
        location: "console.cloud.google.com -> Google Auth Platform / APIs & Services -> OAuth consent screen"
      - task: "Record unlisted English YouTube demo video of the consent flow and submit verification (per-scope justification for gmail.send, up to 3 doc links, video URL)"
        location: "console.cloud.google.com/auth/verification (OAuth Verification Center)"

must_haves:
  truths:
    - "A minimal working gmail.send OAuth consent flow exists: user clicks Connect Gmail, sees Google's consent screen with the real app name, grants gmail.send, and tokens are stored in Convex"
    - "Only the gmail.send scope is requested — never https://mail.google.com/ (restricted; triggers CASA Tier 2)"
    - "The verification submission is filed in the OAuth Verification Center with the demo video, per-scope justification, and privacy policy link"
    - "OAuth client secrets live in Convex env vars, not Vercel"
  artifacts:
    - path: "packages/backend/convex/gmailOAuth.ts"
      provides: "authorize-URL builder action + token-exchange logic + gmailTokens storage (internal)"
      contains: "gmail.send"
    - path: "packages/backend/convex/http.ts"
      provides: "httpAction GET /oauth/google/callback exchanging code for tokens"
      contains: "callback"
    - path: "apps/web/app/connect-gmail/page.tsx"
      provides: "Connect Gmail button page used to record the demo video"
  key_links:
    - from: "apps/web/app/connect-gmail/page.tsx"
      to: "gmailOAuth authorize URL"
      via: "useAction/useQuery fetching the consent URL then redirect"
      pattern: "accounts.google.com|authorize"
    - from: "packages/backend/convex/http.ts"
      to: "oauth2.googleapis.com/token"
      via: "code exchange in the callback httpAction"
      pattern: "oauth2.googleapis.com"
---

<objective>
Complete the Week-1 Google OAuth verification submission: configure the GCP consent screen (gmail.send only, Testing mode), build the minimal working consent flow needed for the mandatory demo video (Connect Gmail -> Google consent -> callback stores tokens in Convex), record the video, and submit in the OAuth Verification Center.

Purpose: Satisfy the OAuth half of Success Criterion 5 — paperwork submitted in Week 1 so the up-to-10-day review clock starts now. Pitfall 7: the demo video REQUIRES a working consent flow, hence the code in this plan.
Output: Consent flow live on the production domain, tokens stored, verification submitted (status visible in the Verification Center).

Requirement mapping: Success Criterion 5 (SC-5, OAuth half). Token lifecycle/refresh handling is Phase 2 (DLVR-03); actual sending is Phase 2 (DLVR-01).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-governance-substrate/01-RESEARCH.md
@packages/backend/convex/http.ts
@packages/backend/convex/schema.ts

<interfaces>
Week-1 checklist order (Research Pattern 11): (1) GCP project + consent screen branding, gmail.send scope ONLY; (2) domain verified [done in plan 08]; (3) homepage [plan 08]; (4) privacy policy linked on consent screen [plan 08]; (5) Testing publishing status + test users; (6) submission = per-scope justification + up to 3 doc links + unlisted English YouTube demo video showing the consent screen with real app name and the client ID visible in the URL bar. Review time: up to 10 days; bounce-backs reset the clock — keep the scope list minimal and stable.
OAuth flow (no extra library — ponytail discipline; plain URLs + fetch):
- Authorize URL: https://accounts.google.com/o/oauth2/v2/auth?client_id=...&redirect_uri=https://YOUR_CONVEX_SITE/oauth/google/callback&response_type=code&scope=https://www.googleapis.com/auth/gmail.send&access_type=offline&prompt=consent&state=<nonce>
- Callback (Convex httpAction, inbound is what httpActions are for): POST https://oauth2.googleapis.com/token {code, client_id, client_secret, redirect_uri, grant_type: "authorization_code"} -> { access_token, refresh_token, expiry }. Store via internal mutation in a `gmailTokens` table (ADD to schema: { tenantId, accessToken, refreshToken, expiresAt, scope, createdAt } index by_tenant — note: tokens are secrets-adjacent; encrypting/at-rest posture documented, Convex is AES-256 at rest).
- Redirect URI host is the Convex deployment's .convex.site domain (httpActions) — add it to the OAuth client's authorized redirect URIs.
- gmailOAuth.ts/http.ts are allow-listed internal-builder modules.
Secrets: GOOGLE_OAUTH_CLIENT_ID/SECRET via `npx convex env set` (Pitfall 8).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: GCP project + consent screen configuration</name>
  <action>Human-only external console setup: create the GCP project, enable Gmail API, configure the OAuth consent screen (gmail.send only, Testing mode), create the OAuth web client, and set GOOGLE_OAUTH_CLIENT_ID/SECRET in Convex env. Google offers no API for consent-screen branding, and the account belongs to the user.</action>
  <what-built>Nothing yet — this is external console setup Claude cannot perform (Google account ownership, no API for consent-screen branding).</what-built>
  <how-to-verify>
    In console.cloud.google.com with the domain-verified Google account:
    1. Create project "Pikar AI" (or reuse); enable the Gmail API.
    2. OAuth consent screen (Google Auth Platform -> Branding): app name "Pikar AI", support email, authorized domain = your production domain, privacy policy URL = https://YOUR-DOMAIN/privacy, homepage = https://YOUR-DOMAIN/.
    3. Scopes: add ONLY https://www.googleapis.com/auth/gmail.send (sensitive — do NOT add mail.google.com).
    4. Publishing status: Testing; add yourself as a test user.
    5. Credentials -> Create OAuth client (Web application). Leave redirect URIs empty for now (Task 2 supplies the exact callback URL) or add a placeholder.
    6. Provide the client ID + secret to the executor by running, in packages/backend: `npx convex env set GOOGLE_OAUTH_CLIENT_ID <id>` and `npx convex env set GOOGLE_OAUTH_CLIENT_SECRET <secret>`.
  </how-to-verify>
  <verify>Consent screen shows app name, privacy policy URL, authorized domain; scope list contains ONLY gmail.send; `npx convex env list` shows GOOGLE_OAUTH_CLIENT_ID and GOOGLE_OAUTH_CLIENT_SECRET.</verify>
  <done>User typed "done"; consent screen configured in Testing mode with gmail.send only; both env vars set in Convex.</done>
  <resume-signal>Type "done" when the consent screen is configured (gmail.send only, Testing mode) and both env vars are set in Convex.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Minimal gmail.send consent flow (authorize -> callback -> token storage)</name>
  <files>packages/backend/convex/gmailOAuth.ts, packages/backend/convex/http.ts, apps/web/app/connect-gmail/page.tsx</files>
  <action>
    Add the `gmailTokens` table to schema.ts (interfaces block shape). Implement `gmailOAuth.ts`: an action `getAuthorizeUrl` building the consent URL (gmail.send scope ONLY, access_type=offline, prompt=consent, random state stored for CSRF check) and an internal mutation `storeTokens`. Extend `http.ts` with GET `/oauth/google/callback`: validate state, exchange the code at oauth2.googleapis.com/token via fetch (client id/secret from Convex env), store tokens via storeTokens keyed to the authenticated/known owner tenant, then redirect to `https://YOUR-DOMAIN/connect-gmail?status=connected`. Create `apps/web/app/connect-gmail/page.tsx`: a "Connect Gmail" button that fetches the authorize URL (useAction) and redirects; shows a success state on ?status=connected. Print the exact callback URL (the deployment's .convex.site path) in the task output so the user can add it to the OAuth client's authorized redirect URIs. Deploy web changes to production (needed for the demo video on the real domain).
    Do NOT implement sending or token refresh (Phase 2, DLVR-01/DLVR-03).
  </action>
  <verify>
    <automated>cd packages/backend && npx convex codegen && npx tsc --noEmit && cd ../../apps/web && pnpm build</automated>
  </verify>
  <done>Typecheck + web build green; authorize URL contains only the gmail.send scope; callback httpAction exchanges and stores tokens; connect-gmail page deployed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Demo video + verification submission</name>
  <action>Human-only: run the consent flow once with the user's Google account, record the unlisted English YouTube demo video of the grant flow, and file the verification submission in the OAuth Verification Center (per-scope justification, doc links, video URL). Google provides no API for verification submission.</action>
  <what-built>Working consent flow at https://YOUR-DOMAIN/connect-gmail (Task 2), consent screen configured (Task 1), homepage/privacy/domain verified (plan 08).</what-built>
  <how-to-verify>
    1. Add the callback URL from Task 2 to the OAuth client's authorized redirect URIs, then run the flow once end-to-end: Connect Gmail -> Google consent screen (real app name visible) -> grant -> redirected with status=connected. Confirm a gmailTokens row exists (Convex dashboard).
    2. Record the demo video (screen recording, English): start on the homepage, click through to Connect Gmail, show the consent screen with the app name AND the client ID visible in the browser URL bar, complete the grant, show the connected state. Upload to YouTube as UNLISTED.
    3. Go to console.cloud.google.com/auth/verification (OAuth Verification Center) and submit: per-scope justification for gmail.send ("sends emails composed in Pikar AI only after the user explicitly reviews and approves each message; no narrower scope exists for sending"), links (homepage, privacy policy), and the video URL.
    4. Capture the "submitted" status (screenshot or URL) for the phase notes.
  </how-to-verify>
  <verify>OAuth Verification Center shows the submission with status "submitted"/"in review"; gmailTokens row visible in the Convex dashboard from the end-to-end run.</verify>
  <done>User typed "submitted" with the Verification Center status/URL; evidence captured for the phase SUMMARY.</done>
  <resume-signal>Type "submitted" with the Verification Center status/URL once the submission is filed, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- Authorize URL requests exactly one scope: gmail.send.
- End-to-end consent flow works on the production domain; gmailTokens row created.
- OAuth Verification Center shows the submission as filed (manual evidence recorded in SUMMARY).
- GOOGLE_OAUTH_* secrets exist only in Convex env (not Vercel, not repo).
</verification>

<success_criteria>
Success Criterion 5 (OAuth half) met: privacy policy, homepage, and verified domain are live (plan 08), the minimal gmail.send consent flow works, the demo video is recorded, and the verification paperwork is SUBMITTED in Week 1 — the external review clock is running.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance-substrate/01-09-SUMMARY.md`
</output>
