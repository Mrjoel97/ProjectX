---
phase: 01-foundation-governance-substrate
plan: 08
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - apps/web/app/page.tsx
  - apps/web/app/privacy/page.tsx
  - apps/web/app/globals.css
autonomous: false
requirements: [SC-5]
user_setup:
  - service: vercel
    why: "The homepage + privacy policy must be publicly accessible on a domain the user can verify in Google Search Console"
    env_vars:
      - name: CONVEX_DEPLOY_KEY
        source: "Convex Dashboard -> Deployment Settings -> Deploy key (production, deployment:deploy permission) -> set in Vercel project env (Production scope)"
    dashboard_config:
      - task: "Create/connect Vercel project (root dir apps/web) and attach the production domain"
        location: "Vercel Dashboard -> Add New Project -> import github.com/Mrjoel97/ProjectX"
  - service: google-search-console
    why: "Google OAuth verification requires domain ownership verification"
    dashboard_config:
      - task: "Verify domain ownership (DNS TXT record) with the same Google account that owns the GCP project"
        location: "search.google.com/search-console -> Add property (Domain)"

must_haves:
  truths:
    - "A public homepage clearly describes Pikar-AI, is not login-walled, and is served on the production domain"
    - "A privacy policy page on the SAME domain discloses how the app accesses/uses/stores/shares Google user data (gmail.send)"
    - "The homepage links to the privacy policy"
    - "The site deploys to Vercel with the documented Convex build command and only NEXT_PUBLIC_CONVEX_URL + CONVEX_DEPLOY_KEY in Vercel"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "public homepage describing the app"
    - path: "apps/web/app/privacy/page.tsx"
      provides: "privacy policy incl. Google user data / Limited Use disclosures"
      contains: "Google"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "/privacy"
      via: "footer/nav link"
      pattern: "href=\"/privacy\""
---

<objective>
Ship the code-free-prerequisite web surface for Google OAuth verification: a public homepage that clearly describes Pikar-AI and a privacy policy on the same domain with the required Google-user-data disclosures, deployed to Vercel on the production domain, with the domain verified in Google Search Console.

Purpose: Items 2-4 of the Week-1 OAuth checklist (Research Pattern 11) — everything plan 09's submission needs that involves the website. Also connects Vercel per Research Pattern 9.
Output: Live homepage + /privacy on the verified domain; Vercel project wired with the Convex deploy command.

Requirement mapping: Success Criterion 5 (SC-5, OAuth-prerequisites portion).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-governance-substrate/01-RESEARCH.md
@.planning/PROJECT.md

<interfaces>
Vercel wiring (verified docs.convex.dev/production/hosting/vercel):
- Vercel root directory: apps/web. Build command: `npx convex deploy --cmd 'pnpm build'` (run in the packages/backend context; use `--cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL` if needed). Budget one iteration on monorepo build wiring (Research Open Question 4).
- Vercel env: ONLY `CONVEX_DEPLOY_KEY` (production key, Production scope) + `NEXT_PUBLIC_CONVEX_URL` (set by convex deploy). All other secrets stay in Convex env vars (Pitfall 8).
Privacy policy must (Google requirements): be hosted on the same domain as the homepage; disclose how the app accesses/uses/stores/shares Google user data; state gmail.send usage (send email on the user's behalf, only at their explicit request, after human review); affirm Google API Services User Data Policy / Limited Use compliance; include contact email. Homepage must not be login-walled and must clearly describe the app.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage + privacy policy pages</name>
  <files>apps/web/app/page.tsx, apps/web/app/privacy/page.tsx, apps/web/app/globals.css</files>
  <action>
    Replace the placeholder `app/page.tsx` with a real public homepage: what Pikar-AI is (governed AI chief-of-staff: speak/type a goal -> planned -> guarded -> human-approved -> delivered by email with a full audit trail), who it's for, and a footer with a link to `/privacy` and a contact email (use the owner's support email). No login wall, no external assets required; server component, minimal styling (ponytail discipline — no component library for this).
    Create `app/privacy/page.tsx`: a complete privacy policy covering: data collected; how Google user data is accessed/used/stored/shared — specifically the `gmail.send` scope (used solely to send emails the user has explicitly reviewed and approved; no reading of mailboxes; tokens stored encrypted in Convex; never shared/sold); Limited Use affirmation ("use of information received from Google APIs adheres to the Google API Services User Data Policy, including the Limited Use requirements"); retention/deletion; contact. Static content, same domain.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build</automated>
  </verify>
  <done>`pnpm build` green; / and /privacy render; homepage links to /privacy; privacy page contains the gmail.send + Limited Use disclosures.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel production</name>
  <files>apps/web/next.config.ts</files>
  <action>
    Deploy via CLI: `vercel link` + `vercel --prod` (or `vercel deploy --prod`) from apps/web with the monorepo settings; configure the build command to `npx convex deploy --cmd 'pnpm build'` per the interfaces block (this requires `CONVEX_DEPLOY_KEY` in Vercel — if not yet set, create it via the Convex dashboard/CLI and set it with `vercel env add CONVEX_DEPLOY_KEY production`). If Vercel CLI is unauthenticated, surface an auth gate (`vercel login`) and retry. Iterate on the monorepo root-dir/build-command wiring as needed (known Open Question 4 — budget one iteration). Confirm only the two allowed env vars live in Vercel.
  </action>
  <verify>
    <automated>PROD_URL=$(cd apps/web && vercel inspect --prod 2>/dev/null | grep -oE 'https://[^ ]+' | head -1); test -n "$PROD_URL" && curl -sf -o /dev/null "$PROD_URL/" && curl -sf -o /dev/null "$PROD_URL/privacy"</automated>
    <note>Executor: after deploy, capture the production URL into PROD_URL (adjust the inspect/ls invocation to whatever the current Vercel CLI emits). The command MUST exit non-zero if the URL cannot be determined or either page is not 200 — no silent fallback.</note>
  </verify>
  <done>Production deployment live; / and /privacy return 200 on the production URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Attach production domain + verify pages live + Search Console domain verification (checkpoint)</name>
  <action>Human checkpoint — domain attachment and Google Search Console DNS verification require the user's accounts; then confirm both pages are live on the production domain.</action>
  <what-built>Public homepage + privacy policy deployed to Vercel production.</what-built>
  <how-to-verify>
    1. Attach your production domain to the Vercel project (Vercel Dashboard -> Project -> Domains) if not already done — Claude cannot pick/purchase your domain.
    2. Open https://YOUR-DOMAIN/ — homepage describes Pikar-AI, no login wall, links to the privacy policy.
    3. Open https://YOUR-DOMAIN/privacy — policy loads on the SAME domain and mentions gmail.send + Limited Use.
    4. In Google Search Console (search.google.com/search-console), add the Domain property and complete DNS TXT verification with the SAME Google account that will own the GCP project (raymondbenedict4@gmail.com unless you prefer another).
  </how-to-verify>
  <verify>https://YOUR-DOMAIN/ and /privacy return 200 on the attached domain; Search Console shows the domain property verified.</verify>
  <done>User typed "approved" with the production domain; domain verified in Search Console.</done>
  <resume-signal>Type "approved" with the final production domain name once both pages are live on it and Search Console shows the domain verified, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter web build` green.
- Production URL serves / and /privacy (200, same domain, no auth).
- Search Console domain property verified (manual evidence).
- Vercel env contains only CONVEX_DEPLOY_KEY + NEXT_PUBLIC_CONVEX_URL.
</verification>

<success_criteria>
OAuth checklist items 2-4 complete: verified domain, compliant public homepage, same-domain privacy policy — plan 09 can configure the consent screen and submit without waiting on web work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance-substrate/01-08-SUMMARY.md`
</output>
