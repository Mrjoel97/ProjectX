---
phase: 01-foundation-governance-substrate
plan: 05
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .mcp.json
  - .gitignore
  - README.md
autonomous: false
requirements: [SC-5]

must_haves:
  truths:
    - "graphify (PyPI graphifyy) is installed and its CLI resolves on the Windows dev machine"
    - "A post-commit git hook auto-updates the repo knowledge graph"
    - "graphify's stdio MCP server is registered in .mcp.json for Claude Code"
    - "graphify-out/graph.json is generated for the repo"
  artifacts:
    - path: ".mcp.json"
      provides: "graphify stdio MCP server entry"
      contains: "graphify"
    - path: ".git/hooks/post-commit"
      provides: "auto graph-update hook (installed by `graphify hook install`)"
  key_links:
    - from: ".git/hooks/post-commit"
      to: "graphify-out/graph.json"
      via: "hook regenerates the graph on commit"
      pattern: "graphify"
---

<objective>
Activate graphify (repo knowledge graph) on this repository: install the tool (PyPI package `graphifyy`, CLI `graphify`), build the initial graph, install the post-commit auto-update hook, and register graphify's stdio MCP server for Claude Code.

Purpose: Satisfy the graphify half of Success Criterion 5.
Output: Installed graphify, generated `graphify-out/graph.json`, a post-commit hook, and a `.mcp.json` entry.

Requirement mapping: Success Criterion 5 (SC-5, graphify half).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-governance-substrate/01-RESEARCH.md

<interfaces>
<!-- Verified: PyPI name is `graphifyy` (two y's — name being reclaimed); CLI binary is `graphify`; Python 3.10+. -->
Install (Windows 11): `uv tool install graphifyy` (or `pipx install graphifyy` / `pip install graphifyy`).
Build graph: `graphify .`  ->  hook: `graphify hook install`
.mcp.json (repo root, stdio):
```json
{ "mcpServers": { "graphify": {
    "command": "python", "args": ["-m", "graphify.serve", "graphify-out/graph.json"],
    "type": "stdio" } } }
```
Windows gotchas (Pitfall 6): if `graphify` not found after `uv tool install`, run `uv tool update-shell` + open a new terminal; for pip add `%APPDATA%\Python\Python3xx\Scripts` to PATH; set `PYTHONUTF8=1` to avoid UnicodeEncodeError.
Committing graphify-out/ lets MCP work from a clean clone (Claude's discretion; default: commit graph.json, gitignore any large caches).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install graphify, build graph, install hook, register MCP</name>
  <files>.mcp.json, .gitignore, README.md</files>
  <action>
    On the Windows 11 dev machine: install graphify via `uv tool install graphifyy` (fall back to `pipx install graphifyy` or `pip install graphifyy`). If the CLI does not resolve, run `uv tool update-shell` and retry in a fresh shell; ensure `PYTHONUTF8=1` is set for the session. Verify with `graphify --version`.
    Run `graphify .` to build the initial graph (`graphify-out/graph.json`). Run `graphify hook install` to add the post-commit auto-update hook.
    Create `.mcp.json` at the repo root with the graphify stdio entry (interfaces block). Decide graphify-out/ commit policy: commit `graphify-out/graph.json` (so MCP works from a clean clone), gitignore any large intermediate caches — update `.gitignore` accordingly.
    Add a short "Repo knowledge graph (graphify)" section to README documenting install + the PYTHONUTF8/PATH Windows notes.
  </action>
  <verify>
    <automated>graphify --version && test -f graphify-out/graph.json && test -f .mcp.json && test -f .git/hooks/post-commit && echo OK</automated>
  </verify>
  <done>graphify CLI resolves; graph.json exists; post-commit hook installed; .mcp.json has the graphify stdio server; README documents setup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify graphify hook fires and MCP server connects (checkpoint)</name>
  <action>Human verification checkpoint — confirm the automated setup from Task 1 works end-to-end (hook auto-update + MCP connectivity in Claude Code). No new artifacts.</action>
  <what-built>graphify installed with post-commit hook + stdio MCP server registered in .mcp.json.</what-built>
  <how-to-verify>
    1. Make a trivial commit and confirm `graphify-out/graph.json`'s modified time updates (hook fired).
    2. Restart Claude Code so it loads `.mcp.json`; confirm the `graphify` MCP server appears/connects (e.g., via `/mcp` or tool availability).
  </how-to-verify>
  <verify>Post-commit hook refreshes graphify-out/graph.json; graphify MCP server shows connected in Claude Code.</verify>
  <done>User typed "approved" — hook and MCP both confirmed working.</done>
  <resume-signal>Type "approved" once the hook auto-updates on commit and the graphify MCP server is connected in Claude Code, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `graphify --version` resolves; `graphify-out/graph.json` present and refreshed after a commit.
- `.mcp.json` contains the graphify stdio server; `.git/hooks/post-commit` exists.
</verification>

<success_criteria>
graphify (git hook + MCP) is active on the repository — the graphify half of Success Criterion 5.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance-substrate/01-05-SUMMARY.md`
</output>
