---
phase: 01-foundation-governance-substrate
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/contracts/src/skill.ts
  - packages/contracts/skills/executive-agent.classifier.md
autonomous: true
requirements: [SC-6]

must_haves:
  truths:
    - "loadSkill('executive-agent.classifier') returns the active v1 body, version, and skillId"
    - "loadSkill throws (fails closed) when no active skill exists for a name"
    - "activateSkill flips statuses atomically (exactly one active row per name); body/name/version are never patched"
    - "The Executive Agent seed skill is stored in the skills table from a markdown source — no agent prompt is hardcoded in convex/"
  artifacts:
    - path: "packages/backend/convex/skills.ts"
      provides: "loadSkill loader (fails closed) + activateSkill (the ONE permitted status mutation) + idempotent seedSkills"
      contains: "by_name_status"
    - path: "packages/backend/convex/skills.test.ts"
      provides: "loader/activation/no-active-throws + no-hardcoded-prompt grep tests"
    - path: "packages/contracts/skills/executive-agent.classifier.md"
      provides: "seed Executive Agent classifier skill document (300-2000 token markdown)"
    - path: "packages/contracts/src/skill.ts"
      provides: "loadSkill return-type contract { body, version, skillId }"
  key_links:
    - from: "packages/backend/convex/skills.ts"
      to: "skills table by_name_status index"
      via: "loadSkill reads the single status==='active' row"
      pattern: "status.*active"
    - from: "convex/skills.ts seedSkills"
      to: "packages/contracts/skills/executive-agent.classifier.md"
      via: "seed body loaded from the markdown file, not inlined"
      pattern: "executive-agent.classifier"
---

<objective>
Build the versioned skills registry + loader contract (SkillOpt readiness) and seed the Executive Agent classifier skill from a markdown document — realizing "no agent prompt is hardcoded anywhere." Skills are immutable-per-version rows; changes = new version; rollback = re-activate a prior version.

Purpose: Satisfy Success Criterion 6 (skills registry + loader + seed skill loads from it).
Output: `convex/skills.ts` (loadSkill/activateSkill/seedSkills), the loader contract, the seed markdown, and passing loader/activation + no-hardcoded-prompt tests.

Requirement mapping: Success Criterion 6 (SC-6). Enables IMPR-02/IMPR-03 (Phase 8 SkillOpt) — see research/SKILLOPT.md.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-governance-substrate/01-RESEARCH.md
@.planning/phases/01-foundation-governance-substrate/01-VALIDATION.md
@.planning/research/SKILLOPT.md
@packages/backend/convex/schema.ts

<interfaces>
<!-- skills table (plan 01 schema): name, version, body, status(active|candidate|rolled_back|archived), evidence?, createdAt
     indexes: by_name_status ["name","status"], by_name_version ["name","version"] -->
Loader contract (packages/contracts/src/skill.ts):
```typescript
export type LoadedSkill = { body: string; version: number; skillId: string };
// loadSkill(ctx, name) -> LoadedSkill  — reads the single status==="active" row via by_name_status; THROWS if none.
```
Discipline: skill bodies immutable per version. A change = insert new row (version+1, status "candidate") + activateSkill(name, version) which atomically sets the prior active -> "archived"/"rolled_back" and the target -> "active". body/name/version are NEVER patched (only `status` and `evidence` may change). Seed = idempotent seedSkills internalMutation (skip if name already exists) inserting executive-agent.classifier v1 from the markdown file. skills.ts is an allow-listed internal-builder module (plan 02 allow-list).
Callers must record {name, version} in telemetry/audit for every use (Phase 8 evidence tracking).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing skills loader/activation + no-hardcoded-prompt tests (RED)</name>
  <files>packages/backend/convex/skills.test.ts</files>
  <behavior>
    convex-test:
    - after seedSkills, loadSkill("executive-agent.classifier") returns { body (non-empty), version: 1, skillId }
    - loadSkill("nonexistent") throws (fails closed)
    - inserting a v2 candidate then activateSkill("executive-agent.classifier", 2) results in exactly one active row (v2), prior v1 no longer "active"; the v1 body row still exists unchanged (immutability)
    - seedSkills run twice inserts only one v1 row (idempotent)
    grep test: scan convex/ (esp. any agent/executive paths) and assert no long inline prompt string constants — prompts must come from the registry.
  </behavior>
  <action>Write the test file. It MUST fail (skills.ts absent). Command: `pnpm --filter backend vitest run convex/skills.test.ts`.</action>
  <verify>
    <automated>cd packages/backend && npx convex codegen && pnpm vitest run convex/skills.test.ts || echo "RED as expected"</automated>
  </verify>
  <done>skills.test.ts exists and fails (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loader contract + seed skill markdown + skills module (GREEN)</name>
  <files>packages/contracts/src/skill.ts, packages/contracts/skills/executive-agent.classifier.md, packages/backend/convex/skills.ts</files>
  <action>
    `packages/contracts/src/skill.ts`: export `LoadedSkill` type + the loader signature contract. `packages/contracts/skills/executive-agent.classifier.md`: write the seed Executive Agent classifier skill document (300-2000 token markdown describing how the Executive Agent classifies a request into direct-tool / specialized-sub-agent / direct-LLM routing — this seeds AGNT-01 behavior for Phase 2). Implement `convex/skills.ts`:
      - `loadSkill(ctx, name)`: query by_name_status for status==="active"; throw "NO_ACTIVE_SKILL" if none; return { body, version, skillId }.
      - `activateSkill` (internalMutation, the ONE permitted status mutation): within a single mutation, set the current active row -> "archived" and the target version -> "active"; never patch body/name/version.
      - `seedSkills` (internalMutation, idempotent): if no row for "executive-agent.classifier" exists, insert v1 status "active" with body read from the markdown (import as string or embed via a build-safe mechanism — do NOT hardcode a prompt inline; load the file content).
    Keep convex/skills.ts a thin adapter; any versioning helpers that are pure logic go in packages/contracts.
  </action>
  <verify>
    <automated>cd packages/backend && npx convex codegen && pnpm vitest run convex/skills.test.ts</automated>
  </verify>
  <done>Loader/activation/idempotent-seed/no-hardcoded-prompt tests pass (GREEN); seed skill body originates from the markdown file.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend vitest run convex/skills.test.ts` green.
- loadSkill fails closed with no active skill; activateSkill flips atomically; bodies immutable per version.
- No hardcoded agent prompt strings in convex/.
</verification>

<success_criteria>
Success Criterion 6 met: a versioned skills registry + loader contract exists, the Executive Agent's seed skill loads from it, and no agent prompt is hardcoded.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance-substrate/01-04-SUMMARY.md`
</output>
