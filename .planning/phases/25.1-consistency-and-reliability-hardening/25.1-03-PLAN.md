---
phase: 25.1-consistency-and-reliability-hardening
plan: 03
type: execute
wave: 3
depends_on: ["25.1-01"]
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/media.ts
  - packages/backend/convex/media.test.ts
  - packages/backend/convex/render/renderReel.ts
  - packages/backend/convex/render/renderReel.test.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/plans.test.ts
  - docs/playbooks/media.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/vault.md
autonomous: true
requirements: [D5, D6, D7, D8]
must_haves:
  truths:
    - "A successfully generated image exists as a vaultDocuments row (kind 'image') the user can find after the thread's plan row is recycled"
    - "A reel reaches the vault at the RENDER terminal even when captions are pending; the caption burn re-points the same doc"
    - "A second reel in the same thread gets its OWN vault doc — the first reel's doc and mp4 survive"
    - "A plan that produced one image can produce another (the guard only blocks genuinely in-flight jobs)"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      contains: "\"image\" in vaultDocuments.kind union; vault-doc pointer field on mediaJobs"
    - path: "packages/backend/convex/media.ts"
      provides: "Image vault save at generation success; in-flight-only second-image guard"
    - path: "packages/backend/convex/render/renderReel.ts"
      provides: "saveReelToVault called at the render terminal regardless of captionsComing"
    - path: "packages/backend/convex/plans.ts"
      provides: "resetPlan clears reelVaultDocId"
  key_links:
    - from: "image success landing (media.ts:1174-1179)"
      to: "vaultDocuments insert + startIngest"
      via: "saveReelToVault pattern (renderReel.ts:606-706), idempotent via mediaJobs pointer field"
    - from: "render terminal (renderReel.ts:507-521)"
      to: "saveReelToVault"
      via: "removal of the !captionsComing gate"
    - from: "resetPlan (plans.ts:864-945)"
      to: "reelVaultDocId cleared"
      via: "one field added to the reset patch"
---

<objective>
Close D5–D8: generated media must reach the vault and survive plan recycling. Images get a vault save (none exists today — bytes strand on `mediaJobs.assetStorageId`); reels save at the render terminal instead of only after caption burn; `resetPlan` stops the second reel from destroying the first reel's vault doc; the second-image `already_started` refusal is scoped to genuinely in-flight jobs.

Purpose: today a user's delivered media silently vanishes (unreachable images, unvaulted reels, overwritten reel #1) — the "vanishing media" half of the phase goal.
Output: vault persistence for both media kinds, regression-proven.
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-RESEARCH.md (D5–D8)
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-CONTEXT.md (vault persistence decisions — binding)
@docs/playbooks/media.md
@docs/playbooks/vault.md
@docs/playbooks/cockpit.md

Notes for the executor:
- Wave-1 plan 25.1-01 already changed renderReel.ts (terminals + retrier). Rebase your mental model on the committed file, not on the research line numbers alone.
- Schema changes are ADDITIVE ONLY: widen `vaultDocuments.kind` with `"image"` (schema.ts:1592-1622 region) and add an optional vault-doc pointer to mediaJobs (e.g. `vaultDocId`) as the idempotency guard for the image save. Per-JOB pointer, not per-plan — after D8's fix a plan can hold several images. No migration needed for additive members.
- The reel upsert stays keyed on plans.reelVaultDocId (renderReel.ts:655-666); D7's fix is one field in resetPlan's clear-set, NOT a re-keying.
- deleteOrphanedFinals (renderReel.ts:717-733): trace it before editing — after resetPlan clears reelVaultDocId, reel #2 saves to a NEW doc and must NOT delete reel #1's mp4. The orphan cleanup must only ever delete storage the CURRENT doc previously pointed at.
- Working tree already carries a foreign `M schema.ts` — read the current file, make your additive edit, and stage schema.ts ONLY if your edit is the sole hunk you introduce; if foreign hunks exist in schema.ts, stage with `git add -p`-equivalent care (stage the file — schema edits are additive and the foreign hunk likely belongs to a merged lane; verify `git diff` before staging).
- Tenant wrappers only; vault doc text = the PROMPT (per CONTEXT) — that is user content going to the USER's own vault, which is fine; audit rows still ids/counts only.
- Tests from inside packages/backend; plain `git commit`; CRLF single-line anchors for playbooks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: image vault save + schema widening (D5)</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/media.ts, packages/backend/convex/media.test.ts, docs/playbooks/vault.md, docs/playbooks/media.md</files>
  <behavior>
    - Test: image generation success landing → a `vaultDocuments` row exists with `kind: "image"`, the prompt as text, the stored image bytes' storageId, correct tenantId; the mediaJobs row carries the vault doc pointer.
    - Test: running the landing twice for the same job does not create a second doc (idempotent via the pointer).
    - Test: tenant scoping — the doc belongs to the job's tenant (mirror existing vault-save tenant assertions).
  </behavior>
  <action>
    Widen `vaultDocuments.kind` with `"image"` and add optional `vaultDocId` to mediaJobs in schema.ts. At the image success landing (media.ts:1174-1179), insert a vaultDocuments row mirroring `saveReelToVault` (renderReel.ts:606-706): title from the prompt (truncated), text = prompt, storageId = assetStorageId, then `startIngest` as the reel save does; patch the job with the doc id. Skip if the pointer is already set. Update vault.md (new doc kind, provenance = generated image, prompt-as-text) and media.md in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/media.test.ts</automated>
  </verify>
  <done>Every image success produces exactly one tenant-scoped vault doc; recycling the plan row no longer strands the image.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reel save at render terminal, resetPlan clears reelVaultDocId, in-flight-only image guard (D6, D7, D8)</name>
  <files>packages/backend/convex/render/renderReel.ts, packages/backend/convex/render/renderReel.test.ts, packages/backend/convex/plans.ts, packages/backend/convex/plans.test.ts, packages/backend/convex/media.ts, packages/backend/convex/media.test.ts, docs/playbooks/media.md, docs/playbooks/cockpit.md</files>
  <behavior>
    - D6 test: render terminal with `captionsComing === true` → vault doc saved (today the `!captionsComing` gate at renderReel.ts:507-521 blocks it and captions are pinned on at cockpit.ts:869,890-894 and media.ts:2400-2410, so the render terminal never saves). Caption-burn success (renderReel.ts:980-985) re-points the SAME doc (doc count stays 1, storageId updated).
    - D7 regression test: resetPlan → `reelVaultDocId` is cleared; after a second render, TWO vault docs exist and the first doc's storageId is untouched (deleteOrphanedFinals did not eat reel #1's mp4).
    - D8 test: a plan whose only mediaJobs row is `succeeded` → `generateImage` accepts; a plan with a `queued` or `submitted` row → still refuses `already_started`.
  </behavior>
  <action>
    Remove the `!captionsComing` gate on the render-terminal `saveReelToVault` call (keep the caption-burn call sites; the upsert makes the double-save converge). Add `reelVaultDocId` to resetPlan's clear-set (plans.ts:864-945). Scope `generateImage`'s guard (media.ts:2477-2504) to `queued|submitted` only — `succeeded` no longer blocks (nothing ever deletes mediaJobs rows, so `succeeded` was a permanent lock). Verify deleteOrphanedFinals only deletes storage previously pointed at by the CURRENT doc. Update media.md (save points, reset semantics, guard scope) and cockpit.md (plans.ts resetPlan contract) in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/render/renderReel.test.ts convex/plans.test.ts convex/media.test.ts</automated>
  </verify>
  <done>Reels vault at render terminal even with captions pending; reel #2 cannot destroy reel #1; second images possible; all three focused suites green.</done>
</task>

</tasks>

<verification>
- `cd packages/backend && npx vitest run convex/media.test.ts convex/plans.test.ts convex/render/renderReel.test.ts` green.
- `npx convex codegen`/typecheck clean after the schema widening (run `npx tsc --noEmit` in packages/backend if the gate is too slow here).
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
Both media kinds reach the vault at their success terminals; nothing a user generated can be silently destroyed or stranded by generating again.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-03-SUMMARY.md`
</output>
