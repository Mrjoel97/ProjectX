---
phase: 25.1-consistency-and-reliability-hardening
verified: 2026-08-21T17:05:00Z
status: human_needed
score: 14/14 defects closed in code; 5 of 14 remain browser/live-unverified (per 25.1-07's own admission)
human_verification:
  - test: "Click Approve on a plan that starts (email/finance/calendar), confirm the success card persists after the row leaves the awaiting list; approve a second, different plan and confirm only its own success card shows"
    expected: "A persistent 'Plan started…' card remains visible after the row drops out of the awaiting list, keyed to the correct plan"
    why_human: "D9 code is unit-proven via renderToStaticMarkup only (25.1-04's own admission); never exercised through a live click. 25.1-07's production reel came from the workspace canvas Generate button, not /dashboard/approvals, so this path is still unexercised in production."
  - test: "Generate a standalone image from the workspace canvas, then check /dashboard/vault for a kind:\"image\" row; generate a second image on the same plan"
    expected: "An image vaultDocuments row appears after generation; a second image on the same plan is not blocked by 'already_started'"
    why_human: "25.1-07's read-only production check found ZERO kind:\"image\" rows in production — no image has been generated since the code shipped. D5/D8 are unit-proven only."
  - test: "Run a research/memo specialist turn that performs a web search, open the resulting memo card"
    expected: "Headings and bold render as HTML elements (not literal # / **); a references block with title, URL and retrieval date appears below the body"
    why_human: "25.1-07 explicitly logs this as NOT attested — a stored vault research doc proves nothing about card rendering, which is the actual defect surface."
  - test: "Trigger a dead letter (or wait for one) and open /dashboard/ops as the owner"
    expected: "A 'Dead letters — all tenants' section lists id/kind/status/age/counts, gated to owner only"
    why_human: "25.1-07 explicitly logs this as NOT attested."
  - test: "After watching a real render go through the sweep window with RELIABILITY_SWEEP_ARMED unset, arm it (npx convex env set --prod RELIABILITY_SWEEP_ARMED 1) and observe the first pass"
    expected: "Stuck submitted/rendering/transcribing/collecting rows terminalize with a notification; fresh/in-flight rows are untouched"
    why_human: "Deliberately shipped dormant by owner decision (arming gate confirmed in code and confirmed unset on prod by 25.1-07). This is intentional non-deployment of live behavior, not a code gap — but the watchdog's real behavior against production data volume (10 plans currently stuck at 'collecting' in prod per 25.1-07) has never been observed with the gate open."
---

# Phase 25.1: Consistency and Reliability Hardening Verification Report

**Phase Goal:** Every governed background job reaches an honest terminal state the user can see — no silent stalls, no vanishing media, no mute Approve button. Scope = the verified defect inventory D1–D14 in 25.1-RESEARCH.md.

**Verified:** 2026-08-21T17:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Method

Goal-backward, source-level verification against 25.1-RESEARCH.md's D1–D14 inventory and each plan's `must_haves`. Every claim below was checked against the actual committed source on this branch (`docs/agent-runtime-format-bump` = main + docs bump + the new, still-uncommitted `25.1-07-SUMMARY.md`), not against SUMMARY prose. All eight focused backend suites and the two focused web suites named by the six execute-plans were re-run fresh as part of this verification (results below) rather than trusted from the SUMMARYs.

## Goal Achievement

### Per-Defect Verification (D1–D14)

| # | Defect | Code evidence (this verification, not the SUMMARY) | Status |
|---|---|---|---|
| D1 | batchToRender refusal wrote no terminal | `render/renderReel.ts:766` `if (!batch.ok)` → `recordRender` with `result:{ok:false, reason: batch.reason}` before returning — no more bare return. `route_bad_response` inline-terminalizes a non-JSON 200 (`:845-847`). | ✓ VERIFIED |
| D2 | renderReel scheduled bare, no retrier/onComplete | All three call sites use `retrier.run` with `{ onComplete: internal.mediaComplete.onRenderComplete }`: `mediaComplete.ts:208`, `media.ts:2586`, `render/renderReel.ts:415`. `grep -rn "scheduler.runAfter.*renderReel"` returns zero hits (only `burnCaptions`'s unrelated bare runAfter at `mediaComplete.ts:296`, which is out of D2's scope). | ✓ VERIFIED |
| D3 | poll chains sever; nothing sweeps stuck states | `reliabilitySweep.ts` — `sweepStuckMediaJobs` (submitted), `sweepStuckPlans` (rendering/pending, transcribing) as `migrations.define` sweeps; `crons.ts:62` registers `crons.interval("reliability-sweep", {minutes:30}, internal.reliabilitySweep.runSweep, {})`. **Ships behind an arming gate** — `reliabilitySweep.ts:353`: `if (process.env.RELIABILITY_SWEEP_ARMED !== "1") return null;`. This is an explicit, documented owner decision (2026-08-21, recorded in-file and in `lib/env.ts`'s `RELIABILITY_SWEEP_ARMED` manifest entry) made because the sweep writes failure terminals to live rows and had never run against real data. **Confirmed unset on production** per 25.1-07. Not a gap — but it means D3/D4's live behavior is unobserved. | ✓ VERIFIED (code) / dormant by design |
| D4 | specialist death leaves plans at collecting forever, invisibly | `reliabilitySweep.ts` `sweepStuckPlans` includes the `collecting` branch (`COLLECTING_STALL_MS`, liveness read from `_scheduled_functions` by `args.planId`, terminal = `proposed` + system-voice body — an existing status `PlanCard` already renders). Same arming gate as D3. 25.1-07's production read found **10 plans genuinely stuck at `collecting`** (17h–5d old), empirically confirming D4 was real — this is strong corroborating evidence the fix targets a real, currently-occurring defect. | ✓ VERIFIED (code) / dormant by design |
| D5 | images never reach the vault | `mediaComplete.ts:339` `saveImageToVault`, called from `landResult`'s success arm; idempotent via `mediaJobs.vaultDocId` (schema.ts:2160). **Zero live images generated in production since ship** (25.1-07) — code-verified only. | ✓ VERIFIED (code) / not live-exercised |
| D6 | reel vault save gated on captions, never saves | `render/renderReel.ts:509-521,519` — `saveReelToVault` now called at the render terminal unconditionally (comment at `:509` confirms the `!captionsComing` gate was removed); caption-burn call sites (`:983`, `:1009`) re-point the same doc via upsert. Live-confirmed by 25.1-07: production `vaultDocuments` row `kind:"reel"` with real storageId, ingested (`ragEntryId` set), created 30.6 min after deploy. | ✓ VERIFIED (code + live) |
| D7 | second reel destroys first reel's vault doc | `plans.ts:955` `reelVaultDocId: undefined` added to `resetPlan`'s clear-set. | ✓ VERIFIED (code) |
| D8 | plan can never produce a second image | `media.ts:2492` guard narrowed to `row.status === "queued" \|\| row.status === "submitted"` (was previously including `succeeded`, which — since nothing ever deletes `mediaJobs` rows — permanently blocked a second image). | ✓ VERIFIED (code) / not live-exercised |
| D9 | Approve success unmounts before message renders; refusal buried; false "already started" | `ApprovalsView.tsx:201` `persistentOutcomes`, `:719` `ResolvedOutcomeCard`, `:153` `STALE_PLAN_MESSAGE`, notice rendered as the FIRST child of the card (`:590-596`, above the `:598` row and `:648` actions block). Discard/schedule route through the same `onOutcome` channel (root-cause fix, not per-caller patch). Never exercised live — 25.1-07's production reel was generated via canvas, not `/dashboard/approvals`. | ✓ VERIFIED (code) / not live-exercised |
| D10 | image plans' Approve button is structurally incapable of succeeding | `ApprovalsView.tsx:582` `const imagePlan = item.kind === "image"`; `:650` renders `IMAGE_CANVAS_NOTE` instead of the Approve button when `imagePlan` is true (`:657` `{!imagePlan && <button.../>}`). | ✓ VERIFIED (code) |
| D11 | memo card renders raw markdown; sources withheld | `cards.tsx:488` `MemoCardBody` renders `body` through `MarkdownDocument` (compact) and a references block (`:494-525`) keyed on `plan.sources` non-empty. Backend threading: `schema.ts:494` `plans.sources` (additive optional), `dispatch.ts:1177` `sources: res.sources.map(...)` into `landSpecialistResult`, `evaluations.ts:1121-1122` patches the plan row. `plans.ts:875` `resetPlan` clears `sources`. Audit-plane confinement verified: `dispatch.ts:181-184` doc comment + `persistResearchFindings` (`:1108-1120`) explicitly state sources feed the vault doc and card, never the audit payload; `dispatch.test.ts`'s runtime §4 scan (per SUMMARY) walks real audit rows for leaked URLs and stays green. Not live-exercised (25.1-07 explicit non-attestation — a stored vault doc proves nothing about card rendering). | ✓ VERIFIED (code) / not live-exercised |
| D12 | env manifest blind spots; dead fal code lingers as an invisible readiness hole | `lib/env.ts:150,164,172` — `MEDIA_RENDER_URL`, `WAN_API_BASE_URL`, `Video_and_image_API_Key` now classified; `FAL_WEBHOOK_SECRET` removed (tombstone comment only, `:155-156`). `MEDIA_SANDBOX_SNAPSHOT_ID` deliberately NOT added — documented reasoning in `production-beta.md:111` (it's a Vercel-only var no Convex process reads; listing it would make `envCheck` cry wolf on every healthy deployment) — this is a reasoned deviation from the plan's stated must_have, not an oversight, and matches CONTEXT's "where the readiness pattern can reach it" allowance. | ✓ VERIFIED (with one documented, justified deviation) |
| D13 | DLQ is write-only | `deadLetters.ts:69` `listAll` — `ownerQuery`, cross-tenant, bounded, newest-first. `ops/page.tsx:714` `AllTenantDeadLetters`, mounted at `:884-886` under "Dead letters — all tenants", owner-gated by mounting. **Correction to the original research premise** (recorded in the SUMMARY, verified true): a tenant-scoped `listNew`/`newCount` + Mark-resolved UI already existed since phase 02-06 — the actual gap D13 closed was the owner's inability to see OTHER tenants' failures. Not attested live (25.1-07 explicit). | ✓ VERIFIED (code) / not live-exercised |
| D14 | ADR drift (fal.ai/Wan still documented as current) | `docs/decisions/024-openai-sora-is-the-media-provider.md` exists; ADR-017 carries a Status-line pointer to ADR-024 (not a body edit — `git diff` confirms ADR-011/012 byte-unchanged). Correctly supersedes ADR-017 (which had already superseded ADR-011's provider line) rather than ADR-011/012 directly — this is a more accurate correction than the plan's own instruction, recorded and justified in the SUMMARY. | ✓ VERIFIED |

### Observable Truths (rolled up from must_haves across the 6 plans)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Every batchToRender refusal leaves an honest failed terminal; retryRender becomes usable | ✓ VERIFIED | D1 evidence above; `render/renderReel.test.ts` passing (see Test Run below) |
| 2 | A crash after markRendering also terminalizes, never a stuck "rendering" row | ✓ VERIFIED | D2 evidence above |
| 3 | Stuck submitted/rendering/transcribing/collecting states terminalize with a notification, fresh rows untouched | ✓ VERIFIED (code) / human_needed (live) | D3/D4 evidence; dormant by owner decision |
| 4 | A generated image reaches the vault and survives plan recycling | ✓ VERIFIED (code) / human_needed (live) | D5 evidence; zero live images generated to date |
| 5 | A reel reaches the vault at the render terminal even with captions pending; a second reel doesn't destroy the first | ✓ VERIFIED (code + live) | D6/D7 evidence; live-confirmed by 25.1-07 |
| 6 | A plan can produce a second image | ✓ VERIFIED (code) | D8 evidence |
| 7 | Approve success survives the row's unmount; refusal is prominent; stale copy is honest | ✓ VERIFIED (code) / human_needed (live) | D9 evidence; never clicked live |
| 8 | Image plans never show a doomed Approve button | ✓ VERIFIED (code) | D10 evidence |
| 9 | Memo cards render formatted markdown with a sources block; audit stays URL-free | ✓ VERIFIED (code) / human_needed (live) | D11 evidence |
| 10 | Env drift/readiness can no longer lie about media vars; dead fal code and stale ADRs are gone; DLQ is visible | ✓ VERIFIED (code) / human_needed (live for DLQ UI) | D12–D14 evidence |

**Score:** 10/10 truths verified in code; 5 of 10 also confirmed live (D6/D7's reel path via 25.1-07); 5 remain browser/live-unverified by the phase's own honest accounting (D3/D4 dormant by design, D5/D8 image path unexercised, D9 approvals click unexercised, D11 card rendering unattested, D13 DLQ UI unattested).

### Test Run (re-executed fresh for this verification, not trusted from SUMMARYs)

```
packages/backend: npx vitest run convex/render/renderReel.test.ts convex/reliabilitySweep.test.ts \
  convex/media.test.ts convex/plans.test.ts convex/dispatch.test.ts convex/deadLetters.test.ts \
  convex/env.test.ts convex/llmRedaction.test.ts
  → 8 files, 497 tests, all passed

packages/backend: npx vitest run convex/isolation.test.ts
  → 32/32 passed (deferred-items.md item 1 part-1 fixture is present and green — matches the
    project memory / deferred-items.md claim, not stale)

apps/web: npx vitest run "app/(app)/dashboard/approvals/approvalsView.test.ts" \
  "app/(app)/dashboard/workspace/memoCard.test.ts"
  → 2 files, 44 tests, all passed

node scripts/check-playbooks.mjs → exit 0
```

No stub/placeholder/TODO anti-patterns found in the touched core files (`reliabilitySweep.ts`, `render/renderReel.ts`, `mediaComplete.ts`, `deadLetters.ts`).

### CONTEXT.md fenced-out scope — confirmed NOT done

- No batch content creation / content scheduling (audit G10) — not present.
- No document canvas (G5) — not present.
- No research-depth change (G4, page fetching/model pins) — `dispatch.ts` comments explicitly confirm research depth is untouched; only presentation + one hop of threading changed.
- No durable-agent refactor onto the Workflow component (G6) — dispatch remains bare `ctx.scheduler.runAfter` for specialist calls per design (D3/D4's watchdog covers the failure mode instead, as CONTEXT specified).
- Production deploy: NOT part of any of the six execute-plans. The 25.1-07 checkpoint records the owner independently chose to deploy to production and adjudicate there — a documented, in-session owner decision, not an automated phase action. Matches the standing deploy-gate policy (re-ask every time).

### Requirements Coverage

No requirement IDs were minted for this phase (explicitly an inserted hardening phase per ROADMAP.md and the phase brief). Coverage was assessed against the D1–D14 defect inventory instead, per the task's own instruction — see the per-defect table above. No orphaned requirements exist because none were declared.

### Anti-Patterns Found

None blocking. One process note: `.planning/ROADMAP.md`'s Phase 25.1 checklist still shows `25.1-07-PLAN.md` as `[ ]` unchecked even though its SUMMARY (PARTIAL PASS) now exists on disk — this matches the known gsd-tools gotcha (roadmap checkbox/table staying stale after a `phase complete`-adjacent step) and should be hand-corrected when this phase is closed out, not treated as a code defect.

### Human Verification Required

See the `human_verification` block in the frontmatter. In summary, five surfaces are code-complete and unit/component-tested but have never been exercised through a browser or live click:

1. **Approvals persistent success/refusal UI (D9)** — never clicked live; the one live reel in production went through the canvas Generate button, not `/dashboard/approvals` Approve.
2. **Image generation → vault save + second-image guard (D5/D8)** — zero images generated in production since this code shipped.
3. **Memo card markdown + sources rendering (D11)** — a stored vault research doc exists in production, but that proves nothing about card rendering, which is the actual defect surface.
4. **DLQ ops-page listing (D13)** — never viewed in a browser as the owner.
5. **Reliability sweep live behavior (D3/D4)** — intentionally shipped dormant (`RELIABILITY_SWEEP_ARMED` unset); confirmed 10 real production plans are currently stuck at `collecting`, so there is a ready, real test case waiting once the owner arms the gate after watching one more healthy render pass through unswept.

### Gaps Summary

No code-level gaps were found — every one of D1–D14's fixes exists in the codebase exactly as claimed, matches its file:line evidence, and passes its own test suite (independently re-run for this verification, not trusted from the SUMMARYs). The mutation-testing discipline documented across all six plans' SUMMARYs (each mutation reddening exactly one test, several rounds of "this test was vacuous, here's the fix") is corroborated by the fact that a spot-check of the actual guard code (D1's terminal write, D6's gate removal, D8's guard narrowing, D9's DOM order, D10's conditional) matches what the tests claim to pin.

The phase's own 25.1-07 UAT checkpoint is honest about its limits — it explicitly declines to claim a "seven-point pass" and lists exactly which five surfaces remain unattested. This verification independently confirms that same boundary rather than finding anything the checkpoint missed or overclaimed. The status is `human_needed`, not `gaps_found`, because nothing SUMMARY-claimed is missing or stubbed — the remaining work is exercising already-shipped, already-tested code through a live browser session (and, for D3/D4, an owner decision to arm a gate that was deliberately left dormant).

---

_Verified: 2026-08-21T17:05:00Z_
_Verifier: Claude (gsd-verifier)_
