---
phase: 25.1-consistency-and-reliability-hardening
plan: 02
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - packages/backend/convex/reliabilitySweep.ts
  - packages/backend/convex/reliabilitySweep.test.ts
  - packages/backend/convex/crons.ts
  - docs/playbooks/watch.json
  - docs/playbooks/media.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/audit-dead-letter.md
autonomous: true
requirements: [D3, D4]
must_haves:
  truths:
    - "A mediaJobs row stuck at 'submitted' past the threshold is terminalized to failed and the user gets a notification"
    - "A plan stuck at renderStatus 'rendering'/'pending' or captionStatus 'transcribing' past the threshold is terminalized with a reason"
    - "A plan stuck at status 'collecting' (specialist died before landing) becomes user-visible instead of invisible forever"
    - "Fresh (under-threshold) rows are never touched by the sweep"
  artifacts:
    - path: "packages/backend/convex/reliabilitySweep.ts"
      provides: "Watchdog sweep internal functions (mediaJobs, renderStatus, captionStatus, collecting)"
    - path: "packages/backend/convex/crons.ts"
      contains: "reliability sweep cron registration"
    - path: "docs/playbooks/watch.json"
      contains: "packages/backend/convex/reliabilitySweep.ts"
  key_links:
    - from: "crons.ts new interval job"
      to: "internal.reliabilitySweep.runSweep"
      via: "cron registration (pattern crons.ts:43-50 vault-pending-extraction-sweep)"
    - from: "reliabilitySweep terminal writes"
      to: "notifications insert per affected tenant"
      via: "agent.timeout notification pattern (cockpit.ts:80-88,161-166)"
---

<objective>
Close D3 and D4: one watchdog cron that sweeps every stuck non-terminal state to a failure terminal plus a user-visible notification — mediaJobs stuck `submitted` (severed poll chain), plans stuck `renderStatus: "rendering"|"pending"` past threshold, `captionStatus: "transcribing"` stranded, and plans stuck `status: "collecting"` (specialist action died before `dispatchAndLand` could land it — today the card is invisible forever, PlanCard renders only at `proposed`, evaluations.ts:891-892).

Purpose: severed scheduler chains are the second silent-stall class; the fix pattern is proven in-repo (`vault-pending-extraction-sweep` was added for this exact failure shape).
Output: `reliabilitySweep.ts` + cron + table-driven threshold tests.
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-RESEARCH.md (D3, D4)
@packages/backend/convex/vaultSweep.ts (the pattern to mirror — structure, wrapper usage, allow-list posture)
@packages/backend/convex/crons.ts (four existing jobs; register the fifth here)
@docs/playbooks/media.md
@docs/playbooks/cockpit.md
@docs/playbooks/audit-dead-letter.md

Notes for the executor:
- Mirror `vaultSweep.ts` exactly for module shape and wrapper/allow-list posture — do not invent a new sweep architecture.
- Self-contained by design: do NOT edit media.ts / renderReel.ts / plans.ts / dispatch.ts in this plan (they belong to other waves). The sweep patches rows directly via its own internal mutations.
- Caption interplay: the sweep only TERMINALIZES a stranded `transcribing`; it does not attempt the vault save — plan 25.1-03 moves the reel vault save to the render terminal, so a swept caption strand still leaves a vaulted reel.
- Thresholds (Claude's discretion per CONTEXT, defaults unless you find better constants in-code): submitted > 45 min (poll cap is 180×10s = 30 min), rendering > 60 min, transcribing > 30 min, collecting > 30 min; cadence: every 30 min. Name them as exported constants so tests pin them.
- `collecting` terminal: inspect the plans status union in schema.ts and pick an existing terminal the UI actually renders honestly (refused/failed-class) — the row must stop being invisible; pair it with a notification naming the thread. Do not add a new status member unless no honest terminal exists (additive union member is allowed if needed).
- Notifications: call the existing notifications insert (agent.timeout pattern, cockpit.ts:80-88,161-166) — do not modify notifications.ts. Notification copy: short, honest, no content ("A background job stalled and was marked failed — open the workspace thread to retry").
- Payload hygiene: any audit/dead-letter write carries ids/counts/reason only.
- Stage only this plan's files; plain `git commit`; CRLF single-line anchors for playbooks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reliabilitySweep module with threshold tests (D3, D4)</name>
  <files>packages/backend/convex/reliabilitySweep.ts, packages/backend/convex/reliabilitySweep.test.ts</files>
  <behavior>
    Seed stuck rows and run the sweep handler (convex-test):
    - mediaJobs `submitted` older than threshold → terminal failed state (mirror what `poll_timeout` writes, media.ts:1404-1408) + notification row for that tenant.
    - plan `renderStatus:"rendering"` stale → `renderStatus:"failed"` + `renderReason:"watchdog_timeout"` + notification. Same for stale `"pending"` (only when armed — use the timestamps that arm the render trigger, e.g. shotsChangedAt/updatedAt, so an un-triggered plan is not swept).
    - plan `captionStatus:"transcribing"` stale → caption failure terminal + notification.
    - plan `status:"collecting"` stale → chosen honest terminal + notification.
    - Boundary: rows JUST under each threshold are untouched (assert unchanged).
    - Transposition-hardening: assert each terminal's reason string per state class (swapping two reasons must redden).
  </behavior>
  <action>
    New file `packages/backend/convex/reliabilitySweep.ts`: an internal sweep entry (`runSweep`) plus whatever internal mutation(s) `vaultSweep.ts` precedent suggests, iterating the four stuck-state queries with indexed/bounded reads. Exported threshold constants. Explicit return types on internal functions. Per-tenant notification via existing notifications insert. No edits to other feature modules.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/reliabilitySweep.test.ts</automated>
  </verify>
  <done>All four stuck states terminalize with notifications; boundary cases proven untouched; module mirrors vaultSweep shape.</done>
</task>

<task type="auto">
  <name>Task 2: cron registration + watch.json + playbooks</name>
  <files>packages/backend/convex/crons.ts, docs/playbooks/watch.json, docs/playbooks/media.md, docs/playbooks/cockpit.md, docs/playbooks/audit-dead-letter.md, packages/backend/convex/reliabilitySweep.test.ts</files>
  <action>
    Register the sweep in crons.ts using the `vault-pending-extraction-sweep` pattern (crons.ts:43-50) at the chosen cadence (interval, every 30 min — cheaper than a daily cron for a stall watchdog and still lazy). Add `packages/backend/convex/reliabilitySweep.ts` and its test to the `media.md` entry in docs/playbooks/watch.json (the Stop hook blocks uncovered new code files). Update playbooks in the same commit: media.md (watchdog thresholds + what it sweeps), cockpit.md (collecting-plan terminal semantics — a dead specialist now surfaces), audit-dead-letter.md (crons.ts gained the fifth job; notification usage). Add one test asserting crons.ts registers the sweep (source scan or convex-test crons introspection, matching however existing tests pin the four current jobs — if none do, a literal-string source scan is acceptable and lazy).
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/reliabilitySweep.test.ts && node ../../scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Cron registered; watch.json covers the new files; three playbooks bumped in the same commit; check-playbooks exit 0.</done>
</task>

</tasks>

<verification>
- `cd packages/backend && npx vitest run convex/reliabilitySweep.test.ts` green.
- crons.ts has exactly five jobs; watch.json diff covers reliabilitySweep files.
</verification>

<success_criteria>
No stuck non-terminal state can outlive one sweep interval without becoming a failed terminal plus a user-visible notification; fresh work is never touched.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-02-SUMMARY.md`
</output>
