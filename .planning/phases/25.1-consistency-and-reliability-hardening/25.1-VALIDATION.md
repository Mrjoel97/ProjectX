---
phase: 25.1
slug: consistency-and-reliability-hardening
status: planned
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-21
---

# Phase 25.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (convex-test in packages/backend; component tests in apps/web) |
| **Config file** | packages/backend/vitest config (existing); apps/web vitest (existing) |
| **Quick run command** | `cd packages/backend && npx vitest run <focused files>` (MUST cd into the package — `--root` from repo root breaks convex-test's `_generated` glob) |
| **Full suite command** | `pnpm gate` (typecheck + test + build, --force) |
| **Estimated runtime** | focused ~30-90s; full gate several minutes |

---

## Sampling Rate

- **After every task commit:** Run the focused vitest files touched by the task (from `packages/backend` or `apps/web` respectively)
- **After every plan wave:** Run `pnpm gate`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~120 seconds (focused runs)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01-T1 refusal terminals | 25.1-01 | 1 | D1 | convex-test (table-driven refusal classes) | `cd packages/backend && npx vitest run convex/render/renderReel.test.ts` | created in-task (new file) | ⬜ pending |
| 01-T2 retrier + onComplete | 25.1-01 | 1 | D2 | convex-test (onRenderComplete unit + non-JSON guard) | `cd packages/backend && npx vitest run convex/render/renderReel.test.ts convex/media.test.ts` | renderReel.test.ts from 01-T1; media.test.ts exists | ⬜ pending |
| 02-T1 watchdog sweep | 25.1-02 | 2 | D3, D4 | convex-test (seed stuck rows + threshold boundaries) | `cd packages/backend && npx vitest run convex/reliabilitySweep.test.ts` | created in-task (new file) | ⬜ pending |
| 02-T2 cron + watch.json | 25.1-02 | 2 | D3 | source-scan / registration test + playbook hook | `cd packages/backend && npx vitest run convex/reliabilitySweep.test.ts && node ../../scripts/check-playbooks.mjs` | from 02-T1 | ⬜ pending |
| 03-T1 image vault save | 25.1-03 | 3 | D5 | convex-test (doc exists, idempotent, tenant-scoped) | `cd packages/backend && npx vitest run convex/media.test.ts` | exists | ⬜ pending |
| 03-T2 reel save/reset/guard | 25.1-03 | 3 | D6, D7, D8 | convex-test (render-terminal save, resetPlan regression, in-flight guard) | `cd packages/backend && npx vitest run convex/render/renderReel.test.ts convex/plans.test.ts convex/media.test.ts` | all exist after wave 1 | ⬜ pending |
| 04-T1 approve success + notice position | 25.1-04 | 4 | D9 | web component test (persistence past unmount, DOM order, keyed planId) | `cd apps/web && npx vitest run "app/(app)/dashboard/approvals/approvalsView.test.ts"` | exists | ⬜ pending |
| 04-T2 image-plan honesty | 25.1-04 | 4 | D10 | web component test (no Approve for image, reel regression) | `cd apps/web && npx vitest run "app/(app)/dashboard/approvals/approvalsView.test.ts"` | exists | ⬜ pending |
| 05-T1 sources threading | 25.1-05 | 5 | D11 | convex-test (plan row carries sources; audit URL-free) | `cd packages/backend && npx vitest run convex/dispatch.test.ts convex/llmRedaction.test.ts` | exist | ⬜ pending |
| 05-T2 memo markdown + sources block | 25.1-05 | 5 | D11 | web component test (heading elements, block presence/absence, preview) | `cd apps/web && npx vitest run "app/(app)/dashboard/workspace" "app/(app)/dashboard/approvals/approvalsView.test.ts"` | outputCard.test.ts exists (or sibling created in-task) | ⬜ pending |
| 06-T1 env manifest + drift | 25.1-06 | 6 | D12 | drift test extension (requireEnvMedia enumeration; FAL absence) | `cd packages/backend && npx vitest run convex/env.test.ts` | exists | ⬜ pending |
| 06-T2 fal removal + ADR | 25.1-06 | 6 | D12, D14 | suite regression + typecheck (route deletion safe) | `cd packages/backend && npx vitest run convex/media.test.ts && npx tsc --noEmit -p .` | exists | ⬜ pending |
| 06-T3 DLQ listing | 25.1-06 | 6 | D13 | convex-test (bounded list, owner gate, payload hygiene) | `cd packages/backend && npx vitest run convex/deadLetters.test.ts` | exists | ⬜ pending |
| 07-T1 live UAT | 25.1-07 | 7 | phase goal (D1–D11 observed) | checkpoint:human-verify | MANUAL — see Manual-Only Verifications | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Existing infrastructure covers most phase behaviours (`media.test.ts`, `plans.test.ts`, `dispatch.test.ts`, `cockpit.test.ts`, `env.test.ts`, `deadLetters.test.ts`, `llmRedaction.test.ts`, `approvalsView.test.ts`, `outputCard.test.ts`). Two NEW test files are created inside the tasks that need them (no separate Wave 0 plan): `packages/backend/convex/render/renderReel.test.ts` (01-T1) and `packages/backend/convex/reliabilitySweep.test.ts` (02-T1) — each is authored test-first within its own tdd task, so no `<automated>` command references a file that its task does not create.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end reel: approve → generate → render → caption → vault save, adjudicated | phase goal | Real provider spend + human judgment of the assembled mp4; no unit suite can prove decoded moving frames | Plan 25.1-07 checkpoint: propose reel in cockpit, approve on /dashboard/approvals, watch the running-state surface, confirm final captioned mp4 plays and the vault row exists |
| Approve success feedback visible | D9 | Perceptual — "does it read as confirmed" is a human call | In the 25.1-07 checkpoint: approve a plan; confirm a persistent success indication survives the card leaving the awaiting list |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are checkpoint tasks (25.1-07 only)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (both new test files created in their own tasks)
- [x] No watch-mode flags (`vitest run` everywhere)
- [x] Feedback latency < 120s (focused files only)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned 2026-08-21 (planner); execution sign-off pending
