---
phase: 25.1-consistency-and-reliability-hardening
plan: 04
type: execute
wave: 4
depends_on: ["25.1-01"]
files_modified:
  - apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx
  - apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [D9, D10]
must_haves:
  truths:
    - "Clicking Approve on a plan that starts leaves a persistent, visible success state that survives the card dropping off the awaiting list"
    - "A refusal notice renders at the TOP of the card, above the action buttons"
    - "Image plans never show an Approve button that is structurally incapable of succeeding — honest copy points at the workspace canvas"
    - "The stale-list 'already started' message tells the truth about what happened"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx"
      provides: "Persistent success card-state; refusal notice repositioned; image-plan honest copy"
    - path: "apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts"
      provides: "Success-persistence, notice-position, image-plan and stale-copy tests"
  key_links:
    - from: "executePlan success return (ApprovalsView.tsx:492-519 handler)"
      to: "component-local completed map keyed by planId"
      via: "setState before the reactive query drops the row"
    - from: "plan.kind === image"
      to: "no Approve button + honest copy"
      via: "conditional render at the actions block (ApprovalsView.tsx:321 area)"
---

<objective>
Close D9 and D10: Approve produces a persistent visible outcome (success currently unmounts the card before its message renders — acknowledged at cockpit.ts:992-996 — indistinguishable from a broken button); refusal notices move above the actions; image plans stop rendering a mute Approve (executePlan's media arm has no image branch → always `no_deck`, cockpit.ts:843-898); the false "already started" copy tells the truth.

Purpose: the "mute Approve button" third of the phase goal.
Output: honest approvals surface, component-tested.
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-RESEARCH.md (D9, D10)
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-CONTEXT.md (approvals decisions — binding; image-arm-vs-honest-button is Claude's discretion)
@docs/design/BRAND.md (MUST read before UI changes — tokens from globals.css, no hardcoded hex, no component library)
@docs/playbooks/dashboard-pages.md
@docs/playbooks/cockpit.md

Notes for the executor:
- DISCRETION RESOLVED AT PLANNING: take the HONEST-BUTTON route for image plans, not an image arm in executePlan (smaller diff, images already generate via api.media.generateImage from the canvas, MediaCanvas.tsx:997-1034; an image arm would duplicate a working path behind a second entry point). Copy should say images are generated from the workspace canvas and link/point there.
- DISCRETION RESOLVED AT PLANNING: persistent success = a retained "started" card-state (component-local map keyed by planId rendering a success card after the row leaves the awaiting list), NOT a toast — it is persistent, testable, and needs no new toast infrastructure. CONTEXT allows either; this is the lazier one given no toast system exists.
- Refusal enumeration is already fully handled (ApprovalsView.tsx:492-519, refusalMessage map :119-142 with fallback) — do NOT rewrite it; only repositioning + additions.
- Stale-list trap: `{ok:true, alreadyStarted:true}` (cockpit.ts:761) fires when the plan row was recycled. Minimal honest fix: change the client copy to not assert "this plan already started" as fact — e.g. "This card is out of date — the plan it referenced was replaced or already handled. Nothing new was started." If you can cheaply distinguish recycled-row from genuinely-started in cockpit.ts (e.g. plan status check before the alreadyStarted return), return a distinct code and map it; otherwise copy-only is acceptable. If cockpit.ts is touched, cockpit.md updates in the same commit.
- Component tests: apps/web vitest, existing approvalsView.test.ts. Harden per repo rule — assert the success state is keyed to the RIGHT plan (swap two planIds must redden), and assert DOM ORDER for the notice (notice node precedes the actions node), not mere presence.
- Stage only this plan's files; plain `git commit`; CRLF single-line anchors for playbooks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: persistent Approve success + refusal notice on top (D9)</name>
  <files>apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx, apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts, docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <behavior>
    - Test: approve success → a success card ("Plan started…") renders and REMAINS rendered after the awaiting list no longer contains the plan (simulate the reactive list dropping the row).
    - Test: success state is keyed to the approved planId (two plans, approve one — the other shows no success state).
    - Test: a refusal renders the notice ABOVE the action buttons (DOM order assertion), and the card's buttons remain usable.
    - Test: the stale/recycled-row response renders the new honest copy (exact string).
  </behavior>
  <action>
    In ApprovalsView.tsx: on a successful executePlan return, record `{planId → outcome}` in component state BEFORE the row unmounts; render entries of that map as persistent success cards in the list (BRAND tokens, role="status"). Move `ApprovalsStateNotice` from below the discard fieldset (:695-699) to the top of the card (above the :321 actions area). Update the stale-copy per the context note (and, if cheap, the distinct return code in cockpit.ts with a test in cockpit.test.ts). Update dashboard-pages.md (approvals feedback contract) — and cockpit.md if cockpit.ts changed — in the same commit.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run "app/(app)/dashboard/approvals/approvalsView.test.ts"</automated>
  </verify>
  <done>Success survives unmount, keyed correctly; refusal notice is first in the card; stale copy honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: image plans get honest copy, not a doomed Approve (D10)</name>
  <files>apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx, apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts, docs/playbooks/dashboard-pages.md</files>
  <behavior>
    - Test: an image plan renders NO Approve button; renders the honest copy pointing at the workspace canvas (exact string), while discard/other controls behave as before.
    - Test: a non-image media plan (reel) still renders its Approve button (regression — the conditional must key on the image discriminator only).
  </behavior>
  <action>
    At the actions block (ApprovalsView.tsx:321 area, "Approve governed generation"), branch on the plan's image discriminator (same field approvals.ts:38-51 uses for the "Image" badge): image plans render honest copy instead of the Approve button — "Images are generated from the workspace canvas. Open the thread to generate or re-generate this image." (adjust to BRAND voice). Leave approvals.ts listing untouched (the card still appears; only the doomed button goes). Note the existing e2e refusal assertion (apps/web/e2e/approvals.spec.ts:310-314) may now be dead for image plans — update that spec ONLY if it reds; otherwise leave e2e alone. Update dashboard-pages.md in the same commit.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run "app/(app)/dashboard/approvals/approvalsView.test.ts"</automated>
  </verify>
  <done>No image plan can present a button that always fails; reel plans unaffected.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run "app/(app)/dashboard/approvals/approvalsView.test.ts"` green; `cd packages/backend && npx vitest run convex/cockpit.test.ts` green if cockpit.ts touched.
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
Every Approve click ends in a visible truth: persistent success, prominent refusal, honest stale copy, or no button where success is impossible.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-04-SUMMARY.md`
</output>
