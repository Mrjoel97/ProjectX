---
phase: 25.1-consistency-and-reliability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/render/renderReel.ts
  - packages/backend/convex/render/renderReel.test.ts
  - packages/backend/convex/mediaComplete.ts
  - packages/backend/convex/media.ts
  - packages/backend/convex/media.test.ts
  - packages/backend/convex/cockpit.ts
  - docs/playbooks/media.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [D1, D2]
must_haves:
  truths:
    - "Every batchToRender refusal leaves the plan at renderStatus 'failed' with a renderReason the canvas can show — never eternal 'assembling'"
    - "After a refused render, media.retryRender is usable (it requires renderStatus === 'failed')"
    - "A throw inside renderReel after markRendering produces a failed terminal via the retrier onComplete, not a stuck 'rendering' row"
  artifacts:
    - path: "packages/backend/convex/render/renderReel.ts"
      provides: "Terminal write + dead letter on batchToRender refusal; guarded response parse"
    - path: "packages/backend/convex/render/renderReel.test.ts"
      provides: "Table-driven refusal-class terminal tests"
    - path: "packages/backend/convex/mediaComplete.ts"
      provides: "onRenderComplete retrier callback writing the crash terminal"
  key_links:
    - from: "renderReel batch refusal branch (renderReel.ts:759-761)"
      to: "plans.renderStatus/renderReason + deadLetters insert"
      via: "failure-terminal mutation (same shape as sibling dead-letter writes at renderReel.ts:432-440)"
    - from: "all three renderReel schedule sites (mediaComplete.ts:239, media.ts:2580, renderReel.ts:408)"
      to: "ActionRetrier retrier.run(..., { onComplete: internal.mediaComplete.onRenderComplete })"
      via: "EXTERNAL_TARGETS pattern (cockpit.ts:657-697)"
---

<objective>
Close D1 and D2 from 25.1-RESEARCH.md: every `batchToRender` refusal writes an honest failed terminal (renderStatus "failed" + renderReason + dead letter), and `renderReel` runs under the ActionRetrier with an onComplete so a post-`markRendering` crash also terminalizes.

Purpose: the primary silent stall in the media pipeline — canvas shows "assembling" forever (`mediaCanvasView.ts:462`) and `retryRender` refuses because it requires `renderStatus === "failed"` (`media.ts:2565`).
Output: honest terminals on every renderReel exit path, proven by table-driven tests.
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-RESEARCH.md (D1, D2 — ground truth, file:line evidence)
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-CONTEXT.md (locked decisions)
@docs/playbooks/media.md
@docs/playbooks/cockpit.md

Notes for the executor:
- Shared working tree carries foreign modifications (schema.ts, calendar.test.ts, etc.). Stage ONLY the files this plan names. Never `git add -A` or `git commit -a`. Use plain `git commit` (gsd-tools commit breaks on Windows quoting).
- Playbook .md files are CRLF — use single-line edit anchors only.
- `"use node"` boundary: any new internalAction/internalMutation in renderReel.ts/mediaComplete.ts carries an explicit return type (circular-inference guard).
- CLAUDE.md: tenant wrappers only; dead-letter/audit payloads carry refs/ids/counts/reason codes ONLY — never prompt text or card content.
- Do NOT unify `evaluateRenderTrigger` and `batchToRender` conditions (the :154-156 assertion refactor) — out of scope; the fix is terminal-on-refusal, not condition unification.
- Run tests from inside packages/backend (never vitest --root from repo root).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: batchToRender refusal writes a failed terminal + dead letter (D1)</name>
  <files>packages/backend/convex/render/renderReel.ts, packages/backend/convex/render/renderReel.test.ts, docs/playbooks/media.md</files>
  <behavior>
    Table-driven over the refusal classes from RESEARCH D1 (renderReel.ts:215-221 non-contiguous indices / bad seconds / unknown kind; :208-209,233,262,273 stale_inputs; :246-258 vault doc missing/foreign/non-video; :229 unrenderable card text; :290-294 out-of-deck blockIndex; :299-301 summed !== targetSeconds):
    - Test per class: drive the refusal → plans row ends `renderStatus: "failed"` with a `renderReason` matching the refusal reason; a deadLetters row exists with status "new" and a redaction-safe payload (ids/counts/reason only — assert NO prompt/card text in the payload).
    - Test: a plan that renders successfully is untouched by the new branch.
    - Mutation-harden per repo rule: assert the reason STRING matches the class, not just "failed" (transposing two reasons must redden).
  </behavior>
  <action>
    In `renderReel` (renderReel.ts), the `!batch.ok` early return at :759-761 currently exits BEFORE `markRendering` (:763) with no status write. Replace the bare return with a terminal write: patch the plan to `renderStatus: "failed"`, `renderReason: batch.reason`, and insert a dead letter using the same helper/shape as the sibling dead-letter writes at renderReel.ts:432-440 and :942-950. Reuse the existing failure-terminal mutation if one exists (grep for the mutation that :432-440's path uses) rather than minting a new one. Audit/dead-letter payload: planId, tenantId, reason, counts — nothing else.
    New tests live in a NEW file `packages/backend/convex/render/renderReel.test.ts` (none exists) using convex-test, following the harness patterns in media.test.ts.
    Update docs/playbooks/media.md in the same commit: document the invariant "no renderReel exit path leaves renderStatus non-terminal without the watchdog able to terminalize it" and bump its `Last verified` line (single-line anchors, CRLF).
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/render/renderReel.test.ts</automated>
  </verify>
  <done>All refusal classes produce `renderStatus:"failed"` + matching `renderReason` + a dead letter; retryRender's precondition (`media.ts:2565`) is now reachable after any refusal; playbook updated in same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renderReel under the ActionRetrier with onComplete (D2)</name>
  <files>packages/backend/convex/render/renderReel.ts, packages/backend/convex/mediaComplete.ts, packages/backend/convex/media.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/media.test.ts, packages/backend/convex/render/renderReel.test.ts, docs/playbooks/cockpit.md, docs/playbooks/media.md</files>
  <behavior>
    - Test: `onRenderComplete` handler unit test — given a failed retrier result for a plan stuck at `renderStatus:"rendering"`, it writes `renderStatus:"failed"` + `renderReason:"render_crashed"` (or executor's chosen stable code) + a dead letter; given a success result it writes nothing.
    - Test: the non-JSON 200 at renderReel.ts:819 no longer throws unguarded — a non-JSON body produces the failed terminal path, not an uncaught exception.
  </behavior>
  <action>
    Mirror `submitBatch`'s shape exactly (`EXTERNAL_TARGETS` → `retrier.run(internal.media.submitBatch, ..., { onComplete: internal.mediaComplete.onSubmitComplete })`, cockpit.ts:657-697; retrier component at convex/index.ts:16). Convert the three bare `ctx.scheduler.runAfter` call sites — mediaComplete.ts:239, media.ts:2580, renderReel.ts:408 — to run `internal.render.renderReel.renderReel` under the retrier with a new `onComplete: internal.mediaComplete.onRenderComplete` (place beside `onSubmitComplete`, same file, explicit return type). onRenderComplete: on retrier failure, terminalize the plan (failed + reason + dead letter) ONLY if it is still non-terminal (`rendering`); idempotent if already failed/rendered. Guard the `res.json()` parse at renderReel.ts:819 (try/catch → throw a typed error so the retrier/onComplete terminalizes, or terminalize inline — executor's choice, but the row must end failed).
    Update docs/playbooks/cockpit.md (cockpit.ts EXTERNAL_TARGETS now includes renderReel) and media.md (renderReel is retried; crash terminal exists) in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/render/renderReel.test.ts convex/media.test.ts</automated>
  </verify>
  <done>No bare runAfter of renderReel remains (grep proves 0 hits); a post-markRendering throw ends in a failed terminal; focused suites green; both playbooks bumped in the same commit.</done>
</task>

</tasks>

<verification>
- `cd packages/backend && npx vitest run convex/render/renderReel.test.ts convex/media.test.ts` green.
- `grep -n "runAfter.*renderReel" packages/backend/convex` → only retrier-mediated scheduling remains.
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
Every renderReel exit path (refusal before markRendering, crash after it, non-JSON render response) leaves `renderStatus:"failed"` + `renderReason` + dead letter; success path unchanged; tests table-driven over all refusal classes.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-01-SUMMARY.md`
</output>
