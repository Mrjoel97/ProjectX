---
phase: 25.1-consistency-and-reliability-hardening
plan: 07
type: execute
wave: 7
depends_on: ["25.1-01", "25.1-02", "25.1-03", "25.1-04", "25.1-05", "25.1-06"]
files_modified: []
autonomous: false
requirements: [D1, D2, D3, D4, D5, D6, D7, D8, D9, D10, D11]
must_haves:
  truths:
    - "One real reel has gone approve → render → caption → vault on dev, adjudicated by the owner watching it"
    - "The owner saw the persistent Approve success state in a real browser"
  artifacts: []
  key_links: []
---

<objective>
Phase-end manual UAT: the live end-to-end reel adjudication and approvals-feedback check. This is the behaviour-level gate no unit suite can fake (project memory: phase 19 shipped 22/22 green with the feature broken). It costs real provider spend and requires the owner — checkpoint, not automation.

Prerequisite: `pnpm gate` green on the branch before starting (full offline gate is the entry ticket to spending money).
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-VALIDATION.md (Manual-Only Verifications table)

Environment notes (project memory):
- Run against the DEV deployment with a prod-build web app (Next dev OOMs on the workspace page). Convex CLI from packages/backend only.
- One reel costs real money (a video clip can exceed the daily LLM budget) — ONE run, watched end to end, is the gate. Do not loop on failure; diagnose offline first.
- The owner (Google account) is the only loggable human identity.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: live reel end-to-end + approvals feedback adjudication</name>
  <what-built>Phase 25.1 hardening: honest render terminals + retrier (25.1-01), stuck-work watchdog (25.1-02), media vault persistence (25.1-03), approvals feedback (25.1-04), memo markdown + sources (25.1-05), hygiene + DLQ listing (25.1-06).</what-built>
  <how-to-verify>
    On the dev deployment, in a real browser as the owner:
    1. Propose a reel in the cockpit; go to /dashboard/approvals and click Approve.
    2. Confirm a PERSISTENT success indication survives the card leaving the awaiting list (D9).
    3. Watch the run: canvas shows progress, and if anything refuses/stalls the state reads FAILED with a reason — never eternal "assembling" (D1/D2/D3).
    4. Confirm the final captioned mp4 PLAYS (decoded moving frames — human judgment) and a vault row for the reel EXISTS (D6). This is the first owner-adjudicated assembled reel.
    5. From the same thread, generate an image; confirm it appears AND a vault doc for it exists (D5). Generate a SECOND image on the same plan — it must not refuse `already_started` (D8).
    6. Ask a research question; confirm the memo card shows formatted headings (no literal '#') and a Sources block when sources were used (D11).
    7. Glance at /ops: the Dead letters section renders (D13).
  </how-to-verify>
  <resume-signal>Type "approved" (goal met) or describe exactly what you saw fail — the failing defect ID if identifiable.</resume-signal>
</task>

</tasks>

<verification>
Owner verdict recorded verbatim in the SUMMARY. Any failure spawns gap-closure planning (/gsd:plan-phase 25.1 --gaps), not ad-hoc fixes at the gate.
</verification>

<success_criteria>
The owner has watched one governed reel reach an honest, visible, vaulted terminal — the phase thesis observed live.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-07-SUMMARY.md` with the verbatim owner verdict and measured spend.
</output>
