---
phase: 25.1-consistency-and-reliability-hardening
plan: 06
type: execute
wave: 6
depends_on: ["25.1-01", "25.1-03"]
files_modified:
  - packages/backend/convex/lib/env.ts
  - packages/backend/convex/env.test.ts
  - packages/backend/convex/http.ts
  - packages/backend/convex/media.ts
  - packages/backend/convex/media.test.ts
  - packages/backend/convex/deadLetters.ts
  - packages/backend/convex/deadLetters.test.ts
  - apps/web/app/(app)/ops/page.tsx
  - docs/decisions/ (new superseding ADR)
  - docs/playbooks/production-beta.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/media.md
  - docs/playbooks/audit-dead-letter.md
  - docs/playbooks/authorization.md
autonomous: true
requirements: [D12, D13, D14]
must_haves:
  truths:
    - "Readiness/drift checks see MEDIA_RENDER_URL, MEDIA_SANDBOX_SNAPSHOT_ID and the other media env vars — an unset var can no longer read as green while renders stall"
    - "No dead fal webhook code remains (route, HMAC, manifest entry)"
    - "docs/decisions/ records the Sora cutover, superseding ADR-011/012"
    - "The owner can SEE dead letters (id, kind, status, age, counts) on the ops surface"
  artifacts:
    - path: "packages/backend/convex/lib/env.ts"
      contains: "MEDIA_RENDER_URL and MEDIA_SANDBOX_SNAPSHOT_ID in ENV_MANIFEST; FAL_WEBHOOK_SECRET removed"
    - path: "packages/backend/convex/deadLetters.ts"
      provides: "owner-scoped redaction-safe DLQ listing query"
    - path: "apps/web/app/(app)/ops/page.tsx"
      provides: "DLQ listing section"
  key_links:
    - from: "env.test.ts drift scan"
      to: "requireEnvMedia call sites"
      via: "extended enumeration (or names moved to literal process.env reads)"
    - from: "ops page"
      to: "deadLetters listing query"
      via: "owner-gated useQuery (existing /ops auth posture)"
---

<objective>
Close D12–D14, the hygiene cluster: env-manifest completeness (blind spots let an unset var stall renders while readiness reads green), removal of the dead fal webhook path, a superseding ADR for the Sora cutover, and a minimal owner-facing DLQ listing (the DLQ is currently write-only).

Purpose: silent-failure hygiene — the ops plane must be able to SEE what the pipeline writes.
Output: complete manifest, less dead code, honest decision record, visible dead letters.
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-RESEARCH.md (D12–D14)
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-CONTEXT.md (hygiene decisions — binding; re-drive worker DEFERRED)
@docs/playbooks/production-beta.md
@docs/playbooks/audit-dead-letter.md
@docs/playbooks/authorization.md

Notes for the executor:
- media.ts was rewritten by waves 1 and 3 — work from committed state; the webhook-param line (research cite media.ts:1521) may have moved.
- The ops page is at apps/web/app/(app)/ops/page.tsx (watched by authorization.md). Render the DLQ listing INSIDE that existing page file — no new web files, so no watch.json change needed.
- Owner gating: mirror the page's existing owner posture and the owner primitive in owner.ts — the DLQ query must refuse non-owners the same way existing ops queries do.
- DLQ listing fields: id, kind/topic, status, createdAt/age, counts, reason code. The payload column already carries only refs/hashes/ids/counts (CLAUDE.md §4) — surface those fields raw-as-stored, never attempt to "enrich" them. No re-drive button (deferred).
- ADR: docs/decisions/ next number AFTER the existing ADR-023 (check the directory — research says next is ADR-024). Content: media provider cutover to OpenAI (`sora-2` 720p 4s video, `gpt-image-2` images, OpenAI tts/stt; evidence media.ts:968-1010,1206,1699, pins packages/cost/src/media.ts:82-86), legacy Wan poller retained for pre-cutover tasks only (media.ts:829-833), supersedes ADR-011/ADR-012. ADRs are immutable — write a NEW file, do not edit the old ones (add only a "Superseded by" pointer line if the existing ADR template convention does that; check a prior superseded ADR for the house style).
- MEDIA_SANDBOX_FIXTURE (silently fakes renders if set live, renderReel.ts:780-784): add it to the manifest/drift visibility too if cheap; at minimum document it in the ADR or playbook. NEXT_PUBLIC_CONVEX_URL is web-side — cover it only "where the readiness pattern can reach it" (CONTEXT); if it cannot, document the gap in production-beta.md instead of building new machinery.
- Stage only this plan's files; plain `git commit`; CRLF single-line anchors for playbooks and .planning files.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: env manifest completeness + drift-test extension (D12)</name>
  <files>packages/backend/convex/lib/env.ts, packages/backend/convex/env.test.ts, docs/playbooks/production-beta.md</files>
  <behavior>
    - Test: ENV_MANIFEST contains MEDIA_RENDER_URL, MEDIA_SANDBOX_SNAPSHOT_ID, WAN_API_BASE_URL, Video_and_image_API_Key (exact names as read in code).
    - Test: the drift scan now catches `requireEnvMedia("X")` call sites (media.ts:837-841 pattern), not only literal `process.env.X` (env.test.ts:38) — proven by asserting the media names are discovered from source, so a future requireEnvMedia addition without a manifest entry reds.
    - Test: FAL_WEBHOOK_SECRET is ABSENT from the manifest (asserted).
  </behavior>
  <action>
    Add the missing media vars to ENV_MANIFEST (lib/env.ts:122-132) and remove FAL_WEBHOOK_SECRET. Extend the env.test.ts drift scan to also enumerate requireEnvMedia("...") string literals across convex source (a second regex over the same file walk — lazy, no new tooling). Decide NEXT_PUBLIC_CONVEX_URL per the context note. Update production-beta.md (env contract) in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/env.test.ts</automated>
  </verify>
  <done>An unset media env var is visible to readiness/drift; the dead secret is gone from the manifest.</done>
</task>

<task type="auto">
  <name>Task 2: remove dead fal webhook code + superseding ADR (D12, D14)</name>
  <files>packages/backend/convex/http.ts, packages/backend/convex/media.ts, packages/backend/convex/media.test.ts, docs/decisions/ (new ADR file), docs/playbooks/cockpit.md, docs/playbooks/media.md</files>
  <action>
    Delete the `/fal/callback/` route (http.ts:354) and its HMAC handling (note at http.ts:268-274), and the vestigial webhook param drop at submitLine (media.ts:1521 region — confirm current line). Grep for remaining `fal`/`FAL_WEBHOOK` references and remove dead ones (leave the legacy Wan POLLER intact — it serves pre-cutover tasks, media.ts:829-833; MS-code-style env-gated retention rule applies: delete only what is UNREACHABLE). Run the focused media/http suites to prove nothing depended on the route. Write the superseding ADR (next number after ADR-023) per the context note. Update cockpit.md (http.ts routes) and media.md (provider truth, poller retention) in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/media.test.ts && npx tsc --noEmit -p .</automated>
  </verify>
  <done>Zero reachable fal-webhook code; ADR on disk superseding ADR-011/012; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: owner-facing DLQ listing (D13)</name>
  <files>packages/backend/convex/deadLetters.ts, packages/backend/convex/deadLetters.test.ts, apps/web/app/(app)/ops/page.tsx, docs/playbooks/audit-dead-letter.md, docs/playbooks/authorization.md</files>
  <behavior>
    - Test (deadLetters.test.ts): the listing query returns rows newest-first with id/kind/status/createdAt/counts; bounded (take limit); a non-owner caller is refused exactly like existing ops queries.
    - Test: the serialized listing output contains no raw-content field beyond what the stored redaction-safe payload carries (structural scan mirroring existing payload-hygiene tests).
  </behavior>
  <action>
    Add an owner-scoped `list` query to deadLetters.ts (tenant-wrapper + owner gate per owner.ts precedent; bounded, indexed by status/createdAt if an index exists — add an additive index only if the table lacks one). Render a "Dead letters" section on the existing /ops page: table of id, kind, status, age, counts; a `status: "new"` count headline; no actions (re-drive deferred, say so in the UI copy). BRAND tokens, no component library. Update audit-dead-letter.md (DLQ is now readable; still insert-only + listing, no mutation) and authorization.md (ops page gained a section) in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/deadLetters.test.ts</automated>
  </verify>
  <done>The owner can enumerate dead letters from the browser; the DLQ is no longer write-only; nothing can mutate it from this surface.</done>
</task>

</tasks>

<verification>
- `cd packages/backend && npx vitest run convex/env.test.ts convex/media.test.ts convex/deadLetters.test.ts` green; backend `tsc --noEmit` clean.
- `node scripts/check-playbooks.mjs` exit 0.
- New ADR present in docs/decisions/; ADR-011/012 files byte-unchanged except any house-style superseded-pointer.
</verification>

<success_criteria>
Readiness cannot lie about media env; dead code and stale decisions are cleaned; dead letters are visible to the owner.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-06-SUMMARY.md`
</output>
