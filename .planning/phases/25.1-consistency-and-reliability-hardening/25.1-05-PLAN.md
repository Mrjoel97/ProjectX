---
phase: 25.1-consistency-and-reliability-hardening
plan: 05
type: execute
wave: 5
depends_on: ["25.1-03", "25.1-04"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/app/(app)/dashboard/workspace/outputCard.test.ts
  - apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx
  - apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/dispatch.ts
  - packages/backend/convex/dispatch.test.ts
  - packages/backend/convex/evaluations.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/business-evaluation.md
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [D11]
must_haves:
  truths:
    - "A research memo card shows rendered headings and bold text, never literal '#' and '**'"
    - "When a research turn captured sources, the card shows a references block with title, URL and retrieval date"
    - "Audit and log payloads still carry counts only — no URL reaches an audit row"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "Memo body through MarkdownDocument; sources references block"
    - path: "packages/backend/convex/schema.ts"
      contains: "optional sources array on the plans table"
    - path: "packages/backend/convex/dispatch.ts"
      provides: "sources threaded through landSpecialistResult"
  key_links:
    - from: "sourcesFromToolOutput (llm.ts:4541-4548)"
      to: "plans.sources"
      via: "dispatch.ts landSpecialistResult (:1096-1098, currently body-only) → evaluations.ts landing (:1044-1106)"
    - from: "plans.sources"
      to: "references block on the memo card (cards.tsx)"
      via: "card render, title + URL + retrieval date"
---

<objective>
Close D11 (presentation only — research DEPTH is explicitly out of scope): the memo plan card renders `plan.body` through the existing `MarkdownDocument` renderer instead of a raw pre-wrap paragraph, and already-captured research sources are threaded through `landSpecialistResult` onto the plan row and rendered as a references block.

Purpose: the memo is the product of a paid specialist turn; literal `#`/`**` and withheld sources make finished work look broken and unverifiable.
Output: formatted memo cards with a references block; redaction rule intact.
</objective>

<context>
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-RESEARCH.md (D11)
@.planning/phases/25.1-consistency-and-reliability-hardening/25.1-CONTEXT.md (research presentation decisions — binding; URLs go to card/vault, NEVER audit)
@docs/design/BRAND.md
@docs/playbooks/cockpit.md
@docs/playbooks/business-evaluation.md

Notes for the executor:
- Renderer already exists and is used three ways: `MarkdownDocument` (apps/web/app/(app)/dashboard/MarkdownDocument.tsx:23-120; tokenizer packages/core/src/documentGen.ts:54-128 — h1-h3, bullets, ordered, pipe tables, **bold**; NO links/images/code). Chat bubbles use it at ChatPane.tsx:279; doc artifacts at cards.tsx:2579,2582. Reuse compact mode; do not extend the tokenizer (no link rendering needed for the BODY — sources render as their own block).
- Sources shape: additive optional field on plans, e.g. `sources: v.optional(v.array(v.object({ title: v.string(), url: v.string(), retrievedAt: v.number() })))`. `sourcesFromToolOutput` output is URL-deduped and comes from tool result parts, never model prose (llm.ts:4541-4548) — provenance is safe to display. retrievedAt = landing time is acceptable if per-source times don't exist.
- Threading path: dispatch.ts:1096-1098 currently passes only `body` to landing; evaluations.ts:1044-1106 wraps with `specialistMemoBody`. Thread `sources` alongside `body`; write them to the plan row at landing. DO NOT add sources to any audit payload — llm.ts:4537-4540 and research.ts:188-211 count-only rules stand, and llmRedaction.test.ts pins must stay untouched.
- References block folding is Claude's discretion — a plain always-visible list under a "Sources" heading is the lazy default; collapse only if the card gets long.
- Approvals preview (ApprovalsView.tsx:583-586, 320-char slice): "where practical" per CONTEXT — a truncated slice through MarkdownDocument can split a token mid-construct; acceptable resolution: strip markdown markers for the preview (one-line regex) OR render the slice through MarkdownDocument if it degrades gracefully. Either satisfies; do not build a smart truncator.
- schema.ts and ApprovalsView.tsx were touched by earlier waves — work from committed state. Stage only this plan's files; plain `git commit`; CRLF single-line anchors for playbooks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: thread sources onto the plan row (D11 backend)</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/dispatch.ts, packages/backend/convex/dispatch.test.ts, packages/backend/convex/evaluations.ts, docs/playbooks/cockpit.md, docs/playbooks/business-evaluation.md</files>
  <behavior>
    - Test (dispatch.test.ts): a research landing with captured sources → the plan row carries the sources array (titles + urls + retrievedAt); a landing with no sources → field absent/undefined.
    - Test: no audit row written during landing contains a URL (extend/reuse the existing count-only assertions; the llmRedaction.test.ts pins must remain green untouched).
  </behavior>
  <action>
    Add the optional `sources` field to plans in schema.ts (additive). In dispatch.ts `landSpecialistResult` (:1096-1098), pass `sources` from the specialist result alongside `body`; in evaluations.ts landing (:1044-1106), persist them on the plan row. Do not touch research.ts vault-doc writing (Sources: lines already exist there, research.ts:102). Update cockpit.md (dispatch contract now carries sources; redaction rule restated) and business-evaluation.md (evaluations.ts landing writes sources) in the same commit.
  </action>
  <verify>
    <automated>cd packages/backend && npx vitest run convex/dispatch.test.ts convex/llmRedaction.test.ts</automated>
  </verify>
  <done>Sources land on the plan row; zero URLs in audit payloads; redaction pins untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: memo card markdown + sources block + approvals preview (D11 frontend)</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, apps/web/app/(app)/dashboard/workspace/outputCard.test.ts, apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx, apps/web/app/(app)/dashboard/approvals/approvalsView.test.ts, docs/playbooks/cockpit.md, docs/playbooks/dashboard-pages.md</files>
  <behavior>
    - Test: memo card with a markdown body renders heading ELEMENTS, not a literal '#' in text content (assert both: heading tag present AND raw marker absent).
    - Test: card with sources renders the references block with title, URL and a formatted retrieval date; card without sources renders NO block (absence asserted).
    - Test: approvals preview no longer shows literal markdown markers for a markdown body.
  </behavior>
  <action>
    Replace the pre-wrap paragraph at cards.tsx:560-573 with `MarkdownDocument` in the compact form used at cards.tsx:2579. Add the references block below the body when `plan.sources` is non-empty (BRAND tokens; anchor per source: title as link text, URL href, retrieval date). Apply the chosen preview treatment at ApprovalsView.tsx:583-586. Tests go in outputCard.test.ts (or a sibling test file if outputCard.test.ts covers a different card — pick the file that already tests memo cards; create `memoCard.test.tsx` beside it only if none does, and if created it needs no watch.json entry — apps/web/app/(app)/dashboard/workspace/ is already covered by cockpit.md). Update cockpit.md and dashboard-pages.md in the same commit.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run "app/(app)/dashboard/workspace" "app/(app)/dashboard/approvals/approvalsView.test.ts"</automated>
  </verify>
  <done>Rendered memo, visible sources, clean preview; absence cases proven.</done>
</task>

</tasks>

<verification>
- Backend: `cd packages/backend && npx vitest run convex/dispatch.test.ts convex/llmRedaction.test.ts` green.
- Web: workspace + approvals focused suites green.
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
A research memo reads as a formatted document with checkable sources on the card; the audit plane never learns a URL.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-consistency-and-reliability-hardening/25.1-05-SUMMARY.md`
</output>
