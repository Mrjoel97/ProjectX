---
phase: 17-calendar-actions
plan: 06
type: execute
gap_closure: true
wave: 2
depends_on: ["17-05"]
files_modified:
  - packages/core/src/calendarManagement.ts
  - packages/core/src/calendarManagement.test.ts
  - packages/core/src/notifications.ts
  - packages/core/src/notifications.test.ts
  - packages/backend/convex/microsoftCalendarAuth.ts
  - packages/backend/convex/microsoftCalendarAuth.test.ts
  - packages/backend/convex/http.ts
  - packages/backend/convex/httpAuth.test.ts
  - apps/web/app/(app)/connect-microsoft/page.tsx
  - apps/web/app/(app)/_components/DisconnectMicrosoft.tsx
  - apps/web/app/(app)/_components/ReconnectBanner.tsx
  - apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx
  - apps/web/e2e/connect-microsoft.spec.ts
  - docs/playbooks/cockpit.md
  - docs/playbooks/onboarding.md
autonomous: true
requirements: [ACTN-02]

must_haves:
  truths:
    - "Microsoft delegated OAuth supports work/school and personal accounts through the common v2 endpoint"
    - "The client never receives an access token or refresh token; status exposes booleans/timestamps only"
    - "The grant is exactly offline_access plus Calendars.ReadWrite; no Mail, Contacts, Directory, or application permission is requested"
    - "State binds the callback to the authenticated tenant and a tampered/cross-provider state is rejected before token exchange"
    - "A reconnect replaces the Microsoft token row and retires only Microsoft reconnect notifications"
    - "Google connection and reconnect behavior remains unchanged"
  artifacts:
    - path: "packages/backend/convex/microsoftCalendarAuth.ts"
      provides: "authorize URL, state verification, internal token store/read/update/delete, safe status/connect URL"
      min_lines: 180
    - path: "packages/backend/convex/http.ts"
      provides: "server-only /microsoft-calendar/callback code exchange"
      contains: "/microsoft-calendar/callback"
    - path: "apps/web/app/(app)/connect-microsoft/page.tsx"
      provides: "bounded Microsoft Calendar consent surface"
      min_lines: 60
  key_links:
    - from: "apps/web/app/(app)/connect-microsoft/page.tsx"
      to: "packages/backend/convex/microsoftCalendarAuth.ts"
      via: "tenant-scoped connect URL/status queries"
      pattern: "microsoftCalendarConnectUrl"
    - from: "packages/backend/convex/http.ts"
      to: "packages/backend/convex/microsoftCalendarAuth.ts"
      via: "verified state then internal token-store mutation"
      pattern: "microsoftCalendarAuth.store"
---

<objective>
Add a separate Microsoft Calendar delegated-OAuth connection without changing the existing combined
Google Mail/Calendar/Drive grant. Tokens stay server-only, refresh-capable, revocable, and tenant-
bound. Ship a bounded connect/disconnect/reconnect UI and connection status.
</objective>

<context>
@.planning/phases/17-calendar-actions/17-VERIFICATION.md
@.planning/phases/17-calendar-actions/17-05-SUMMARY.md
@packages/backend/convex/gmailAuth.ts
@packages/backend/convex/http.ts
@apps/web/app/(app)/connect-gmail/page.tsx
@CLAUDE.md
@docs/design/BRAND.md
@docs/playbooks/cockpit.md
@docs/playbooks/onboarding.md

Provider contract verified against Microsoft identity platform v2 auth-code flow and Graph v1.0:

- authorize/token base: `https://login.microsoftonline.com/common/oauth2/v2.0`
- delegated scope: `offline_access Calendars.ReadWrite`
- callback env: `MICROSOFT_OAUTH_CLIENT_ID`, `MICROSOFT_OAUTH_CLIENT_SECRET`,
  `MICROSOFT_CALENDAR_REDIRECT_URI`
- refresh responses may rotate `refresh_token`; the next plan must persist the newest token.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Microsoft authorization URL, state trust boundary, and crown-jewel token store</name>
  <files>packages/core/src/calendarManagement.ts, packages/core/src/calendarManagement.test.ts, packages/backend/convex/microsoftCalendarAuth.ts, packages/backend/convex/microsoftCalendarAuth.test.ts</files>
  <behavior>
    - The authorize URL uses the `common` v2 endpoint, response_type=code, response_mode=query, and the exact delegated scope set.
    - State signed with the Microsoft client secret returns only its bound tenant; changed tenant/provider/signature returns null.
    - Missing env produces a bounded `{configured:false,url:null}` client result, not a subscription crash.
    - Store replaces the tenant row; get/update/delete are internal only; safe status returns no scope/token.
    - Reconnect marks unread `microsoft_calendar_reconnect` rows read and leaves `gmail_reconnect` untouched.
  </behavior>
  <action>
Mirror the established Google module split, but do not share token rows or rename `gmailTokens`.
Reuse the exported HMAC primitive only; use the Microsoft secret and include a provider discriminator
in the signed data so Google state cannot be replayed into the Microsoft callback. Use constant-time
signature comparison if the runtime primitive is available; otherwise length-check then byte-XOR.
Define the exact scope string once in `@pikar/core`. No MSAL dependency is needed for this server-
side three-endpoint flow.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/microsoftCalendarAuth.test.ts</automated>
    <automated>npx vitest run packages/core/src/calendarManagement.test.ts</automated>
  </verify>
  <done>Token fields never appear in any exported tenant query or audit/notification payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: server callback exchange and safe failure redirects</name>
  <files>packages/backend/convex/http.ts, packages/backend/convex/httpAuth.test.ts</files>
  <behavior>
    - OAuth error/missing code/bad state fail before the token POST and redirect to the Microsoft connect page with a bounded code.
    - Success exchanges the code server-side, requires access_token and refresh_token, stores expiry/scope, then 303s to Connections.
    - Raw Microsoft error bodies, tokens, codes, and state never appear in redirects, logs, audit, or thrown messages.
    - A Google callback still follows its unchanged route and token store.
  </behavior>
  <action>
Add `GET /microsoft-calendar/callback`. Post form-urlencoded to the tenant-independent `common` token
endpoint with the configured redirect URI. Parse only known scalar fields. Use fixed public error
codes (`cancelled`, `missing_callback`, `invalid_state`, `exchange_failed`, `missing_refresh`) rather
than provider messages. The callback is a server route; never send the authorization code to React.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/httpAuth.test.ts convex/microsoftCalendarAuth.test.ts convex/calendar.test.ts</automated>
  </verify>
  <done>Success/failure paths are deterministic, redaction-safe, and provider-separated.</done>
</task>

<task type="auto">
  <name>Task 3: connect, disconnect, status, and provider-specific reconnect UI</name>
  <files>packages/core/src/notifications.ts, packages/core/src/notifications.test.ts, packages/backend/convex/calendar.test.ts, apps/web/app/(app)/connect-microsoft/page.tsx, apps/web/app/(app)/_components/DisconnectMicrosoft.tsx, apps/web/app/(app)/_components/ReconnectBanner.tsx, apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx, apps/web/e2e/connect-microsoft.spec.ts, docs/playbooks/cockpit.md, docs/playbooks/onboarding.md</files>
  <action>
Add `microsoft_calendar_reconnect` as a static notification kind/message. Generalize
`ReconnectBanner` by provider without changing Gmail holds: Microsoft notifications link to
`/connect-microsoft`; Gmail rows and held email requests keep their current copy and link. Add a
Microsoft card to Connections and a private connect page with configured/loading/connected/error
states plus an explicit delegated-permission explanation. Disconnect must confirm and delete the
local Microsoft token row. Do not invent or call a Google-style token revocation endpoint: the
Microsoft v2 flow used here has no equivalent endpoint in this integration. Explain honestly that
removing tenant/app consent is a separate owner action in Microsoft My Apps or Entra. Reuse brand
tokens; do not add global CSS unless required.

Add the H3 regression before widening any further behavior: seed a positive stored Google token
whose scope includes Calendar and a pre-widening stored Google token whose scope does not. The
pre-widening row must produce provider-specific reconnect state before `freshAccessToken` or any
Google Calendar fetch, while the positive row reaches the bounded availability adapter. Pair the
zero-fetch assertion with the positive fetch witness, and mutation-remove the stored-scope guard to
force the named test red. This is the offline half of the live H3 negative repeated in Plan 17-11.

`onboarding.md` owns both provider connect/disconnect/reconnect surfaces and exact consent copy;
`cockpit.md` owns Calendar runtime behavior. Update both without overlapping `watch.json` entries.

Mutation guards: expose scope in the status query; remove provider from signed state; accept a
missing refresh token; route a Microsoft reconnect to `/connect-gmail`. Each must turn a named test
red, then be restored.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/notifications.test.ts</automated>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/calendar.test.ts</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web exec playwright test e2e/connect-microsoft.spec.ts</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The UI resolves without secrets, onboarding ownership is explicit, and H3 proves a pre-widening Google stored scope reconnects before any provider call.</done>
</task>

</tasks>

<verification>
Focused auth/backend/UI tests pass; source scans find Microsoft tokens only in internal modules and
the callback; Google tests stay green. No Microsoft Graph Calendar request exists yet.
</verification>

<success_criteria>
- Both personal and organizational Microsoft accounts can enter the delegated auth flow.
- Only `offline_access Calendars.ReadWrite` is requested.
- Tokens never cross the server/client or audit boundary.
- Provider-specific reconnect/disconnect states are truthful.
- Existing Google connection remains intact.
</success_criteria>

<output>
Create `17-06-SUMMARY.md` with env names, exact scopes, callback error-code table, mutation evidence,
and a live setup note for Azure app registration and redirect URI.
</output>
