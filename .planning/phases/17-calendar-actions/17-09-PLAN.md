---
phase: 17-calendar-actions
plan: 09
type: execute
gap_closure: true
wave: 5
depends_on: ["17-08"]
files_modified:
  - packages/backend/convex/calendarEvents.ts
  - packages/backend/convex/calendarEvents.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/dispatchGuard.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/calendar-management.spec.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [ACTN-02]

must_haves:
  truths:
    - "The agent can list a bounded set of the tenant's Pikar-managed active events and receives stable refs plus content-plane labels"
    - "The agent can stage update/move or cancel/delete, but the staging tool performs no provider write and never approves"
    - "Staging refreshes the provider snapshot first, refuses attendees/missing/conflict, and copies the fresh etag into the proposed plan"
    - "Natural-language time is resolved only from trusted client nowMs/tz; ambiguous/past/unsupported input writes nothing"
    - "The user sees provider, original state, proposed state, operation, and an accurate Approve promise"
    - "Cross-tenant managed-event refs are indistinguishable from missing refs"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "listManagedCalendarEvents and proposeCalendarChange tools"
      contains: "proposeCalendarChange"
    - path: "packages/backend/convex/calendarEvents.ts"
      provides: "fresh-snapshot staging mutation and bounded tenant list"
      contains: "stageChange"
    - path: "apps/web/e2e/calendar-management.spec.ts"
      provides: "offline stage→card→approve-visible behavior without provider network"
      min_lines: 80
  key_links:
    - from: "packages/backend/convex/llm.ts"
      to: "packages/backend/convex/calendar.ts"
      via: "inspect-only action before stage mutation"
      pattern: "inspectManagedEvent"
    - from: "packages/backend/convex/llm.ts"
      to: "packages/backend/convex/calendarEvents.ts"
      via: "one atomic stageChange mutation after inspection"
      pattern: "calendarEvents.stageChange"
---

<objective>
Expose safe in-loop discovery and staging for the management action delivered in 17-08. The agent
may identify and propose changes to Pikar-managed events; the provider write remains exclusively
behind the existing human Approve mutation.
</objective>

<context>
@.planning/phases/17-calendar-actions/17-VERIFICATION.md
@.planning/phases/17-calendar-actions/17-08-SUMMARY.md
@packages/backend/convex/llm.ts
@packages/backend/convex/calendarEvents.ts
@CLAUDE.md
@docs/design/BRAND.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bounded managed-event discovery with opaque stable refs</name>
  <files>packages/backend/convex/calendarEvents.ts, packages/backend/convex/calendarEvents.test.ts, packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - listManagedCalendarEvents returns at most 20 active owned rows, newest start first, with managedEventId/provider/title/time/tz only.
    - Deleted, foreign, and attendee-unsafe rows are absent.
    - Zero rows returns an honest no-managed-events string.
    - The tool is read-only, uses no provider endpoint, and writes no audit content.
    - Listing is strictly read-only; Plan 17-08's bounded migration is already complete, and unmigrated/needsRefresh rows are omitted with refs-only counts.
  </behavior>
  <action>
Require the Plan 17-08 migration completion readback as setup; never call a migration/backfill
mutation from this tool/query. Add a closed, local executable tool. Titles/times are allowed in the model turn because they are the
same content plane already shown on the plan card; they must never be copied into audit, telemetry,
notifications, or trace fields. Return stable Convex managedEventId refs, not array indexes alone.
Keep the cap and truncation disclosure explicit.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/calendarEvents.test.ts convex/cockpitTools.test.ts convex/traceParity.test.ts</automated>
  </verify>
  <done>Discovery is tenant-safe, bounded, content-plane-only, and has a non-vacuous foreign-row test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: inspect-then-stage update/move/delete with trusted clock and fresh etag</name>
  <files>packages/backend/convex/calendarEvents.ts, packages/backend/convex/calendarEvents.test.ts, packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts, packages/backend/convex/dispatchGuard.test.ts</files>
  <behavior>
    - update accepts optional title/when/duration but requires at least one actual change.
    - move/reschedule phrases resolve through parseSendTime using clientContext; model never supplies epoch/tz.
    - delete/cancel carries no new title/time/duration and shows the exact target before staging.
    - Tool first inspects provider state; missing, attendee-bearing, or reconnect-needed events write no plan proposal.
    - Fresh provider state/etag is atomically copied into registry and plan with kind=calendar_manage, status=proposed.
    - Zero fetch/write-action calls occur after the final stage mutation; no approve mutation is reachable.
    - Foreign refs return the same not-found result as missing refs.
  </behavior>
  <action>
Add `proposeCalendarChange` with a closed operation enum and optional natural-language fields. Use
the trusted client clock exactly as create staging does. Call the inspect-only action, then one
internal `stageChange` mutation that re-checks ownership/active state and persists the inspected
etag plus original snapshot and desired overrides. This closes the TOCTOU window inside the app;
provider-side changes after staging are caught by If-Match at approval.

Do not accept external provider ids from the model—only managedEventId. Do not let delete accept a
cancellation comment; comments could be sent to attendees. Static guard must prove llm.ts names
inspect but no create/manage/terminal/executePlan function.

Mutation ledger: skip inspect; accept externalEventId; stage without etag; write status=approved;
default ambiguous time; accept delete content; remove tenant re-check. Each must fail a named test.
  </action>
  <verify>
    <automated>cd packages/backend &amp;&amp; npx vitest run convex/cockpitTools.test.ts convex/calendarEvents.test.ts convex/dispatchGuard.test.ts</automated>
  </verify>
  <done>All seven mutations go RED and the restored tool can only propose a fresh, owned, attendee-free change.</done>
</task>

<task type="auto">
  <name>Task 3: truthful management card and offline user-observable path</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, apps/web/e2e/calendar-management.spec.ts, docs/playbooks/cockpit.md</files>
  <action>
Finish the Plan-05 management card using the landed fields. Show provider, original event title/time,
operation, and changed fields. An unchanged field is not repeated as if it changed. For delete, use
destructive styling already established in the app and copy that says the event will be removed only
after approval; do not promise attendee notifications. Render conflict/reconnect/attendee refusal as
static reason-code copy with a restage/reconnect path.

Add an offline browser spec using seeded registry/provider fixtures: list → stage update → Calendar
management card; list → stage delete → destructive card; no email chrome; no provider write before
Approve; trace verbs render. This test need not complete a network action—that is Plan 11.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web exec playwright test e2e/calendar-management.spec.ts</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Both management operations are reviewable and visually distinct before any external write.</done>
</task>

</tasks>

<verification>
Focused tool, registry, guard, trace-parity, web typecheck, and offline Playwright gates pass. Source
scan proves the new tools contain no provider write or approval reference.
</verification>

<success_criteria>
- Users can identify and stage changes to Pikar-managed events.
- Staging is current-version, tenant-safe, trusted-clock, and attendee-safe.
- The card tells the truth about the exact provider effect.
- Nothing external happens until Approve.
- Update/move and cancel/delete are both user-observable offline.
</success_criteria>

<output>
Create `17-09-SUMMARY.md` with tool schemas, staging order, cap/truncation rule, mutation evidence,
and screenshots/test evidence for both card variants.
</output>
