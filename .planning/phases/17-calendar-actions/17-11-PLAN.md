---
phase: 17-calendar-actions
plan: 11
type: execute
gap_closure: true
wave: 7
depends_on: ["17-10"]
files_modified:
  - .planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json
  - .planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md
  - .planning/phases/17-calendar-actions/17-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/onboarding.md
autonomous: false
requirements: [ACTN-02]

must_haves:
  truths:
    - "Real Google and Microsoft accounts each pass availability, approved create, conflict refusal, and restaged update; approved delete passes on Google only, while Microsoft delete is explicitly refused as a provider limitation"
    - "No real provider write occurs before the visible Approve gate"
    - "A duplicate approval/retry creates or mutates no duplicate event"
    - "Live audit, DLQ, notification, trace, plan, registry, and provider evidence agree at each governed boundary; the later native Microsoft cleanup is recorded as an external cleanup divergence without exposing tokens or event prose"
    - "ACTN-02 closes only as 'management, minus Microsoft delete' and only when every ADR-023-permitted provider/operation evidence row is present"
  artifacts:
    - path: ".planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json"
      provides: "machine-readable refs-only provider lifecycle proof"
      contains: "phase17-calendar-live-evidence.v1"
    - path: "docs/playbooks/cockpit.md"
      provides: "last-verified live Calendar operations and honest residual scope"
      contains: "LIVE-VERIFIED"
  key_links:
    - from: "17-LIVE-EVIDENCE.json"
      to: "17-VERIFICATION.md"
      via: "every G1/G2/H1/H2/H3 row receives observed evidence"
      pattern: "ACTN-02"
---

<objective>
Pay the real-provider gate for ADR-023's ACTN-02 closure: management, minus Microsoft delete. The
agent automates deployment, browser, queries, evidence capture, and cleanup. The owner performs
only unavoidable consent and one out-of-band calendar edit used to prove optimistic conflict
refusal. Google retains approved delete; Microsoft delete must end in an explicit provider-limit
refusal without a Graph write.
</objective>

<context>
@.planning/phases/17-calendar-actions/17-VERIFICATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md
@.planning/phases/17-calendar-actions/17-10-SUMMARY.md
@.planning/phases/17-calendar-actions/17-GRAPH-CONCURRENCY-PROBE.json
@.planning/phases/17-calendar-actions/17-08-SUMMARY.md
@docs/decisions/023-microsoft-calendar-is-update-only.md
@docs/design/BRAND.md
@docs/playbooks/cockpit.md
@docs/playbooks/onboarding.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: live environment preflight and zero-write readiness evidence</name>
  <files>.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json</files>
  <action>
Resolve the exact deployed commit, build timestamp, Convex deployment, Google/Microsoft OAuth env
presence (names/booleans only), redirect URIs, fixture absence, and current provider connection
status. Start/reuse backend and production web servers; never ask the owner to run CLI commands.
Run the Plan-10 focused smoke gate once. Before Google reconnect, run H3 against the real
pre-widening stored scope: require reconnect plus zero Calendar fetches/events/success audits, or
STOP if that historical row is unavailable. Complete the legacy migration dry-run and bounded
max-100 apply pages to `done=true`, then require zero pending before any listing. Validate the Graph
probe's PATCH evidence independently of its collapsed `supported` field: require
`stalePatchStatus === 412`, `stalePatchPreserved === true`, cleanup, and current deployment/account
hashes. Never require `supported === true` or stale DELETE=412: ADR-023 records that Graph ignored
stale DELETE, so Microsoft delete remains forbidden regardless of probe input. A PATCH-evidence or
binding mismatch triggers the create-only fallback and blocks Microsoft update. Initialize exact
root keys `schema,deploymentUrlHash,
capturedAt,preflight,creates,conflicts,updates,deletes,cleanup,reconciliation` with schema
`phase17-calendar-live-evidence.v1`. Store no content, secret, token, scope value or raw response.
  </action>
  <verify>
    <automated>node -e "const h=require('./.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json'),k=['schema','deploymentUrlHash','capturedAt','preflight','creates','conflicts','updates','deletes','cleanup','reconciliation'];if(JSON.stringify(Object.keys(h).sort())!==JSON.stringify(k.sort())||h.schema!=='phase17-calendar-live-evidence.v1'||!h.preflight.migration.done||h.preflight.migration.pending!==0||!h.preflight.h3.reconnectRequired||h.preflight.h3.providerFetchCount!==0||h.preflight.h3.providerEventDelta!==0||h.preflight.graphProbe.stalePatchStatus!==412||h.preflight.graphProbe.stalePatchPreserved!==true||!h.preflight.graphProbe.cleanupMissing)throw Error('preflight')"</automated>
  </verify>
  <done>Both connect surfaces load on the fresh build and no provider event has been created by preflight.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint A: owner grants Google and Microsoft delegated consent</name>
  <files>configured disposable Google and Microsoft Calendar accounts</files>
  <action>PAUSE after Task 1. Prepare both consent pages. After resume, machine-read connected/account-hash booleans before Task 2; never request or capture secrets.</action>
  <what-built>Fresh live deployment with both bounded provider connection pages open and callback/env checks complete.</what-built>
  <how-to-verify>
The owner clicks each prepared consent button, selects the designated disposable Google and Microsoft
Calendar accounts, reviews the requested scopes, and returns to Connections. No command, secret,
token, or dashboard edit is requested from the owner.
  </how-to-verify>
  <verify>Continuation agent confirms both safe connection states and expected account hashes without token values.</verify>
  <done>Both designated disposable provider accounts are connected.</done>
  <resume-signal>Reply `both connected` or identify the provider whose consent failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: real availability and exactly-once approved create on both providers</name>
  <files>.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json</files>
  <action>
Using the connected accounts, create known private busy blocks directly only if authorized by the
owner/session. Ask the cockpit for each provider's availability and compare returned intervals.
Stage uniquely titled timed events `PIKAR ACTN02 GOOGLE DELETE ME` and
`PIKAR ACTN02 MICROSOFT UPDATE ME`. Capture trace and provider-specific card before approval; query
the provider to prove absence. Approve each, attempt duplicate approval, then capture exactly one
event, plan done, registry active/id/etag, and exact created audit key set. Populate JSON only from
`smoke.calendarLifecycleReadback`, with exact provider/plan/run/registry/external ids, etags, counts,
audit names/payload-key sets and leak booleans; store no title/time/content/token/raw response.
  </action>
  <verify>
    <automated>node -e "const h=require('./.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json');for(const p of ['google','microsoft']){const x=h.creates?.[p];if(!x||!x.planId||!x.calendarRunId||!x.registryId||!x.externalEventId||!x.createdEtag||x.preApproveCount!==0||x.postApproveCount!==1||x.duplicateProviderCount!==0||x.duplicateRegistryCount!==0||x.contentLeak||x.tokenLeak)throw Error('create '+p)}"</automated>
  </verify>
  <done>Both providers have one and only one Pikar event created after approval.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint B: owner makes one out-of-band edit per staged event</name>
  <files>the exact disposable provider events identified by Task 2</files>
  <action>PAUSE after recording both staged etags. After resume, require each provider etag is nonempty and different before approval; never silently refresh the staged plan.</action>
  <what-built>Update/move proposals staged against fresh Google and Microsoft etags, with exact proposed times visible and no write yet.</what-built>
  <how-to-verify>
The owner opens each named test event in its native Calendar UI and changes its time by five minutes,
then returns without touching Pikar. This is the unavoidable external concurrency action that makes
the stored proposal etag stale.
  </how-to-verify>
  <verify>Continuation agent machine-compares staged and provider etags and requires inequality for both providers.</verify>
  <done>Both exact staged proposals are stale due to owner edits.</done>
  <resume-signal>Reply `both edited` or identify which provider could not be edited.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: conflict refusal, restaged update, Google delete, Microsoft refusal, and cleanup</name>
  <files>.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json, .planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md, docs/playbooks/cockpit.md, docs/playbooks/onboarding.md</files>
  <action>
Approve each now-stale proposal. Verify conflict notification/code, no overwrite, unchanged provider
event, and no success audit. Restage from the fresh provider snapshot and approve; verify the desired
move/title/duration, new etag, registry update, and refs/code-only update audit. Stage delete using
cancel/delete language for Google, approve, attempt it again, and verify the event is absent,
registry deleted, and exactly one deletion audit. Separately request Microsoft cancel/delete and
require the visible `provider_unsupported` refusal before token fetch or provider write: the event
and active registry row remain unchanged, zero deletion success audits exist, and the copy says the
event is still on Microsoft Calendar. Then remove the Microsoft test event directly as authorized
cleanup, never through the product's forbidden Graph delete path. Record the native cleanup
separately: provider event absent, registry still active at its last governed state, and zero delete
success audits. That expected divergence must not be misreported as a product delete. Delete any
known busy blocks/test residue. Run token/
event-title absence scans over audit, DLQ, notification, and logs. Record timestamps, ids, counts,
statuses and exact readback refs in JSON; keep screenshots/content outside this refs-only artifact.
Mark each runbook row pass/fail; do not edit
REQUIREMENTS or VERIFICATION here.

If either provider or required operation/refusal outcome fails, keep ACTN-02 open, preserve the event
for diagnosis unless it poses external risk, and return a structured blocker with exact
provider/status/code evidence.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>node -e "const h=require('./.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json');for(const p of ['google','microsoft']){const c=h.conflicts?.[p],u=h.updates?.[p];if(!c||c.stagedEtag===c.providerEtag||c.failureCode!=='conflict'||c.successAuditCount!==0||c.contentLeak||c.tokenLeak)throw Error('conflict '+p);if(!u||u.providerEtag!==u.registryEtag||u.successAuditCount!==1||u.duplicateAuditCount||u.contentLeak||u.tokenLeak)throw Error('update '+p)}const d=h.deletes?.google,r=h.deletes?.microsoftRefusal;if(!d||d.providerExists!==false||d.registryStatus!=='deleted'||d.successAuditCount!==1||d.duplicateAuditCount||!d.replaySuccess||d.contentLeak||d.tokenLeak)throw Error('delete google');if(!r||r.failureCode!=='provider_unsupported'||r.providerFetchCount!==0||r.providerExistsAfterRefusal!==true||r.registryStatusAfterRefusal!=='active'||r.providerExistsAfterNativeCleanup!==false||r.registryStatusAfterNativeCleanup!=='active'||r.successAuditCount!==0||r.contentLeak||r.tokenLeak)throw Error('microsoft refusal');if(h.cleanup.providerEventCount||h.cleanup.testPlanCount||h.cleanup.testNotificationCount||h.cleanup.contentLeak||h.cleanup.tokenLeak)throw Error('cleanup')"</automated>
  </verify>
  <done>Both providers pass safe create/update conflict semantics, Google delete succeeds, Microsoft delete is explicitly refused without a provider write, and cleanup is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint C: owner confirms provider-visible outcomes and no residue</name>
  <files>Google Calendar, Microsoft Calendar, and the refs-only evidence summary</files>
  <action>PAUSE after Task 3. After approval, Task 4 re-reads every exact ref before changing shared completion metadata; a mismatch leaves ACTN-02 pending.</action>
  <what-built>Complete ADR-023 lifecycle evidence, native-provider cleanup, and explicit recording of the expected post-cleanup Microsoft provider/registry divergence.</what-built>
  <how-to-verify>
Open each native Calendar and confirm the two Pikar test events and known busy blocks are gone. Review
the captured evidence summary: provider-visible times/counts must match Pikar's cards and registry,
and no attendee was invited or notified.
  </how-to-verify>
  <verify>Owner reports no native-provider residue or count mismatch; record only approval result/timestamp.</verify>
  <done>Human provider-visible observations agree with the machine evidence.</done>
  <resume-signal>Reply `approved` or name the mismatched provider/evidence row.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: final machine reconciliation and truthful Phase 17 closure</name>
  <files>.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json, .planning/phases/17-calendar-actions/17-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, docs/playbooks/cockpit.md, docs/playbooks/onboarding.md</files>
  <action>
After—and only after—Checkpoint C approval, re-run exact `calendarLifecycleReadback` calls for both
providers. Fill `reconciliation` with ownerApproved/approvedAt, lifecycleEvidenceConsistent,
final zero-residue counts, and contentLeak/tokenLeak false. Revalidate all IDs, etag transitions,
audit key sets, duplicate counts, conflict refusal and cleanup. Require the final Microsoft
readback to show the expected native-cleanup state—provider absent, registry active, no delete
success audit—without treating that external cleanup as a governed product delete. Recursively reject forbidden keys:
title, subject, body, description, attendees, email, accessToken, refreshToken, scope, rawResponse,
and any token-prefixed key except the boolean `tokenLeak`.

Only after that JSON gate passes: change `17-VERIFICATION.md` from gaps_found to passed with exact
refs and residual exclusions; mark ACTN-02 complete in REQUIREMENTS checkbox/traceability; update
ROADMAP Phase 17 to eleven completed plans; and update Cockpit/Onboarding Last verified evidence.
Any red/missing checkpoint/probe/H3/duplicate/leak/residue leaves all completion markers pending.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),h=JSON.parse(fs.readFileSync('.planning/phases/17-calendar-actions/17-LIVE-EVIDENCE.json','utf8')),r=h.reconciliation,m=h.deletes?.microsoftRefusal;if(h.schema!=='phase17-calendar-live-evidence.v1'||!r?.ownerApproved||!r.lifecycleEvidenceConsistent||r.providerEventCount||r.testPlanCount||r.testNotificationCount||r.contentLeak||r.tokenLeak||m.providerExistsAfterNativeCleanup!==false||m.registryStatusAfterNativeCleanup!=='active')throw Error('reconciliation');const bad=new Set(['title','subject','body','description','attendees','email','accessToken','refreshToken','scope','rawResponse']),walk=o=>{if(o&amp;&amp;typeof o==='object')for(const[k,v]of Object.entries(o)){if(bad.has(k)||(/^token/i.test(k)&amp;&amp;k!=='tokenLeak'))throw Error('forbidden '+k);walk(v)}};walk(h);for(const p of ['google','microsoft'])if(h.creates[p].duplicateProviderCount||h.creates[p].duplicateRegistryCount||h.conflicts[p].successAuditCount||h.updates[p].duplicateAuditCount)throw Error('duplicate '+p);if(h.deletes.google.duplicateAuditCount||m.successAuditCount)throw Error('delete evidence');console.log('PHASE17_LIVE_GATE_PASS')"</automated>
    <automated>rg -n "ACTN-02.*Complete|Phase 17.*11/11|status: passed" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/17-calendar-actions/17-VERIFICATION.md</automated>
    <automated>node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>Machine and human evidence agree, docs match ADR-023, and ACTN-02 closes as management minus Microsoft delete without content/token leakage.</done>
</task>

</tasks>

<verification>
Every ADR-023-permitted provider-by-operation row is live PASS, Microsoft delete refusal is observed,
conflict behavior is owner-induced, and lifecycle snapshots reconcile through the expected external
Microsoft cleanup divergence. No secret/content leaks exist and cleanup is owner-confirmed. A fresh
GSD verification pass may then decide whether to mark ACTN-02 complete with the recorded limitation.
</verification>

<success_criteria>
- Google and Microsoft availability/create/update pass live; Google delete passes live.
- Microsoft cancel/delete is explicitly refused as unsupported before any provider write.
- Conflict prevents stale overwrite on both providers.
- Duplicate approvals/retries are idempotent.
- All writes remain human-approved and attendee-free.
- Evidence is measurable, redaction-safe, and cleaned up.
</success_criteria>

<output>
Create `17-11-SUMMARY.md` only after Checkpoint C approval. Include exact live timestamps/counts,
provider ids as refs, audit key sets, cleanup confirmation, and the retained Microsoft-delete
provider limitation.
</output>
