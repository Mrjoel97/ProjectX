---
phase: 17-calendar-actions
plan: 10
type: execute
gap_closure: true
wave: 6
depends_on: ["17-09"]
files_modified:
  - packages/backend/convex/calendar.test.ts
  - packages/backend/convex/microsoftCalendar.test.ts
  - packages/backend/convex/calendarEvents.test.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/dispatchGuard.test.ts
  - packages/backend/convex/traceParity.test.ts
  - packages/backend/convex/smoke.ts
  - packages/core/src/calendar.test.ts
  - packages/core/src/calendarManagement.test.ts
  - packages/core/src/actionType.test.ts
  - apps/web/e2e/calendar-management.spec.ts
  - .planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [ACTN-02]

must_haves:
  truths:
    - "The complete offline Calendar gate exits within its declared timeout with no hanging worker"
    - "Both providers cover availability, create, update/move, delete/cancel, reauth, transient, conflict, replay, attendee refusal, and tenant isolation"
    - "Every absence/static claim has a positive anchor and at least one mutation observation"
    - "No existing Google create assertion is weakened to accommodate Microsoft or management"
    - "The UAT runbook can gather requirement-closing evidence from both providers without asking the owner to run terminal commands"
  artifacts:
    - path: ".planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md"
      provides: "two-provider create/manage/conflict live evidence protocol"
      contains: "Microsoft"
    - path: "packages/backend/convex/dispatchGuard.test.ts"
      provides: "non-vacuous governance/endpoint ownership matrix"
      min_lines: 450
  key_links:
    - from: "17-UAT-RUNBOOK.md"
      to: "17-VERIFICATION.md"
      via: "each gap/human gate maps to a capture row"
      pattern: "ACTN-02"
---

<objective>
Close Nyquist and operational-validation gaps before live provider work: run the focused integrated
suite, mutation-audit every governance claim, verify type/build/playbooks, and rewrite the owner UAT
runbook for Microsoft parity plus conflict-safe management.
</objective>

<context>
@.planning/phases/17-calendar-actions/17-VERIFICATION.md
@.planning/phases/17-calendar-actions/17-05-SUMMARY.md
@.planning/phases/17-calendar-actions/17-06-SUMMARY.md
@.planning/phases/17-calendar-actions/17-07-SUMMARY.md
@.planning/phases/17-calendar-actions/17-08-SUMMARY.md
@.planning/phases/17-calendar-actions/17-09-SUMMARY.md
@.planning/phases/17-calendar-actions/17-VALIDATION.md
@CLAUDE.md
@docs/design/BRAND.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: focused two-provider requirement matrix and bounded runtime</name>
  <files>packages/backend/convex/calendar.test.ts, packages/backend/convex/microsoftCalendar.test.ts, packages/backend/convex/calendarEvents.test.ts, packages/backend/convex/cockpit.test.ts, packages/backend/convex/cockpitTools.test.ts, packages/backend/convex/dispatchGuard.test.ts, packages/backend/convex/traceParity.test.ts, packages/backend/convex/smoke.ts, packages/core/src/calendar.test.ts, packages/core/src/calendarManagement.test.ts, packages/core/src/actionType.test.ts, apps/web/e2e/calendar-management.spec.ts</files>
  <action>
Build a visible matrix for provider × operation × boundary:

- Google/Microsoft availability and reauth;
- create idempotency (Google deterministic id/409, Microsoft transactionId);
- update desired-state reconciliation, If-Match conflict, and replay;
- delete missing-as-success, If-Match conflict, and replay;
- attendee-bearing refusal before every write;
- plan/registry/terminal two-tenant isolation with non-vacuity;
- audit/DLQ exact key sets and provider-body absence;
- tool→stage→Approve separation and double-approve no-op.
- exact `calendarLifecycleReadback` schema/key sets, positive plan/registry/audit witnesses,
  duplicate counts, provider id/etag transitions, and recursive content/token absence.

Add only tests needed to fill matrix holes. Run the backend gate through Plan 17-05's executable
wrapper with a 90-second outer timeout and 20-second per-test timeout. The wrapper must assert exit
0, a positive JSON-reporter total, and passed===total before printing the measurable total/duration.
If it hangs, stop only that Vitest process, identify the named resource,
and fix the test lifecycle; do not broaden timeouts. Do not run unrelated full suites.
  </action>
  <verify>
    <automated>npx vitest run packages/core/src/calendar.test.ts packages/core/src/calendarManagement.test.ts packages/core/src/actionType.test.ts</automated>
    <automated>node scripts/run-calendar-test-gate.mjs --timeout-ms 90000 --test-timeout-ms 20000 --hook-timeout-ms 10000 -- convex/calendar.test.ts convex/microsoftCalendar.test.ts convex/calendarEvents.test.ts convex/cockpit.test.ts convex/cockpitTools.test.ts convex/dispatchGuard.test.ts convex/traceParity.test.ts</automated>
    <automated>pnpm --filter @pikar/web exec playwright test e2e/calendar-management.spec.ts</automated>
  </verify>
  <done>The focused matrix has exact passing counts and finishes under the declared timeout.</done>
</task>

<task type="auto">
  <name>Task 2: mutation/type/build gate and machine-readable live runbook</name>
  <files>packages/backend/convex/dispatchGuard.test.ts, packages/backend/convex/calendar.test.ts, packages/backend/convex/microsoftCalendar.test.ts, packages/core/src/calendarManagement.test.ts, .planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md, docs/playbooks/cockpit.md</files>
  <action>
Review the mutation ledgers from Plans 05–09. Re-run at least one mutation for each critical class:
approval reachability, tenant guard, attendee refusal, etag/If-Match, retry reconciliation, token
redaction/rotation, and provider default. Every mutation must fail a specifically named test and the
restored focused gate must pass. Run core/backend/web typechecks, web production build, and
check-playbooks. Backend test-file diagnostic baseline may be recorded, but zero production
diagnostics is mandatory. Do not “fix” unrelated test files.

Prove old Google create fixtures pass without calendarProvider and that Gmail/Drive Google grant
tests are unchanged. Record hashes or exact named tests, not plan claims.

Rewrite the UAT runbook as an agent-operated, no-terminal-command owner protocol. It must include:
Google/Microsoft app/env/callback preflight; completion of the bounded legacy migration before any
listing; H3 pre-widening Google stored-scope negative before reconnect; exact provider availability;
stage/card/Approve creates; out-of-band stale-etag edits; 412 refusal; restaged update; delete and
idempotent repeat; final cleanup; and the final owner visual check. Define the machine artifact
`17-LIVE-EVIDENCE.json` (`phase17-calendar-live-evidence.v1`) with exact refs-only sections
`preflight,creates,conflicts,updates,deletes,cleanup,reconciliation`. Every section must be generated
from `smoke.calendarLifecycleReadback`, not prose markers, and carry provider/plan/run/registry/
external-event ids, etag transitions, exact audit names/key sets/counts, duplicate counts, conflict
and cleanup booleans, plus recursive content/token absence. Keep attendee-bearing proof offline.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core typecheck</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>rg -n "phase17-calendar-live-evidence.v1|calendarLifecycleReadback|H3|Google|Microsoft|conflict|cleanup|ROADMAP|REQUIREMENTS|VERIFICATION" .planning/phases/17-calendar-actions/17-UAT-RUNBOOK.md</automated>
  </verify>
  <done>Every critical negative claim is mutation-proven, all production gates pass, and the runbook defines exact JSON readbacks rather than prose markers.</done>
</task>

</tasks>

<verification>
The full focused Calendar matrix completes with exact counts, all relevant type/build/playbook gates
are recorded, mutation coverage is non-vacuous, and the live runbook covers both requirement gaps.
</verification>

<success_criteria>
- No current focused gate is hanging or red.
- Both provider/operation matrices are complete offline.
- Critical governance claims are mutation-proven.
- The shipped Google slice is explicitly regression-proven.
- Live verification is reduced to real-provider observations and owner consent.
</success_criteria>

<output>
Create `17-10-SUMMARY.md` with exact test counts/durations, mutation table, type/build output,
unchanged-Google evidence, and the final live checklist carried into Plan 11.
</output>
