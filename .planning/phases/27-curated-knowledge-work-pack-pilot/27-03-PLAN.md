---
phase: 27-curated-knowledge-work-pack-pilot
plan: 03
type: execute
wave: 2
depends_on: ["27-02"]
files_modified:
  - packages/backend/convex/workflowPackEventLog.ts
  - packages/backend/convex/workflowPackEventLog.test.ts
  - packages/core/src/workflowPackMetrics.ts
  - packages/core/src/workflowPackMetrics.test.ts
  - docs/playbooks/workflow-packs.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [PACK-04]
must_haves:
  truths:
    - "Outcome metrics use closed events containing refs, hashes, ids, counts and enums ONLY (CLAUDE.md §4)."
    - "This plane never re-emits cost or latency: cost is read from `spendEvents` (rail `reasoning`, index `by_correlation`), latency from `telemetry.durationMs` / `agentSteps`."
    - "Missing-connector surprise, citation coverage and zero-denominator states have deterministic definitions."
    - "No prompt, output, excerpt, URL, financial value, customer name or connector body enters telemetry."
  artifacts:
    - path: packages/backend/convex/workflowPackEventLog.ts
      provides: Tenant-scoped append-only recorder over the `workflowPackEvents` table that 27-02 creates
    - path: packages/core/src/workflowPackMetrics.ts
      provides: Pure outcome metric definitions
  key_links:
    - from: packages/backend/convex/workflowPackEventLog.ts
      to: packages/core/src/workflowPackMetrics.ts
      via: closed event vocabulary
      pattern: "run_completed|capability_missing|plan_approved"
---

<objective>Add a privacy-bounded measurement plane that rewards completed useful outcomes, not installations.</objective>
<context>@.planning/phases/27-curated-knowledge-work-pack-pilot/27-RESEARCH.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-VALIDATION.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-READINESS.md</context>

<scope_corrections>
Applied 2026-08-23 from `27-READINESS.md`. Binding on the executor:

1. **The `workflowPackEvents` TABLE is a 27-02 deliverable, not this plan's.** 27-02 owns `schema.ts`
   (and therefore the `tenantData.ts` classification and the `audit-dead-letter.md` playbook bump the
   new table forces). This plan is the **metrics module + recorder only**. Do NOT edit
   `packages/backend/convex/schema.ts` or `packages/core/src/tenantData.ts` here. If the table is
   absent when this plan runs, 27-02 did not finish — stop and fix it there, do not add it here.

2. **Do NOT re-emit cost or latency.** `spendEvents` owns cost (rail `reasoning`, `amountCents`,
   index `by_correlation`) and `telemetry.durationMs` / `agentSteps` own latency. A duplicate cost
   number that can disagree with the billing plane is worse than no number. This plane would already
   be the FIFTH tenant event plane beside `audit`, `telemetry`, `agentSteps` and `spendEvents`; keep
   it to the events that genuinely have no home — pack run lifecycle, plan decisions, missing-source
   surprise, recommendation impressions — and READ cost/latency from their existing owners by
   `correlationId`.

3. **CLAUDE.md §4 is a structural requirement, not a guideline.** Event payloads carry refs, hashes,
   ids, counts and closed enums ONLY. Never raw user content: no prompt, output, excerpt, URL,
   subject line, customer name or financial value.

4. **Backend module renamed** `workflowPackEvents.ts` → `workflowPackEventLog.ts`, so the module name
   collides neither with the `workflowPackEvents` TABLE that 27-02 creates nor with
   `packages/core/src/workflowPackMetrics.ts`.

5. **Playbook (CLAUDE.md §9, Stop-hook enforced).** Both new files sit under the
   `packages/core/src/workflowPack` / `packages/backend/convex/workflowPack` prefixes that 27-02
   registers in `docs/playbooks/watch.json` against `docs/playbooks/workflow-packs.md`. Update that
   playbook in this plan's commit and bump its `Last verified` line. If 27-02 did not register the
   prefixes, register them here.
</scope_corrections>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define deterministic outcome metrics</name>
  <files>packages/core/src/workflowPackMetrics.test.ts, packages/core/src/workflowPackMetrics.ts</files>
  <action>Define time-to-first-useful-outcome, recommendation acceptance, plan decisions, runtime-versus-preflight connector surprise, citation/unsupported-claim rates, completion outcome and follow-up recovery. Represent unavailable denominators as not-applicable, never invented success. Cost and latency are NOT defined or stored here: define them as derived reads keyed by `correlationId` — cost from `spendEvents` (rail `reasoning`, index `by_correlation`), latency from `telemetry.durationMs` / `agentSteps` — so this module never produces a second cost number.</action>
  <verify><automated>cd packages/core && npx vitest run src/workflowPackMetrics.test.ts</automated></verify>
  <done>Every success measure has bounded deterministic numerator, denominator and unknown semantics, and no measure re-derives cost or latency from pack events.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Persist tenant-scoped privacy-safe events</name>
  <files>packages/backend/convex/workflowPackEventLog.test.ts, packages/backend/convex/workflowPackEventLog.ts, docs/playbooks/workflow-packs.md, docs/playbooks/watch.json</files>
  <action>Write the append-only recorder over the `workflowPackEvents` table 27-02 added — do NOT add or edit the table, and do NOT touch `schema.ts` or `tenantData.ts`. Enforce tenant derivation through the `convex/lib/functions.ts` wrappers (CLAUDE.md §2), append-only writes, and exact validators for the closed event vocabulary (pack run lifecycle, plan decisions, missing-source surprise, recommendation impressions — no cost or latency event kinds). Link existing plan/artifact ids by ref only. Add a forbidden-key test asserting the payload validator structurally rejects free text (CLAUDE.md §4). Update `docs/playbooks/workflow-packs.md` and bump its `Last verified` line; if 27-02 has not registered the `workflowPack` path prefixes in `docs/playbooks/watch.json`, register them here.</action>
  <verify><automated>cd packages/backend && npx vitest run convex/workflowPackEventLog.test.ts</automated></verify>
  <done>Two-tenant tests prove isolation and raw content is rejected structurally.</done>
</task>
</tasks>
<verification>- `cd packages/core && npx vitest run src/workflowPackMetrics.test.ts`
- `cd packages/backend && npx vitest run convex/workflowPackEventLog.test.ts`
- Manual: `docs/playbooks/workflow-packs.md` updated with a `Last verified` bump in the same commit.

Note: `pnpm --filter <pkg> test -- <name>` does NOT filter — the `--` is swallowed and the whole
suite runs green whether or not this plan's files exist. Never use it as a gate here.
`node scripts/check-playbooks.mjs` is likewise not a gate (it reads stdin at module top and every
terminal path is `process.exit(0)`) — the playbook line above is the real obligation.</verification>
<success_criteria>PACK-04 is measurable in principle without a content or PII side channel: the metric definitions and the event plane exist and are proven privacy-bounded by test. The emission gate — that real pack runs actually write these events — belongs to 27-07, where the emitting call sites land.</success_criteria>
<output>Create `27-03-SUMMARY.md`.</output>
