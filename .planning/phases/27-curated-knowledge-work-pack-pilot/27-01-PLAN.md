---
phase: 27-curated-knowledge-work-pack-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - third_party/knowledge-work-plugins/manifest.json
  - third_party/knowledge-work-plugins/source-snapshot/
  - third_party/knowledge-work-plugins/LICENSE
  - THIRD_PARTY_NOTICES.md
  - scripts/verify-knowledge-work-provenance.mjs
  - packages/contracts/src/skills/knowledgeWorkProvenance.test.ts
  - docs/playbooks/skill-registry.md
autonomous: true
requirements: [PACK-01]
must_haves:
  truths:
    - "Every selected upstream source resolves to one exact commit and a committed snapshot hash."
    - "Apache-2.0 attribution and prominent modification notices survive redistribution."
    - "Upstream changes can enter only as a reviewed diff and never auto-publish or auto-activate."
  artifacts:
    - path: third_party/knowledge-work-plugins/manifest.json
      provides: Canonical upstream commit, selected source hashes, license and intended adapted destinations
    - path: scripts/verify-knowledge-work-provenance.mjs
      provides: Offline fail-closed manifest and notice verifier
  key_links:
    - from: third_party/knowledge-work-plugins/manifest.json
      to: packages/contracts/skills
      via: adapted body hashes (the canonical .md, NOT the auto-derived .ts under src/skills/)
      pattern: "sourceFiles|commit|adaptedBody"
---

<objective>Pin the upstream material and make provenance, licensing, and human-reviewed updates mechanically enforceable.</objective>

<context>
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-CONTEXT.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-RESEARCH.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Freeze the exact upstream inventory and notices</name>
  <files>third_party/knowledge-work-plugins/manifest.json, third_party/knowledge-work-plugins/source-snapshot/, third_party/knowledge-work-plugins/LICENSE, THIRD_PARTY_NOTICES.md</files>
  <action>**Step 0 (MANDATORY FIRST — do not skip): re-inventory the upstream tree at the pinned SHA and record the true paths.** The source table in `27-RESEARCH.md` (the "Customer Complaint Response" row and the whole selection table) is **UNVERIFIED-AT-SHA and known-wrong in at least one place**: `small-business/skills/ticket-deflector/` does NOT exist at `5267cf7` (that tree has `customer-pulse` / `customer-pulse-check`), and Sales Call Prep and Process/SOP Builder were never inventoried at all. Because the rest of this task fails closed on an absent path, trusting that table halts the plan on its first step. So: fetch/list the upstream tree at the one pinned commit, write the ACTUAL directory paths for all six workflows into `third_party/knowledge-work-plugins/manifest.json` as the authoritative inventory, and note in the manifest that it supersedes 27-RESEARCH's table. Do NOT invent, guess, or substitute a plausible replacement path — if a workflow has no upstream counterpart at that SHA, record it as `upstreamSource: null` with a one-line reason and let the later plan decide.

Then: resolve one 40-character upstream commit, commit only the selected source snapshot for the six workflows, record its SHA-256 hashes and intended adapted destinations, preserve Apache-2.0 and any NOTICE material, and identify the planned Pikar modifications. Adapted destinations are the **canonical `.md` bodies under `packages/contracts/skills/`** — `packages/contracts/src/skills/*.ts` are AUTO-DERIVED constants and `skillBodies.test.ts` asserts the pair is byte-identical, so every manifest hash pins the `.md`, never the `.ts`. Leave adapted-body hashes explicitly pending for Plan 07. Fail if an expected path is absent (after Step 0 has recorded what actually exists); never substitute current main.</action>
  <verify><automated>cd packages/contracts && npx vitest run src/skills/knowledgeWorkProvenance.test.ts</automated></verify>
  <done>All six workflows have reproducible source and modification records with no floating ref.</done>
</task>
<task type="auto">
  <name>Task 2: Enforce manual reviewed updates</name>
  <files>scripts/verify-knowledge-work-provenance.mjs, packages/contracts/src/skills/knowledgeWorkProvenance.test.ts</files>
  <action>Implement an offline source-only verifier for commit shape, snapshot hashes, license and notice presence. Add a diff-report mode that never fetches, publishes, schedules, or activates anything; document that updating the pinned SHA is a reviewed GSD change. The complete adapted-body check is deliberately deferred to Plan 07.

The test **must live inside an existing package that already has vitest** — there is no root vitest, no root `vitest.config`, `scripts/` is not a pnpm workspace and turbo's test task cannot reach it, so a test under `scripts/` has no home and never runs. Put it at `packages/contracts/src/skills/knowledgeWorkProvenance.test.ts`: `packages/contracts/vitest.config.ts` includes `src/**/*.test.ts`, that directory is already watched by `skill-registry.md` (so no `watch.json` change is needed), and it can resolve `third_party/` via `fileURLToPath` exactly as the neighbouring `skillBodies.test.ts` resolves the canonical `.md` files.

Playbook (CLAUDE.md §9, Stop-hook enforced): update `docs/playbooks/skill-registry.md` in this same change with how the pinned upstream snapshot and adapted-body hashes relate to the registry, and bump its `Last verified` line.</action>
  <verify><automated>node scripts/verify-knowledge-work-provenance.mjs --check-source</automated></verify>
  <done>Upstream snapshot drift fails locally and cannot mutate a candidate or production skill.</done>
</task>
</tasks>

<verification>- `cd packages/contracts && npx vitest run src/skills/knowledgeWorkProvenance.test.ts`
- `node scripts/verify-knowledge-work-provenance.mjs --check-source` (must exit non-zero on drift — prove it by mutating one hash)
- `docs/playbooks/skill-registry.md` updated with its `Last verified` line bumped.

> **Note (offline):** the upstream re-inventory in Task 1 Step 0 needs one network fetch of the pinned commit. It cannot be done in an offline sitting, and no path may be guessed to work around that.</verification>
<success_criteria>PACK-01 is mechanically proven for every copied or adapted file.</success_criteria>
<output>Create `27-01-SUMMARY.md`.</output>
