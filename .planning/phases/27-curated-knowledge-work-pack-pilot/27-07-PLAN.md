---
phase: 27-curated-knowledge-work-pack-pilot
plan: 07
type: execute
wave: 3
depends_on: ["27-03", "27-04", "27-05", "27-06"]
files_modified:
  - packages/backend/convex/workflowPackRuntime.ts
  - packages/backend/convex/workflowPackRuntime.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/artifacts.ts
  - packages/backend/convex/workflowPackEvents.ts
  - packages/backend/convex/workflowPackMetrics.ts
  - packages/backend/convex/workflowPackMetrics.test.ts
autonomous: true
requirements: [PACK-02, PACK-03, PACK-04]
must_haves:
  truths:
    - "A selected pack runs through the one existing agent loop with an exact code-owned tool record and governed outputs."
    - "Real discovery, run, plan, artifact, missing-source, cost and latency events feed a bounded tenant metric projection."
  artifacts:
    - path: packages/backend/convex/workflowPackRuntime.ts
      provides: Native Executive-Agent pack adapter, output handling and event hooks
    - path: packages/backend/convex/workflowPackMetrics.ts
      provides: Tenant-scoped outcome projection over non-vacuous events
  key_links:
    - from: packages/backend/convex/workflowPackRuntime.ts
      to: packages/backend/convex/llm.ts
      via: runAgentLoop with exact registry-derived tool names
      pattern: "runAgentLoop|toolsForWorkflowPack"
---

<objective>Join the six bodies to the real Executive Agent runtime and wire privacy-safe events from real workflow terminals.</objective>
<context>
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-CONTEXT.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-RESEARCH.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-VALIDATION.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-01-PLAN.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-02-PLAN.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire packs through the real Executive Agent loop</name>
  <files>packages/backend/convex/workflowPackRuntime.test.ts, packages/backend/convex/workflowPackRuntime.ts, packages/backend/convex/llm.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/artifacts.ts</files>
  <action>Add a thin native adapter that validates a closed pack id, loads the exact registry body/version, filters the existing loop to registry-derived tool names, preserves the web/Vault/inbox untrusted-data fences, and maps results only to existing plan/artifact surfaces. Test unknown-id refusal, exact tool equality, injected-source containment, missing-source states, plan approval boundaries and no second router/runtime/store.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- workflowPackRuntime</automated></verify>
  <done>All six packs execute through the real loop and cannot reach an ungranted write.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire non-vacuous events and a bounded outcome projection</name>
  <files>packages/backend/convex/workflowPackRuntime.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/artifacts.ts, packages/backend/convex/workflowPackEvents.ts, packages/backend/convex/workflowPackMetrics.test.ts, packages/backend/convex/workflowPackMetrics.ts</files>
  <action>Emit recommendation shown/accepted, preflight, run, plan decision, artifact, missing-source, completion, cost and latency events from their real terminals. Add a bounded tenant read model over the pure metrics. Test two-tenant isolation, exact forbidden keys and a complete journey whose counters are non-zero and internally consistent.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- workflowPackEvents workflowPackMetrics</automated></verify>
  <done>PACK-04 measures real workflow journeys without raw content or install counts.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- workflowPackRuntime workflowPackEvents workflowPackMetrics`
</verification>
<success_criteria>The pilot has one executable native path from selected pack through governed output and measured outcome.</success_criteria>
<output>Create `27-07-SUMMARY.md`.</output>
