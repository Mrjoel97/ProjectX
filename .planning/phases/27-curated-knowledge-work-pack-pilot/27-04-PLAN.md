---
phase: 27-curated-knowledge-work-pack-pilot
plan: 04
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - packages/contracts/skills/pack-business-pulse.md
  - packages/contracts/skills/pack-campaign-plan.md
  - packages/contracts/src/skills/packBusinessPulse.ts
  - packages/contracts/src/skills/packCampaignPlan.ts
  - packages/contracts/src/skills/skillBodies.test.ts
  - packages/backend/scripts/workflow-pack-fixtures/business-pulse.json
  - packages/backend/scripts/workflow-pack-fixtures/campaign-plan.json
  - docs/playbooks/skill-registry.md
  - docs/playbooks/workflow-packs.md
autonomous: true
requirements: [PACK-01, PACK-02, PACK-03]
must_haves:
  truths:
    - "Business Pulse names the Phase 26 summary, content-shelf and CRM inputs it CANNOT read as missing, rather than implying it read them."
    - "Campaign Plan produces a plan document; it never claims to have run research, drafted content, generated media, or launched anything."
    - "Both bodies use provider-neutral Pikar language and cannot widen their grants."
    - "Each body is TWO committed files (canonical .md + auto-derived .ts) plus a skillBodies.test.ts entry, byte-identical."
  artifacts:
    - path: packages/contracts/skills/pack-business-pulse.md
      provides: Canonical Business Pulse candidate body (the file the manifest hash pins)
    - path: packages/contracts/skills/pack-campaign-plan.md
      provides: Canonical Campaign Plan candidate body (the file the manifest hash pins)
    - path: packages/contracts/src/skills/packBusinessPulse.ts
      provides: Auto-derived Business Pulse constant the Convex runtime ships
    - path: packages/contracts/src/skills/packCampaignPlan.ts
      provides: Auto-derived Campaign Plan constant the Convex runtime ships
  key_links:
    - from: packages/contracts/skills/pack-business-pulse.md
      to: packages/core/src/workflowPacks.ts
      via: registry-owned body plus code-owned grants
      pattern: "business-pulse"
---

<objective>Adapt and fixture the two proactive planning/briefing workflows on native Pikar sources.</objective>
<context>@.planning/phases/27-curated-knowledge-work-pack-pilot/27-RESEARCH.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-VALIDATION.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-READINESS.md</context>

<owner_decisions>
Settled 2026-08-23 by the readiness audit. Do not re-litigate; author to these.

- **Both packs stay in the pilot.** Business Pulse's nominal inputs — the Phase 26 report summaries
  (`reportsBusiness.ts` business/operations/sentMail are `tenantQuery`, **not** agent tools), the
  content shelf (`content.ts` is `tenantQuery`-only by construction) and CRM facts (the only contact
  tools are `resolveContacts`, labels never addresses, and `stageCrmWrite`) — are classified
  `missing` in 27-02's operation matrix. **Do NOT add new read tools** to make either pack look
  complete: that is scope no plan owns and it inflates the eval corpus 27-08 pays for. Write the
  bodies so the pack states honestly what it could not read.
- **Packs are LEAF AGENTS.** Verified at `llm.ts:4337` (`grantDispatch: toolNames === undefined`) and
  `llm.ts:4342` (`grantSkillAuthoring: toolNames === undefined`): a tool allow-list structurally
  disables specialist dispatch. The Executive Agent keeps composition. **Campaign Plan PRODUCES A
  PLAN** — it does not orchestrate research + content + media, and its body must not be written as if
  it could.
</owner_decisions>

<tasks>
<task type="auto">
  <name>Task 1: Adapt Business Pulse and Campaign Plan</name>
  <files>packages/contracts/skills/pack-business-pulse.md, packages/contracts/skills/pack-campaign-plan.md, packages/contracts/src/skills/packBusinessPulse.ts, packages/contracts/src/skills/packCampaignPlan.ts, packages/contracts/src/skills/skillBodies.test.ts</files>
  <action>Rewrite the pinned sources into Pikar terminology, composing only the seams 27-02's operation matrix marks `existing` (Blueprint, Vault, research, the bounded artifact reads in `content.ts`). Preserve provenance markers and forbid sends, paid-media launch, CRM mutation and fabricated performance.

**A body is TWO files plus a test entry — not one.** Canonical bodies are `.md` under
`packages/contracts/skills/` (30 files today, kebab-case); `packages/contracts/src/skills/` holds the
**auto-derived `.ts`** constant the Convex runtime ships. `packages/contracts/src/skills/skillBodies.test.ts`
asserts the pair is byte-identical (LF-normalized). So for each pack write the `.md`, derive the `.ts`
constant from it, **and add the `[canonical .md basename, derived constant]` entry to the `bodies`
array in `skillBodies.test.ts`** plus its import. 27-01's manifest hashes pin the **`.md`**, never the `.ts`.

**Business Pulse — what it may NOT claim (owner decision A).** Phase 26 report summaries, the content
shelf and CRM facts are `tenantQuery`-only and are `missing` in the matrix. The body must instruct the
pack to **name each unread input explicitly in its output** ("no revenue/payment source is connected";
"Phase 26 business and operations summaries are not readable from this workflow"), never to infer,
estimate or narrate around them. Same treatment for HubSpot/QuickBooks/payment sections: explicit
partial state, not silence.

**Business Pulse must NOT assume a stable Command Center priority order.** `HOME_PRIORITY_ORDER`
(`packages/core/src/home.ts:78`) was reordered on 2026-08-23 (85daa4b) — connection-failure moved from
priority 0 to sixth — and **five files re-declare that order as a literal without importing it**. Do
not hardcode, rank, or reference a positional priority in either body or in any fixture assertion;
refer to signals by their code, never by their place in a list.

**Campaign Plan is a leaf agent (owner decision B).** Rewrite its brief so it **produces a plan
document** — objectives, audience, channels, message angles, sequencing, measurement, owner-decision
points — and remove any language that has it commission research, draft content, generate media or
dispatch a specialist. Passing a tool allow-list turns dispatch off in code; a body that promises
orchestration is a promise the runtime cannot keep. Composition stays with the Executive Agent.</action>
  <verify><automated>cd packages/contracts && npx vitest run src/skills/skillBodies.test.ts</automated>
<automated>node scripts/verify-knowledge-work-provenance.mjs --check-source</automated></verify>
  <done>Both immutable candidate bodies describe only `existing` operations from 27-02's matrix, name every `missing` input out loud, and each ships as an .md/.ts pair the sync test covers.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add outcome and adversarial fixtures</name>
  <files>packages/backend/scripts/workflow-pack-fixtures/business-pulse.json, packages/backend/scripts/workflow-pack-fixtures/campaign-plan.json</files>
  <action>Add positive, sparse/partial, unsupported-claim, citation and injected-source cases against the fixture schema 27-02 Task 3 created. Assert structured outcome, source coverage, artifacts/plans and exact absence of forbidden tools; never compare prose.

**Fixtures live beside the runner**, at `packages/backend/scripts/workflow-pack-fixtures/` — repo-root
`scripts/` is not a pnpm workspace and nothing there is reachable by `turbo run test`. There is no
`evals/workflow-packs/` directory.

**Cap each pack at ~5 fixtures, and no more.** 27-08 inherits this bill: `run-eval-golden.mjs:88` sets
`COST_CAP_USD = 2.0`, evidence only writes on an all-green unfiltered run, and the realistic spend
across attempts is $10-25. Every fixture you add here is paid for six times over there.

Required adversarial coverage for these two packs specifically:
  - **Business Pulse**: a case where the Phase 26 summaries / content shelf / CRM are unreadable and
    the pack must SAY SO — fail the case if the output contains a revenue, pipeline or performance
    figure, or narrates a trend it had no source for.
  - **Campaign Plan**: a case that fails if the trace shows a dispatch attempt, or if the output
    claims research was run, content was drafted, media was generated, or a campaign was launched.
    The pass condition is a plan document, not an executed campaign.
  - Neither pack's fixtures may assert a Command Center priority *position* (see Task 1).</action>
  <verify><automated>cd packages/backend && node scripts/run-workflow-pack-evals.mjs --packs business-pulse,campaign-plan --fixtures-only</automated></verify>
  <done>Fixtures fail fabricated data, silent source gaps, uncited claims and execution claims, and each pack carries ~5 cases.</done>
</task>
</tasks>
<verification>- `cd packages/contracts && npx vitest run src/skills/skillBodies.test.ts`
- `node scripts/verify-knowledge-work-provenance.mjs --check-source` (upstream snapshot only — the
  adapted-body hash check lands in 27-08, so this command alone does **not** prove Task 1 was done)
- `cd packages/backend && node scripts/run-workflow-pack-evals.mjs --packs business-pulse,campaign-plan --fixtures-only`
- Playbooks updated in this same commit with their `Last verified` line bumped:
  `docs/playbooks/workflow-packs.md` (the pack bodies and their fixtures) and
  `docs/playbooks/skill-registry.md` (two new registry bodies under `packages/contracts/skills/`).

Do **not** use `pnpm --filter <pkg> test -- <name>` anywhere in this plan: the `--` is swallowed and
the whole suite runs (measured: `pnpm --filter @pikar/core test -- zzznonexistent` ran 41 files /
1123 tests green). Do **not** use `node scripts/check-playbooks.mjs` as a gate: it reads stdin at
module top (blocks forever if run bare) and every terminal path is `process.exit(0)`.</verification>
<success_criteria>Two candidate workflows produce useful governed outcomes using only existing native capabilities, name every input they could not read, and Campaign Plan's output is a plan rather than an orchestration.</success_criteria>
<output>Create `27-04-SUMMARY.md`.</output>
