---
phase: 27-curated-knowledge-work-pack-pilot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/workflowPacks.ts
  - packages/core/src/workflowPacks.test.ts
  - packages/core/src/index.ts
  - packages/core/src/tenantData.ts
  - packages/core/src/tenantData.test.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/scripts/run-workflow-pack-evals.mjs
  - docs/playbooks/workflow-packs.md
  - docs/playbooks/skill-registry.md
  - docs/playbooks/audit-dead-letter.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [PACK-02, PACK-03]
must_haves:
  truths:
    - "Six closed workflow ids resolve through the existing Executive Agent and one agent loop."
    - "Every described operation is existing, missing, or forbidden and exact tools come only from code."
    - "A first pack version is candidate-only and ordinary discovery cannot see it."
    - "Packs are LEAF agents: an allow-list structurally disables specialist dispatch, and the plan text says so."
  artifacts:
    - path: packages/core/src/workflowPacks.ts
      provides: Total workflow definitions, operation matrix, preflight and exact tool grants
    - path: packages/backend/convex/skills.ts
      provides: Candidate-only first publication and evidence-gated activation seam. SCOPE IS DEV-ONLY
        for this phase - eval evidence is a column on the skills ROW with an exact-version pin, so a
        DEV eval can never certify a PROD candidate and version numbers differ per deployment. 27-08
        and 27-09 both state this; it is restated here because this plan authors the gate itself.
  key_links:
    - from: packages/core/src/workflowPacks.ts
      to: packages/backend/convex/llm.ts
      via: exact filtered toolNames
      pattern: "toolsForWorkflowPack|toolNames"
---

<objective>Freeze the native pack contract, capability boundary, candidate lifecycle, and operational ownership once.</objective>
<context>@.planning/phases/27-curated-knowledge-work-pack-pilot/27-CONTEXT.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-RESEARCH.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-VALIDATION.md
@.planning/phases/27-curated-knowledge-work-pack-pilot/27-READINESS.md</context>

<owner_decisions>
Settled 2026-08-23 by the readiness audit. Do not re-litigate; build to these.

- **All six packs stay in the pilot.** A capability the agent cannot reach is classified `missing`
  and carried by the phase's honest-partial-state contract. Do **NOT** add new read tools to make a
  pack look complete — that is scope no plan owns and it inflates the eval corpus.
- **Packs are LEAF AGENTS.** Verified at `llm.ts:4337` (`grantDispatch: toolNames === undefined`) and
  `llm.ts:4342` (`grantSkillAuthoring: toolNames === undefined`): passing a tool allow-list
  structurally disables specialist dispatch and skill authoring. The Executive Agent keeps
  composition. Campaign Plan **produces a plan**; it does not orchestrate research/content/media.
</owner_decisions>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the total native pack and operation contracts</name>
  <files>packages/core/src/workflowPacks.test.ts, packages/core/src/workflowPacks.ts, packages/core/src/index.ts</files>
  <action>Create the six-id registry, output/preflight contracts, and a total existing/missing/forbidden operation matrix. Derive exact runtime tool names only from code-owned existing operations. Test totality, unknown-id refusal, structural absence of writes, and that skill text cannot add a tool.

This matrix is the **vocabulary 27-04/05/06 author their bodies against** — `existing` / `missing` /
`forbidden` are the only three words, and a body may only claim an `existing` operation. Say this in
the module doc comment so a downstream lane cannot invent a fourth state.

Classify as **`missing`** (owner decision A — do NOT add read tools to close these):
  - Phase 26 summaries — `reportsBusiness.ts` business/operations/sentMail are `tenantQuery`, not agent tools.
  - The content shelf — `content.ts` is `tenantQuery`-only by construction.
  - CRM facts — the only contact tools are `resolveContacts` (labels, never addresses) and `stageCrmWrite`.
  - Brand guidance — there is **no tenant brand store**; `brandVoice` is a per-plan string at `schema.ts:757`.
That starves Business Pulse, Brand Review and Campaign Plan of some of their nominal inputs. That is
the intended, recorded outcome.

Encode the leaf-agent fact in code, not just prose: a pack that carries a `toolNames` allow-list can
never dispatch a specialist (`llm.ts:4337/4342`), so `dispatch`-shaped operations are `forbidden` for
every pack, and Campaign Plan's contract output is a **plan document**, not an orchestration.</action>
  <verify><automated>cd packages/core && npx vitest run src/workflowPacks.test.ts</automated></verify>
  <done>Every pack has a complete, exact, mutation-resistant capability definition, and the three starved packs are explicitly `missing` rather than silently optimistic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add candidate-only publication, the pack event table, and pack evidence fields</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts, packages/core/src/tenantData.ts, packages/core/src/tenantData.test.ts, docs/playbooks/workflow-packs.md, docs/playbooks/skill-registry.md, docs/playbooks/audit-dead-letter.md, docs/playbooks/watch.json</files>
  <action>Add the minimal provenance/browser evidence storage and an idempotent first-publication path that always creates a candidate. Strengthen the existing activation choke point for pack candidates while preserving archived rollback exemption. Register one playbook owner. Do not add a plugin loader, MCP client, router, grant UI, or second agent loop.

**THE CENTRAL DEFECT THIS TASK EXISTS TO CLOSE — read before writing a line.** Both existing branches
are hostile to a dark pilot:
  - `seedSkills` (`skills.ts:639`) inserts `version: 1, status: "active"` when `rows.length === 0`
    **regardless of gating**, and `packages/backend/package.json`'s `dev` script runs `seedSkills` on
    every dev boot. Six pack names added to `SEEDS` would go **live, at v1, un-evaluated, on the next
    boot** — the exact opposite of this phase.
  - `insertCandidate` (`skills.ts:716`) throws `NOT_GATED` unless the name is gated, and then throws
    `NO_ACTIVE_SKILL` at `skills.ts:730` when `rows.length === 0`. It cannot publish a first-ever
    candidate either.

**Required shape:** keep the six pack bodies **OUT of `SEEDS` entirely**, and give the packs their own
first-publication mutation that **always mints `status: "candidate"`** (idempotent: re-running never
mints candidate N+1). **Do NOT change the general `rows.length === 0` branch** — `skill.ts:266-268`
and `skills.ts:557` lean on it by name, and CLAUDE.md §7 requires a fresh clone to boot. Test both
directions: a dev boot leaves zero pack rows active, and re-running first-publication is a no-op.

**Skill bodies are two files plus a test entry.** Canonical bodies are `.md` under
`packages/contracts/skills/` (30 files today); `packages/contracts/src/skills/` holds **auto-derived
`.ts`** constants, and `skillBodies.test.ts` asserts the pair is byte-identical. This task authors no
body (27-04/05/06 do), but the first-publication path must read the derived constant the way `SEEDS`
does — never a hardcoded prompt (CLAUDE.md §5) — and any provenance hash/manifest field must pin the
**`.md`**.

**Also land the `workflowPackEvents` `defineTable` here** (this plan already owns `schema.ts` and
`watch.json`, so a later plan re-opening them buys nothing). Classify it `tenant_owned` in
`packages/core/src/tenantData.ts` — an unclassified table turns `tenantData.test.ts` and
`isolation.test.ts` red — with a `ponytail:` comment naming the **open erasure question** (whether
tenant erasure must also remove pack events), recorded for the owner in the `admission_plane` style,
not resolved here. Do **NOT** re-emit cost or latency into it: `spendEvents` owns cost and
`telemetry.durationMs` / `agentSteps` own latency, and a second number that can disagree with the
billing plane is worse than none. Keep it to pack run lifecycle, plan decisions, missing-source
surprise, and recommendation impressions.

**Playbooks (CLAUDE.md §9, enforced by a Stop hook).** Create `docs/playbooks/workflow-packs.md` from
`docs/playbooks/TEMPLATE.md`; update `docs/playbooks/skill-registry.md` (skills registry / candidate
lifecycle changed) and `docs/playbooks/audit-dead-letter.md` (`tenantData.ts` classification changed),
and bump the `Last verified` line on **each** playbook you touch. In `docs/playbooks/watch.json`
register under `workflow-packs.md` the prefixes `packages/core/src/workflowPack`,
`packages/backend/convex/workflowPack`, `packages/backend/scripts/run-workflow-pack-evals.mjs` and
`packages/backend/scripts/workflow-pack-fixtures/` — new code under `packages/*` that no playbook
covers is blocked by the hook.</action>
  <verify><automated>cd packages/backend && npx vitest run convex/skills.test.ts</automated>
<automated>cd packages/core && npx vitest run src/tenantData.test.ts</automated></verify>
  <done>Fresh deployments cannot bootstrap-activate a pack, activation requires exact-version evidence, and the pack event table is classified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold fixture validation before workflow lanes</name>
  <files>packages/backend/scripts/run-workflow-pack-evals.mjs</files>
  <action>Create the fixture schema/parser and fixtures-only mode used by Plans 04-06. Require stable operation ids, structured outcomes, source-state expectations and exact allowed/forbidden tool traces. Candidate execution and evidence recording are added only after publication in Plan 08.

**Location is load-bearing.** The runner lives at `packages/backend/scripts/` beside
`run-eval-golden.mjs` — which already owns the convex invocation, the cost cap and the evidence
write — and its fixtures at `packages/backend/scripts/workflow-pack-fixtures/` (the directory
27-04/05/06 write into). Repo-root `scripts/` is **not** a pnpm workspace, has no vitest and no root
vitest config, so nothing there can be tested or reached by `turbo run test`.

**There is no `.test.ts` for this file, deliberately.** The backend vitest config includes only
`convex/**/*.test.ts`, so a test beside the runner would never execute — a gate that cannot run is
worse than none. The runnable check is the `--self-test` mode itself: assert-based, in-file, exits
non-zero on failure (CLAUDE.md §8). Make `--self-test` actually fail on a malformed fixture — verify
it goes red before you call the task done.</action>
  <verify><automated>cd packages/backend && node scripts/run-workflow-pack-evals.mjs --fixtures-only --self-test</automated></verify>
  <done>Workflow lanes can validate fixture structure and operation ids without a deployed candidate, and a broken fixture turns the self-test red.</done>
</task>
</tasks>

<verification>- `cd packages/core && npx vitest run src/workflowPacks.test.ts`
- `cd packages/backend && npx vitest run convex/skills.test.ts`
- `cd packages/core && npx vitest run src/tenantData.test.ts`
- `cd packages/backend && node scripts/run-workflow-pack-evals.mjs --fixtures-only --self-test`
- Playbooks updated in this same commit with their `Last verified` line bumped:
  `docs/playbooks/workflow-packs.md` (new), `docs/playbooks/skill-registry.md`,
  `docs/playbooks/audit-dead-letter.md`, plus the four `watch.json` prefixes from Task 2.

Do **not** use `pnpm --filter <pkg> test -- <name>` anywhere in this plan: the `--` is swallowed and
the whole suite runs (measured: `pnpm --filter @pikar/core test -- zzznonexistent` ran 41 files /
1123 tests green). Do **not** use `node scripts/check-playbooks.mjs` as a gate: it reads stdin at
module top (blocks forever if run bare) and every terminal path is `process.exit(0)`.</verification>
<success_criteria>PACK-02/03 share one native, code-authorized runtime and a fail-closed candidate lifecycle: no pack row is ever `active` after a dev boot, and every pack operation is `existing`, `missing` or `forbidden` in code.</success_criteria>
<output>Create `27-02-SUMMARY.md`.</output>
