---
phase: 31-marketing-surface-and-funnel-v0
plan: "06"
type: execute
wave: 5
depends_on:
  - "31-05"
files_modified:
  - apps/web/e2e/marketing-uat.spec.ts
  - apps/web/app/(app)/dashboard/marketing/marketingView.test.ts
  - packages/backend/convex/funnels.test.ts
  - packages/backend/convex/contacts.test.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/marketing.md
autonomous: true
requirements:
  - MKTG-01
  - MKTG-02
  - MKTG-03
must_haves:
  truths:
    - "Automated evidence traces each roadmap success criterion across core, Convex, UI, and browser seams"
    - "Regression guards fail if event-level tracking, a public contact write, a publisher/send action, or a middleware exception is introduced"
    - "The browser scenario separates authenticated funnel creation/lead recording from unauthenticated GET execution"
    - "The full repository test/type/build gates pass before live activation is requested"
  artifacts:
    - path: "apps/web/e2e/marketing-uat.spec.ts"
      provides: "Authenticated Marketing plus unauthenticated funnel journey and responsive/accessibility checks"
      min_lines: 120
    - path: "docs/playbooks/marketing.md"
      provides: "Executable UAT, cleanup, and negative-space checklist"
      contains: "No funnelEvents, public contact write, or publishing action"
  key_links:
    - from: "apps/web/e2e/marketing-uat.spec.ts"
      to: "/dashboard/marketing"
      via: "direct authenticated dark-route navigation"
      pattern: "dashboard/marketing"
    - from: "apps/web/e2e/marketing-uat.spec.ts"
      to: "Convex /f/ route"
      via: "new unauthenticated browser/request context"
      pattern: "/f/"
    - from: "31-VALIDATION.md"
      to: "unit/integration/component/e2e suite"
      via: "per-task command map"
      pattern: "31-06"
---

<objective>
Close automated validation gaps and prepare reproducible live UAT without activating Marketing navigation.

Purpose: Turn roadmap promises and the threat model into executable evidence before the human activation gate.
Output: Focused Playwright scenario, strengthened negative-space tests, passing repository gates, and cleanup-ready runbook.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-03-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-04-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-05-SUMMARY.md
@apps/web/playwright.config.ts
@docs/playbooks/dashboard-pages.md
@docs/playbooks/marketing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the disposable-tenant Marketing and unauthenticated funnel browser journey</name>
  <files>apps/web/e2e/marketing-uat.spec.ts, docs/playbooks/dashboard-pages.md, docs/playbooks/marketing.md</files>
  <action>
Following existing authenticated Playwright fixtures and cleanup patterns, create a focused spec that can run against the configured environment without hard-coded credentials or tokens. In the authenticated context, visit the dark route directly; assert Gmail's honest state plus all five named social rows (Meta/Instagram, LinkedIn, TikTok, X, YouTube), both blockers on each, and no fake metrics; create or select a disposable Vault artifact; create a source-tagged funnel; copy the one-time URL(s); and record a uniquely named disposable lead. Open a fresh unauthenticated request/browser context for each approved `visit`, `claim`, and `download` stage. Prove each request increments only its named integer counter and returns `302` to the same stored bytes, then deactivate and confirm every stage fails with the minimal 404 and no count. Revisit Marketing to verify list/counters/status and verify the lead exists via the existing Pipeline/Contacts UI with approved provenance. Cover narrow and desktop layouts, keyboard focus, and section independence. Always clean up/deactivate disposable data in `finally`; redact tokens from attachments and traces. Permit environment-based skips with a precise reason for unavailable Clerk/storage prerequisites, but never turn a failed assertion into a skip.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test:e2e -- marketing-uat.spec.ts --list</automated>
    <manual>Review trace/screenshot configuration to confirm raw token text is masked or attachments disabled during the secret-disclosure step.</manual>
  </verify>
  <done>The scenario is collectable, reproducible, cleanup-safe, and covers authenticated management plus genuinely unauthenticated funnel execution.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the privacy, public-write, middleware, and publishing negative space</name>
  <files>apps/web/app/(app)/dashboard/marketing/marketingView.test.ts, packages/backend/convex/funnels.test.ts, packages/backend/convex/contacts.test.ts</files>
  <action>
Fill any remaining automated gaps in 31-VALIDATION.md with focused invariant cases: catalog completeness and missing-not-zero; no raw secret in list; exactly three aggregate integer counters; no `funnelEvents` or event-like fields/table; no funnel POST or public contact HTTP action; `apps/web/middleware.ts` unchanged/unaware of `/f/`; no redirect from caller input; no model/provider/send/publish handler in Marketing; capture delegates to the Phase 19 writer; suppressed leads remain outbound-blocked; and workspace CTA is navigation only. Prefer behavioral assertions; use small source-structure guards only for negative capabilities that cannot be exercised through an API. Do not pin irrelevant formatting or implementation details.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- funnels.test.ts contacts.test.ts cockpit.test.ts gmail.test.ts</automated>
    <automated>pnpm --filter @pikar/web test -- marketingView.test.ts</automated>
  </verify>
  <done>Every locked negative-space boundary fails a named regression test if violated.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete pre-activation quality gate and reconcile validation docs</name>
  <files>docs/playbooks/dashboard-pages.md, docs/playbooks/marketing.md</files>
  <action>
Run the phase-focused tests first, then the repository test/typecheck and production web build. Fix only Phase 31 regressions in the files owned by Plans 31-01 through 31-06; do not broaden scope to unrelated failures. Update the playbooks with exact commands, prerequisites, expected evidence, and cleanup. Update the 31-06 summary with any pre-existing failures separately from Phase 31 failures. Do not mark browser/UAT sign-off or enable navigation in this plan.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All Phase 31 automated suites and repository quality gates pass, or any unrelated pre-existing blocker is precisely evidenced without falsely activating the feature.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- marketing.test.ts`
- `pnpm --filter @pikar/backend test -- funnels.test.ts contacts.test.ts cockpit.test.ts gmail.test.ts`
- `pnpm --filter @pikar/web test -- marketingView.test.ts`
- `pnpm --filter @pikar/web test:e2e -- marketing-uat.spec.ts --list`
- `pnpm test`
- `pnpm typecheck`
- `pnpm --filter @pikar/web build`
- `node scripts/check-playbooks.mjs`
- Navigation remains disabled after every command.
</verification>

<success_criteria>
- Each requirement and roadmap success criterion has an automated seam plus a documented live check.
- Threat-model negative space is protected by regression tests.
- The disposable E2E can prove the public request is unauthenticated without leaking its token into artifacts.
- The application is production-build clean while Marketing remains dark.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-06-SUMMARY.md`.
</output>
