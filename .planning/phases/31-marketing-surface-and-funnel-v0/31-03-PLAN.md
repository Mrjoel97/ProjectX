---
phase: 31-marketing-surface-and-funnel-v0
plan: "03"
type: execute
wave: 3
depends_on:
  - "31-02"
files_modified:
  - packages/backend/convex/http.ts
  - packages/backend/convex/funnels.test.ts
  - docs/playbooks/marketing.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements:
  - MKTG-02
must_haves:
  truths:
    - "The funnel is served by a Convex httpAction as the first unauthenticated read, without widening Next/Clerk middleware"
    - "Only the approved GET path/stage grammar is accepted; unknown, invalid, and deactivated links fail closed without tenant disclosure"
    - "Every successful visit, claim, or download response is HTTP 302 whose Location comes only from trusted Convex storage resolution"
    - "The public route writes aggregate counters only and exposes no contact, publishing, or event-capture endpoint"
  artifacts:
    - path: "packages/backend/convex/http.ts"
      provides: "Narrow unauthenticated funnel GET route"
      contains: "httpAction"
    - path: "packages/backend/convex/funnels.test.ts"
      provides: "HTTP method/path/status/redirect/source regression matrix"
      contains: "302"
  key_links:
    - from: "packages/backend/convex/http.ts"
      to: "packages/backend/convex/funnels.ts"
      via: "runMutation/runAction of the internal resolver"
      pattern: "internal.funnels"
    - from: "packages/backend/convex/http.ts"
      to: "Convex storage"
      via: "trusted resolver result used as Location"
      pattern: "Location"
    - from: "apps/web/middleware.ts"
      to: "packages/backend/convex/http.ts"
      via: "No link: public traffic terminates directly at Convex"
      pattern: "must remain unchanged"
---

<objective>
Expose the approved funnel semantics as the narrow first unauthenticated Convex HTTP read.

Purpose: Deliver MKTG-02's shareable link and redirect behavior without creating a public Next.js route, middleware exception, contact write, or tracking surface.
Output: GET-only Convex `httpAction`, integration tests, and deployed-route operating notes.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-02-SUMMARY.md
@packages/backend/convex/http.ts
@packages/backend/convex/funnels.ts
@apps/web/middleware.ts
@docs/playbooks/cockpit.md
@docs/playbooks/marketing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the exact GET-only funnel route with fail-closed parsing and redirect behavior</name>
  <files>packages/backend/convex/http.ts, packages/backend/convex/funnels.test.ts</files>
  <action>
Write router-level failing cases, then register the Plan 31-00 path grammar in the existing Convex `httpRouter`. Parse the token and stage with strict length/character/allowlist bounds before invoking the internal resolver; parse `?s=` exactly per the frozen source contract and cap its length before normalization. Accept GET only. For each accepted `visit`, `claim`, or `download` stage, atomically increment only the matching counter and return `302` with a `Location` header targeting the same persisted stored bytes. The location must be the trusted storage URL returned by the internal function and must pass an explicit `https:`/expected-storage-origin check before use. Return the same minimal `404` without incrementing for malformed/unknown/deactivated/missing-storage cases and a minimal `405` for disallowed methods if the router can distinguish it. Do not echo token/source, set auth cookies, emit request metadata logs, add permissive CORS, or add any contact-write route. Preserve every existing webhook route and handler exactly.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- funnels.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Router tests prove exact grammar and counter effects, safe 302, uniform failures, query bounds, method handling, and regression safety for existing HTTP routes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the unauthenticated boundary remains isolated from web middleware and public writes</name>
  <files>packages/backend/convex/funnels.test.ts</files>
  <action>
Add negative-space assertions or a small source-level invariant test, following existing test conventions, that the funnel route is registered only in Convex HTTP, no funnel/contact path was added to Next routes or `apps/web/middleware.ts`, and the public handler calls only the internal aggregate resolver. Assert the schema remains free of funnel event tables and the router has no POST/PUT/PATCH/DELETE registration for the funnel prefix. This is a regression guard, not a new middleware abstraction.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- funnels.test.ts</automated>
    <manual>Review `git diff -- apps/web/middleware.ts`; it must be empty for Phase 31.</manual>
  </verify>
  <done>Tests and diff review make the “first unauthenticated read” and “link-only public route” boundaries explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Add live-route operations and cockpit cross-reference</name>
  <files>docs/playbooks/marketing.md, docs/playbooks/cockpit.md</files>
  <action>
Document the actual deployed Convex site URL construction, approved stage examples using redacted tokens, expected response matrix, safe curl/incognito verification, counter comparison, deactivation, and cleanup. Cross-link the cockpit playbook to Marketing for draft/plan actions while stating the cockpit has no social publisher or funnel metric rollup in Phase 31. Never include a real raw token in documentation or command history examples.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>An operator can safely verify or deactivate the public funnel and understands that Next middleware and outbound publishing are out of scope.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- funnels.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`
- `git diff -- apps/web/middleware.ts` is empty.
- Source inspection confirms no funnel `POST`, public contact mutation, raw-token log, or caller-provided redirect.
</verification>

<success_criteria>
- A caller with only an unguessable token can exercise precisely the approved unauthenticated GET semantics.
- Successful terminal requests increment the right integer and redirect with 302 to trusted storage.
- The web auth perimeter, existing HTTP integrations, and negative-space boundaries remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-03-SUMMARY.md`.
</output>
