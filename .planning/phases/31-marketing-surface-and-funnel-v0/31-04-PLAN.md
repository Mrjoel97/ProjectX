---
phase: 31-marketing-surface-and-funnel-v0
plan: "04"
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - packages/backend/convex/contacts.ts
  - packages/backend/convex/contacts.test.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/backend/convex/gmail.test.ts
  - docs/playbooks/contacts-crm.md
autonomous: true
requirements:
  - MKTG-03
must_haves:
  truths:
    - "Lead capture follows the exact actor/write-boundary and provenance decision recorded in Plan 31-00"
    - "Every Phase 31 lead is upserted into the existing Phase 19 contacts table; no second lead table or shadow list exists"
    - "Email normalization, consent evidence, and suppression posture use the Phase 19 invariants rather than marketing-specific duplicates"
    - "No unauthenticated contact write is introduced under the compatible Phase 31 contract"
    - "A Marketing-captured suppressed address is refused both when executePlan materializes recipients and when gmail.send performs its final delivery backstop"
  artifacts:
    - path: "packages/backend/convex/contacts.ts"
      provides: "Authenticated Marketing lead-recording mutation routed through Phase 19 contact upsert"
      exports: ["recordMarketingLead"]
    - path: "packages/backend/convex/contacts.test.ts"
      provides: "Single-store, tenant, provenance, consent, duplicate, and suppression tests"
      contains: "recordMarketingLead"
    - path: "packages/backend/convex/cockpit.test.ts"
      provides: "Approve-time executePlan refusal regression for a Marketing-captured suppressed address"
      contains: "executePlan"
    - path: "packages/backend/convex/gmail.test.ts"
      provides: "Send-time gmail.send refusal regression for the same captured suppressed-address setup"
      contains: "reason: suppressed"
  key_links:
    - from: "packages/backend/convex/contacts.ts"
      to: "upsertContactRow"
      via: "single normalized Phase 19 write path"
      pattern: "upsertContactRow"
    - from: "packages/backend/convex/contacts.ts"
      to: "packages/backend/convex/schema.ts"
      via: "existing contacts and suppressions tables only"
      pattern: "contacts|suppressions"
    - from: "packages/backend/convex/cockpit.test.ts"
      to: "api.cockpit.executePlan"
      via: "captured suppressed recipient is withheld before request materialization"
      pattern: "executePlan|withheld"
    - from: "packages/backend/convex/gmail.test.ts"
      to: "internal.gmail.send"
      via: "captured suppressed recipient is refused at the final delivery convergence point"
      pattern: "gmail.send|suppressed"
    - from: "apps/web/app/(app)/dashboard/marketing/MarketingView.tsx"
      to: "packages/backend/convex/contacts.ts"
      via: "authenticated lead recording mutation"
      pattern: "recordMarketingLead"
---

<objective>
Add the approved Marketing lead-recording adapter to the single Phase 19 contacts store.

Purpose: Satisfy MKTG-03 without a parallel lead database, ambiguous provenance, suppressed-contact outreach, or a new public write.
Output: Tested authenticated contacts mutation and updated consent/suppression runbooks.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-01-SUMMARY.md
@packages/backend/convex/contacts.ts
@packages/backend/convex/contacts.test.ts
@packages/backend/convex/schema.ts
@docs/playbooks/contacts-crm.md
@docs/playbooks/marketing.md
@Skills/lead-engine/SKILL.md
@packages/contracts/skills/lead-engine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the decision-approved authenticated Marketing lead adapter</name>
  <files>packages/backend/convex/contacts.ts, packages/backend/convex/contacts.test.ts, packages/backend/convex/cockpit.test.ts, packages/backend/convex/gmail.test.ts</files>
  <action>
Before editing, re-check 31-00: this plan may execute only for a compatible authenticated-operator write contract. If the decision selected a public visitor form, deferred capture, or an origin/consent value unsupported by the approved Phase 19 schema, stop and replan rather than inventing a route or enum. Add failing tests and implement a narrowly named authenticated mutation that validates bounded name/email/company/context fields, normalizes email through existing helpers, records the exact approved `origin` and `consentSource`/consent evidence, and calls the exported Phase 19 `upsertContactRow`. Enforce tenant scope and preserve existing duplicate behavior. Consult suppressions before returning any suggested next action; a suppressed address may remain in the system of record where Phase 19 permits it but must return `outboundAllowed: false` and a truthful reason.

Add one targeted cross-boundary regression setup to each real email convergence suite, not just a contacts helper test. In `cockpit.test.ts`, seed a suppression, capture that same address through `recordMarketingLead`, approve a real email plan, call `api.cockpit.executePlan`, and prove the address is named in `withheld`, never materialized as a request, and never enters fan-out. In `gmail.test.ts`, seed/capture the same suppressed-address shape, create the delivery request using the suite's production-shaped fixture, invoke `internal.gmail.send`, and prove `{ delivered: false, reason: "suppressed" }`, a blocked request, and zero provider POSTs. These regressions must fail if either convergence point consults the contact row instead of the suppressions table. Do not create a `leads` table, public action/httpAction, email send, sequence, or automatic campaign membership.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- contacts.test.ts cockpit.test.ts gmail.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Tests prove authentication, tenant isolation, normalized single-store upsert, exact provenance/consent, duplicate handling, and that the captured suppressed address is refused at both `executePlan` and `gmail.send` with no outbound/public side effect.</done>
</task>

<task type="auto">
  <name>Task 2: Document Marketing-to-Contacts provenance and outbound gating</name>
  <files>docs/playbooks/contacts-crm.md</files>
  <action>
Update the contacts/CRM playbook with the exact Plan 31-00 actor, origin, consent source/evidence, duplicate policy, and suppression outcome. Explain that recording a contact does not authorize outreach: every later outbound action still needs the existing consent/suppression checks and an explicit approved plan. Name the two pinned product-email convergence points, `executePlan` and `gmail.send`, and link their targeted tests. State that Phase 31 adds no public lead form under the compatible contract and no automatic email/social action. Link to the already-owned Marketing playbook without editing it in this Wave-2 plan, avoiding concurrent ownership with Plan 31-02.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Product copy, operators, and future agents have one unambiguous lead provenance and plan-gated outbound contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- contacts.test.ts cockpit.test.ts gmail.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`
- `rg -n "defineTable" packages/backend/convex/schema.ts` confirms no Phase 31 leads table.
- `rg -n "httpAction|send|publish" packages/backend/convex/contacts.ts` is reviewed; no new public/outbound path exists.
</verification>

<success_criteria>
- A signed-in operator can record a decision-compliant lead once and find it in the Phase 19 contact store.
- Provenance, consent, deduplication, and suppression behavior are explicit and tested.
- Capture does not imply or trigger outreach, publishing, or a new unauthenticated write.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-04-SUMMARY.md`.
</output>
