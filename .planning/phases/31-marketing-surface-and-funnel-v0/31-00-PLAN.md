---
phase: 31-marketing-surface-and-funnel-v0
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
autonomous: false
requirements:
  - MKTG-01
  - MKTG-02
  - MKTG-03
must_haves:
  truths:
    - "Application edits are blocked until an owner explicitly resolves all three semantic ambiguities"
    - "The approved semantics are checked for compatibility with the current MKTG-01/02/03 wording and Phase 31 threat boundary"
    - "Any incompatible choice stops execution and returns Phase 31 to requirements/research/planning instead of being silently implemented"
  artifacts:
    - path: ".planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md"
      provides: "Durable owner-approved semantic contract and compatibility verdict"
      contains: "Stage semantics, source persistence, lead-capture boundary, and proceed/replan verdict"
  key_links:
    - from: "31-00-SUMMARY.md"
      to: "31-01 through 31-07"
      via: "Every downstream plan reads and enforces the decision record"
      pattern: "PROCEED_COMPATIBLE|STOP_REPLAN"
---

<objective>
Resolve the three product/security semantics that research could not derive from the current requirements, before any application file is edited.

Purpose: Prevent the implementation from inventing funnel-stage meaning, attribution storage, or a public write boundary. The owner decides; the executor records and checks compatibility.
Output: `31-00-SUMMARY.md` containing exact decisions and either `PROCEED_COMPATIBLE` or `STOP_REPLAN`.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Freeze the Phase 31 semantic contract</name>
  <files>.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md</files>
  <action>Pause before every application edit, present the full decision context and options below, then record the owner's verbatim answer plus the normalized contract and compatibility verdict in the execution summary. Do not choose defaults, translate silence into approval, or continue downstream on `STOP_REPLAN`.</action>
  <decision>Choose exact semantics for funnel stages, source attribution persistence, and lead capture. No application edit may occur before all three answers and the compatibility verdict are recorded.</decision>
  <context>
Research identified three ambiguities that affect schema, public API, security, copy, and tests. The current requirements lock several surrounding boundaries: only aggregate integer counters, no per-event/visitor table, one unguessable public token, GET-only Convex `httpAction`, first unauthenticated read, Phase 19 as the single contacts store, no publishing, and outbound actions behind an explicit plan.

Decision A — stage/URL semantics. Approve the exact Phase 31 contract or require replanning: `GET /f/:token/:stage` where stage is `visit`, `claim`, or `download`; every accepted request atomically increments exactly its named raw-request counter (including bots/retries) and returns `302` to the same persisted, trusted Vault storage bytes. Stages do not assert unique people or require sequence. Unknown stages/tokens and missing/deactivated assets return the same minimal `404` without incrementing.

- `A1 THREE_STAGE_302_PATHS` — approve the exact grammar above: each of the three GET paths increments one named raw counter and returns `302` to the persisted storage bytes.
- `A3 REQUIREMENT_CHANGE` — the three counters cannot honestly represent the intended funnel; stop and amend MKTG-02 before implementation.

Decision B — source-attribution persistence. Select or fully define one contract compatible with `?s=&lt;source&gt;`, exactly three aggregate counters, no event rows, and bounded attacker-controlled storage.

- `B1 TOKEN_PER_SOURCE` — source is normalized at authenticated link creation and stored on the funnel/link row; each source link receives its own token and three counters. Public `?s=` is accepted only for reporting/redirect continuity and does not create new keys.
- `B2 BOUNDED_AGGREGATE` — retain source breakdown in a bounded aggregate structure. Owner must define allowed source keys, cap/overflow behavior, and acknowledge any requirement/schema amendment beyond the three counters.
- `B3 NO_PERSISTENCE` — parse/forward `?s=` but persist no source attribution. Owner must explicitly accept whether this satisfies MKTG-02; if not, stop and amend it.

Decision C — lead-capture boundary. Select one meaning of “captured lead” and record the exact actor and write boundary.

- `C1 AUTHENTICATED_OPERATOR` — a signed-in operator records a lead from the Marketing surface into the Phase 19 contacts store, with the approved origin/consent semantics and existing suppression posture. No public contact-write endpoint is added.
- `C2 PUBLIC_VISITOR_FORM` — a visitor submits contact data from the unauthenticated funnel. This conflicts with “only public route is a link/redirect” and requires requirements amendment plus a new abuse/rate-limit/consent threat plan; verdict must be `STOP_REPLAN`.
- `C3 DEFER_CAPTURE` — Phase 31 does not capture a lead. MKTG-03 cannot close, so verdict must be `STOP_REPLAN`.

Compatibility rule: `PROCEED_COMPATIBLE` is allowed only when the chosen contracts are completely specified and fit the locked requirements without opening an additional public write, event-level tracking, publishing, or Phase 32 metrics. Otherwise record `STOP_REPLAN`, the exact requirement/threat-model amendment needed, and do not execute Plans 31-01 through 31-07.
  </context>
  <options>
    <option id="compatible-contract">
      <name>Approve a complete compatible contract</name>
      <pros>Unblocks implementation while retaining the Phase 31 privacy, storage, and public-route boundaries.</pros>
      <cons>Requires explicit product choices now; research recommendations are not automatically accepted.</cons>
    </option>
    <option id="replan-contract">
      <name>Choose semantics that require amendment</name>
      <pros>Allows the intended product behavior to take priority over the current wording.</pros>
      <cons>Blocks downstream application work until requirements, research, validation, and plans are revised.</cons>
    </option>
  </options>
  <resume-signal>Reply with Decision A, Decision B, and Decision C (including every requested semantic detail), then explicitly state either PROCEED_COMPATIBLE or STOP_REPLAN. The executor must copy the answer verbatim into 31-00-SUMMARY.md and add a concise normalized contract table.</resume-signal>
  <verify>
    <manual>Confirm the decision record contains exact request/response/counter semantics, bounded source persistence, lead actor/write boundary, and one compatibility verdict. Reject incomplete shorthand.</manual>
  </verify>
  <done>All three decisions are explicit and durable. A compatible verdict unblocks Plan 31-01; an incompatible verdict leaves every downstream plan blocked and identifies the replan work.</done>
</task>

</tasks>

<verification>
- No file outside `.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md` was changed.
- The summary includes the exact strings `PROCEED_COMPATIBLE` or `STOP_REPLAN`, never an inferred equivalent.
- The validation checklist's four Wave-0 decision fields are all populated.
</verification>

<success_criteria>
- The owner, not the implementer, resolved all three ambiguities.
- The contract is sufficiently exact to derive schema, route, UI copy, and tests.
- Incompatible semantics cause a safe planning stop before application edits.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md` with the verbatim response, normalized decision table, compatibility analysis, and proceed/replan verdict.
</output>
