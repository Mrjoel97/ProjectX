---
phase: 31-marketing-surface-and-funnel-v0
plan: "05"
subsystem: web
requirements-addressed: [MKTG-01, MKTG-02, MKTG-03]
requirements-completed: []
---

# Phase31-05 — authenticated Marketing surface

Added the direct authenticated route and three independently recoverable sections. Gmail reads live status/configuration; five social channels truthfully name legal-entity and provider-review blockers. File links use the bounded native downloadable-artifact picker, including continuation after empty filtered pages. Tokens stay in ephemeral readonly copy controls; rows show source/status and full raw integer counters with bot/retry caveats. Creation/deactivation errors are explicit and deactivation requires confirmation. Source replacement/deletion and already-obtained-byte caveats are visible.

Lead capture uses the existing authenticated Contacts adapter, defaults consent absent, and presents exact suppression/consent/approval posture. A small static intent/channel handoff consumes query parameters once into an empty idle ChatPane composer, never overwrites an existing draft and never sends. No model, provider, browser, account or lead mutations ran during implementation.

Nine focused web tests passed, covering all six channels, exact/unknown counts, actual selection/create and secret panel clearing, filtered pagination, consent absence/suppression, confirmed deactivation, independent failure and closed prefill semantics. Web TypeScript passed. Parent owns full combined build, commit and live acceptance.

Approved deviations: sidebar entry remains absent until31-07 because the existing navigation does not implement a disabled/soon state; no generic navigation abstraction was added. Tests use the supported `.test.tsx` extension. Product copy states legal-entity/provider-review blockers without internal Phase32 jargon. The additional ChatPane/helper files are necessary for a real unsent prefill because no such seam existed. These checks do not close live desktop/mobile acceptance or activate navigation.

Final qualification: 37 tests passed across Marketing, marketingPrefill, existing cockpitAccess and pinnedPrompts suites. Final web typecheck, playbook checker and diff whitespace check passed. Biome formatting/check passed with 12 test-only non-null assertion warnings and no errors. No full build or live workflow was run in this lane; those remain the parent qualification/acceptance scope.
