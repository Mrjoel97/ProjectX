---
phase: 31-marketing-surface-and-funnel-v0
plan: "01"
type: execute
wave: 1
depends_on:
  - "31-00"
files_modified:
  - packages/core/src/marketing.ts
  - packages/core/src/marketing.test.ts
  - packages/core/src/index.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/funnels.test.ts
  - docs/playbooks/marketing.md
  - docs/playbooks/watch.json
autonomous: true
requirements:
  - MKTG-01
  - MKTG-02
  - MKTG-03
must_haves:
  truths:
    - "Execution stops without a PROCEED_COMPATIBLE decision record"
    - "Every displayed channel state is connected, connectable, or blocked with a concrete reason; unknown data is never rendered as zero"
    - "The funnel schema stores aggregate integer counters only and follows the approved source-attribution contract"
    - "The marketing playbook locks the public-route, contact-store, publishing, and Phase 32 negative-space boundaries"
  artifacts:
    - path: "packages/core/src/marketing.ts"
      provides: "Shared channel-state and funnel counter contracts plus the honest v0 channel catalog"
      exports: ["MARKETING_CHANNEL_CATALOG", "MarketingChannelState", "FunnelCounters"]
    - path: "packages/backend/convex/schema.ts"
      provides: "Tenant-owned funnel/link aggregate storage with hashed token and no event table"
      contains: "visits, claims, downloads"
    - path: "docs/playbooks/marketing.md"
      provides: "Implementation/operator contract and required validation matrix"
      contains: "Plan 31-00 decisions and forbidden-scope guardrails"
  key_links:
    - from: "packages/core/src/marketing.ts"
      to: "apps/web/app/(app)/dashboard/marketing/MarketingView.tsx"
      via: "shared catalog and state discriminants"
      pattern: "MARKETING_CHANNEL_CATALOG|MarketingChannelState"
    - from: "packages/backend/convex/schema.ts"
      to: "packages/backend/convex/funnels.ts"
      via: "funnels table and indexes"
      pattern: "funnels"
---

<objective>
Freeze Phase 31's shared contracts, minimal aggregate schema, and operational guardrails before backend or UI behavior is implemented.

Purpose: Give every later plan one decision-backed model for honest channel state, funnel counters/source, and locked negative space.
Output: Tested core marketing contract, Convex schema seam, and watched marketing playbook.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
@CLAUDE.md
@docs/decisions/015-marketing-milestone-pulled-pre-beta.md
@.planning/design/growth-surfaces-canvas-funnels-connections.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add decision-backed core marketing contracts and honest catalog tests</name>
  <files>packages/core/src/marketing.ts, packages/core/src/marketing.test.ts, packages/core/src/index.ts</files>
  <action>
First assert `31-00-SUMMARY.md` says `PROCEED_COMPATIBLE`; stop with no application edits otherwise. Write failing tests, then implement a small dependency-free core module that defines: the exhaustive `connected | connectable | blocked-with-reason` state discriminant; required reason metadata for blocked states and CTA metadata only for connectable Gmail; integer/non-negative funnel counter parsing and formatting that never converts missing values to zero; source/stage constants exactly matching Plan 31-00; and the total v0 catalog containing exactly Gmail, Meta/Instagram, LinkedIn, TikTok, X, and YouTube. Gmail is the only potentially connected/connectable channel and is blocked only when Google OAuth is not configured. Every one of the five social providers must be `blocked-with-reason` with copy that explicitly states both current blockers: the legal entity is not formed, and Phase 32 provider suitability/OAuth review has not started. Tests must table-drive all six catalog entries, including TikTok, and prove every social reason contains both facts without promising a date or rendering a disabled Connect control. Do not add vendor metrics, publishing capability, aspirational connection states, or synthetic zero counts. Export only the contracts downstream needs from the core barrel.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- marketing.test.ts</automated>
    <automated>pnpm --filter @pikar/core typecheck</automated>
  </verify>
  <done>Catalog/state exhaustiveness, missing-vs-zero behavior, integer guards, and approved source/stage semantics are covered by passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal tenant-owned funnel aggregate schema and schema guards</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/funnels.test.ts</files>
  <action>
Add the Phase 31 funnel/link table and indexes to the existing Convex schema without changing unrelated tables. Store tenant ownership, Vault artifact ownership reference, title/status/timestamps, a token hash (never the raw token), the three integer aggregate counters `visits`, `claims`, and `downloads`, and only the source representation explicitly approved in 31-00. Use named indexes needed for tenant list and token-hash lookup. Add structural tests that assert allowed fields/indexes and reject the privacy regressions called out by research: no `funnelEvents` table, visitor ID, IP, user agent, referrer history, per-click timestamp, or unbounded source-key/event map. Do not add an arbitrary destination URL; redirects must later derive from a tenant-owned Vault storage reference.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- funnels.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Schema compiles, exposes the required indexes and three counters, matches the approved attribution model, and contains no event-level tracking surface.</done>
</task>

<task type="auto">
  <name>Task 3: Establish the watched marketing runbook and locked-boundary matrix</name>
  <files>docs/playbooks/marketing.md, docs/playbooks/watch.json</files>
  <action>
Create the marketing playbook with: the normalized Plan 31-00 contract; the complete Gmail, Meta/Instagram, LinkedIn, TikTok, X, and YouTube catalog; both legal-entity and Phase 32 provider-review blockers on every social channel; funnel lifecycle and one-time token disclosure; exact public GET grammar; raw-counter caveat; `?s=` behavior; Phase 19 contact provenance/consent/suppression boundary; threat checklist; incident/deactivation procedure; and explicit exclusions for public contact writes (unless requirements were replanned), visitor/event logging, publishing, outbound send, conversion rates, campaign reporting, and Phase 32 metrics. Add the playbook to the existing watch registry using its established schema. Include a requirement-to-test matrix referencing 31-VALIDATION.md. Preserve existing watch entries and ordering conventions.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    <manual>Confirm playbook language matches the exact approved decisions rather than the research recommendation when they differ.</manual>
  </verify>
  <done>The marketing contract is operationally documented, watched for drift, and makes forbidden scope auditable.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- marketing.test.ts`
- `pnpm --filter @pikar/backend test -- funnels.test.ts`
- `pnpm --filter @pikar/core typecheck`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`
- Review `git diff -- packages/backend/convex/schema.ts` to confirm no unrelated table changed and no event table exists.
</verification>

<success_criteria>
- All later code can import one honest channel/counter contract.
- Schema embodies the Plan 31-00 decisions and no more data than MKTG-02 permits.
- Operators and reviewers have one watched source for security, consent, and non-goals.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-01-SUMMARY.md`.
</output>
