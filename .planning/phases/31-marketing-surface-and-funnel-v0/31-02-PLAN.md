---
phase: 31-marketing-surface-and-funnel-v0
plan: "02"
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - packages/backend/convex/funnels.ts
  - packages/backend/convex/funnels.test.ts
  - packages/backend/convex/_generated/api.d.ts
  - docs/playbooks/marketing.md
autonomous: true
requirements:
  - MKTG-02
must_haves:
  truths:
    - "Only an authenticated owner can create, list, or deactivate a funnel, and the referenced Vault artifact belongs to that owner"
    - "The raw cryptographic token is disclosed only by creation; storage and later reads expose only its hash or masked identifier"
    - "Every approved visit, claim, or download request atomically increments exactly its named raw counter and returns 302 to trusted persisted storage bytes"
    - "Counters remain non-negative safe integers under retries and concurrent requests"
  artifacts:
    - path: "packages/backend/convex/funnels.ts"
      provides: "Authenticated management API plus internal token resolution/counter mutation"
      exports: ["create", "list", "deactivate"]
    - path: "packages/backend/convex/funnels.test.ts"
      provides: "Ownership, token secrecy, attribution, lifecycle, and counter safety coverage"
      min_lines: 150
    - path: "packages/backend/convex/_generated/api.d.ts"
      provides: "Codegen-owned funnels module registration consumed as api.funnels and internal.funnels"
      contains: "funnels: typeof funnels"
  key_links:
    - from: "packages/backend/convex/funnels.ts"
      to: "packages/backend/convex/schema.ts"
      via: "tenant index and tokenHash index"
      pattern: "withIndex"
    - from: "packages/backend/convex/_generated/api.d.ts"
      to: "packages/backend/convex/funnels.ts"
      via: "Convex codegen module import/fullApi entry supporting public and internal visibility projections"
      pattern: "import type * as funnels|funnels: typeof funnels"
    - from: "packages/backend/convex/funnels.ts"
      to: "packages/backend/convex/vault.ts"
      via: "owner-scoped Vault document lookup and storage ID"
      pattern: "vaultDocId|storageId"
    - from: "packages/backend/convex/http.ts"
      to: "packages/backend/convex/funnels.ts"
      via: "internal resolver invoked by the public HTTP action"
      pattern: "resolve|increment"
---

<objective>
Implement the authenticated funnel management boundary and the atomic internal counter/redirect primitive.

Purpose: Keep secret creation and tenant ownership on authenticated functions while giving the later HTTP action one narrow internal operation for unauthenticated reads.
Output: Tested Convex funnel functions with one-time raw token disclosure and aggregate-only mutation.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-01-SUMMARY.md
@packages/backend/convex/schema.ts
@packages/backend/convex/vault.ts
@packages/backend/convex/auth.ts
@docs/playbooks/marketing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build authenticated owner-scoped funnel lifecycle with one-time token disclosure</name>
  <files>packages/backend/convex/funnels.ts, packages/backend/convex/funnels.test.ts, packages/backend/convex/_generated/api.d.ts</files>
  <action>
Extend the schema-first tests, then implement `create`, `list`, and `deactivate` using the repository's Clerk/Convex auth helpers and stable API/error conventions. `create` must verify the referenced Vault document belongs to the authenticated tenant and resolves to a stored artifact; generate exactly 32 random bytes (256 bits) with the platform cryptographic RNG; encode them as an unpadded 43-character base64url token; hash the domain-separated raw token with the existing `contentHash` helper; persist only the hash; initialize exactly three counters as integer zero; normalize title/source per the core/31-00 contract; and return the raw token and derived public URL(s) only in that mutation response. `list` must return counters/status/source but never raw token or full hash. `deactivate` must be idempotent and tenant-scoped. Reject cross-tenant, deleted/missing artifact, invalid source/stage configuration, and unauthenticated calls. Never log the token or include it in an error. Tests must pin 32-byte entropy, the 43-character base64url shape, and `contentHash` compatibility end to end.

As soon as the new `funnels.ts` module and its public exports exist, run the repository-owned command `pnpm --filter @pikar/backend codegen` against the configured Convex deployment; do not hand-edit generated declarations. Confirm `_generated/api.d.ts` gained the codegen import and `fullApi` module entry for `funnels`, and add compile-time witnesses in `funnels.test.ts` for `api.funnels.create`, `api.funnels.list`, and `api.funnels.deactivate`. Codegen failure is blocking: do not proceed to downstream typecheck with a stale generated API.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend codegen</automated>
    <automated>pnpm --filter @pikar/backend test -- funnels.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Lifecycle tests prove auth/ownership, token entropy/hash-only persistence, one-time disclosure, safe list DTO, correct initial integers, and idempotent deactivation; generated declarations expose all three management functions through `api.funnels`.</done>
</task>

<task type="auto">
  <name>Task 2: Build one atomic internal public-read resolver from the approved semantics</name>
  <files>packages/backend/convex/funnels.ts, packages/backend/convex/funnels.test.ts, packages/backend/convex/_generated/api.d.ts</files>
  <action>
Add a non-exported/publicly-unaddressable internal mutation (and an internal action only if storage URL resolution requires the action runtime) that accepts the already parsed token/stage/source contract from Plan 31-00. Hash the token, use the token-hash index, reject unknown/deactivated rows with the same minimal result, validate `visit | claim | download` before mutation, and atomically increment exactly the named raw counter. For every accepted stage, resolve the same redirect location from the persisted Vault/storage relationship so the HTTP layer can return `302` to stored bytes; never accept a destination from the request or follow user-controlled schemes. Enforce `Number.isSafeInteger`, non-negative current values, and fail closed before overflow or before any increment when storage is unavailable. Implement source behavior exactly as frozen in 31-00, including allowlist/cap/overflow rules if selected, without per-request records. Tests must cover each stage's one-counter increment plus redirect, repeated and concurrent requests, invalid stages, unknown/deactivated tokens, unsafe integer boundary, source normalization/overflow, missing storage objects with no count, and absence of event rows.

Re-run `pnpm --filter @pikar/backend codegen` after the internal resolver exports are final. Add compile-time witnesses for the exact downstream seams: `internal.funnels.resolveAndIncrement` (and any separately named internal storage resolver actually used by Plan 31-03) must typecheck, while management calls remain available at `api.funnels`. Confirm the generated module entry is present exactly once and generated output, `funnels.test.ts`, `http.ts`'s planned reference, and the UI's planned `api.funnels` references all agree on names. Do not hand-edit `_generated/api.d.ts` to mask a codegen or visibility mismatch.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend codegen</automated>
    <automated>pnpm --filter @pikar/backend test -- funnels.test.ts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>The internal primitive is atomic, safe-integer bounded, aggregate-only, decision-exact, and returns a trusted redirect result without exposing tenant data; generated API parity proves `api.funnels` and `internal.funnels` both expose exactly the downstream names.</done>
</task>

<task type="auto">
  <name>Task 3: Document lifecycle, token recovery posture, and counter semantics</name>
  <files>docs/playbooks/marketing.md</files>
  <action>
Update the playbook with actual function names, token disclosure/recovery behavior (create a replacement/deactivate if lost; never retrieve raw token), Vault ownership failure cases, counter overflow failure behavior, and the exact concurrency/source tests now protecting the implementation. Retain the warning that raw request counters are not unique-human analytics if that is the approved contract.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Operator documentation matches the implemented backend and does not promise token recovery or analytics the system cannot provide.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend codegen`
- `pnpm --filter @pikar/backend test -- funnels.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`
- `_generated/api.d.ts` contains exactly one generated `funnels` import/module-map entry, and compile-time witnesses resolve `api.funnels.create/list/deactivate` plus `internal.funnels.resolveAndIncrement`.
- Search test snapshots and logs for a generated raw token; none may persist beyond the create response assertion.
</verification>

<success_criteria>
- Authenticated tenant owners can safely manage links for their own Vault artifacts.
- A raw token is unguessable, non-recoverable from storage, and disclosed only once.
- The public-read primitive preserves exact integer aggregate semantics under concurrency without event-level data.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-02-SUMMARY.md`.
</output>
