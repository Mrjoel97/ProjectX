---
phase: 31-marketing-surface-and-funnel-v0
plan: "06"
subsystem: validation
requirements-addressed: [MKTG-01, MKTG-02, MKTG-03]
requirements-completed: []
---

# Phase31-06 — pre-activation acceptance preparation

Added a collectable three-scenario Playwright suite: desktop/mobile keyboard and honest channel state; an exact synthetic stored-file funnel journey through fresh anonymous request contexts; and an explicitly retained synthetic suppressed-contact capture/Pipeline journey. The public checks compare original SHA256 bytes, same destination, each independent counter, HEAD noncounting, fixed source attribution despite query tampering, and deactivated empty404 with unchanged counters. The unique owned link is deactivated in finally; original files are preserved.

Native Convex Auth uses browser-exported storageState without reading JWTs. No owner provisioning, authoring, paid ingestion, model/provider/send invocation or live execution occurred. Trace/screenshot/video/retries are disabled. Token HTTP errors are closed and assertions avoid raw URLs; the panel clears before request assertions and final page cleanup protects automatic error-context snapshots.

Approved cleanup deviation: Contacts has no native delete, so the exact tagged example.test contact/suppression fixture is retained and documented rather than claiming complete deletion. It is read by exact fixture value before mutation; missing prerequisites skip precisely, supplied broken fixtures fail. No unsuppress or tenant deletion is introduced. Existing funnel schema/router/middleware and contact suppression tests already cover backend negative-space boundaries; only the missing UI mutation-hook guard was added.

Validation in this lane: three Playwright tests collected; ten Marketing/prefill tests passed. Initial source guard had a jsdom file-URL mismatch, corrected to the existing project-relative filesystem pattern before the passing run. Web typecheck passed during spec preparation. Coordinating root/release lane owns the final full repository test/type/build results; those must be incorporated before qualifying31-06 as complete. Live UAT and navigation remain gated by31-07.

## Consolidated qualification

The full backend run recorded 4,398 passes and four outdated structural-inventory failures.
Commit `36e4c69` corrects the exact HTTP/module counts and operator-probe audit classification,
with positive boundary witnesses; all 57 tests in those three owning suites then passed. The
original failing full-run receipt is retained, not rewritten. Nine other non-web suites passed
2,616 tests, and all eleven non-web package typechecks passed.

The full web suite passed 1,014 tests with two existing skipped recurrence-enable cases; recurrence
remains deferred. The subsequently added UI negative-space assertion passed in the ten-test focused
Marketing/prefill run. Web typecheck and the production Next build passed; existing middleware
deprecation and local-render file-tracing warnings remain. All 21 registered free gates, their
registry self-test, and the three planning/operator-control tests passed. Native codegen and exact
evaluator pins were already qualified. Exact release `644408df4c2c630346c5909aac23f9a352ede287`
then passed CI `34692922592`, deployment `34693161766`, and the durable production probe at
`12:17:48Z`. Production deployment `6409489035` succeeded at `12:17:51Z`; the receipt is
`.tmp/qualification-2026-09-12/marketing-production-deployment-receipt.json`.
Deployed browser/HTTP evidence and owner navigation approval remain separate from these results.
