---
phase: 31-marketing-surface-and-funnel-v0
plan: "05"
type: execute
wave: 4
depends_on:
  - "31-03"
  - "31-04"
files_modified:
  - apps/web/app/(app)/dashboard/marketing/page.tsx
  - apps/web/app/(app)/dashboard/marketing/MarketingView.tsx
  - apps/web/app/(app)/dashboard/marketing/marketingView.test.ts
  - apps/web/app/(app)/layout.tsx
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/marketing.md
  - docs/playbooks/watch.json
autonomous: true
requirements:
  - MKTG-01
  - MKTG-02
  - MKTG-03
must_haves:
  truths:
    - "Marketing renders three independent honest sections: channel state, tracked links, and captured-lead recording"
    - "Gmail state comes from live configured/connected queries; Meta/Instagram, LinkedIn, TikTok, X, and YouTube each name both the legal-entity and Phase 32 provider-review blockers and never show fabricated zero metrics"
    - "A raw funnel token/link is shown only immediately after creation; later rows expose status, source, and three integer aggregates without reconstructing the secret"
    - "The page can frame work for the existing Executive Agent workspace but cannot send or publish; the Marketing nav remains disabled until Plan 31-07 evidence"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/marketing/page.tsx"
      provides: "Thin authenticated Marketing route wrapper"
    - path: "apps/web/app/(app)/dashboard/marketing/MarketingView.tsx"
      provides: "Channels, tracked links, captured leads, and workspace CTA surface"
      min_lines: 180
    - path: "apps/web/app/(app)/dashboard/marketing/marketingView.test.ts"
      provides: "Honest state, counter, one-time token, lead, and non-publishing component coverage"
      min_lines: 150
    - path: "apps/web/app/(app)/layout.tsx"
      provides: "Disabled Marketing navigation entry pending evidence"
      contains: "soon: true"
  key_links:
    - from: "MarketingView.tsx"
      to: "api.gmailAuth.gmailStatus/api.gmailAuth.gmailConnectUrl"
      via: "independent live Gmail state derivation"
      pattern: "gmailStatus|gmailConnectUrl"
    - from: "MarketingView.tsx"
      to: "api.funnels.create/list/deactivate"
      via: "authenticated tracked-link management"
      pattern: "funnels"
    - from: "MarketingView.tsx"
      to: "api.contacts.recordMarketingLead"
      via: "authenticated Phase 19 lead adapter"
      pattern: "recordMarketingLead"
    - from: "MarketingView.tsx"
      to: "/dashboard/workspace"
      via: "navigation/prefill only; no direct model/provider invocation"
      pattern: "dashboard/workspace"
---

<objective>
Build the authenticated Marketing page dark, with honest channel state, bounded funnel management, Phase 19 lead recording, and plan-gated agent navigation.

Purpose: Deliver the user-visible MKTG-01/02/03 surface without exposing an unvalidated nav route, duplicating stores, fabricating metrics, or adding publisher capability.
Output: Tested route/view, disabled nav item, and dashboard/marketing playbook registration.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-RESEARCH.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-VALIDATION.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-00-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-03-SUMMARY.md
@.planning/phases/31-marketing-surface-and-funnel-v0/31-04-SUMMARY.md
@apps/web/app/(app)/layout.tsx
@apps/web/app/(app)/dashboard/workspace/page.tsx
@packages/backend/convex/gmailAuth.ts
@packages/core/src/marketing.ts
@docs/playbooks/dashboard-pages.md
@docs/playbooks/marketing.md
@docs/design/BRAND.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the thin route and independently resilient Marketing sections</name>
  <files>apps/web/app/(app)/dashboard/marketing/page.tsx, apps/web/app/(app)/dashboard/marketing/MarketingView.tsx, apps/web/app/(app)/dashboard/marketing/marketingView.test.ts</files>
  <action>
Start with component/server-render tests using the existing Vitest conventions (`.test.ts`, not `.test.tsx`). Add a thin authenticated route wrapper and a client view with three independent sections so one query failure does not erase the others:

1. Channels maps the exhaustive core catalog: Gmail, Meta/Instagram, LinkedIn, TikTok, X, and YouTube. Derive Gmail as `connected` only from `gmailStatus.connected`; otherwise `connectable` only when `gmailConnectUrl.configured`, linking to the existing Gmail connection flow; when configuration is absent show the Google OAuth blocker. Render missing/loading/error honestly. Each of Meta/Instagram, LinkedIn, TikTok, X, and YouTube renders `blocked-with-reason` and explicitly names both blockers: the legal entity is not formed and Phase 32 provider suitability/OAuth review has not started. Do not render a disabled Connect control, followers, impressions, posts, engagement, or zeros.
2. Tracked links lists a bounded newest-first set from `api.funnels.list`, with title, approved source/stage URL labels, active/deactivated state, raw-count caveat, and the three integer counters. The create form must select an existing tenant-owned downloadable Vault artifact and bounded source/title inputs. Display raw URL(s) in an ephemeral creation-success panel with copy action and a “shown once” warning; clearing/navigation removes them. Later list rows must not offer secret recovery. Deactivate requires an explicit confirm.
3. Captured leads provides only the Plan 31-00-compatible authenticated record form, passes exact provenance/consent fields to `recordMarketingLead`, displays suppression/outbound posture from the result, then links to the existing Pipeline/Contacts surface. It may show an aggregate contact count already available from Contacts, not a duplicate lead table/list.

Use existing dashboard layout primitives, CSSProperties/token styling, accessibility labels, keyboard focus, and responsive stacking. Tests must table-drive all six channels (explicitly including TikTok), prove both legal-entity and Phase 32 provider-review reasons on every social row, and cover connected/connectable/blocked/error states, absence of fake zero/provider metrics/disabled social Connect controls, empty vs failed links, exact integer display, one-time URL panel, deactivate, lead validation/suppression result, and independence of the three sections.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- marketingView.test.ts</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>The dark route renders all three truthful sections, manages links/leads through authenticated APIs, and component tests prove partial-failure, secret, counter, and accessibility behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add the plan-gated Executive Agent handoff and non-publishing assertions</name>
  <files>apps/web/app/(app)/dashboard/marketing/MarketingView.tsx, apps/web/app/(app)/dashboard/marketing/marketingView.test.ts</files>
  <action>
Add one visible “Ask the Executive Agent” action that navigates to `/dashboard/workspace` with a bounded, non-sensitive channel/funnel context prefill using the established workspace URL/state mechanism. It must not call a model, `proposePlan`, send mutation, provider adapter, or new Marketing tool. Copy must say the agent will draft/propose a plan and that publishing/sending requires the existing explicit approval path; social publishing is unavailable in v0. Add tests that assert the navigation target and absence of send/publish controls or provider calls. Do not edit `cockpit-agent`, `content-drafter`, action type contracts, approval schemas, or Gmail send code.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- marketingView.test.ts</automated>
    <manual>Search the view imports and handlers: only Convex read/manage/capture calls and workspace navigation are reachable.</manual>
  </verify>
  <done>The page helps users formulate marketing work through the existing governed workspace while adding no outbound execution capability.</done>
</task>

<task type="auto">
  <name>Task 3: Register Marketing dark in nav and playbook ownership</name>
  <files>apps/web/app/(app)/layout.tsx, docs/playbooks/dashboard-pages.md, docs/playbooks/marketing.md, docs/playbooks/watch.json</files>
  <action>
Add Marketing to the existing dashboard navigation data as disabled/`soon: true` with no active href, matching current disabled-item rendering and accessibility. Do not expose `/dashboard/marketing` in nav yet; direct authenticated access is for evidence only. Register the new route/view/test paths under the marketing/dashboard playbooks without stealing ownership of shared `layout.tsx` conventions, and document the two-step activation: dark route now, href only in Plan 31-07 after browser evidence. Update screenshots/checklists in prose only if the playbook convention requires it; do not claim UAT has passed.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- marketingView.test.ts</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
    <manual>Inspect nav data: Marketing is visible as unavailable/soon and cannot navigate.</manual>
  </verify>
  <done>Marketing is discoverable but safely dark, and file/playbook ownership plus activation criteria are explicit.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web test -- marketingView.test.ts`
- `pnpm --filter @pikar/web typecheck`
- `pnpm --filter @pikar/web build`
- `node scripts/check-playbooks.mjs`
- Source search confirms no provider metric, model call, send, publish, or token-recovery implementation in the new route.
</verification>

<success_criteria>
- The Marketing page truthfully exposes Gmail/connect blockers, aggregate tracked links, and single-store lead recording.
- Empty, loading, error, missing, and real zero states remain distinct.
- Secret links are one-time outputs; list data is bounded and secret-free.
- Agent assistance is a workspace handoff and every outbound action remains plan-gated.
- Navigation is still disabled pending live evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-surface-and-funnel-v0/31-05-SUMMARY.md`.
</output>
