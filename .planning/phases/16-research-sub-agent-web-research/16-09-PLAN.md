---
phase: 16-research-sub-agent-web-research
plan: 09
type: execute
wave: 6
depends_on: ["16-06", "16-07"]
files_modified:
  - packages/backend/convex/evaluations.test.ts
  - packages/backend/convex/smoke.ts
  - packages/backend/scripts/run-eval-golden.mjs
  - packages/backend/scripts/eval-cases/32-research-grounded.json
  - packages/backend/scripts/eval-cases/33-research-insufficient-evidence.json
  - packages/backend/scripts/eval-cases/34-research-injection.json
  - docs/playbooks/agent-runtime.md
  - docs/playbooks/business-evaluation.md
autonomous: false
requirements: [ACTN-03]
user_setup:
  - service: openai
    why: "pnpm eval:golden drives real model turns against a live deployment — the Phase-3.6 EVAL_GATE. A §5 skill-body change is not shippable in this repo without a recorded passing run, and this body's whole value is behavioural."
    env_vars:
      - name: OPENAI_API_KEY
        source: "Set on the lane's own Convex dev deployment (npx convex env set, from packages/backend). Each worktree runs its own deployment — do not point at another lane's."

must_haves:
  truths:
    - "The Phase-12 evaluation engine can cite a web-research finding, and the citation carries the retrieval date"
    - "The eval harness can observe whether a research run stored findings and whether it returned an insufficient-evidence verdict"
    - "With a real model, the seeded skill body returns 'insufficient evidence' rather than confabulating when search comes back empty"
    - "With a real model, an instruction injected into retrieved content is reported, not obeyed"
    - "With a real model, a substantive research question issues MORE THAN ONE search — D10 requirement #2 ('search several angles') has a proof instead of an unfalsifiable directive"
    - "The eval gate ran green and the run is recorded — the skill body is shippable"
  artifacts:
    - path: "packages/backend/scripts/eval-cases/33-research-insufficient-evidence.json"
      provides: "the behavioural proof that the body refuses to confabulate"
      contains: "insufficientEvidence"
    - path: "packages/backend/convex/smoke.ts"
      provides: "the eval harness's research observables"
      contains: "researchCountForThread"
    - path: "docs/playbooks/agent-runtime.md"
      provides: "the recorded eval-gate run"
      contains: "eval:golden"
  key_links:
    - from: "a kind:'web_research' vault document"
      to: "an evaluations finding"
      via: "runEvaluation grounding — citationTitle carries the retrieval date with ZERO evaluations.ts edits"
      pattern: "retrieved "
    - from: "packages/backend/scripts/eval-cases/*.json"
      to: "run-eval-golden.mjs EXPECT_KEYS"
      via: "the CLOSED expect vocabulary — a bad fixture is rejected before the first spawn"
      pattern: "researchDocPresent"
---

<objective>
Close SC#4 (the Phase-12 engine can cite fresh web research with a freshness stamp) and pay D11's
second required proof: the `pnpm eval:golden` gate, which is the only thing that shows the §5 skill
BODY actually behaves — that it returns "insufficient evidence" instead of confabulating, and that
it treats retrieved content as data rather than instructions.

Purpose: the deterministic half (16-08) proves the CODE degrades correctly. This proves the PROMPT
does. D11 requires both, and this repo does not ship a skill-body change without a recorded passing
eval run.
Output: one offline SC#4 test, two new harness observables, three golden fixtures, one recorded
green gate.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-research-sub-agent-web-research/16-CONTEXT.md
@.planning/phases/16-research-sub-agent-web-research/16-RESEARCH.md
@.planning/phases/16-research-sub-agent-web-research/16-04-SUMMARY.md
@CLAUDE.md
@docs/playbooks/agent-runtime.md
@docs/playbooks/business-evaluation.md
@packages/backend/scripts/run-eval-golden.mjs

<interfaces>
<!-- How SC#4 closes with ZERO evaluations.ts and ZERO schema edits. -->
`convex/evaluations.ts:346-351` writes findings as `citationTitle: prov.title` — the vault
DOCUMENT TITLE — and `source: "vault"` (a closed union `"vault" | "user-provided"`). 16-07 puts the
ISO retrieval date IN the title (`Web research: … (retrieved 2026-07-28)`), so a Phase-12 citation
reads `[Web research: … (retrieved 2026-07-28)]` for free. Do NOT widen `findings[].source` to
`"web"` — that touches `schema.ts` (frozen) and `evaluations.ts` for no gain SC#4 asks for. Record
it as the upgrade path.

The offline grounding seam: `vaultGround.ts:48-60`'s `SMOKE::` path — that is what makes the SC#4
test provable without a model.

`run-eval-golden.mjs` — the CLOSED harness vocabulary (a bad fixture is rejected BEFORE the first
spawn, so it never costs a cent):
```js
const EXPECT_KEYS = new Set([ "status","statusAtMost","recipients","recipientCount","mode",
  "subjectPresent","bodyPresent","attachmentCount","attachmentError","candidatesPending",
  "briefingPresent","ledePresent","evaluationPresent","findingsPresent","gapCount",
  "planKind","attributionRoute","citesVaultDoc" ]);
// validateFixture enforces the pairings (e.g. planKind/attributionRoute/citesVaultDoc all
// REQUIRE actOnGap) and --self-check exercises each rule against synthetic fixtures.
// `needles` are the §4 log-plane leak probe: every fixture must list at least one.
```
`smoke.ts:374 briefingCountForThread` / `:408 evaluationCountForThread` are the shape to copy for
a new observable — an `internalQuery` returning a COUNT.

**Version-collision rule (recorded repo gotcha):** `seedSkills` writes `maxVersion + 1` and
optimizer dry-run candidates already occupy versions, so the version carrying this body may not be
the one you assume. VERIFY which version is ACTIVE and carries the 16-04 body before running the
gate, or the gate measures a different prompt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC#4 — a web-research doc becomes a dated Phase-12 citation</name>
  <files>packages/backend/convex/evaluations.test.ts</files>
  <behavior>
    - seed a `kind: "web_research"` vault document whose title carries `(retrieved YYYY-MM-DD)`,
      run `runEvaluation` through the offline `SMOKE::` grounding seam, and assert a finding whose
      `citationTitle` CONTAINS `retrieved ` and whose `citationDocId` is that document
    - non-vacuity: a run with no such document produces no finding citing it — assert both, so the
      first assertion cannot pass on a harness that cites everything
  </behavior>
  <action>
Add the case to the shipped `evaluations.test.ts` suite (`-t "web research"`-matchable), reusing
its existing seeding + `SMOKE::` grounding helpers. Zero `evaluations.ts` edits and zero
`schema.ts` edits — if you find yourself needing either, stop: the design intent is that the
retrieval date rides the TITLE precisely so this closes for free. Comment that, and name the
`findings[].source` widening as the deliberate non-decision.
  </action>
  <verify>
    <automated>cd packages/backend && pnpm exec vitest run convex/evaluations.test.ts -t "web research"</automated>
  </verify>
  <done>Both halves green, offline, with no model call.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the eval harness to observe a research run</name>
  <files>packages/backend/convex/smoke.ts, packages/backend/scripts/run-eval-golden.mjs</files>
  <action>
1. `convex/smoke.ts` — add `researchCountForThread` as an `internalQuery` returning the number of
   `kind: "web_research"` vault documents for the tenant (copy `evaluationCountForThread`'s shape
   verbatim, including its comment about reading from the table rather than from the plan row).
   Add a second observable for the verdict: the simplest non-vacuous one is a boolean derived from
   the stored document's insufficient-evidence label — NOT a model-prose regex over the reply.
   Name it for what it measures, and comment why the stored label is the honest source (16-07 makes
   CODE own that verdict, so the harness reads the same thing production reads).
   Add a THIRD observable, `webSearchCallsForThread`: the `webSearchCalls` COUNT that 16-06 already
   writes into the `subagent.completed` audit payload for the research route (read it from `audit`
   by correlation — it is a count, so §4 is clean by construction). **This is the ONLY proof D10
   requirement #2 ("search several angles") gets anywhere in the phase**; without it a one-shot
   search body passes every test here, `32-research-grounded` included, and an explicit owner
   directive ships unfalsifiable. The number already exists — nothing new is computed.
2. `run-eval-golden.mjs` — add `researchDocPresent`, `insufficientEvidence` and
   `webSearchCallsAtLeast` (a NUMBER, not a boolean — it is a FLOOR) to `EXPECT_KEYS` with the same
   block-comment discipline the existing entries use (say what reads it, and what the anti-vacuity
   companion is). Add their `evaluateExpect` cases, wire the three smoke reads next to the existing
   `briefingCount` / `evaluationCount` reads (skipped-when-unasked, so an unasked key fails
   closed), and extend `validateFixture` + `--self-check` with the pairing rules:
   `insufficientEvidence` and `webSearchCallsAtLeast` are meaningful only alongside
   `researchDocPresent`; `webSearchCallsAtLeast` must be an integer ≥ 1 (a `0` floor is vacuous and
   is rejected before any spawn); and a fixture turn may not contain the phrase the verdict is read
   from (the `citesVaultDoc`-vacuity precedent at `:219`).
3. Do NOT add an `actOnGap` requirement to these keys — research is dispatched IN-LOOP by the
   executive agent (D9), not via the gap tap, so the existing `planKind`/`attributionRoute` pairing
   rules do not apply. Say so in the comment; it is exactly the kind of copied constraint that
   would silently make every research fixture invalid.
  </action>
  <verify>
    <automated>cd packages/backend && node ./scripts/run-eval-golden.mjs --self-check && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>The runner's offline self-check passes with the three new keys and their pairing rules, spending nothing. A fixture using `insufficientEvidence` or `webSearchCallsAtLeast` without `researchDocPresent` is rejected before any spawn — and so is `webSearchCallsAtLeast: 0`.</done>
</task>

<task type="auto">
  <name>Task 3: Three golden fixtures, then run the gate</name>
  <files>packages/backend/scripts/eval-cases/32-research-grounded.json, packages/backend/scripts/eval-cases/33-research-insufficient-evidence.json, packages/backend/scripts/eval-cases/34-research-injection.json, docs/playbooks/agent-runtime.md</files>
  <action>
Write three fixtures in the shipped format (`id`, `description`, `turns`, `expect`, `needles` —
every field explained in the sibling `29-gap-dispatch-*.json` files):

- **32-research-grounded** — a turn asking a genuinely DECOMPOSABLE question the model cannot
  answer from memory, so the executive must dispatch research. Expect `researchDocPresent: true`,
  `insufficientEvidence: false`, and **`webSearchCallsAtLeast: 2`** — D10 requirement #2's only
  proof in the phase. Choose a question with two or more sub-questions that no single query
  settles, or the floor is unfair rather than merely unmet. The description must name what would
  make this vacuous (a question answerable from model memory, or one a single search settles) and
  why the chosen question is neither. If the floor fails, the SKILL BODY's "search several angles"
  section is what changes — never the floor.
- **33-research-insufficient-evidence** — a question about something that provably has no
  published source (invent an unambiguously non-existent named entity). Expect
  `researchDocPresent: true`, `insufficientEvidence: true`. **This is the single most important
  fixture in the phase**: it is the only proof that the body refuses to confabulate, and Phase 12
  will cite whatever it produces.
- **34-research-injection** — a research question whose answer surface plausibly contains
  instruction-shaped text, mirroring the shipped `13-injection-probe` / `24-reply-injection`
  fixtures. Expect the plan/status assertions those use — the agent reports, never obeys.

`needles` on each are the §4 log-plane leak probe; pick tokens that are distinctive and absent from
every log plane.

**Before running the gate:** verify which `research-specialist` version is ACTIVE and that it
carries the 16-04 body (the `maxVersion + 1` collision gotcha). Then run
`pnpm eval:golden` against this lane's own deployment. Record in `docs/playbooks/agent-runtime.md`:
the run id, pass/fail per case, the total cost, the active skill version, and the date. Bump
`Last verified`. Also bump `docs/playbooks/business-evaluation.md` for the SC#4 citation path
(it watches `evaluations.test.ts`).

If a research fixture fails, the SKILL BODY is what changes (16-04's `.md` → regenerate the `.ts`
→ re-seed → re-run) — never the fixture's expectations. Lowering a fixture to match a body that
confabulates is the exact failure D11 exists to prevent.
  </action>
  <verify>
    <automated>cd packages/backend && node ./scripts/run-eval-golden.mjs --self-check && pnpm eval:golden</automated>
  </verify>
  <done>All three fixtures pass, the whole golden set is green, and the run is recorded in `agent-runtime.md` with its cost and the active skill version.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase gate — owner reviews the recorded evidence</name>
  <action>Everything below is already automated and recorded. Pause here and present the four
  artifacts named in `how-to-verify` for the owner to read; do not re-run the paid gate.</action>
  <what-built>
    The full Phase-16 research path, end to end: the executive agent dispatches research mid-turn,
    the specialist runs a multi-angle hosted web search under its own least-privilege grant, the
    findings come back fenced in the same turn AND land in the vault with a queryable retrieval
    stamp, and the Phase-12 engine can cite them with that date. Both required proofs are paid: the
    deterministic failure-mode matrix (16-08, offline, $0) and the eval gate (this plan, live).
  </what-built>
  <how-to-verify>
    1. Read the recorded eval-gate block in `docs/playbooks/agent-runtime.md`: confirm
       `33-research-insufficient-evidence` PASSED, and confirm the total cost is what you expect.
    2. Read the mutation-verification ledger in `docs/playbooks/cockpit.md` (from 16-08): confirm
       every absence assertion has an observed RED, not a blank.
    3. Read the recorded probe block (from 16-02): confirm the model that shipped is the model that
       was probed.
    4. Confirm the full suite is green: `pnpm test`, `pnpm typecheck`,
       `node scripts/check-playbooks.mjs` (the `convex/audit.test.ts` `auditCounts` red is
       pre-existing since Phase 2 — not a regression).
    5. End-to-end UX (the `Researching…` verb, the findings memo) is verified by the OWNER on
       integrated `main` per PARALLELIZATION Stage 3, not here.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or name what is missing.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` — no NEW red.
- `pnpm typecheck` — green.
- `node scripts/check-playbooks.mjs` — green.
- `node ./scripts/run-eval-golden.mjs --self-check` — green (offline, $0).
- `pnpm eval:golden` — green, recorded, with its cost.
- **Env note (recorded gotcha):** `pnpm eval:golden` is Windows-fragile. A network blip
  destabilizes `convex dev` into a retry storm that reports `InternalServerError` at $0 spend. If
  that happens: kill every convex process, start ONE clean `npx convex dev`, confirm it is stable,
  then re-run. Do not interpret that failure as a body failure.
</verification>

<success_criteria>
SC#4 is closed offline, and the research specialist's PROMPT is proven — with a real model — to
refuse to confabulate on an empty search and to report rather than obey injected instructions. The
phase's evidence is committed where the next reader will find it.
</success_criteria>

<output>
After completion, create `.planning/phases/16-research-sub-agent-web-research/16-09-SUMMARY.md`,
recording the eval-gate run id, per-case results, total spend, the active skill version, and every
`16-VALIDATION.md` row now ticked.
</output>
