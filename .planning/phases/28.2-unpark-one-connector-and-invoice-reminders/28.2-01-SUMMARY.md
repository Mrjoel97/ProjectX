---
phase: 28.2-unpark-one-connector-and-invoice-reminders
plan: 01
status: complete (code); the lane pass is an owner runbook
completed: 2026-09-06
commits: [see the phase-close commit's two parents — feat(28.2-01a) carry-over, feat(28.2-01b) open invoices]
requirements-completed: [G22 code half]
requirements-pending: [REVN-02 — QuickBooks lane `passed` on production by owner live evidence; the 28-27 subset release]
---

# 28.2-01 — The evidence window is reachable, and an invoice can be picked

**Owner decisions (2026-09-06):** QuickBooks is the ONE lane; the owner has an empty or sandbox company
(an Intuit sandbox ships with sample invoices, so the reminder proof runs in `sandbox` while the empty
production company proves read + revoke); this session lands the other session's owner-connect change.

**What changed.**
1. **Carried over from the shared tree's uncommitted work** (applied verbatim onto main, it applied
   cleanly): `providerGates.connectableProviderGates` — ONE connect-start predicate (`startAllowedFor`)
   shared by the gate and the Connections surface. The gate had admitted an unproven lane for the owner
   while the panel filtered on `passed` alone, so the owner branch was reachable by no button and no lane
   could ever gather the evidence wave 7 judges. Now the OWNER sees admitted-but-unproven lanes as rows
   marked `unproven` ("Not yet verified: this connector has never completed a live read against the
   provider. Connecting it here is how that evidence gets collected."); tenants see exactly the passed
   lanes, as before. Two `noUncheckedIndexedAccess` type errors in its test fixed on landing.
2. **`RevenuePackPanel`:** the quickbooks/stripe invoice-reminder offers can list the tenant's OPEN
   invoices on request — the same gated read `stageInvoiceReminder` refetches from (`quickbooks.readEntity
   Invoice` / `stripeConnector.readEntity invoices`, 90 days): owed > 0, oldest due first, ten rows,
   exact-decimal amount + currency code, lateness in words, partial and unavailable reads say so. One
   "Draft a reminder" per row sends `reminderOpener(providerLabel, id)`, pinned by test to eval case 42's
   first turn. Nothing loads on render; nothing in the panel stages or sends; PayPal's offer gets no list.
   This closes the one product gap: a solopreneur does not know their QuickBooks invoice ids and nothing
   ever showed one.

**Not code — the owner runbook** (in `docs/playbooks/revenue-connectors.md`, eight steps): env ×3 (+
currency), gate row (`--seal-decision from-owner --apply`), connect as owner, live read + confirmed revoke,
seal `passed` with `partner-tier-and-poll-budget` cleared against a doc anchor, per-deployment eval evidence
(`CONVEX_URL` = prod; `--skill revenue-specialist@1`, `revenue-invoice-reminder@1`, `revenue-cash-flow@1`,
`revenue-payroll-confidence@1`) then `activateSkill` each, verify offers → pick an invoice → `proposed` →
Approve → sent, then 28-27's subset release. Phase 28 stays incomplete by design.

**Tests.** `RevenuePackPanel.test.tsx` 11/11 (openInvoiceRows table: paid dropped, due-date order with
unknown last, cap, partial sentence, unavailable passthrough; the opener equals the eval fixture's turn;
list only for quickbooks/stripe and only on request; rows + controls with no send/approve button; the four
states in words). `connectorConnections.test.ts` 32/32, `providerGates.test.ts`, `connectorCallbacks.test.ts`,
`connectorRows.test.ts` green.

**Measured:** repo typecheck 12/12; full backend suite in two shards 67 files / 2034 tests + 66 files /
1932 tests, both exit 0; contracts, core, extraction, pii, revenue, voice, billing, cost, vault all green;
web green except the two known jsdom-install artifacts of this worktree (`workflows/PinnedWorkflowButton`,
`workflows/WorkflowPackCustomizer.container` — green on CI's clean install); biome ci exit 0.

**Playbooks:** revenue-connectors.md (both halves + the runbook), cockpit.md (panel), onboarding.md
(Connections rows) bumped. ROADMAP: Phase 28.2 inserted after 28.1.

**Coordination:** the shared tree still holds the same six files dirty on `feat/28.1-billing-mapping`
(143 commits behind main). They are now redundant with main; that branch should be reset to main or
deleted rather than merged.
