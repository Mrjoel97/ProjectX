---
phase: 28.2-unpark-one-connector-and-invoice-reminders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/providerGates.ts
  - packages/backend/convex/connectorConnections.ts
  - packages/backend/convex/connectorConnections.test.ts
  - apps/web/app/(app)/dashboard/profile/ConnectionsPanel.tsx
  - apps/web/app/(app)/dashboard/profile/connectorRows.ts
  - apps/web/app/(app)/dashboard/profile/connectorRows.test.ts
  - apps/web/app/(app)/dashboard/workspace/RevenuePackPanel.tsx
  - apps/web/app/(app)/dashboard/workspace/RevenuePackPanel.test.tsx
  - docs/playbooks/revenue-connectors.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/onboarding.md
autonomous: true
requirements: [G22 (code half); REVN-02 stays pending until the owner's live seal]
must_haves:
  truths:
    - "The owner can start a consent for an admitted-but-unproven lane from the Connections panel, and the row says it is unproven; tenants still see only passed lanes"
    - "The gate and the surface read ONE predicate for connect-start, so the surface can never be stricter than the gate again"
    - "On a passed lane, the invoice-reminder offer can list the tenant's open invoices (owed > 0, oldest due first, capped, partial says so) and one control per row sends the exact opener eval case 42 drives"
    - "Nothing in the panel stages, sends or schedules; the cockpit turn does, behind Approve"
    - "The owner runbook to pass QuickBooks (env, gate row, connect, live read, revoke, seal, per-deployment eval evidence, activation, subset release) is written down in the playbook"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/RevenuePackPanel.test.tsx"
      provides: "openInvoiceRows table; opener pinned to the eval fixture; list only for quickbooks/stripe; four states in words"
    - path: "packages/backend/convex/connectorConnections.test.ts"
      provides: "owner sees unproven admitted lanes; tenant does not; both answers exercised"
  key_links:
    - from: "connectorConnections.connections"
      to: "providerGates.connectableProviderGates"
      via: "the one connect-start predicate (startAllowedFor) shared with connectStartAllowed"
    - from: "RevenuePackPanel Draft a reminder"
      to: "quickbooks.readEntity / stripeConnector.readEntity → reminderOpener → cockpit → stageInvoiceReminder"
      via: "the same gated read the tool refetches from; the same opener the eval drives"
---

<objective>
The code half of G22: make the evidence-gathering window reachable (the owner-connect change carried
over from the shared tree) and close the one product gap (no surface ever showed an invoice id). The
lane itself passes only by the owner's live evidence — that is the runbook, not code.
</objective>

<context>
@.planning/phases/28.2-unpark-one-connector-and-invoice-reminders/28.2-RESEARCH.md
@docs/playbooks/revenue-connectors.md (Release semantics; The two axes; Callback, status and UI operations)

Notes for the executor:
- Task 1 is a CARRY-OVER: the other session's uncommitted diff on the shared tree, applied verbatim
  onto main (it applied cleanly). Do not re-derive it.
- `quickbooks.readEntity` / `stripeConnector.readEntity` are already public tenant actions; the
  panel calls them on request, never on render (a read is a provider call and an audit row).
- `formatMoneyAmount` is exact-decimal by currency exponent; never format minor units with Intl.
</context>

<tasks>
1. Carry over the owner-connect change (`connectableProviderGates`, `unproven` rows, tests). Run its tests.
2. `RevenuePackPanel`: `openInvoiceRows` (pure), `reminderOpener` (pinned to eval case 42's turn), the
   on-request list on the quickbooks/stripe invoice-reminder offers, one "Draft a reminder" per row.
3. Tests; typecheck; CI lint LAST.
4. Playbooks: revenue-connectors.md (both halves + the owner runbook), cockpit.md (panel), onboarding.md
   (Connections panel rows). ROADMAP: insert Phase 28.2.
</tasks>
