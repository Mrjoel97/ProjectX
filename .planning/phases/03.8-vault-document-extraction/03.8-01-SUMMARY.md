---
phase: 03.8-vault-document-extraction
plan: 01
subsystem: vault
tags: [convex, unpdf, fflate, extraction, scheduler, worktree-lanes]

# Dependency graph
requires:
  - phase: 05-knowledge-vault-graphrag
    provides: vault ingest spine (vaultIngestText docId seam, vaultIngest workflow, markReady/markFailed lifecycle)
  - phase: 04-intake
    provides: extractVisual hosted-OCR + transcribeAudio patterns the Wave-2 lanes copy
provides:
  - extractionKindFor classifier + caps consts (VAULT_EXTRACT_CHAR_CAP/PAGE_CAP, MIN_CHARS_PER_PAGE, TRANSCRIBABLE_CONTAINER_MIME) on the @pikar/vault barrel
  - vaultDocuments schema growth — "extracting" status literal + extractionTruncated optional flag (additive, no migration)
  - vaultUpload auto-extract hook — schedules internal.vaultExtract.extractDoc / internal.vaultTranscribe.transcribeDoc by kind; TXT/MD/CSV path regression-guarded
  - internal.vault.ingestExtractedText (scheduler-safe seam) + markExtracting + getDocForExtraction
  - "use node" not_implemented stubs vaultExtract.ts / vaultTranscribe.ts (codegen-compilable lane files)
  - officeText.ts stub (subpath-only import — fflate stays out of the V8 bundle)
  - unpdf@1.6.2 + fflate@0.8.3 pinned exact; lockfile + both package.jsons FROZEN for the phase
  - watch.json six new exact convex paths; vault.md extraction-lifecycle + per-lane append-only subsections; PARALLELIZATION.md 3.8 lane table + extended singleton list
affects: [03.8 Wave-2 lanes 1-4, vault, phase-close verify]

# Tech tracking
tech-stack:
  added: [unpdf@1.6.2 (backend), fflate@0.8.3 (vault)]
  patterns:
    - "Wave-0 contract: all shared-file edits land sequentially on main; Wave-2 lanes own pairwise-disjoint files only"
    - "extraction seam: actions produce text and call internal.vault.ingestExtractedText — never write embeddings/graph"
    - "honest pill: row stays pending_extraction at insert; the action flips to extracting when work starts"

key-files:
  created:
    - packages/vault/src/extractKind.ts
    - packages/vault/src/extractKind.test.ts
    - packages/vault/src/officeText.ts
    - packages/backend/convex/vaultExtract.ts
    - packages/backend/convex/vaultTranscribe.ts
  modified:
    - packages/backend/convex/schema.ts
    - packages/backend/convex/vault.ts
    - packages/backend/convex/vault.test.ts
    - packages/vault/src/index.ts
    - docs/playbooks/watch.json
    - docs/playbooks/vault.md
    - .planning/PARALLELIZATION.md

key-decisions:
  - "Seam stores RAW extracted text (planner-confirmed): scanText at extraction time is the lanes' fail-closed gate + audit-counts source, not a persistence transform — consistent with TXT uploads; downstream re-scans pre-model"
  - "officeText stays off the @pikar/vault index barrel — subpath export @pikar/vault/officeText keeps fflate structurally out of the V8 Convex bundle"
  - "Classifier stays dumb: video/quicktime classifies transcribe; container REJECTION is Lane 4's TRANSCRIBABLE_CONTAINER_MIME check (honest failure, never silent-pending)"
  - "NO skills.ts edit (deliberate absence verified): attachment-extractor reused as-is, transcription takes no prompt"

patterns-established:
  - "Lane playbook appends: each Wave-2 lane writes ONLY inside its own vault.md subsection — Stop hook satisfied per-lane, no cross-lane merge conflicts"
  - "Scheduled-function asserts: filter ctx.db.system _scheduled_functions rows by /vaultExtract|vaultTranscribe/ (cockpit.test.ts pattern)"

requirements-completed: [EXTR-A]

# Metrics
duration: 13min
completed: 2026-07-18
---

# Phase 3.8 Plan 01: Wave-0 Extraction Contract Summary

**Every shared edit the four parallel lanes would fight over landed sequentially on `main`: the extractionKindFor classifier + caps, the scheduler-safe ingestExtractedText seam, the by-kind vaultUpload dispatch, codegen-compilable lane stubs, pinned unpdf/fflate, and the lane-ownership governance — schema.ts, vault.ts, watch.json, package.jsons, and the lockfile are now FROZEN for the phase.**

## Performance

- **Duration:** ~13 min
- **Started:** 2026-07-17T22:39:23Z
- **Completed:** 2026-07-17T22:52:00Z
- **Tasks:** 3
- **Files modified:** 13

## Accomplishments

- All ten Wave-0 contract items from RESEARCH landed in one commit series on `main`; a Wave-2 session can start from a fresh worktree, `pnpm install` + `npx convex dev` cleanly, and find its stub file waiting
- The identity wall (Pitfall 3) is closed: `internal.vault.ingestExtractedText` mirrors the public docId path (patch text/hash/size → `processing` → `workflow.start(ingestDoc)`) with a fail-closed tenant guard, callable from scheduler-invoked actions
- `vaultUpload` of a PDF schedules `extractDoc`, an mp4 schedules `transcribeDoc`, a TXT-with-text rides the unchanged workflow path (regression-guarded), and an unrecognized zip stays storage-only — all convex-test-proven via `_scheduled_functions` inspection
- Pairwise-disjoint lane ownership is committed to the repo: PARALLELIZATION.md 3.8 table, vault.md per-lane append-only subsections, watch.json exact-path coverage for all six future lane files

## Task Commits

Each task was committed atomically (TDD tasks carry RED + GREEN commits):

1. **Task 1: Deps + pure-TS contract package** — `1f291f2` (test, RED) + `299d562` (feat, GREEN: classifier + caps + officeText stub + unpdf/fflate pins + lockfile)
2. **Task 2: Convex contract — schema union, seam, upload hook, lane stubs** — `edbe358` (test, RED) + `1834e13` (feat, GREEN)
3. **Task 3: Governance — watch.json, vault.md, PARALLELIZATION.md** — `9033495` (docs)

## Files Created/Modified

- `packages/vault/src/extractKind.ts` — `extractionKindFor` (pdf/image/office/transcribe, octet-stream extension fallback) + the 4 caps consts; on the index barrel (dep-free, V8-safe)
- `packages/vault/src/officeText.ts` — `extractOfficeText` stub throwing `office_parse_not_implemented`; subpath-only (Lane 2 replaces body)
- `packages/backend/convex/schema.ts` — `extracting` status literal + `extractionTruncated` optional flag (additive)
- `packages/backend/convex/vault.ts` — upload dispatch hook + `markExtracting` + `ingestExtractedText` + `getDocForExtraction`
- `packages/backend/convex/vaultExtract.ts` / `vaultTranscribe.ts` — `"use node"` `not_implemented` stubs (Lanes 1/4 replace bodies)
- `packages/backend/convex/vault.test.ts` — 10 new tests: scheduling by kind, TXT regression guard, seam patch/flip/workflow, tenant guards, markExtracting, getDocForExtraction
- `docs/playbooks/watch.json` / `docs/playbooks/vault.md` / `.planning/PARALLELIZATION.md` — lane-ownership governance

## Decisions Made

- Seam stores RAW extracted text (the planner-confirmed RESEARCH Open-Q2 call) — preview/download honesty and TXT-upload consistency; extraction-time scanText is the gate + counts source in the lanes
- Deps installed with `pnpm add -E` so both pins are caret-free without touching `.npmrc`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `pnpm --filter @pikar/backend test -- vault.test` does not actually filter (the `--` passes through to the whole suite); per-file verification used `pnpm exec vitest run convex/vault.test.ts` instead (18/18 green). Full-suite baseline noise (documented pre-existing `audit.test.ts` red) unchanged.
- Backend `tsc -p . --noEmit` carries the documented pre-existing `.test.ts` noise; grep-filtered to the touched files → zero errors.
- Biome findings on schema.ts/vault.ts/vault.test.ts/index.ts are byte-identical to the pre-edit baseline (stash-diffed) — format/organizeImports assists only; the five new files are Biome-clean.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Wave 2 may start: 4 parallel worktree sessions (`lane-1/vault-extract`, `lane-2/office-parsers`, `lane-3/sweep-ui`, `lane-4/video-transcribe`) — each lane's ownership contract is readable from `.planning/PARALLELIZATION.md` + `docs/playbooks/vault.md`
- Frozen for the rest of the phase: `schema.ts`, `vault.ts`, `watch.json`, both package.jsons, `pnpm-lock.yaml`
- Deliberately ABSENT (verified): no `skills.ts` edit — attachment-extractor is reused as-is and transcription takes no prompt

---
*Phase: 03.8-vault-document-extraction*
*Completed: 2026-07-18*

## Self-Check: PASSED

All 6 claimed artifacts exist on disk; all 5 task commits (1f291f2, 299d562, edbe358, 1834e13, 9033495) exist in git; skills.ts untouched (deliberate absence verified).
