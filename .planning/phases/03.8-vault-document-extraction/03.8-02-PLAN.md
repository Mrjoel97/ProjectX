---
phase: 03.8-vault-document-extraction
plan: 02
type: execute
wave: 2
depends_on: ["03.8-01"]
files_modified:
  - packages/backend/convex/vaultExtract.ts
  - packages/backend/convex/vaultExtract.test.ts
autonomous: false
requirements: [EXTR-B, EXTR-D, EXTR-E, EXTR-F]
must_haves:
  truths:
    - "A text-layer PDF extracts for free (unpdf, costUsd 0, no model call) and becomes searchable via the seam"
    - "A scanned/no-text-layer PDF and any image go through the hosted gpt-4o-mini path (attachment-extractor skill, spend recorded)"
    - "A governed stop (kill switch / budget) marks the doc failed with the reason — never a throw, never spend"
    - "Extracted text over VAULT_EXTRACT_CHAR_CAP is truncated and the row carries extractionTruncated: true"
    - "scanText runs fail-closed on extracted output BEFORE any audit write; audit payloads are refs/counts only"
  artifacts:
    - path: "packages/backend/convex/vaultExtract.ts"
      provides: "extractDoc dispatcher: SMOKE + gate + pdf/image/office dispatch + scan + audit + seam"
      exports: ["extractDoc"]
      min_lines: 120
    - path: "packages/backend/convex/vaultExtract.test.ts"
      provides: "EXTR-B/D/E/F offline coverage (convex-test + pdf-lib fixtures)"
      min_lines: 100
  key_links:
    - from: "packages/backend/convex/vaultExtract.ts"
      to: "internal.vault.ingestExtractedText"
      via: "ctx.runMutation after scan gate + truncation"
      pattern: "ingestExtractedText"
    - from: "packages/backend/convex/vaultExtract.ts"
      to: "internal.guardrails.preCall / recordSpend"
      via: "governed gate before work; spend after hosted call"
      pattern: "guardrails\\.(preCall|recordSpend)"
    - from: "packages/backend/convex/vaultExtract.ts"
      to: "@pikar/vault/officeText"
      via: "office dispatch (stub throws until Lane 2 merges; catch → markFailed)"
      pattern: "extractOfficeText"
---

<objective>
Lane 1 — PDF + image extraction (the hosted path + the free text-layer path). Replace the Wave-0
`extractDoc` stub with the real dispatcher: SMOKE short-circuit, governance gate, PDF
text-layer-first via unpdf with hosted OCR fallback, image OCR via the verbatim `extractVisual`
shape, office delegation to `@pikar/vault/officeText`, redact-then-audit ordering, char-cap
truncation, and the internal seam call.

THIS PLAN RUNS AS AN INDEPENDENT CLAUDE CODE SESSION in its own git worktree
(`.worktrees/lane-1-vault-extract`, branch `lane-1/vault-extract`) with its OWN `npx convex dev`
deployment, per `.planning/PARALLELIZATION.md`. It touches ONLY the two files above — every
shared file was frozen by 03.8-01. Merge `main` into the lane daily; merge to `main` when done
and announce. `.planning/STATE.md`/`ROADMAP.md` bookkeeping and the `docs/playbooks/vault.md`
Lane-1 subsection follow the PARALLELIZATION.md append-only keep-both rule (not part of the
interference contract).

Purpose: PDFs and images stop rotting at pending_extraction.
Output: working extractDoc + offline test suite + a live smoke proving unpdf under deployed Node.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.8-vault-document-extraction/03.8-RESEARCH.md  (Patterns 1-2, Pitfalls 1/6/8, Governance Call Shapes table, Code Examples)
@.planning/PARALLELIZATION.md
@packages/backend/convex/intake.ts  (extractVisual lines ~83-106 — copy the shape VERBATIM; SMOKE sniff lines ~63/89)
@packages/backend/convex/vault.ts  (the Wave-0 internal seam: getDocForExtraction, markExtracting, markFailed, ingestExtractedText)
@packages/backend/convex/intake.test.ts  (rateLimiter component registration block, lines ~22-42 — copy for preCall tests)
@packages/backend/convex/vaultRedaction.test.ts  (needle-scan pattern for refs-only audit assertions)
@docs/playbooks/vault.md  (extraction lifecycle section — append Lane-1 notes in the Lane-1 subsection ONLY)

Project skills: packages/backend/.claude/skills (convex-actions, convex-function-syntax) apply.

<interfaces>
<!-- Wave-0 contract this lane codes against (frozen — do not edit these files): -->

From @pikar/vault (index barrel):
```typescript
extractionKindFor(mimeType: string, filename?: string): "pdf"|"image"|"office"|"transcribe"|null
VAULT_EXTRACT_CHAR_CAP = 400_000;  VAULT_EXTRACT_PAGE_CAP = 50;  MIN_CHARS_PER_PAGE = 25;
```
From @pikar/vault/officeText (subpath import — NOT the barrel):
```typescript
extractOfficeText(bytes: Uint8Array, mimeType: string): { text: string }  // throws on malformed input
```
From internal.vault (Wave 0):
```typescript
getDocForExtraction({ vaultDocId, tenantId }) → { storageId, mimeType, title, status }  // fail-closed tenant guard
markExtracting({ vaultDocId });  markFailed({ vaultDocId, reason });
ingestExtractedText({ docId, tenantId, text, truncated })  // patches + status "processing" + starts ingest workflow
```
Governance (verified shapes — RESEARCH table):
```typescript
ctx.runMutation(internal.guardrails.preCall, {}) → { ok: true } | { ok: false; reason: "kill_switch"|"daily_budget_exhausted" }
priceUsage("openai/gpt-4o-mini", usage) → Result; if ok → ctx.runMutation(internal.guardrails.recordSpend, { costUsd })
ctx.runQuery(internal.skills.getActiveSkill, { name: ATTACHMENT_EXTRACTOR_SKILL }) → { body, version, skillId }
ctx.runMutation(internal.audit.log, { tenantId, correlationId, eventType, actor: "system", payload })  // refs/ids/counts ONLY
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dispatcher spine — SMOKE, gate, lifecycle, scan-then-audit, seam</name>
  <files>packages/backend/convex/vaultExtract.ts, packages/backend/convex/vaultExtract.test.ts</files>
  <behavior>
    - SMOKE::extract::hello bytes → status walk pending_extraction→extracting→processing, doc.text "hello", NO model call, NO spend, ingest workflow started (EXTR-D)
    - kill switch ON → status "failed", failureReason "kill_switch", no throw, no audit-success row (EXTR-E)
    - scanText Err (PII_POISON-style path) → "failed" + reason "pii_scan_failed" + exactly ONE refs-only audit row BEFORE which nothing was written (EXTR-E; mirror vaultRedaction.test.ts needle scan: raw text absent from every audit/log write)
    - success audit "vault.extracted" carries { vaultDocId, kind, path, piiCounts, charCount, truncated } — counts only, needle-scanned (EXTR-E)
    - office mimeType → extractOfficeText called; its (current stub) throw → "failed" + "office_parse_failed" — proving Lane 2's merge needs zero Lane-1 changes
    - text over VAULT_EXTRACT_CHAR_CAP → truncated at the cap, ingestExtractedText called with truncated: true (EXTR-F unit)
    - any unexpected throw inside the body → markFailed with a refs-only reason (try/catch wrapper; a governed stop is a RETURN, never a throw)
  </behavior>
  <action>
    Replace the stub body of `extractDoc` with the RESEARCH Pattern-1 spine, in order:
    (1) `internal.guardrails.preCall` gate → on !ok: markFailed(reason) + return null;
    (2) markExtracting; (3) getDocForExtraction + `ctx.storage.get(storageId)` → bytes (NEVER
    bytes-as-args — 5 MiB cap); (4) decodeUtf8 prefix sniff for `SMOKE::extract::<text>`
    (intake.ts grammar) → short-circuit with <text>; (5) dispatch on
    extractionKindFor(mimeType, title): "office" → `extractOfficeText` (subpath import) in
    try/catch → markFailed("office_parse_failed"); "pdf"/"image" → Task 2's engines (for THIS
    task, land the pdf/image branches as small stubs the Task-2 commit fills — keep the file
    compiling); (6) scanText FAIL-CLOSED on the extracted output → Err: markFailed +
    ONE refs-only audit "vault.extraction_failed"; (7) truncate at VAULT_EXTRACT_CHAR_CAP,
    remember `truncated`; (8) refs/counts-only success audit; (9) `ingestExtractedText` with
    RAW (post-gate) text — planner-confirmed decision: raw into the content plane, scan is
    gate+counts only (downstream re-scans pre-model). Whole body in try/catch → markFailed.
    "use node", actions only, explicit `Promise<null>` return, NO llm.ts import (duplicate the
    1-line model resolver if needed).
    Tests: convex-test; register the rateLimiter component (copy intake.test.ts:22-42 block);
    seed docs via the real vaultUpload/insert path; sentinel bytes via storage.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- vaultExtract</automated>
  </verify>
  <done>Full offline spine green: sentinel walk, governed stop, scan-fail path, audit needle scans, office-stub catch, truncation flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF text-layer-first + hosted OCR fallback + image path</name>
  <files>packages/backend/convex/vaultExtract.ts, packages/backend/convex/vaultExtract.test.ts</files>
  <behavior>
    - pdf-lib fixture WITH drawn text → extracted via unpdf text layer, result records path "text_layer", zero spend, no skill load (EXTR-B)
    - pdf-lib fixture with pages but NO text (a "scan") → garbage heuristic trips (chars/page < MIN_CHARS_PER_PAGE) → hosted path chosen (assert the branch; the hosted call itself is exercised via the SMOKE seam / mocked usage per intake.test.ts precedent) (EXTR-B)
    - a >VAULT_EXTRACT_PAGE_CAP-page no-text fixture → pdf-lib copyPages slices to the first 50 pages before the hosted call
    - image mimeType → hosted path with the file content part carrying the image's real mediaType
  </behavior>
  <action>
    1. TOP of vaultExtract.ts, before any unpdf usage: the 3-line `Promise.withResolvers`
       polyfill (Pitfall 1 — Convex node actions default to Node 20; unpdf bundles pdf.js 5.x
       which requires it; local Node 24 masks the bug, ONLY the live smoke proves it deployed).
    2. PDF branch: `getDocumentProxy(bytes)` → `extractText(pdf, { mergePages: false })` →
       join pages; heuristic `joined.replace(/\s/g,"").length / max(totalPages,1) >= MIN_CHARS_PER_PAGE`
       → text-layer result (costUsd 0). Else hosted fallback: if totalPages >
       VAULT_EXTRACT_PAGE_CAP, slice with pdf-lib `copyPages` (installed; it CANNOT extract text
       — Pitfall 8, fixtures + slicing only), then send (possibly sliced) PDF bytes as a `file`
       content part — extractVisual shape verbatim: skill body from
       `internal.skills.getActiveSkill(ATTACHMENT_EXTRACTOR_SKILL)` as system, gpt-4o-mini,
       `AbortSignal.timeout(45_000)`, `maxRetries: 1`, then priceUsage → recordSpend.
    3. Image branch: same hosted call with the image bytes + real mimeType.
    4. Tests: build fixtures IN-TEST with pdf-lib (Code Examples block — drawText fixture and
       no-text "scan" fixture); no binary fixtures in the repo.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- vaultExtract && pnpm --filter @pikar/backend exec tsc -p . --noEmit</automated>
  </verify>
  <done>Text-layer PDFs extract free; scans + images route hosted with page cap + spend recording; polyfill in place; all offline tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live smoke on the lane's own deployment (non-negotiable — Pitfall 1)</name>
  <what-built>extractDoc: free text-layer PDF extraction + hosted OCR for scans/images, governed and audited, feeding the vault ingest seam.</what-built>
  <action>Automate the smoke before pausing: start `npx convex dev` in this worktree (own deployment), generate a real text-layer PDF with pdf-lib, push it through the vault upload path, and capture the resulting doc row + deployment logs. Then pause for the human image test + confirmation below.</action>
  <verify>Manual — human confirms both live paths per the steps below.</verify>
  <done>Text-layer PDF extracted on the DEPLOYED runtime with costUsd 0 and no Promise.withResolvers error; real image OCR'd hosted with spend recorded; audit rows refs/counts only.</done>
  <how-to-verify>
    Claude automates first, you confirm results:
    1. Claude runs `npx convex dev` in this worktree (own deployment), generates a real
       text-layer PDF with pdf-lib, uploads it through the vault upload path (script or UI),
       and shows the doc row reaching status processing/ready with text present —
       THIS is the only place `Promise.withResolvers` under deployed Node 20 can surface.
       Expected: no "Promise.withResolvers is not a function" in the deployment log; costUsd 0.
    2. You drag a real image (e.g. a screenshot with visible text) into the vault UI
       (`pnpm dev` in this worktree). Expected: pill walks pending_extraction → extracting →
       processing → ready without refresh; detail panel/search shows the OCR'd text; spend
       recorded (check guardrails daily spend or the audit row).
    3. Skim the audit rows for the two docs: refs/counts only, no extracted text.
  </how-to-verify>
  <resume-signal>Type "approved" (both paths verified live) or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- vaultExtract` green offline (no live backend running)
- Backend typecheck clean; `node scripts/check-playbooks.mjs` exit 0 (vault.md Lane-1 subsection touched in the same commits)
- Needle greps: no raw extracted text in any audit.log call site; no `import ... from "./llm"` in vaultExtract.ts
- Live smoke: text-layer PDF (deployed unpdf) + real image (hosted, spend) both verified
</verification>

<success_criteria>
- EXTR-B/D/E/F(unit) proven offline; Pitfall-1 disproven live on this lane's deployment
- Zero files outside vaultExtract.ts / vaultExtract.test.ts changed (playbook subsection + GSD bookkeeping excepted, keep-both on merge)
- After Lane 2 merges to main and is merged into this lane, office docs flow with NO Lane-1 code change
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-vault-document-extraction/03.8-02-SUMMARY.md`.
Merge the lane branch to `main` and announce.
</output>
