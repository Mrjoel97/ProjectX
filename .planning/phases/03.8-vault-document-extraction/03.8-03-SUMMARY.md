---
phase: 03.8-vault-document-extraction
plan: 03
subsystem: vault
tags: [fflate, ooxml, docx, xlsx, pptx, vitest, pure-ts]

# Dependency graph
requires:
  - phase: 03.8-vault-document-extraction (plan 01, Wave 0)
    provides: frozen extractOfficeText(bytes, mimeType) signature + officeText.ts stub, fflate@0.8.3 pinned, extractionKindFor OOXML mime routing, Lane-1 dispatcher catch → markFailed("office_parse_failed")
provides:
  - Real extractOfficeText — deterministic DOCX/XLSX/PPTX flatten (fflate unzipSync + attribute-tolerant regex XML text-walk, entity decode, no XML parser dep)
  - DOCX: word/document.xml, runs joined per </w:p> paragraph, newline-separated
  - XLSX: optional sharedStrings indirection (t="s" cells) + literal <v> cells, tab-joined rows, "Sheet N" headers, numeric filename sort
  - PPTX: ppt/slides/slide*.xml numeric sort (slide2 < slide10), "Slide N" headers, <a:t> runs newline-joined
  - Typed office_parse_failed errors on every malformed-input path (not-a-zip, missing part, unrecognized mime)
  - 15-test EXTR-C suite with in-test zipSync-built fixtures (no binary fixtures in repo) incl. per-format determinism assertions
affects: [03.8-06 integration merge, lane-1 vaultExtract office branch]

# Tech tracking
tech-stack:
  added: []
  patterns: ["one attribute-tolerant regex text-walk (<tag ...>...</tag>) shared across all three OOXML formats", "in-test fixture generation with the same dep the parser uses (zipSync/strToU8)", "subpath-only module — deliberately NOT on the @pikar/vault index barrel (V8-bundle hygiene)"]

key-files:
  created:
    - packages/vault/src/officeText.test.ts
  modified:
    - packages/vault/src/officeText.ts
    - docs/playbooks/vault.md

key-decisions:
  - "Regex flatten over an XML parser dep — the locked fidelity bar is deterministic FLATTEN (text runs only), so lossy-but-boring extraction is correct, not a shortcut"
  - "Rich-text <si> (multiple runs) indexes as multiple sharedStrings entries — ponytail ceiling documented in source; per-<si> grouping is the upgrade path"
  - "noUncheckedIndexedAccess compliance via ?? fallbacks + Object.entries flatMap, not non-null assertions"

patterns-established:
  - "office_parse_failed: <detail> stable error prefix — the Lane-1 dispatcher contract for markFailed"

requirements-completed: [EXTR-C]

# Metrics
duration: ~3.5h wall across two sessions (first session killed by session limit mid-PPTX-GREEN; resumed and finished)
completed: 2026-07-18
---

# Phase 3.8 Plan 03: Lane 2 — Office Parsers Summary

**Real extractOfficeText: deterministic DOCX/XLSX/PPTX flatten via one fflate unzipSync + one attribute-tolerant regex XML text-walk — pure TS, zero Convex edits, typed office_parse_failed throws for the Lane-1 dispatcher**

## Performance

- **Duration:** ~3.5h wall across two sessions (active work far less; session 1 killed mid-Task-3)
- **Started:** 2026-07-18T01:59:07+03:00 (first commit)
- **Completed:** 2026-07-18T05:19:06+03:00 (last code commit)
- **Tasks:** 3/3 (all TDD)
- **Files modified:** 2 code files + playbook subsection + bookkeeping

## Accomplishments

- Wave-0 `extractOfficeText` stub replaced with the real flatten for all three OOXML formats behind the FROZEN signature — Lane 1's dispatcher picks it up on merge with zero change on either side
- Every Pitfall-7 gotcha covered by tests: attribute-bearing `<w:t xml:space="preserve">` runs, entity decode (5 named + numeric dec/hex), sharedStrings indirection with numeric literals kept, optional sharedStrings.xml, numeric filename sort (sheet2/slide2 before sheet10/slide10)
- All fixtures built in-test with `zipSync`/`strToU8` — no binary fixtures in the repo
- Determinism asserted per format (same bytes in → byte-identical text out)
- `@pikar/vault` suite 42/42 green + `tsc -p . --noEmit` clean; index barrel untouched (fflate stays out of the V8 bundle)

## Task Commits

Each task was committed atomically (TDD RED → GREEN):

1. **Task 1: DOCX flatten + shared walk helpers** — `0bc772f` (test RED), `8416a14` (feat GREEN)
2. **Task 2: XLSX flatten (sharedStrings indirection)** — `28e4e3a` (test RED), `88838c1` (feat GREEN)
3. **Task 3: PPTX flatten + robustness pass** — `143ec8b` (test RED), `2c42bb3` (feat GREEN + strict-index typecheck fixes)

## Files Created/Modified

- `packages/vault/src/officeText.ts` — the real implementation: `decodeEntities`, `runsOf` (attribute-tolerant), `numericSorted`, `docxText`/`xlsxText`/`pptxText`, mime dispatch in `extractOfficeText`
- `packages/vault/src/officeText.test.ts` — 15 tests: 6 DOCX, 4 XLSX, 2 PPTX, 3 determinism
- `docs/playbooks/vault.md` — Lane 2 build notes appended inside the `#### Lane 2 — Office parsers` subsection only (PARALLELIZATION §6 append-only rule; shared `Last verified` header deliberately untouched to avoid cross-lane merge conflicts)

## Decisions Made

- Regex text-walk over an XML parser dependency — matches the plan's locked FLATTEN fidelity bar; one shared `runsOf` helper covers all three formats
- Rich-text `<si>` ceiling accepted and marked with a `ponytail:` comment in source (multiple runs per `<si>` index as multiple sharedStrings entries; per-`<si>` grouping is the named upgrade path)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Strict-index typecheck failures under noUncheckedIndexedAccess**
- **Found during:** Task 3 (the plan's typecheck verify step — first time `tsc -p . --noEmit` ran on this file)
- **Issue:** 6 TS errors: unchecked `NAMED[e]`, regex capture-group indexing (`m[1]`, `row[1]`, `cell[2]`, `v[1]`), and `entries[path]` after key-filtering
- **Fix:** `?? match` / `?? ""` fallbacks on capture groups; `numericSorted` restructured to `Object.entries(...).flatMap(...)` so the value is never indexed by key
- **Files modified:** packages/vault/src/officeText.ts
- **Verification:** `tsc -p . --noEmit` exit 0; all 42 tests still green
- **Committed in:** 2c42bb3 (part of Task 3 GREEN commit)

---

**Total deviations:** 1 auto-fixed (Rule 1)
**Impact on plan:** Type-safety fix required by the plan's own verify step. No scope creep.

## Issues Encountered

- First session was killed by a session limit mid-Task-3 with the PPTX GREEN implementation uncommitted. Resume path: inspected the working-tree diff (implementation was complete and correct), ran the full suite (green), fixed the typecheck reds, committed as Task 3 GREEN. No work redone.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `lane-2/office-parsers` is done and self-contained: `git diff --stat main` shows only officeText.ts + officeText.test.ts (+ playbook subsection/bookkeeping)
- Ready for plan 06 (Wave 3 integration): merge this branch to main — Lane 1's existing office dispatch branch flows real text with no code change on either side
- NOT merged, NOT pushed (plan 06's job per orchestrator instruction; the plan's own "merge and announce" output line is superseded)

## Self-Check: PASSED

- FOUND: packages/vault/src/officeText.ts
- FOUND: packages/vault/src/officeText.test.ts
- FOUND: commits 0bc772f, 8416a14, 28e4e3a, 88838c1, 143ec8b, 2c42bb3

---
*Phase: 03.8-vault-document-extraction*
*Completed: 2026-07-18*
