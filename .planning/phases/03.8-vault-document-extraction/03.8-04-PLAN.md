---
phase: 03.8-vault-document-extraction
plan: 04
type: execute
wave: 2
depends_on: ["03.8-01"]
files_modified:
  - packages/backend/convex/vaultSweep.ts
  - packages/backend/convex/vaultSweep.test.ts
  - apps/web/app/(app)/dashboard/vault/DocGrid.tsx
  - apps/web/app/(app)/dashboard/vault/PreviewModal.tsx
  - apps/web/app/(app)/dashboard/vault/Dropzone.tsx
  - apps/web/app/(app)/dashboard/vault/page.tsx
  - apps/web/e2e/vault.spec.ts
autonomous: true
requirements: [EXTR-G, EXTR-H, EXTR-F]
must_haves:
  truths:
    - "A one-shot governed sweep schedules extraction for every eligible pending_extraction row (both kinds), oldest handled via the resumable migrations runner — the existing backlog heals"
    - "A failed doc shows a failed badge + Retry on BOTH the item card and the detail panel, with the failure reason in the panel"
    - "The extracting state renders as a visible pill between pending_extraction and processing — reactive, no refresh"
    - "A truncated doc's detail panel says the file was truncated (extractionTruncated honesty)"
    - "The offline E2E walks a SMOKE:: upload from pending to searchable"
  artifacts:
    - path: "packages/backend/convex/vaultSweep.ts"
      provides: "migrations-based backlog sweep + retryExtraction tenantMutation"
      exports: ["sweepPendingExtraction", "runSweep", "retryExtraction"]
      min_lines: 40
    - path: "apps/web/app/(app)/dashboard/vault/DocGrid.tsx"
      provides: "extracting pill + failed badge + card Retry"
    - path: "apps/web/app/(app)/dashboard/vault/PreviewModal.tsx"
      provides: "failure reason + panel Retry + truncation note"
    - path: "apps/web/e2e/vault.spec.ts"
      provides: "EXTR-H offline E2E extension"
  key_links:
    - from: "packages/backend/convex/vaultSweep.ts"
      to: "internal.vaultExtract.extractDoc / internal.vaultTranscribe.transcribeDoc"
      via: "kind-dispatched ctx.scheduler.runAfter (Wave-0 stub names — no cross-lane import)"
      pattern: "scheduler\\.runAfter"
    - from: "apps/web/app/(app)/dashboard/vault (DocGrid + PreviewModal)"
      to: "api.vaultSweep.retryExtraction"
      via: "useMutation on the Retry affordances"
      pattern: "retryExtraction"
---

<objective>
Lane 3 — the sweep, the retry, and the visible half of the phase: backlog self-heal
(migrations-based one-shot sweep), the `retryExtraction` mutation, the vault route's status
pills / failed+Retry / truncation-note UI, and the offline E2E.

THIS PLAN RUNS AS AN INDEPENDENT CLAUDE CODE SESSION in its own git worktree
(`.worktrees/lane-3-sweep-ui`, branch `lane-3/sweep-ui`) with its OWN `npx convex dev`
deployment, per `.planning/PARALLELIZATION.md`. It touches ONLY the files above (all Lane-3
owned; no other lane touches apps/web or vaultSweep*). It calls Lane 1/4's actions ONLY through
their Wave-0-frozen `internal.*` stub names — while those lanes are unmerged, a swept/retried doc
lands at failed("not_implemented"), which is itself a valid UI state to build against.
`.planning/STATE.md`/`ROADMAP.md` and the `docs/playbooks/vault.md` Lane-3 subsection follow the
append-only keep-both rule (not part of the interference contract).

Purpose: the vault heals its backlog and the user can SEE extraction working, failing, retrying.
Output: vaultSweep.ts + tests, the vault UI lifecycle states, extended offline E2E.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.8-vault-document-extraction/03.8-RESEARCH.md  (Pattern 4 — sweep + retry; Open Question 1 — the UI surfaces)
@.planning/PARALLELIZATION.md
@docs/design/BRAND.md  (§ MANDATORY before any UI edit — tokens, pill/badge idioms, calm executive voice, no color-alone meaning)
@packages/backend/convex/migrations.ts  (the installed migrations instance + runner precedent)
@packages/backend/convex/vault.ts  (the Wave-0 seam + status union semantics)
@apps/web/app/(app)/dashboard/vault/DocGrid.tsx
@apps/web/app/(app)/dashboard/vault/PreviewModal.tsx
@apps/web/app/(app)/dashboard/vault/Dropzone.tsx
@apps/web/e2e/vault.spec.ts  (existing spec — extend, don't rewrite)

Project skills: packages/backend/.claude/skills (convex-function-syntax, convex-schema) apply to vaultSweep.ts.

<interfaces>
<!-- Wave-0 contract this lane codes against (frozen): -->
```typescript
// @pikar/vault barrel:
extractionKindFor(mimeType: string, filename?: string): "pdf"|"image"|"office"|"transcribe"|null

// internal action names (stubs until Lanes 1/4 merge — reference by name, never import their modules):
internal.vaultExtract.extractDoc({ vaultDocId, tenantId })
internal.vaultTranscribe.transcribeDoc({ vaultDocId, tenantId })

// vaultDocuments row (post-Wave-0): status ∈ processing|ready|failed|pending_extraction|extracting;
// failureReason?: string; extractionTruncated?: boolean
```
Sweep shape (RESEARCH Pattern 4 — migrations component is installed + registered):
```typescript
import { migrations } from "./migrations";
export const sweepPendingExtraction = migrations.define({ table: "vaultDocuments", migrateOne: ... });
export const runSweep = migrations.runner(internal.vaultSweep.sweepPendingExtraction);
// operator one-shot: npx convex run vaultSweep:runSweep
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vaultSweep.ts — governed backlog sweep + retryExtraction</name>
  <files>packages/backend/convex/vaultSweep.ts, packages/backend/convex/vaultSweep.test.ts</files>
  <behavior>
    - sweep over seeded rows: pending_extraction + recognized kind + storageId → extractDoc (pdf/image/office) or transcribeDoc (video) scheduled; ready/processing/failed rows untouched; unrecognized mime (application/zip) skipped (EXTR-G)
    - retryExtraction: owner tenant + status "failed"|"pending_extraction" → status reset to "pending_extraction", failureReason cleared, correct action scheduled by kind; returns refs only
    - retryExtraction on a "ready" doc → no-op/rejection; cross-tenant doc → no-op (owner guard, fail-closed)
  </behavior>
  <action>
    Create `vaultSweep.ts` (DEFAULT runtime — no "use node") per RESEARCH Pattern 4:
    `sweepPendingExtraction = migrations.define({...})` with the guard chain
    (status === "pending_extraction", storageId present, extractionKindFor(mimeType, title)
    non-null) then the two-line kind ternary scheduling the Wave-0 stub names. Add
    `runSweep = migrations.runner(...)` following migrations.ts's existing runner precedent.
    `retryExtraction = tenantMutation` (§2 wrapper) with owner guard + status guard + the SAME
    kind ternary; clear failureReason + extractionTruncated on retry.
    Governance note (comment it): each scheduled extractDoc/transcribeDoc self-gates via
    preCall, so the sweep needs no separate rate-limit config; `ponytail:` batch-size ceiling
    default 100 — per-batch runAfter stagger is the upgrade path if a huge backlog ever
    burst-schedules.
    Tests: convex-test with the migrations component registered (copy the registration approach
    from the intake.test.ts/rateLimiter precedent adapted to migrations); assert scheduled
    function names + args, statuses untouched where required, owner-guard fail-closed.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- vaultSweep</automated>
  </verify>
  <done>Sweep schedules exactly the eligible rows by kind; retry is owner-guarded and kind-correct; tests green offline.</done>
</task>

<task type="auto">
  <name>Task 2: Vault UI lifecycle — extracting pill, failed + Retry (card AND panel), truncation note</name>
  <files>apps/web/app/(app)/dashboard/vault/DocGrid.tsx, apps/web/app/(app)/dashboard/vault/PreviewModal.tsx, apps/web/app/(app)/dashboard/vault/Dropzone.tsx, apps/web/app/(app)/dashboard/vault/page.tsx</files>
  <action>
    Read BRAND.md first. All states reactive via the existing useQuery subscriptions (no
    polling, no refresh):
    1. DocGrid: add the `extracting` status branch (pill idiom matching the existing
       pending_extraction/processing pills — text carries the meaning, never color alone);
       failed cards get the failed badge + a Retry affordance calling
       `api.vaultSweep.retryExtraction` via useMutation.
    2. PreviewModal (detail panel): failed docs show `failureReason` verbatim (it is refs-only
       by construction — honest reasons like "unsupported_video_container", "kill_switch",
       "office_parse_failed"; render human-readably) + the SAME Retry affordance;
       `extractionTruncated` docs show the calm truncation note: "Extracted the first part of
       this file — large file truncated." No amber (--held belongs to the approval gate alone),
       no new component library, reuse the sibling card/pill idioms.
    3. Dropzone copy fix: the "Storage-only: Images, Videos" subtext is now false twice over —
       update to reflect that PDF/Office/images/videos are extracted and become searchable.
    4. page.tsx: only wiring if needed (props/handlers for retry); keep the diff minimal.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit</automated>
  </verify>
  <done>All five statuses render distinctly; Retry on card AND panel; reason + truncation note in panel; Dropzone copy honest; web typecheck clean; Biome baseline-diff shows no new findings on touched files.</done>
</task>

<task type="auto">
  <name>Task 3: Offline E2E — SMOKE:: upload walks to searchable</name>
  <files>apps/web/e2e/vault.spec.ts</files>
  <action>
    Extend the EXISTING vault.spec.ts (don't rewrite): a test uploading a small file whose bytes
    are `SMOKE::extract::<needle>` with a binary mime (e.g. application/pdf) → assert the pill
    reaches processing/ready without refresh, then `vaultSearch` (the SMOKE:: search seam this
    spec already uses) surfaces the doc (EXTR-H). Add a second row for video: bytes
    `SMOKE::transcribe::<needle>` with mime video/mp4 (coordination note from Lane 4 — sentinel
    only, no real video). If Lane 1/4 are not yet merged into this worktree, the SMOKE
    short-circuit lives in their actions, so these tests will only pass locally AFTER merging
    main post-Lane-1/4 — structure the spec now, mark the two tests with a clear skip-guard on
    failure ("requires lanes 1/4 merged"), and remove the guard during 03.8-06 integration.
    Follow the spec's existing auth/seed conventions verbatim. Known env constraint (STATE
    precedent): if E2E creds are absent, the bar is typecheck + Playwright discovery + a
    documented run attempt — do not fabricate a workaround.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test e2e/vault.spec.ts --list</automated>
  </verify>
  <done>Spec discovers + typechecks with the two new tests; runnable offline once lanes merge; conventions unchanged.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- vaultSweep` green offline; web typecheck exit 0
- `node scripts/check-playbooks.mjs` exit 0 (vault.md Lane-3 subsection touched in-commit)
- Playwright discovers the extended vault.spec.ts
- `git diff --stat main` confined to the seven owned files (+ playbook subsection/bookkeeping)
</verification>

<success_criteria>
- EXTR-G proven offline; EXTR-H structured and discovery-verified (fully green at 03.8-06 integration); EXTR-F's UI half rendered per BRAND
- Sweep + retry dispatch by kind through the Wave-0 stub names only — zero imports from Lane 1/4 modules
- The one-shot production sweep run itself is 03.8-06's job (post-merge, on main's deployment) — this plan only ships the machinery
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-vault-document-extraction/03.8-04-SUMMARY.md`.
Merge the lane branch to `main` and announce.
</output>
