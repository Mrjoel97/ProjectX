---
phase: 03.8
slug: vault-document-extraction
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-18
---

# Phase 03.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest + convex-test (backend, offline); Playwright (web E2E, offline via SMOKE seams) |
| **Config file** | existing — `packages/backend` vitest config, `apps/web` Playwright config |
| **Quick run command** | `pnpm --filter @pikar/backend test -- <touched-file>.test.ts` |
| **Full suite command** | `pnpm --filter @pikar/backend test` (+ `pnpm --filter @pikar/web typecheck`) |
| **Estimated runtime** | ~30s quick / ~4min full (note: full suite under a live dev server has OOM'd — run offline) |

---

## Sampling Rate

- **After every task commit:** Run the touched-file quick command
- **After every plan wave:** Run the full suite command
- **Before `/gsd:verify-work`:** Full suite must be green (modulo the documented pre-existing audit.test.ts red)
- **Max feedback latency:** ~240 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| (filled by gsd-planner per plan) | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Contract-plan stubs land WITH their test files (dispatcher unit test, seam-variant test) so every lane starts green
- [ ] Format fixtures generated in-test (fflate-built OOXML, pdf-lib-built PDFs — no binary fixtures in repo, per RESEARCH)
- [ ] Video: SMOKE:: short-circuit only offline; live smoke in Lane 4's own deployment

*Existing infrastructure (vitest/convex-test/Playwright) covers all phase requirements — no new framework.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Multi-page scanned-PDF OCR quality via reused `attachment-extractor` skill | phase goal | model-output quality judgment | upload a scanned multi-page PDF in a lane deployment; read extracted text in the detail panel |
| Deployed-Node unpdf behavior (`Promise.withResolvers`) | Lane 1 | runtime differs local vs deployed | Lane 1 live smoke: extract a born-digital PDF against the lane's dev deployment |
| Video transcription end-to-end | Lane 4 | no offline video fixture | upload a small real mp4 in Lane 4's deployment; verify transcript reaches the seam |
| Status pills / retry / truncation-note rendering | UX decisions | visual | phase-close human-verify in browser |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 240s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
