---
phase: 03.8-vault-document-extraction
plan: 06
type: execute
wave: 3
depends_on: ["03.8-02", "03.8-03", "03.8-04", "03.8-05"]
files_modified:
  - apps/web/e2e/vault.spec.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [EXTR-A, EXTR-B, EXTR-C, EXTR-D, EXTR-E, EXTR-F, EXTR-G, EXTR-H, EXTR-I]
must_haves:
  truths:
    - "All four lane branches are merged to main and the merged whole is green (suites, typechecks, playbook hook)"
    - "The pre-existing pending_extraction backlog on the main deployment healed after the one-shot sweep"
    - "A human watched one of EACH format (text PDF, scanned PDF, image, DOCX, XLSX, PPTX, mp4) walk pending_extraction → extracting → processing → ready with no refresh, and found each doc's unique content via search"
    - "The honest-failure paths are visibly honest: .mov → failed + reason; kill switch → failed + Retry recovers; oversize doc → truncation note"
  artifacts:
    - path: "apps/web/e2e/vault.spec.ts"
      provides: "EXTR-H tests un-skip-guarded and green against the merged whole"
  key_links:
    - from: "main (merged)"
      to: "all four lane branches"
      via: "git merges + full-suite verification"
      pattern: "merge"
---

<objective>
Phase close — integration + verification. The four lanes were built and smoke-tested in
isolation; this plan merges them, proves the MERGED whole (the only place office docs flow
through Lane 1's dispatcher into Lane 3's UI, and the E2E can finally run un-guarded), runs the
one-time backlog sweep on the main deployment, and holds the phase-close human-verify.

Run this in the MAIN checkout after all four lanes have merged (or perform the merges here).
Purpose: no lane-local green is trusted as phase-done; the goal is verified end to end.
Output: green merged main, healed backlog, human-approved UX + extraction quality.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.8-vault-document-extraction/03.8-RESEARCH.md  (§ Live smoke / phase-close human-verify — the checklist this plan executes)
@.planning/PARALLELIZATION.md
@.planning/phases/03.8-vault-document-extraction/  (all four lane SUMMARYs — read for deviations before merging)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge all lanes, verify the merged whole, heal the backlog</name>
  <files>apps/web/e2e/vault.spec.ts</files>
  <action>
    1. Confirm/perform the four lane merges to `main` (order flexible; resolve
       STATE.md/ROADMAP.md/vault.md conflicts keep-both per PARALLELIZATION.md). Verify with
       `git log --merges` + presence of all lane files on main.
    2. Full offline verification on merged main (NO live convex dev running — documented OOM
       risk): `pnpm --filter @pikar/backend test` + `pnpm --filter @pikar/vault test` +
       backend & web typechecks + `node scripts/check-playbooks.mjs`. Baseline noise tolerance:
       ONLY the documented audit.test.ts red + runCockpitAgent machine-load timeouts.
    3. Remove the "requires lanes 1/4 merged" skip-guards from vault.spec.ts's two 03.8 tests
       and run `pnpm --filter @pikar/web test:e2e -- vault` (subject to the documented E2E-creds
       constraint — if creds are absent, record the blocker verbatim; do not weaken auth).
    4. Integration proof the lanes could never see: upload (via convex-test already covered; do
       it live in step 5's deployment) — and grep-level sanity: Lane 1's dispatcher imports
       `@pikar/vault/officeText` whose body is now Lane 2's real parser.
    5. Start `npx convex dev` on the MAIN deployment and run the one-shot backlog sweep:
       `npx convex run vaultSweep:runSweep`. Watch pending_extraction rows drain (governed —
       budget/kill-switch gate each doc). Record before/after counts.
    6. `graphify update .` + `node scripts/extract-convex-edges.mjs` (per CLAUDE.md).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test && pnpm --filter @pikar/vault test && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Merged main green (modulo documented baseline reds); E2E guards removed; backlog sweep executed with recorded counts; graph updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase-close human-verify — the RESEARCH checklist</name>
  <what-built>The complete vault extraction pipeline: 6 formats + video, auto-extract on upload, backlog sweep, retry, honest failure/truncation UX.</what-built>
  <action>Automate first: with main merged and green from Task 1, start `pnpm dev` + `npx convex dev` on the main deployment and prepare one sample file of each format (pdf-lib PDF, fflate OOXML trio, any image/mp4 from disk) so the human walk-through below is drag-drop-and-watch.</action>
  <verify>Manual — human executes the RESEARCH phase-close checklist below.</verify>
  <done>All seven formats walk to ready and are searchable; .mov / kill-switch / truncation paths visibly honest; backlog healed; approval given.</done>
  <how-to-verify>
    With `pnpm dev` + `npx convex dev` on main, upload ONE OF EACH and watch (no refresh):
    1. Text-layer PDF, scanned PDF, image, DOCX, XLSX, PPTX, short mp4 — each pill walks
       pending_extraction → extracting → processing → ready reactively.
    2. Search for content UNIQUE to each doc — including something said only in the video.
       All seven found.
    3. Scanned-PDF quality check (RESEARCH Open Q3): the extracted text is a verbatim
       transcription, NOT a summary/description. If it summarizes, note it — the documented
       upgrade path is seeding a `vault-extractor` skill (a follow-up, not this phase).
    4. Upload a .mov → failed badge + "unsupported video container" reason in the panel.
    5. Kill switch ON → upload anything → failed + reason; kill switch OFF → Retry (card and
       panel both) → doc recovers to ready.
    6. Upload a text-heavy doc large enough to trip VAULT_EXTRACT_CHAR_CAP → truncation note
       visible in the detail panel.
    7. Confirm the pre-existing backlog healed (Task 1's sweep counts; the vault shows no
       stale pending_extraction rows that have a recognized format).
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe gaps for /gsd:plan-phase 03.8 --gaps.</resume-signal>
</task>

</tasks>

<verification>
- Full suites + typechecks + playbook hook green on merged main
- Backlog drained (recorded counts); human-verify checklist all confirmed
- ROADMAP.md phase 3.8 ticked; STATE.md updated (phase-close bookkeeping)
</verification>

<success_criteria>
- The phase goal is TRUE end to end: a non-text vault upload of any supported format becomes searchable via the vaultIngestText seam, and unsupported/failed/oversize cases are visibly honest
- All nine derived requirements (EXTR-A..I) verified on the merged whole, not just lane-locally
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-vault-document-extraction/03.8-06-SUMMARY.md`,
tick Phase 3.8 in ROADMAP.md, update STATE.md.
</output>
