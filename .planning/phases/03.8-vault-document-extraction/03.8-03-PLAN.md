---
phase: 03.8-vault-document-extraction
plan: 03
type: execute
wave: 2
depends_on: ["03.8-01"]
files_modified:
  - packages/vault/src/officeText.ts
  - packages/vault/src/officeText.test.ts
autonomous: true
requirements: [EXTR-C]
must_haves:
  truths:
    - "A DOCX flattens to its paragraph text (runs joined, newline per paragraph, entities decoded)"
    - "An XLSX flattens sheet-by-sheet: shared-string cells resolved, literal cells kept, rows tab-joined, 'Sheet N' headers, numeric sheet order"
    - "A PPTX flattens slide-by-slide in NUMERIC order (slide2 before slide10) with 'Slide N' headers"
    - "A corrupt/non-OOXML zip throws a typed error the Lane-1 dispatcher already converts to markFailed('office_parse_failed')"
    - "Zero model calls, zero spend, zero Convex edits — pure TS, deterministic"
  artifacts:
    - path: "packages/vault/src/officeText.ts"
      provides: "extractOfficeText real implementation (fflate unzipSync + regex XML text-walk)"
      exports: ["extractOfficeText"]
      min_lines: 60
    - path: "packages/vault/src/officeText.test.ts"
      provides: "EXTR-C coverage with in-test zipSync-built fixtures (no binary fixtures in repo)"
      min_lines: 80
  key_links:
    - from: "packages/vault/src/officeText.ts"
      to: "fflate"
      via: "unzipSync(bytes) → { [path]: Uint8Array }"
      pattern: "unzipSync"
    - from: "packages/backend/convex/vaultExtract.ts (Lane 1, no edit here)"
      to: "packages/vault/src/officeText.ts"
      via: "the Wave-0-frozen signature — replacing this body wires office docs with zero cross-lane edits"
      pattern: "export function extractOfficeText"
---

<objective>
Lane 2 — Office parsers: replace the Wave-0 `extractOfficeText` stub with the real deterministic
flatten for DOCX/XLSX/PPTX. One `fflate.unzipSync` + one attribute-tolerant regex XML text-walk
covers all three formats — no XML parser dep, no model call. The locked fidelity bar is
deterministic FLATTEN (text runs only; embedded charts/images ignored v1), so lossy-but-boring
regex extraction is correct, not a shortcut.

THIS PLAN RUNS AS AN INDEPENDENT CLAUDE CODE SESSION in its own git worktree
(`.worktrees/lane-2-office-parsers`, branch `lane-2/office-parsers`) with its OWN
`npx convex dev` deployment (needed only for install/typecheck — this lane's tests are plain
vitest, no backend), per `.planning/PARALLELIZATION.md`. It touches ONLY the two files above —
ZERO convex edits, zero package.json edits (fflate was pinned by 03.8-01).
`.planning/STATE.md`/`ROADMAP.md` and the `docs/playbooks/vault.md` Lane-2 subsection follow the
append-only keep-both rule (not part of the interference contract).

Purpose: Office uploads become searchable for free.
Output: working extractOfficeText + fixture-driven test suite; Lane 1's dispatcher picks it up on
merge with no code change on either side.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.8-vault-document-extraction/03.8-RESEARCH.md  (Pattern 3 — the format/element table; Pitfall 7 — the OOXML gotchas; Code Examples — in-test fixture generation)
@.planning/PARALLELIZATION.md
@packages/vault/src/officeText.ts  (the Wave-0 stub — the signature is FROZEN)
@packages/vault/src/normalize.ts + normalize.test.ts  (this package's existing style: pure functions, plain vitest, ponytail-terse)

<interfaces>
<!-- FROZEN contract (Wave 0) — the signature Lane 1 already calls: -->
```typescript
export function extractOfficeText(bytes: Uint8Array, mimeType: string): { text: string };
// throws on malformed/unrecognized input — the dispatcher catches → markFailed("office_parse_failed")
```
OOXML MIMEs dispatched here (from extractionKindFor, same package):
- application/vnd.openxmlformats-officedocument.wordprocessingml.document  (DOCX)
- application/vnd.openxmlformats-officedocument.spreadsheetml.sheet        (XLSX)
- application/vnd.openxmlformats-officedocument.presentationml.presentation (PPTX)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The shared walk helpers + DOCX flatten</name>
  <files>packages/vault/src/officeText.ts, packages/vault/src/officeText.test.ts</files>
  <behavior>
    - zipSync-built DOCX fixture with two runs in one paragraph → "Hello world &amp; co" decoded to "Hello world & co", single line
    - two paragraphs → newline-separated
    - `<w:t xml:space="preserve">` (attribute-bearing tag) runs are captured (attribute-tolerant regex — Pitfall 7)
    - numeric entities (&#65; / &#x41;) decode; the 5 named entities decode
    - a zip without word/document.xml under the DOCX mime → throws
    - non-zip bytes → throws (typed message, e.g. "office_parse_failed: not a zip")
  </behavior>
  <action>
    Implement per RESEARCH Pattern 3: `unzipSync(bytes)` from fflate; helpers
    `decodeEntities(s)` (5 named + numeric decimal/hex) and
    `runsOf(xml, tag)` using the attribute-tolerant regex
    `<${tag}(?:\s[^>]*)?>([\s\S]*?)</${tag}>` with matchAll. DOCX: decode
    `word/document.xml` (fflate strFromU8), split on `</w:p>` for paragraph boundaries, collect
    `<w:t>` runs per paragraph, join runs with "", paragraphs with "\n". Dispatch on mimeType in
    `extractOfficeText`; unknown mime or missing expected entry → throw. Build ALL fixtures
    in-test with `zipSync`/`strToU8` (same dep the parser uses — RESEARCH Code Examples).
    Keep it ~one screen; this package's ponytail style applies.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test -- officeText</automated>
  </verify>
  <done>DOCX flatten green incl. attribute-tolerant runs, entity decode, corrupt-input throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: XLSX flatten (sharedStrings indirection)</name>
  <files>packages/vault/src/officeText.ts, packages/vault/src/officeText.test.ts</files>
  <behavior>
    - fixture with xl/sharedStrings.xml + two sheets: cells with t="s" resolve through sharedStrings; numeric/literal `<v>` cells kept as-is (Pitfall 7: numbers are NOT garbage)
    - rows emit as tab-joined lines; each sheet prefixed "Sheet N" header; sheets in NUMERIC filename order (sheet2 before sheet10)
    - missing sharedStrings.xml but literal-only cells → still flattens (sharedStrings is optional)
  </behavior>
  <action>
    Parse `xl/sharedStrings.xml` `<t>` runs into an index array (attribute-tolerant). For each
    `xl/worksheets/sheet*.xml` (numeric sort on the filename number): walk `<row>` blocks, within
    each walk `<c>` cells — a cell with `t="s"` indexes sharedStrings via its `<v>` integer,
    otherwise emit the `<v>` literal (entity-decoded). Tab-join cells per row, newline per row,
    `Sheet N` header per sheet, blank line between sheets.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test -- officeText</automated>
  </verify>
  <done>XLSX fixtures green: indirection, literals, numeric sheet order, optional sharedStrings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PPTX flatten + final robustness pass</name>
  <files>packages/vault/src/officeText.ts, packages/vault/src/officeText.test.ts</files>
  <behavior>
    - fixture with ppt/slides/slide1.xml, slide2.xml, slide10.xml → output order 1, 2, 10 ("Slide N" headers — numeric sort, Pitfall 7)
    - `<a:t>` runs per slide joined with newlines
    - a PPTX-mime zip with no ppt/slides/*.xml → throws
    - re-run determinism: same bytes in → byte-identical text out (all three formats — one assertion each)
  </behavior>
  <action>
    PPTX: collect `ppt/slides/slide*.xml`, numeric sort, `<a:t>` runs, `Slide N` header per
    slide. Then the polish pass: ensure every throw path carries a stable prefixed message,
    confirm zero imports beyond fflate (no barrel import cycles), and add the determinism
    assertions. Do NOT add officeText to packages/vault/src/index.ts (Wave-0 decision — keeps
    fflate out of the V8 bundle; Lane 1 imports the subpath).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/vault test && pnpm --filter @pikar/vault exec tsc -p . --noEmit</automated>
  </verify>
  <done>All three formats flatten deterministically; full @pikar/vault suite + typecheck green; index barrel untouched.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/vault test` fully green (plain vitest, no backend needed)
- `git diff --stat main` shows ONLY officeText.ts + officeText.test.ts (+ playbook subsection/bookkeeping)
- grep: no `officeText` in packages/vault/src/index.ts; no new deps in any package.json
</verification>

<success_criteria>
- EXTR-C proven with in-test fixtures covering every Pitfall-7 gotcha
- Zero Convex edits, zero shared-file edits — the purest lane
- After merge, Lane 1's existing office branch flows real text with no change on either side
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-vault-document-extraction/03.8-03-SUMMARY.md`.
Merge the lane branch to `main` and announce.
</output>
