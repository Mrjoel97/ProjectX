---
phase: 03.8-vault-document-extraction
plan: 05
type: execute
wave: 2
depends_on: ["03.8-01"]
files_modified:
  - packages/backend/convex/vaultTranscribe.ts
  - packages/backend/convex/vaultTranscribe.test.ts
autonomous: false
requirements: [EXTR-I]
must_haves:
  truths:
    - "An mp4/webm/mpeg vault upload gets its audio track transcribed via the existing hosted transcription rail (no ffmpeg, no new deps) and becomes searchable via the seam"
    - "An unsupported container (video/quicktime etc.) fails HONESTLY: status failed + reason unsupported_video_container — never a silent forever-pending row"
    - "A governed stop (kill switch / budget) marks the doc failed with the reason, no spend"
    - "Transcription spend is duration-priced and recorded; audit rows carry kind 'video' + counts only (durationSeconds, charCount) — never transcript text"
  artifacts:
    - path: "packages/backend/convex/vaultTranscribe.ts"
      provides: "transcribeDoc: gate → container check → transcribe → scan → audit → seam"
      exports: ["transcribeDoc"]
      min_lines: 80
    - path: "packages/backend/convex/vaultTranscribe.test.ts"
      provides: "EXTR-I offline coverage (SMOKE::transcribe:: sentinel — NO video fixtures)"
      min_lines: 60
  key_links:
    - from: "packages/backend/convex/vaultTranscribe.ts"
      to: "internal.vault.ingestExtractedText"
      via: "ctx.runMutation after scan gate + char-cap truncation"
      pattern: "ingestExtractedText"
    - from: "packages/backend/convex/vaultTranscribe.ts"
      to: "experimental_transcribe (ai@7) + priceTranscription (@pikar/cost)"
      via: "the intake.ts transcribeAudio shape, copied (never imported across 'use node' modules)"
      pattern: "experimental_transcribe|priceTranscription"
---

<objective>
Lane 4 — video (and stray audio) transcription: replace the Wave-0 `transcribeDoc` stub with the
real action. The endpoint accepts mp4/webm/mpeg containers directly (audio track demuxed
server-side), and the vault's 8 MiB cap sits under the API's 25 MB limit — so every video that
can enter the vault fits the API. The only failure path is an unsupported container, which fails
honestly.

THIS PLAN RUNS AS AN INDEPENDENT CLAUDE CODE SESSION in its own git worktree
(`.worktrees/lane-4-video-transcribe`, branch `lane-4/video-transcribe`) with its OWN
`npx convex dev` deployment, per `.planning/PARALLELIZATION.md`. It touches ONLY the two files
above. Zero new deps, zero skill rows (transcription takes no prompt — §5 does not apply).
`.planning/STATE.md`/`ROADMAP.md` and the `docs/playbooks/vault.md` Lane-4 subsection follow the
append-only keep-both rule (not part of the interference contract). Lane 4 adds NO e2e file —
the video E2E row is Lane 3's, using this lane's SMOKE sentinel grammar.

Purpose: "something said only in a video" becomes searchable; the Videos tab stops being storage-only.
Output: working transcribeDoc + sentinel-driven offline tests + the mandatory live container smoke.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.8-vault-document-extraction/03.8-RESEARCH.md  (§ Video Transcription (Lane 4) — the whole lane spec; Governance Call Shapes table)
@.planning/PARALLELIZATION.md
@packages/backend/convex/intake.ts  (transcribeAudio lines ~61-75 — COPY the shape; SMOKE sniff grammar lines ~41/63; do NOT import intake.ts)
@packages/backend/convex/vault.ts  (Wave-0 seam: getDocForExtraction, markExtracting, markFailed, ingestExtractedText)
@packages/backend/convex/intake.test.ts  (rateLimiter component registration block ~22-42)
@docs/playbooks/vault.md  (append Lane-4 notes in the Lane-4 subsection ONLY)

Project skills: packages/backend/.claude/skills (convex-actions) apply.

<interfaces>
<!-- Wave-0 contract (frozen): -->
```typescript
// @pikar/vault barrel:
TRANSCRIBABLE_CONTAINER_MIME: ReadonlySet<string>  // video/mp4, video/webm, video/mpeg + audio list — NO video/quicktime
VAULT_EXTRACT_CHAR_CAP = 400_000;

// internal.vault:
getDocForExtraction({ vaultDocId, tenantId }) → { storageId, mimeType, title, status }
markExtracting({ vaultDocId });  markFailed({ vaultDocId, reason });
ingestExtractedText({ docId, tenantId, text, truncated })
```
The transcription engine (copy from intake.ts:61-75 — a local duplicate, NOT an import;
"use node" modules never import each other, §96 circular-inference rule):
```typescript
import { experimental_transcribe as transcribe } from "ai";
const result = await transcribe({
  model: openai.transcription("gpt-4o-transcribe"),
  audio: bytes,
  abortSignal: AbortSignal.timeout(45_000),
});
const priced = priceTranscription(result.durationInSeconds ?? 0);   // @pikar/cost
if (priced.ok) await ctx.runMutation(internal.guardrails.recordSpend, { costUsd: priced.value });
// result.text is the transcript
```
SMOKE grammar (same sentinel family as intake): bytes decoding to `SMOKE::transcribe::<text>`
short-circuit to `<text>` — no API call, no spend.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: transcribeDoc — the full spine, sentinel-tested</name>
  <files>packages/backend/convex/vaultTranscribe.ts, packages/backend/convex/vaultTranscribe.test.ts</files>
  <behavior>
    - SMOKE::transcribe::hello bytes (mime video/mp4) → status walk pending_extraction→extracting→processing, doc.text "hello", ingest workflow started, NO API call, NO spend (EXTR-I)
    - mime video/quicktime → status "failed", failureReason "unsupported_video_container" — mime-only check, before any byte work, no spend (EXTR-I)
    - kill switch ON → "failed" + "kill_switch", no throw (governed stop is a RETURN)
    - success audit "vault.extracted" carries { vaultDocId, kind: "video", durationSeconds, charCount, truncated } — counts only, needle-scanned for transcript absence
    - scanText Err path → "failed" + "pii_scan_failed" + ONE refs-only audit row
    - transcript over VAULT_EXTRACT_CHAR_CAP → truncated, seam called with truncated: true
    - audio/* mime in TRANSCRIBABLE_CONTAINER_MIME → same spine (stray audio uploads don't rot)
  </behavior>
  <action>
    Replace the stub body, structure identical to Lane 1's Pattern-1 spine:
    (1) preCall gate → markFailed(reason)/return; (2) markExtracting; (3) getDocForExtraction +
    ctx.storage.get → bytes (never bytes-as-args); (4) decodeUtf8 sniff for
    `SMOKE::transcribe::<text>` FIRST; (5) container check:
    `!TRANSCRIBABLE_CONTAINER_MIME.has(mimeType)` → markFailed("unsupported_video_container")
    (the honest-failure decision — a .mov gets a visible reason, not eternal pending);
    (6) the copied transcribe call + priceTranscription → recordSpend; (7) scanText fail-closed
    gate → Err: markFailed + one refs-only audit; (8) counts-only success audit (kind "video",
    durationSeconds from result, charCount, truncated); (9) truncate at VAULT_EXTRACT_CHAR_CAP →
    `ingestExtractedText` with raw (post-gate) transcript. Whole body try/catch → markFailed.
    `ponytail:` comment the duration ceiling (8 MiB bounds duration in practice; chunking is the
    upgrade path if a duration limit ever surfaces).
    Tests: convex-test + rateLimiter registration (intake.test.ts block); sentinel bytes via
    storage; NO video fixtures, NO encoder dep (RESEARCH Lane-4 §4 — offline = sentinel only;
    container rejection needs no real bytes).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- vaultTranscribe && pnpm --filter @pikar/backend exec tsc -p . --noEmit</automated>
  </verify>
  <done>Sentinel spine, container rejection, governed stop, audit needle scans, truncation all green offline; no import of intake.ts or llm.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live container smoke on the lane's own deployment (non-negotiable)</name>
  <what-built>transcribeDoc: vault videos transcribed through the existing hosted rail, governed, audited counts-only, feeding the ingest seam.</what-built>
  <action>Automate first: start `npx convex dev` in this worktree, run `pnpm dev`, and stage the upload path so the human only has to drop in a short real mp4 and webm and confirm the results below.</action>
  <verify>Manual — human confirms container acceptance live per the steps below.</verify>
  <done>mp4 + webm transcripts land through the seam on the lane deployment; duration-priced spend recorded; unsupported container fails honestly.</done>
  <how-to-verify>
    Container acceptance is only provable live (RESEARCH: MEDIUM confidence until this smoke).
    Claude automates first, you confirm:
    1. Claude runs `npx convex dev` in this worktree and pushes one SHORT real mp4 (&lt; 8 MiB,
       with clearly spoken words) through the vault upload path (UI via `pnpm dev`, or a small
       script) — you can supply any short clip, e.g. a phone recording.
       Expected: pill walks to processing/ready; detail/search surfaces words actually spoken;
       duration-priced spend recorded; audit row is counts-only.
    2. Same with one short webm.
    3. Optionally rename a .mov upload → expect the failed badge with
       "unsupported_video_container" (the honest path; UI badge rendering itself is Lane 3's —
       verify via the doc row/status if Lane 3 isn't merged into this worktree yet).
  </how-to-verify>
  <resume-signal>Type "approved" (mp4 + webm transcribed live, spend recorded) or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- vaultTranscribe` green offline
- Backend typecheck clean; `node scripts/check-playbooks.mjs` exit 0 (Lane-4 playbook subsection in-commit)
- Needle greps: transcript text absent from every audit/log call site; zero imports from intake.ts/llm.ts/vaultExtract.ts
- Live smoke: mp4 + webm accepted by the endpoint, transcript through the seam, spend recorded
</verification>

<success_criteria>
- EXTR-I proven offline via sentinel + live via real containers on this lane's deployment
- Zero files outside vaultTranscribe.ts / vaultTranscribe.test.ts changed (playbook subsection + bookkeeping excepted)
- Zero new dependencies, zero skills.ts edits, zero schema edits
</success_criteria>

<output>
After completion, create `.planning/phases/03.8-vault-document-extraction/03.8-05-SUMMARY.md`.
Merge the lane branch to `main` and announce.
</output>
