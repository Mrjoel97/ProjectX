---
phase: 08-self-improvement
plan: 07
type: execute
wave: 5
depends_on: ["08-03", "08-04", "08-05"]
files_modified:
  - skillopt/envs/pikar_cockpit/__init__.py
  - skillopt/envs/pikar_cockpit/dataloader.py
  - skillopt/envs/pikar_cockpit/rollout.py
  - skillopt/envs/pikar_cockpit/adapter.py
  - skillopt/envs/pikar_cockpit/configs/pikar_cockpit/default.yaml
  - skillopt/envs/pikar_cockpit/skills/initial.md
  - skillopt/requirements.txt
  - .github/workflows/skillopt.yml
autonomous: true
requirements: [IMPR-02]
must_haves:
  truths:
    - "The Python env package loads the scrubbed export JSON and splits it train/valid"
    - "rollout.py scores an edit by executing the REAL cockpit skill via `convex run llm:runCockpitAgent` (no second engine)"
    - "The CI job ships DORMANT: it reads optimizerConfig.enabled and no-ops when false (kill switch)"
    - "The workflow runs on a cron AND manual workflow_dispatch (the manual kick)"
    - "The scheduled cron runs the optimize pipeline ONLY when optimizerEligibility (08-03) reports a breach; workflow_dispatch bypasses eligibility for manual runs"
  artifacts:
    - path: "skillopt/envs/pikar_cockpit/dataloader.py"
      provides: "SplitDataLoader over the /skillopt/export JSON"
    - path: "skillopt/envs/pikar_cockpit/rollout.py"
      provides: "run_batch that shells to convex run llm:runCockpitAgent with a pinned candidate"
    - path: ".github/workflows/skillopt.yml"
      provides: "cron + workflow_dispatch batch runner, kill-switch gated"
      contains: "workflow_dispatch"
  key_links:
    - from: ".github/workflows/skillopt.yml"
      to: "optimizerConfig.enabled"
      via: "reads it via convex run as step 1; exits 0 (no-op) if false"
      pattern: "optimizerConfig|enabled"
    - from: ".github/workflows/skillopt.yml"
      to: "optimizerEligibility (08-03)"
      via: "cron gate — proceeds only when eligible===true; workflow_dispatch bypasses"
      pattern: "optimizerEligibility|eligible"
    - from: "rollout.py"
      to: "convex run llm:runCockpitAgent"
      via: "subprocess to node (never a shell); pins skillVersions to the candidate"
      pattern: "runCockpitAgent"
---

<objective>
The offline SkillOpt batch runner: a Python env package glued to the export + write-back endpoints,
and a GitHub Actions workflow that runs it DORMANT.

Purpose: IMPR-02 deployment — an offline SkillOpt batch job on a CI cron (+ workflow_dispatch), NOT a
hosted sidecar (locked decision). The env package reuses the eval harness's proven `convex run
llm:runCockpitAgent` invocation to score edits — no second cockpit engine. The exact SkillOpt v0.2.0
YAML split keys are LOW-confidence upstream (RESEARCH OQ2) and are VERIFIED in the Plan 08 dry-run;
this plan builds everything the TS seam already pinned (export JSON shape + write-back contract).
Output: skillopt/envs/pikar_cockpit/*, requirements.txt (skillopt==0.2.0), .github/workflows/skillopt.yml.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-self-improvement/08-RESEARCH.md
@.planning/research/SKILLOPT.md
@packages/backend/scripts/run-eval-golden.mjs

<interfaces>
<!-- EXPORT JSON CONTRACT (pinned in Plan 04): { skillName, items:[{ id, task_description, conversation,
     hard, soft, skillVersion, split:"train"|"valid" }] }. dataloader.py consumes this verbatim. -->
<!-- WRITE-BACK CONTRACT (Plan 05): POST /skillopt/writeback { name, body, runId, negativeRate,
     sampleCount, tenantId } (Bearer SKILLOPT_TOKEN) → { ok, fromVersion, toVersion }. -->
<!-- run-eval-golden.mjs is the EXACT invocation rollout.py replicates:
     must("llm:runCockpitAgent", { tenantId, threadId, planId, text, skillVersions:{ [name]:version } }).
     Success is judged by CLI OUTPUT, not exit code (Node24 crash); tenantIds contain "|" so call node
     DIRECTLY via subprocess, never through a shell (RESEARCH Pitfall 6). -->
<!-- SkillOpt v0.2.0 env-package files + signatures (RESEARCH § env-package contract): SplitDataLoader
     .load_split_items(split_path); run_batch(*, items, skill_content, out_root, workers, max_completion_tokens)
     persisting <out_root>/predictions/<id>/conversation.json and returning dicts with id/hard/soft;
     EnvAdapter (build_train_env/build_eval_env/rollout/get_task_types). Copy the searchqa config as the
     template; the split-path YAML keys are verified in the dry-run. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pikar_cockpit SkillOpt env package</name>
  <files>skillopt/envs/pikar_cockpit/__init__.py, skillopt/envs/pikar_cockpit/dataloader.py, skillopt/envs/pikar_cockpit/rollout.py, skillopt/envs/pikar_cockpit/adapter.py, skillopt/envs/pikar_cockpit/configs/pikar_cockpit/default.yaml, skillopt/envs/pikar_cockpit/skills/initial.md, skillopt/requirements.txt</files>
  <action>
    Build the env package to the v0.2.0 contract (RESEARCH § env-package contract):
    - requirements.txt: `skillopt==0.2.0` (pinned — pre-1.0, re-verify keys at build time).
    - dataloader.py: a SplitDataLoader whose load_split_items(split_path) reads the exported items and
      returns the ones whose "split" matches (train vs valid_unseen); each item already has "id".
    - rollout.py: run_batch(*, items, skill_content, out_root, workers=4, max_completion_tokens=4096):
      for each item, write skill_content to a temp candidate (POST /skillopt/writeback OR a dev pin) and
      call `convex run llm:runCockpitAgent` with skillVersions pinned to that candidate — via
      subprocess to NODE directly (never a shell; tenantId has "|"), parsing stdout JSON, judging success
      by output not exit code. Persist <out_root>/predictions/<id>/conversation.json and return dicts with
      id + hard (the exported reward) + soft. ponytail: reuse run-eval-golden.mjs's must() invocation
      shape rather than inventing a new call.
    - adapter.py: thin EnvAdapter wiring (build_train_env/build_eval_env/rollout/get_task_types).
    - configs/pikar_cockpit/default.yaml: conservative hyperparameters — LOW edit budget / learning_rate
      and a TINY valid split (RESEARCH Pitfall 3: each edit re-rolls live LLM calls; keep the dry-run cheap).
      Copy searchqa/default.yaml as the template. Add a comment flagging the split-path keys as verified-in-dry-run.
    - skills/initial.md: a placeholder note that the current active cockpit-agent body is pulled at run
      start (the CI job fetches it); do not hardcode a stale prompt (CLAUDE.md §5).
    Leave ONE runnable check: a `python -m` / `__main__` self-check in dataloader.py that asserts the
    split partition of a tiny inline fixture matches the export "split" field (no network, no SkillOpt).
  </action>
  <verify>
    <automated>python skillopt/envs/pikar_cockpit/dataloader.py && python -c "import ast,glob,sys; [ast.parse(open(f,encoding='utf-8').read()) for f in glob.glob('skillopt/envs/pikar_cockpit/*.py')]; print('PY_OK')"</automated>
  </verify>
  <done>Env package parses; the dataloader self-check passes; rollout replicates the eval invocation shape.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions batch runner (dormant, kill-switch gated)</name>
  <files>.github/workflows/skillopt.yml</files>
  <action>
    Workflow with `on: { schedule: [{ cron: "..." }], workflow_dispatch: {} }` (the cron is the future
    cadence; the manual kick is workflow_dispatch). Steps:
    1. KILL-SWITCH GATE (first step): read optimizerConfig.enabled via `convex run
       optimizerConfig:getOptimizerConfig` (or the eligibility query); if enabled is false → echo
       "optimizer dormant — skipping" and EXIT 0 (no-op). This is the belt-and-suspenders companion to
       disabling the schedule (RESEARCH Pitfall 5). Ships DORMANT: the Plan-01 default is false.
    2. ELIGIBILITY GATE (after the kill switch, BEFORE pip-install/export): the LOCKED trigger policy
       is "threshold breach + manual kick" (08-CONTEXT Deferred Ideas explicitly defers the nightly
       SkillOpt-Sleep schedule) — so an enabled cron must NOT run the pipeline every tick. Call
       `convex run optimizerEligibility:optimizerEligibility` (08-03) and branch on the event:
       - `github.event_name == 'workflow_dispatch'` (the manual kick / dry-run) → BYPASS eligibility, proceed.
       - else (scheduled cron) → proceed ONLY when `eligible === true`; otherwise echo
         "not eligible — skipping" and EXIT 0. This keeps the cron a breach-triggered loop, never a
         disguised nightly sleep. ponytail: parse the `eligible` field from the `convex run` JSON stdout
         (output-not-exit-code, RESEARCH Pitfall 6) — no extra tooling.
    3. (only if enabled AND (dispatched OR eligible)) install `pip install -r skillopt/requirements.txt`; GET /skillopt/export
       (Bearer SKILLOPT_TOKEN secret) → export.json; run `python scripts/train.py --config
       configs/pikar_cockpit/default.yaml --out_root outputs/run` → best_skill.md; POST /skillopt/writeback
       with the best_skill.md + runId + negativeRate/sampleCount (from the eligibility query); then trigger
       `pnpm eval:golden --skill cockpit-agent@<toVersion>` to record evidence.
    Secrets used: SKILLOPT_TOKEN, CONVEX_URL/deploy key, OPENAI_API_KEY. Document them in the workflow.
    The workflow is NOT armed for real runs this phase (dormant); its correctness is confirmed by the
    Plan 08 dry-run (which may run the steps manually / via workflow_dispatch).
    ponytail: no matrix, no caching cleverness — one linear job; add complexity only when a real cadence exists.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/skillopt.yml',encoding='utf-8')); assert 'workflow_dispatch' in d['on'] or 'workflow_dispatch' in d[True]; print('YML_OK')"</automated>
  </verify>
  <done>Valid workflow with cron + workflow_dispatch; step 1 no-ops when kill switch off; step 2 runs the cron pipeline only on an optimizerEligibility breach (workflow_dispatch bypasses).</done>
</task>

</tasks>

<verification>
- Python files parse; dataloader self-check green; skillopt.yml is valid YAML with workflow_dispatch.
- The kill-switch gate is the FIRST workflow step; the runner ships dormant (default enabled=false).
- The scheduled cron gates on optimizerEligibility (breach-only); only workflow_dispatch bypasses it — no disguised nightly sleep.
- Exact SkillOpt YAML split keys are flagged for verification in the Plan 08 dry-run.
</verification>

<success_criteria>
IMPR-02 deployment: an offline, kill-switch-gated, dormant CI batch runner wired to the export +
write-back endpoints, scoring edits via the real cockpit skill.
</success_criteria>

<output>
After completion, create `.planning/phases/08-self-improvement/08-07-SUMMARY.md`.
</output>
