---
phase: 08-self-improvement
plan: 06
type: execute
wave: 5
depends_on: ["08-01", "08-02", "08-05"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/app/(app)/ops/page.tsx
  - packages/backend/convex/skills.ts
  - packages/backend/convex/optimizerConfig.ts
autonomous: true
requirements: [IMPR-01, IMPR-02, IMPR-03]
must_haves:
  truths:
    - "The delivered response surface shows a thumbs up/down + optional comment control, editable/undoable"
    - "The ops page shows the optimizer kill switch and can flip it (writes optimizerConfig.enabled)"
    - "The ops page lists a ready candidate with its before/after + evidence and a one-click activate"
    - "Activate routes through activateSkill's EVAL_GATE — an unevaluated candidate cannot go live"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "feedback control on the delivered ReportCard rows"
      contains: "submitFeedback"
    - path: "apps/web/app/(app)/ops/page.tsx"
      provides: "optimizer kill-switch toggle + candidate review/activate panel"
      contains: "optimizerConfig"
  key_links:
    - from: "cards.tsx feedback control"
      to: "feedback.submitFeedback / myFeedback"
      via: "useMutation/useQuery keyed by the delivered requestId"
      pattern: "submitFeedback|myFeedback"
    - from: "ops/page.tsx activate button"
      to: "skills.activateCandidate → activateSkill EVAL_GATE"
      via: "owner-gated mutation"
      pattern: "activate"
---

<objective>
Put the human controls on surfaces the owner already uses: the feedback thumbs on the delivered
response, and the kill switch + candidate-activate on the ops page.

Purpose: IMPR-01 UI (feedback control) + IMPR-02/03 operator surface (kill switch, before/after diff
+ evidence, one-click activate). CONTEXT is explicit: reuse the delivered PlanCard/ReportCard surface
and the Phase-7 ops page — NO new bespoke screen.
Output: the feedback control on ReportCard rows; the optimizer panel on the ops page.

NOTE: Live render + the activate flow are HUMAN-VERIFIED in Plan 08's checkpoint (the dry-run + feedback
walkthrough), which is where the whole seam is proven end-to-end. This plan ships the built controls.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-self-improvement/08-CONTEXT.md
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@apps/web/app/(app)/ops/page.tsx

<interfaces>
<!-- READ docs/design/BRAND.md FIRST (CLAUDE.md §10): use globals.css tokens, no new component library,
     no amber (--held is the approval gate's alone), meaning never in colour alone (§6 a11y). -->
<!-- ReportCard (cards.tsx:429) renders one row per recipient from api.plans.reportForPlan; each row
     corresponds to a requests row (its _id is the requestId the feedback keys off). -->
<!-- Feedback API (Plan 02): submitFeedback({requestId, rating, comment?}), undoFeedback({requestId}),
     myFeedback({requestId}) → current row | null. -->
<!-- optimizerConfig (Plan 01): getOptimizerConfig, setOptimizerConfig({enabled}). -->
<!-- skills: activateSkill is internalMutation — Convex mutations can't runMutation, so add a thin
     owner-gated PUBLIC/tenant mutation (activateCandidate) that performs the SAME gated activation
     (reuse activateSkill's logic — factor a shared helper rather than duplicating the EVAL_GATE). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feedback control on the delivered ReportCard rows</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    In ReportCard (cards.tsx:429), for each DELIVERED recipient row (status "sent"), render a small
    feedback control:
    - Thumbs up / thumbs down buttons + an optional single-line comment field (revealed on thumbs-down,
      the qualitative "why" SkillOpt needs; optional on thumbs-up). NO 1–5 stars (locked decision).
    - Reads api.feedback.myFeedback({ requestId: row._id }) to render the persisted state; the active
      choice is visibly selected. Clicking the other thumb EDITS (submitFeedback); clicking the selected
      thumb again UNDOES (undoFeedback) — editable/undoable, a mis-tap doesn't poison the signal.
    - BRAND: reuse existing card/label idioms, globals.css tokens, no new library, no amber. The
      selected state must be distinguishable WITHOUT relying on colour alone (aria-pressed + a label/icon).
    ponytail: no debounce/optimistic-cache machinery — Convex reactivity re-renders myFeedback; the
    laziest correct control is two buttons + a text input wired to the three mutations.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>ReportCard sent rows render an editable/undoable thumbs±comment control wired to the feedback API; web typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Optimizer panel on the ops page (kill switch + candidate activate)</name>
  <files>apps/web/app/(app)/ops/page.tsx, packages/backend/convex/skills.ts, packages/backend/convex/optimizerConfig.ts</files>
  <action>
    1. skills.ts: add an owner-gated `activateCandidate` mutation (tenant/public, callable from the ops
       UI) that performs the SAME gated activation as activateSkill. Factor activateSkill's core
       (archive-current + EVAL_GATE + activate-target) into a shared helper so the gate is defined ONCE
       (CLAUDE.md §8 root-cause: never a second copy of the EVAL_GATE). Also add a query the ops panel
       reads: latest candidate row(s) for gated skills with { name, fromVersion=active, toVersion, evidence, body }
       (the before/after diff + evidence source). ponytail: a plain "newest candidate per gated skill" read.
    2. optimizerConfig.ts: add a public/tenant `setOptimizerEnabled({ enabled })` wrapper the ops toggle
       calls (it maps to the Plan-01 setOptimizerConfig upsert) + expose getOptimizerConfig to the client.
    3. ops/page.tsx: add an "Optimizer" section (StatTile idiom + a panel):
       - Kill-switch toggle bound to optimizerConfig.enabled (DORMANT/OFF is the shipped default; the
         label states it ships dormant until Phase 9).
       - A "Candidate ready" panel: when a gated-skill candidate exists, show fromVersion→toVersion, the
         before/after body diff (a simple side-by-side or unified text diff — no diff library, a plain
         line compare), the recorded evidence, and an Activate button calling activateCandidate. Activate
         is refused by EVAL_GATE if no passing eval evidence — surface that error inline (don't swallow it).
       BRAND tokens; no amber; a11y labels on the toggle/buttons.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test skills && pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Ops page flips the kill switch and activates a candidate through the shared EVAL_GATE; backend + web green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` + `pnpm --filter @pikar/backend test skills` green.
- The EVAL_GATE lives in ONE helper shared by activateSkill + activateCandidate (no duplicate gate).
- Live paint + the activate flow are owner-verified in Plan 08.
</verification>

<success_criteria>
IMPR-01 UI + IMPR-02/03 operator surface: feedback on the delivered response; kill switch + one-click
gated activate with before/after + evidence on the existing ops page.
</success_criteria>

<output>
After completion, create `.planning/phases/08-self-improvement/08-06-SUMMARY.md`.
</output>
