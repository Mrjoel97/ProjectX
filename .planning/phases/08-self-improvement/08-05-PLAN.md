---
phase: 08-self-improvement
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-04"]
files_modified:
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/convex/http.ts
  - packages/core/src/notificationTemplates.ts
  - packages/core/src/notificationTemplates.test.ts
autonomous: true
requirements: [IMPR-02, IMPR-03]
must_haves:
  truths:
    - "A SkillOpt best_skill.md POSTed back becomes a NEW candidate version (maxVer+1, status candidate)"
    - "Prior skill rows are immutable — write-back never patches a body/status to active"
    - "A non-gated skill name is rejected; an identical body is idempotent (no churn)"
    - "Every optimization writes ONE insert-only audit row: {skillName, fromVersion, toVersion, runId, negativeRate, sampleCount} — refs/counts only (§4)"
    - "The owner is notified 'candidate ready' via the existing notify choke point"
  artifacts:
    - path: "packages/backend/convex/skills.ts"
      provides: "insertCandidate internalMutation (external body → candidate row, gated + idempotent)"
      exports: ["insertCandidate"]
    - path: "packages/backend/convex/http.ts"
      provides: "authenticated POST /skillopt/writeback route"
      contains: "/skillopt/writeback"
    - path: "packages/core/src/notificationTemplates.ts"
      provides: "optimizer.candidate notification kind"
      contains: "optimizer.candidate"
  key_links:
    - from: "http.ts /skillopt/writeback"
      to: "skills.insertCandidate"
      via: "auth then ctx.runMutation — routes through the registry, never a raw patch"
      pattern: "insertCandidate"
    - from: "writeback handler"
      to: "audit (insert-only)"
      via: "records {skillName, fromVersion, toVersion, runId, negativeRate, sampleCount}"
      pattern: "toVersion"
---

<objective>
Accept a SkillOpt-authored skill body as a new CANDIDATE version through the registry's gate, and
record the optimization's before/after + triggering evidence.

Purpose: IMPR-02 write-back + IMPR-03 versioning/evidence. The write-back MUST route candidate →
owner → activateSkill's EVAL_GATE (never patch the registry). It writes an insert-only, refs/counts-only
audit row and notifies the owner. Rollback and the activate gate already exist (skills.ts) — this plan
adds only the candidate-insert seam + the audit + the notification kind.
Output: skills.insertCandidate, POST /skillopt/writeback, the optimizer.candidate notification kind.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-self-improvement/08-RESEARCH.md
@packages/backend/convex/skills.ts
@packages/backend/convex/http.ts

<interfaces>
<!-- skills.ts (verified): seedSkills' gated branch inserts version=maxVersion+1, status:"candidate"
     for a CHANGED body of a gated skill; activateSkill's EVAL_GATE blocks a candidate of a gated skill
     without passing evidence. isGatedSkill(name) / GATED_SKILLS from packages/contracts/src/skill.ts.
     The drift test asserts only SEED constants match their .md — an externally-authored candidate body
     is LEGAL (RESEARCH Pitfall 4, skills.test.ts:457-468). -->
<!-- audit is INSERT-ONLY (CLAUDE.md §3) and payloads are refs/hashes/ids/counts ONLY (§4). The
     optimization audit row carries {skillName, fromVersion, toVersion, runId, negativeRate, sampleCount}
     — NO skill body, NO user content. Use the existing audit insert seam. -->
<!-- notificationTemplates.ts: NotificationKind is a CLOSED union + NOTIFICATION_KINDS + MESSAGES Record.
     Add "optimizer.candidate" to all three (a missing key is a compile error). notify carries the kind
     only; refs (skillName, toVersion) ride structured fields, never the label (§4). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: skills.insertCandidate — external body → gated candidate row</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - insertCandidate({ name:"cockpit-agent", body:"<new>" }) on a registry with active v11 → inserts
      v12 status:"candidate"; v11 row is UNCHANGED (immutable), still active.
    - Returns { name, fromVersion:11, toVersion:12 } (the before/after the audit needs).
    - A non-gated name (isGatedSkill false) → throws/rejects (only gated skills go through the eval gate).
    - A body byte-identical to the NEWEST row → idempotent no-op (returns the existing version, inserts nothing).
    - Never sets status:"active" and never patches a prior row.
  </behavior>
  <action>
    New insertCandidate internalMutation mirroring seedSkills' gated-candidate branch but taking an
    EXTERNAL body arg: find max version for name; if !isGatedSkill(name) → reject; if body === newest
    row body → return existing (idempotent, the Pitfall-1 precedent); else insert
    { name, version:maxVersion+1, body, status:"candidate", createdAt }. Return { name, fromVersion:activeVersion, toVersion:maxVersion+1 }.
    Extend skills.test.ts with the four cases above (reuse the existing seed harness in that file).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test skills</automated>
  </verify>
  <done>External body → new candidate; priors immutable; non-gated rejected; identical body idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: POST /skillopt/writeback (audit + notify) + optimizer.candidate kind</name>
  <files>packages/backend/convex/http.ts, packages/core/src/notificationTemplates.ts, packages/core/src/notificationTemplates.test.ts</files>
  <action>
    1. notificationTemplates.ts: add "optimizer.candidate" to NotificationKind, NOTIFICATION_KINDS, and
       MESSAGES (a static label, e.g. "A new optimized skill candidate is ready for your review." — NO
       interpolation, §4). Extend notificationTemplates.test.ts's exhaustive-kinds coverage.
    2. http.ts: add `POST /skillopt/writeback`, httpAction, Bearer `SKILLOPT_TOKEN` auth (401 on mismatch,
       same as /export). Body JSON: { name, body, runId, negativeRate, sampleCount, tenantId }. On auth OK:
       - `const { fromVersion, toVersion } = await ctx.runMutation(internal.skills.insertCandidate, { name, body })`.
       - Insert ONE audit row via the existing insert-only audit seam:
         payload { skillName:name, fromVersion, toVersion, runId, negativeRate, sampleCount } — refs/counts ONLY (§3/§4).
       - `await ctx.runMutation(internal.notifications.notify, { tenantId, kind:"optimizer.candidate", message: notificationMessage("optimizer.candidate") })`.
       - Return `Response.json({ ok:true, fromVersion, toVersion })`.
       The candidate is NOT active — it awaits the owner's activateSkill click (Plan 06 ops control), which
       passes EVAL_GATE only after eval:golden records evidence (Plan 08 dry-run).
    Note the audit-payload shape is asserted in skills.test.ts or a small http-adjacent test; the §4
    static-label scan (llmRedaction.test.ts) already guards notify — extend it if it enumerates kinds.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test notificationTemplates && pnpm --filter @pikar/backend exec tsc -p tsconfig.json --noEmit 2>&1 | grep -v "\.test\.ts" | grep -c "error TS" | grep -qx 0 && echo WRITEBACK_OK</automated>
  </verify>
  <done>Write-back inserts a candidate, writes a refs-only optimization audit row, notifies the owner; new kind compiles.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test skills` + `pnpm --filter @pikar/core test notificationTemplates` green.
- http.ts source typechecks; the audit payload carries refs/counts ONLY (no body, no content).
- The candidate never reaches active without the owner's gated click.
</verification>

<success_criteria>
IMPR-02 write-back + IMPR-03 evidence: an optimized body lands as an eval-gated candidate with a
before/after + triggering-evidence audit trail and an owner notification — the registry stays immutable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-self-improvement/08-05-SUMMARY.md`.
</output>
