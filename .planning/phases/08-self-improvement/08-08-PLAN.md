---
phase: 08-self-improvement
plan: 08
type: execute
wave: 6
depends_on: ["08-06", "08-07"]
files_modified:
  - docs/playbooks/skill-registry.md
  - docs/playbooks/watch.json
autonomous: false
requirements: [IMPR-01, IMPR-02, IMPR-03]
must_haves:
  truths:
    - "The whole loop is proven end-to-end on cockpit-agent: export → SkillOpt → candidate → eval green → owner activate → active flips"
    - "The feedback control works on a real delivered response (thumbs, comment, edit, undo)"
    - "The kill switch reads dormant (OFF) at ship — no autonomous run against live traffic"
    - "The skill-registry playbook documents write-back + candidate provenance + the names-in-prose HARD BLOCKER for Phase 9"
    - "New Phase-8 code paths are registered under a playbook watch (the §9 Stop hook can protect them)"
  artifacts:
    - path: "docs/playbooks/skill-registry.md"
      provides: "write-back + candidate-provenance + kill-switch + names-in-prose-ceiling documentation"
      contains: "write-back"
    - path: "docs/playbooks/watch.json"
      provides: "watched-path registration for the new feedback/optimizer/skillopt paths"
      contains: "skilloptExport"
  key_links:
    - from: "watch.json"
      to: "the new Phase-8 source paths"
      via: "skill-registry.md (or a new entry) watches feedback/optimizer/http-skillopt/skillopt-env"
      pattern: "skillopt|feedback|optimizer"
---

<objective>
Prove the seam and close the phase: the manual cockpit-agent dry-run (proof-of-life), the feedback-UI
walkthrough, kill-switch dormancy, and the playbook/watch definition-of-done.

Purpose: the phase's proof-of-life (RESEARCH § Manual cockpit-agent dry-run) — build the FULL loop,
ship it DORMANT, and prove the seam with ONE owner-verified manual run. Plus CLAUDE.md §9 DoD: extend
the skill-registry playbook and register the new watched paths.
Output: an updated playbook + watch.json, and an owner-verified end-to-end dry-run.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-self-improvement/08-RESEARCH.md
@.planning/phases/08-self-improvement/08-CONTEXT.md
@docs/playbooks/skill-registry.md
@docs/playbooks/watch.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playbook + watch.json (definition-of-done)</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/watch.json</files>
  <action>
    1. skill-registry.md: add a section covering the Phase-8 write-back seam:
       - Candidate provenance: SkillOpt-authored bodies enter ONLY via skills.insertCandidate → POST
         /skillopt/writeback, land as `candidate`, reach `active` ONLY through the owner's gated
         activateCandidate/activateSkill EVAL_GATE (never a raw patch). Rollback = activateSkill(prior).
       - The optimization audit row shape ({skillName, fromVersion, toVersion, runId, negativeRate,
         sampleCount} — refs/counts only, §4) and the SEPARATE PII-scrubbed export plane (§4 spirit).
       - Kill switch: optimizerConfig.enabled (default OFF/dormant) read by the CI job + ops toggle.
       - HARD BLOCKER for Phase 9: `packages/pii` does NOT scrub person names in prose (RESEARCH
         Pitfall 2) — accepted only for the solo-owner/own-tenant beta export; MUST be resolved (NER)
         before Phase-9 multi-user. State it explicitly here as a gating item.
       - Bump the `Last verified` line.
    2. watch.json: register the new Phase-8 source paths so the §9 Stop hook protects them. Extend the
       skill-registry.md entry (or add a focused entry) to cover: packages/backend/convex/feedback.ts,
       optimizerConfig.ts, optimizerEligibility.ts, skilloptExport.ts, the /skillopt routes in http.ts
       (already watched? http.ts isn't currently watched — add it under this entry), skillopt/ (the
       Python env), .github/workflows/skillopt.yml, and packages/core/src/optimizerBreach.ts. The
       feedback UI (cards.tsx) is already under cockpit.md's watch — verify and leave it there.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs && python -c "import json; json.load(open('docs/playbooks/watch.json',encoding='utf-8')); print('WATCH_OK')"</automated>
  </verify>
  <done>Playbook documents the write-back/candidate/kill-switch/names-in-prose blocker; watch.json is valid and covers the new paths; check-playbooks exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual cockpit-agent dry-run (proof-of-life) + feedback-UI walkthrough</name>
  <action>Owner-run verification only (no code) — perform the dry-run + walkthrough in <how-to-verify> and confirm the seam end-to-end.</action>
  <what-built>
    The full Phase-8 self-improvement loop, shipped DORMANT: feedback capture + attribution, scrubbed
    trajectory export, SkillOpt CI batch runner, candidate write-back through the eval gate, ops-page
    kill switch + candidate activate, and the owner notification.
  </what-built>
  <how-to-verify>
    Run the RESEARCH § "Manual cockpit-agent dry-run" (steps 1–6), owner-verified end-to-end. The point
    is the SEAM, not a measured quality gain — seed a small synthetic scored trajectory set for
    cockpit-agent if beta feedback is near-zero.

    A. FEEDBACK UI (IMPR-01): on a real delivered response (a sent ReportCard row), click thumbs-down,
       type a comment, confirm it persists (myFeedback), click thumbs-up to EDIT it, then click again to
       UNDO — the row should clear. Confirm BRAND compliance (tokens, no amber, a11y).
    B. EXPORT (IMPR-02): hit GET /skillopt/export with the Bearer SKILLOPT_TOKEN → confirm scrubbed JSON
       (no raw email/SSN/phone in any task_description/conversation/comment).
    C. OPTIMIZE: run `python scripts/train.py --config configs/pikar_cockpit/default.yaml --out_root
       outputs/dry` with a low edit budget + tiny valid split → confirm outputs/dry/best_skill.md emitted.
       VERIFY the exact SkillOpt v0.2.0 split-path YAML keys here (RESEARCH OQ2) and correct default.yaml if needed.
    D. WRITE-BACK (IMPR-02/03): POST best_skill.md to /skillopt/writeback → confirm a NEW cockpit-agent
       candidate row (version N+1, status candidate), prior rows immutable, an optimization audit row
       written (refs/counts only), and the owner "candidate ready" notification arrives.
    E. EVAL GATE: run `pnpm eval:golden --skill cockpit-agent@N+1` → confirm green (23/23) + evidence recorded.
    F. ACTIVATE (IMPR-02): on the ops page, review the before/after diff + evidence → click Activate →
       confirm activateSkill passes EVAL_GATE and the active version flips to N+1. Then confirm ROLLBACK:
       activate cockpit-agent N → restores instantly (status-exempt, no eval).
    G. DORMANCY (IMPR-02): confirm optimizerConfig.enabled reads false (the CI job would no-op on schedule).

    Before starting: full suite green — `pnpm --filter @pikar/backend test` and the touched-file suites
    (feedback, cockpit, optimizerConfig, optimizerEligibility, skilloptExport, skills) + `pnpm --filter
    @pikar/core test optimizerBreach notificationTemplates` + `pnpm --filter @pikar/web typecheck`.
  </how-to-verify>
  <resume-signal>Type "approved" once the dry-run flips the active version and rollback restores it, or describe what broke.</resume-signal>
</task>

</tasks>

<verification>
- check-playbooks exits 0; watch.json valid and covers the new paths.
- Owner-verified: export scrubbed, candidate written + audited + notified, eval green, activate flips, rollback restores, kill switch dormant.
</verification>

<success_criteria>
The full loop is proven end-to-end on cockpit-agent and ships DORMANT (kill switch OFF); the playbook
records the write-back/candidate/kill-switch model and the names-in-prose Phase-9 hard blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/08-self-improvement/08-08-SUMMARY.md`.
</output>
