---
phase: 03.6-agent-eval-gate
verified: 2026-07-15T21:15:00Z
status: passed
score: 4/4 must-haves verified
---

# Phase 3.6: Agent Eval Gate Verification Report

**Phase Goal:** Agent behavior changes stop being blind — a golden set of scripted conversations evaluates every new agent-skill version against the live model before it can be activated, and the production eval signals already being written (review outcomes, regenerate/fallback counts, DLQ rate, cost) become readable on the ops page.
**Verified:** 2026-07-15T21:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria used as must-haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A golden set of ~15–25 scripted cockpit conversations runs on demand against the live model via `pnpm eval:golden`, asserting on plan/tool state (never reply text), with a hard per-run cost cap and zero possibility of a real send or mailbox read | ✓ VERIFIED | `packages/backend/scripts/run-eval-golden.mjs` + 15 JSON fixtures in `packages/backend/scripts/eval-cases/` exist; `--self-check` exits 0 (`[eval:golden] self-check PASSED (15 fixtures valid, ...)`); asserts via `plans:getById` + `assertEvalCaseClean` (never `res.reply`); $1.00 cost cap coded (`totalCost > COST_CAP_USD` abort exit 2); live run recorded in 03.6-05-SUMMARY: run `049b868e`, 15/15, $0.0453, zero sends (all recipients `@golden.example`), zero mailbox reads (case 09 degradation) |
| 2 | `activateSkill` refuses to activate a never-before-active (candidate) version of a gated skill without a recorded passing eval run; rollback to a previously-active version is structurally exempt and always works | ✓ VERIFIED | `packages/backend/convex/skills.ts` contains `EVAL_GATE` check (`isGatedSkill(name) && target.status === "candidate" && !hasPassingEvidence(...)`) before any patch; `packages/contracts/src/skill.ts` has `GATED_SKILLS`, `isGatedSkill`, `hasPassingEvidence` (fail-closed JSON parse); `skills.test.ts` 30/30 green covering refuse/exempt/evidence-mismatch cases; live gate cycle proven in 03.6-05-SUMMARY (v5→v6: candidate published, activateSkill refused EVAL_GATE, `pnpm eval:golden --skill cockpit-agent@6` run `1ce0cf85` green → evidence recorded → activateSkill succeeded → rollback to archived v5 succeeded with zero evidence) |
| 3 | The ops page shows production eval signals — approve/edit/reject rates, regenerate count, fallback count, DLQ rate, cost per delivered plan — from existing telemetry/audit data, with no new write paths | ✓ VERIFIED | `packages/backend/convex/opsSignals.ts` exports `evalSignals` tenantQuery, zero `db.insert/patch/replace/delete` calls (grep confirms only a code comment referencing the rule, no actual write call); only schema change is the `by_tenant_created` index (not a write path); `apps/web/app/(app)/ops/page.tsx` renders `useQuery(api.opsSignals.evalSignals, {})` in an "Eval signals" stat grid above the dead-letter list, using brand CSS-variable tokens (`var(--card)`, `var(--ink)`, etc.); `opsSignals.test.ts` 6/6 green (rates, tenant isolation, windowing, refs-only shape); ops card human-approved live on 2026-07-15 per 03.6-05-SUMMARY |
| 4 | The legacy `executive-agent.classifier` skill row is archived, and the agent-runtime playbook's "no live-model eval gate" gap is closed (playbook updated in the same phase) | ✓ VERIFIED | `executiveAgentClassifierSkillBody` import removed from `skills.ts` (grep found zero occurrences); classifier seeds-array entry removed (only historical-constant comment references remain); `archiveSkill` internalMutation exists and was run live (`03.6-05-SUMMARY`: `{"archived": true}`, `seedSkills` re-run did not resurrect it, `getActiveSkill` now throws `NO_ACTIVE_SKILL`); `docs/playbooks/agent-runtime.md` Known-gaps section: `~~No live-model eval gate~~ — CLOSED (03.6)` with pointer to the eval-harness section + EVAL_GATE invariant; `Last verified: 2026-07-15 against 03.6-04` |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/contracts/src/skill.ts` | `GATED_SKILLS`, `isGatedSkill`, `hasPassingEvidence`, `EvalEvidence` | ✓ VERIFIED | All present, fail-closed JSON parse implemented |
| `packages/backend/convex/skills.ts` | `EVAL_GATE`, `recordEvalEvidence`, `getSkillVersion`, gated candidate-publish, `archiveSkill`, classifier removed | ✓ VERIFIED | All present; grep confirms `EVAL_GATE` string, no classifier body import |
| `packages/backend/convex/skills.test.ts` | gate refuse/exempt/evidence cases | ✓ VERIFIED | 30/30 tests green |
| `packages/backend/convex/schema.ts` | telemetry `by_tenant_created` index | ✓ VERIFIED | Present at line 235 |
| `packages/backend/convex/opsSignals.ts` | `evalSignals` tenantQuery, read-only | ✓ VERIFIED | Exports `evalSignals`; zero write calls |
| `packages/backend/convex/opsSignals.test.ts` | rate/isolation/windowing tests | ✓ VERIFIED | 6/6 green |
| `apps/web/app/(app)/ops/page.tsx` | Eval signals section | ✓ VERIFIED | `EvalSignals()` component wired to `api.opsSignals.evalSignals` |
| `packages/backend/convex/llm.ts` | `skillVersions` pin + `costUsd` in loop return | ✓ VERIFIED | Threaded through `runCockpitAgent` → `runAgentLoop` → `buildCockpitTools` → `draftDocument`; `costUsd` returned from `runAgentLoop`/`runCockpitAgent` |
| `packages/backend/convex/runCockpitAgent.test.ts` | pin-loads-pinned-body + costUsd cases | ✓ VERIFIED | 9/9 tests green |
| `packages/backend/scripts/run-eval-golden.mjs` | the runner | ✓ VERIFIED | Exists; `--self-check` exits 0 |
| `packages/backend/scripts/eval-cases/` | 15+ JSON fixtures | ✓ VERIFIED | Exactly 15 files, all four locked buckets + 2 injection probes present |
| `packages/backend/convex/smokeAssert.ts` | `assertEvalCaseClean` | ✓ VERIFIED | Present at line 539 |
| `packages/backend/package.json` / root `package.json` | `eval:golden` script | ✓ VERIFIED | Both wired (root forwards to backend filter) |
| `.planning/phases/03.6-agent-eval-gate/03.6-05-SUMMARY.md` | phase-close record with live-run evidence | ✓ VERIFIED | Contains run ids `049b868e` / `1ce0cf85`, cost, gate-cycle evidence row |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `skills.ts activateSkill` | `contracts/skill.ts` | `isGatedSkill` + `hasPassingEvidence` before patch | ✓ WIRED | Confirmed at skills.ts:75-89 |
| `skills.ts seedSkills` | `skills` table | gated changed-body branch inserts `candidate`, never archives active | ✓ WIRED | Confirmed at skills.ts:195+ |
| `ops/page.tsx` | `convex/opsSignals.ts` | `useQuery(api.opsSignals.evalSignals, {})` | ✓ WIRED | Confirmed at page.tsx:58 |
| `opsSignals.ts` | telemetry/audit/deadLetters tables | tenant-scoped indexed reads | ✓ WIRED | `by_tenant_created` index used; opsSignals.test.ts proves windowing/isolation |
| `llm.ts runCockpitAgent` | `skills.ts getSkillVersion` | `ctx.runQuery(internal.skills.getSkillVersion)` when pinned | ✓ WIRED | Confirmed at llm.ts:1238 (`const pin = skillVersions?.[COCKPIT_AGENT_SKILL]`) |
| `llm.ts runAgentLoop` | `runCockpitAgent` return | `costUsd` propagated | ✓ WIRED | Confirmed at llm.ts:1054/1259/1268 |
| `run-eval-golden.mjs` | `llm:runCockpitAgent` | `must()` spawn per fixture turn | ✓ WIRED | Live run 049b868e (15/15) confirms end-to-end wiring |
| `run-eval-golden.mjs` | `skills:recordEvalEvidence` | evidence written on all-green `--skill` runs | ✓ WIRED | Live gate cycle: evidence row inspected raw in 03.6-05-SUMMARY, exact `EvalEvidence` shape |
| `run-eval-golden.mjs` | `smokeAssert:assertEvalCaseClean` | standing-invariant check after every case | ✓ WIRED | Referenced in runner; exercised in live run |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|-------------|--------|----------|
| EVAL-01 | 03.6-01, 03.6-03, 03.6-04, 03.6-05 | Golden-set eval harness + eval-gated activation | ✓ SATISFIED | Marked `[x]` in REQUIREMENTS.md; gate + harness code verified live and in test suites |
| EVAL-02 | 03.6-02, 03.6-05 | Ops-page production eval signals from existing data, no new writes | ✓ SATISFIED | Marked `[x]` in REQUIREMENTS.md; `opsSignals.ts` verified read-only, ops card human-approved live |

No orphaned requirements — REQUIREMENTS.md maps only EVAL-01/EVAL-02 to Phase 3.6, both accounted for across the five plans' `requirements` frontmatter fields.

### Anti-Patterns Found

None. No `TODO`/`FIXME`/`PLACEHOLDER` markers, no stub returns, no empty handlers found in the phase's key files (`skills.ts`, `opsSignals.ts`, `llm.ts`, `run-eval-golden.mjs`, `ops/page.tsx`). The one pre-existing backend test failure (`audit.test.ts` — `auditCounts` component not registered) is documented in three separate SUMMARYs (03.6-01, 03.6-04, 03.6-05) as a pre-existing, unrelated issue and reproduces identically today (214/215 passed) — not a regression introduced by this phase.

### Human Verification Required

None outstanding. The phase's own plan 05 was a `checkpoint:human-verify` task and was already executed and approved by the user on 2026-07-15 (per 03.6-05-SUMMARY: "Task 2 human-verify APPROVED" covering the live golden run, the full gate cycle, classifier archival, and the ops card rendering in the browser). This verifier independently re-ran the offline-verifiable portions (vitest suites, self-check, typecheck, check-playbooks, grep guards, commit-hash presence) and confirms no drift from the recorded live evidence.

### Gaps Summary

No gaps. All four roadmap success criteria are structurally verified in the codebase (artifacts exist, are substantive, and are wired end-to-end) and were additionally confirmed live via a human-approved checkpoint whose evidence (run ids, evidence JSON, playbook closures) is independently checkable against the current repository state. Local re-execution of `pnpm --filter @pikar/backend exec vitest run skills opsSignals runCockpitAgent` (45/45 green), `run-eval-golden.mjs --self-check` (exit 0), `node scripts/check-playbooks.mjs` (exit 0), `pnpm --filter @pikar/web typecheck` (clean), and the full backend suite (214/215, the one documented pre-existing red) all reproduce the SUMMARYs' claims exactly.

---

*Verified: 2026-07-15T21:15:00Z*
*Verifier: Claude (gsd-verifier)*
