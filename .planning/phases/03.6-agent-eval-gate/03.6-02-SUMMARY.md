---
phase: 03.6-agent-eval-gate
plan: 02
subsystem: ops
tags: [convex, tenantQuery, telemetry, audit, dead-letters, eval-signals, ops-page]

# Dependency graph
requires:
  - phase: 02-runtime (telemetry/audit/deadLetters planes)
    provides: telemetry rows (decisionCounts/regenerateCount/reviewOutcome/costUsd), audit by_tenant_ts, deadLetters by_tenant/by_tenant_status
  - phase: 03.2.1-agent-driven-cockpit
    provides: cockpit send rows in telemetry (costUsd 0, decisionCounts {}, real reviewOutcome)
provides:
  - opsSignals.evalSignals read-only tenantQuery — the five locked EVAL-02 signal families (decision counts, reviewOutcome distribution, regenerate/fallback/DLQ counts, cost per delivered send) from EXISTING rows
  - telemetry by_tenant_created index (tenant-scoped time-windowed telemetry reads)
  - Ops-page "Eval signals" stat grid (brand tokens, honest lane-labeled copy)
affects: [03.6-04 (playbook close), 03.6-05 (human-verify checkpoint), skill-activation rollback decisions]

# Tech tracking
tech-stack:
  added: []
  patterns: [read-only signals module separate from insert-only audit.ts, windowed-audit-read rule (by_tenant_ts + gte, never un-windowed collect), honesty-by-framing for lane-skewed metrics]

key-files:
  created:
    - packages/backend/convex/opsSignals.ts
    - packages/backend/convex/opsSignals.test.ts
  modified:
    - packages/backend/convex/schema.ts
    - apps/web/app/(app)/ops/page.tsx
    - docs/playbooks/watch.json
    - docs/playbooks/agent-runtime.md

key-decisions:
  - "Signals module is read-only by construction: zero db.insert/patch/replace/delete (grep-proven); the only schema change is an index"
  - "Audit read is ALWAYS windowed via by_tenant_ts + gte — the table is unbounded and auditCounts cannot count per-type"
  - "Honesty by framing (Pitfall 6): gate decisions labeled pipeline-only, reviewOutcome distribution labeled as the cockpit approve-proxy, cost labeled pipeline-LLM-only"
  - "Ops page h1 split to 'Ops' with tracked-caps section labels (Eval signals / Dead letters); legacy dead-letter list JSX untouched"

patterns-established:
  - "opsSignals pattern: new read modules over governance planes go in their own file as tenantQuery — never reads added to audit.ts"
  - "Windowed compound-index reads for unbounded tables (by_tenant_created / by_tenant_ts + gte(since))"

requirements-completed: [EVAL-02]

# Metrics
duration: 10min
completed: 2026-07-15
---

# Phase 03.6 Plan 02: EVAL-02 Read Side Summary

**Read-only `opsSignals.evalSignals` tenantQuery computing all five locked production eval signals (gate decisions, reviewOutcome distribution, regenerate/llm.fallback/DLQ counts, cost per delivered send) from existing telemetry/audit/deadLetters rows, plus the telemetry `by_tenant_created` index and an honest-labeled stat grid on the ops page**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-15T15:28:55Z
- **Completed:** 2026-07-15T15:38:35Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Production eval signals are finally readable: a falling approve-rate after a skill activation IS the production eval, and rollback is already one click (EVAL-02, roadmap SC3 satisfied structurally)
- No new write path: opsSignals.ts contains zero db writes (grep-proven), the only schema change is an index (Convex backfills automatically)
- Tenant-scoped + time-windowed by construction: cross-tenant caller sees all zeros; the audit read is never an un-windowed collect; refs/counts-only return shape structurally asserted
- Pitfall 6 avoided in the UI: gate decisions labeled "(legacy pipeline lane)", reviewOutcome distribution labeled as the cockpit approve-proxy, cost labeled "Pipeline LLM cost / delivered send" with the rate-limiter caveat in a title attribute

## Task Commits

Each task was committed atomically (Task 1 was TDD):

1. **Task 1 (RED): failing eval-signals tests** - `388af70` (test)
2. **Task 1 (GREEN): by_tenant_created index + evalSignals tenantQuery** - `dccc349` (feat)
3. **Task 1 (docs): watch.json registration + agent-runtime EVAL-02 note** - `c8d0b66` (docs)
4. **Task 2: ops-page eval-signals stat grid** - `61aafd1` (feat)

## Files Created/Modified
- `packages/backend/convex/opsSignals.ts` - NEW read-only module: `evalSignals` tenantQuery folding telemetry (windowed via `by_tenant_created`), audit (windowed via `by_tenant_ts`, eventType filtered in memory), and deadLetters (tenant-prefix read, windowed in memory) into counts/rates only; 30-day default window; `costPerDeliveredUsd` is 0 (never NaN) when nothing delivered
- `packages/backend/convex/opsSignals.test.ts` - 6 convex-test cases: seeded-row sums/rates (pipeline + cockpit-style rows), windowed+filtered fallback count, DLQ new/total windowing, cross-tenant all-zeros isolation, telemetry windowing, refs-only structural shape
- `packages/backend/convex/schema.ts` - telemetry gains `.index("by_tenant_created", ["tenantId", "createdAt"])` — the ONLY schema change
- `apps/web/app/(app)/ops/page.tsx` - "Eval signals" stat grid above the dead-letter list (globals.css tokens, tracked-caps labels, StatTile per BRAND.md §5); legacy inline-styled dead-letter list untouched
- `docs/playbooks/watch.json` - opsSignals.ts + opsSignals.test.ts registered under agent-runtime.md (its list was reserved for Phase 3.6 paths)
- `docs/playbooks/agent-runtime.md` - EVAL-02 read-side key-file note (what evalSignals reads, the windowed-audit rule, the cockpit-zeros caveat); Last verified → 03.6-02

## Decisions Made
- Fixed reference instant + explicit `sinceMs` in every test for determinism (the handler's `Date.now()` default is a one-line fallback, not separately tested)
- Ops page `h1` changed from "Ops — dead letters" to "Ops" with tracked-caps section labels for the two sections — the dead-letter list JSX itself is byte-identical (ponytail: shortest working diff)
- reviewOutcome vocabulary rendered in fixed order so a zero-count outcome shows an honest 0 (BRAND.md §5 empty-state rule)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Regenerated Convex API bindings for the new module**
- **Found during:** Task 2 (ops-page typecheck)
- **Issue:** `api.opsSignals` missing from git-ignored `convex/_generated/api.d.ts` (codegen is boot-order produced, not committed) → web typecheck failed
- **Fix:** Ran `npx convex codegen` in packages/backend
- **Files modified:** none committed (`_generated/` is git-ignored)
- **Verification:** `pnpm --filter @pikar/web typecheck` clean
- **Committed in:** n/a (generated artifacts are not committed)

---

**Total deviations:** 1 auto-fixed (1 blocking, zero committed-file impact)
**Impact on plan:** None — routine codegen refresh required by boot order (CLAUDE.md §7). No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required. (The `by_tenant_created` index backfills automatically on the next deploy; `npx convex codegen` already pushed it to the dev deployment.)

## Next Phase Readiness
- All five locked signal families readable, tenant-scoped, windowed, zero new write paths — plan 05's human-verify checkpoint can visually confirm the card live
- agent-runtime.md watch list now active for Phase 3.6 paths; plan 04's fuller playbook close builds on this registration
- Verification: `vitest run opsSignals` 6/6 green; `pnpm --filter @pikar/web typecheck` clean; `check-playbooks.mjs` exit 0; grep guards clean (no raw builder import, no db writes in opsSignals.ts)

---
*Phase: 03.6-agent-eval-gate*
*Completed: 2026-07-15*

## Self-Check: PASSED

- All created files exist (opsSignals.ts, opsSignals.test.ts, SUMMARY.md)
- All 4 task commits found (388af70, dccc349, c8d0b66, 61aafd1)
- must_have artifact contents verified: `by_tenant_created` in schema.ts + opsSignals.ts (withIndex), `api.opsSignals.evalSignals` wired in ops/page.tsx
