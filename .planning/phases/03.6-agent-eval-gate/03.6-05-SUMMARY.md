---
phase: 03.6-agent-eval-gate
plan: 05
subsystem: agent-eval-harness
tags: [convex, eval-gate, golden-set, llm, skills, ops-signals, phase-gate]

# Dependency graph
requires:
  - phase: 03.6-agent-eval-gate plan 01
    provides: EVAL_GATE on activateSkill + recordEvalEvidence + seedSkills candidate-publish + archiveSkill
  - phase: 03.6-agent-eval-gate plan 02
    provides: opsSignals evalSignals query + ops-page eval-signals card
  - phase: 03.6-agent-eval-gate plan 03
    provides: skillVersions pin + costUsd threading through runCockpitAgent
  - phase: 03.6-agent-eval-gate plan 04
    provides: pnpm eval:golden runner + 15 fixtures + assertEvalCaseClean + evidence write
provides:
  - "Phase 3.6 live sign-off: all four roadmap success criteria observed on the real dev deployment (live model, real evidence writes, real browser)"
  - "First recorded golden-set baseline: run 049b868e, 15/15, $0.0453 — the reference point for future skill-version evals"
  - "cockpit-agent v6 live-active with recorded EvalEvidence (run 1ce0cf85) — the first version ever activated THROUGH the gate"
  - "executive-agent.classifier archived on the live deployment, seedSkills-proof (SC4 closed live)"
affects: [03.7-inbox-briefing, phase-8-skillopt]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Eval runs require a persistent `npx convex dev` process — per-command cold starts of the local backend produce InternalServerError + banner-contaminated runner output (operational prerequisite, not a harness bug)"
    - "Model-nondeterminism flakes (03-happy-group chose individual mode once) are absorbed by the one-retry policy and judged against fixture intent, not auto-tightened"

key-files:
  created:
    - .planning/phases/03.6-agent-eval-gate/03.6-05-SUMMARY.md
  modified:
    - packages/contracts/skills/cockpit-agent.md
    - packages/contracts/src/skills/cockpitAgent.ts

key-decisions:
  - "The gate-cycle candidate (cockpit-agent v6, trailing-newline body edit) was left ACTIVE on the deployment and its source mirror committed (6f0274f) so the repo matches the live active body"
  - "The 03-happy-group single failure across runs was judged a nondeterministic model flake, not a fixture defect — no fixture tightening; noted for future watching"
  - "The first-attempt infra failure (12 cases red from convex cold-starts) was diagnosed as environment, not harness — resolution is a running convex dev process, recorded as an operational prerequisite"

patterns-established:
  - "Gate cycle live recipe proven: trivial .md edit → mirror regen → seedSkills (candidate vN, old stays active) → activateSkill refused EVAL_GATE → pnpm eval:golden --skill name@N → evidence on row → activateSkill passes → rollback to archived prior succeeds with zero evidence"

requirements-completed: [EVAL-01, EVAL-02]

# Metrics
duration: ~85min (incl. live human-verify session)
completed: 2026-07-15
---

# Phase 3.6 Plan 05: Phase Gate Summary

**Phase 3.6 signed off live: first real golden run 15/15 at $0.0453 (cap $1.00, zero sends), the full EVAL_GATE cycle refused-then-unlocked-then-rollback-exempt on cockpit-agent v5→v6, the classifier archived seedSkills-proof, and the ops eval-signals card human-verified in the browser**

## Performance

- **Duration:** ~85 min (automated sweep + live proofs + human-verify session)
- **Started:** 2026-07-15T16:40:00Z (approx, after 03.6-04 close)
- **Completed:** 2026-07-15T18:02:00Z
- **Tasks:** 2/2 (Task 1 auto sweep; Task 2 human-verify APPROVED)
- **Files modified:** 2 (the v6 mirror pair) + this SUMMARY
- **Total live-model spend across all runs:** ~$0.16

## Accomplishments

All four live proofs executed on the local dev deployment (`local:local-joel_feruzi-pikar_ai_50c69`), 2026-07-15:

### Task 1 — Automated phase-gate sweep (verification-only, no commit)

- `skills` / `opsSignals` / `runCockpitAgent` suites: **45/45 green**
- Full backend suite: **214/215** — the sole red is the documented pre-existing `audit.test.ts` failure (auditCounts-unregistered); nothing NEW red
- `pnpm --filter @pikar/web typecheck` clean
- `run-eval-golden.mjs --self-check` exit 0; `check-playbooks.mjs` exit 0
- All grep guards pass: `EVAL_GATE` in skills.ts; no `SMOKE::` under eval-cases/; `executiveAgentClassifierSkillBody` not imported by skills.ts; zero db writes in opsSignals.ts

### Task 2 — Live phase gate (human-verify APPROVED)

**1. Classifier archival (SC4):** `skills:archiveSkill {"name":"executive-agent.classifier"}` → `{"archived": true}`; a `seedSkills` re-run did NOT resurrect it; `getActiveSkill` now throws `NO_ACTIVE_SKILL: executive-agent.classifier`. The zombie stays archived.

**2. First live golden run (SC1):** run **`049b868e`** — **15/15 passed**, total cost **$0.0453** (cap $1.00), zero real sends (all recipients @golden.example), zero mailbox reads (case 09 degradation graceful).

**3. Full gate cycle (SC2), all five steps live:**
- (a) Trivial edit (trailing newline) to canonical `cockpit-agent.md` + regenerated `cockpitAgent.ts` mirror → `seedSkills` published **candidate v6**, v5 stayed active.
- (b) `activateSkill v6` → **refused**: `EVAL_GATE: cockpit-agent v6 has no recorded passing eval run (run pnpm eval:golden --skill cockpit-agent@6)`.
- (c) `pnpm eval:golden --skill cockpit-agent@6` → run **`1ce0cf85`**, 15/15 passed (one retry: 12-attach-remove, transient DNS EAI_AGAIN), cost **$0.0462** → evidence recorded on the v6 row. Raw row inspected: `{"runner":"eval:golden","runId":"1ce0cf85","pass":true,"casesPassed":15,"casesTotal":15,"retriedCases":["12-attach-remove"],"costUsd":0.0462354,"model":"openai/gpt-4o-mini","skillVersions":{"cockpit-agent":6},"ts":1784137259408}` — refs/counts only, version-pinned.
- (d) Re-ran `activateSkill v6` → **SUCCEEDED**; v6 active, v5 archived.
- (e) **Rollback exemption:** `activateSkill v5` (archived, no evidence) → immediate success; then rolled forward to v6. Source mirror committed as `6f0274f` so the repo matches the live active body; skills suite re-verified 30/30 green after the commit.

**4. Ops card (EVAL-02/SC3):** `/ops` ("Compliance" rail item) verified by the user in the browser — eval-signals card renders real numbers, brand tokens intact, legacy dead-letter list intact below. APPROVED.

## Task Commits

1. **Task 1: Automated phase-gate sweep** — no commit (verification-only, per plan)
2. **Task 2: Live phase gate (v6 mirror, repo-matches-deployment)** — `6f0274f` (chore)

**Plan metadata:** see final docs commit for this SUMMARY + STATE/ROADMAP updates.

## Files Created/Modified

- `packages/contracts/skills/cockpit-agent.md` — trailing-newline body edit (the gate-cycle candidate trigger)
- `packages/contracts/src/skills/cockpitAgent.ts` — regenerated mirror of the now-live v6 body

## Decisions Made

- Left the deployment on **v6** (the gate-cycle candidate) rather than reverting — the edit is a trailing newline, and keeping it means the deployed active body has recorded eval evidence and matches the committed mirror.
- Judged the one-off `03-happy-group` failure (model chose individual mode despite explicit "one group email... together on the same thread") a **nondeterministic model flake**, not a fixture defect or prompt regression — it passed all subsequent runs. No fixture change; watch on future runs.
- Diagnosed the first-attempt 12-case infra failure as **environment** (per-command convex cold starts), not harness: each bare `npx convex run` cold-started the local backend → InternalServerError + a "waiting for local backend to start..." banner contaminating parsed output.

## Deviations from Plan

None - plan executed exactly as written. (The v6 mirror commit `6f0274f` is the plan's own gate-cycle step 3a/3e "leave the deployment on the intended body" convention, not a deviation.)

## Issues Encountered

- **Eval runs need a persistent backend:** running `pnpm eval:golden` without a live `npx convex dev` process fails ~all cases with InternalServerError from per-command local-backend cold starts. **Operational prerequisite: keep `npx convex dev` running during eval runs.** (Candidate playbook line for agent-runtime.md's eval-harness section.)
- **Transient DNS flake:** case 12-attach-remove hit `EAI_AGAIN` once during the pinned v6 run; the one-retry policy absorbed it (recorded in `retriedCases`).
- **/ops discoverability:** the ops page lives at `/ops` via the `(app)` route group — `/dashboard/ops` 404s. Minor UX note; the "Compliance" rail item resolves correctly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Phase 3.6 complete pending orchestrator `verify_phase_goal`** — all four roadmap SCs observed live: SC1 (golden set on demand, cost-capped, zero-send), SC2 (gated refusal / evidence unlock / rollback exemption), SC3 (ops signals readable), SC4 (classifier archived, playbook gap closed in 03.6-04, confirmed live here).
- Phase 3.7 (inbox briefing) can extend the golden set with briefing/injection-probe fixtures per its roadmap SC5; Phase 8 SkillOpt plugs into this exact harness + evidence pipeline.
- Baseline for future comparisons: 15/15 at ~$0.045/run on gpt-4o-mini; known flake surface: 03-happy-group (mode choice), transient DNS on attachment cases.

## Self-Check: PASSED

- `6f0274f` present in git log (v6 mirror pair, 2 files).
- Both modified source files exist on disk (`cockpit-agent.md`, `cockpitAgent.ts`).
- This SUMMARY exists at the planned path.
- Live proofs are deployment-state + human-verified (run ids 049b868e / 1ce0cf85 recorded above; ops card approved by user) — not re-runnable offline, recorded as evidence per the plan's checkpoint protocol.

---
*Phase: 03.6-agent-eval-gate*
*Completed: 2026-07-15*
