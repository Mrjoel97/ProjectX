---
phase: 03.6-agent-eval-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/opsSignals.ts
  - packages/backend/convex/opsSignals.test.ts
  - apps/web/app/(app)/ops/page.tsx
  - docs/playbooks/watch.json
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "The ops page shows production eval signals — approve/edit/reject decision counts, reviewOutcome distribution, regenerate count, llm.fallback count, DLQ rate, cost per delivered send — computed from EXISTING telemetry/audit/deadLetters rows"
    - "No new write path exists: opsSignals is read-only (a tenantQuery), and the only schema change is an index"
    - "Signals are tenant-scoped and time-windowed; a cross-tenant caller sees zeros; the audit read is never an un-windowed collect"
    - "The card's labels are honest about what rows contain (cockpit sends carry costUsd 0 and empty decisionCounts — the reviewOutcome distribution is the cockpit approve-proxy)"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "telemetry by_tenant_created index (an index is not a write path; Convex backfills automatically)"
      contains: "by_tenant_created"
    - path: "packages/backend/convex/opsSignals.ts"
      provides: "evalSignals tenantQuery returning counts/rates only"
      exports: ["evalSignals"]
    - path: "packages/backend/convex/opsSignals.test.ts"
      provides: "seeded-row rate assertions + tenant isolation + windowing"
    - path: "apps/web/app/(app)/ops/page.tsx"
      provides: "Eval signals section on the existing ops page"
      contains: "evalSignals"
  key_links:
    - from: "apps/web/app/(app)/ops/page.tsx"
      to: "convex/opsSignals.ts"
      via: "useQuery(api.opsSignals.evalSignals, {})"
      pattern: "api\\.opsSignals\\.evalSignals"
    - from: "packages/backend/convex/opsSignals.ts"
      to: "telemetry/audit/deadLetters tables"
      via: "tenant-scoped indexed reads (by_tenant_created / by_tenant_ts / by_tenant_status)"
      pattern: "withIndex"
---

<objective>
Make the production eval signals that are already being written finally readable: one new read-only `opsSignals.evalSignals` tenantQuery over existing telemetry/audit/deadLetters data, one telemetry index, and one new section on the existing ops page. No new writes, no new tables (locked decision).

Purpose: EVAL-02. A falling approve-rate after a skill activation IS the production eval; rollback is already one click. This is the read-side half of "agent behavior changes stop being blind."
Output: `convex/opsSignals.ts` + tests + the ops-page eval-signals card.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.6-agent-eval-gate/03.6-CONTEXT.md
@.planning/phases/03.6-agent-eval-gate/03.6-RESEARCH.md  (Pattern 6 — verified field semantics; Pitfall 6 — honest framing)
@packages/backend/convex/schema.ts
@packages/backend/convex/deadLetters.ts  (newCount/listNew tenantQuery precedent)
@packages/backend/convex/lib/functions.ts  (tenantQuery wrapper — §2, raw builders banned)
@apps/web/app/(app)/ops/page.tsx
@docs/design/BRAND.md  (§10 — read BEFORE any UI work; use globals.css tokens)

<interfaces>
From packages/backend/convex/schema.ts (verified):
```ts
telemetry: defineTable({
  tenantId: v.string(), correlationId: v.string(), requestId: v.id("requests"),
  tokensIn: v.number(), tokensOut: v.number(), costUsd: v.number(), durationMs: v.number(),
  decisionCounts: v.any(),      // record<string,number>; keys approve/edit/reject/regenerate — POPULATED ONLY by the legacy pipeline; cockpit rows carry {}
  regenerateCount: v.number(),  // 0 on cockpit rows
  reviewOutcome: v.string(),    // "sent"/"rejected"/"expired"/"failed"/"blocked" — covers cockpit rows too
  createdAt: v.number(),
}).index("by_correlation", ["correlationId"])   // NO tenant index yet — this plan adds one

audit: ...  .index("by_tenant_ts", ["tenantId", "ts"])          // llm.fallback events read via this, windowed
deadLetters: ... .index("by_tenant", ["tenantId"]).index("by_tenant_status", ["tenantId", "status"])
```

From apps/web/app/(app)/ops/page.tsx (existing): `"use client"` page, `useQuery(api.deadLetters.listNew)` dead-letter list with inline styles (pre-brand — do NOT rewrite the legacy list, ponytail).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: telemetry tenant index + opsSignals.evalSignals tenantQuery + tests + watch registration</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/opsSignals.ts, packages/backend/convex/opsSignals.test.ts, docs/playbooks/watch.json, docs/playbooks/agent-runtime.md</files>
  <behavior>
    New opsSignals.test.ts (convex-test; register any needed components mirroring existing test setups; seed rows via raw t.run db.insert like audit.test.ts precedent):
    - Rates computed: seed tenant A with 3 pipeline-style telemetry rows (decisionCounts {approve:1}/{edit:1}/{reject:1, regenerate:2}, regenerateCount, costUsd 0.02 each, reviewOutcome "sent"/"sent"/"rejected") + 1 cockpit-style row (decisionCounts {}, costUsd 0, reviewOutcome "sent") → evalSignals returns decisionCounts sums {approve:1, edit:1, reject:1, regenerate:2}, reviewOutcomes {sent:3, rejected:1}, totalCostUsd 0.06, deliveredCount 3, costPerDeliveredUsd 0.02, requestCount 4.
    - Fallback count: seed audit rows for tenant A — 2 with eventType "llm.fallback" inside the window, 1 "llm.fallback" OLDER than the window, 5 other eventTypes → fallbackCount === 2 (windowed, filtered).
    - DLQ: seed 2 deadLetters rows (1 "new", 1 "resolved") in-window → dlqNew 1, dlqTotal 2, and a rate against requestCount.
    - Tenant isolation: the same query as tenant B returns all-zero counts (cross-tenant leak = failure).
    - Windowing: telemetry rows older than sinceMs are excluded.
    - Refs-only shape: the returned object contains ONLY numbers/records-of-numbers — no draft/subject/body/recipient strings (assert structurally).
  </behavior>
  <action>
    Schema: add `.index("by_tenant_created", ["tenantId", "createdAt"])` to the telemetry table. One line; an index is not a write path and needs no migration (Convex backfills automatically — RESEARCH Pattern 6).

    Create `packages/backend/convex/opsSignals.ts` — a NEW module (do NOT add reads to audit.ts; keep the insert-only module visibly insert-only). Use the `tenantQuery` wrapper from `./lib/functions` (§2 — raw query import banned):
    ```ts
    export const evalSignals = tenantQuery({
      args: { sinceMs: v.optional(v.number()) },  // default: Date.now() - 30 days (discretion default, window shown in the card label)
      handler: async (ctx, { sinceMs }) => { ... }
    });
    ```
    - telemetry: `withIndex("by_tenant_created", q => q.eq("tenantId", ctx.tenantId).gte("createdAt", since)).collect()` — bounded by per-tenant request count at beta scale. Fold: sum `decisionCounts` keys (approve/edit/reject/regenerate), sum `regenerateCount`, sum `costUsd`, tally `reviewOutcome` values into a record, count rows, count `reviewOutcome === "sent"` as deliveredCount.
    - audit: `withIndex("by_tenant_ts", q => q.eq("tenantId", ctx.tenantId).gte("ts", since)).collect()` then filter `eventType === "llm.fallback"` in memory — WINDOWED, never an un-windowed audit collect (anti-pattern: audit is unbounded; the auditCounts aggregate counts all events, not per-type, so it cannot serve this).
    - deadLetters: `by_tenant` prefix read filtered to `createdAt >= since` for total; count `status === "new"` from the same rows (bounded table).
    - Return counts/rates ONLY (refs/counts by construction, §4): `{ windowStartMs, requestCount, decisionCounts, reviewOutcomes, regenerateTotal, fallbackCount, dlqNew, dlqTotal, totalCostUsd, deliveredCount, costPerDeliveredUsd }` (costPerDeliveredUsd = totalCostUsd / deliveredCount, 0 when deliveredCount 0 — never NaN).
    - Module doc: note the honesty caveats verbatim from RESEARCH Pattern 6 — cockpit rows carry costUsd 0 / decisionCounts {} / regenerateCount 0; decisionCounts is pipeline-only; reviewOutcomes covers both lanes.

    §9 hook: `opsSignals.ts`/`opsSignals.test.ts` are NEW code files no playbook covers — register `"packages/backend/convex/opsSignals.ts"` and `"packages/backend/convex/opsSignals.test.ts"` under the `"agent-runtime.md"` entry in `docs/playbooks/watch.json` (its empty list is explicitly reserved for Phase 3.6 paths — agent-runtime.md:117), and add a short EVAL-02 read-side note to `docs/playbooks/agent-runtime.md` (what evalSignals reads, the windowed-audit rule, the cockpit-zeros caveat) + bump its `Last verified` → 03.6-02. Plan 04 completes the fuller playbook close in wave 3.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run opsSignals && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>evalSignals returns correct sums/rates from seeded rows, tenant-isolated and windowed, refs-only shape; telemetry has by_tenant_created; new files registered in watch.json; check-playbooks exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Ops-page eval-signals section (brand tokens, honest labels)</name>
  <files>apps/web/app/(app)/ops/page.tsx</files>
  <action>
    Read `docs/design/BRAND.md` first (§10). Add an "Eval signals" section to the existing ops page, ABOVE the dead-letter list (signals are the ambient read; dead letters are the incident read). Use the `globals.css` CSS variables for colors — never hardcode a hex a token covers. Do NOT rewrite the legacy inline-styled dead-letter list (ponytail: the shortest working diff).

    Content: `const signals = useQuery(api.opsSignals.evalSignals, {})` and render a compact stat grid:
    - "Review outcomes (30 days)" — the reviewOutcome distribution (sent/rejected/expired/failed/blocked) with counts. Label copy notes this is the approve-proxy for cockpit sends.
    - "Gate decisions (pipeline)" — approve/edit/reject counts + regenerate total, labeled "(legacy pipeline lane)" per Pitfall 6 (cockpit rows carry empty decisionCounts — an unlabeled 0 would lie to the operator).
    - "Fallbacks" — llm.fallback count in the window.
    - "DLQ" — new / total in the window (+ rate vs requestCount when requestCount > 0).
    - "Pipeline LLM cost / delivered send" — costPerDeliveredUsd with the honest framing (cockpit reasoning spend goes to the rate limiter, not telemetry — a title attribute/short caption carries the caveat).
    Loading state mirrors the page's existing `undefined` handling; zero-data state renders the grid with zeros (not an error).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Ops page typechecks with the eval-signals section wired to api.opsSignals.evalSignals; brand tokens used for the new section; legacy dead-letter list untouched; live visual verification deferred to plan 05's checkpoint.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run opsSignals` green (rates, isolation, windowing, refs-only shape).
- `pnpm --filter @pikar/web typecheck` clean.
- `node scripts/check-playbooks.mjs` exit 0 (opsSignals paths registered under agent-runtime.md).
- grep guards: no `query(` raw-builder import in opsSignals.ts (tenantQuery only); no write (`db.insert|db.patch|db.replace|db.delete`) anywhere in opsSignals.ts.
</verification>

<success_criteria>
- Roadmap SC3 satisfied structurally: all five locked signal families readable from existing data, tenant-scoped, windowed, zero new write paths.
- Pitfall 6 avoided: every metric is labeled against what its rows actually contain.
</success_criteria>

<output>
After completion, create `.planning/phases/03.6-agent-eval-gate/03.6-02-SUMMARY.md`
</output>
