---
phase: 03.6-agent-eval-gate
plan: 05
type: execute
wave: 4
depends_on: ["03.6-02", "03.6-04"]
files_modified: []
autonomous: false
requirements: [EVAL-01, EVAL-02]

must_haves:
  truths:
    - "One live pnpm eval:golden run on the dev deployment passes all cases with total cost printed under the $1.00 cap, exit 0, zero sends and zero mailbox reads"
    - "One full gate cycle is proven live: gated skill edit → candidate published → activateSkill refused → pinned eval green → evidence recorded → activateSkill passes → rollback to the prior version works WITHOUT evidence"
    - "The executive-agent.classifier row is archived on the live deployment and a seedSkills re-run does not resurrect it"
    - "The ops-page eval-signals card renders real numbers on the dev deployment"
  artifacts:
    - path: ".planning/phases/03.6-agent-eval-gate/03.6-05-SUMMARY.md"
      provides: "phase-close record incl. the live-run evidence (run id, cost, cases)"
  key_links:
    - from: "pnpm eval:golden (live)"
      to: "skills.evidence row"
      via: "recordEvalEvidence on the all-green pinned run"
      pattern: "recordEvalEvidence"
---

<objective>
Close Phase 3.6 with the proofs that only a live deployment can give: the first real golden-set run (live model, real reasoning, real cost), the full gate cycle end to end, the classifier archival on the live deployment, and the ops card on real data. Everything automated has already shipped (plans 01–04); this plan is the automated final sweep plus the single human-verify checkpoint.

Purpose: EVAL-01/EVAL-02 live sign-off — roadmap SC1–SC4 all have a live-only component (model nondeterminism, real evidence write, real dev-deployment state, visual card check) that offline vitest structurally cannot observe.
Output: phase-complete verification; the orchestrator then runs verify_phase_goal.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.6-agent-eval-gate/03.6-CONTEXT.md
@.planning/phases/03.6-agent-eval-gate/03.6-01-SUMMARY.md
@.planning/phases/03.6-agent-eval-gate/03.6-02-SUMMARY.md
@.planning/phases/03.6-agent-eval-gate/03.6-03-SUMMARY.md
@.planning/phases/03.6-agent-eval-gate/03.6-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase-gate sweep</name>
  <files>(verification only — no production files)</files>
  <action>
    Run the full offline gate and record results in the SUMMARY:
    1. `pnpm --filter @pikar/backend exec vitest run skills opsSignals runCockpitAgent` — the three phase suites green.
    2. `pnpm --filter @pikar/backend test` — full backend suite; the documented pre-existing reds (audit.test.ts auditCounts-unregistered; runCockpitAgent/cockpitDraft cold-start timeout flakes that pass in isolation at --testTimeout=30000) are acceptable, anything NEW red is a blocker.
    3. `pnpm --filter @pikar/web typecheck` clean.
    4. `node packages/backend/scripts/run-eval-golden.mjs --self-check` exit 0.
    5. `node scripts/check-playbooks.mjs` exit 0.
    6. Grep guards: `EVAL_GATE` in skills.ts; no `SMOKE::` under scripts/eval-cases/; `executiveAgentClassifierSkillBody` not imported by skills.ts; no db write calls in opsSignals.ts.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run skills opsSignals runCockpitAgent && node packages/backend/scripts/run-eval-golden.mjs --self-check && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All offline gates green (modulo the documented pre-existing reds); results recorded for the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live phase gate — golden run, gate cycle, classifier archival, ops card</name>
  <files>(live dev deployment state — no repo files; live-only defect fixes, if any, follow the gap-closure convention)</files>
  <action>Present the four live proofs below to the user and pause. Everything was automated in plans 01-04; this checkpoint verifies the automation against the live model, the live deployment, and the real browser — the only observers of model nondeterminism, real evidence writes, and the rendered card.</action>
  <verify>User types "approved" after all four proof steps pass on the dev deployment</verify>
  <done>Live golden run exit 0 under the cost cap with zero sends; gate cycle refused-then-passed with rollback exempt; classifier archived and non-resurrecting; ops card rendering real numbers</done>
  <what-built>The complete agent eval gate: a live-model golden-set harness (`pnpm eval:golden`), an eval-gated `activateSkill` with candidate-publishing `seedSkills` and structural rollback exemption, the ops-page eval-signals card, and the classifier housekeeping — plans 01–04.</what-built>
  <how-to-verify>
    On a machine with the dev deployment (CONVEX_DEPLOYMENT + OPENAI_API_KEY set):

    1. **Classifier archival (SC4, one-time live flip):**
       `npx convex run skills:archiveSkill '{"name":"executive-agent.classifier"}'`
       then `npx convex run skills:seedSkills '{}'` and confirm no active classifier row returns (e.g. `npx convex run skills:getActiveSkill '{"name":"executive-agent.classifier"}'` throws NO_ACTIVE_SKILL — the zombie stays archived).

    2. **First live golden run (SC1):** `pnpm eval:golden`
       Expect: 15 sequential cases, per-case PASS lines, total cost printed (expected ~$0.05–0.15, hard cap $1.00), exit 0. Confirm zero sends (no email arrives anywhere; recipients are @golden.example) and zero mailbox reads (case 09 reports the graceful degradation). If a case fails on both attempts, judge: bad fixture (tighten the final turn / use statusAtMost) vs a real prompt regression.

    3. **Full gate cycle (SC2):**
       a. Make a trivial edit to the cockpit-agent canonical skill `.md` (e.g. add a clarifying sentence) and run its 5-file mirror regeneration, then `npx convex run skills:seedSkills '{}'` → confirm a NEW version N exists with status "candidate" while the old version stays active.
       b. `npx convex run skills:activateSkill '{"name":"cockpit-agent","version":N}'` → must FAIL with EVAL_GATE.
       c. `pnpm eval:golden --skill cockpit-agent@N` → green run; confirm evidence JSON landed on the vN row (refs/counts only — runId, cases, cost, model, skillVersions, ts; no prompt/content text).
       d. Re-run the activateSkill from (b) → must SUCCEED (vN active, prior version archived).
       e. **Rollback exemption:** `npx convex run skills:activateSkill '{"name":"cockpit-agent","version":<prior>}'` → must succeed IMMEDIATELY with no evidence on that row (archived target = structurally exempt). Then either roll forward to vN again or revert the .md edit + reseed — leave the deployment on the intended body.

    4. **Ops card (EVAL-02/SC3):** open `/ops` in the browser → the "Eval signals" section renders: review-outcome distribution, pipeline gate decisions, fallback count, DLQ new/total, cost-per-delivered with its caveat label. Numbers consistent with the deployment's real telemetry (zeros are fine where honest); brand tokens, no layout breakage; legacy dead-letter list intact below.
  </how-to-verify>
  <resume-signal>Type "approved" (all four proofs pass) or describe which step failed and how</resume-signal>
</task>

</tasks>

<verification>
- Task 1's automated sweep green.
- Human-verify approved: live golden run under cap with exit 0, gate cycle refused-then-passed-then-rollback-exempt, classifier archived and non-resurrecting, ops card rendering.
</verification>

<success_criteria>
- All four roadmap success criteria observed live: SC1 (golden set on demand, cost-capped, zero-send), SC2 (gated candidate refused / evidence unlocks / rollback exempt), SC3 (ops signals readable), SC4 (classifier archived + playbook gap closed — closed in 03.6-04, confirmed here).
- Phase 3.6 ready for the orchestrator's verify_phase_goal.
</success_criteria>

<output>
After completion, create `.planning/phases/03.6-agent-eval-gate/03.6-05-SUMMARY.md` (include the live run id, cases passed, total cost, and the gate-cycle evidence row reference)
</output>
