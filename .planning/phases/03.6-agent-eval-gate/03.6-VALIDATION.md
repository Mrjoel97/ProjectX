---
phase: 3.6
slug: agent-eval-gate
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-15
---

# Phase 3.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (backend: convex-test; core: colocated unit tests) |
| **Config file** | packages/backend/vitest.config.ts / packages/core vitest defaults |
| **Quick run command** | `pnpm --filter @pikar/backend test <suite-glob>` |
| **Full suite command** | `pnpm --filter @pikar/backend test` + `pnpm --filter @pikar/core test` |
| **Estimated runtime** | ~60–120 seconds (backend full suite) |

---

## Sampling Rate

- **After every task commit:** Run the targeted suite for the touched module (e.g. `test skills`, `test evalGolden`)
- **After every plan wave:** Run the full backend + core suites
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

*To be filled by the planner — every plan task maps here.*

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| — | — | — | EVAL-01 | unit/integration | *(planner fills)* | ❌ W0 | ⬜ pending |
| — | — | — | EVAL-02 | unit | *(planner fills)* | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Gate tests for `activateSkill` candidate-vs-rollback semantics (extend existing `skills.test.ts`)
- [ ] Fixture-schema/assertion unit tests for the golden-set case format (offline, no model)
- [ ] Ops eval-signals query tests (convex-test over seeded telemetry/audit/deadLetters rows)

*Existing infrastructure (vitest + convex-test + smoke runner conventions) covers the frameworks — no new framework installs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live golden-set run against the real model completes under the cost cap with per-case pass/fail | EVAL-01 | Requires live model + dev deployment; nondeterministic reasoning cannot run in CI/offline vitest | Run `pnpm eval:golden` against the dev deployment; confirm all cases pass, total cost printed and under cap, exit 0; confirm zero sends and no mailbox reads |
| Ops eval-signals card renders real production numbers | EVAL-02 | Visual check on live data | Open /ops on the dev deployment; confirm the eval-signals section shows rates/counts consistent with seeded or real telemetry |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
