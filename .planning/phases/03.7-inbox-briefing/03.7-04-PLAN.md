---
phase: 03.7-inbox-briefing
plan: 04
type: execute
wave: 4
depends_on: ["03.7-02", "03.7-03"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/cockpit-briefing.spec.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [CKPT-04]

must_haves:
  truths:
    - "A BRIEFING card renders in the workspace pane grouped Needs you / Today / Yesterday / This week, each item timestamp + sender + one-line gist (SC-1)"
    - "The card renders even when the thread has NO plan row — a briefing ask is usually the first message (Pitfall 6)"
    - "Needs-you items are plain suggestions (text + flags) — no button or link performs an action (SC-4); acting goes through the normal conversation → PLAN → Approve"
    - "An offline Playwright E2E proves the whole loop with zero model calls and zero Gmail traffic"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "BriefingCard + CardList wiring with an independent briefings query"
      contains: "BriefingCard"
    - path: "apps/web/e2e/cockpit-briefing.spec.ts"
      provides: "offline SMOKE::agent::brief E2E over the seeded fixture"
      min_lines: 40
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "api.briefings.byThread"
      via: "second useQuery in CardList, independent of plan status"
      pattern: "api\\.briefings\\.byThread"
    - from: "apps/web/e2e/cockpit-briefing.spec.ts"
      to: "smoke:seedInboxFixture"
      via: "npx convex run seeding in test setup (offlineDigest:true, baseMs = SMOKE_NOW_MS)"
      pattern: "seedInboxFixture"
---

<objective>
The user-facing BRIEFING card and the offline E2E that locks the whole loop: seeded fixture inbox → `SMOKE::agent::brief=today` → briefInbox → deterministic offline digest → briefings row → grouped, triaged card.

Purpose: SC-1 (grouped card with timestamp/sender/gist) and SC-4 (Needs-you as suggestions) become visible and regression-locked.
Output: BriefingCard in cards.tsx + cockpit-briefing.spec.ts.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/BRAND.md
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@apps/web/e2e/cockpit-attachment.spec.ts
@.planning/phases/03.7-inbox-briefing/03.7-RESEARCH.md

<interfaces>
CardList today (cards.tsx:507) — the early returns are the trap to fix:
```tsx
export function CardList({ threadId }: { threadId?: string }) {
  const plan = useQuery(api.plans.byThread, threadId ? { threadId } : "skip");
  if (!threadId) return <p style={muted}>No artifacts yet.</p>;
  if (plan === undefined) return <p style={muted}>Loading…</p>;
  if (plan === null) return <p style={muted}>No plan yet — answer the questions to build one.</p>;
  // ...status dispatch: ResolutionCard / PlanCard / ScheduledCard / CanceledCard / DraftCard / ReportCard
}
```
From Plan 02: `api.briefings.byThread({ threadId })` → latest briefing row or null; row shape `{ range, tz, items: [{ bucket: "today"|"yesterday"|"thisWeek", sender, ts, gist, category, needsReply, deadline?, isUnread? }], listedCount, createdAt }`.
From Plan 03: the offline digest (smoke path) emits item index 0 with needsReply:true + deadline — the E2E's Needs-you row. SMOKE clock: SMOKE_NOW_MS = Date.UTC(2020,0,1,12,0,0) = 1577880000000; seed with that baseMs so buckets are deterministic.
Styling: inline styles + globals.css CSS variables exactly like sibling cards (`var(--teal-600)` etc.) — no component library, no hardcoded hex a token covers; read BRAND.md first.
E2E precedent: existing cockpit-*.spec.ts drive the cockpit chat with SMOKE:: sentinel messages against the dev deployment (auth.setup.ts session).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BriefingCard + plan-independent CardList wiring</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    Restructure CardList: add `const briefing = useQuery(api.briefings.byThread, threadId ? { threadId } : "skip");` and render `{briefing && <BriefingCard briefing={briefing} />}` INDEPENDENTLY of plan status — including when `plan === null` (replace the bare "No plan yet" early return with a branch that still renders the briefing, showing the no-plan hint only when there is also no briefing). Keep every existing card's behavior byte-equivalent otherwise.
    BriefingCard (match sibling card chrome + BRAND.md tokens):
    - Header: "INBOX BRIEFING" + the range and resolved zone (`briefing.tz`) + created time.
    - "Needs you" section FIRST: items where `needsReply || deadline` — render sender, time, gist, and the deadline string as plain text with a subtle emphasis token; NO buttons, NO links, NO onClick that seeds an action (SC-4: suggestions only — the user acts by typing in the chat, crossing the normal PLAN → Approve gate).
    - Then Today / Yesterday / This week sections (skip empty ones): each item `time — sender — gist`, time formatted with `Intl.DateTimeFormat` in `briefing.tz` (hour+minute for today/yesterday, weekday+date for thisWeek); an unread dot via `isUnread`.
    - Cap honesty: when `listedCount > items.length` render "summarized {items.length} of {listedCount}".
    - data-testid hooks for the E2E: `briefing-card`, `briefing-needs-you`, `briefing-section-today` (etc.), `briefing-item`.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <done>Typecheck clean; card renders from the row shape alone (no plan dependency); zero action affordances in Needs-you.</done>
</task>

<task type="auto">
  <name>Task 2: offline cockpit-briefing E2E + §9 close</name>
  <files>apps/web/e2e/cockpit-briefing.spec.ts, docs/playbooks/cockpit.md</files>
  <action>
    New spec following the cockpit-attachment.spec.ts conventions (auth session, workspace page, chat composer):
    1. Setup: seed the fixture inbox via `npx convex run smoke:seedInboxFixture '{"tenantId":"<the E2E tenant>","offlineDigest":true,"baseMs":1577880000000}'` (child_process execSync, mirroring how run-smoke-*.mjs shell to convex run; resolve the E2E tenant the same way the existing specs/smoke scripts do). Idempotent — safe on re-runs.
    2. Send `SMOKE::agent::brief=today` in the cockpit chat.
    3. Assert: `briefing-card` visible; `briefing-needs-you` contains at least one item (the offline digest pins index 0 needsReply:true); grouped sections present per the fixture's baseMs-relative timestamps (two today, one yesterday — "This week" per fixture); an item shows sender text from the fixture; NO element inside the card is a button/link that mutates (assert zero `button`/`a` inside needs-you items).
    4. Assert the assistant chat reply is the counts-only template (contains "Briefing ready:" and does NOT contain any fixture gist/body needle) — the loop-visible half of SC-2, end-to-end.
    §9: bump cockpit.md (BRIEFING card + E2E path, `Last verified: 03.7-04`) in the same commit — apps/web/e2e/ and the workspace dir are cockpit.md-watched.
  </action>
  <verify>
    <automated>pnpm --filter web e2e -- cockpit-briefing</automated>
  </verify>
  <done>Spec green offline (zero model calls, zero Gmail traffic — fixture + smoke digest only); check-playbooks exit 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web typecheck` clean; `pnpm --filter web e2e -- cockpit-briefing` green against a running `npx convex dev` + `pnpm dev`.
- Existing cockpit specs still green (CardList restructure is additive).
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
A briefing ask on a fresh thread (no plan) renders the grouped, triaged card; Needs-you is informational only; the full loop is regression-locked by an offline E2E.
</success_criteria>

<output>
After completion, create `.planning/phases/03.7-inbox-briefing/03.7-04-SUMMARY.md`
</output>
