---
phase: 03.7-inbox-briefing
plan: 08
type: execute
wave: 8
depends_on: ["03.7-06", "03.7-07"]
files_modified:
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/cockpit-briefing.spec.ts
  - docs/playbooks/cockpit.md
autonomous: true
gap_closure: true
requirements: [CKPT-04]

must_haves:
  truths:
    - "The BRIEFING card leads with the synthesized lede sentence at the top — the user reads the story of their inbox before any row (Gap 1.1)"
    - "The card renders needs-you ABOVE the fyi remainder, and the fyi remainder is grouped by time (secondary) — action-first, never a chronological dump (Gap 1.2)"
    - "Automated notifications render as ONE collapsed row ('N automated notifications'), not N rows (Gap 1.3)"
    - "Each surfaced item's category is visible in the card via a BRAND-token tag — the axis the model computes is finally shown, not discarded (Gap 1.4)"
    - "SC-4 preserved: the card still has ZERO button / link / onClick — acting re-enters chat → PLAN → Approve; an injected body can never become a one-click affordance"
    - "The card renders purely from @pikar/core's buildBriefingView — the ordering/collapse/lede intelligence is NOT re-implemented in the component"
  artifacts:
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "BriefingCard reshaped to render buildBriefingView output: lede → needs-you → time-grouped fyi → collapsed noise row; per-row category tag"
      contains: "buildBriefingView"
    - path: "apps/web/e2e/cockpit-briefing.spec.ts"
      provides: "assertions for the lede text, the collapsed row, a category tag, action-first order, and still-zero interactive controls"
      contains: "lede"
  key_links:
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "@pikar/core buildBriefingView"
      via: "the card is a dumb renderer over the pure view model"
      pattern: "buildBriefingView"
---

<objective>
Make the card show the intelligent report the digest already computes: a lede at the top, action-first ordering, collapsed noise, and a visible category — rendered declaratively from `buildBriefingView`, with SC-4 (no action controls) intact.

Purpose: Gap 1.2/1.3/1.4 and the render of Gap 1.1's lede. This is the half the human verifier rejected as "a receipt, not a report" — the fix is presentation, driven entirely by the pure view model so no intelligence leaks into the component.
Output: reshaped BriefingCard + updated offline E2E.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@docs/design/BRAND.md
@.planning/phases/03.7-inbox-briefing/03.7-04-SUMMARY.md
@.planning/phases/03.7-inbox-briefing/03.7-UAT.md

<interfaces>
Current BriefingCard (cards.tsx ~655): reads `{ tz, items, listedCount }` off the row, computes `needsYou = items.filter(needsReply||deadline)`, renders NEEDS YOU then the three time buckets (BUCKETS at ~522) each filtering `items` by `bucket`. `BriefingRow`/`BriefingSection`/`SenderCell`/`RowBody` (~603–653) are the row primitives — REUSE them; the reshape changes the CONTAINER (sections chosen by the view model), not the row look. Row keys on `item.id` (the duplicate-key fix, 508d0d7 — keep it).

New from @pikar/core (plan 06): `buildBriefingView(briefing)` → `{ lede: string; needsYou: BriefingItem[]; timeSections: { bucket: Bucket; items: BriefingItem[] }[]; collapsedCount: number }`. The row now carries `synopsis?` on the briefing (plan 07) — buildBriefingView already folds it into `lede`.

BRAND (docs/design/BRAND.md, CLAUDE.md §10): use globals.css CSS variables — NO amber (`--held` is the approval gate's alone; deadline emphasis is weight, per 03.7-04); the unread dot is marked AND aria-labelled (never colour alone, §6). No component library. Existing tokens in this file: `var(--ink)`, `var(--ink-soft)`, `var(--teal-600)`, `var(--rule)`, and the shared `box`/`label`/`dim` style objects.

SC-4 (03.7-04 decision, E2E-enforced): the card asserts `button` and `a` `toHaveCount(0)`. The reshape MUST keep it — the collapsed row and category tag are TEXT, not toggles/links.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reshape BriefingCard over buildBriefingView</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    Rewrite BriefingCard's body to render `const view = buildBriefingView(briefing)`:
    1. LEDE first: render `view.lede` as a prominent one-line summary directly under the "INBOX BRIEFING" label (e.g. `var(--ink)`, slightly larger/medium weight) — this is the "story of the inbox" the user reads first. Keep the existing meta line (range · tz · built) below or beside it.
    2. NEEDS YOU: render `view.needsYou` via the existing BriefingSection (unchanged look) when non-empty, keeping the "Suggestions only — ask in chat…" caption.
    3. TIME-GROUPED FYI: iterate `view.timeSections` (already ordered today/yesterday/thisWeek, empty skipped) rendering each with BriefingSection. This is the secondary axis — action-first is satisfied because needs-you sits above.
    4. COLLAPSED NOISE: when `view.collapsedCount > 0`, render ONE static text row, e.g. "{count} automated notifications" (a muted `dim` line with a testid `briefing-collapsed`), NOT a list of rows and NOT a clickable disclosure (SC-4 — text only).
    5. CATEGORY TAG: surface `item.category` on each surfaced row as a small text tag (a muted pill using existing tokens — e.g. `var(--ink-soft)` on a subtle `var(--rule)`-bordered span, or the existing `label`/`dim` idiom). Add it inside the row primitive (RowBody or a new cell) so needs-you and fyi rows both show it. NO amber, no new colour semantics.
    Do NOT re-implement ordering/collapse/lede in the component — all of it comes from buildBriefingView. Keep row keys on `item.id`. Keep the tz-formatted timestamps (briefing.tz). Confirm by read: zero button/link/onClick remains in the card block.
    Read docs/design/BRAND.md first and match the existing sibling-card idiom before inventing any pattern.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>BriefingCard renders lede + needs-you + time-grouped fyi + one collapsed row + per-row category tag, all from buildBriefingView; typecheck exit 0; no interactive controls added.</done>
</task>

<task type="auto">
  <name>Task 2: update the offline E2E for the reshaped card</name>
  <files>apps/web/e2e/cockpit-briefing.spec.ts, docs/playbooks/cockpit.md</files>
  <action>
    Extend the existing offline spec (it seeds `smoke:seedInboxFixture { offlineDigest:true, baseMs:1577880000000 }` and drives `SMOKE::agent::brief=today`):
    - Assert the lede line is present and non-empty (the offline digest's deterministic synopsis flows through composeLede → a "N messages, M need you — Offline briefing synopsis." style string). Assert it appears ABOVE the first `briefing-item` in DOM order (lede-first).
    - Assert action-first: the NEEDS YOU section (or its first needs-you row, "Sarah Chen" per the offline fixture) appears before the time-grouped rows.
    - Assert a category tag is rendered on a row (e.g. testid or visible text for one of action/fyi).
    - Assert the collapsed row: if the current 5-message offline fixture has no newsletter, EITHER extend the offline deterministic digest to mark one item `category:"newsletter"` (smoke path in digestInbox, plan 07) so `briefing-collapsed` renders and assert it shows the count, OR assert `briefing-collapsed` is absent when count is 0 and add a targeted note. Prefer seeding one newsletter so the collapse is actually exercised.
    - Keep the SC-4 assertions: `button` and `a` inside the card `toHaveCount(0)`.
    The live run stays BLOCKED offline (auth.setup needs a Gmail-connected user — the standing blocker); this spec type-loads and Playwright discovers it. The real run happens in plan 09's connected human-verify session (`pnpm --filter @pikar/web test:e2e -- cockpit-briefing`).
    §9: bump docs/playbooks/cockpit.md `Last verified` (cards.tsx + e2e are cockpit.md-watched) — same commit.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test cockpit-briefing --list && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Spec asserts lede-first, action-first order, category tag, collapsed row, and zero interactive controls; Playwright discovers it; cockpit.md bumped. (Live execution deferred to 09.)</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web typecheck` exit 0; Biome on cards.tsx baseline-diffed (no NEW findings beyond its pre-existing set).
- Playwright discovers cockpit-briefing.spec.ts.
- Read-confirm: zero `button` / `<a` / `onClick` inside the BriefingCard block (SC-4).
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
The card is an intelligent report — lede first, action-first, noise collapsed, category visible — rendered from the pure view model, with SC-4 intact. The live paint is proven in plan 09's human-verify.
</success_criteria>

<output>
After completion, create `.planning/phases/03.7-inbox-briefing/03.7-08-SUMMARY.md`
</output>
