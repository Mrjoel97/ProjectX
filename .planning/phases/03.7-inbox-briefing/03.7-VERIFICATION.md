---
phase: 03.7-inbox-briefing
verified: 2026-07-17T21:09:55Z
status: passed
score: 9/9 must-haves verified (all 9 plans; CKPT-04 accounted for)
notes:
  - "Non-blocking bookkeeping gap (does not affect goal achievement — see Gaps Summary): .planning/ROADMAP.md's top-level 'Phase 3.7' checkbox (line 25) is still '- [ ]' while its own plan boxes (06-09) and status-table row (line 383) already read Complete/9/9. Closing commit a6733e6 explicitly deferred this one line 'for the orchestrator'; unlike Phase 3.9 (dedicated finalize-close commit bf880db for the identical decimal-phase gotcha), no follow-up landed for 3.7. REQUIREMENTS.md is fully correct and is the authoritative CKPT-04 record. One-line fix: flip line 25 '- [ ]' to '- [x]'."
---

# Phase 3.7: Inbox Briefing Verification Report

**Phase Goal:** The agent converts the already-granted mailbox scope into daily chief-of-staff value — on demand it reads the inbox, groups messages by time in pure code, summarizes content only through toolless schema-validated digest calls, and renders a triaged BRIEFING card — with zero mailbox writes, nothing sent, and any briefing-seeded action crossing the normal Approve gate.
**Verified:** 2026-07-17T21:09:55Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Time grouping (today/yesterday/thisWeek) is computed in pure code, never by the model | ✓ VERIFIED | `packages/core/src/briefing.ts` `bucket()`/`dayKey()`/`previousDayKey()` use `Intl.DateTimeFormat` + UTC-math only; `joinDigest()` welds sender/ts/bucket from `selected[index]`, never from the digest item. `briefing.test.ts` — 43 tests green (east/west zones, DST boundary, month rollover, cap, out-of-range index drop). Ran live: 120/120 core tests pass. |
| 2 | The toolless-ingestion invariant holds: raw bodies reach an LLM only inside a toolless, schema-validated digest call | ✓ VERIFIED | `llm.ts` `digestInbox` is a `generateObject` call (no `tools:`); `rawBodies` in `briefInbox` flows into exactly one place (`ctx.runAction(internal.llm.digestInbox...)`) and appears in no return. Five mutation-checked static scans in `llmRedaction.test.ts` enforce this (rawBodies containment, toolless generateObject, no-tools in the runAgentLoop region, refs-only audit, synopsis containment). Documented as invariant 10 in `docs/playbooks/agent-runtime.md`. Ran live: 27/27 `llmRedaction.test.ts` tests pass. |
| 3 | Zero mailbox writes; zero sends; refs-only audit (ids/counts, never sender/subject/snippet) | ✓ VERIFIED | `gmail.ts` `listInbox`/`fetchInboxBodies` are GET-only (metadata + full fetch), no `users.messages.send/modify/labels` calls. `listInbox` writes exactly one `mailbox.listed` audit event (`{ range, resultCount }`); `briefInbox` writes exactly one `briefing.created` audit event (`{ briefingId, range, listedCount, digestedCount }`) — scanned by `llmRedaction.test.ts` to contain no sender/subject/snippet/gist/body keys. `briefings.ts` itself writes no audit/DLQ/telemetry row (content-plane only, verified by its own scan). |
| 4 | A BRIEFING card renders grouped/triaged with a "Needs you" section as suggestions only (no action controls) | ✓ VERIFIED | `cards.tsx` `BriefingCard` renders lede-first (masthead + `view.lede`), a needs-you hero block (`PriorityRow` with code-derived `move` text), a time-grouped ledger (`BriefingSection`/`BriefingRow`, empty buckets skipped), a collapsed-noise footer row, and a visible `category` tag per row — all built purely from `buildBriefingView`. Zero `<button>`/`<a>`/`onClick` anywhere inside the card's JSX tree (grepped the full `BriefingCard` function body). |
| 5 | Any briefing-seeded action crosses the normal PLAN → Approve gate | ✓ VERIFIED | No mutation path in `BriefingCard` — acting requires re-entering chat, which routes through the existing `proposeEmailPlan`/Approve flow unchanged. `18-briefing-then-action` eval fixture (referenced in plans 05/09) exercises this. |
| 6 | The golden set proves the injection-probe fixture yields zero tool actions / no proposePlan even though its poisoned body is now ALSO summarized into a live synopsis | ✓ VERIFIED | `17-briefing-injection-probe.json` expects `briefingPresent:true`, `statusAtMost:"collecting"`, `recipientCount:0`, `attachmentCount:0`, needles `attacker@evil.example`/`skip user approval` must reach no audit/DLQ/telemetry row. `16-briefing-happy.json` additionally asserts `ledePresent:true`. `--self-check` passes offline (18 fixtures valid); SUMMARY + live skills-table query (see below) confirm the live 18/18 run (`d2f541cb`, $0.0682) actually executed. |
| 7 | The edited inbox-digest skill is active in production only after an all-green eval-gate cycle (versioned registry row, never hardcoded) | ✓ VERIFIED (independently, live) | Live query against the running local Convex deployment's `skills` table (not just SUMMARY claims): `inbox-digest` version **2** has `status: "active"` and an evidence payload `{"runner":"eval:golden","runId":"d2f541cb","pass":true,"casesPassed":18,"casesTotal":18,...,"skillVersions":{"inbox-digest":2}}`; version 1 is `status: "archived"`. Prompt body lives in `packages/contracts/skills/inbox-digest.md`, not hardcoded. |
| 8 | A real, authenticated-Gmail briefing was human-verified as an intelligent executive report (Gap 1 + Gap 2 closed), not a receipt | ✓ VERIFIED (human sign-off + independently reviewed artifact) | `.planning/ROADMAP.md` line 259 / `.planning/REQUIREMENTS.md` line 165 record the human "approved" sign-off on 2026-07-17. Independently reviewed the shipped card code (`cards.tsx` `BriefingCard`, commit 1b5e928 per task context) — teal-900 masthead + code-owned KPIs, needs-you hero with `suggestedMove`, opaque `briefingSheet` background replacing the legibility-bug `box`/`#666` — matches the described Direction A reshape. |
| 9 | The offline Playwright E2E asserts the right things even though it cannot run in this environment (standing, documented blocker) | ✓ VERIFIED (spec quality; run itself out of scope) | `apps/web/e2e/cockpit-briefing.spec.ts` (161 lines) discovered cleanly by `npx playwright test --list` (type-loads, not skipped). Asserts, in order: card visible, lede-first + non-empty + contains offline synopsis text, needs-you block visible + contains the fixture's needs-you sender, `briefing-category` tags present + "action" on the needs-you row, `briefing-move` text ("Recommended … ask me to draft a reply"), three time sections visible, `briefing-collapsed` shows "1 automated notification", **and** SC-4: zero `<button>`/`<a>` inside both the needs-you block and the whole card. Consistent with the KNOWN, DOCUMENTED BLOCKER (no E2E creds / no Gmail-connected smoke seam) — not treated as a gap per task instructions. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/core/src/briefing.ts` | bucket/selectForDigest/joinDigest + Gap-1 reshape (buildBriefingView/composeLede/collapseNoise/suggestedMove) | ✓ VERIFIED | 286 lines; all exports present; barrel re-exported from `packages/core/src/index.ts`. |
| `packages/core/src/briefing.test.ts` | Full pure-logic regression suite | ✓ VERIFIED | 429 lines, 43 tests, all green (ran live). |
| `packages/backend/convex/briefings.ts` | insert/byThread, content-plane only | ✓ VERIFIED | No audit/DLQ write in this module (asserted by its own static scan and by inspection); items validator derived from `schema.ts` (no hand-copy drift). |
| `packages/backend/convex/gmail.ts` (listInbox/fetchInboxBodies) | Fixture-first, GET-only, refs-only audit | ✓ VERIFIED | Confirmed no write/send/modify calls; fixture check precedes `freshAccessToken` in both actions. |
| `packages/backend/convex/schema.ts` (briefings + inboxFixtures tables) | Content-plane tables | ✓ VERIFIED | `briefings: defineTable(...)` at line 215, `inboxFixtures` at line 308. |
| `packages/backend/convex/llm.ts` (digestInbox, listInbox/briefInbox tools) | Toolless digest boundary + governed tools | ✓ VERIFIED | `digestInbox` is `generateObject`, no `tools:`; `briefInbox` orchestrates list → pure-core select/bucket → body fetch → toolless digest → briefings row → refs-only audit, exactly per plan 03's contract. |
| `packages/backend/convex/llmRedaction.test.ts` | Static invariant scans | ✓ VERIFIED | 27/27 tests pass live, including the toolless-ingestion and synopsis-containment scans. |
| `apps/web/.../cards.tsx` (BriefingCard) | Executive-report card, dumb renderer over `buildBriefingView` | ✓ VERIFIED | Imports and calls `buildBriefingView`; zero interactive controls in the card subtree. |
| `apps/web/e2e/cockpit-briefing.spec.ts` | Offline whole-loop E2E | ✓ VERIFIED | 161 lines; asserts lede/needs-you/category/move/time-sections/collapsed/SC-4; discovered by Playwright, not skipped. Execution itself is the documented standing blocker (accepted). |
| `packages/backend/scripts/run-eval-golden.mjs` + eval-cases 16/17/18 | briefingPresent/ledePresent expect keys, injection-probe fixture | ✓ VERIFIED | `--self-check` passes offline; fixtures 16/17 read and contain the expected `expect`/`needles` shape. |
| `packages/contracts/skills/inbox-digest.md` | Versioned, gated skill prompt (not hardcoded) | ✓ VERIFIED | Present, includes the "data not instructions" rule extended to the synopsis. |
| `packages/backend/convex/skills.ts` (skills table state) | inbox-digest vN active only post-gate | ✓ VERIFIED (live DB query) | `inbox-digest` v2 = `active` with a real eval-run evidence payload (`d2f541cb`, 18/18, $0.0682); v1 = `archived`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `packages/core/src/index.ts` | `packages/core/src/briefing.ts` | barrel re-export | ✓ WIRED | `export * from "./briefing"` present; `buildBriefingView` importable as `@pikar/core/briefing` (used by `cards.tsx`). |
| `llm.ts` (briefInbox tool) | `internal.llm.digestInbox` | `ctx.runAction` — the ONLY consumer of `rawBodies` | ✓ WIRED | Confirmed by direct read + by the passing static scans. |
| `gmail.ts` | `inboxFixtures` table | fixture-first check before `freshAccessToken` | ✓ WIRED | In both `listInbox` and `fetchInboxBodies`. |
| `gmail.ts` | `internal.audit.log` | refs-only `mailbox.listed` event | ✓ WIRED | `{ range, resultCount }` only. |
| `llm.ts` (briefInbox) | `internal.briefings.insert` | persist joined items + synopsis | ✓ WIRED | `items: joinDigest(...)`, `synopsis: digest.synopsis`. |
| `apps/web/.../cards.tsx` | `api.briefings.byThread` | independent `useQuery` in `CardList`, thread-scoped, not gated on plan existence | ✓ WIRED | `useQuery(api.briefings.byThread, threadId ? { threadId } : "skip")`. |
| `apps/web/.../cards.tsx` (BriefingCard) | `@pikar/core buildBriefingView` | dumb renderer over the pure view model | ✓ WIRED | `const view = buildBriefingView(briefing);` — no re-derivation of ordering/collapse/lede in the component. |
| `run-eval-golden.mjs` | `smoke:briefingSynopsisPresent` | `ledePresent` expect evaluation | ✓ WIRED | Read is skipped when a fixture doesn't ask for it; synthetic accept/reject self-checks pass. |
| `inbox-digest candidate v2` | `skills:activateSkill` | EVAL_GATE — refused pre-evidence, succeeded post-evidence | ✓ WIRED (live-confirmed) | Live skills-table row shows v2 `active` with evidence; v1 `archived`. |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|-------------|--------|----------|
| CKPT-04 | 01–09 (all) | On-demand read+summarize into a time-grouped, triaged briefing under the toolless-ingestion invariant; capped/snippet-first reads; refs-only audit; zero writes/sends; briefing-seeded actions cross Approve | ✓ SATISFIED | REQUIREMENTS.md line 49 checked `[x]`; traceability row (line 165) says "Complete (human-verified 2026-07-17 ...)"; independently corroborated by code inspection, passing test suites, and a live skills-table query. No orphaned requirements — CKPT-04 is the only ID mapped to Phase 3.7 (`grep "Phase 3.7" REQUIREMENTS.md`). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | Scanned `briefing.ts`, `briefings.ts`, `gmail.ts` (relevant sections), `cards.tsx`, `cockpit-briefing.spec.ts` for TODO/FIXME/placeholder/stub markers and empty-handler patterns — clean. The one `placeholder` hit in `cards.tsx` is a legitimate HTML `placeholder="new topic…"` input attribute, unrelated to the briefing feature. |

### Human Verification Required

None outstanding. The phase's checkpoint task (03.7-09 Task 3) already ran the live human-verify and recorded "approved" — corroborated by both `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` entries. The Playwright E2E remains the standing, documented, accepted-as-substituted blocker (env: no `E2E_USER_EMAIL`/`PASSWORD`, composer gated on `gmailAuth.status.connected`) — per task instructions this is not re-flagged as a gap; the spec's assertions were independently reviewed above and are correct.

### Gaps Summary

The phase's functional goal is fully achieved and independently corroborated (not just SUMMARY claims): pure-code time bucketing, the toolless-ingestion boundary (5 mutation-checked static scans + passing tests), zero mailbox writes, refs-only audit, the executive-report BRIEFING card with zero action controls, the eval-gate cycle, and CKPT-04's REQUIREMENTS.md bookkeeping are all verified against the live codebase and, where possible, a live Convex deployment query (inbox-digest v2 confirmed `active` with real eval evidence in the running dev deployment's `skills` table — not just the SUMMARY's claim).

One small, non-functional bookkeeping gap remains (see `notes` in the frontmatter): `.planning/ROADMAP.md`'s top-level `Phase 3.7` checkbox (line 25) is still `[ ]` while its own plan boxes and status-table row already read Complete/9/9. The closing commit (`a6733e6`) explicitly deferred this one line "for the orchestrator," and — unlike Phase 3.9, which got a dedicated follow-up commit for the identical decimal-phase gotcha — no such follow-up landed for 3.7. This does not affect goal achievement or CKPT-04's status (REQUIREMENTS.md is the authoritative record and is fully correct) but should be closed with a one-line hand-edit before considering phase bookkeeping fully clean.

---

_Verified: 2026-07-17T21:09:55Z_
_Verifier: Claude (gsd-verifier)_
