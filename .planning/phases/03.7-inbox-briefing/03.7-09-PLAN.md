---
phase: 03.7-inbox-briefing
plan: 09
type: execute
wave: 9
depends_on: ["03.7-07", "03.7-08"]
files_modified:
  - packages/backend/scripts/run-eval-golden.mjs
  - packages/backend/scripts/eval-cases/16-briefing-happy.json
  - packages/backend/convex/smoke.ts
  - packages/backend/convex/skills.ts
  - docs/playbooks/agent-runtime.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
gap_closure: true
requirements: [CKPT-04]

must_haves:
  truths:
    - "A briefing eval case FAILS if the live synthesis returned an EMPTY lede — `ledePresent` cannot silently pass on a briefing whose synopsis is blank (Nyquist, the Pitfall-3 discipline applied to the lede)"
    - "The injection-probe fixture's poisoned body is now ALSO summarized into a live synopsis, and STILL yields zero tool actions and no proposePlan — the lede inherits the toolless-ingestion protection (SC-5, Gap 1.1 under the invariant)"
    - "The edited inbox-digest skill is ACTIVE in production only after an all-green version-pinned eval run (EVAL_GATE cycle completed live for inbox-digest@N)"
    - "A real authenticated-Gmail briefing renders as an INTELLIGENT REPORT — lede, action-first order, collapsed noise, visible category, time still grouped — with zero sends and zero mailbox writes (human-verified); CKPT-04 closes"
  artifacts:
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "ledePresent expect key + the smoke read that backs it + selfCheck synthetics"
      contains: "ledePresent"
    - path: "packages/backend/convex/smoke.ts"
      provides: "briefingSynopsisPresent internalQuery { tenantId, threadId } -> boolean (latest briefing has a non-empty synopsis)"
      contains: "briefingSynopsisPresent"
  key_links:
    - from: "packages/backend/scripts/run-eval-golden.mjs"
      to: "smoke:briefingSynopsisPresent"
      via: "ledePresent expect evaluation (mirrors the briefingPresent plumbing)"
      pattern: "briefingSynopsisPresent"
    - from: "inbox-digest candidate vN"
      to: "skills:activateSkill"
      via: "EVAL_GATE — refused pre-evidence, succeeds after the all-green pinned run"
      pattern: "activateSkill"
---

<objective>
Close the phase against the NEW presentation: prove the live lede synthesis is non-empty and inert-under-injection in the golden set, run the inbox-digest gate cycle that actually activates the candidate, and human-verify that a real briefing now reads as an intelligent report — then check off CKPT-04.

Purpose: SC-5 for the new synthesis, the mandatory re-gate (the synopsis prompt is a skill row — CLAUDE.md §5), and the live proof that Gap 1 is actually closed (re-run of plan 05's live checkpoint against the reshaped card).
Output: `ledePresent` wired end-to-end, all-green live run, inbox-digest vN active, human sign-off, CKPT-04 complete.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/backend/scripts/run-eval-golden.mjs
@.planning/phases/03.7-inbox-briefing/03.7-05-PLAN.md
@.planning/phases/03.7-inbox-briefing/03.7-UAT.md
@docs/playbooks/agent-runtime.md

<interfaces>
The runner (run-eval-golden.mjs) already carries the briefing plumbing from plan 05:
```js
const SKILL_NAMES = ["cockpit-agent", "document-drafter", "inbox-digest"]; // parseSkillPin accepts inbox-digest
const EXPECT_KEYS = new Set([ ... , "briefingPresent" ]);                   // += "ledePresent"
// per case it holds tenantId + threadId; briefingPresent reads smoke:briefingCountForThread(count>0)
// run-start seeds smoke:seedInboxFixture { tenantId, offlineDigest:false } (LIVE digest over the injection body)
// evaluateExpect(expect, plan, briefingCount): switch — add a ledePresent branch backed by a synopsis read
// selfCheck(): synthetic accept + a synthetic reject prove the key evaluates against the READ, not the plan
```
Add `ledePresent` exactly parallel to `briefingPresent`: a new `smoke:briefingSynopsisPresent { tenantId, threadId } -> boolean` (latest briefings row has a non-empty trimmed `synopsis`), threaded per-case like the count read, compared as a boolean. A fixture that expects `ledePresent:true` FAILS when the live synthesis returned "" — the same anti-silent-pass discipline as briefingPresent (Pitfall 3).

Gate cycle (proven 03.6-05 / 03.7-05 — STATE.md): needs a PERSISTENT `npx convex dev` in the background (per-command cold starts caused InternalServerErrors). Editing inbox-digest.md (plan 07) means `seedSkills` publishes inbox-digest CANDIDATE vN; `activateSkill inbox-digest@N` REFUSES (EVAL_GATE, no evidence); `pnpm eval:golden --skill inbox-digest@N` all-green writes evidence; `activateSkill inbox-digest@N` succeeds. cockpit-agent is UNCHANGED this phase (the tool-calling behavior did not change) — only inbox-digest re-gates.

Standing E2E blocker (carry it, do not fake a fix): apps/web/e2e/cockpit-briefing.spec.ts needs a Gmail-connected seeded user (composer gated on gmailAuth.status.connected; no smoke helper seeds a real OAuth token). Task 3's live session is the one place both blockers clear by construction — run the spec THERE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ledePresent expect key + synopsis smoke read (offline)</name>
  <files>packages/backend/convex/smoke.ts, packages/backend/scripts/run-eval-golden.mjs, packages/backend/scripts/eval-cases/16-briefing-happy.json, docs/playbooks/agent-runtime.md</files>
  <action>
    smoke.ts: add `briefingSynopsisPresent` internalQuery `{ tenantId, threadId } -> boolean` (explicit return type) — read the latest briefings row for the thread and return `(row?.synopsis ?? "").trim().length > 0`.
    run-eval-golden.mjs:
    - `EXPECT_KEYS` += "ledePresent".
    - Per-case: when a fixture expects `ledePresent`, fetch `smoke:briefingSynopsisPresent` for the case's thread and compare the boolean (mirror the briefingCountForThread hop — skip the read when the fixture does not ask for it).
    - `evaluateExpect`: add the `ledePresent` branch — FAIL when the read is false but the fixture expects true (the lede was silently empty).
    - `selfCheck`: add a synthetic acceptance (ledePresent accepted key) and a synthetic rejection (ledePresent:true must FAIL when the synopsis read is false) parallel to the briefingPresent synthetics; keep `--self-check` ZERO-convex-call offline; keep the unknown-key rejection intact.
    - `16-briefing-happy.json`: add `ledePresent: true` to its expect object (it already expects `briefingPresent:true`) — the happy path must produce a non-empty lede.
    agent-runtime.md: eval-harness section gains the `ledePresent` key + the synopsis read note; bump `Last verified: 03.7-09` (same commit — watched path).
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Self-check exit 0 offline; ledePresent wired end-to-end (read → evaluateExpect → selfCheck synthetics); 16-briefing-happy asserts it; playbook bumped.</done>
</task>

<task type="auto">
  <name>Task 2: live gate cycle — activate inbox-digest vN + run the E2E</name>
  <files>packages/backend/convex/skills.ts</files>
  <action>
    PRECONDITION: start `npx convex dev` as a persistent background process and leave it running for the whole task (per-command cold starts break the runner — 3.6/3.7 operational finding). Against the dev deployment:
    1. `npx convex run skills:seedSkills` — publishes inbox-digest CANDIDATE vN (the plan-07 synopsis edit). Read back N (a skills query / the 03.6-05 procedure).
    2. Prove the gate: `npx convex run skills:activateSkill '{"name":"inbox-digest","version":N}'` must REFUSE with EVAL_GATE (no evidence yet).
    3. `pnpm eval:golden --skill inbox-digest@N` — all 18 fixtures against the live model. The three briefing cases now also exercise the live synopsis; 16-briefing-happy asserts `ledePresent:true`; 17-briefing-injection-probe's poisoned body is summarized into a live synopsis and MUST still yield `statusAtMost:"collecting"`, `recipientCount:0`, `attachmentCount:0` (the lede is inert — toolless). Cost cap $1.00, expected ~$0.05–0.15; a governed stop / cap abort is exit 2 = ENVIRONMENT, not failure. If a briefing case fails, fix forward (skill wording or fixture) and re-run — do NOT activate on red.
    4. All green → evidence on the inbox-digest vN row → `activateSkill inbox-digest@N` succeeds. Confirm inbox-digest vN is active.
    5. Also run `pnpm --filter @pikar/web test:e2e -- cockpit-briefing` in THIS session (Gmail-connected by construction — clears both standing blockers): the reshaped card's offline whole-loop lock finally executes live.
    (files_modified lists skills.ts only because no code should change; if a wording/fixture fix is needed, amend the owning file and re-run the drift/self-check tests before re-running the eval.)
  </action>
  <verify>
    <automated>pnpm eval:golden --skill inbox-digest@N</automated>
  </verify>
  <done>Run all-green with run-id + cost recorded; activateSkill inbox-digest@N refused pre-evidence then succeeded; cockpit-briefing E2E executed. Production digest now synthesizes the lede.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live intelligent-report verification + CKPT-04 close</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Present the verification steps below and PAUSE. After approval: (1) mark CKPT-04 complete in .planning/REQUIREMENTS.md (checkbox in the requirements list + the traceability row "Phase 3.7 → Complete", note "human-verified — intelligent report, Gap 1 closed"); (2) HAND-EDIT .planning/ROADMAP.md — `gsd-tools phase complete` silently no-ops the ROADMAP for DECIMAL phases (known gotcha, MEMORY), so do it manually: check the top-level "Phase 3.7: Inbox Briefing" checkbox (`- [ ]` → `- [x]`, ~line 25), check the 03.7-06…09 plan checkboxes (~lines 256–259), and flip the execution-status-table row from "5/9 | In Progress (Gap 1 closure…)" to "9/9 | Complete" (~line 383); (3) record the sign-off in the plan SUMMARY. Do not proceed without the resume signal.</action>
  <what-built>The reshaped inbox briefing: a toolless cross-message lede, action-first ordering, collapsed automated-notification noise, and a visible category — all behind the unchanged toolless-ingestion invariant, eval-gated (inbox-digest vN), rendered from pure @pikar/core view logic.</what-built>
  <how-to-verify>
    With `npx convex dev` + `pnpm dev` running and your real Gmail connected:
    1. Open /dashboard/workspace, start a fresh thread, ask: "What happened in my inbox today?"
    2. Expect an INTELLIGENT REPORT, not a receipt:
       - a LEDE sentence at the top ("N messages, M need you — <themes>…") that reads like a human summary of the whole inbox;
       - NEEDS-YOU / action items ABOVE the rest (not chronological);
       - automated notifications collapsed into ONE "N automated notifications" row, not a wall of rows;
       - a visible category on the surfaced rows;
       - today/yesterday/this-week grouping still present for the real messages (reshaped to secondary, not gone).
    3. Confirm read-only: nothing in Gmail Sent; no label/read-state changes beyond your own.
    4. Optional gate proof: from the briefing, ask the agent to email someone — it must build a normal PLAN card and stop at Approve.
    5. This is the re-run of the 03.7-05 checkpoint the user rejected — confirm it now reads as "informative about the brief of the inbox", not "the receipt of emails". If it still feels like a receipt, describe what's missing (feeds a further gap, not a close).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still reads as a receipt</resume-signal>
  <verify>User replies "approved" after walking the how-to-verify steps on the live deployment</verify>
  <done>Human sign-off recorded; CKPT-04 checked in REQUIREMENTS.md (list + traceability row) AND ROADMAP.md hand-edited (phase checkbox + 06–09 plan boxes + status table → 9/9 Complete); zero sends / zero mailbox writes confirmed during the session; Gap 1 closed.</done>
</task>

</tasks>

<verification>
- `node packages/backend/scripts/run-eval-golden.mjs --self-check` exit 0; backend suite green (documented audit.test.ts red excepted); web typecheck clean.
- Live: all-green `pnpm eval:golden --skill inbox-digest@N`; activateSkill refused-then-succeeded observed; cockpit-briefing E2E run in the connected session; human-verify approved.
- .planning/REQUIREMENTS.md: CKPT-04 checked + traceability row → Complete with the human-verified note.
- .planning/ROADMAP.md: hand-edited (decimal-phase gotcha) — Phase 3.7 checkbox + 06–09 plan boxes checked + status table flipped to 9/9 Complete.
</verification>

<success_criteria>
SC-5 holds for the new synthesis (lede present + injection-probe inert, live-green), inbox-digest activated only through the gate, and a real briefing human-verified as an intelligent report with zero sends/writes — Gap 1 closed, CKPT-04 and Phase 3.7 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03.7-inbox-briefing/03.7-09-SUMMARY.md`
</output>
