---
phase: 03.7-inbox-briefing
plan: 06
type: tdd
wave: 6
depends_on: ["03.7-05"]
files_modified:
  - packages/core/src/briefing.ts
  - packages/core/src/briefing.test.ts
  - packages/core/src/index.ts
  - docs/playbooks/cockpit.md
autonomous: true
gap_closure: true
requirements: [CKPT-04]

must_haves:
  truths:
    - "Given a set of briefing items, pure code orders needs-you / action items ABOVE fyi — the briefing is action-first, never chronological (Gap 1.2)"
    - "Pure code collapses newsletter/automated-notification items into ONE aggregate ('N automated notifications'), so 12 notifications never render as 12 rows (Gap 1.3)"
    - "Pure code composes the lede sentence from CODE-OWNED counts (total, needs-you) welded to the model's qualitative synopsis clause — counts are never the model's to state (ADR-004, Gap 1.1)"
    - "Time grouping (today/yesterday/thisWeek via bucket()) is PRESERVED and still tested — reshaped to a secondary axis inside the fyi remainder, NOT deleted (locked constraint: deleting time grouping needs a roadmap change, not a card edit)"
    - "A missing/empty synopsis degrades to a counts-only lede — the view never throws on a pre-delta row with no synopsis"
  artifacts:
    - path: "packages/core/src/briefing.ts"
      provides: "DigestBatch type, composeLede, collapseNoise, buildBriefingView (the reshaped view model) — all pure, no Convex/DOM"
      contains: "buildBriefingView"
    - path: "packages/core/src/briefing.test.ts"
      provides: "action-first order, newsletter-collapse, lede-composition, and time-grouping-preserved cases with fixed nowMs"
      contains: "buildBriefingView"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/briefing.ts"
      via: "re-export of DigestBatch / composeLede / buildBriefingView (consumed by plan 07 backend + plan 08 card)"
      pattern: "buildBriefingView"
---

<objective>
Move ALL the briefing intelligence — action-first ordering, noise collapse, and lede composition — into pure, tested `@pikar/core`, where the ordering/collapse logic MUST live (never the card). This is the interface-first contract both the backend synthesis (07) and the reshaped card (08) build against.

Purpose: Gap 1's reshape ("intelligent report, not a receipt") is a data transform over the already-persisted `category`/`needsReply` axes — the laziest, most testable home is core (ponytail §8: the axis already exists and is free).
Output: `buildBriefingView` + `composeLede` + `collapseNoise` + the `DigestBatch` type, exported and regression-tested.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-inbox-briefing/03.7-UAT.md
@packages/core/src/briefing.ts
@.planning/design/inbox-briefing.md

<interfaces>
Existing `@pikar/core` briefing exports (packages/core/src/briefing.ts — DO NOT change their behavior; build on them):
```ts
export interface BriefingItem {
  id: string; bucket: Bucket; sender: string; subject: string; ts: number;
  gist: string; category: string; needsReply: boolean; deadline?: string; isUnread?: boolean;
}
export interface DigestItem { index: number; gist: string; category: string; needsReply: boolean; deadline?: string; }
export type Bucket = "today" | "yesterday" | "thisWeek";
export function bucket(msgTsMs: number, nowMs: number, tz: string): Bucket | null; // pure, tested — PRESERVE
export function joinDigest(selected, items, nowMs, tz): BriefingItem[];             // ADR-004 weld — untouched
```
The persisted briefing row shape the card reads (schema.ts `briefings`): `{ range, tz, items: BriefingItem[], listedCount, createdAt }` — plan 07 adds `synopsis?: string`.

`category` is ALREADY one of `"action" | "fyi" | "newsletter" | "other"` on every item (inbox-digest skill + `digestSchema`). The inbox-digest skill body: `newsletter` = "bulk, marketing, digests, automated notifications" and is NEVER `needsReply`. That is the axis to collapse — no new classification, no new model call here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — cases for order, collapse, lede, and time-grouping-preserved</name>
  <files>packages/core/src/briefing.test.ts</files>
  <behavior>
    Write failing tests (fixed `nowMs`, an east- and a west-of-UTC tz) against the not-yet-written exports:
    - composeLede(items, listedCount, synopsis):
      - Test: returns a sentence LEADING with code-owned counts, e.g. "24 messages, 3 need you" — assert the numbers come from `listedCount` and `items.filter(needsReply||deadline).length`, NOT from the synopsis string (pass a synopsis that contains a WRONG number like "99" and assert "99" is not the count the lede states).
      - Test: a non-empty synopsis is appended as the qualitative clause ("… — mostly billing notifications and two recruiting threads").
      - Test: synopsis === "" or undefined → counts-only lede, no dangling separator, no throw.
    - collapseNoise(items): partitions into { surfaced: BriefingItem[]; collapsedCount: number }.
      - Test: `category === "newsletter"` items are removed from `surfaced` and counted in `collapsedCount`; a 12-newsletter input yields collapsedCount 12 and zero newsletter rows in `surfaced`.
      - Test: `action`/`fyi`/`other` items stay in `surfaced` (only newsletters collapse — do NOT hide "other", conservative).
      - Test: a `needsReply` item is NEVER collapsed even if mis-categorized newsletter (defense: needs-you wins).
    - buildBriefingView(briefing): returns { lede, needsYou, timeSections, collapsedCount }.
      - Test (ACTION-FIRST, Gap 1.2): `needsYou` = items where `needsReply || deadline`, and `timeSections` (the fyi remainder) contains none of them — needs-you is a separate top block, never interleaved chronologically.
      - Test (TIME PRESERVED, locked constraint): `timeSections` is ordered [today, yesterday, thisWeek], each carrying only its bucket's surfaced-non-needsYou items, empty buckets skipped — proving time grouping survives as the SECONDARY axis.
      - Test (COLLAPSE, Gap 1.3): newsletters are absent from every timeSection and surface only as `collapsedCount`.
      - Test: `lede` equals composeLede over the full item set (counts reflect ALL items incl. collapsed + needs-you).
  </behavior>
  <action>Add the cases to briefing.test.ts importing the new symbols from "./briefing". They MUST fail to compile/run (RED) — the symbols do not exist yet. Do not implement in this task.</action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- briefing</automated>
  </verify>
  <done>New cases present and RED (import/type error or assertion failure) — a genuine red, committed as the test half.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the view model + export it</name>
  <files>packages/core/src/briefing.ts, packages/core/src/index.ts, docs/playbooks/cockpit.md</files>
  <action>
    In briefing.ts, add (pure — no Date.now(), no I/O, matching the module's existing discipline):
    - `export interface DigestBatch { items: DigestItem[]; synopsis: string; }` — the toolless digest's full return (plan 07's digestInbox resolves to this; the model's `synopsis` is the cross-message clause).
    - `export function composeLede(items, listedCount, synopsis?): string` — counts CODE-OWNED: `total = listedCount`, `needsYou = items.filter(i => i.needsReply || i.deadline).length`; build "`{total} messages, {needsYou} need you`", append " — {synopsis.trim()}" only when the trimmed synopsis is non-empty. Never read a number from synopsis.
    - `export function collapseNoise(items): { surfaced: BriefingItem[]; collapsedCount: number }` — collapse `i.category === "newsletter" && !i.needsReply && !i.deadline`; everything else stays surfaced.
    - `export interface BriefingView { lede: string; needsYou: BriefingItem[]; timeSections: { bucket: Bucket; items: BriefingItem[] }[]; collapsedCount: number; }`
    - `export function buildBriefingView(briefing): BriefingView` — accept the persisted row shape (`{ items: BriefingItem[]; listedCount: number; synopsis?: string }`, widen as needed). Compute: `needsYou = items.filter(needsReply||deadline)`; run `collapseNoise` on the REMAINDER (items not in needsYou); `timeSections` = for each bucket in ["today","yesterday","thisWeek"], the surfaced remainder with that bucket, skip empty; `collapsedCount` from collapseNoise; `lede = composeLede(items, listedCount, synopsis)`.
    Keep `bucket`/`joinDigest`/`selectForDigest` untouched — time grouping stays pure and tested (locked constraint). Add a header comment naming Gap 1 and the "reshaped, not deleted" contract for time.
    Re-export the new symbols from packages/core/src/index.ts (the barrel llm.ts + cards.tsx import from).
    §9: bump `Last verified` in docs/playbooks/cockpit.md (briefing.ts is a cockpit.md-watched path) — same commit.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- briefing && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All Task-1 cases GREEN; new symbols exported from the barrel; cockpit.md Last verified bumped; time-grouping tests still pass (preserved).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- briefing` green (incl. the preserved bucket/joinDigest cases).
- `pnpm --filter @pikar/core typecheck` (or workspace typecheck) clean.
- `node scripts/check-playbooks.mjs` exit 0.
</verification>

<success_criteria>
The ordering (action-first), collapse (newsletters → one count), and lede composition (code-owned counts + model synopsis) all live in pure tested `@pikar/core`; time grouping is preserved as the secondary axis. The card (08) and backend (07) now have a typed contract to build against.
</success_criteria>

<output>
After completion, create `.planning/phases/03.7-inbox-briefing/03.7-06-SUMMARY.md`
</output>
