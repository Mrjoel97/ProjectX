---
phase: 03.7-inbox-briefing
plan: 09
subsystem: testing
tags: [eval-gate, inbox-digest, skills-registry, golden-eval, convex, human-verify]

# Dependency graph
requires:
  - phase: 03.7-07
    provides: inbox-digest candidate v2 (toolless cross-message synopsis/lede) + briefings.synopsis persistence
  - phase: 03.7-08
    provides: reshaped BriefingCard rendered from buildBriefingView (lede-first, action-first, collapsed noise)
  - phase: 03.6-04
    provides: pnpm eval:golden runner + EVAL_GATE (activateSkill refuses a gated candidate without passing evidence)
provides:
  - "ledePresent eval expect-key wired end-to-end (smoke read -> evaluateExpect -> selfCheck synthetics), asserted by 16-briefing-happy"
  - "smoke:briefingSynopsisPresent internalQuery (latest briefing has a non-empty trimmed synopsis)"
  - "inbox-digest v2 activated through the live EVAL_GATE — the production digest now synthesizes the lede"
  - "CKPT-04 closed (human-verified intelligent executive report); Phase 3.7 plan-slate 9/9"
affects: [inbox-briefing, agent-eval-gate, future skill re-gates]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Anti-silent-pass eval key (Pitfall 3): ledePresent FAILS when the live synthesis returned an empty synopsis, mirroring briefingPresent"
    - "Live re-gate on a skill-body edit (CLAUDE.md §5): candidate refused pre-evidence -> version-pinned green eval -> activate"

key-files:
  created: []
  modified:
    - packages/backend/convex/smoke.ts
    - packages/backend/scripts/run-eval-golden.mjs
    - packages/backend/scripts/eval-cases/16-briefing-happy.json
    - docs/playbooks/agent-runtime.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md

key-decisions:
  - "inbox-digest v2 activated ONLY through the live EVAL_GATE cycle (refused pre-evidence, activated after all-green run d2f541cb) — no manual bypass"
  - "cockpit-agent was NOT re-gated this phase (its tool-calling behavior did not change); only inbox-digest re-gates on the plan-07 synopsis edit"
  - "The cockpit-briefing Playwright E2E stays the known-blocked regression lock (no E2E creds); the user's live human-verify is its standing substitute and is APPROVED"

patterns-established:
  - "ledePresent expect-key: parallel to briefingPresent, backed by a synopsis smoke read, self-check proves it evaluates against the READ not the plan"

requirements-completed: [CKPT-04]

# Metrics
duration: ~45min (continuation session; incl. environment stabilization)
completed: 2026-07-17
---

# Phase 3.7 Plan 09: Phase Close — inbox-digest v2 Eval-Gated & Human-Verified Summary

**Wired the `ledePresent` anti-silent-pass eval key, ran the live inbox-digest@2 gate cycle (refused pre-evidence -> 18/18 golden green -> activated), and closed CKPT-04 on a human-verified intelligent inbox report — Gap 1 + Gap 2 (presentation) closed.**

## Performance

- **Duration:** ~45 min (continuation agent; a large share was environment stabilization, not plan work)
- **Completed:** 2026-07-17
- **Tasks:** 3 (Task 1 pre-completed & verified; Tasks 2–3 executed this session)
- **Files modified:** 6 (4 in Task 1's prior commit, 2 in this session's Task 3)

## Accomplishments
- **Task 1 (verified, not redone):** `ledePresent` expect-key wired end-to-end — `smoke:briefingSynopsisPresent` read -> `evaluateExpect` branch -> `selfCheck` accept/reject synthetics; `16-briefing-happy.json` asserts `ledePresent:true`; agent-runtime.md bumped.
- **Task 2 (live gate cycle):** `activateSkill inbox-digest@2` REFUSED pre-evidence with `EVAL_GATE`; `pnpm eval:golden --skill inbox-digest@2` ran all 18 cases green (run **d2f541cb**, **$0.0682**); evidence recorded on the v2 row; `activateSkill inbox-digest@2` then SUCCEEDED — **inbox-digest v2 is ACTIVE**. The live synopsis is now exercised by three briefing cases: 16-briefing-happy asserts the non-empty lede; 17-briefing-injection-probe's poisoned body was summarized into a live synopsis yet stayed inert (statusAtMost collecting / recipientCount 0 / attachmentCount 0); 18-briefing-then-action passed.
- **Task 3 (human-verify + CKPT-04):** Human-verify APPROVED — the user viewed the reshaped BRIEFING card live and approved it ("I like it. It looks good. Let's proceed. I approve."). CKPT-04 checked off in REQUIREMENTS.md (list + traceability row) and ROADMAP hand-edited (plan-09 box + status row 6/9 -> 9/9 Complete).

## Task Commits

1. **Task 1: ledePresent expect key + synopsis smoke read (offline)** - `12c0ed5` (feat) — completed & committed prior to this continuation session; verified present.
2. **Task 2: live inbox-digest@2 gate cycle** - no source-file commit (a deployment-state change: evidence recorded + v2 activated in the local Convex deployment; skills.ts unchanged as the plan anticipated).
3. **Task 3: CKPT-04 close** - `a6733e6` (docs) — REQUIREMENTS.md + ROADMAP.md.

**Plan metadata:** SUMMARY.md + STATE.md in the final metadata commit.

_Checkpoint-pause doc from the prior session: `ca2425b`._

## Eval Runs (live spend on local-joel_feruzi-pikar_ai_50c69)

| Run id | Result | Cost | Note |
| --- | --- | --- | --- |
| 4d7801e9 | 7/18 | $0.0253 | ENOTFOUND api.openai.com network blip cascaded 08–18 (environment, not case failure) |
| b883fb44 | 7/18 partial | ~$0.03 | 01–07 clean, then convex-local-backend crashed mid-run (environment) |
| **d2f541cb** | **18/18 PASS** | **$0.0682** | The authoritative green run — evidence recorded on inbox-digest v2 (11 + 18 recovered on the one-retry policy) |

**Total live spend this plan:** ~$0.13 across all attempts (well under the $1.00 cap). The authoritative activation-backing run is **d2f541cb — 18/18 — $0.0682**.

## Files Created/Modified
- `packages/backend/convex/smoke.ts` - `briefingSynopsisPresent` internalQuery (Task 1, prior commit)
- `packages/backend/scripts/run-eval-golden.mjs` - `ledePresent` key + read + selfCheck synthetics (Task 1, prior commit)
- `packages/backend/scripts/eval-cases/16-briefing-happy.json` - asserts `ledePresent:true` (Task 1, prior commit)
- `docs/playbooks/agent-runtime.md` - eval-harness ledePresent note + Last verified bump (Task 1, prior commit)
- `.planning/REQUIREMENTS.md` - CKPT-04 checked (list + traceability row -> Complete, human-verified note) (Task 3)
- `.planning/ROADMAP.md` - plan-09 box checked + status row 6/9 In Progress -> 9/9 Complete (Task 3)

## Decisions Made
- Activated inbox-digest v2 strictly through the live EVAL_GATE (no bypass) — the synopsis prompt is a skill row (CLAUDE.md §5), so a body edit mandates a re-gate.
- Did not check the ROADMAP top-level "Phase 3.7" checkbox and did not run `phase complete` — per orchestrator instruction, phase closure (gsd-verifier -> phase complete -> parent-artifact resolution) is handled after this plan returns.

## Deviations from Plan

None to the plan's task logic — all tasks executed as written. Two operational (environment) deviations were handled under Rule 3 (blocking):

### Environment Handling

**1. [Rule 3 - Blocking] Transient network + backend instability during the live eval**
- **Found during:** Task 2 (live gate cycle)
- **Issue:** Two eval attempts were killed by environment: an `ENOTFOUND api.openai.com` DNS blip (run 4d7801e9, cascaded 08–18) and a `convex-local-backend.exe` crash mid-run (InternalServerError / "waiting for local backend to start"). Both are environment failures, not case-logic failures — cases 01–07 passed cleanly on both prior attempts, confirming the code path was healthy throughout.
- **Fix:** Per the one-retry / fresh-run policy, re-ran on a clean fresh tenant. When the local backend binary had crashed (port 3210 dead), the deployment was restarted to "Convex functions ready" (same deployment, sqlite state intact: v1 active / v2 gated / no premature evidence). The orchestrator then verified a stable environment (fresh convex dev, no retry storm, DNS reachable) and signaled GO; the authoritative run d2f541cb then went 18/18 green.
- **Verification:** Gate refused v2 pre-evidence, then activated post-evidence; `getActiveSkill inbox-digest` returns version 2.
- **Committed in:** N/A (no source change; deployment-state only).

## Issues Encountered
- **E2E remains blocked (standing since 03.3):** `pnpm --filter @pikar/web test:e2e -- cockpit-briefing` cannot run — `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` are absent from the environment, and the composer is gated on a real Gmail OAuth token no smoke helper can seed. This was NOT faked. The user's LIVE human-verify (Task 3, APPROVED) is the documented standing substitute for that regression lock.
- Eval environment flakiness (DNS + local-backend crash) delayed Task 2; resolved by fresh re-runs on a stabilized backend (see Deviations).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Gap 1 (intelligent briefing) + Gap 2 (presentation) are closed; inbox-digest v2 is eval-gated and active in the local deployment.
- CKPT-04 is complete. Phase 3.7 plan-slate is 9/9. Phase-level closure (goal verification + `phase complete` + top-level box) is left to the orchestrator.
- Standing blocker to carry forward: the cockpit-briefing E2E needs a Gmail-connected harness user + E2E creds before it can execute in CI; until then live human-verify substitutes.

## Self-Check: PASSED

- FOUND: `.planning/phases/03.7-inbox-briefing/03.7-09-SUMMARY.md`
- FOUND commit `12c0ed5` (Task 1) and `a6733e6` (Task 3)
- Verified: `getActiveSkill inbox-digest` returns `version: 2` (v2 active post-gate)
- Verified: key files exist with `ledePresent` (run-eval-golden.mjs) and `briefingSynopsisPresent` (smoke.ts)

---
*Phase: 03.7-inbox-briefing*
*Completed: 2026-07-17*
