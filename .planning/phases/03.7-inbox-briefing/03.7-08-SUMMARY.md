---
phase: 03.7-inbox-briefing
plan: 08
subsystem: ui
tags: [react, convex, briefing, cockpit, playwright, "@pikar/core"]

# Dependency graph
requires:
  - phase: 03.7-06
    provides: "@pikar/core buildBriefingView / composeLede / collapseNoise — the pure view model (lede, action-first needs-you, time-grouped fyi, collapsed-noise count)"
  - phase: 03.7-07
    provides: "backend synthesis — digestInbox returns DigestBatch{items,synopsis}; briefings.synopsis persisted; offline smoke digest flags one newsletter item"
provides:
  - "BriefingCard reshaped as a dumb renderer over buildBriefingView: lede-first, action-first needs-you, time-grouped fyi remainder, collapsed-noise row, per-row category tag"
  - "cockpit-briefing.spec.ts extended to lock Gap 1.1/1.2/1.3/1.4 offline (lede-first, action-first order, category tag, collapsed row with count)"
affects: [03.7-09, cockpit, inbox-briefing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Card as a dumb renderer over a pure @pikar/core view model — no ordering/collapse/lede logic in the component (ADR-004)"
    - "Category/collapsed-count surfaced as TEXT only — SC-4 (zero button/link/onClick) preserved structurally"

key-files:
  created: []
  modified:
    - "apps/web/app/(app)/dashboard/workspace/cards.tsx"
    - "apps/web/e2e/cockpit-briefing.spec.ts"
    - "docs/playbooks/cockpit.md"

key-decisions:
  - "BriefingCard renders buildBriefingView output verbatim — the intelligence stays in @pikar/core, never re-derived in the component"
  - "Category tag lives in RowBody (shared) so needs-you AND fyi rows both show it; muted --ink-soft on a --rule hairline, no amber"
  - "Collapsed noise is ONE muted text line (briefing-collapsed), not a clickable disclosure — SC-4 holds"

patterns-established:
  - "SECTION_META bucket→title/testid lookup replaces the card-owned BUCKETS filter loop; the view emits the ordered, empty-skipped sections"

requirements-completed: [CKPT-04]

# Metrics
duration: 22min
completed: 2026-07-17
---

# Phase 03.7 Plan 08: Reshaped BRIEFING Card Summary

**BriefingCard rebuilt as a dumb renderer over `@pikar/core`'s `buildBriefingView` — lede-first, action-first needs-you above a time-grouped fyi remainder, automated-notification noise collapsed to one text line, and the model's category axis finally shown as a per-row tag — with SC-4 (zero action controls) intact.**

## Performance

- **Duration:** ~22 min
- **Started:** 2026-07-17T17:00Z
- **Completed:** 2026-07-17T17:22Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- BriefingCard now renders the intelligent report the digest already computes: `view.lede` at the top (code-owned counts welded to the model's synopsis clause), `view.needsYou` as the action-first top block, `view.timeSections` as the secondary time-grouped axis below it, a single `briefing-collapsed` line for `view.collapsedCount`, and a `briefing-category` text tag on every surfaced row.
- All ordering/collapse/lede intelligence stays in `@pikar/core` — the component re-derives none of it (ADR-004). The card-owned `BUCKETS` filter loop and the local `needsYou` computation were removed in favour of `SECTION_META` (bucket → title/testid chrome only).
- The offline E2E now locks Gap 1.1/1.2/1.3/1.4: lede precedes the first row, the needs-you (Sarah Chen) row is the first row, a category tag reads "action", and the collapsed row renders WITH its count — an offline regression lock (the smoke digest guarantees ≥1 newsletter), not a human-only check.
- SC-4 preserved structurally: the collapsed row and category tags are TEXT, not toggles/links; the card keeps zero button/link/onClick (read-confirmed; the spec asserts `button`/`a` `toHaveCount(0)`).

## Task Commits

Each task was committed atomically:

1. **Task 1: reshape BriefingCard over buildBriefingView** - `d799afa` (feat)
2. **Task 2: extend the offline E2E + bump cockpit.md** - `ab71055` (test)

**Plan metadata:** _(final docs commit — this SUMMARY + STATE + ROADMAP)_

## Files Created/Modified
- `apps/web/app/(app)/dashboard/workspace/cards.tsx` - BriefingCard reshaped over `buildBriefingView`; new `briefing-lede` line, `SECTION_META` lookup, `briefing-collapsed` row, and a `briefing-category` tag in `RowBody`. Row primitives (`BriefingRow`/`BriefingSection`/`SenderCell`/`RowBody`/`SubjectLine`, the 3-column grid, `item.id` keys, tz-formatted times) reused unchanged.
- `apps/web/e2e/cockpit-briefing.spec.ts` - Added lede-first, action-first, category-tag, and collapsed-row-with-count assertions; kept the SC-4 zero-controls and SC-2 counts-only-reply assertions.
- `docs/playbooks/cockpit.md` - Added the 03.7-08 `Last verified` entry (§9; cards.tsx + e2e are cockpit.md-watched).

## Decisions Made
- Rendered `view.lede` as a prominent `--ink`/medium-weight line and pushed the `range · tz · built` meta line below it — the lede is the story read first, the meta is provenance.
- Placed the category tag in `RowBody` (shared by needs-you and fyi rows) rather than a new grid cell, so it rides the existing row layout with no column-grid change and both row kinds show it.
- Kept the three section testids (`briefing-section-today/-yesterday/-thisweek`) via `SECTION_META` so the existing E2E section-visible assertions carry over unchanged.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. Web typecheck exit 0 on both tasks; Biome baseline-diffed on `cards.tsx` (no new findings — the sole `useHookAtTopLevel` at ResolutionCard is pre-existing and unrelated); Playwright discovers the spec; `check-playbooks` exit 0.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The reshaped card is offline-verified (typecheck + Playwright discovery + the extended assertions type-load). **NOT yet run live** — the standing blockers are unchanged: `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` are absent from the env and `page.tsx` gates the composer on `gmailAuth.status.connected`, which no smoke helper can seed.
- Plan 09 (the connected human-verify session) runs `pnpm --filter @pikar/web test:e2e -- cockpit-briefing` with a Gmail-connected user by construction, proves the live paint of the reshaped card, and closes CKPT-04.

## Self-Check: PASSED

- FOUND: apps/web/app/(app)/dashboard/workspace/cards.tsx
- FOUND: apps/web/e2e/cockpit-briefing.spec.ts
- FOUND: docs/playbooks/cockpit.md
- FOUND: .planning/phases/03.7-inbox-briefing/03.7-08-SUMMARY.md
- FOUND commit: d799afa (Task 1)
- FOUND commit: ab71055 (Task 2)

---
*Phase: 03.7-inbox-briefing*
*Completed: 2026-07-17*
