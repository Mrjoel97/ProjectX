---
phase: 15.4-vault-redesign-and-scoped-browse-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/vault.ts
  - packages/backend/convex/vault.test.ts
  - docs/playbooks/vault.md
autonomous: true
requirements: [VALT-16]

must_haves:
  truths:
    - "A search performed inside folder A cannot return a document from folder B, even when hybrid search proposes both IDs."
    - "Root search remains backward-compatible when folderId is omitted."
    - "Tenant isolation and ingesting-folder sealing remain enforced after the search contract widens."
    - "A foreign or missing folderId returns no scoped results without becoming an ownership oracle."
  artifacts:
    - path: "packages/backend/convex/vault.ts"
      provides: "optional folderId on the bounded tenant-scoped vaultSearch contract"
      contains: "folderId"
    - path: "packages/backend/convex/vault.test.ts"
      provides: "cross-folder, root, category, isolation and sealed-search regression coverage"
  key_links:
    - from: "vaultSearch"
      to: "vaultDocuments.folderId"
      via: "bounded candidate metadata resolution and server-side equality filter"
      pattern: "folderId"
---

<objective>
Make Vault hybrid search honor the folder currently being browsed.

Purpose: `listVaultDocs` is already scoped by `folderId`, but `vaultSearch` is not. A visual redesign on top of that mismatch would ship a silent cross-folder correctness defect.

Output: a backward-compatible optional `folderId` search argument with mutation-resistant tests and no schema change.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-CONTEXT.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-RESEARCH.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-VALIDATION.md
@packages/backend/AGENTS.md
@packages/backend/convex/_generated/ai/guidelines.md
@packages/backend/convex/vault.ts
@packages/backend/convex/vault.test.ts
@packages/backend/convex/vaultSealing.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pin and implement the folder-search boundary</name>
  <files>packages/backend/convex/vault.test.ts, packages/backend/convex/vault.ts</files>
  <action>
Use a red/green/refactor loop inside this single task. First extend the existing Vault action tests around the `SMOKE::` candidate seam. Seed two ready documents owned by the same tenant in folders A and B, plus a foreign-tenant document and an ingesting/sealed member. Add assertions for:

- `{ folderId: A }` returns only A when candidates contain A and B;
- `{ folderId: A, category }` returns the category/folder intersection;
- omitted `folderId` preserves current root behavior;
- foreign/missing folder IDs expose no document and no ownership distinction;
- sealed members stay excluded.

Make the cross-folder assertion mutation-resistant: removing only the folder predicate must turn it red. Confirm the new scoped assertion is red, then add `folderId?: Id<"vaultFolders">` to `vaultSearch`. Validate it through tenant-owned data without revealing whether a foreign ID exists. Carry folder identity through a dedicated narrow metadata resolver or a backward-compatible extension of `ownedDocsMeta`; if extending the shared resolver, keep result order and every existing field unchanged because grounding relies on index-parallel results.

Filter after tenant ownership and sealed-member resolution, intersecting folder and optional category. Candidate count stays bounded by the existing RAG limit, so do not touch `schema.ts`. Keep folderId optional so all existing callers continue to work.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- vault.test.ts vaultFolders.test.ts vaultSealing.test.ts</automated>
  </verify>
  <done>All scoped/root/isolation/sealing cases pass at task completion, and a temporary local mutation deleting the folder comparison was observed to make the cross-folder test fail before being restored.</done>
</task>

<task type="auto">
  <name>Task 2: record the scoped-search invariant and close backend gates</name>
  <files>packages/backend/convex/vault.ts, packages/backend/convex/vault.test.ts, docs/playbooks/vault.md</files>
  <action>
Update `docs/playbooks/vault.md` with the optional folder-scoped search contract, isolation/sealing invariant, focused test command, rollback note and current `Last verified` line. Run focused tests and backend typecheck. Confirm the public return shape still excludes `text`, `tenantId`, content hashes and filenames; no `.collect()` or schema edit was introduced. Review the diff specifically for the shared `ownedDocsMeta` ordering contract.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Focused tests, backend typecheck and playbook watcher pass with a zero-migration, metadata-only contract widening.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- vault.test.ts vaultFolders.test.ts vaultSealing.test.ts`
- `pnpm --filter @pikar/backend typecheck`
- Mutation check: remove the folder comparison and confirm the same-tenant cross-folder test fails.
</verification>

<success_criteria>
- Folder search is server-scoped and tenant-safe.
- Existing root search and sealing behavior are unchanged.
- No schema/read-cap/privacy blast radius is added.
</success_criteria>

<output>
Create `.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-01-SUMMARY.md`.
</output>
