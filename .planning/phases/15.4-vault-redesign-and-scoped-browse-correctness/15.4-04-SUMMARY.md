---
phase: 15.4-vault-redesign-and-scoped-browse-correctness
plan: 04
subsystem: vault-verification
tags: [playwright, convex, nextjs, vault, e2e, uat, playbooks]

requires:
  - phase: 15.4-03
    provides: connected Nord Edge preview/import surfaces and retained governed controls
provides:
  - Executed authenticated Playwright coverage over the live Vault and local Convex data plane
  - Full backend/web regression, typecheck, production-build and playbook-watcher evidence
  - Regression-safe deletion of offline SMOKE-ingested documents
  - Owner-approved responsive and control-retention UAT
affects: [phase-26-pending-pages, vault-playbook, cockpit-playbook]

tech-stack:
  added: []
  patterns:
    - Seed an authenticated E2E tenant through the internal idempotent onboarding seam
    - Use the stable user-id segment of the JWT subject at the backend trust boundary
    - Keep external Drive and model-backed digest success honest rather than faking them in E2E

key-files:
  created:
    - apps/web/e2e/vault-redesign.spec.ts
  modified:
    - packages/backend/convex/vault.ts
    - packages/backend/convex/vault.test.ts
    - packages/backend/convex/vaultDigest.test.ts
    - docs/playbooks/vault.md
    - docs/playbooks/cockpit.md

key-decisions:
  - "The browser gate must execute against the real authenticated route and live local Convex deployment."
  - "Only explicit smoke::<hash> sentinels skip RAG deletion; real RAG IDs keep the existing cascade."
  - "Drive import and digest AI success remain live capabilities and are not simulated as successful."

patterns-established:
  - "Connected E2E non-vacuity: pass both an in-folder and same-tenant out-of-folder document ID and assert only the member survives."
  - "Synthetic component IDs are detected narrowly at the component boundary, without weakening real-ID cleanup."

requirements-completed: [VALT-16]

duration: 2h08m
completed: 2026-08-05
---

# Phase 15.4 Plan 04: Connected Vault Verification Summary

**The approved Nord Edge Vault ran end to end on its authenticated route, the browser gate caught
and fixed a real deletion defect, every automated release gate passed, and the owner approved the
responsive connected surface.**

## Performance

- **Duration:** 2h08m
- **Started:** 2026-08-04T19:29:13Z
- **Completed:** 2026-08-04T21:37:43Z
- **Tasks:** 2 (one automated task, one blocking owner checkpoint)
- **Files changed:** 6

## Accomplishments

- Added and executed authenticated browser coverage for root/category browse, real folder upload,
  scoped search isolation, preview provenance/download controls, Escape/focus restoration,
  confirmation-gated deletion, and empty/no-result recovery.
- Caught a connected-only defect where offline ingest's synthetic `smoke::<contentHash>` value was
  passed to the RAG component's branded-ID validator during delete.
- Fixed that defect narrowly while retaining row, graph-ownership, real-RAG and digest-cancellation
  cleanup, with focused regression tests.
- Passed both full package suites, both typechecks, a production Next build, and both playbook
  watchers; refreshed the Vault and Cockpit playbooks with the real runtime and rollback boundary.
- Received owner approval for the live responsive/control-retention checkpoint on 2026-08-05.

## Task Commits

1. **Connected deletion defect found by E2E:** `4df7ac0` — `fix(vault): delete smoke-ingested documents safely`
2. **Executed browser coverage and watched playbooks:** `c4c041b` — `test(vault): execute connected redesign coverage`

## Verification

| Gate | Result |
|---|---|
| Focused Playwright Vault run | **2/2 passed** (auth setup + connected Vault journey) |
| Focused backend delete/digest regression | **43/43 passed** |
| Full `@pikar/backend` suite | **passed**, exit 0 |
| Full `@pikar/web` suite | **5/5 files, 43/43 tests passed** |
| Backend and web typechecks | **passed**, exit 0 each |
| Next production build | **passed**, all 21 pages generated; `/dashboard/vault` present |
| `node scripts/check-playbooks.mjs` | **passed**, exit 0 |
| Authenticated owner UAT | **approved 2026-08-05** |

The first sandboxed build attempt could not reach Google Fonts. The unchanged build was rerun with
network access and completed successfully; this was an environment boundary, not a code failure.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] SMOKE-ingested documents could not be deleted**

- **Found during:** Task 1 connected Playwright execution.
- **Issue:** Offline ingest stores a synthetic `smoke::<contentHash>` reference, but deletion treated
  it as a real RAG component ID and failed validation before closing the confirmation flow.
- **Fix:** Skip component deletion only for the explicit sentinel; preserve every other cleanup path.
- **Evidence:** Direct-delete and folder-cancellation regression tests, plus the green connected E2E.
- **Committed in:** `4df7ac0`.

---

**Total deviations:** 1 auto-fixed bug. No schema, index, migration, dependency, Drive scope, or
production data change was required.

## Known Honest Boundaries

- Google Drive import remains a real external integration; the E2E verifies its reachable entry
  point but does not fake provider success.
- Model-backed folder digest success requires a configured model key. Offline UAT verifies the
  action/error boundary, not fabricated AI output.
- Exact category counts remain intentionally absent until a maintained bounded counter contract
  exists.

## Next Phase Readiness

- Phase 15.4 is complete and `VALT-16` has automated and owner-approved evidence.
- The non-Vault mockup pages remain in Phase 26. That phase is a zero-plan placeholder and must be
  explicitly planned/resequenced before any of its navigation entries become live.
- The detailed dependency and cost map is
  `docs/superpowers/plans/2026-08-04-pending-pages-vault-integration-plan.md`.

## Self-Check: PASSED

The E2E spec, both watched playbook updates, both implementation commits, all automated gates, and
the owner approval are present.

---
*Phase: 15.4-vault-redesign-and-scoped-browse-correctness*
*Completed: 2026-08-05*
