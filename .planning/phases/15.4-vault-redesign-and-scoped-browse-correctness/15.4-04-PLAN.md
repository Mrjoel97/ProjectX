---
phase: 15.4-vault-redesign-and-scoped-browse-correctness
plan: 04
type: execute
wave: 4
depends_on: ["15.4-03"]
files_modified:
  - apps/web/e2e/vault-redesign.spec.ts
  - docs/playbooks/vault.md
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [VALT-16]

must_haves:
  truths:
    - "The focused Vault Playwright spec is executed, not merely authored."
    - "Backend/web suites, typechecks, production build and playbook watcher pass after the complete redesign."
    - "Both playbooks watching the Vault E2E path are current in the same executor session."
    - "An authenticated owner verifies the real route at responsive widths and confirms every retained control."
  artifacts:
    - path: "apps/web/e2e/vault-redesign.spec.ts"
      provides: "browser regression coverage for connected Vault route states"
    - path: "docs/playbooks/vault.md"
      provides: "final Vault invariants, commands and verification record"
    - path: "docs/playbooks/cockpit.md"
      provides: "E2E watch-map acknowledgement and current verification record"
  key_links:
    - from: "apps/web/e2e/vault-redesign.spec.ts"
      to: "/dashboard/vault"
      via: "authenticated Playwright navigation and user-visible assertions"
      pattern: "dashboard/vault"
---

<objective>
Close Phase 15.4 with actually executed browser coverage, repository-wide automated gates and authenticated owner UAT.

Purpose: verify the connected route rather than accepting static mock fidelity, and satisfy both playbook watchers governing the E2E path.

Output: focused Playwright evidence, green full gates, current Vault/Cockpit playbooks and owner approval.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-CONTEXT.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-VALIDATION.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-03-SUMMARY.md
@docs/design/mockups/vault-redesign.html
@docs/playbooks/vault.md
@docs/playbooks/cockpit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: execute browser coverage and every final automated gate</name>
  <files>apps/web/e2e/vault-redesign.spec.ts, docs/playbooks/vault.md, docs/playbooks/cockpit.md</files>
  <action>
Add stable Playwright coverage for deterministic route composition and state transitions supported by the repository's authenticated/fixture setup: root/category search, folder open and folder-scoped search, preview open/close, and empty/no-results recovery. Do not fake successful external Drive import.

Run the focused spec explicitly, then both package test suites, both typechecks and the production build. Update `docs/playbooks/vault.md` with final commands, rollback and `Last verified`. Because `apps/web/e2e/` is also watched by `docs/playbooks/cockpit.md`, record this Vault-specific spec and refresh that playbook's verification line in the same task. Run `check-playbooks`; do not suppress a failure or weaken a test.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test:e2e -- e2e/vault-redesign.spec.ts &amp;&amp; pnpm --filter @pikar/backend test &amp;&amp; pnpm --filter @pikar/web test &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; pnpm --filter @pikar/web typecheck &amp;&amp; pnpm --filter @pikar/web build &amp;&amp; node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The focused Playwright spec actually runs, all final gates pass, and both watching playbooks are current.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: authenticated responsive and control-retention UAT</name>
  <files>none</files>
  <action>
Present the running authenticated `/dashboard/vault` route and the mockup side by side. Ask the owner to verify desktop/tablet/mobile root browse; categories/search; folder open and scoped search; preview text/provenance/correction/download/delete; upload/folder upload/Drive entry; digest rebuild; empty/no-results/error recovery; keyboard/Escape/focus restoration; and absence of fabricated category counts.
  </action>
  <verify>
    <manual>Owner types "approved" or reports exact visual/behavioral defects.</manual>
  </verify>
  <done>Owner approves the real connected route, not the static HTML mock.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web test:e2e -- e2e/vault-redesign.spec.ts`
- `pnpm --filter @pikar/backend test && pnpm --filter @pikar/web test`
- `pnpm --filter @pikar/backend typecheck && pnpm --filter @pikar/web typecheck && pnpm --filter @pikar/web build`
- `node scripts/check-playbooks.mjs`
- Authenticated responsive owner UAT.
</verification>

<success_criteria>
- The complete connected Vault redesign passes automated and human verification.
- Both watched playbooks match the shipped implementation.
- VALT-16 has goal-level evidence.
</success_criteria>

<output>
Create `.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-04-SUMMARY.md` after the checkpoint is approved.
</output>
