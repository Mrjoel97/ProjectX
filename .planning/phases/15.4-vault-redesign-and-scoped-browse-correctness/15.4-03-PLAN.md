---
phase: 15.4-vault-redesign-and-scoped-browse-correctness
plan: 03
type: execute
wave: 3
depends_on: ["15.4-02"]
files_modified:
  - apps/web/app/(app)/dashboard/vault/PreviewModal.tsx
  - apps/web/app/(app)/dashboard/vault/Dropzone.tsx
  - apps/web/app/(app)/dashboard/vault/PreFlight.tsx
  - apps/web/app/(app)/dashboard/vault/DriveBrowser.tsx
  - apps/web/app/(app)/dashboard/vault/previewState.ts
  - apps/web/app/(app)/dashboard/vault/previewState.test.ts
  - apps/web/app/(app)/dashboard/vault/PreviewControls.tsx
  - apps/web/app/(app)/dashboard/vault/PreviewControls.test.tsx
  - docs/playbooks/vault.md
autonomous: true
requirements: [VALT-16]

must_haves:
  truths:
    - "The redesigned preview retains extracted text, provenance/entities, identity correction, download and confirmed delete."
    - "Processing, failed, unsupported/no-preview and long-content documents have explicit usable states."
    - "Upload, folder upload and Drive import surfaces remain usable and visually coherent with the redesign."
    - "Preview/import component tests, web typecheck and the Vault playbook watcher pass before final browser verification."
  artifacts:
    - path: "apps/web/app/(app)/dashboard/vault/PreviewModal.tsx"
      provides: "Nord Edge document detail with every existing governed control"
    - path: "apps/web/app/(app)/dashboard/vault/previewState.test.ts"
      provides: "preview state and control-retention regression coverage"
    - path: "apps/web/app/(app)/dashboard/vault/PreviewControls.test.tsx"
      provides: "component/handler tests for metadata correction, citations, download and confirmed delete"
  key_links:
    - from: "PreviewModal.tsx"
      to: "vaultDocText/vaultDownloadUrl/docEntities and metadata/delete mutations"
      via: "existing lazy queries and guarded handlers"
      pattern: "api.vault"
---

<objective>
Finish the Nord Edge Vault with a connected preview/import experience and its watched subsystem record.

Purpose: the redesign is incomplete if its most sensitive actions disappear behind a pretty card or if exceptional document states silently degrade.

Output: retained preview/import controls, explicit detail states, focused automated evidence, and an updated Vault playbook.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-CONTEXT.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-RESEARCH.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-VALIDATION.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-02-SUMMARY.md
@docs/design/mockups/vault-redesign.html
@apps/web/app/(app)/dashboard/vault/PreviewModal.tsx
@apps/web/app/(app)/dashboard/vault/Dropzone.tsx
@apps/web/app/(app)/dashboard/vault/DriveBrowser.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: pin preview states and retained controls</name>
  <files>apps/web/app/(app)/dashboard/vault/previewState.ts, apps/web/app/(app)/dashboard/vault/previewState.test.ts</files>
  <action>
Create a pure preview state/control model and tests for ready text, ready binary/no inline preview, processing, extraction failure, unsupported/missing bytes and long/truncated content. Pin capability rules for identity correction, citations/entities, download and delete; delete must remain confirmation-gated and foreign/missing data must never create an active control.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- previewState.test.ts</automated>
  </verify>
  <done>Every document lifecycle state and retained control is represented by an automated pure contract.</done>
</task>

<task type="auto">
  <name>Task 2: pin preview controls with component and handler tests</name>
  <files>apps/web/app/(app)/dashboard/vault/PreviewControls.tsx, apps/web/app/(app)/dashboard/vault/PreviewControls.test.tsx</files>
  <action>
Extract pure preview control/presenter components with explicit callbacks. Render them under Vitest with `react-dom/server` and assert metadata correction, provenance/citations, signed-download affordance and delete-confirmation states. Exercise exported callback adapters/descriptors so each control is pinned to the intended existing query/mutation handler; confirm delete cannot invoke the destructive handler until the confirmation state is true. Tests must fail if any retained action is removed or bypasses confirmation.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- PreviewControls.test.tsx</automated>
  </verify>
  <done>Every retained preview action has component-level presence and callback/confirmation regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: redesign preview and import surfaces without reducing capability</name>
  <files>apps/web/app/(app)/dashboard/vault/PreviewModal.tsx, apps/web/app/(app)/dashboard/vault/PreviewControls.tsx, apps/web/app/(app)/dashboard/vault/Dropzone.tsx, apps/web/app/(app)/dashboard/vault/PreFlight.tsx, apps/web/app/(app)/dashboard/vault/DriveBrowser.tsx, docs/playbooks/vault.md</files>
  <action>
Apply the approved preview/import visual composition using the shared Vault tokens from Plan 02. Keep lazy queries, identity/type correction, entity provenance, signed download, confirmed delete, folder budget estimate/reservation and the custom Drive browser, wiring preview actions through the tested presenter. Make modal semantics accessible: labelled dialog, Escape, focus restoration, focus-visible controls and non-scrolling background where the current framework allows. Render explicit state copy from the pure model. Update `docs/playbooks/vault.md` in this executor session with preview/import control invariants, state coverage, commands, rollback and current `Last verified` line.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- vault &amp;&amp; pnpm --filter @pikar/web typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The preview/import experience matches the design direction, every pre-existing control remains functional/accessibile, and the watched Vault playbook is current.</done>
</task>

</tasks>

<verification>
- Focused Vault web tests and web typecheck pass.
- `node scripts/check-playbooks.mjs` passes and `docs/playbooks/vault.md` carries this plan's preview/import verification record.
- No mock-only data, category count, hidden real control or telemetry content leak ships.
</verification>

<success_criteria>
- All approved Vault states are visually delivered on live contracts.
- Existing mature capability is retained.
- VALT-16 has automated and human evidence.
</success_criteria>

<output>
Create `.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-03-SUMMARY.md`.
</output>
