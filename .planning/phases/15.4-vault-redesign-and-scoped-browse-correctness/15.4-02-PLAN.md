---
phase: 15.4-vault-redesign-and-scoped-browse-correctness
plan: 02
type: execute
wave: 2
depends_on: ["15.4-01"]
files_modified:
  - apps/web/app/(app)/dashboard/vault/page.tsx
  - apps/web/app/(app)/dashboard/vault/DocGrid.tsx
  - apps/web/app/(app)/dashboard/vault/CategoryTabs.tsx
  - apps/web/app/(app)/dashboard/vault/FolderBreadcrumb.tsx
  - apps/web/app/(app)/dashboard/vault/VaultStats.tsx
  - apps/web/app/(app)/dashboard/vault/vaultViewState.ts
  - apps/web/app/(app)/dashboard/vault/vaultViewState.test.ts
  - apps/web/app/(app)/dashboard/vault/VaultBrowseControls.tsx
  - apps/web/app/(app)/dashboard/vault/VaultBrowseControls.test.tsx
  - apps/web/app/globals.css
  - docs/playbooks/vault.md
autonomous: true
requirements: [VALT-16]

must_haves:
  truths:
    - "Root and inside-folder browse use the approved Nord Edge hierarchy while every current import/upload/folder action remains reachable."
    - "Folder search passes folderId to the server and stale results cannot cross a folder/query transition."
    - "True empty, category empty, folder empty, no-results, loading, error, partial-ingest and stale-digest states remain distinguishable."
    - "No category badge claims a count that the bounded backend does not provide."
  artifacts:
    - path: "apps/web/app/(app)/dashboard/vault/DocGrid.tsx"
      provides: "connected root/folder cards and scoped search"
      contains: "folderId"
    - path: "apps/web/app/(app)/dashboard/vault/vaultViewState.ts"
      provides: "pure exhaustive state derivation for honest browse copy"
    - path: "apps/web/app/(app)/dashboard/vault/VaultBrowseControls.test.tsx"
      provides: "component-level retention coverage for upload, folder upload, Drive import, folder open and digest rebuild"
    - path: "apps/web/app/globals.css"
      provides: "scoped semantic Nord Edge Vault tokens and responsive composition"
  key_links:
    - from: "DocGrid.tsx"
      to: "api.vault.vaultSearch"
      via: "query/category/folderId action arguments"
      pattern: "vaultSearch"
    - from: "page.tsx"
      to: "existing Dropzone/PreFlight/DriveBrowser flows"
      via: "retained live handlers and dialogs"
---

<objective>
Apply the Nord Edge design to the connected root and folder-browse experience, including honest state handling and the new scoped-search contract.

Purpose: deliver the dominant Vault redesign without replacing the mature ingestion/folder data plane or fabricating mock-only data.

Output: responsive root/folder composition, semantic styles, exhaustive state derivation/tests, and retained real controls.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-CONTEXT.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-RESEARCH.md
@.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-01-SUMMARY.md
@docs/design/mockups/vault-redesign.html
@apps/web/app/(app)/dashboard/vault/page.tsx
@apps/web/app/(app)/dashboard/vault/DocGrid.tsx
@apps/web/app/(app)/dashboard/vault/CategoryTabs.tsx
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: build and pin the honest Vault view-state model</name>
  <files>apps/web/app/(app)/dashboard/vault/vaultViewState.ts, apps/web/app/(app)/dashboard/vault/vaultViewState.test.ts</files>
  <action>
Extract a small pure state classifier used by the browse UI. Cover root empty, empty category, empty folder, no search results, initial loading, list/search error, partial ingest/failed member and stale digest. Keep it data-oriented—no React test dependency or new package. Test every state and add a mutation assertion showing an error cannot collapse to an empty result.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- vaultViewState.test.ts</automated>
  </verify>
  <done>The state matrix is exhaustive and pure, with errors/partial states structurally distinct from empty.</done>
</task>

<task type="auto">
  <name>Task 2: pin retained browse actions at component level</name>
  <files>apps/web/app/(app)/dashboard/vault/VaultBrowseControls.tsx, apps/web/app/(app)/dashboard/vault/VaultBrowseControls.test.tsx</files>
  <action>
Extract the Nord Edge browse action cluster/status presenters as pure React components receiving explicit callbacks/state. With existing React/`react-dom/server` and Vitest (no new DOM dependency), render the presenters and assert the upload, folder upload, Drive import, folder open and digest rebuild controls/labels appear in their valid states. Add a small handler-contract test that invokes exported action descriptors/callback adapters and proves each maps to the correct existing page handler. Include negative assertions for hidden/disabled states so a dead decorative button cannot satisfy the test.

The tests must fail if any retained action is removed or rewired to the wrong callback. Do not mock successful external Drive behavior; this is component/handler wiring coverage.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- VaultBrowseControls.test.tsx</automated>
  </verify>
  <done>Every retained root/folder action has a component-level presence and callback-wiring regression test.</done>
</task>

<task type="auto">
  <name>Task 3: connect scoped search and Nord Edge root/folder composition</name>
  <files>apps/web/app/(app)/dashboard/vault/page.tsx, apps/web/app/(app)/dashboard/vault/DocGrid.tsx, apps/web/app/(app)/dashboard/vault/CategoryTabs.tsx, apps/web/app/(app)/dashboard/vault/FolderBreadcrumb.tsx, apps/web/app/(app)/dashboard/vault/VaultStats.tsx, apps/web/app/(app)/dashboard/vault/VaultBrowseControls.tsx</files>
  <action>
Reshape the existing component tree to the approved mock hierarchy while preserving all real handlers and wiring them through the tested browse presenters. Pass active `folderId` to `vaultSearch`; include folderId in request/race identity so a prior response cannot paint into a newly opened folder. Keep category filtering at root and in folder as supported by the backend.

Retain single-file upload, folder upload, Google Drive browser/import, folder open/cancel/rebuild, manifest warnings, processing/failure pills, load bounds and capped-stat copy. Do not add category counts. Wire the pure state model to distinct loading/error/empty/no-results/partial/stale render branches. Keep semantic headings, buttons and labels.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- vault &amp;&amp; pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>Both root and folder browse render through real queries/actions, scoped search cannot leak another folder, and every existing entry/action remains represented.</done>
</task>

<task type="auto">
  <name>Task 4: add scoped Nord Edge tokens, update the playbook, and close UI gates</name>
  <files>apps/web/app/globals.css, docs/playbooks/vault.md</files>
  <action>
Translate the mock's warm canvas, navy ink, soft borders, low shadows, accent states, radii, spacing and responsive grid into semantic Vault tokens/classes. Scope rules under a Vault root class where possible. Do not change global `.pane-canvas` or `.clay-card` semantics used by other pages. Add hover/focus/disabled/reduced-motion states and mobile/tablet breakpoints without page-local inline hex values. Update `docs/playbooks/vault.md` in this same executor session with the state matrix, retained browse actions, scoped-style boundary, focused commands, rollback and current `Last verified` line.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck &amp;&amp; pnpm --filter @pikar/web build &amp;&amp; node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The root/folder UI builds, stays responsive, and the diff does not globally recolor unrelated dashboard components.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/web test -- vault`
- `pnpm --filter @pikar/web typecheck`
- `pnpm --filter @pikar/web build`
- Static review: no category-count badges, no inline mock data, no new dependency, no altered global clay/pane semantics.
</verification>

<success_criteria>
- Approved Nord Edge root/folder layout is connected to the real Vault.
- Folder search uses the Phase 15.4 backend scope.
- Honest state distinctions and existing actions survive the redesign.
</success_criteria>

<output>
Create `.planning/phases/15.4-vault-redesign-and-scoped-browse-correctness/15.4-02-SUMMARY.md`.
</output>
