---
phase: 03.3-attachment-generation
plan: 04
type: execute
wave: 2
depends_on: ["03.3-01", "03.3-02"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
requirements: [CKPT-02]
autonomous: true
must_haves:
  truths:
    - "The agent has a governed generateAttachment tool: scan topic → draft → render PDF → store → record ref on the plan"
    - "regenerateAttachment supersedes in place (new bytes, delete old storage after the new ref persists)"
    - "removeAttachment drops one ref and deletes its storage (no orphan bytes)"
    - "A render-failed or over-byte-cap attachment makes the plan NOT proposable"
    - "SMOKE::agent::attach|regenerate|removeAttachment drive the tools offline with fixed bytes (no model/render)"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "generateAttachment/regenerateAttachment/removeAttachment tools + proposePlan gate + SMOKE ops"
      contains: "generateAttachment"
  key_links:
    - from: "llm.ts generateAttachment"
      to: "scanText -> draftDocument -> markdownToPdf -> storage.store -> recordAttachments"
      via: "governed tool boundary (mirrors draftBody)"
      pattern: "recordAttachments"
    - from: "llm.ts proposePlan"
      to: "plan.attachmentError / total size > PLAN_ATTACHMENT_CAP_BYTES"
      via: "structural not-approvable gate"
      pattern: "attachmentError"
---

<objective>
Add the three governed attachment tools to `buildCockpitTools`, gate `proposePlan` on
render/cap health, and wire the offline `SMOKE::agent::attach|regenerate|removeAttachment` ops.
Each tool IS the enforcement boundary (mirrors `draftBody`/`proposePlan`); the loop drives them
unchanged once registered.

Purpose: this is the CKPT-02 agent surface — the user asks (or the agent suggests + user confirms),
the tool generates a redaction-safe PDF and records it on the plan, and a broken/oversized document
blocks approval (RESEARCH Seam 1, Seam 8; CONTEXT block-on-render-fail + pause-as-data).
Output: 3 tools + proposePlan gate + SMOKE ops + cockpit-agent skill v-bump, with per-tool unit
tests via the `__invokeCockpitTool` shim.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-attachment-generation/03.3-CONTEXT.md
@.planning/phases/03.3-attachment-generation/03.3-RESEARCH.md
@.planning/phases/03.3-attachment-generation/03.3-VALIDATION.md

<interfaces>
From packages/backend/convex/llm.ts buildCockpitTools (:410-568) — the patterns to mirror:
```typescript
const readPlan = async (): Promise<PlanRow> => { /* :411-415 cross-tenant guard */ };
// draftBody (:518-542) — the EXACT governed-content pattern:
execute: async ({ intent }) => {
  const scan = scanText(intent); if (!scan.ok) throw new Error("cockpit: body-intent scan failed"); // fail-closed §4
  const plan = await readPlan();
  const draft = await ctx.runAction(internal.llm.draftCockpit, { tenantId, safeText, safeTextHash, greetingName });
  await ctx.runMutation(internal.plans.patchPlan, { planId, bodyIntent: intent, body: draft.body });
  return "Body drafted and saved to the plan.";
}
// proposePlan (:543-566) — reads facts from the ROW, guards recipients/subject/body (:551-555)
```
From Plan 01 (@pikar/core + llm.ts): `draftDocument({tenantId,safeText,safeTextHash}) -> {title, markdown}`,
`markdownToPdf(title, markdown) -> Uint8Array` (throws on render-fail), `buildDocFilename`,
`PLAN_ATTACHMENT_CAP_BYTES`, `exceedsByteCap`.
From Plan 02 (plans.ts): `recordAttachments({planId, attachments, attachmentError})` (array + set/clear error).
Convex storage (RESEARCH §3): `ctx.storage.store(new Blob([bytes],{type:"application/pdf"})) -> Id<"_storage">`;
`ctx.storage.delete(id)` (delete old on supersede/remove — storage is NOT GC'd, orphans bill forever).
From llm.ts SMOKE agent seam (:655-708): `AgentSmokeOp` union, `parseAgentSmoke`, `runAgentSmokeOp`,
the `invokeTool` helper. cockpitTools.test.ts uses the `__invokeCockpitTool` shim against REAL primitives offline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generateAttachment / regenerateAttachment / removeAttachment tools</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - generateAttachment({ topic }): scanText(topic) fail-closed → draftDocument → markdownToPdf → build filename (buildDocFilename over existing plan filenames) → over-cap check → storage.store → recordAttachments appends the ref + CLEARS attachmentError → returns a "generated <filename>" label (NO url, NO bytes)
    - render throw (markdownToPdf rejects) → recordAttachments sets attachmentError, adds NO ref → tool returns a "couldn't generate, tell the user" message (block-on-render-fail)
    - over PLAN_ATTACHMENT_CAP_BYTES (single doc or running total) → no store, recordAttachments sets attachmentError → tool returns an over-size message
    - regenerateAttachment({ index, topic }): generate new bytes → store new id → recordAttachments replaces the slot → THEN storage.delete the OLD id (delete after the new ref persists — RESEARCH O3)
    - removeAttachment({ index }): splice the ref out of plan.attachments → storage.delete that id → recordAttachments writes the shortened array
    - each tool re-reads the row via readPlan (cross-tenant guard); returns labels/counts only (§2-D/§4)
  </behavior>
  <action>
    Add three `tool(...)` entries to `buildCockpitTools` (`llm.ts:429-567`), each a thin governed
    wrapper mirroring `draftBody` (:518-542):
    - `generateAttachment` (input `{topic:string}`): `scanText(topic)` fail-closed → `readPlan()` →
      `internal.llm.draftDocument({tenantId, safeText, safeTextHash})` → `markdownToPdf(title,
      markdown)` inside try/catch → `buildDocFilename(topic, today, existing)` → sum sizes vs
      `PLAN_ATTACHMENT_CAP_BYTES` (`exceedsByteCap`) → `ctx.storage.store(new Blob([bytes],
      {type:"application/pdf"}))` → `internal.plans.recordAttachments` (append ref, CLEAR
      attachmentError). Render throw OR over-cap → `recordAttachments` SETS `attachmentError` and adds
      no ref; return a plain-language failure the agent relays (retryable). Return only a label
      (filename + count) — never a URL/bytes.
    - `regenerateAttachment` (`{index, topic}`): same generation; replace the slot at `index`; delete
      the OLD storageId AFTER `recordAttachments` persists the new ref (supersede-in-place, O3).
    - `removeAttachment` (`{index}`): drop the ref at `index`, `ctx.storage.delete` it,
      `recordAttachments` with the shortened array (clear attachmentError if now clean).
    Reuse `today` from a pinned clock in the SMOKE path so fixtures stay deterministic. Add per-tool
    tests to `cockpitTools.test.ts` via the `__invokeCockpitTool` shim against the real primitives
    offline (register `auditCounts` if any path hits audit — 03.2.1-03 quirk). `// ponytail: no new
    spend code — the loop's preCall/recordSpend already governs the turn, exactly like draftBody.`
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>generateAttachment produces a stored PDF ref on the plan (scan→draft→render→store→record); regenerate supersedes + deletes old bytes; remove deletes bytes; render-fail/over-cap set attachmentError with no ref; per-tool tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: proposePlan render-OK + under-byte-cap gate</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - proposePlan refuses (returns a "cannot propose" message, does NOT call proposeEmailPlan) when plan.attachmentError is set (render-failed / over-cap) — V7
    - proposePlan refuses when the total size of plan.attachments exceeds PLAN_ATTACHMENT_CAP_BYTES — V7
    - proposePlan with healthy attachments (or none) proceeds exactly as today (recipients/subject/body guards unchanged)
  </behavior>
  <action>
    Extend the `proposePlan` guards (`llm.ts:551-555`): after the existing recipients/subject/body
    checks, refuse if `plan.attachmentError` is set OR the summed `plan.attachments` size exceeds
    `PLAN_ATTACHMENT_CAP_BYTES` — return a message telling the agent what's wrong (regenerate/remove
    the document) so it re-asks rather than proposing an un-sendable plan. Structural facts come from
    the ROW, never model args (unchanged invariant). Add the gate cases to `cockpitTools.test.ts`.
    `// ponytail: the cap is re-checked here as defense-in-depth; generateAttachment already refuses over-cap.`
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>proposePlan refuses on attachmentError or over-cap and proceeds when healthy (V7); tests green.</done>
</task>

<task type="auto">
  <name>Task 3: SMOKE::agent attachment ops + cockpit-agent skill v-bump</name>
  <files>packages/backend/convex/llm.ts, packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts</files>
  <action>
    Extend the offline agent sentinel (`llm.ts:655-708`): add `{ kind:"attach"; topic:string }`,
    `{ kind:"regenerate"; index:number; topic:string }`, `{ kind:"removeAttachment"; index:number }`
    to the `AgentSmokeOp` union; parse `attach=<topic>`, `regenerate=<i>:<topic>`,
    `removeAttachment=<i>` in `parseAgentSmoke`; route them to the new tools in `runAgentSmokeOp`. The
    `draftDocument` SMOKE path (Plan 01) already returns FIXED markdown; ensure `markdownToPdf` on that
    fixed markdown yields deterministic FIXED bytes so the fan-out/E2E carry a real attachment offline
    with no model/render-lib variance. Bump the `cockpit-agent` skill (canonical .md + byte-identical
    derived .ts → seedSkills publishes v-next automatically): teach it to (a) SUGGEST an attachment
    with one question and only `generateAttachment` after the user confirms (RESEARCH O4), (b) reason
    about attachments by filename/index, (c) never claim a send. Keep the skills.test.ts drift row
    green (the derived constant must equal the .md).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>SMOKE::agent::attach|regenerate|removeAttachment drive the tools offline with deterministic fixed bytes; cockpit-agent skill v-bump encodes suggest-then-confirm + filename/index reasoning; skill drift test green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (per-tool shim tests, proposePlan gate, SMOKE ops, skill drift).
- Maps V7 (render-fail/over-cap → not approvable) and seeds the V6/V9 offline harness.
- Governance: tools return labels/counts only; no address/URL/bytes surfaced to the model (§2-D/§4).
</verification>

<success_criteria>
- generateAttachment/regenerateAttachment/removeAttachment are governed tools that keep the send-safety invariants.
- proposePlan structurally blocks a render-failed / over-cap plan.
- SMOKE ops + cockpit-agent v-bump make the attachment flow drivable offline and via natural language.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-attachment-generation/03.3-04-SUMMARY.md`
</output>
