---
phase: 03.3-attachment-generation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/gmailAuth.ts
requirements: [CKPT-02]
autonomous: true
must_haves:
  truths:
    - "buildMime emits a structurally-correct multipart/mixed message when attachments are present"
    - "A zero-attachment send is byte-identical to today's plain-text message"
    - "The send action loads attachment bytes from storage and base64-encodes them into the MIME"
    - "The gmail.sent audit still carries refs only — no bytes, no base64, no signed URL (§4)"
  artifacts:
    - path: "packages/backend/convex/gmail.ts"
      provides: "multipart buildMime + storage byte-load in send"
      contains: "multipart/mixed"
    - path: "packages/backend/convex/gmailAuth.ts"
      provides: "getForDelivery returns resolved attachment refs"
      contains: "attachmentRefs"
  key_links:
    - from: "gmail.send"
      to: "ctx.storage.get(storageId) -> base64 -> buildMime part"
      via: "action-context byte load"
      pattern: "storage\\.get"
    - from: "gmailAuth.getForDelivery"
      to: "requests.attachmentRefs -> attachments rows"
      via: "resolve filename/mimeType/storageId per ref"
      pattern: "attachmentRefs"
---

<objective>
Upgrade the Gmail send seam to carry attachments: `buildMime` gains a `multipart/mixed` branch (with
a byte-identical zero-attachment fallback), `getForDelivery` resolves the request's attachment refs,
and `send` loads the bytes from storage. Everything else on the send spine — retrier, audit,
telemetry, onComplete→DLQ — is reused VERBATIM. This plan reads `requests.attachmentRefs` +
the `attachments` table, both of which already exist, so it is independent of the other Wave-1 plans.

Purpose: CKPT-02's whole point is that a generated PDF rides the EXISTING governed send unchanged
except for the multipart assembly (RESEARCH §2, Seam 4).
Output: multipart `buildMime` + attachment-carrying `send`/`getForDelivery`, with a colocated
assert self-check proving structure (V3) and byte-identical empty-branch (V4).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-attachment-generation/03.3-CONTEXT.md
@.planning/phases/03.3-attachment-generation/03.3-RESEARCH.md
@.planning/phases/03.3-attachment-generation/03.3-VALIDATION.md

<interfaces>
From packages/backend/convex/gmail.ts — the seams to change:
```typescript
// :59-68 CURRENT single-part (keep as the zero-attachment branch, byte-for-byte):
function buildMime(to, subject, body): string {
  return ["To: "+to, "Subject: "+subject, "MIME-Version: 1.0",
          'Content-Type: text/plain; charset="UTF-8"', "", body].join("\r\n");
}
// :70-76 base64Url — outer raw-message url-safe encoder (KEEP; this is base64url, NOT the inner part encoder)
// :86-139 send: getForDelivery -> SMOKE::fail throw (:95) -> freshAccessToken (:102)
//   -> raw = base64Url(buildMime(recipient, subject, body)) (:109) -> POST {raw} (:110-117)
//   -> audit gmail.sent { requestId, messageId } (:130-136, refs only)
```
From packages/backend/convex/gmailAuth.ts getForDelivery (:120-133):
```typescript
return { tenantId, correlationId, recipient, subject: r.goal, body: r.editedBody ?? r.draft ?? "" };
// ADD: attachments: resolve r.attachmentRefs -> [{ filename, mimeType, storageId }]
```
RESEARCH §2 hard rules: CRLF everywhere; MIME-Version:1.0 top-level ONCE; boundary =
"=_pikar_"+randomUUID().replace(/-/g,""); inner parts use STANDARD base64 (CTE: base64) wrapped at 76;
outer whole message → base64url via the EXISTING base64Url(); each attachment part gets
Content-Type: application/pdf; name="…" + Content-Disposition: attachment; filename="…"; closing
delimiter "--boundary--" is MANDATORY. Filenames are app-generated sanitized ASCII (Plan 01) → no
RFC-2047 encoding needed. `// ponytail: RFC 2047 encoded-word; ASCII names skip it.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: multipart buildMime with byte-identical empty branch</name>
  <files>packages/backend/convex/gmail.ts</files>
  <behavior>
    - buildMime(to, subject, body, []) === the exact legacy single-part string (V4 byte-identity)
    - buildMime(to, subject, body, [{filename, mimeType:"application/pdf", base64}]) → multipart/mixed with: one top-level MIME-Version:1.0; a boundary "=_pikar_<hex>"; a text/plain part (base64, wrapped 76); one application/pdf part per attachment with Content-Disposition: attachment; filename="…"; a MANDATORY closing "--<boundary>--"; CRLF line endings throughout (V3)
    - inner attachment payload uses STANDARD base64 (not url-safe); the outer raw message is still base64url via base64Url() (the classic bug the test guards)
  </behavior>
  <action>
    Change `buildMime` signature to accept an optional `attachments: {filename: string; mimeType:
    string; base64: string}[]` param. When empty/absent → RETURN THE EXACT CURRENT STRING (assert
    byte-identity so today's plain-text sends are provably unchanged — V4). When present → emit
    `multipart/mixed` per the RESEARCH §2 structure in <interfaces>: boundary from
    `crypto.randomUUID()` (normal node action — allowed), the body as a base64 text/plain part, then
    one `application/pdf` attachment part per entry (STANDARD base64, wrap at 76 chars), closing
    `--boundary--`. Colocate an assert self-check beside `buildMime`: multipart structure, N part
    delimiters, mandatory closing delimiter, base64url-outer/base64-inner distinction, and the
    byte-identical empty branch. `// ponytail: hard 8MB/plan cap (Plan 04) keeps us on the simple raw
    send; add uploadType=resumable only if a real doc exceeds it.`
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>buildMime multipart is structurally correct (V3) and the zero-attachment branch is byte-identical to today (V4); self-check green.</done>
</task>

<task type="auto">
  <name>Task 2: getForDelivery resolves refs + send loads bytes</name>
  <files>packages/backend/convex/gmailAuth.ts, packages/backend/convex/gmail.ts</files>
  <action>
    Extend `getForDelivery` (`gmailAuth.ts:120-133`) to resolve `r.attachmentRefs` → `[{filename,
    mimeType, storageId}]` (look up each `attachments` table row) and return it alongside the existing
    `{tenantId, correlationId, recipient, subject, body}`. In `send` (`gmail.ts:86-139`): after the
    token refresh (:102) and BEFORE building the MIME (:109), for each resolved ref load the bytes via
    `const blob = await ctx.storage.get(storageId)` (action ctx — allowed); `if (!blob) throw` (a
    missing/deleted id is a hard failure → retrier → onComplete DLQ, never a silent send without the
    promised attachment); `Buffer.from(await blob.arrayBuffer()).toString("base64")`. Pass the parts
    to the upgraded `buildMime(recipient, subject, body, parts)`. Keep the `SMOKE::fail` throw (:95),
    the awaiting_reauth path (:103-106), the transient-500 retry throw (:118-121), and the `gmail.sent`
    audit (:130-136) — the audit payload stays `{requestId, messageId}` REFS ONLY (never bytes, base64,
    filename-content, or a URL — §4). Preserve the explicit `SendResult` return type (the §96
    circular-inference guard at :78-84).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>getForDelivery returns resolved attachment refs; send loads storage bytes and assembles the multipart message; a missing storage id throws (blocks/DLQs, never sends without the attachment); gmail.sent audit still refs-only; existing gmail tests green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (buildMime self-check + gmail tests).
- Maps V3 (multipart structurally correct) and V4 (zero-attachment byte-identical).
- Governance: gmail.sent payload asserted refs-only (no bytes/base64/URL — §4).
</verification>

<success_criteria>
- buildMime emits correct multipart/mixed when attachments present, byte-identical single-part when empty.
- send loads bytes from storage and delivers them; retrier/audit/telemetry/DLQ reused verbatim.
- No file bytes/base64/URL ever reach an audit/DLQ/telemetry payload.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-attachment-generation/03.3-03-SUMMARY.md`
</output>
