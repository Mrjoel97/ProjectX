---
phase: 03.3-attachment-generation
plan: 05
subsystem: ui
tags: [convex, attachments, storage, react, workflow, convex-test]

# Dependency graph
requires:
  - phase: 03.3-02
    provides: plans.attachments schema, attachmentUrls query, reportForPlan per-row attachments
  - phase: 03.3-03
    provides: gmail.send multipart + getForDelivery resolving requests.attachmentRefs
  - phase: 03.3-04
    provides: attachment tools (generate/regenerate/removeAttachment) + attachmentError propose gate + SMOKE::agent sentinels
provides:
  - executePlan materializes plan.attachments into attachments rows once and shares the ids across every recipient's request (CKPT-02 send fan-out)
  - PLAN card attachment rows (filename + size + signed download + regenerate + remove) reflecting attachmentError
  - REPORT card delivered-with-attachment re-download of the exact sent bytes
  - first convex-test coverage of executePlan's successful proposed->delivering path (workflow + workflow/workpool registered)
affects: [03.3-06, phase-close, e2e]

# Tech tracking
tech-stack:
  added: ["@convex-dev/workpool@0.4.7 (test-only devDep — component registration for convex-test)"]
  patterns:
    - "Shared-ref fan-out: one generated document set → one attachments row per doc → same id array on every recipient request (immutable storage, no duplication)"
    - "convex-test can drive workflow.start by registering workflow + workflow/workpool (workpool hoisted via a pinned test devDep)"
    - "Card action buttons re-enter the agent tool-loop via sendCockpitMessage (natural language live; composer SMOKE sentinels drive the same tools offline)"

key-files:
  created: []
  modified:
    - packages/backend/convex/cockpit.ts
    - packages/backend/convex/cockpit.test.ts
    - packages/backend/package.json
    - apps/web/app/(app)/dashboard/workspace/cards.tsx
    - docs/playbooks/cockpit.md

key-decisions:
  - "executePlan materializes attachments rows ONCE outside the fan-out loop and points every recipient's attachmentRefs at the shared id array — one byte set to all, no per-recipient copies"
  - "Added @convex-dev/workpool as a test-only devDep (pinned to the version workflow@0.4.4 resolves) to register workflow/workpool in convex-test and cover the successful executePlan path end-to-end"
  - "PLAN/REPORT card buttons send natural-language instructions via sendCockpitMessage (live-correct, clean chat history); offline determinism stays composer-driven via SMOKE::agent sentinels"

patterns-established:
  - "Shared-ref attachment fan-out (CKPT-02 truth #1)"
  - "workflow/workpool component registration unlocks convex-test coverage of the approve->seed->fanout path"

requirements-completed: [CKPT-02]

# Metrics
duration: 30min
completed: 2026-07-14
---

# Phase 03.3 Plan 05: Attachment Send Fan-out + PLAN/REPORT Cards Summary

**executePlan fans one generated document set to every recipient via shared attachment ids, and the PLAN/REPORT cards gain download/regenerate/remove + delivered-with-attachment re-download — closing the CKPT-02 loop from generated ref to governed per-recipient send to live report.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-07-14T00:38Z
- **Completed:** 2026-07-14T00:53Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- `executePlan` materializes `plan.attachments` into `attachments` table rows once, then seeds the SAME `Id<"attachments">` array on every recipient's `requests` row (shared bytes, no duplication); zero-attachment plans keep `attachmentRefs: []`. Send spine (`deliverApprovedPlan`/`gmail.send`/retrier/audit/DLQ) unchanged.
- First convex-test coverage of the successful proposed→delivering path: registering `workflow` + `workflow/workpool` lets `executePlan` run end-to-end, asserting shared refs across recipients + exactly one materialized row, plus the unchanged zero-attachment `[]` path.
- PLAN card `PlanAttachments`: filename + size + signed download link (tenant-guarded `attachmentUrls`, bearer capability §4), per-attachment Regenerate (topic input) + Remove wired to `sendCockpitMessage`, and an `attachmentError` unapprovable-until-fixed note.
- REPORT card: each recipient shows a 📎 delivered-with-attachment chip re-downloading the exact sent bytes.

## Task Commits

1. **Task 1: executePlan propagates attachments to the fan-out** - `b8ed793` (feat)
2. **Task 2: PLAN + REPORT card attachment rows** - `1f5663a` (feat)

**Playbook metadata:** `4fc5939` (docs: cockpit playbook Last verified → 03.3-05)

## Files Created/Modified
- `packages/backend/convex/cockpit.ts` - executePlan materializes attachments rows + shares ids across recipient requests
- `packages/backend/convex/cockpit.test.ts` - attachment fan-out tests (shared ref + zero-attach) driving workflow.start via registered workflow/workpool
- `packages/backend/package.json` - @convex-dev/workpool@0.4.7 test-only devDep
- `apps/web/app/(app)/dashboard/workspace/cards.tsx` - PlanAttachments section + REPORT delivered-with-download
- `docs/playbooks/cockpit.md` - Last verified → 03.3-05

## Decisions Made
- Materialize attachments rows once (outside the fan-out loop) and reuse their ids for all recipients — the CKPT-02 "one byte set, shared ids" invariant; a shared row has no `requestId` (belongs to no single request).
- Add `@convex-dev/workpool` as a test-only devDep pinned to the resolved version so convex-test can register `workflow/workpool` and drive `executePlan` end-to-end — real coverage of the previously-untested successful send path (a version mismatch would fail loudly, a good signal).
- Card buttons send natural-language messages (live-correct + clean chat) rather than SMOKE sentinels; offline determinism remains composer-driven per the established E2E pattern.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] convex-test could not drive executePlan's successful path**
- **Found during:** Task 1 (executePlan propagation test)
- **Issue:** The plan asks the test to assert seeded request rows carrying the shared refs, but every seeding path reaches `workflow.start`, which convex-test could not run (the repo's existing note: workflow component not loaded → covered only by live smoke:fanout). A `workflow.start` throw rolls back the mutation, so seeded rows were unobservable.
- **Fix:** Registered the `workflow` + nested `workflow/workpool` components in `cockpit.test.ts` (same pattern cockpitTools.test.ts uses for the aggregate). `workpool` is a transitive pnpm dep not hoisted into the package, so added it as a test-only devDependency pinned to `0.4.7` (the exact version `@convex-dev/workflow@0.4.4` already resolves) to make the relative import + `import.meta.glob` resolve.
- **Files modified:** packages/backend/convex/cockpit.test.ts, packages/backend/package.json, pnpm-lock.yaml
- **Verification:** Both new tests green; `executePlan` returns `{ok:true, workflowId}` and the seeded rows/attachments rows are asserted.
- **Committed in:** `b8ed793` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking).
**Impact on plan:** The deviation strengthens coverage — it produced the FIRST convex-test proof of the successful proposed→delivering path (previously live-only), exactly the assertions the plan asked for. No scope creep; the added dep is test-only and pinned to the already-resolved version (CLAUDE.md §6 respected — not a bump).

## Issues Encountered
- The known pre-existing `audit.test.ts` red (`auditCounts` aggregate unregistered in that suite) remains; it is unrelated to this plan and documented across STATE.md history as NOT a regression. Backend suite: 111 passed, 1 pre-existing red. Web typecheck: green.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CKPT-02 send fan-out + cards complete; ready for Plan 06 (phase close + human-verify V6/V9 — fan-out carries the attachment, E2E asserts the card rows).
- No blockers.

---
*Phase: 03.3-attachment-generation*
*Completed: 2026-07-14*

## Self-Check: PASSED
- All modified files exist on disk.
- All task/doc commits present (b8ed793, 1f5663a, 4fc5939).
