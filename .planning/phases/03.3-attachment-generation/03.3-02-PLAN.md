---
phase: 03.3-attachment-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/plans.test.ts
requirements: [CKPT-02]
autonomous: true
must_haves:
  truths:
    - "A plan row can carry an array of generated-attachment refs (storageId/filename/mimeType/size) — all optional, no migration"
    - "A plan row can carry a transient attachmentError marker (render-failed / over-cap) that the propose gate reads"
    - "A tenant-scoped query returns signed download URLs for a plan's attachments; a cross-tenant caller gets nothing"
    - "The live REPORT projection can surface each recipient's delivered attachment filename + a re-download URL"
  artifacts:
    - path: "packages/backend/convex/schema.ts"
      provides: "plans.attachments array field + plans.attachmentError marker (both optional)"
      contains: "attachments: v.optional(v.array"
    - path: "packages/backend/convex/plans.ts"
      provides: "recordAttachments mutation + attachmentUrls tenantQuery + reportForPlan URL extension"
      contains: "attachmentUrls"
  key_links:
    - from: "apps/web cards (Plan 05)"
      to: "plans.attachmentUrls"
      via: "useQuery tenant-scoped signed getUrl"
      pattern: "storage\\.getUrl"
    - from: "plans.reportForPlan"
      to: "requests.attachmentRefs -> storage.getUrl"
      via: "per-recipient delivered-attachment URL"
      pattern: "attachmentRefs"
---

<objective>
Extend the content-plane adapter so the plan row can hold generated-attachment refs + a render/cap
error marker, and add the tenant-scoped read queries the PLAN/REPORT cards need (signed download
URLs). Schema + adapter ONLY — no tools, no send, no UI.

Purpose: CKPT-02 hangs attachment refs on the plan (RESEARCH O1: inline-on-plan is the pre-approval
source of truth) and needs the FIRST `storage.getUrl` in the codebase, served from a tenant-guarded
query so a signed bearer URL never crosses tenants (RESEARCH §3).
Output: `plans.attachments` + `plans.attachmentError` schema fields, `recordAttachments`,
`attachmentUrls`, and the `reportForPlan` URL extension — with a convex-test round-trip (V5).
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-attachment-generation/03.3-CONTEXT.md
@.planning/phases/03.3-attachment-generation/03.3-RESEARCH.md
@.planning/phases/03.3-attachment-generation/03.3-VALIDATION.md

<interfaces>
From packages/backend/convex/schema.ts plans table (:133-176) — add after `body` (:149):
```typescript
// Generated outbound attachments (CKPT-02). Inline on the plan = pre-approval source of truth
// for the PLAN card; executePlan materializes attachments-table rows at fan-out. All optional → no migration.
attachments: v.optional(v.array(v.object({
  storageId: v.id("_storage"),
  filename: v.string(),
  mimeType: v.string(),
  size: v.number(),
}))),
attachmentError: v.optional(v.string()), // transient: render-failed / over-byte-cap → blocks propose (V7)
```
From packages/backend/convex/plans.ts patchPlan (:61-77): drops undefined keys before patch.
From plans.ts reportForPlan (:141-166): reads requests by_plan (tenant-guarded), joins gmail.sent
audit by_correlation → `{recipient, status, correlationId, messageId}`. Extend each row with the
delivered attachment url(s).
Convex storage (verified, RESEARCH §3):
  ctx.storage.getUrl(id) -> string|null  (query|mutation|action) — stable signed URL, tenant-guard on the ref row
  ctx.storage.store(blob,{sha256?}) -> Id<"_storage">  (mutation|action)   — used by the convex-test to seed a blob
From schema.ts requests table: `attachmentRefs: v.array(v.id("attachments"))` (:116) already exists, seeded [].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema fields + recordAttachments mutation</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/plans.ts</files>
  <action>
    Add `attachments` (optional array of `{storageId, filename, mimeType, size}`) and
    `attachmentError` (optional string) to the `plans` table in `schema.ts` after `body` (:149) —
    all-optional so no migration (same discipline as `safeText`/`candidates`). Define a shared
    `ATTACHMENTS` validator const in `plans.ts` (mirror the existing `CANDIDATES` const at :26-39).
    Add an internalMutation `recordAttachments({ planId, attachments, attachmentError })` to `plans.ts`:
    it ALWAYS patches `attachments` (the full array — supersede/remove replace it wholesale) and
    sets `attachmentError` (passing `undefined` clears the field, mirroring `clearCandidates` at
    :108-113 — a successful generate clears a prior error). This is the single write surface the
    attachment tools (Plan 04) use, so the drop-undefined `patchPlan` never has to special-case the
    error clear. Content-plane only — NEVER audited (§4).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>plans.attachments + plans.attachmentError exist (optional, no migration); recordAttachments writes the array + sets/clears attachmentError; backend typecheck + existing plans tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: attachmentUrls query + reportForPlan URL extension + storage round-trip test</name>
  <files>packages/backend/convex/plans.ts, packages/backend/convex/plans.test.ts</files>
  <behavior>
    - store a blob → put its id on a plan's attachments → attachmentUrls({planId}) returns [{filename, mimeType, size, url}] with a non-null url; the stored→getUrl round-trip resolves (V5)
    - attachmentUrls is a tenantQuery guarded on the plan row: a caller whose identity ≠ plan.tenantId gets an empty/denied result (never another tenant's signed URL) — V5 isolation
    - reportForPlan rows gain `attachments: [{filename, url}]` derived from each request's attachmentRefs (empty array when a request carries none) — no change to the existing recipient/status/messageId fields
  </behavior>
  <action>
    Add `attachmentUrls` tenantQuery to `plans.ts`: read the plan row, guard `plan.tenantId ===
    ctx.tenantId` (mirror `byThread`), map `plan.attachments` → `[{filename, mimeType, size, url:
    await ctx.storage.getUrl(storageId)}]`. This is the FIRST `storage.getUrl` in the codebase — the
    signed URL is a bearer capability, so it is ONLY ever returned from this tenant-guarded query and
    NEVER logged (§4). Extend `reportForPlan` (:141-166): for each request row, resolve its
    `attachmentRefs` (look up the `attachments` table rows) → `{filename, url: getUrl(storageId)}[]`
    and add an `attachments` field to each report row (REPORT re-download of the EXACT sent bytes —
    storage is immutable per id, so persist the send-time id, never regenerate). Add a `plans.test.ts`
    convex-test covering the store→getUrl round-trip + the tenant guard + the reportForPlan extension.
    If the test path touches `audit.log`, register the `auditCounts` aggregate component in the test
    (known convex-test 0.0.54 quirk — see 03.2.1-03).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>attachmentUrls returns tenant-scoped signed URLs (cross-tenant denied); reportForPlan surfaces per-recipient delivered-attachment filename+url; storage round-trip convex-test green (V5).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (schema loads, recordAttachments, attachmentUrls round-trip + tenant guard, reportForPlan extension).
- Maps V5 (storage round-trip + tenant-guarded getUrl).
</verification>

<success_criteria>
- plans row carries attachments[] + attachmentError (optional, no migration).
- recordAttachments is the single content-plane write surface for the array + error marker.
- attachmentUrls + reportForPlan expose tenant-scoped signed download URLs; no URL ever logged.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-attachment-generation/03.3-02-SUMMARY.md`
</output>
