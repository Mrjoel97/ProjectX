---
phase: 03.3-attachment-generation
plan: 05
type: execute
wave: 3
depends_on: ["03.3-02", "03.3-03", "03.3-04"]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
requirements: [CKPT-02]
autonomous: true
must_haves:
  truths:
    - "On Approve, every recipient's request row references the SAME generated attachment(s) — one byte set, shared ids"
    - "The PLAN card shows attachment filename(s) with download/preview, plus regenerate and remove controls, before Approve"
    - "The REPORT card shows each recipient delivered-with-attachment and re-downloads the exact sent bytes"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "executePlan materializes attachments rows + seeds requests.attachmentRefs at fan-out"
      contains: "attachmentRefs"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "PLAN attachment rows (download/regenerate/remove) + REPORT delivered-with-download"
      contains: "attachmentUrls"
  key_links:
    - from: "cockpit.executePlan"
      to: "plan.attachments -> attachments rows -> requests.attachmentRefs (shared across recipients)"
      via: "fan-out seed (:225-240)"
      pattern: "attachmentRefs"
    - from: "cards.PlanCard"
      to: "plans.attachmentUrls + sendCockpitMessage(regenerate/remove)"
      via: "download/preview + regenerate + remove"
      pattern: "attachmentUrls"
---

<objective>
Wire the attachment through the send fan-out and onto the cards. `executePlan` propagates the
plan's generated attachments to every recipient's request row (same bytes, shared ids — this slice
sends one document set to all), and the PLAN/REPORT cards gain the attachment controls
(download/preview + regenerate + remove before Approve; delivered-with-download after).

Purpose: closes the CKPT-02 loop from generated ref → governed per-recipient send → live report
(RESEARCH Seam 5, Seam 7).
Output: `executePlan` fan-out propagation + PLAN/REPORT card attachment rows.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-attachment-generation/03.3-CONTEXT.md
@.planning/phases/03.3-attachment-generation/03.3-RESEARCH.md
@.planning/phases/03.3-attachment-generation/03.3-VALIDATION.md

<interfaces>
From packages/backend/convex/cockpit.ts executePlan (:197-255) — the fan-out seed to change:
```typescript
// :225-240 per-target loop seeds one requests row with attachmentRefs: [] (:234)
for (const recipient of targets) {
  const correlationId = crypto.randomUUID();
  const requestId = await ctx.db.insert("requests", {
    tenantId: ctx.tenantId, correlationId, goal: subject, recipient, draft: body,
    status: "approved", attachmentRefs: [], planId, createdAt: Date.now(),
  });
}
// sole workflow.start(deliverApprovedPlan) caller (:243-251) — do NOT change the send spine
```
From Plan 02: `plans.attachments` = `{storageId, filename, mimeType, size}[]`; `attachmentUrls` query;
`reportForPlan` now returns per-row `attachments: [{filename, url}]`.
From schema.ts attachments table (:180-188): `{tenantId, requestId?, storageId, filename, mimeType, size}`.
requests.attachmentRefs is `v.array(v.id("attachments"))`.
From cards.tsx: PlanCard (:47-105, Approve at :90-97), ReportCard (:121-149, useQuery reportForPlan),
CardList dispatcher (:258-280). Reuse the AttachmentPicker download/remove list idiom
(apps/web/app/(app)/_components/AttachmentPicker.tsx :95-124). Regenerate/remove re-enter the loop via
`api.cockpit.sendCockpitMessage` (SMOKE offline; natural language live).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: executePlan propagates attachments to the fan-out</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts</files>
  <action>
    In `executePlan` (`cockpit.ts:197-255`), before/inside the per-target loop (:225-240): if
    `plan.attachments?.length`, materialize ONE `attachments` table row per generated document (RESEARCH
    O1 — the inline plan refs become table rows here), then set EVERY recipient's request
    `attachmentRefs` to that SAME array of `Id<"attachments">` (one byte set shared across all
    recipients this slice — no per-recipient duplication, no extra storage writes). Create the
    `attachments` rows once (outside the loop) and reuse their ids. The `deliverApprovedPlan` fan-out,
    `gmail.send`, retrier, audit, telemetry, and onComplete→DLQ are UNCHANGED (Plan 03 already reads
    `attachmentRefs`). Keep the CAS/idempotency (:205-215), the gmail pre-check (:211-212), and the
    sole-`workflow.start` invariant intact. Extend `cockpit.test.ts` to assert: a proposed plan with
    an attachment seeds N request rows each carrying the shared ref ids; a zero-attachment plan still
    seeds `attachmentRefs: []` (unchanged path).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test</automated>
  </verify>
  <done>executePlan creates attachments rows and points every recipient's attachmentRefs at the shared ids; zero-attachment plans unchanged; cockpit.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: PLAN + REPORT card attachment rows</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    PLAN card (`PlanCard` :47-105): add an attachments section reading reactive `plan.attachments` +
    the new `useQuery(api.plans.attachmentUrls, {planId})`. For each: show filename + size + a
    download/preview link (the signed url) + a Remove button + a Regenerate control. Regenerate/Remove
    re-enter the agent loop via `useAction(api.cockpit.sendCockpitMessage)` (offline: the
    `SMOKE::agent::removeAttachment=<i>` / `regenerate=<i>:<topic>` sentinels from Plan 04; live: a
    natural-language message) — no new backend action needed (ponytail: the tools already exist behind
    the loop). If `plan.attachmentError` is set, show it and note the plan can't be approved until the
    document is fixed (the Approve button already reflects proposePlan's refusal since the plan won't
    be `proposed`). REPORT card (`ReportCard` :121-149): render each row's new `attachments:
    [{filename, url}]` from `reportForPlan` as delivered-with-filename + a re-download link (the EXACT
    sent bytes). Reuse the `AttachmentPicker` download/remove list idiom (:95-124) + existing chip/teal
    styles. `pnpm --filter @pikar/web typecheck` must stay green (mind noUncheckedIndexedAccess).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
  </verify>
  <done>PLAN card shows attachment filename(s) with download/preview + regenerate + remove and reflects attachmentError; REPORT card shows delivered-with-attachment + re-download; web typecheck green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test` green (executePlan propagation).
- `pnpm --filter @pikar/web typecheck` green (card rows).
- Feeds V6 (fan-out carries the attachment) and V9 (E2E asserts the card rows) — proven in Plan 06.
</verification>

<success_criteria>
- Every recipient's send carries the same generated document(s) via shared attachment ids.
- PLAN card lets the user inspect/regenerate/remove before the single Approve.
- REPORT card shows delivered-with-attachment and re-downloads the exact sent bytes.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-attachment-generation/03.3-05-SUMMARY.md`
</output>
