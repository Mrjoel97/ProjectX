---
phase: 03.3-attachment-generation
plan: 06
type: execute
wave: 4
depends_on: ["03.3-03", "03.3-04", "03.3-05"]
files_modified:
  - packages/backend/convex/smoke.ts
  - packages/backend/scripts/run-smoke-fanout.mjs
  - packages/backend/scripts/run-smoke-guardrails.mjs
  - apps/web/e2e/cockpit-attachment.spec.ts
  - docs/playbooks/cockpit.md
requirements: [CKPT-02]
autonomous: false
must_haves:
  truths:
    - "The governed fan-out carries a generated attachment to every recipient with refs-only logs (V6)"
    - "A budget/kill-switch stop during generation pauses as data — no attachment, no DLQ (V8)"
    - "The full chat→attach→PLAN(download)→Approve→REPORT(download) flow passes offline (V9)"
    - "A real PDF arrives attached to a real email, audit showing refs only (V10)"
  artifacts:
    - path: "apps/web/e2e/cockpit-attachment.spec.ts"
      provides: "End-to-end attachment flow over SMOKE::agent::attach"
      contains: "SMOKE::agent::attach"
    - path: "docs/playbooks/cockpit.md"
      provides: "Attachment invariants + Last verified bump (§9)"
      contains: "attachment"
  key_links:
    - from: "smoke:fanout"
      to: "SMOKE attachment → multipart send → per-recipient rows"
      via: "seedFanout + run-smoke-fanout.mjs"
      pattern: "attachment"
    - from: "cockpit-attachment.spec.ts"
      to: "PLAN filename+download → Approve → REPORT delivered-with-attachment"
      via: "Playwright over the offline SMOKE path"
      pattern: "REPORT"
---

<objective>
Prove CKPT-02 end to end and close the phase: extend the fan-out + guardrails smokes to carry an
attachment through the governed send (refs-only), add the `cockpit-attachment` Playwright E2E, do the
single live human-verify send, and update the cockpit playbook (§9). This is the only plan with a
human checkpoint.

Purpose: the governance-critical proof — an attachment rides the SAME spine (audit/telemetry/DLQ)
with zero bytes/base64/URL in any log plane, and a real PDF lands in a real inbox (RESEARCH §Validation
V6/V8/V9/V10).
Output: extended smokes + E2E spec + playbook close + human-verified live send.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-attachment-generation/03.3-CONTEXT.md
@.planning/phases/03.3-attachment-generation/03.3-RESEARCH.md
@.planning/phases/03.3-attachment-generation/03.3-VALIDATION.md

<interfaces>
From RESEARCH Seam 8: `smoke.ts seedFanout` (:197-255), smoke scripts `package.json:15-20` →
`scripts/run-smoke-*.mjs` + `smokeRun.mjs` (judges pass/fail by matching the CLI failure banner —
never trust `npx convex run` exit codes on Windows/Node24). SMOKE fixtures: `SMOKE::agent::attach`
(Plan 04) drives a fixed-bytes PDF with no model/render variance; `SMOKE::fail` (gmail.ts:95) forces a
per-recipient send failure for the DLQ-isolation assertion.
Prior E2E specs live in `apps/web/e2e/` (cockpit-report.spec.ts, cockpit-resolve.spec.ts). Run:
`pnpm --filter web exec playwright test cockpit-attachment`. E2E needs a running `convex dev` (NOT
--once) + `next dev :3111`.
Governance to assert (§4): NO bytes / base64 / filename-content / signed URL in any audit / deadLetters
/ telemetry payload — only storageId / filename / size / sha256 / counts.
docs/playbooks/cockpit.md already watches llm.ts/plans.ts/cockpit.ts/gmail.ts/cards.tsx — the
attachment changes need an invariant section + a `Last verified` bump (§9 Stop hook clears on it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend smoke:fanout + smoke:guardrails for attachments</name>
  <files>packages/backend/convex/smoke.ts, packages/backend/scripts/run-smoke-fanout.mjs, packages/backend/scripts/run-smoke-guardrails.mjs</files>
  <action>
    Extend `smoke.ts seedFanout` (:197-255) + `run-smoke-fanout.mjs` so a seeded plan carries a
    `SMOKE::agent::attach` fixed-bytes PDF (Plan 04) through `executePlan` → `deliverApprovedPlan` →
    the multipart `gmail.send`. Assert (V6): every recipient's send carries the attachment part; a
    forced-fail row (`SMOKE::fail`) dead-letters ALONE while the rest still send; terminals write-once;
    and — the governance-critical check — scan the `audit` / `deadLetters` / telemetry rows and assert
    ZERO file bytes / base64 / filename-content / signed URL (refs only: storageId/filename/size/
    counts, §4). Extend `run-smoke-guardrails.mjs` (V8): drive a generation turn under an exhausted
    daily budget / kill-switch and assert `preCall` returns the paused reply — NO attachment stored, NO
    `deadLetters` row, plan not approvable. Judge pass/fail via the smokeRun.mjs banner match (Windows
    exit-code caveat).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend smoke:fanout && pnpm --filter @pikar/backend smoke:guardrails</automated>
  </verify>
  <done>smoke:fanout carries the attachment to every recipient with refs-only logs and isolated DLQ (V6); smoke:guardrails proves budget-stop pause-as-data with no attachment/DLQ (V8); both green against a live convex dev.</done>
</task>

<task type="auto">
  <name>Task 2: cockpit-attachment Playwright E2E</name>
  <files>apps/web/e2e/cockpit-attachment.spec.ts</files>
  <action>
    New `apps/web/e2e/cockpit-attachment.spec.ts` driving the offline `SMOKE::agent::` grammar (mirror
    cockpit-report.spec.ts): add a recipient (`SMOKE::agent::add=a@x.com`), set subject, draft body
    (`SMOKE::route=direct_llm::` sentinel), generate an attachment (`SMOKE::agent::attach=Proposal`),
    then `propose`. Assert (V9): the PLAN card shows the attachment filename + a download link (and no
    attachmentError); click Approve; the REPORT card fills per recipient showing delivered-with-attachment
    + a re-download link. Also drive `SMOKE::agent::removeAttachment=1` in a variant and assert the row
    disappears (regenerate/remove pre-approval). Spec must be Playwright-discovered and type-load.
  </action>
  <verify>
    <automated>pnpm --filter web exec playwright test cockpit-attachment</automated>
  </verify>
  <done>cockpit-attachment.spec.ts passes: chat→attach→PLAN(filename+download)→Approve→REPORT(delivered-with-attachment); remove-attachment variant asserted (V9).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — real PDF attachment delivered + audit refs-only + playbook close</name>
  <action>Manual verification checkpoint (V10, the sole manual validation). Follow the how-to-verify steps below; this is truly manual because a real Gmail send needs human OAuth consent and a human inbox to confirm the PDF opens. Also update docs/playbooks/cockpit.md with the attachment invariants and bump its Last verified to 03.3-06 before signing off.</action>
  <what-built>
    The full CKPT-02 attachment path on the live Gmail-connected dev backend: the agent generates a
    PDF from an instruction, attaches it to an approved plan, and the governed fan-out delivers it.
    Also: the cockpit playbook (§9) is updated with the attachment invariants (block-on-render-fail,
    refs-only attachment logs, 8MB/plan cap, multipart send, storage-delete-on-remove/supersede) and
    its `Last verified` line bumped to 03.3-06. watch.json needs no edit here — the one new pure-validator file `packages/core/src/documentGen.ts` was already registered under the cockpit.md entry in Plan 03.3-01; all other touched files are pre-existing cockpit.md paths.
  </what-built>
  <how-to-verify>
    1. Ensure `convex dev` (NOT --once) + `next dev :3111` are running and Gmail is connected; run
       `skills:seedSkills` so `document-drafter` (+ the cockpit-agent v-bump) are active.
    2. In the cockpit, ask the agent to draft an email to your own test address AND attach a document
       (e.g. "attach a one-page proposal on X"). Confirm when it offers to generate.
    3. On the PLAN card: open/preview the generated PDF — confirm it is a real, readable document
       (title/headings/body, clean/minimal, no logo). Optionally Regenerate with new instructions and
       Remove to confirm those controls work.
    4. Approve (exactly one). Confirm the recipient inbox receives the email WITH the PDF attached and
       it opens correctly.
    5. On the REPORT card: confirm delivered-with-attachment and that re-download returns the exact
       sent file.
    6. Inspect the audit/deadLetters for this send: confirm refs only (storageId/filename/size/
       messageId/counts) — NO file bytes, base64, or signed URL (§4).
  </how-to-verify>
  <resume-signal>Type "approved" (real PDF delivered + audit refs-only), or describe the defect.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend smoke:fanout` + `smoke:guardrails` green (V6, V8).
- `pnpm --filter web exec playwright test cockpit-attachment` green (V9).
- Human-verify: real PDF delivered, audit refs-only (V10).
- cockpit.md playbook updated + Last verified → 03.3-06 (§9 Stop hook clears).
</verification>

<success_criteria>
- The attachment rides the governed fan-out to every recipient with refs-only logs and isolated DLQ.
- A budget/kill-switch stop during generation pauses as data — no attachment, no DLQ.
- End-to-end offline flow passes; a real PDF is delivered and human-verified with audit refs only.
- CKPT-02 satisfied; playbook closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-attachment-generation/03.3-06-SUMMARY.md`
</output>
