---
phase: 13-proactive-in-app-review
plan: 04
type: execute
wave: 4
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - .planning/phases/13-proactive-in-app-review/13-04-SUMMARY.md
autonomous: false
requirements: [BEVL-03]

must_haves:
  truths:
    - "Running the real production entry point end to end produces a visible review card and a working notification"
    - "The review tab shows no composer in the running app"
    - "The full test suite and every governance gate is green before the phase closes"
  artifacts:
    - path: ".planning/phases/13-proactive-in-app-review/13-04-SUMMARY.md"
      provides: "recorded evidence of the live run"
  key_links:
    - from: "Convex dashboard function runner"
      to: "internal.proactiveReview.runWeekly"
      via: "the exact production path including scheduler fan-out"
      pattern: "runWeekly"
---

<objective>
Close the phase on evidence, not inference: run every automated gate, then exercise the REAL
production entry point (`proactiveReview:runWeekly` from the Convex dashboard, `{}`) and confirm
the in-app delivery with your own eyes.

Purpose: the workspace has no component-test harness, so the pinned tab / composer suppression /
card rendering are verifiable only by running the app. This is the phase's one manual gate.
Output: recorded evidence for `/gsd:verify-work`.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-proactive-in-app-review/13-VALIDATION.md
@.planning/phases/13-proactive-in-app-review/13-01-SUMMARY.md
@.planning/phases/13-proactive-in-app-review/13-02-SUMMARY.md
@.planning/phases/13-proactive-in-app-review/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full phase gate</name>
  <files>(no source changes — verification only)</files>
  <action>
Run the phase gate from `13-VALIDATION.md` and record the exact numbers in the summary:

1. `pnpm test` (full turbo suite).
2. `pnpm --filter @pikar/web typecheck`.
3. `pnpm --filter @pikar/backend typecheck`.
4. `node scripts/check-playbooks.mjs`.
5. `graphify update . && node scripts/extract-convex-edges.mjs` (idempotent; confirms the graph
   reflects the new module).

Baseline discipline: the backend suite carries ONE pre-existing failure (474/475, recorded at
12-05). Report it by NAME and confirm it is the same one — do not treat it as a regression, and do
not hide a NEW failure behind it.

If a gate fails, FIX IT here rather than deferring — this is the last plan of the phase.

Then start the deployment for the live run: `npx convex dev` from `packages/backend` (per the
recorded gotcha: run the convex CLI from `packages/backend` only), and `pnpm dev` for the web app.
Note that `.planning` memory records Next dev OOMing on the workspace page during replays — if the
workspace page fails to load under `pnpm dev`, build for production and serve that instead.

Do NOT add a dev-only UI trigger for the cron. The dashboard function runner IS the locked
verification path.
  </action>
  <verify>
    <automated>pnpm test && pnpm --filter @pikar/web typecheck && pnpm --filter @pikar/backend typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All gates green (backend at the known 474/475-equivalent baseline with the same named failure); graph refreshed; a Convex deployment and the web app are running for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live proactive review</name>
  <files>(no source changes — human verification of the running app)</files>
  <action>Pause and hand the verification steps below to the owner. Do not proceed, do not
substitute an automated approximation, and do not add a dev-only UI trigger — the Convex dashboard
function runner on `internal.proactiveReview.runWeekly` is the locked verification path because it
exercises the exact production entry point including the scheduler fan-out.</action>
  <what-built>
A weekly Convex cron (`proactive-review`, Monday 06:00 UTC) that runs the Phase-12 evaluation
engine per onboarded tenant and delivers the result in-app: an evaluation card on a pinned
"Weekly review" workspace tab plus a notification — touching no Gmail/OAuth token at any point.
  </what-built>
  <how-to-verify>
1. Open the **Convex dashboard** for your dev deployment → **Functions** → find
   `proactiveReview:runWeekly` → **Run function** with arguments `{}`.
   - Expect: the run succeeds, and the **Logs** tab shows one scheduled `proactiveReview:reviewOne`
     per onboarded tenant (one per tenant that has a `business_profile` vault doc).
2. In the dashboard **Data** tab, open `evaluations` and confirm a new row with
   `threadId: "proactive-review"` for your tenant. Open `notifications` and confirm a row with
   `kind: "weekly_review"`.
   - **Critically:** confirm there is NO new `audit` row other than the existing `evaluation.ran`,
     and that `evaluation.ran`'s payload contains only counts and enum strings — no finding text,
     no citation titles, no document names.
3. Open `http://localhost:3000/dashboard/workspace`.
   - Expect: a **"Weekly review"** tab in the tab strip, with **no × close button**.
4. Click the "Weekly review" tab.
   - Expect: the evaluation card renders on the right with a header reading
     `Weekly review · <today's date>`.
   - Expect: the left pane shows the short explainer text and **NO message composer / text box**.
     (Typing there would throw — the composer is deliberately suppressed.)
5. Look at the notification banner at the top of the app.
   - Expect: "Your weekly business review is ready." and that **clicking it opens the review tab**.
6. Run `proactiveReview:runWeekly {}` from the dashboard a SECOND time without changing anything
   in your vault.
   - Expect: a SECOND `evaluations` row appears (the card refreshes weekly) but **NO second
     `weekly_review` notification** — notify-on-change is what keeps the bell meaningful.
   - Expect: the card may now show a "what changed" line; with no real change it should show none.
7. Confirm nothing mailbox-related happened: no Gmail send, no `invalid_grant`, no reconnect
   prompt — the review works even with Gmail disconnected.

Report anything that differs, especially: a composer appearing on the review tab, a second
notification on the unchanged re-run, any extra audit row, or the review tab being missing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what differed</resume-signal>
  <done>The owner has run `proactiveReview:runWeekly {}` from the dashboard and confirmed: the
review card on the pinned tab, no composer on that tab, the clickable notification, a silent
unchanged re-run, no extra audit row, and no mailbox activity.</done>
</task>

</tasks>

<verification>
- The live run used `internal.proactiveReview.runWeekly` — not a test, not a dev-only button.
- Every observation in the summary is something that was actually seen, not inferred from code.
</verification>

<success_criteria>
- `pnpm test` green at the known baseline; `check-playbooks.mjs` exits 0; both typechecks clean.
- The owner has run the dashboard invocation and confirmed the card, the pinned tab, the suppressed
  composer, the clickable notification, and the silent unchanged re-run.
- No mailbox token was touched at any point.
</success_criteria>

<output>
After completion, create `.planning/phases/13-proactive-in-app-review/13-04-SUMMARY.md` recording
the exact test counts, the dashboard run ids/timestamps, and the owner's verification verdict.
</output>
