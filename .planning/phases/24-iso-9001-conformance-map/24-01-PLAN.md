---
phase: 24-iso-9001-conformance-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/governance/iso-9001-conformance-map.md
  - docs/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "One canonical, thin map indexes the existing evidence system; it does not duplicate playbooks, ADRs, audit rows, incident narratives, or the ISO standard."
    - "The map names ISO 9001:2015 including Amendment 1:2024, defines a software/product and governed-service scope, and explicitly disclaims certification or organization-wide conformity."
    - "Every roadmap-mandated clause family points to substantive file-level evidence, explains the operating control, names its limitation, and gives a verification method."
    - "Tests are not presented as ISO internal audit, owner checkpoints are not presented as management review, and source presence is not presented as live WORM operation."
  artifacts:
    - path: "docs/governance/iso-9001-conformance-map.md"
      provides: "The single canonical ISO 9001 evidence-alignment map"
      contains: "ISO 9001:2015"
    - path: "docs/README.md"
      provides: "Discoverability and the rule that governance evidence indexes point to existing records rather than duplicate them"
  key_links:
    - from: "docs/governance/iso-9001-conformance-map.md"
      to: "packages/backend/convex/audit.ts and packages/backend/convex/worm.ts"
      via: "the 7.5/8.5.1 rows separate insert-only operational evidence from conditional external preservation"
      pattern: "7\\.5|8\\.5\\.1"
    - from: "docs/governance/iso-9001-conformance-map.md"
      to: "packages/backend/convex/skills.ts and packages/backend/scripts/run-eval-golden.mjs"
      via: "the 8.5.6/8.6 rows scope versioning, release evidence, activation, exceptions, and rollback"
      pattern: "8\\.5\\.6|8\\.6"
    - from: "docs/governance/iso-9001-conformance-map.md"
      to: "docs/playbooks and docs/decisions"
      via: "the 7.5/8.3 rows describe Git-backed documented information and design/change evidence without copying it"
      pattern: "7\\.5|8\\.3"
---

<objective>
Create the canonical, scoped ISO 9001:2015 evidence map from the governance mechanisms that already
operate.

Purpose: GOVN-02 is classification work, not a new process engine. The repository already has the
audit, skill registry, eval, ADR, playbook, GSD, telemetry, DLQ and notification evidence. This plan
makes that evidence findable and honestly bounded without creating a parallel QMS.

Output: one Markdown evidence index under `docs/governance/`, plus one discoverability paragraph in
the existing docs README. No source code, database, schema, dependency, generated binder or user UI.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/design/iso9001-qms-layer.md
@.planning/phases/24-iso-9001-conformance-map/24-RESEARCH.md
@.planning/phases/24-iso-9001-conformance-map/24-VALIDATION.md
@docs/README.md
@docs/decisions/002-insert-only-audit.md
@docs/decisions/003-skill-registry-for-prompts.md
@docs/playbooks/audit-dead-letter.md
@docs/playbooks/skill-registry.md
@docs/playbooks/agent-runtime.md
@docs/playbooks/ci-gate.md
@scripts/check-playbooks.mjs
@docs/playbooks/watch.json
@packages/backend/convex/audit.ts
@packages/backend/convex/auditImmutability.test.ts
@packages/backend/convex/worm.ts
@packages/backend/convex/wormCursor.ts
@packages/backend/convex/skills.ts
@packages/backend/convex/skills.test.ts
@packages/backend/convex/deadLetters.ts
@packages/backend/convex/notifications.ts
@packages/backend/scripts/run-eval-golden.mjs

<interfaces>
The map's status vocabulary is CLOSED:

- `Direct — defined scope`
- `Supporting`
- `Partial — gap named`
- `Not assessed — organization-wide`
- `Not applicable — justified`

Use the exact matrix header
`| Clause | Intent | Status | Evidence | Control | Limitation / gap | Verification |`.
Every data row must populate all seven cells. Every local evidence pointer in the matrix Evidence
column or corrective-action index uses a single backticked `repo:PATH` reference,
with an optional `#symbol`/`#section` suffix. This contract is root-agnostic: `repo:apps/...`,
`repo:.github/...`, `repo:CLAUDE.md`, and any other real path are valid. Non-path evidence may use
the explicit `commit:`, `run:`, or `symbol:` prefixes; external sources use ordinary Markdown links,
not code spans. Never use a line number as the locator, never put a command in these evidence cells,
and do not put literal pipe characters inside cells; the closing plan parses this exact contract.

The standards baseline is ISO 9001:2015 including Amendment 1:2024. Link primary ISO/ISO-IAF
sources from 24-RESEARCH.md; do not reproduce normative text. The pending 2026 revision is a review
trigger, not a guessed requirement set.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the one-map boundary, status vocabulary, and discoverability rule</name>
  <files>docs/governance/iso-9001-conformance-map.md, docs/README.md</files>
  <action>
Create `docs/governance/iso-9001-conformance-map.md` as the ONE canonical operational evidence map.
It must open with:

- title, owner/reviewer placeholder, `Last reviewed`, and baseline `ISO 9001:2015 including
  Amendment 1:2024`;
- a defined scope limited to Pikar software/product design, governed behavior release, and governed
  service operation;
- the hard claim boundary from `.planning/design/iso9001-qms-layer.md`: the map is not a
  certificate, conformity declaration, certification audit, or claim that Pikar/customers are ISO
  certified;
- the closed five-status vocabulary from the plan's `interfaces` contract;
- evidence hierarchy: runtime record/read surface → executable test/smoke → implementation/schema
  → dated/versioned verification → playbook/ADR → plan/design intent;
- the rule that this file POINTS to existing artifacts and never copies their content.

Name Amendment 1:2024 explicitly and add a narrow review trigger for the next published edition.
For 4.1/4.2, record that climate relevance was considered for this defined evidence-map scope and
give a short honest disposition; do not manufacture an environmental objective or climate program.

Update `docs/README.md` with one compact section for `governance/`: it contains pointer-only
cross-system evidence indexes, not a third family of subsystem runbooks or mutable records. Link the
canonical map. Preserve the existing playbook and ADR rules byte-for-meaning; do not turn the README
into a quality manual.

Do not add a generator, JSON/YAML mirror, PDF, schema, table, dependency, `watch.json` registration,
or a second ISO document. The design/research files remain historical inputs and must be labelled as
such by the canonical map rather than edited.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='docs/governance/iso-9001-conformance-map.md'; const s=fs.readFileSync(p,'utf8'); for(const x of ['ISO 9001:2015','Amendment 1:2024','defined scope','not a certificate','Direct — defined scope','Not assessed — organization-wide','pointer']) if(!s.toLowerCase().includes(x.toLowerCase())) throw new Error('missing '+x); const r=fs.readFileSync('docs/README.md','utf8'); if(!r.includes('governance/iso-9001-conformance-map.md')) throw new Error('map not discoverable'); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const files=['docs/governance/iso-9001-conformance-map.md']; if(files.length!==1||!files.every(fs.existsSync)) throw new Error('canonical map missing'); const s=fs.readFileSync(files[0],'utf8'); for(const bad of ['Pikar is ISO certified','customers are ISO certified','declaration of conformity']) if(s.includes(bad)&amp;&amp;!s.toLowerCase().includes('not a certificate')) throw new Error('unbounded claim: '+bad); console.log('ok')"</automated>
  </verify>
  <done>One discoverable map has the exact standard baseline, explicit defined scope, closed statuses,
  evidence hierarchy, and no-certification boundary; no parallel artifact or runtime was added.</done>
</task>

<task type="auto">
  <name>Task 2: Map the real audit, documented-information, design, change and release evidence</name>
  <files>docs/governance/iso-9001-conformance-map.md</files>
  <action>
Build the thin clause matrix with the exact seven-column header from the interfaces contract. Populate the
roadmap-mandated mappings first and make every claim specific:

1. **7.5 / 8.5.1 — audit and controlled operation.** Point to `CLAUDE.md` §§3–4,
   ADR-002, `audit.ts#log`, `AuditPayload`, the audit schema/indexes,
   `auditImmutability.test.ts`, `worm.ts#exportAudit`, `wormCursor.ts`, retention math, Phase-7
   verification, and the governed plan→Approve→execute path. Distinguish the insert-only hot copy
   from true external preservation. `WORM_BUCKET`-unset is an intentional skip; real S3
   Object-Lock delete refusal remains conditional unless newer dated live evidence is found.
2. **7.5 / 8.3 — GSD, playbooks and ADRs.** Point to REQUIREMENTS/ROADMAP, representative PLAN /
   SUMMARY / VALIDATION / VERIFICATION artifacts, `docs/README.md`, ADRs, playbook TEMPLATE,
   `watch.json`, `check-playbooks.mjs`, `.claude/settings.json`, and Git. State that the watcher is a
   Claude hook, fails open on tool/Git failure, and is not a universal human/non-Claude merge gate.
3. **8.5.6 — skill/code change control.** Point to ADR-003, skills schema,
   `skills.ts#seedSkills/#activateSkillVersion/#recordEvalEvidence/#insertCandidate`, tests,
   playbook, Git and rollback. Say bodies are immutable per version while status/evidence are the
   sanctioned mutable fields.
4. **8.6 — release.** Point to the golden runner, exact version evidence, owner activation, Phase
   3.6 verification and CI/phase verification. Scope `Direct` to gated candidate skill versions.
   State bootstrap v1, explicitly ungated skills, unpaid/failed candidates, on-demand live runs,
   and non-automated deployment as limitations. Never say “all skills are eval-gated.”
5. **10.2 — only the detection/containment half in this plan.** Point to DLQ writer/read/resolve,
   notification choke point, tests, audit row and playbook. Mark `Partial — gap named`: resolved
   status is not cause/action/effectiveness. Plan 24-02 owns the real corrective-action linkage.

Add only substantive adjacent rows from research: 4.1–4.4, 6.1–6.3, 7.1.6, 8.1–8.4,
8.5.2, 8.7, 9.1, 9.2, 9.3, 10.3. Mark company-wide leadership/policy/competence/internal-audit/
management-review areas `Not assessed — organization-wide`; tests are NOT 9.2, owner checkpoints
are NOT 9.3, and owner authorization is NOT a company quality-policy substitute.

Paraphrase clause intent and link official sources from research. Do not quote or recreate the
standard. Prefer path + exported function/section; do not cite brittle line numbers.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/auditImmutability.test.ts convex/worm.test.ts convex/skills.test.ts convex/deadLetters.test.ts convex/notifications.test.ts --maxWorkers=1</automated>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/governance/iso-9001-conformance-map.md','utf8'); for(const x of ['7.5','8.3','8.5.1','8.5.6','8.6','9.1','9.2','9.3','10.2','10.3','auditImmutability.test.ts','activateSkillVersion','run-eval-golden.mjs','deadLetters.ts']) if(!s.includes(x)) throw new Error('missing '+x); for(const x of ['tests are not','owner checkpoints are not','conditional']) if(!s.toLowerCase().includes(x)) throw new Error('missing boundary '+x); console.log('ok')"</automated>
  </verify>
  <done>Every roadmap-mandated mapping names real code/record/test evidence and its limitation;
  adjacent clauses are included only when substantive, and organization-wide gaps are not relabelled.</done>
</task>

<task type="auto">
  <name>Task 3: Add evidence-use rules, gap dispositions and narrow maintenance triggers</name>
  <files>docs/governance/iso-9001-conformance-map.md</files>
  <action>
Close Plan 01's map as an operational index, not a snapshot dump:

- For every `Partial — gap named` row, add exactly one disposition: close in Phase 24, accepted
  limitation, defer to company-certification readiness with trigger/reason, or block the claim
  until live evidence exists.
- Add a “How to use this map” section: start at the clause row, follow the actual source/test/runtime
  pointer, verify current state, and treat dated summaries only as historical evidence for the exact
  version/run named.
- Add a “Maintenance triggers” section limited to ISO edition publication; audit/WORM mechanism;
  skill candidate/eval/activation/rollback semantics; playbook/ADR/GSD change control; introduction
  of actual internal-audit/management-review processes; or a new marketing/compliance claim.
- State that ordinary feature changes do not update this map when the mapped mechanism is unchanged.
- Add the exact offline verification commands from 24-VALIDATION and a Manual-Only section for
  claim judgment, real S3 Object Lock, organization-wide clauses, and edition re-baseline.
- Add a `Known limitations` section that preserves at least: conditional live WORM evidence,
  hook-scoped playbook enforcement, gated-vs-ungated release scope, 10.2 linkage deferred to 24-02,
  and no 9.2/9.3 evidence.

Do not paste test output, audit rows, ADR prose, playbook prose, ISO text, or incident narratives
into the map. Paths and concise control descriptions are the stable evidence.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/governance/iso-9001-conformance-map.md','utf8'); for(const x of ['How to use this map','Maintenance triggers','Manual-Only','Known limitations','company-certification readiness','ordinary feature changes']) if(!s.toLowerCase().includes(x.toLowerCase())) throw new Error('missing '+x); const partial=(s.match(/Partial — gap named/g)||[]).length; if(partial&lt;1) throw new Error('map hides all gaps'); if(!s.includes('24-02')) throw new Error('10.2 gap has no owner'); console.log('ok')"</automated>
    <automated>node scripts/check-playbooks.mjs check</automated>
  </verify>
  <done>The map tells a future reader how to verify current evidence, gives every partial row one
  disposition, and changes only when an evidence mechanism or external claim changes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/auditImmutability.test.ts convex/worm.test.ts convex/skills.test.ts convex/deadLetters.test.ts convex/notifications.test.ts --maxWorkers=1`
- `node packages/backend/scripts/run-eval-golden.mjs --self-check`
- `node scripts/check-playbooks.mjs check`
- Node map assertions in Tasks 1–3 pass.
- `git diff -- docs/governance/iso-9001-conformance-map.md docs/README.md` contains one map and one
  discoverability change, no copied binder content.
</verification>

<success_criteria>
- One canonical map exists with a defined scope and no-certification boundary.
- The audit, playbook/ADR/GSD, skill versioning/rollback, eval release and DLQ evidence mappings are
  file-level, substantive and honestly limited.
- Company-wide/internal-audit/management-review claims remain not assessed.
- Every partial row has one explicit disposition and the map has narrow maintenance triggers.
</success_criteria>

<output>
After completion, create
`.planning/phases/24-iso-9001-conformance-map/24-01-SUMMARY.md`, recording the map path, evidence
families mapped, exact offline results, and every limitation intentionally left open for Plan 24-02
or company-certification readiness.
</output>
