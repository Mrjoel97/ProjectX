---
phase: 24-iso-9001-conformance-map
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - docs/governance/iso-9001-conformance-map.md
autonomous: false
requirements: [GOVN-02]

must_haves:
  truths:
    - "Clause 10.2 is supported by real, linked nonconformity→cause→action→effectiveness→closure chains, not by treating a DLQ resolved flag as corrective action."
    - "The corrective-action closure is a bounded pointer index inside the one canonical map; it creates no second incident store, table, runbook, template or approval process."
    - "Every local evidence pointer resolves, required clause/status/limitation fields are present, and no unqualified certification/conformity claim survives."
    - "The owner has reviewed the semantic scope and gap dispositions; live WORM and organization-wide clauses remain explicitly manual/not-assessed unless real evidence exists."
  artifacts:
    - path: "docs/governance/iso-9001-conformance-map.md"
      provides: "Owner-reviewed final conformance foundation with a non-vacuous 10.2 evidence index"
      contains: "Corrective-action evidence index"
  key_links:
    - from: "docs/governance/iso-9001-conformance-map.md#corrective-action-evidence-index"
      to: ".planning/phases/19-contacts-crm-follow-ups/19-10-SUMMARY.md and 19-13-SUMMARY.md"
      via: "a pointer chain from measured behavior defect through cause/fix/regression/UAT closure"
      pattern: "19-10-SUMMARY|19-13-SUMMARY"
    - from: "docs/governance/iso-9001-conformance-map.md#corrective-action-evidence-index"
      to: ".planning/phases/07-resilience-operations-hardening/07-06-SUMMARY.md and 07-VERIFICATION.md"
      via: "a second real chain for the missing general notification render surface and verified closure"
      pattern: "07-06-SUMMARY|07-VERIFICATION"
    - from: "docs/governance/iso-9001-conformance-map.md"
      to: "the repository evidence paths"
      via: "final path-resolution and anti-overclaim review"
      pattern: "Evidence|Limitation|Verification"
---

<objective>
Close only the genuine gap the map exposed: link material nonconformities to their already-existing
cause, corrective-action and effectiveness evidence, then prove the final map is non-vacuous and
semantically honest.

Purpose: DLQ/notifications detect and contain failures, but ISO 10.2 is overstated unless cause,
action and effectiveness can be followed. Pikar already records those elements in selected GSD
gap-closure/debug/commit/test artifacts. This plan adds the missing links inside the same map and
performs the owner claim-boundary gate—nothing more.

Output: the final owner-reviewed pointer-only map satisfying GOVN-02. No QMS runtime, second binder,
corrective-action table, mandatory ceremony, ISO text copy, or certification claim.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-iso-9001-conformance-map/24-RESEARCH.md
@.planning/phases/24-iso-9001-conformance-map/24-VALIDATION.md
@.planning/phases/24-iso-9001-conformance-map/24-01-SUMMARY.md
@docs/governance/iso-9001-conformance-map.md
@packages/backend/convex/deadLetter.ts
@packages/backend/convex/deadLetters.ts
@packages/backend/convex/deadLetters.test.ts
@packages/backend/convex/notifications.ts
@packages/backend/convex/notifications.test.ts
@docs/playbooks/audit-dead-letter.md
@.planning/phases/19-contacts-crm-follow-ups/19-10-SUMMARY.md
@.planning/phases/19-contacts-crm-follow-ups/19-13-SUMMARY.md
@.planning/phases/19-contacts-crm-follow-ups/19-VERIFICATION.md
@.planning/phases/07-resilience-operations-hardening/07-06-SUMMARY.md
@.planning/phases/07-resilience-operations-hardening/07-VERIFICATION.md
</context>

<task_split_rationale>
Four tasks are intentional. Task 1 authors the only genuine gap closure; Task 2 independently
proves the resulting map is mechanically non-vacuous; Task 3 is a blocking semantic owner review;
and Task 4 must remain post-checkpoint so it can record the actual review date and rerun the final
gate after any owner-requested correction. Combining across that checkpoint would falsely permit
finalization before approval.
</task_split_rationale>

<tasks>

<task type="auto">
  <name>Task 1: Add a bounded, real corrective-action evidence index for clause 10.2</name>
  <files>docs/governance/iso-9001-conformance-map.md</files>
  <action>
Add `## Corrective-action evidence index` inside the canonical map. It is an INDEX, not a process
or record store. Its entry criteria are “material product/service or governance nonconformity where
recurrence prevention/effectiveness evidence matters,” not every test failure or DLQ row.

Use this exact CLOSED table header so the integrity assertion can parse it:
`| Nonconformity / observed effect | Containment / immediate correction | Cause evidence | Corrective action / recurrence prevention | Effectiveness evidence | Closure / disposition and owner / date |`.
Do not place literal pipe characters inside cells. Every row must populate all six cells.

Every cell is a concise pointer to existing evidence—not copied narrative. Encode each local file
as one backticked `repo:PATH[#symbol-or-section]` reference. Encode non-path
historical evidence, when needed, with `commit:`, `run:`, or `symbol:`; use ordinary Markdown links
for external URLs. This makes every local pointer mechanically enumerable without a root allowlist.
Seed exactly two non-vacuous closed chains:

- **Phase 19 CRM follow-up/clock chain.** Observation begins at
  `.planning/phases/19-contacts-crm-follow-ups/19-10-SUMMARY.md` (the strengthened fixture measured
  the wrong op). Follow the actual dropped-clock/fabricated-address correction commits and the
  evidence named in `19-13-SUMMARY.md`/`19-VERIFICATION.md`, including the exact fixture/run/UAT
  closure that shows effectiveness. Do not pretend the original 35/35 was one post-fix full run;
  preserve the recorded splice caveat.
- **Phase 7 notification visibility chain.** Use `07-06-SUMMARY.md` and `07-VERIFICATION.md` to link
  the owner-found missing general in-app render surface to `NotificationsBanner`, its tests/build,
  live owner verification, and closure. Keep real S3 Object Lock and full external-mailbox matrix
  deferrals separate; they are not “corrected” by this example.

Then revise the main 10.2 clause row:

- DLQ + notifications = direct detection/containment/visibility evidence;
- the index = supporting evidence that selected material nonconformities were cause-analysed,
  corrected and effectiveness-checked;
- no claim that every DLQ row receives formal corrective action;
- no generic `Direct` organization-wide 10.2 verdict. Keep `Partial — gap named` or a precisely
  scoped split status unless the evidence actually supports more.

Do not add a new document, incident id, workflow, required template, table/schema, owner gate, or
duplicated incident description. Future material examples may append pointer rows only when real
evidence exists.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),s=fs.readFileSync('docs/governance/iso-9001-conformance-map.md','utf8'),lines=s.split(/\r?\n/),split=l=>l.trim().replace(/^\||\|$/g,'').split('|').map(x=>x.trim()),h=['nonconformity / observed effect','containment / immediate correction','cause evidence','corrective action / recurrence prevention','effectiveness evidence','closure / disposition and owner / date'],at=lines.findIndex(l=>{const c=split(l);return c.length===h.length&amp;&amp;h.every((v,i)=>c[i].toLowerCase()===v)});if(!s.includes('## Corrective-action evidence index'))throw Error('missing index heading');if(at===-1)throw Error('missing exact corrective-action header');const rows=[];for(let i=at+2;i&lt;lines.length&amp;&amp;/^\s*\|/.test(lines[i]);i++)rows.push(split(lines[i]));if(rows.length!==2)throw Error('expected exactly two evidence chains');rows.forEach((r,i)=>{if(r.length!==6||r.some(x=>!x))throw Error('incomplete index row '+(i+1))});const body=rows.flat().join(' ');for(const x of ['repo:.planning/phases/19-contacts-crm-follow-ups/19-10-SUMMARY.md','repo:.planning/phases/19-contacts-crm-follow-ups/19-13-SUMMARY.md','repo:.planning/phases/07-resilience-operations-hardening/07-06-SUMMARY.md','repo:.planning/phases/07-resilience-operations-hardening/07-VERIFICATION.md','splice'])if(!body.toLowerCase().includes(x.toLowerCase()))throw Error('missing chain evidence '+x);console.log('ok: exact header and two complete evidence chains')"</automated>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/deadLetters.test.ts convex/notifications.test.ts --maxWorkers=1</automated>
  </verify>
  <done>The 10.2 row distinguishes detection from corrective action and links two complete real
  evidence chains without creating a second process or store.</done>
</task>

<task type="auto">
  <name>Task 2: Prove map integrity, evidence resolution, bounded statuses and anti-overclaim</name>
  <files>docs/governance/iso-9001-conformance-map.md</files>
  <action>
Run the final integrity walk and correct ONLY the map:

1. Confirm every required clause/status family from 24-VALIDATION exists: 4.1–4.3, 7.5, 8.3,
   8.5.1, 8.5.6, 8.6, 9.1, 9.2, 9.3, 10.2, 10.3.
2. Confirm each matrix row has actual evidence, control, limitation/gap, and verification—not just
   a filename or generic claim.
3. Parse the exact seven-column evidence matrix and six-column corrective-action index contracts.
   Enumerate every `repo:` reference in the matrix `Evidence` cells and all index cells, reject any
   path-like code span without that prefix, strip a `#symbol`/`#section` suffix, and require
   `fs.existsSync` for every resulting local path regardless of its root (`apps/`, `.github/`, a
   root file, or any other repository directory).
   Wildcard families are allowed only when accompanied by at least one concrete representative
   path. Do not treat a failed path check as a documentation limitation—correct the pointer.
4. Reject evidence citations whose only locator is `file.ts:123`; replace with path + exported
   function/section. Dates/run ids/commit hashes remain legitimate historical locators.
5. Verify every status is one of the five closed values. No percentages, maturity scores,
   `Compliant`, `Certified`, or `Conformant` verdicts.
6. Search certification language semantically. The words may occur in the title, baseline,
   prohibition, external-source names, and future-certification boundary, but never as an
   unqualified Pikar/customer claim.
7. Confirm one canonical operational map. The design and Phase-24 research are explicitly inputs,
   not alternate live binders.
8. Confirm WORM, playbook-hook, gated-release, 9.2/9.3, and 10.2 limitations are all still present
   after editing.

Use the exact one-off Node assertion in this task's verify block; it parses both table contracts, validates every
required cell, rejects statuses outside the five-value vocabulary, resolves every extracted local
pointer, checks required clauses, and preserves the anti-overclaim checks. Do NOT commit a Markdown
parser or `check-iso-map` framework unless a concrete repeated-maintenance need is proven; that
would be a new mechanism beyond this phase.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='docs/governance/iso-9001-conformance-map.md',s=fs.readFileSync(p,'utf8'),lines=s.split(/\r?\n/),split=l=>l.trim().replace(/^\||\|$/g,'').split('|').map(x=>x.trim()),table=h=>{const at=lines.findIndex(l=>{const c=split(l);return c.length===h.length&amp;&amp;h.every((v,i)=>c[i].toLowerCase()===v)});if(at===-1)throw Error('missing table: '+h.join(', '));const out=[];for(let i=at+2;i&lt;lines.length&amp;&amp;/^\s*\|/.test(lines[i]);i++)out.push(split(lines[i]));if(out.length===0)throw Error('empty table: '+h[0]);return out},mh=['clause','intent','status','evidence','control','limitation / gap','verification'],ih=['nonconformity / observed effect','containment / immediate correction','cause evidence','corrective action / recurrence prevention','effectiveness evidence','closure / disposition and owner / date'],main=table(mh),idx=table(ih),allowed=new Set(['Direct — defined scope','Supporting','Partial — gap named','Not assessed — organization-wide','Not applicable — justified']);main.forEach((r,i)=>{if(r.length!==7||r.some(x=>!x))throw Error('incomplete matrix row '+(i+1));if(!allowed.has(r[2]))throw Error('invalid status '+r[2])});idx.forEach((r,i)=>{if(r.length!==6||r.some(x=>!x))throw Error('incomplete index row '+(i+1))});for(const c of ['4.1','4.2','4.3','7.5','8.3','8.5.1','8.5.6','8.6','9.1','9.2','9.3','10.2','10.3'])if(!main.some(r=>r[0].includes(c)))throw Error('missing clause '+c);const texts=main.map(r=>r[3]).concat(idx.flat()),paths=[],prose=s.replace(/```[\s\S]*?```/g,''),command=/^(?:node|pnpm|npm|npx|git|rg|vitest|pwsh|powershell)(?:\s|$)/,pathlike=/^(?:\.{0,2}[\/\\]|[^:\s]+[\/\\]|[^:\s]+\.[A-Za-z][A-Za-z0-9]{0,11}(?:#.*)?$)/;for(const m of prose.matchAll(/`([^`\n]+)`/g)){const x=m[1].trim();if(x.startsWith('repo:')){const raw=x.slice(5);if(!raw||/[\s?]/.test(raw)||/^[/\\]/.test(raw)||/^[A-Za-z]:/.test(raw)||raw.includes('\\')||raw.split('/').includes('..'))throw Error('invalid repo reference '+x);if(/:\d+(?::\d+)?(?:#.*)?$/.test(raw))throw Error('line-number pointer '+x);const q=raw.split('#')[0];if(!fs.existsSync(q))throw Error('missing evidence path '+q);paths.push(q)}else if(/^(?:commit|run|symbol):/.test(x)||/^https?:\/\//.test(x)||command.test(x))continue;else if(pathlike.test(x))throw Error('untyped local path; use repo: '+x)}for(const text of texts){if(/`repo:[^`\n]+`/.test(text))continue;const status=main.find(r=>r[3]===text)?.[2];if(status&amp;&amp;!status.startsWith('Not '))throw Error('mapped evidence lacks repo reference: '+text)}if(paths.length===0)throw Error('no repo evidence paths extracted');for(const bad of ['Pikar is ISO certified','customers are ISO certified','fully compliant','100% conformant'])if(s.toLowerCase().includes(bad.toLowerCase()))throw Error('forbidden claim '+bad);console.log('ok: '+main.length+' matrix rows, '+idx.length+' index rows, '+new Set(paths).size+' local paths')"</automated>
  </verify>
  <done>All required rows are substantive, every local pointer resolves, statuses are bounded, and
  no line-number-only citation, parallel binder, false WORM claim, or certification overclaim remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OWNER GATE — scope, applicability and no-process-theater review</name>
  <what-built>
    One canonical ISO 9001:2015 + Amendment 1:2024 evidence map over Pikar's existing audit,
    skill-versioning, eval-release, GSD, ADR, playbook, telemetry, DLQ, notification and
    corrective-action evidence. No source code, runtime, second binder, certification claim or user
    workflow was introduced.
  </what-built>
  <how-to-verify>
    Read `docs/governance/iso-9001-conformance-map.md` as an external skeptical reviewer:

    1. **Claim boundary.** Confirm it cannot reasonably be read as “Pikar is ISO certified,” “this
       product makes customers certified,” or a declaration that the whole company conforms.
    2. **Scope.** Confirm the direct claims are limited to software/product design, governed
       behavior release, and governed service operation—the things the cited evidence actually
       covers.
    3. **No relabelling.** Confirm tests/GSD verification are not called clause 9.2 internal audit,
       owner UAT/activation is not called 9.3 management review, and `requireOwner` is not called a
       quality policy.
    4. **10.2.** Open both corrective-action index chains. Confirm each has a real observation,
       cause, action, effectiveness result and closure, and that the map does not say every DLQ row
       receives that treatment.
    5. **WORM.** Confirm implemented export code is separated from the unperformed real-bucket
       delete-refusal proof unless newer dated evidence was actually produced.
    6. **Process theater.** Confirm the phase added one map and one README link only—no duplicate
       audit, incident register, risk register, quality manual, generated PDF, approval ceremony or
       clause vocabulary in the product UI.
    7. **Genuine gaps.** Confirm organization-wide clauses and live-only residuals are named and
       disposed, not hidden or filled with documents written only to look compliant.
    8. **External use.** Before any marketing/procurement conformity statement, require review
       against a licensed copy of the standard by a qualified ISO professional. This map alone is
       not that review.

    Approve only if the artifact makes the trust moat more truthful without creating parallel
    process. If any row overclaims or understates a control, cite the clause and requested correction.
  </how-to-verify>
  <resume-signal>Type "approved", or list clause-specific corrections.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record review status and run the complete Phase 24 offline gate</name>
  <files>docs/governance/iso-9001-conformance-map.md</files>
  <action>
After owner approval, set the map's `Last reviewed` to the actual review date and record the review
scope in one line. Do not write “ISO approved,” “certified,” or “conformity verified”; the owner
approved the evidence map and claim boundary only.

If Task 3 returned corrections, make the narrow corrections first and rerun Task 2's integrity
checks. Preserve every honest limitation. Then run the full 24-VALIDATION offline gate:

- audit immutability + WORM unit controls;
- skills/eval/rollback controls;
- DLQ/notification controls;
- eval-runner self-check;
- playbook checker in the actual changed tree;
- Task 1/2 map assertions;
- `git diff --check` on the map and README.

Record exact command results in `24-02-SUMMARY.md`, not in the conformance map. If an underlying
control test is red, do not rewrite the map to hide it: mark the affected evidence row current-
failure/partial and record the blocker. If a failure is pre-existing or environmental, name it and
its evidence level. Do not expand into source fixes inside Phase 24 without a separately authorized
gap-closure plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='docs/governance/iso-9001-conformance-map.md',s=fs.readFileSync(p,'utf8'),lines=s.split(/\r?\n/),split=l=>l.trim().replace(/^\||\|$/g,'').split('|').map(x=>x.trim()),table=h=>{const at=lines.findIndex(l=>{const c=split(l);return c.length===h.length&amp;&amp;h.every((v,i)=>c[i].toLowerCase()===v)});if(at===-1)throw Error('missing table: '+h.join(', '));const out=[];for(let i=at+2;i&lt;lines.length&amp;&amp;/^\s*\|/.test(lines[i]);i++)out.push(split(lines[i]));if(out.length===0)throw Error('empty table: '+h[0]);return out},mh=['clause','intent','status','evidence','control','limitation / gap','verification'],ih=['nonconformity / observed effect','containment / immediate correction','cause evidence','corrective action / recurrence prevention','effectiveness evidence','closure / disposition and owner / date'],main=table(mh),idx=table(ih),allowed=new Set(['Direct — defined scope','Supporting','Partial — gap named','Not assessed — organization-wide','Not applicable — justified']);main.forEach((r,i)=>{if(r.length!==7||r.some(x=>!x))throw Error('incomplete matrix row '+(i+1));if(!allowed.has(r[2]))throw Error('invalid status '+r[2])});idx.forEach((r,i)=>{if(r.length!==6||r.some(x=>!x))throw Error('incomplete index row '+(i+1))});for(const c of ['4.1','4.2','4.3','7.5','8.3','8.5.1','8.5.6','8.6','9.1','9.2','9.3','10.2','10.3'])if(!main.some(r=>r[0].includes(c)))throw Error('missing clause '+c);const texts=main.map(r=>r[3]).concat(idx.flat()),paths=[],prose=s.replace(/```[\s\S]*?```/g,''),command=/^(?:node|pnpm|npm|npx|git|rg|vitest|pwsh|powershell)(?:\s|$)/,pathlike=/^(?:\.{0,2}[\/\\]|[^:\s]+[\/\\]|[^:\s]+\.[A-Za-z][A-Za-z0-9]{0,11}(?:#.*)?$)/;for(const m of prose.matchAll(/`([^`\n]+)`/g)){const x=m[1].trim();if(x.startsWith('repo:')){const raw=x.slice(5);if(!raw||/[\s?]/.test(raw)||/^[/\\]/.test(raw)||/^[A-Za-z]:/.test(raw)||raw.includes('\\')||raw.split('/').includes('..'))throw Error('invalid repo reference '+x);if(/:\d+(?::\d+)?(?:#.*)?$/.test(raw))throw Error('line-number pointer '+x);const q=raw.split('#')[0];if(!fs.existsSync(q))throw Error('missing evidence path '+q);paths.push(q)}else if(/^(?:commit|run|symbol):/.test(x)||/^https?:\/\//.test(x)||command.test(x))continue;else if(pathlike.test(x))throw Error('untyped local path; use repo: '+x)}for(const text of texts){if(/`repo:[^`\n]+`/.test(text))continue;const status=main.find(r=>r[3]===text)?.[2];if(status&amp;&amp;!status.startsWith('Not '))throw Error('mapped evidence lacks repo reference: '+text)}if(paths.length===0)throw Error('no repo evidence paths extracted');for(const bad of ['Pikar is ISO certified','customers are ISO certified','fully compliant','100% conformant'])if(s.toLowerCase().includes(bad.toLowerCase()))throw Error('forbidden claim '+bad);console.log('ok: '+main.length+' matrix rows, '+idx.length+' index rows, '+new Set(paths).size+' local paths')"</automated>
    <automated>pnpm --filter @pikar/backend exec vitest run convex/auditImmutability.test.ts convex/worm.test.ts convex/skills.test.ts convex/deadLetters.test.ts convex/notifications.test.ts --maxWorkers=1</automated>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
    <automated>node scripts/check-playbooks.mjs check</automated>
    <automated>git diff --check -- docs/governance/iso-9001-conformance-map.md docs/README.md</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/governance/iso-9001-conformance-map.md','utf8'); if(!/Last reviewed:\s*2026-/.test(s)) throw new Error('owner review not recorded'); for(const x of ['Corrective-action evidence index','not a certificate','conditional','Not assessed — organization-wide']) if(!s.toLowerCase().includes(x.toLowerCase())) throw new Error('missing final boundary '+x); console.log('ok')"</automated>
  </verify>
  <done>The owner-reviewed map passes every offline gate, records only evidence-map approval, and
  leaves any live/company-wide residual visibly conditional or not assessed.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend exec vitest run convex/auditImmutability.test.ts convex/worm.test.ts convex/skills.test.ts convex/deadLetters.test.ts convex/notifications.test.ts --maxWorkers=1`
- `node packages/backend/scripts/run-eval-golden.mjs --self-check`
- `node scripts/check-playbooks.mjs check`
- `git diff --check -- docs/governance/iso-9001-conformance-map.md docs/README.md`
- Every corrective-action chain opens through observation → cause → action → effectiveness → closure.
- Owner checkpoint approved the map's scope/claims, not ISO conformity.
</verification>

<success_criteria>
- Clause 10.2 is evidenced with real corrective-action chains rather than a DLQ status label.
- The final artifact remains one pointer-only map with one README link and no parallel process.
- All required clauses, evidence paths, statuses, limitations and verification methods pass integrity checks.
- The owner confirms the map is scoped, honest, and free of certification/process-theater overclaim.
- GOVN-02 is satisfied only after Plan 01 and this closing plan are complete.
</success_criteria>

<output>
After completion, create
`.planning/phases/24-iso-9001-conformance-map/24-02-SUMMARY.md`, including both corrective-action
chains, owner review scope/date, exact offline results, live/manual residuals, and a statement that
no ISO certification/conformity assessment was performed.
</output>
