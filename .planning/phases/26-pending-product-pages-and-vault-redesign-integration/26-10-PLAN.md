---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 10
type: execute
wave: 7
depends_on: ["26-09"]
files_modified:
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [FIN-01]

# REPLANNED 2026-08-21, against evidence, before any live stack was spent. The previous revision
# asserted "only evidence is missing" and then asked for three things that could not be produced:
# a non-owner state no route mounts, a presentation rollback no runtime switch implements, and a
# browser run whose own command does not filter and whose owner grant had no inverse. What it
# claimed about the IMPLEMENTATION was accurate and is unchanged — the route, projection, controls,
# component tests and nav are landed and correct. Only the EVIDENCE tasks are rewritten.
#
# Closed in pre-flight, already on disk, NOT to be redone by this plan:
#   - owner:revokeOwner (packages/backend/convex/owner.ts) + 6 tests. The grant's missing inverse.
#   - finance.spec.ts: revoke in seedOnboarded + afterAll, so the spec is re-runnable instead of
#     single-use; try/finally on the media kill-switch so a failed assertion cannot leave a
#     deployment-wide switch ON.
#   - authorization.md / dashboard-pages.md / cockpit.md: the inverse, and two false claims about
#     nav state and the resume command corrected in place.

must_haves:
  truths:
    - "The landed /dashboard/finance shell preserves the roadmap's Cost/Cash split: Pikar spend is the USD Cost Console, while business money stays in its separate Business tab."
    - "The Pikar-spend Cost tab never presents income, combined ceilings, or invented history."
    - "Owner controls show returned effective state; non-owners cannot reach global facts."
    - "The non-owner boundary is OBSERVED, by revoking and re-granting a real account — not inferred from the absence of markup. This is newly possible and is the reason the pre-flight existed."
    - "Evidence already banked on 2026-08-09 is CITED, not re-manufactured. The three items that UAT recorded as unobserved are the scope of the human checkpoint, and nothing else."
    - "Nav went live ahead of its own gate. That is recorded as an accepted deviation from Phase 26 SC#7 with the date and the direction that authorised it — never quietly normalised."
  artifacts:
    - path: apps/web/app/(app)/dashboard/finance/page.tsx
      provides: Connected Finance route (landed; not modified by this plan)
    - path: apps/web/e2e/finance.spec.ts
      provides: Authenticated coverage, rail, owner-role and controlled-spend browser evidence
  key_links:
    - from: /dashboard/finance?tab=spend
      to: the landed Cost projection and owner controls
      via: FinanceTabs keeps Pikar spend distinct from Business/Cash and mounts owner-only Operator state only for owners
      pattern: "PikarSpendTab|OperatorTab|finance.summary|finance.spendSeries"
---
<objective>Close the three named-unobserved evidence items on the already-landed Cost Console, using a spec that is now re-runnable, and record the route decision and the SC#7 deviation honestly. No source, projection, control or nav change.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md</execution_context>
<context>@docs/design/mockups/pending-pages.html
@docs/design/BRAND.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-VALIDATION.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-09-SUMMARY.md
@docs/playbooks/authorization.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Execute the authenticated browser gate — with the command that actually filters</name>
  <files>docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>Do not rebuild the route, projection, controls, tests or navigation; they are landed and were re-verified in the 26-10 pre-flight audit. Reconcile the roadmap amendment with product reality explicitly: `/dashboard/finance` is the landed shell, `?tab=spend` is the Cost surface over Pikar spend, and the Business tab is the separate Cash/business-money concern. No duplicate `/dashboard/cost` route is created and no later Cash work is reopened. Treat a renewed requirement for a literal `/dashboard/cost` path as a blocking replanning decision, not an inferred source edit.

STACK PRECONDITIONS, all of which must hold before the run is cited (a run that stops in `auth.setup.ts` is NOT a gate): local `convex dev` running (not `--once`); a PRODUCTION build of the web app on `:3111` (`next dev` OOMs on dashboard pages in this repo); `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` exported for a signed-up, onboarded user.

RUN EXACTLY THIS, from `apps/web`:

    npx playwright test e2e/finance.spec.ts

NOT `pnpm --filter @pikar/web test:e2e -- e2e/finance.spec.ts`. That form is recorded in dashboard-pages.md (~line 648) as swallowing the `--` and queueing all 59 tests; it is why two previous attempts produced ~25 unrelated tenant-precondition failures and no Finance evidence.

KNOWN SPEC WEAKNESSES — record which ones the run actually hit; do NOT silently treat a pass as coverage of them: (a) section 5's `$5.00`/`$1.90` unlanded assertions are window totals and accumulate across runs inside 30 days, so a second run against the same tenant reads doubled figures; (b) section 3's coverage assertion self-skips on any tenant that has ever spent, and permanent `uat-26-10:` rows already exist on the reviewing tenant; (c) the ledger-row dedup assertion assumes the seeded row lands in the first 25-row page; (d) the non-owner ceiling check is a whole-page string scan that can flake on a coincidental figure; (e) the cross-origin `fetch` to `/api/query` is the only one in the suite and is CORS-unproven locally. A FRESH signed-up tenant avoids (a), (b) and (c) and is the recommended target.

Record exact run evidence in both playbooks — the command, the tenant used, per-test results, and controlled/seeded rows versus any separately observed provider event. A seeded `actual` row is an accounting state and is never evidence that money reached a provider.</action>
  <verify><automated>cd apps/web &amp;&amp; npx playwright test e2e/finance.spec.ts</automated></verify>
  <done>The authenticated spec passes against `/dashboard/finance`, proves the Cost/Business separation and the owner boundary, the playbooks name the run and its tenant, and any weakness above that the run did not genuinely exercise is written down as not-covered rather than implied.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The three unobserved UAT items — and only those three</name>
  <files>none</files>
  <action>The 26-10 owner UAT was PERFORMED on 2026-08-09 and is recorded in dashboard-pages.md (rails, Unknown-vs-$0, both unlanded sentences, the blended figure, arm/confirm, effective-state readback, revert, the two source-scan guards). CITE it. Do not re-run what it already covers. That same record names three items as *not observed and not claimed*, and they are the whole of this checkpoint:

(1) THE NON-OWNER VIEW — now reachable for the first time. With `owner:revokeOwner` landed, the owner can observe the boundary from their own account: run `npx convex run owner:revokeOwner` with their own `users._id` from `packages/backend`, reload `/dashboard/finance`, and confirm there is NO Operator tab and NOT ONE of `$50.00`/`$100.00`/`$250.00` anywhere in the page source; then `owner:bootstrapOwner` to restore. Verify what EXISTS: absence of the Operator tab and of every deployment ceiling. Do NOT look for the "managed by the operator" copy — `FinanceTabs` mounts `OperatorTab` only for an owner, so `DeploymentSection`'s non-owner branch is unreachable markup in the shipped product. Asking for it was a defect in the previous revision of this plan.

(2) RESPONSIVE AND KEYBOARD at desktop / tablet / mobile, confirming Business/Cash and Pikar-spend Cost stay visibly distinct at every width.

(3) The run from Task 1, reviewed rather than merely green: compare the live rails against the operator reads, and inspect one estimate / reserve / actual / refund end to end.

NOT IN SCOPE, and the reason is recorded rather than worked around: the previous revision asked to "demonstrate that presentation can be disabled while ledger writes continue." There is NO runtime presentation switch. The only rollback is deleting the nav `href`, which makes the route undiscoverable but still URL-reachable, and requires a source edit plus a rebuild — which Task 3 forbids. The two switches actually reachable from this page are SPEND kill switches with all-tenant blast radius; flipping one would halt every tenant's model calls and produce NO ledger movements, the exact inverse of the property. Verify the rollback by INSPECTION (the nav entry, and that the ledger writers do not import the page), and carry the gap forward as a deferred item.

Owner approval covers the existing `/dashboard/finance` route resolution. If the owner instead requires a literal `/dashboard/cost`, stop for a separately planned migration.</action>
  <verify><manual>Owner types approved or reports exact defects.</manual></verify>
  <done>The three named items are observed or explicitly declined, the non-owner state was seen rather than inferred, and the rollback gap is recorded instead of faked.</done>
</task>
<task type="auto">
  <name>Task 3: Close the checkpoint, and record the SC#7 deviation as a deviation</name>
  <files>docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>After Task 2 approval, leave the landed Finance navigation and source untouched. Record in both playbooks: the authenticated run, the owner approval, the `/dashboard/finance` shell resolution, and the rollback gap from Task 2.

Then write 26-10-SUMMARY.md, and it must state three things the record does not currently carry:
  1. Phase 26 SC#7 requires authenticated responsive UAT BEFORE a nav item goes live. The Finance href was added on 2026-08-09 on owner direction, with the browser run and three UAT items outstanding. That is an accepted deviation with a date and an authoriser — name it as such. It is also why 26-18 (wave 14) closed while 26-10 (wave 7) did not.
  2. What the pre-flight changed and why (`revokeOwner`, the spec's re-runnability, the kill-switch `try`/`finally`), so the next reader does not re-derive it.
  3. Which of the spec weaknesses listed in Task 1 the run did and did not exercise.

Also update 26-VALIDATION.md: 26-10's row, and its stale command (it still quotes the non-filtering `test:e2e -- ` form). 26-06/26-07/26-08 are recorded there as pending too — note them; do not silently close them.

Do not claim a new provider charge unless separately observed.</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>26-10-SUMMARY.md exists and records closure, the SC#7 deviation and the deferred rollback gap, without duplicate implementation work.</done>
</task>
</tasks>
<verification>- Existing component/typecheck/build evidence is preserved; this plan runs the missing authenticated Finance E2E and the three-item blocking UAT only.
- `node scripts/check-playbooks.mjs` is a Stop-hook contract that exits 0 on the block path too, so it CANNOT fail a `&amp;&amp;` chain. It is a reminder here, not a gate. The real gates are the Playwright run and the owner checkpoint.</verification>
