---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 10
type: execute
wave: 7
depends_on: ["26-09"]
files_modified:
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [FIN-01]

must_haves:
  truths:
    - "The landed /dashboard/finance shell preserves the roadmap's Cost/Cash split: Pikar spend is the USD Cost Console, while business money stays in its separate Business tab."
    - "The Pikar-spend Cost tab never presents income, combined ceilings, or invented history."
    - "Owner controls show returned effective state; non-owners cannot reach global facts."
    - "Already-landed navigation is accepted only after the missing authenticated browser run and owner checkpoint are recorded; failure rolls presentation back without stopping instrumentation."
  artifacts:
    - path: apps/web/app/(app)/dashboard/finance/page.tsx
      provides: Connected Finance route
    - path: apps/web/e2e/finance.spec.ts
      provides: Existing authenticated coverage, rail, owner-role and controlled-spend browser evidence
  key_links:
    - from: /dashboard/finance?tab=spend
      to: the landed Cost projection and owner controls
      via: FinanceTabs keeps Pikar spend distinct from Business/Cash and mounts owner-only Operator state only for owners
      pattern: "PikarSpendTab|OperatorTab|finance.summary|finance.spendSeries"
---
<objective>Resume the already-landed Cost Console, reconcile its route/surface boundary, and close only the missing authenticated browser, owner and summary evidence.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md</execution_context>
<context>@docs/design/mockups/pending-pages.html
@docs/design/BRAND.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-VALIDATION.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-09-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Reconcile the landed shell and execute the missing authenticated browser gate</name>
  <files>docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>Resume from the landed 26-10 implementation; do not rebuild the route, projection, controls, tests or navigation. Reconcile the roadmap amendment with current product reality explicitly: `/dashboard/finance` is the landed shell, `?tab=spend` is the Cost surface over Pikar spend, and the Business tab is the separate Cash/business-money concern. This preserves the no-fabrication semantic split without creating a duplicate `/dashboard/cost` route or reopening later Cash work. Confirm the existing component suite is `financeView.test.ts` (not `.tsx`) and treat any failure of that split—or a renewed requirement for a literal `/dashboard/cost` path—as a blocking replanning decision, not an inferred source edit. With the authenticated local stack and credentials available, execute only the missing `e2e/finance.spec.ts` gate. Record exact run evidence, including controlled rows versus any separately observed provider event, in both playbooks.</action>
  <verify><automated>pnpm --filter @pikar/web test:e2e -- e2e/finance.spec.ts &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>The existing authenticated spec passes against `/dashboard/finance`, proves the Cost/Business separation and owner boundary, and the playbooks no longer describe the browser gate as pending.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Finance owner/non-owner UAT and instrumentation-safe rollback</name>
  <files>none</files>
  <action>Verify the already-active `/dashboard/finance` shell at desktop/tablet/mobile and keyboard states; confirm Business/Cash and Pikar-spend Cost remain visibly distinct; compare live rails with operator reads; inspect one estimate/reserve/actual/refund; confirm control warning/effective state as owner and managed-by-operator state as non-owner. Distinguish seeded terminal rows from separately executed live-provider evidence. Demonstrate that presentation can be disabled while ledger writes continue. Owner approval covers the existing route resolution; if the owner instead requires a literal `/dashboard/cost`, stop for a separately planned migration.</action>
  <verify><manual>Owner types approved or reports exact defects.</manual></verify>
  <done>Truthful UI, role boundary and no-history-hole rollback are approved.</done>
</task>
<task type="auto">
  <name>Task 3: Close the resumed checkpoint and summary without reopening source</name>
  <files>docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>After Task 2 approval, leave the already-landed Finance navigation and source untouched. Record the authenticated run, owner approval, `/dashboard/finance` shell resolution, and instrumentation-safe presentation rollback in both playbooks, then create the required summary with the prior implementation commits and the newly closed evidence. Do not claim a new provider charge unless separately observed.</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>The existing navigation has complete browser/human evidence, the route decision is explicit, and 26-10-SUMMARY.md records closure without duplicate implementation work.</done>
</task>
</tasks>
<verification>- Existing component/typecheck/build evidence is preserved; this resume runs the missing authenticated Finance E2E and blocking UAT only.</verification>
<success_criteria>- FIN-01 is connected without fabricated accounting.
- Instrumentation survives UI rollback.</success_criteria>
<output>Create `26-10-SUMMARY.md` only after approval.</output>
