---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 13.1
type: execute
wave: 10.1
depends_on: ["26-13"]
files_modified:
  - packages/backend/convex/content.ts
  - packages/backend/convex/content.test.ts
  - apps/web/app/(app)/dashboard/content/ContentView.tsx
  - apps/web/app/(app)/dashboard/content/contentView.test.ts
  - apps/web/e2e/content.spec.ts
  - packages/backend/convex/smoke.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "A standalone generated image is a finished artifact and appears on the shelf; a reel's scene image never does, because it never becomes a vault document at all."
    - "The shelf's kind whitelist stays a POSITIVE list, and each entry's justification is checked against the WRITE SITE, not the field name."
    - "Search narrows the same bounded union the grid already reads; it never becomes an unbounded scan and never silently drops the cap."
    - "A thumbnail is minted on demand for the rows the user can see, never eagerly for the whole shelf."
  artifacts:
    - path: packages/backend/convex/content.ts
      provides: the image lane in the union, its counts, and a bounded title filter
    - path: apps/web/app/(app)/dashboard/content/ContentView.tsx
      provides: the Images chip, the search box and image thumbnails
  key_links:
    - from: mediaComplete.saveImageToVault
      to: the Content shelf
      via: kind "image" as a shelf lane
      pattern: "LANE_BY_KIND|image"
---
<objective>Put the standalone images Pikar makes back on the shelf of things Pikar made, and make the shelf searchable.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-12-SUMMARY.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-13-SUMMARY.md
@packages/backend/convex/mediaComplete.ts
@packages/backend/convex/content.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: The image lane — a repair, and the false comment goes with it</name>
  <files>packages/backend/convex/content.test.ts, packages/backend/convex/content.ts</files>
  <action>Add `image` to `LANE_BY_KIND` as its own lane and to `KINDS_BY_LANE`, and DELETE the comment claiming image rows are media intermediates — it is false and it is what caused the exclusion. Replace it with the checked fact: `mediaComplete.saveImageToVault` is scoped to `plans.mediaMode === "image"` and is `saveReelToVault`'s twin, while a reel's scene images are deleted by `deleteIntermediates` and never become vault documents, so a `kind:"image"` vault row is ALWAYS a standalone deliverable. Images carry `storageId` + `storedMimeType` (the real image mime) and `sourcePlanId`/`sourceThreadId` from 26-11, so `bytes`, `provenance` and `reuse` need no new code; `promotion` must report `not-applicable` (an image has no `origin`, and `saveImageToVault` already starts its own ingest). Test: an image row appears in `all` and under `lane:"image"`, counts split correctly, a reel's `mediaJobs` intermediate produces NO shelf row, and the existing lanes are unchanged. Keep the mutation check honest — dropping `image` from the whitelist must redden a named test.</action>
  <verify><automated>pnpm --filter @pikar/backend test content</automated></verify>
  <done>A standalone image is on the shelf, a scene image is not, and no comment in the module asserts something its cited module does not say.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded title search</name>
  <files>packages/backend/convex/content.test.ts, packages/backend/convex/content.ts</files>
  <action>Add an optional `query` arg to `listArtifacts` that filters on title, case-insensitively, OVER THE SAME bounded per-kind window the page already reads — never a new scan and never a `.collect()`. The cap does not disappear because a filter was applied: a search that discards rows must still report `partial` with `row-cap` when the window was full, or the page will claim "no results" for a shelf that simply has more pages. Test the empty result, the capped-window result, that search composes with the lane filter, and that a query never widens what a tenant can see.</action>
  <verify><automated>pnpm --filter @pikar/backend test content</automated></verify>
  <done>Search narrows the union without lying about how much of it was looked at.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The Images chip, the search box and thumbnails</name>
  <files>apps/web/app/(app)/dashboard/content/ContentView.tsx, apps/web/app/(app)/dashboard/content/contentView.test.ts, apps/web/e2e/content.spec.ts, packages/backend/convex/smoke.ts, docs/playbooks/dashboard-pages.md, docs/playbooks/agent-runtime.md</files>
  <action>Add the Images chip to `LaneFilters` and a debounced search input that resets the cursor (a cursor from one query means nothing under another — the same rule the lane filter already follows). Render a thumbnail for an image card ONLY, minted on demand through `api.vault.vaultDownloadUrl` for the rows currently on screen — never a subscription per card for the whole shelf, which is the property `contentView.test.ts` already scans for and which must stay true. An image opens in the same `PreviewModal` as a document (its render branch already handles image mimes). Extend `smoke.seedContentShelf` with a standalone image fixture and `e2e/content.spec.ts` with: the image renders and opens, search narrows the grid, and a cleared search restores it. Update both playbooks.</action>
  <verify><automated>pnpm --filter @pikar/web test content &amp;&amp; pnpm --filter @pikar/web typecheck &amp;&amp; pnpm --filter @pikar/web build</automated>then, from `apps/web`, `npx playwright test e2e/content.spec.ts --project=chromium`</verify>
  <done>The shelf shows both things Pikar makes, and you can find one by name.</done>
</task>
</tasks>
<verification>- Backend + web focused suites, web typecheck, prod build, executed browser gate, watcher.</verification>
<success_criteria>- A standalone generated image is reachable from the Content library.
- No shelf-membership decision rests on a claim its cited module does not make.</success_criteria>
<output>Create `26-13.1-SUMMARY.md`.</output>
