---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 19
type: execute
wave: 15
depends_on: ["26-18"]
files_modified:
  - packages/core/src/home.ts
  - packages/core/src/home.test.ts
  - packages/core/src/index.ts
  - packages/backend/convex/home.ts
  - packages/backend/convex/home.test.ts
  - packages/backend/convex/briefings.ts
  - packages/backend/convex/briefings.test.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [HOME-01]

must_haves:
  truths:
    - "Recommendation priority is deterministic, code-owned and contains no interpolated user content."
    - "Home composes stable source summaries rather than rereading raw unbounded tables."
    - "Home composes the bounded Phase 19 Pipeline summary and preserves its loading/error/partial semantics."
    - "Any failed required health signal produces Unknown, never Healthy."
  artifacts:
    - path: packages/core/src/home.ts
      provides: Pure priority and health-state functions
    - path: packages/backend/convex/home.ts
      provides: Bounded summary and health projections
    - path: packages/backend/convex/briefings.ts
      provides: Latest tenant briefing projection
  key_links:
    - from: home summary
      to: approvals/finance/content/reports and Phase 19 Pipeline source-summary helpers
      via: composed narrow facts with dedupe definitions
      pattern: "priority|unknown|latestForTenant"
    - from: home.summary pipeline section
      to: Phase 19 bounded Pipeline summary
      via: narrow contacts-needing-attention/follow-ups/consent/suppression counts with source status intact
      pattern: "pipelineSummary|partial|unavailable"
---
<objective>Build the deterministic Command Center backend only after all source surfaces are stable.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@apps/web/app/(app)/dashboard/page.tsx
@packages/backend/convex/briefings.ts
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-18-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pin the priority total order and health lattice</name>
  <files>packages/core/src/home.test.ts, packages/core/src/home.ts, packages/core/src/index.ts</files>
  <action>Test the locked order: connection failure, severe unresolved DLQ, stale approval, scheduled risk, diagnostic blocker, binding constraint, workspace. Each result has code-owned label/reason/route/supporting timestamp/count only. Test ties and no-signal fallback. Implement health as healthy only when every required loaded signal is healthy; loading/error/partial becomes Unknown.</action>
  <verify><automated>pnpm --filter @pikar/core test -- home</automated></verify>
  <done>Clearing each blocker deterministically reveals the next and a failed source can never return healthy.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Compose summaries, health and latest briefing</name>
  <files>packages/backend/convex/home.test.ts, packages/backend/convex/home.ts, packages/backend/convex/briefings.test.ts, packages/backend/convex/briefings.ts, docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>Build separate summary and health subscriptions from narrow source-summary helpers, with explicit delivered/artifact definitions and no legacy/cockpit double count. Compose the bounded Phase 19 Pipeline summary established by the plan-18 gate: contacts needing attention, follow-ups due, consent/suppression counts and coverage only—no opportunities/stages/value and no raw contact data. Preserve Pipeline loading/unavailable/error/partial/capped status in home.summary; a Pipeline failure must not silently become zero or erase other sections. Add briefings.latestForTenant using the tenant/time index and bounded projection. Briefing affordances return workspace links only—no silent writes or action-sounding promises. Test auth/isolation, Pipeline ready-to-partial-to-error transitions, source failure, partial coverage, owner/non-owner global health and stable recency. Update both watched playbooks.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- home briefings &amp;&amp; pnpm --filter @pikar/core typecheck &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Home queries are bounded and source-composed; Pipeline status survives composition without fake zeros; health failures remain Unknown.</done>
</task>
</tasks>
<verification>- Core home and backend home/briefing suites, typechecks, watcher.
- Mutation check: fail one health source and assert Unknown.</verification>
<success_criteria>- HOME-01 backend has one deterministic definition of priority/health.
- Existing raw `requests.list` home reads are no longer needed by v2.</success_criteria>
<output>Create `26-19-SUMMARY.md`.</output>
