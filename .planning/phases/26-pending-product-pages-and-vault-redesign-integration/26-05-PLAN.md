---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 05
type: execute
wave: 3
depends_on: ["26-03", "26-04"]
files_modified:
  - apps/web/app/(app)/dashboard/approvals/page.tsx
  - apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx
  - apps/web/app/(app)/dashboard/approvals/approvalsView.test.tsx
  - apps/web/app/(app)/layout.tsx
  - apps/web/e2e/approvals.spec.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [APRV-01]

must_haves:
  truths:
    - "The nav badge and page use the same live summary and the route is enabled only after connected gates pass."
    - "Every promised button reaches its guarded terminal or the originating cockpit thread."
    - "Loading, empty, partial, error, busy, stale and refusal states are visible and accessible."
  artifacts:
    - path: apps/web/app/(app)/dashboard/approvals/page.tsx
      provides: Connected Approvals composition
    - path: apps/web/e2e/approvals.spec.ts
      provides: Authenticated email/media/calendar/memo and race browser evidence
  key_links:
    - from: Approvals page and rail badge
      to: api.approvals.summary
      via: one shared Convex subscription
      pattern: "approvals.summary"
---

<objective>Ship and owner-verify the real Approvals route, then activate its navigation entry.</objective>
<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>
<context>
@docs/design/mockups/pending-pages.html
@docs/design/BRAND.md
@apps/web/app/(app)/layout.tsx
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-VALIDATION.md
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-03-SUMMARY.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-04-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build all connected Approvals states and execute browser coverage</name>
  <files>apps/web/app/(app)/dashboard/approvals/page.tsx, apps/web/app/(app)/dashboard/approvals/ApprovalsView.tsx, apps/web/app/(app)/dashboard/approvals/approvalsView.test.tsx, apps/web/e2e/approvals.spec.ts, docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>Implement the BRAND-aligned route without copying static mock values, while keeping the rail item disabled/Soon; the hidden route remains directly reachable for automated and owner UAT. Use absolute timestamps with browser IANA fallback, four real Awaiting kinds, bounded scheduled/in-flight/decision/cleared lanes, on-demand attachment access, and cockpit deep links for revise/change-time. Add destructive confirmation, busy/refusal/stale results, retry and partial copy. Explicitly wire initial scheduling: resolve the browser/tenant IANA timezone into an absolute instant, show that absolute timezone-resolved time for confirmation, call `setPlanSendTime`, then execute only after confirmation. Browser-test scheduling replay, stale status, and invalid/past time. Wire Scheduled Cancel to the existing guarded `cancelScheduledPlan` terminal; browser-test it as idempotent, prove it prevents duplicate delivery, and prove a lost scheduler race reports already fired/in-flight instead of canceled. Also cover one email/media/calendar/memo, double approve, discard non-rearm, and schedule move/fire race. Do not fake Gmail, calendar, media, or other provider success: seeded terminal rows prove UI states only; any external-success claim requires separately executed live evidence recorded in the summary. Update both playbooks because E2E is cockpit-watched.</action>
  <verify><automated>pnpm --filter @pikar/web test -- approvals &amp;&amp; pnpm --filter @pikar/web typecheck &amp;&amp; pnpm --filter @pikar/web build &amp;&amp; pnpm --filter @pikar/web test:e2e -- e2e/approvals.spec.ts &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>The focused browser spec actually runs; no hard-coded count/date/cost/timezone remains; initial scheduling and cancellation/races are proven; nav is still disabled pending owner approval.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Authenticated Approvals UAT and rollback confirmation</name>
  <files>none</files>
  <action>Present the directly reachable but nav-hidden `/dashboard/approvals` at desktop/tablet/mobile. Ask the owner to exercise keyboard/focus, loading/empty/error/partial/refusal, all four plan kinds, double approve, absolute-timezone scheduling confirmation, idempotent scheduled cancel, lost scheduler race, discard, schedule move/race, decision save, Ops link and cockpit deep links. Distinguish seeded terminal-state evidence from any separately executed live-provider result. Demonstrate rollback while workspace, `/review` and `/requests` remain reachable.</action>
  <verify><manual>Owner types approved or reports exact defects.</manual></verify>
  <done>The connected route and its reversible fallback are owner-approved.</done>
</task>
<task type="auto">
  <name>Task 3: Enable Approvals navigation after owner approval</name>
  <files>apps/web/app/(app)/layout.tsx, docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>Only after Task 2 approval, replace the disabled Approvals item with the live route and badge wired to the same `approvals.summary` subscription. Record the approval/evidence and route-disable rollback in both playbooks.</action>
  <verify><automated>pnpm --filter @pikar/web typecheck &amp;&amp; pnpm --filter @pikar/web build &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Navigation activates post-approval, the production build is green, and the page/badge share one source.</done>
</task>
</tasks>
<verification>- Focused web/component tests, production build and executed Playwright spec.
- Blocking authenticated owner UAT.</verification>
<success_criteria>- APRV-01 is demonstrated by real backend data.
- Approvals can be disabled without reverting additive state.</success_criteria>
<output>Create `26-05-SUMMARY.md` only after approval.</output>
