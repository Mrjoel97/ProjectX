---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 14
type: execute
wave: 11
depends_on: ["26-13"]
files_modified:
  - packages/core/src/reports.ts
  - packages/core/src/reports.test.ts
  - packages/core/src/index.ts
  - packages/backend/convex/reportsBusiness.ts
  - packages/backend/convex/reportsBusiness.test.ts
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [RPRT-01]

must_haves:
  truths:
    - "All business/operations cards share one resolved window and timezone."
    - "Only comparable Growth-OS snapshots produce movement; actioning a gap alone never closes it."
    - "Metrics name their population and disclose missing/incomplete rows."
  artifacts:
    - path: packages/core/src/reports.ts
      provides: Pure comparison, completeness, percentile and coverage semantics
    - path: packages/backend/convex/reportsBusiness.ts
      provides: Bounded tenant business and operations projections
  key_links:
    - from: reportsBusiness.ts
      to: evaluations/blueprint/vault/telemetry/feedback indexes
      via: one resolved DashboardWindow passed to every projection
      pattern: "sinceMs|untilMs|timezone"
---
<objective>Define truthful report metrics and the bounded business/operations read plane.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-RESEARCH.md
@packages/backend/convex/evaluations.ts
@packages/backend/convex/opsSignals.ts
@packages/backend/convex/blueprint.ts
@packages/backend/convex/feedback.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pin report comparison and completeness semantics</name>
  <files>packages/core/src/reports.test.ts, packages/core/src/reports.ts, packages/core/src/index.ts</files>
  <action>Test latest comparable Growth-OS selection, stable gap identity closure, absent deltas, blueprint completeness, coverage labels, named approval numerator/denominator, median/p95 exclusion counts and half-open buckets. Implement pure functions only; never count object keys ad hoc or merge unlike frameworks.</action>
  <verify><automated>pnpm --filter @pikar/core test -- reports</automated></verify>
  <done>Mutation of framework comparability or gap identity makes focused tests red.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build bounded business and operations projections</name>
  <files>packages/backend/convex/reportsBusiness.test.ts, packages/backend/convex/reportsBusiness.ts, docs/playbooks/dashboard-pages.md</files>
  <action>Implement separate business and operations queries over bounded evaluation, blueprint, vault, telemetry, feedback and source summaries. Server-clamp 7/30/90-day absolute windows; return the resolved window on every result. Findings/prose remain on-demand content-plane detail. Mark incomplete reasoning-cost/review populations instead of relabeling legacy metrics. Update playbook and tests for auth/isolation/caps.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- reportsBusiness &amp;&amp; pnpm --filter @pikar/core typecheck &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Every card is window-consistent, bounded and explicit about incomplete populations.</done>
</task>
</tasks>
<verification>- Core reports and backend reportsBusiness suites, typechecks, watcher.</verification>
<success_criteria>- RPRT-01 business/operations semantics are deterministic.
- No fabricated semantic Vault coverage is emitted.</success_criteria>
<output>Create `26-14-SUMMARY.md`.</output>

