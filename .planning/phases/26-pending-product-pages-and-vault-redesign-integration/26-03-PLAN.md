---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/plans.test.ts
  - packages/backend/convex/deliverApprovedPlan.ts
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [APRV-01]

must_haves:
  truths:
    - "Discard is an idempotent proposed-to-canceled transition that can never be re-armed."
    - "Moving a live schedule returns moved, already_fired, or not_scheduled and never lies after the scheduler wins."
    - "Email progress counters are terminal-owned and legacy progress remains explicitly partial."
  artifacts:
    - path: packages/backend/convex/cockpit.ts
      provides: discardPlan and atomic moveScheduledPlan transitions
    - path: packages/backend/convex/plans.ts
      provides: cancellation provenance and progress transition helpers
  key_links:
    - from: cockpit state transitions
      to: plans cancellation/progress fields
      via: same-transaction status guards and refs-only audit
      pattern: "discarded|already_fired|counterComplete"
---

<objective>Complete the missing Approvals write semantics without weakening the existing execute gate.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-CONTEXT.md
@docs/playbooks/cockpit.md
@packages/backend/convex/cockpit.ts
@packages/backend/convex/plans.ts
@packages/backend/convex/deliverApprovedPlan.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add discard and true schedule-move CAS transitions</name>
  <files>packages/backend/convex/cockpit.test.ts, packages/backend/convex/plans.test.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/plans.ts</files>
  <action>Write red tests for double discard, foreign IDs, discard after start, attempted re-arm of a discarded row, move after scheduler fire, cancel/move race and replay. Implement `discardPlan` with `cancelKind:"discarded"` and refs-only `plan.discarded`; implement one atomic current-schedule move returning the locked discriminated result. Preserve legacy canceled-row reopen only for scheduled cancellations and never treat missing provenance on newly written rows as discarded.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- cockpit plans</automated></verify>
  <done>Removing the provenance or status guard makes the focused race test red; replay produces no duplicate transition/audit.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Maintain bounded delivery progress and document rollback</name>
  <files>packages/backend/convex/deliverApprovedPlan.ts, packages/backend/convex/cockpit.test.ts, packages/backend/convex/plans.test.ts, docs/playbooks/cockpit.md</files>
  <action>Update the existing delivery seed/terminal path so recipient total, queued, sent and failed counters move exactly once under retries. Mark new plans counter-complete; legacy rows remain partial and use only the bounded fallback later. Update `cockpit.md` with transition table, scheduler-race result, discard non-rearm invariant, focused commands, and rollback: hide Approvals while retaining workspace, `/review`, `/requests`, and stored provenance.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- cockpit plans &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Counters cannot exceed recipient total, retry does not double-count, and the watched playbook is current.</done>
</task>
</tasks>
<verification>
- `pnpm --filter @pikar/backend test -- cockpit plans`
- Mutation check: allow discarded re-arm and observe the provenance test fail.
- `pnpm --filter @pikar/backend typecheck && node scripts/check-playbooks.mjs`
</verification>
<success_criteria>
- Approvals controls have guarded idempotent terminals.
- Existing execute/schedule behavior remains backward-compatible.
</success_criteria>
<output>Create `26-03-SUMMARY.md`.</output>

