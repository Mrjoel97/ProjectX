---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/schema.ts
  - packages/backend/convex/dashboardSchema.test.ts
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "All Phase 26 schema changes are additive, indexed, and owned by this plan alone."
    - "Legacy rows remain readable as explicit legacy/unknown/partial states."
    - "No historical spend, provenance, or progress is fabricated by a migration."
  artifacts:
    - path: packages/backend/convex/schema.ts
      provides: Additive indexes and optional fields needed by all connected pages
    - path: packages/backend/convex/dashboardSchema.test.ts
      provides: Schema/index/optional-field regression contract
  key_links:
    - from: packages/backend/convex/schema.ts
      to: approvals, finance, content, reports, and home adapters
      via: compound tenant/time indexes and optional legacy-compatible fields
      pattern: "by_tenant_status_createdAt|spendEvents|by_tenant_createdAt"
---

<objective>
Land the one additive schema foundation before page readers and writers diverge.

Output: all required Phase 26 indexes/tables/optional fields, with zero destructive narrowing or invented backfill.
</objective>

<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-RESEARCH.md
@packages/backend/AGENTS.md
@packages/backend/convex/_generated/ai/guidelines.md
@packages/backend/convex/schema.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pin the additive schema contract</name>
  <files>packages/backend/convex/dashboardSchema.test.ts, packages/backend/convex/schema.ts</files>
  <action>Test and add: `plans.by_tenant_status_createdAt`; optional `cancelKind`, `canceledAt`, recipient counters and counter-completeness; append-only `spendEvents` with tenant/time, tenant/rail/time and correlation indexes plus a durable coverage-start record; `mediaJobs.by_tenant_createdAt`; optional Vault source thread/plan provenance and tenant/origin/time index; bounded request, briefing and feedback tenant/time indexes. Add report snapshot metadata only if an existing table cannot represent one immutable as-of/window snapshot without mutable joins. Keep all legacy fields optional and do not write a backfill.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- dashboardSchema</automated></verify>
  <done>Every new field/index is asserted, existing legacy fixtures still validate, and the diff contains no removal or required historical field.</done>
</task>
<task type="auto">
  <name>Task 2: Close schema static gates</name>
  <files>packages/backend/convex/schema.ts, packages/backend/convex/dashboardSchema.test.ts</files>
  <action>Run backend typecheck and focused schema tests. Inspect the diff for one-table-per-fact discipline, tenant-prefixed indexes, no raw content in spend/snapshot metadata, and no schema edit outside this plan.</action>
  <verify><automated>pnpm --filter @pikar/backend typecheck &amp;&amp; pnpm --filter @pikar/backend test -- schema dashboardSchema</automated></verify>
  <done>The additive schema compiles and all existing schema contracts remain green.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- schema dashboardSchema`
- `pnpm --filter @pikar/backend typecheck`
</verification>
<success_criteria>
- All later plans can depend on stable tenant/time indexes.
- Rollback is consumer disablement; schema rows/indexes need no rollback.
</success_criteria>
<output>Create `26-02-SUMMARY.md`.</output>

