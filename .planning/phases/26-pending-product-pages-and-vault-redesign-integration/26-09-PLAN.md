---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 09
type: execute
wave: 6
depends_on: ["26-07", "26-08"]
files_modified:
  - packages/backend/convex/finance.ts
  - packages/backend/convex/finance.test.ts
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [FIN-01]

must_haves:
  truths:
    - "Tenant summaries expose personal rails and tracked spend without exposing deployment-global state."
    - "Global ceilings, kill switches and per-request budgets are owner-gated server-side and shown separately."
    - "Series and job ledgers are bounded, timezone-resolved, and coverage-aware."
  artifacts:
    - path: packages/backend/convex/finance.ts
      provides: Tenant Finance projections and owner-only control adapters
    - path: packages/backend/convex/finance.test.ts
      provides: Isolation, owner rejection, clamp, control-audit and coverage tests
  key_links:
    - from: finance summaries/series
      to: limiter reads and spendEvents indexes
      via: separate current-enforcement and historical-reporting projections
      pattern: "ownerQuery|ownerMutation|coverageStartedAt"
---
<objective>Expose the truthful Cost Console backend after all three rails are instrumented.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-07-SUMMARY.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-08-SUMMARY.md
@packages/backend/convex/lib/functions.ts
@packages/backend/convex/owner.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build tenant Finance summaries, series and ledger</name>
  <files>packages/backend/convex/finance.test.ts, packages/backend/convex/finance.ts</files>
  <action>Test auth, foreign rows, period clamps, cursor ties, no-event coverage, mixed rail phases and unlanded media. Implement separate summary, spendSeries and mediaLedger projections. Return current remaining tenant rails from enforcement helpers and historical movements from the ledger; label reset UTC separately from browser/chart timezone. Never combine deployment ceilings or fabricate pre-coverage spend.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- finance</automated></verify>
  <done>All reads are tenant-indexed/bounded and unknown history cannot become `$0`.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add owner-only global controls</name>
  <files>packages/backend/convex/finance.test.ts, packages/backend/convex/finance.ts, docs/playbooks/dashboard-pages.md</files>
  <action>Add owner query/mutation adapters for each distinct deployment rail, master/media kill switches and bounded per-request budget. Reject non-owners directly, validate numeric ranges, return effective stored state, require confirmation in the UI contract, and audit old/new boolean/numeric values only. Record operator blast radius and rollback in the playbook.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- finance owner &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Direct non-owner calls fail and every accepted control change has one safe audit row.</done>
</task>
</tasks>
<verification>- Finance and owner focused tests; backend typecheck; watcher.</verification>
<success_criteria>- FIN-01 backend tells current and historical truth separately.
- Global controls are not presentation-gated only.</success_criteria>
<output>Create `26-09-SUMMARY.md`.</output>

