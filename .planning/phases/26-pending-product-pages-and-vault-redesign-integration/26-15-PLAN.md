---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 15
type: execute
wave: 11
depends_on: ["26-13"]
files_modified:
  - packages/contracts/src/auditProjection.ts
  - packages/contracts/src/auditProjection.test.ts
  - packages/contracts/src/index.ts
  - packages/backend/convex/reportsGovernance.ts
  - packages/backend/convex/reportsGovernance.test.ts
  - docs/playbooks/audit-dead-letter.md
autonomous: true
requirements: [RPRT-01]

must_haves:
  truths:
    - "Raw audit.payload and raw actor IDs never cross the browser boundary."
    - "Unknown event types return safe shells; nested/content-like values are dropped before serialization."
    - "WORM cursor and active skill metadata are owner-only and never include prompt bodies."
  artifacts:
    - path: packages/contracts/src/auditProjection.ts
      provides: Allowlisted flat audit viewer contract and sanitizer
    - path: packages/backend/convex/reportsGovernance.ts
      provides: Paginated tenant governance and owner-only operational projections
  key_links:
    - from: reportsGovernance query
      to: audit.by_tenant_ts
      via: sanitize-before-return projection
      pattern: "sanitize|unknown|ownerQuery"
---
<objective>Build the safe governance viewer boundary independently of report metric work.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@packages/contracts/src/audit.ts
@packages/backend/convex/audit.ts
@packages/backend/convex/wormCursor.ts
@packages/backend/convex/skills.ts
@docs/playbooks/audit-dead-letter.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Inventory and sanitize audit viewer events</name>
  <files>packages/contracts/src/auditProjection.test.ts, packages/contracts/src/auditProjection.ts, packages/contracts/src/index.ts</files>
  <action>Inventory production event names and define allowlisted safe keys per known event. Test nested objects, arrays of objects, addresses, message/body/prompt-like keys, oversized strings and unknown event types. Return timestamp, normalized type/category/actor, correlation ref and flat primitive/string-array refs only. Unknown events expose an event shell with zero payload detail. Never stringify raw payload as a fallback.</action>
  <verify><automated>pnpm --filter @pikar/contracts test -- auditProjection</automated></verify>
  <done>Injected unsafe content cannot appear in the projected JSON or snapshots.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expose tenant governance and owner-only metadata</name>
  <files>packages/backend/convex/reportsGovernance.test.ts, packages/backend/convex/reportsGovernance.ts, docs/playbooks/audit-dead-letter.md</files>
  <action>Implement cursor-paginated tenant audit reads that sanitize before serialization. Normalize tenant actor to `you` and system to fixed labels. Add owner-only WORM cursor metadata labeled last cursor advance—not durability—and active skill name/version/status/gated metadata without bodies. Test direct non-owner rejection and foreign rows. Log only a code-owned invariant signal when sanitization drops unsafe data.</action>
  <verify><automated>pnpm --filter @pikar/contracts test -- auditProjection &amp;&amp; pnpm --filter @pikar/backend test -- reportsGovernance audit worm skills &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Governance rows reveal no content and global metadata remains owner-gated.</done>
</task>
</tasks>
<verification>- Unsafe-payload injection suite, governance/owner tests, typecheck, watcher.</verification>
<success_criteria>- RPRT-01 has a server-safe governance boundary.
- The mock's false schema-safety and WORM-health claims are corrected.</success_criteria>
<output>Create `26-15-SUMMARY.md`.</output>
