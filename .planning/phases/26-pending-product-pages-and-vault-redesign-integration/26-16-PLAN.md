---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 16
type: execute
wave: 12
depends_on: ["26-14", "26-15"]
files_modified:
  - packages/backend/convex/reportPack.ts
  - packages/backend/convex/reportPack.test.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/vault.md
autonomous: true
requirements: [RPRT-01]

must_haves:
  truths:
    - "A board pack is rendered from one immutable as-of/window snapshot, never mixed reactive browser reads."
    - "The generated PDF is tenant-owned, carries coverage caveats, and is non-groundable until explicit promotion."
    - "Audit contains only report/snapshot IDs, window, section counts and render result."
  artifacts:
    - path: packages/backend/convex/reportPack.ts
      provides: Snapshot, governed render/store and tenant-safe artifact result
    - path: packages/backend/convex/reportPack.test.ts
      provides: Snapshot consistency, isolation, grounding and audit evidence
  key_links:
    - from: generateBoardPack
      to: reportsBusiness and reportsGovernance internal projections
      via: one captured asOf/window snapshot before render
      pattern: "asOf|snapshot|render"
---
<objective>Generate a consistent board-pack artifact through the existing governed document path.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-14-SUMMARY.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-15-SUMMARY.md
@docs/playbooks/vault.md
@packages/core/src/documentGen.ts
@packages/backend/convex/vault.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Capture one immutable report snapshot</name>
  <files>packages/backend/convex/reportPack.test.ts, packages/backend/convex/reportPack.ts</files>
  <action>Test concurrent source changes and prove all sections retain one `asOf`, resolved window/timezone and coverage metadata. Reuse an existing compatible immutable row if possible; otherwise use the optional snapshot schema from plan 02. Store bounded section data in the content plane, never audit. Enforce tenant ownership and idempotency for replayed generation requests.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- reportPack reportsBusiness reportsGovernance</automated></verify>
  <done>A source mutation after capture cannot alter the input rendered for that report ID.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render/store the non-groundable PDF and safe audit</name>
  <files>packages/backend/convex/reportPack.test.ts, packages/backend/convex/reportPack.ts, docs/playbooks/dashboard-pages.md, docs/playbooks/vault.md</files>
  <action>Reuse the shipped structured-spec/document renderer; never pass model-authored markup. Store the PDF under the tenant as an `origin:"agent"` artifact with source timestamps/caveats and no ingest call. Return a tenant-safe artifact ref/download adapter. Test render failure, foreign access, replay, absence from RAG, and exact audit key set. Document retention and rollback: disable generation while existing snapshots/artifacts remain immutable.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- reportPack vault &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>One downloadable PDF is produced from one snapshot and remains non-groundable until plan-11 promotion.</done>
</task>
</tasks>
<verification>- Report pack/business/governance/vault focused tests, typecheck and watcher.</verification>
<success_criteria>- RPRT-01 board pack is consistent, isolated and safely audited.</success_criteria>
<output>Create `26-16-SUMMARY.md`.</output>

