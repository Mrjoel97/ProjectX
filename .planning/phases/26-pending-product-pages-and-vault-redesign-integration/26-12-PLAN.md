---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 12
type: execute
wave: 9
depends_on: ["26-11"]
files_modified:
  - packages/backend/convex/content.ts
  - packages/backend/convex/content.test.ts
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "Content is a bounded typed union of documents, generated attachments, rendered reels and sent mail."
    - "Signed URLs are ownership-checked, minted on demand and never logged/audited."
    - "Reels require valid render/sidecar proof and reuse only produces a cockpit-prefill link."
    - "Refresh Research is a provenance-backed cockpit deep link only; unknown provenance is explicitly unavailable and never starts work."
  artifacts:
    - path: packages/backend/convex/content.ts
      provides: Artifact/sent-mail summaries, detail adapters and safe actions
    - path: packages/backend/convex/content.test.ts
      provides: Union pagination, URL ownership, sidecar and feedback batching tests
  key_links:
    - from: content projections
      to: vaultDocuments, plans/media, requests and feedback indexes
      via: bounded server-side union/cursor contract
      pattern: "listArtifacts|artifactById|listSentMail"
    - from: research artifact refresh affordance
      to: originating cockpit thread
      via: sourceThreadId-derived non-action deep link or explicit provenance-unavailable result
      pattern: "refreshResearch|sourceThreadId"
---
<objective>Build one safe Content-library read/action plane over the existing artifact stores.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-11-SUMMARY.md
@packages/backend/convex/vault.ts
@packages/backend/convex/media.ts
@packages/backend/convex/plans.ts
@packages/backend/convex/requests.ts
@packages/backend/convex/feedback.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement bounded artifact and sent-mail projections</name>
  <files>packages/backend/convex/content.test.ts, packages/backend/convex/content.ts</files>
  <action>Test auth/isolation, cross-kind stable pagination, legacy unknown provenance, capped counts, grouped request terminals and batched feedback. Implement summaries/lists returning narrow discriminated cards; do not expose `requests.list`, raw recipients, storage IDs, bodies, or client-side concatenated unbounded arrays.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- content requests feedback</automated></verify>
  <done>All artifact kinds paginate predictably and list rows contain no raw content/recipient addresses.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add owned detail/download/reel/reuse/refresh adapters</name>
  <files>packages/backend/convex/content.test.ts, packages/backend/convex/content.ts, docs/playbooks/dashboard-pages.md</files>
  <action>Add by-id detail and on-demand URL adapters that reuse existing Vault/plan/media proofs. Require rendered status, storage and sidecar proof for reels. Define reuse as a code-owned /dashboard/workspace?thread=... prefill/deep-link result with no duplication, attachment or send. Define Refresh Research only for research artifacts with owned stable sourceThreadId: return a code-owned non-action cockpit URL/label that opens the originating thread so the user may request a refresh; it must not schedule, mutate, dispatch, or claim a refresh occurred. For legacy/unknown provenance return an explicit unavailable reason. Expose promotion result/state from the plan-11 mutation. Test known/foreign/missing provenance, guessed foreign IDs, zero refresh-side writes/audit, and assert URLs never enter audit/logs.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- content media plans requests &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Every Content action has a safe terminal; Refresh Research is provenance-backed navigation only; unknown provenance is honest and documented.</done>
</task>
</tasks>
<verification>- Content/media/plans/requests focused suites; typecheck; watcher.</verification>
<success_criteria>- CONT-01 has a bounded unified projection without copying blobs.
- Trust and URL boundaries remain server-enforced.</success_criteria>
<output>Create `26-12-SUMMARY.md`.</output>
