---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 12
type: execute
wave: 9
depends_on: ["26-11"]
files_modified:
  - packages/backend/convex/content.ts
  - packages/backend/convex/content.test.ts
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "Content is a bounded typed artifact shelf of documents, approved memos and rendered reels only."
    - "Sent mail belongs to Reports and research briefs belong to the Knowledge Vault; neither appears in Content projections or actions."
    - "Signed URLs are ownership-checked, minted on demand and never logged/audited."
    - "Reels require valid render/sidecar proof and reuse only produces a cockpit-prefill link."
    - "Promotion remains origin-guarded and idempotent; reuse remains a cockpit deep link and never starts hidden work."
  artifacts:
    - path: packages/backend/convex/content.ts
      provides: Bounded artifact-shelf summaries, detail adapters and safe actions
    - path: packages/backend/convex/content.test.ts
      provides: Union pagination, URL ownership, sidecar and feedback batching tests
  key_links:
    - from: content projections
      to: existing document/memo and rendered-media stores
      via: bounded server-side union/cursor contract
      pattern: "listArtifacts|artifactById"
    - from: eligible agent artifact
      to: plan-11 trust promotion and cockpit reuse
      via: explicit typed terminals with no duplicate, send or hidden execution
      pattern: "promote|reuse"
---
<objective>Build one safe Content-library read/action plane over the existing artifact stores.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-11-SUMMARY.md
@packages/backend/convex/vault.ts
@packages/backend/convex/media.ts
@packages/backend/convex/plans.ts
@packages/backend/convex/evaluations.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the bounded document, memo and reel shelf</name>
  <files>packages/backend/convex/content.test.ts, packages/backend/convex/content.ts</files>
  <action>Test auth/isolation, cross-kind stable pagination, legacy unknown provenance and capped counts for documents, approved memos and rendered reels. Implement summaries/lists returning narrow discriminated cards with no storage IDs, raw bodies or client-side concatenated unbounded arrays. Encode the amended boundary as negative tests: sent-mail/request rows and research briefs never enter the union, because Reports and the Knowledge Vault own them respectively.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- content evaluations media</automated></verify>
  <done>Only the three artifact-shelf kinds paginate predictably, and sent mail/research cannot leak into Content.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add owned detail, download, reel, reuse and promotion adapters</name>
  <files>packages/backend/convex/content.test.ts, packages/backend/convex/content.ts, docs/playbooks/dashboard-pages.md</files>
  <action>Add by-id detail and on-demand URL adapters that reuse existing Vault/evaluation/plan/media proofs. Require rendered status, storage and sidecar proof for reels. Define reuse as a code-owned `/dashboard/workspace?thread=...` prefill/deep-link result with no duplication, attachment, send or dispatch. Expose the plan-11 promotion result/state only for eligible origin-agent shelf artifacts. **PINNED BY 26-11 (owner decision, 2026-08-22): the promotion surface is `api.vault.promoteToReference` and 26-12 CALLS IT DIRECTLY, only PROJECTING its `{ok:true,state:"processing"|"already_promoted"} | {ok:false,reason:"ineligible"}` result. Do NOT wrap it in an `api.content.*` mutation -- duplicating the tenant/origin guard is how one of the two paths ends up unguarded. `content.ts` may READ `origin` to decide whether to offer the action; the guard itself lives in one place.** Note also that 26-11 landed `sourceThreadId` on BOTH rendered-media write sites, so a reel's thread absence is now genuine legacy absence and not a missed write site -- the known/unknown-provenance tests can rely on that. Test known/foreign/missing provenance, guessed foreign IDs, ineligible promotion, zero reuse-side writes/audit, and assert signed URLs never enter audit/logs. Do not add Refresh Research or sent-mail adapters: those surfaces are expressly outside Content.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- content media plans evaluations &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Every shelf action has a safe terminal, and neither sent mail nor research has a Content action path.</done>
</task>
</tasks>
<verification>- Content/media/plans/requests focused suites; typecheck; watcher.</verification>
<success_criteria>- CONT-01 has a bounded artifact-shelf projection without copying blobs.
- Trust and URL boundaries remain server-enforced.</success_criteria>
<output>Create `26-12-SUMMARY.md`.</output>
