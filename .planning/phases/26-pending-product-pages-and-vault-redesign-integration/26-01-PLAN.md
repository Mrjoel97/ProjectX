---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/dashboard.ts
  - packages/core/src/dashboard.test.ts
  - packages/core/src/index.ts
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Every Phase 26 page shares one bounded window, money-state, and partial-result vocabulary."
    - "Time windows are half-open and costs remain integer USD cents with estimate/reserve/actual/refund/unlanded distinct."
    - "All new dashboard backend modules and route directories are covered by one watched playbook."
  artifacts:
    - path: packages/core/src/dashboard.ts
      provides: Pure dashboard result, time-window, money-state, and clamp helpers
    - path: docs/playbooks/dashboard-pages.md
      provides: Cross-page authorization, pagination, rollout, rollback, and verification invariants
  key_links:
    - from: packages/core/src/dashboard.ts
      to: Phase 26 page adapters
      via: exported pure contracts consumed before JSX formatting
      pattern: "resolveDashboardWindow|DashboardMoney|partial"
---

<objective>
Freeze the shared product-page vocabulary and register the new surface with the playbook watcher.

Output: pure reusable contracts with mutation-resistant tests and one operational playbook for all new page modules/routes.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-CONTEXT.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-RESEARCH.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-VALIDATION.md
@docs/design/BRAND.md
@docs/playbooks/watch.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the bounded dashboard primitives</name>
  <files>packages/core/src/dashboard.test.ts, packages/core/src/dashboard.ts, packages/core/src/index.ts</files>
  <action>Create red tests, then implement: half-open window validation/clamping; named IANA timezone with an explicit browser-fallback marker; discriminated money phases; bounded/partial metadata; stable cursor/order helpers; and code-owned loading/empty/error/refusal states. Reject reversed/oversized windows and invalid cents. Keep the module framework-free and do not add a dependency.</action>
  <verify><automated>pnpm --filter @pikar/core test -- dashboard</automated></verify>
  <done>Pure tests cover boundaries, invalid input, partial honesty, and every money phase.</done>
</task>
<task type="auto">
  <name>Task 2: Register the dashboard-pages operational boundary</name>
  <files>docs/playbooks/dashboard-pages.md, docs/playbooks/watch.json</files>
  <action>Create the playbook from the repository template. Record tenant/owner authorization, no raw rows, pagination/caps, signed-URL handling, route gating, legacy fallbacks, per-page rollback and commands. Register exact prefixes for new backend modules, new dashboard route folders, and the page E2E specs. Do not absorb existing cockpit/vault/media watched files.</action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; pnpm --filter @pikar/core typecheck</automated></verify>
  <done>The watcher recognizes every planned new module/route and does not steal ownership from existing subsystem playbooks.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @pikar/core test -- dashboard`
- `pnpm --filter @pikar/core typecheck`
- `node scripts/check-playbooks.mjs`
</verification>

<success_criteria>
- DASH-01 has one tested page-contract vocabulary.
- New files cannot silently bypass playbook ownership.
</success_criteria>

<output>Create `26-01-SUMMARY.md`.</output>

