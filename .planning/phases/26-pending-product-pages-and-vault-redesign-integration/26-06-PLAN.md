---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 06
type: execute
wave: 4
depends_on: ["26-05"]
files_modified:
  - packages/backend/convex/spendLedger.ts
  - packages/backend/convex/spendLedger.test.ts
  - packages/core/src/spend.ts
  - packages/core/src/spend.test.ts
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [FIN-01]

must_haves:
  truths:
    - "Spend history is append-only, correlation-idempotent, and starts at an explicit coverage timestamp."
    - "Limiter state remains enforcement truth while the ledger is reporting/reconciliation truth."
    - "Pre-coverage history is unknown and every movement retains its rail and phase."
  artifacts:
    - path: packages/backend/convex/spendLedger.ts
      provides: Internal append/query helpers for spend events and coverage
    - path: packages/core/src/spend.ts
      provides: Pure movement validation and aggregation
  key_links:
    - from: spendLedger.ts
      to: spendEvents.by_correlation and tenant/time indexes
      via: insert-if-absent correlation guard
      pattern: "correlationId|coverageStartedAt"
---
<objective>Build the durable accounting seam before instrumenting any rail.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-RESEARCH.md
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-02-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define movement arithmetic and append-only storage</name>
  <files>packages/core/src/spend.test.ts, packages/core/src/spend.ts, packages/backend/convex/spendLedger.test.ts, packages/backend/convex/spendLedger.ts</files>
  <action>Test rail/phase/positive-cents validation, estimate/reserve/actual/refund aggregation, unlanded state and retry replay. Implement pure arithmetic plus internal append/read helpers. The correlation identity must include the movement phase so reserve and actual can coexist while a replay of either inserts once. Coverage start is durable even before the first paid event. Expose no update/delete function and store only refs, code-owned model/kind, counts and cents.</action>
  <verify><automated>pnpm --filter @pikar/core test -- spend &amp;&amp; pnpm --filter @pikar/backend test -- spendLedger</automated></verify>
  <done>Replay is a no-op, distinct phases remain distinct, and pre-coverage aggregation returns unknown rather than zero.</done>
</task>
<task type="auto">
  <name>Task 2: Record ledger operations and rollback invariants</name>
  <files>docs/playbooks/dashboard-pages.md, packages/backend/convex/spendLedger.ts</files>
  <action>Document retention, indexes, correlation construction, reconstruction limits and the non-negotiable rollback rule: Finance may hide, but ledger instrumentation stays active. Run redaction/static scans and package typechecks.</action>
  <verify><automated>pnpm --filter @pikar/core typecheck &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>The new writer is watched, typed, append-only and operationally documented.</done>
</task>
</tasks>
<verification>- Core and backend focused tests.
- Mutation check: replay a correlation and assert one row.</verification>
<success_criteria>- FIN-01 has a trustworthy history substrate.
- No UI or limiter behavior changes in this plan.</success_criteria>
<output>Create `26-06-SUMMARY.md`.</output>

