---
phase: 26-pending-product-pages-and-vault-redesign-integration
plan: 04
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - packages/backend/convex/approvals.ts
  - packages/backend/convex/approvals.test.ts
  - docs/playbooks/dashboard-pages.md
autonomous: true
requirements: [APRV-01]

must_haves:
  truths:
    - "Approvals exposes page-specific bounded projections, never raw plan/request/audit rows."
    - "Summary, awaiting, scheduled, in-flight, decisions and cleared reads are tenant-isolated and stably ordered."
    - "Legacy progress and missing cost/provenance render partial or unknown, never zero."
  artifacts:
    - path: packages/backend/convex/approvals.ts
      provides: summary and cursor-paginated Approvals projections
    - path: packages/backend/convex/approvals.test.ts
      provides: auth, isolation, pagination, capped and legacy-honesty evidence
  key_links:
    - from: approvals.ts
      to: plans.by_tenant_status_createdAt
      via: tenant-prefixed indexed queries and narrow projections
      pattern: "withIndex\(\"by_tenant_status_createdAt\""
---

<objective>Build the bounded read model that both the Approvals route and rail badge consume.</objective>
<execution_context>@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/26-pending-product-pages-and-vault-redesign-integration/26-RESEARCH.md
@packages/backend/convex/plans.ts
@packages/backend/convex/notifications.ts
@packages/backend/convex/evaluations.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement summary and paginated plan lanes</name>
  <files>packages/backend/convex/approvals.test.ts, packages/backend/convex/approvals.ts</files>
  <action>Test unauthenticated and guessed foreign IDs, stable cursor ties, empty vs capped, and legacy fields. Implement separate summary, awaiting, scheduled, in-flight and cleared queries using the compound index. Return timestamps/counts/kind/thread/ref metadata only; attachment URLs remain on-demand. Awaiting includes landed proposed email/media/calendar/memo plans. Cost is absent/unknown until ledger coverage exists.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- approvals</automated></verify>
  <done>Every list is bounded and removing the tenant/status predicate fails an isolation/non-vacuity test.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add bounded decision and blocked projections</name>
  <files>packages/backend/convex/approvals.test.ts, packages/backend/convex/approvals.ts, docs/playbooks/dashboard-pages.md</files>
  <action>Add an open-question projection with code-validated scorecard paths/types and a bounded blocked summary linking to Ops. Do not expose arbitrary evaluation field names, notification bodies, DLQ payloads, or resolution controls. Record the projection/partial contracts and rollback in the dashboard playbook.</action>
  <verify><automated>pnpm --filter @pikar/backend test -- approvals &amp;&amp; pnpm --filter @pikar/backend typecheck &amp;&amp; node scripts/check-playbooks.mjs</automated></verify>
  <done>Decision writes can only target validated fields and blocked rows reveal no content or PII.</done>
</task>
</tasks>
<verification>- `pnpm --filter @pikar/backend test -- approvals`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`</verification>
<success_criteria>- One summary powers both page and badge.
- Every Approvals lane has honest bounded semantics.</success_criteria>
<output>Create `26-04-SUMMARY.md`.</output>

