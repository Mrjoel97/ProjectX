---
phase: 03.5-deferred-send
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/cockpit.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
autonomous: true
requirements: [SCHD-01]

must_haves:
  truths:
    - "Approve with a future sendAt sets status 'scheduled', arms ctx.scheduler.runAt, stores scheduledFunctionId, and seeds the requests rows AT APPROVE (content frozen) — nothing sends before fire (SC3)"
    - "Approve with sendAt unset (or <= now) runs the immediate synchronous startFanout — today's behavior, unchanged (SC1, RESEARCH Open Question 2)"
    - "At fire, startScheduledDelivery starts the SAME internal.deliverApprovedPlan.deliverApprovedPlan — awaiting_reauth/DLQ/telemetry are the identical code (SC4 free)"
    - "cancelScheduledPlan is CAS-guarded on status==='scheduled', calls ctx.scheduler.cancel, flips to 'canceled', writes a refs-only plan.canceled audit, and is idempotent — after cancel nothing sends (SC4)"
    - "workflow.start( appears exactly once in the backend (inside startFanout)"
    - "The PLAN card shows the resolved absolute send time (user tz) with an editable picker before the single Approve; after Approve a ScheduledCard shows 'Scheduled for <abs, tz> · Cancel'; a canceled plan shows 'Canceled' (SC2)"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "startFanout helper; executePlan scheduled branch; startScheduledDelivery internalMutation; cancelScheduledPlan tenantMutation"
      contains: "startFanout"
    - path: "packages/backend/convex/plans.ts"
      provides: "setPlanSendTime tenantMutation — the picker's tenant-guarded content-plane writer"
      contains: "setPlanSendTime"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "PLAN-card datetime picker + ScheduledCard + Canceled note + CardList dispatch + hasDraft gating"
      contains: "ScheduledCard"
  key_links:
    - from: "executePlan (future sendAt)"
      to: "internal.cockpit.startScheduledDelivery"
      via: "ctx.scheduler.runAt(sendAt, ...) — stores the returned scheduledFunctionId"
      pattern: "scheduler\\.runAt"
    - from: "startScheduledDelivery"
      to: "startFanout → workflow.start(deliverApprovedPlan)"
      via: "the SINGLE call site both branches share"
      pattern: "workflow\\.start\\("
    - from: "cancelScheduledPlan"
      to: "ctx.scheduler.cancel + plan.canceled audit"
      via: "CAS gate on status==='scheduled' (cancel throws on an already-fired id)"
      pattern: "scheduler\\.cancel"
    - from: "cards.tsx PLAN picker / ScheduledCard"
      to: "api.plans.setPlanSendTime / api.cockpit.cancelScheduledPlan"
      via: "one source of truth — the plan row's sendAt"
      pattern: "setPlanSendTime|cancelScheduledPlan"
---

<objective>
The load-bearing structural change: branch `executePlan` to SCHEDULE instead of START when the plan
carries a future send time, without adding a second `workflow.start` call site; add the scheduled
callback, the cancel path, and the card UI (picker + ScheduledCard). This is where "approve once"
becomes "approve once, fires at the requested moment, cancellable until then".

Purpose: SC3 (Approve schedules, never starts; nothing before fire; spine reused) + SC4 (cancellable
until fire, audited; dead-token-at-fire inherits awaiting_reauth for free) + SC2's card surface.
Output: startFanout extraction + scheduled branch + startScheduledDelivery + cancelScheduledPlan +
setPlanSendTime + picker/ScheduledCard + the offline fake-timer tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.5-deferred-send/03.5-CONTEXT.md
@.planning/phases/03.5-deferred-send/03.5-RESEARCH.md
@.planning/phases/03.5-deferred-send/03.5-VALIDATION.md
@docs/playbooks/cockpit.md
@.planning/phases/03.5-deferred-send/03.5-01-SUMMARY.md
@.planning/phases/03.5-deferred-send/03.5-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — build against these directly. -->

executePlan (cockpit.ts:197-280) — the human approve gate + the SOLE workflow.start call site
(:268-276). After the CAS (:207-215), the token check (:211-212), and the UNCHANGED seed loop
(attachments materialize :229-240, requests rows :242-265, each with a server-minted correlationId),
today it calls workflow.start (:268) then patches status→delivering (:277). Refactor: extract the
workflow.start + status→delivering patch into `async function startFanout(ctx, {planId, tenantId,
requestIds, correlationIds, planCid})` (a plain helper, NOT a Convex fn — a mutation ctx works
exactly as today). Then branch: if plan.sendAt !== undefined && plan.sendAt > Date.now() →
ctx.scheduler.runAt(plan.sendAt, internal.cockpit.startScheduledDelivery, args) + patch
{status:"scheduled", scheduledFunctionId} + return {ok:true, scheduled:true}; else → startFanout(ctx,
args) (immediate, behavior-identical — covers sendAt unset AND sendAt<=now per Open Question 2).

The repo scheduler idiom (review.ts:63 runAfter + :116 cancel) — ctx.scheduler.runAt(timestampMs,
internal.fn, args) → Promise<Id<"_scheduled_functions">>; ctx.scheduler.cancel(id). cancel THROWS
on an already-committed fn — the status==="scheduled" CAS is the guard (RESEARCH Pitfall 1/Pattern 4).

startScheduledDelivery — an internalMutation({ args: { planId, tenantId, requestIds:v.array(v.id
("requests")), correlationIds:v.array(v.string()), planCid:v.string() }, handler: (ctx,a) =>
startFanout(ctx,a) }) — the scheduled callback that ONLY starts the frozen fan-out (RESEARCH Pattern 2).

cancelScheduledPlan — a tenantMutation (RESEARCH Pattern 4): get the plan, tenant-guard, if status
!== "scheduled" return {ok:true, alreadyResolved:true} (idempotent CAS), else ctx.scheduler.cancel
(plan.scheduledFunctionId) + patch{status:"canceled"} + ctx.runMutation(internal.audit.log,
{tenantId, correlationId: plan.correlationId ?? String(planId), eventType:"plan.canceled",
actor:ctx.tenantId, payload:{planId}}) refs-only (§4). audit.log is insert-only (CLAUDE.md §3) —
use internal.audit.log exactly like existing callers (deadLetter.ts:43 pattern).

plans.ts writers: patchPlan (internal, :71) already accepts sendAt (Plan 01). Add setPlanSendTime as
a tenantMutation (the picker's tenant-guarded writer): args {planId, sendAt: v.optional(v.number())},
guard plan.tenantId===ctx.tenantId, patch {sendAt} (undefined clears → immediate). plans.ts imports
tenantQuery at :14 — add tenantMutation to that import.

cards.tsx: Plan type = NonNullable<FunctionReturnType<typeof api.plans.byThread>> (:29). PlanCard
(:191-251) renders when status==="proposed" — add the native <input type="datetime-local"> bound to
plan.sendAt (epoch↔local wall-clock via the browser tz; the browser IS in the user's tz — RESEARCH
Pattern 6 / "Don't Hand-Roll") + a "send immediately" clear, writing via useMutation(api.plans.
setPlanSendTime); show the resolved absolute time + tz label above Approve (SC2). CardList (:419-440)
dispatch: add status==="scheduled" → <ScheduledCard> ("Scheduled for <abs,tz> · Cancel" →
useMutation(api.cockpit.cancelScheduledPlan)); status==="canceled" → a "Canceled" note; and gate
hasDraft (:426) off when status is scheduled/canceled (RESEARCH Open Question 3). Reuse the existing
box/label/chip/btn/dim tokens — NO new component library (ponytail).

Offline scheduler testing (VALIDATION §"Convex scheduled-function testing"): cockpit.test.ts:29-36
withDelivery() registers workflow + workpool. Use vi.useFakeTimers() before building the instance,
executePlan with a future sendAt → assert status "scheduled" + scheduledFunctionId set + a
_scheduled_functions system row exists + NO requests row past "approved"; vi.advanceTimersByTime +
await t.finishInProgressScheduledFunctions() → status "delivering". Cancel test: cancelScheduledPlan
→ status "canceled" + system row gone + refs-only plan.canceled audit; advance+finish → nothing sent.
vi.useRealTimers() in teardown.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: startFanout extraction + executePlan scheduled branch + startScheduledDelivery</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts</files>
  <behavior>
    - Immediate path (sendAt unset OR sendAt <= Date.now()): synchronous startFanout, status →
      "delivering", return {ok:true, workflowId} — byte-identical to today (the existing cockpit.test
      approve-gate/fan-out cases stay green).
    - Future sendAt: status → "scheduled", scheduledFunctionId stored, a _scheduled_functions row
      exists, NO requests row advances past "approved"; return {ok:true, scheduled:true}.
    - advanceTimers + finishInProgressScheduledFunctions → startScheduledDelivery runs → status
      "delivering" (the SAME fan-out).
    - git grep 'workflow.start(' returns exactly one hit (inside startFanout).
  </behavior>
  <action>
    RESEARCH Pattern 2/3. In cockpit.ts:
    - Extract the workflow.start block (:268-277) into a module-level `async function startFanout(ctx,
      { planId, tenantId, requestIds, correlationIds, planCid })` that calls workflow.start(...) then
      patches {status:"delivering", correlationId: planCid, workflowId}. This becomes the SOLE
      workflow.start call site.
    - In executePlan, AFTER the unchanged seed loop, build args = {planId, tenantId: ctx.tenantId,
      requestIds, correlationIds, planCid}. Branch: if (plan.sendAt !== undefined && plan.sendAt >
      Date.now()) { const scheduledFunctionId = await ctx.scheduler.runAt(plan.sendAt,
      internal.cockpit.startScheduledDelivery, args); await ctx.db.patch(planId, {status:"scheduled",
      scheduledFunctionId}); return {ok:true, scheduled:true}; } else { await startFanout(ctx, args);
      return {ok:true, workflowId}; }. Keep the return type union explicit (dodges TS7022 — the file
      already does this at :199-202); add the scheduled/alreadyResolved shapes.
    - Export `startScheduledDelivery = internalMutation({ args:{planId,tenantId,requestIds,
      correlationIds,planCid}, handler:(ctx,a)=>startFanout(ctx,a) })`.
    Add cockpit.test.ts cases (reuse withDelivery(); VALIDATION Wave-0): (a) arm→fire (future sendAt →
    scheduled + scheduledFunctionId + system row + requests still "approved"; advance+finish →
    delivering); (b) immediate-path-unchanged (sendAt unset → synchronous delivering, no scheduled
    row). Follow the fake-timer pattern from the RESEARCH code example verbatim.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpit && node -e "const {execSync}=require('child_process'); const n=execSync('git grep -c \"workflow.start(\" -- packages/backend/convex').toString().trim(); const total=n.split('\n').reduce((s,l)=>s+Number(l.split(':').pop()),0); if(total!==1){console.error('EXPECTED 1 workflow.start, got '+total+':\n'+n);process.exit(1)} console.log('single call site OK')"</automated>
  </verify>
  <done>executePlan schedules on a future sendAt (status scheduled, nothing sent) and starts immediately otherwise; startScheduledDelivery fires the same fan-out; workflow.start( appears exactly once; the existing approve-gate/fan-out tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cancelScheduledPlan (CAS + scheduler.cancel + refs-only audit) + setPlanSendTime writer</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/plans.ts, packages/backend/convex/cockpit.test.ts</files>
  <behavior>
    - cancelScheduledPlan on a status==="scheduled" plan: ctx.scheduler.cancel(scheduledFunctionId),
      status → "canceled", a refs-only plan.canceled audit row (payload {planId} ONLY), return
      {ok:true, canceled:true}; the _scheduled_functions row is gone; advance+finish sends NOTHING.
    - Double-cancel / a non-scheduled plan → {ok:true, alreadyResolved:true}, no throw (idempotent).
    - A cross-tenant caller → "plan not found" throw (no cross-tenant cancel).
    - setPlanSendTime (tenantMutation): tenant-guards then patches sendAt (undefined clears → immediate).
  </behavior>
  <action>
    RESEARCH Pattern 4. In cockpit.ts add cancelScheduledPlan tenantMutation exactly per the
    interfaces block: tenant-guard → CAS on status==="scheduled" (return alreadyResolved otherwise —
    the guard that prevents cancel() throwing on an already-fired id, Pitfall 1) → ctx.scheduler.cancel
    → patch{status:"canceled"} → internal.audit.log {eventType:"plan.canceled", payload:{planId}}
    refs-only (§4 — NEVER subject/body/recipients; audit is insert-only §3). In plans.ts add
    setPlanSendTime tenantMutation (add tenantMutation to the :14 import from ./lib/functions):
    args {planId, sendAt:v.optional(v.number())}, guard plan.tenantId===ctx.tenantId, patch {sendAt}
    (drop-undefined semantics — passing undefined clears the field → immediate send).
    Add cockpit.test.ts cancel-before-fire case (VALIDATION Wave-0): schedule → cancelScheduledPlan →
    assert status "canceled" + the _scheduled_functions system row gone + a refs-only plan.canceled
    audit row (payload has planId, no subject/body/recipient) + double-cancel no-ops; then
    advanceTimers + finishInProgressScheduledFunctions → assert NO requests row moved past "approved".
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpit plans</automated>
  </verify>
  <done>cancelScheduledPlan halts a scheduled send (status canceled, scheduler row gone, refs-only audit, nothing sends after), is idempotent, and is tenant-guarded; setPlanSendTime tenant-guards the picker write; green.</done>
</task>

<task type="auto">
  <name>Task 3: PLAN-card datetime picker + ScheduledCard + Canceled note + CardList dispatch</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx</files>
  <action>
    RESEARCH Pattern 6. In cards.tsx:
    - PlanCard (:191-251): add a native <input type="datetime-local"> above Approve, bound to
      plan.sendAt (convert epoch↔local wall-clock using the browser tz — the browser IS in the user's
      tz; use the value's local ISO for the input and Date parsing back to epoch ms on change) + a
      "Send immediately" button that clears it. On change/clear call useMutation(api.plans.
      setPlanSendTime)({planId, sendAt}). Render the resolved ABSOLUTE time + a tz label
      (Intl.DateTimeFormat().resolvedOptions().timeZone) as read-only text so the user SEES the exact
      moment before the single Approve (SC2). Reuse box/label/chip/btn/dim tokens — no new library.
    - Add ScheduledCard({plan}) — "Scheduled for <abs time, user tz>" + a Cancel button calling
      useMutation(api.cockpit.cancelScheduledPlan)({planId}); a busy flag makes a double-click a no-op
      (mirror PlanCard's approve()). Add a small "Canceled" note component/branch.
    - CardList (:419-440): add `{plan.status === "scheduled" && <ScheduledCard plan={plan} />}` and a
      `{plan.status === "canceled" && <div…>Canceled</div>}` branch; gate hasDraft (:426) off when
      status is "scheduled" or "canceled" (Open Question 3 — the ScheduledCard/Canceled note dominates).
      The existing reporting/proposed/resolving branches stay; a scheduled plan that later flips to
      delivering falls through to the existing ReportCard.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <done>PLAN card shows an editable send-time picker + the resolved absolute time/tz before Approve; a scheduled plan renders ScheduledCard with a working Cancel; a canceled plan shows "Canceled"; DraftCard is suppressed under scheduled/canceled; web typechecks.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test cockpit plans` green (arm→fire, cancel-before-fire, immediate unchanged).
- `git grep 'workflow.start('` over packages/backend/convex returns exactly ONE hit (inside startFanout).
- `pnpm --filter web typecheck` green.
- Refs-only §4: the plan.canceled audit payload is {planId} only — no subject/body/recipients/sendAt.
- The pre-existing audit.test.ts auditCounts red is NOT a regression (PARALLELIZATION.md).
</verification>

<success_criteria>
Approve on a plan with a future send time schedules (never starts) the same governed fan-out and
sends nothing before the requested moment; at fire the identical deliverApprovedPlan runs (so a dead
token → awaiting_reauth for free); the scheduled plan is cancellable until it fires with a refs-only
audit; the single workflow.start invariant holds; and the card surface shows the absolute time before
Approve, the ScheduledCard + Cancel after, and Canceled on halt.
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-deferred-send/03.5-03-SUMMARY.md`.
Update cockpit.md §9 Last verified (cockpit.ts/plans.ts/cards.tsx are watch-protected) in the same change.
</output>
