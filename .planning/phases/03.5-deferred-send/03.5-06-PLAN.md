---
phase: 03.5-deferred-send
plan: 06
type: execute
wave: 6
depends_on: [03, 05]
files_modified:
  - packages/core/src/emailIntent.ts
  - packages/core/src/emailIntent.test.ts
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpit.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [SCHD-01]

must_haves:
  truths:
    - "executePlan REFUSES to arm ctx.scheduler.runAt when plan.sendAt exceeds now + SEND_TIME_HORIZON_MS — regardless of which write path (NL setSendTime tool, picker setPlanSendTime, or Plan 05 reschedule) set the field; the horizon is enforced at the authoritative schedule-vs-immediate chokepoint, mirroring the existing > Date.now() past-guard, not merely at the NL path"
    - "parseSendTime returns a new 'tooFar' variant when the resolved instant is beyond a tunable horizon (now + SEND_TIME_HORIZON_MS) — pure, deterministic, offline-testable, no Date.now"
    - "The horizon is a tunable constant with a ponytail comment naming the ceiling (Gmail Testing-mode 7-day refresh-token expiry — design/scheduled-send.md) and the upgrade path (raise/remove once verified OAuth lands post-Phase-9)"
    - "setSendTime handles 'tooFar' with a clear re-ask ('further out than I can reliably schedule — the connection may expire before then; pick a sooner time') and writes NOTHING; its switch is exhaustive (explicit case 'none', no catch-all default) so a future SendTimeParse variant is a compile error, never a silent fall-through to immediate-send"
    - "The PLAN-card and CanceledCard datetime pickers cap max at now + SEND_TIME_HORIZON_MS (native platform hint, one shared exported horizon) — the soft UI complement to executePlan's hard server-side refusal"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "executePlan far-future refusal — a beyond-horizon plan.sendAt returns {ok:false, reason:'send_time_too_far'} BEFORE seeding/arming (the authoritative chokepoint covering every write path)"
      contains: "send_time_too_far"
    - path: "packages/core/src/emailIntent.ts"
      provides: "SEND_TIME_HORIZON_MS constant + the tooFar SendTimeParse variant + horizon check in parseSendTime"
      contains: "SEND_TIME_HORIZON_MS"
    - path: "packages/backend/convex/llm.ts"
      provides: "setSendTime handles the tooFar case (re-ask, no write); exhaustive switch"
      contains: "tooFar"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "datetime picker max = now + SEND_TIME_HORIZON_MS (imported from @pikar/core)"
      contains: "SEND_TIME_HORIZON_MS"
  key_links:
    - from: "executePlan scheduling decision"
      to: "refusal when plan.sendAt > Date.now() + SEND_TIME_HORIZON_MS"
      via: "an early return BEFORE the CAS flip/seed/arm — mirrors the gmail_not_connected early guard"
      pattern: "send_time_too_far"
    - from: "parseSendTime + setSendTime + cards.tsx pickers"
      to: "@pikar/core SEND_TIME_HORIZON_MS + the tooFar variant"
      via: "one exported horizon — the pure parser, the NL re-ask, the native picker max, and executePlan all read it"
      pattern: "tooFar|SEND_TIME_HORIZON_MS"
---

<objective>
Add a far-future cap so a user can't schedule a send so far out the Gmail OAuth token is dead by fire
time. The AUTHORITATIVE guard is a refusal in executePlan (the schedule-vs-immediate chokepoint) — one
check covers every write path (NL tool, picker, Plan 05 reschedule). A pure-parser `tooFar` variant +
the setSendTime re-ask fail fast in conversation, and the native picker caps its `max` — but the
server-side executePlan refusal is the one that must exist.

Purpose: SCHD-01 refinement (the CONTEXT `<deferred>` "far-future scheduling cap / token-expiry
mitigation" item; slice 1 shipped with "No far-future cap"). The structural risk is Gmail Testing-
mode's 7-day refresh-token expiry (design/scheduled-send.md) — a schedule past it finds a dead token.
The horizon is a tunable calibration knob shared by the pure parser (offline-SMOKE-able, Plan 01's
discipline) and executePlan. Output: the horizon constant + tooFar variant + executePlan refusal +
setSendTime handling + native picker cap.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.5-deferred-send/03.5-CONTEXT.md
@.planning/phases/03.5-deferred-send/03.5-RESEARCH.md
@.planning/design/scheduled-send.md
@docs/playbooks/cockpit.md
@.planning/phases/03.5-deferred-send/03.5-01-SUMMARY.md
@.planning/phases/03.5-deferred-send/03.5-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — build against these directly. -->

WHY executePlan is the authoritative gate (the blocker this plan closes): a `tooFar` check in the
setSendTime NL tool ALONE is insufficient. The picker's writer setPlanSendTime (plans.ts:102-109)
patches `sendAt` VERBATIM with no bound; cards.tsx's `<input type="datetime-local">` onChange
(cards.tsx:271-273) writes straight through (its `max` is a soft hint that does NOT block keystrokes
or a programmatic write); and Plan 05's reschedule re-approves through executePlan. The ONE place the
schedule-vs-immediate decision is actually made is executePlan (cockpit.ts:422:
`if (plan.sendAt !== undefined && plan.sendAt > Date.now())`). Guard there = every path covered
(ponytail root-cause fix — one guard at the chokepoint, not one per caller).

executePlan (cockpit.ts:343-435) return type union is `{ ok:true; workflowId?; alreadyStarted?:true;
scheduled?:true } | { ok:false; reason:"gmail_not_connected" }`. Its early guards return BEFORE any
state change: the token check (cockpit.ts:360-361) returns `{ ok:false, reason:"gmail_not_connected" }`
before the CAS flip (:364) and the seed loop (:391-414). Add the horizon refusal as a sibling early
guard in the SAME spot (after the token check, before the CAS flip) so a beyond-horizon plan never
seeds rows or flips status. Extend the reason union to `"gmail_not_connected" | "send_time_too_far"`.
Import SEND_TIME_HORIZON_MS from "@pikar/core" (cockpit.ts already imports from @pikar/core siblings).

packages/core/src/emailIntent.ts (Plan 01) — the pure parser. SendTimeParse (emailIntent.ts:208-212)
is `{kind:"resolved";epochMs} | {kind:"ambiguous"} | {kind:"past"} | {kind:"none"}`. SEND_TIME_EPSILON_MS
(:214) is the existing tunable-constant precedent to mirror. parseSendTime (:284-376) resolves in THREE
places returning {kind:"resolved", epochMs}: the relative branch (:294), the day-anchor branch (:350),
and the no-day PM branch (:371); each first checks the past-epsilon. Factor a tiny `classify(epochMs,
nowMs): SendTimeParse` (past | tooFar | resolved) so all three branches share it — ponytail, one helper
over three copies. `packages/core/src/index.ts` does `export *`, so SEND_TIME_HORIZON_MS surfaces for
cockpit.ts and cards.tsx to import.

packages/backend/convex/llm.ts setSendTime tool (llm.ts:760-796) — the execute switch (:782-794)
handles resolved/ambiguous/past then `default: // "none"`. That default is a trap: a new `tooFar`
variant would fall into it and read as "no time = immediate send". Add an explicit `case "tooFar":`
re-ask AND convert `default:` to `case "none":` so the switch is exhaustive (a future variant becomes
a TS error, not a silent fall-through). parseSendTime is already imported (llm.ts:50).

apps/web/app/(app)/dashboard/workspace/cards.tsx — the PLAN-card picker (cards.tsx:267-275) sets
`min={toLocalInputValue(Date.now())}`. Add `max={toLocalInputValue(Date.now() + SEND_TIME_HORIZON_MS)}`
(import SEND_TIME_HORIZON_MS from "@pikar/core"). The CanceledCard picker EXISTS after Plan 05 (this
plan depends_on [03, 05], so it lands second) — give it the same max, sharing the one constant.

cockpit.test.ts (Plan 03) — withDelivery()/seedSchedulable/listScheduled helpers + vi.useFakeTimers()
+ t.finishInProgressScheduledFunctions() are in the file. Copy that harness for the executePlan-refusal
test. cockpitTools.test.ts (Plan 02) drives setSendTime via the __invokeCockpitTool shim with a pinned
clientContext — copy a resolved/ambiguous case for the tooFar case.

Horizon value: default SEND_TIME_HORIZON_MS = 7 * 24 * 60 * 60 * 1000 (the design doc's ceiling — Gmail
Testing-mode's 7-day refresh-token lifetime). ponytail comment names it as the ceiling + upgrade path
(raise/remove once Google OAuth verification lands post-Phase-9). A calibration knob, not a hard truth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEND_TIME_HORIZON_MS + tooFar variant in the pure parseSendTime</name>
  <files>packages/core/src/emailIntent.ts, packages/core/src/emailIntent.test.ts</files>
  <behavior>
    - parseSendTime("in 8 days", nowMs, tz) (any resolve beyond nowMs + SEND_TIME_HORIZON_MS) → {kind:"tooFar"}.
    - parseSendTime("in 2 hours" / "tomorrow 9am", nowMs, tz) (within the horizon) → {kind:"resolved"} (unchanged).
    - Past → {kind:"past"}; ambiguous → {kind:"ambiguous"}; no time → {kind:"none"} (all unchanged).
    - Boundary: exactly at nowMs + SEND_TIME_HORIZON_MS is allowed; strictly beyond is tooFar.
    - PURE — the horizon is nowMs + SEND_TIME_HORIZON_MS; no Date.now.
  </behavior>
  <action>
    In emailIntent.ts:
    - Add `export const SEND_TIME_HORIZON_MS = 7 * 24 * 60 * 60 * 1000;` next to SEND_TIME_EPSILON_MS
      (:214) with a ponytail comment: the ceiling is Gmail Testing-mode's 7-day refresh-token expiry
      (design/scheduled-send.md) — a schedule past it finds a dead token at fire; upgrade path = raise
      or remove once verified Google OAuth lands (post-Phase-9). Calibration knob, not a hard truth.
    - Add `| { kind: "tooFar" }` to the SendTimeParse union (:208-212) with a one-line comment (beyond
      the horizon → RE-ASK, never a silent clamp — the connection may expire before then).
    - Factor `function classify(epochMs, nowMs): SendTimeParse` → {kind:"past"} when epochMs <= nowMs +
      SEND_TIME_EPSILON_MS, {kind:"tooFar"} when epochMs > nowMs + SEND_TIME_HORIZON_MS, else
      {kind:"resolved", epochMs}; replace the three `epochMs <= nowMs + EPSILON ? past : resolved`
      returns (relative :294, day-anchor :350, no-day PM :359-371) with `return classify(epochMs, nowMs)`.
    - Colocate emailIntent.test.ts cases (the runnable check): a beyond-horizon expression → tooFar; a
      just-inside-horizon → resolved; the exact boundary → resolved; re-confirm an existing within-
      horizon case still resolves (no regression). Fixed nowMs + ianaTz.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test emailIntent</automated>
  </verify>
  <done>SEND_TIME_HORIZON_MS exported + tunable with the ponytail ceiling/upgrade comment; parseSendTime returns tooFar strictly beyond the horizon, resolved at/within it; past/ambiguous/none unchanged; no Date.now; no new dependency; green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: executePlan far-future refusal — the authoritative server-side guard</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpit.test.ts</files>
  <behavior>
    - executePlan on a proposed plan whose plan.sendAt > Date.now() + SEND_TIME_HORIZON_MS: returns
      {ok:false, reason:"send_time_too_far"} BEFORE any state change — status stays "proposed", NO
      requests rows seeded, NO scheduler armed. True regardless of which path set sendAt (NL tool,
      picker, or Plan 05 reschedule) — this is the chokepoint covering all of them.
    - executePlan with a within-horizon future sendAt → still schedules (status "scheduled", armed) — unchanged.
    - executePlan with sendAt unset OR <= now → immediate startFanout — unchanged.
  </behavior>
  <action>
    Ponytail root-cause fix (ONE guard at the chokepoint, not one per caller). In cockpit.ts:
    - Import SEND_TIME_HORIZON_MS from "@pikar/core".
    - Extend executePlan's return type union: `reason: "gmail_not_connected" | "send_time_too_far"`.
    - Add an early guard right AFTER the token check (cockpit.ts:360-361, the gmail_not_connected
      return) and BEFORE the CAS flip (:364): `if (plan.sendAt !== undefined && plan.sendAt >
      Date.now() + SEND_TIME_HORIZON_MS) return { ok: false, reason: "send_time_too_far" };` — no seed,
      no status flip, no arm (mirrors the gmail_not_connected fail-before-mutate guard). A one-line
      comment ties it to the design-doc token-life ceiling.
    - Do NOT alter the existing `> Date.now()` schedule/immediate branch (:422) — the new guard sits
      upstream so the branch only ever sees an in-window sendAt.
    Add cockpit.test.ts case (reuse withDelivery()/seedSchedulable + fake timers): seed a proposed plan
    with sendAt = SMOKE_NOW + SEND_TIME_HORIZON_MS + 3_600_000 → executePlan → assert
    {ok:false, reason:"send_time_too_far"} + status still "proposed" + zero requests rows + no
    _scheduled_functions row. Plus a within-horizon control (sendAt = now + 1hr → scheduled, armed).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpit</automated>
  </verify>
  <done>executePlan refuses a beyond-horizon sendAt (send_time_too_far) before seeding/arming, from any write path; within-horizon and immediate paths unchanged; green.</done>
</task>

<task type="auto">
  <name>Task 3: setSendTime tooFar re-ask (exhaustive switch) + native picker max + playbook close</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts, apps/web/app/(app)/dashboard/workspace/cards.tsx, docs/playbooks/cockpit.md</files>
  <behavior>
    - setSendTime on a tooFar parse: return the re-ask ("further out than I can reliably schedule — the
      connection may expire before then; ask the user for a sooner time. Nothing was scheduled.") and
      call NO patchPlan (fail-fast in conversation; never a silent clamp).
    - The setSendTime switch is exhaustive: explicit case "none" replaces the catch-all default.
    - The PLAN-card and CanceledCard datetime pickers cap max at now + SEND_TIME_HORIZON_MS (soft UI
      complement to executePlan's hard refusal), sharing the one exported constant with the NL path.
  </behavior>
  <action>
    In llm.ts setSendTime execute (llm.ts:782-794): add `case "tooFar": return "That's further out than
    I can reliably schedule — the connection may expire before then; ask the user for a sooner time.
    Nothing was scheduled.";` and change `default: // "none"` to `case "none":` (same return) so the
    switch is exhaustive over SendTimeParse. Do NOT touch the tool's clock/zone contract (§2-D). The
    horizon message is a tool return string, not a hardcoded prompt — no skill-mirror change needed
    (the agent relays the tool result; the existing "## Scheduling" skill already covers re-asking, §5).
    In cards.tsx: import SEND_TIME_HORIZON_MS from "@pikar/core" and add
    `max={toLocalInputValue(Date.now() + SEND_TIME_HORIZON_MS)}` to the PLAN-card picker (cards.tsx:270)
    AND the CanceledCard picker (added by Plan 05 — this plan depends_on [03, 05], so the card exists) —
    one constant, both seams.
    In cockpitTools.test.ts: add a setSendTime tooFar case (drive a beyond-horizon expression through
    the shim with a pinned clientContext → assert the re-ask string is returned and patchPlan was NOT
    called — mirror the existing ambiguous/past no-write assertions).
    Update docs/playbooks/cockpit.md (§9): add the invariant "a send time beyond SEND_TIME_HORIZON_MS
    (default 7d, Gmail Testing-mode token life) is refused at executePlan (send_time_too_far) — the
    authoritative gate covering the NL tool, the picker, and reschedule; setSendTime also re-asks and
    the pickers cap max; never a silent clamp; the horizon is a tunable knob (raise/remove post
    verified-OAuth)". Bump the Last verified line to 03.5-06 (this plan lands after 05 per depends_on,
    so its bump is the final one — no stomp).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpitTools && pnpm --filter web typecheck && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>setSendTime re-asks (no write) on tooFar with an exhaustive switch; the pickers cap max at the shared horizon; cockpitTools proves the tooFar re-ask writes nothing; web typechecks; cockpit.md carries the executePlan-refusal invariant + Last verified → 03.5-06; check-playbooks exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test emailIntent` green (tooFar beyond horizon, resolved within/at boundary, no regressions).
- `pnpm --filter @pikar/backend test cockpit cockpitTools` green (executePlan refuses beyond-horizon before seeding/arming; tooFar re-ask writes nothing).
- `pnpm --filter web typecheck` green (picker max + the @pikar/core import).
- `node scripts/check-playbooks.mjs` exits 0 (cockpit.md updated — cockpit.ts/llm.ts/cards.tsx/emailIntent.ts are watch-protected).
- The horizon is one exported constant shared by the pure parser, executePlan, the NL tool, and the native pickers (no divergence).
- No new dependency; no hardcoded scheduling prompt in llm.ts (§5); no Date.now in the parser (§1).
</verification>

<success_criteria>
A user can no longer schedule a send so far out the Gmail token would be dead at fire time — and the
guard holds no matter how sendAt was written: executePlan refuses a beyond-horizon schedule
(send_time_too_far) before seeding or arming, covering the NL tool, the picker, and Plan 05's
reschedule at one chokepoint. The pure parser flags tooFar for a fast conversational re-ask, and the
pickers cap their range as a soft complement. The horizon is a tunable knob with its ceiling
(Testing-mode token expiry) and upgrade path (post verified-OAuth) named in a ponytail comment.
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-deferred-send/03.5-06-SUMMARY.md`.
Update docs/playbooks/cockpit.md in the SAME change (§9 — emailIntent.ts/cockpit.ts/llm.ts/cards.tsx
are watch-protected). Bump the Last verified line to 03.5-06 (final bump — this plan lands after 05).
</output>
