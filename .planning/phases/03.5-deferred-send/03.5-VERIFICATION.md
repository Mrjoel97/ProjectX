---
phase: 03.5-deferred-send
verified: 2026-07-19T01:00:00Z
status: passed
score: 10/10 must-haves verified
---

# Phase 03.5: Deferred Send Verification Report

**Phase Goal:** "Send this at 4 AM" — a plan carries a future send time, shown absolute on the PLAN card
before the single Approve; execution scheduled through the same governed fan-out, cancellable until it
fires. Recurring sends stay out of v1. Plus two Wave 5/6 refinements: (1) a canceled plan can be
re-scheduled (canceled→proposed→scheduled) via the existing executePlan seam; (2) a far-future cap
(SEND_TIME_HORIZON_MS = 7d, Gmail Testing-mode token life) refused authoritatively at executePlan
(send_time_too_far), with a pure-parser tooFar variant + setSendTime re-ask + native picker max.

**Verified:** 2026-07-19
**Status:** passed
**Re-verification:** No — initial verification (scoped to Plans 05/06; Plans 01–04 were verified at
phase close in 03.5-04-SUMMARY.md / prior playbook entries and are not re-checked here beyond a
regression pass via the full `cockpit` test run).

## Scope Note

Per the task, 01–04 were already verified at the earlier phase close (03.5-04, SCHD-01 marked
Complete in REQUIREMENTS.md). This report focuses full 3-level verification on the two newly-executed
plans:
- **03.5-05** — `reschedulePlan` (canceled→proposed reschedule) + `CanceledCard` reschedule surface.
- **03.5-06** — `SEND_TIME_HORIZON_MS` far-future cap across parser, executePlan, setSendTime, pickers.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A canceled plan can be re-scheduled: new future time + re-approve → canceled→proposed→scheduled, re-arming via the EXISTING executePlan branch (content re-frozen) | ✓ VERIFIED | `reschedulePlan` (cockpit.ts:488-523) CAS-flips canceled→proposed; re-approve goes through unchanged `executePlan` (cockpit.ts:343-444). Test "reschedule happy path" (cockpit.test.ts:361-411) proves the full cycle including re-arm + fire→delivering. |
| 2 | reschedulePlan refuses (re-asks) on a missing/past sendAt — never silently un-cancels into a send | ✓ VERIFIED | cockpit.ts:502-504 (`needs_future_time`, writes nothing). Test "reschedule re-ask" (cockpit.test.ts:413-438) asserts status stays canceled, 0 requests deleted, 0 audit rows. |
| 3 | reschedulePlan deletes the canceled plan's orphaned requests rows (by_plan) so reportForPlan doesn't double-count | ✓ VERIFIED | cockpit.ts:505-511 (`by_plan` query + delete loop). Test asserts 2 orphan rows → 0 after reschedule → exactly 2 fresh rows after re-approve (cockpit.test.ts:372,381,398). |
| 4 | canceled stays terminal unless explicitly re-scheduled; the re-armed plan is cancellable again | ✓ VERIFIED | CAS guard (cockpit.ts:499, idempotent `alreadyResolved` on non-canceled). Test "reschedule idempotent" (cockpit.test.ts:440-448). Re-armed plan uses unchanged `cancelScheduledPlan`/ScheduledCard — no new cancel path needed. |
| 5 | No second workflow.start/scheduler.runAt call site added; reschedulePlan writes ONE refs-only plan.rescheduled audit | ✓ VERIFIED | `git grep` shows exactly one `workflow.start(` (cockpit.ts:303) and one `scheduler.runAt(` (cockpit.ts:432). reschedulePlan's only db write beyond status/deletes is `internal.audit.log` with `payload: { planId }` (cockpit.ts:514-520), asserted refs-only in test (cockpit.test.ts:388-392, checks payload does NOT contain subject/body/recipient strings). |
| 6 | executePlan refuses ctx.scheduler.runAt when plan.sendAt exceeds now + SEND_TIME_HORIZON_MS, covering every write path (NL, picker, reschedule) | ✓ VERIFIED | Early guard at cockpit.ts:369-370, positioned AFTER the gmail token check and BEFORE the CAS flip/seed loop — a single chokepoint upstream of all paths. Test "far-future refusal" (cockpit.test.ts:277-302) proves fail-before-mutate (status unchanged, 0 requests, 0 scheduled rows) + a within-horizon control still schedules. |
| 7 | parseSendTime returns a new tooFar variant beyond the horizon — pure, deterministic, no Date.now | ✓ VERIFIED | emailIntent.ts:212 (`{kind:"tooFar"}` union member), `classify()` helper (emailIntent.ts:228-232) shared by all three resolve sites (:312, :368, :389). emailIntent.test.ts:298-299 ("in 169 hours" → tooFar); 39/39 tests pass. No `Date.now` call in the file (nowMs is injected). |
| 8 | The horizon is a tunable constant with a ponytail comment naming the ceiling + upgrade path | ✓ VERIFIED | emailIntent.ts:217-221: `export const SEND_TIME_HORIZON_MS = 7 * 24 * 60 * 60 * 1000;` with a comment naming the Gmail Testing-mode 7-day ceiling and the raise/remove-post-verified-OAuth upgrade path. |
| 9 | setSendTime handles tooFar with a clear re-ask and writes nothing; the switch is exhaustive (case "none", no default) | ✓ VERIFIED | llm.ts:792-795 — explicit `case "tooFar"` (re-ask string) and `case "none"` (no catch-all `default:` remains in the switch). cockpitTools.test.ts:442-447 ("setSendTime re-asks on a TOO-FAR time and writes NOTHING") asserts the re-ask string and no patch. |
| 10 | The PLAN-card and CanceledCard pickers cap max at now + SEND_TIME_HORIZON_MS | ✓ VERIFIED | cards.tsx:274 (PlanCard) and cards.tsx:387 (CanceledCard) both set `max={toLocalInputValue(Date.now() + SEND_TIME_HORIZON_MS)}`, importing the same constant from `@pikar/core` (cards.tsx:10). |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/backend/convex/cockpit.ts` — `reschedulePlan` | CAS canceled→proposed + future-time re-ask + orphan cleanup + refs-only audit | ✓ VERIFIED | Lines 476-523, matches spec exactly; wired into cards.tsx via `api.cockpit.reschedulePlan`. |
| `packages/backend/convex/cockpit.ts` — `executePlan` far-future guard | Beyond-horizon refusal before seed/arm | ✓ VERIFIED | Lines 363-370, sibling early guard to `gmail_not_connected`. |
| `apps/web/.../cards.tsx` — `CanceledCard` reschedule surface | Picker + Reschedule button → reschedulePlan → executePlan | ✓ VERIFIED | Lines 347-410; CardList dispatch passes `plan`/`threadId` (line 1082). |
| `packages/core/src/emailIntent.ts` — `SEND_TIME_HORIZON_MS` + `tooFar` + `classify()` | Tunable constant, new union member, shared helper | ✓ VERIFIED | Lines 208-232; all 3 resolve branches route through `classify()`. |
| `packages/backend/convex/llm.ts` — `setSendTime` tooFar case | Re-ask, no write, exhaustive switch | ✓ VERIFIED | Lines 782-796. |
| `apps/web/.../cards.tsx` — picker `max` cap | Both pickers import & apply `SEND_TIME_HORIZON_MS` | ✓ VERIFIED | Lines 274, 387. |

All artifacts exist, are substantive (no stubs/placeholders), and are wired (imported + used + covered by passing tests).

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `reschedulePlan` | status canceled→proposed + `ctx.db.delete` requests by_plan + `internal.audit.log plan.rescheduled` | CAS + future-sendAt guard | ✓ WIRED | Confirmed in code (cockpit.ts:498-521) and exercised end-to-end by the happy-path test. |
| `CanceledCard` picker/Reschedule | `api.plans.setPlanSendTime` + `api.cockpit.reschedulePlan` + `api.cockpit.executePlan` | `useMutation` calls, sequenced in `doReschedule()` | ✓ WIRED | cards.tsx:353-375; `execute` only called when `reschedule` returns `ok:true` — a `needs_future_time` result short-circuits (line 367-370) and surfaces the inline re-ask, matching spec. |
| `executePlan` scheduling decision | refusal when `plan.sendAt > Date.now() + SEND_TIME_HORIZON_MS` | early return before CAS flip/seed/arm | ✓ WIRED | cockpit.ts:369-370; test proves zero requests/zero scheduled rows on refusal. |
| `parseSendTime` + `setSendTime` + cards.tsx pickers | `@pikar/core SEND_TIME_HORIZON_MS` + `tooFar` variant | one exported horizon, four consumers | ✓ WIRED | Single export (emailIntent.ts:221) consumed by cockpit.ts:14, llm.ts (via `parseSendTime` return), cards.tsx:10 — no divergent duplicate constant found anywhere in the codebase. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SCHD-01 | 03.5-01..06 | Deferred send: future send time, absolute display, scheduled fan-out, cancellable, reauth-degrade parity | ✓ SATISFIED | Marked Complete in REQUIREMENTS.md (line 56, line 167) at Phase 3.5 close (Plan 04); Plans 05/06 are refinements (reschedule + far-future cap) both declaring `requirements: [SCHD-01]` and both fully verified above. No orphaned requirement IDs found for "Phase 3.5" in REQUIREMENTS.md beyond SCHD-01. |

### Anti-Patterns Found

None. Scanned `cockpit.ts`, `emailIntent.ts`, `llm.ts`, `cards.tsx` for TODO/FIXME/PLACEHOLDER/"coming soon"/empty-handler patterns — no matches outside intentional `ponytail:`-style calibration comments (which are the mandated convention, not a defect).

### Invariant Checks (CLAUDE.md)

- **§1 domain logic in packages/*:** `SEND_TIME_HORIZON_MS`, `tooFar`, `classify()` all live in `packages/core/src/emailIntent.ts` (pure TS); `cockpit.ts`/`llm.ts` only import + orchestrate. Held.
- **§3 audit insert-only:** `reschedulePlan` calls `internal.audit.log` only (no `patch`/`replace`/`delete` on the `audit` table anywhere in the touched files). `audit.ts` itself exports only `log` (insert), `countAudit` (read), `backfillAuditCounts` (reads audit, does not mutate it) — pre-existing, unchanged by this phase. Held.
- **§4 refs-only:** `plan.rescheduled` payload is `{planId}` only — asserted directly in test (cockpit.test.ts:388-392) checking the payload string does NOT contain subject/body/recipient content. Held.
- **§5 no hardcoded prompts:** The `setSendTime` tooFar re-ask is a tool-return string (data returned to the agent), not a system/skill prompt — matches the existing pattern of the other `SendTimeParse` re-ask branches (ambiguous/past), which were already this way pre-phase. No skill-registry change needed or made. Held.
- **Single call site:** `git grep`-style check confirms exactly one `workflow.start(` (cockpit.ts:303) and one `scheduler.runAt(` (cockpit.ts:432) in cockpit.ts — reschedule and the far-future guard add neither. Held.

### Test Results (this session, live-run)

- `pnpm --filter @pikar/core test emailIntent` — 39/39 passed.
- `pnpm --filter @pikar/backend test cockpit` — 4 files / 73 tests passed (cockpit.test.ts 16, cockpitTools.test.ts 38, runCockpitAgent.test.ts 16, cockpitDraft.test.ts 3). `runCockpitAgent.test.ts` did not time out in this run (its 5000ms machine-load flake is a documented non-regression when it does occur).
- `pnpm --filter web typecheck` — exit 0.
- `node scripts/check-playbooks.mjs` — exit 0 (cockpit.md carries both the 03.5-05 and 03.5-06 invariant entries; "Last verified" header reads 03.5-06).

The pre-existing `audit.test.ts` `auditCounts` failure was not re-triggered/chased in this verification pass (out of scope, documented non-regression per task instructions).

### Human Verification Required

None required for Plans 05/06 — both are fully covered by deterministic offline tests (fake timers +
`finishInProgressScheduledFunctions`, pinned-clock `cockpitTools` shim, pure-function `emailIntent`
tests) that exercise the exact state transitions and refusal paths claimed. The original SCHD-01
live-fire/token-expiry human-verify was already completed at Plan 04 phase close (per
`03.5-VALIDATION.md`) and is not re-litigated here.

### Gaps Summary

No gaps. All 10 derived observable truths verified against live code and passing tests; all artifacts
exist, are substantive, and are wired; both key-link groups confirmed; SCHD-01 requirement coverage
intact with no orphans; project invariants (insert-only/refs-only audit, single call site, no hardcoded
prompts, domain logic in packages/*) all hold.

---

_Verified: 2026-07-19_
_Verifier: Claude (gsd-verifier)_
