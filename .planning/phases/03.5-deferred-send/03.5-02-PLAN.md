---
phase: 03.5-deferred-send
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/llmRedaction.test.ts
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
autonomous: true
requirements: [SCHD-01]

must_haves:
  truths:
    - "When the user volunteers a natural-language time, the agent calls setSendTime, which parses it (with the CLIENT's nowMs + ianaTz, never the model's) and writes plan.sendAt"
    - "An ambiguous or past parse writes NOTHING and returns a re-ask message (SC2: re-ask, never guess)"
    - "buildAgentContext surfaces the resolved/pending send time so the agent reasons about it without inventing a clock"
    - "sendAt / scheduledFunctionId / any scheduling field never appears in an audit/DLQ/telemetry payload (§4)"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "setSendTime tool in buildCockpitTools; buildAgentContext send-time line; SMOKE sendTime= op; clientContext threaded into buildCockpitTools/runCockpitAgent"
      contains: "setSendTime"
    - path: "packages/backend/convex/cockpit.ts"
      provides: "optional clientContext {tz, nowMs} threaded sendCockpitMessage → runCockpitAgent"
      contains: "clientContext"
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "## Scheduling section — call setSendTime on a volunteered time, confirm the absolute time, re-ask on ambiguity/past"
      contains: "Scheduling"
  key_links:
    - from: "setSendTime tool"
      to: "@pikar/core parseSendTime"
      via: "nowMs + ianaTz supplied by the trusted client (clientContext), NEVER the model"
      pattern: "parseSendTime\\("
    - from: "sendCockpitMessage"
      to: "runCockpitAgent → buildCockpitTools"
      via: "optional clientContext {tz, nowMs} arg threaded down; SMOKE path pins them offline"
      pattern: "clientContext"
---

<objective>
Add the fast-path natural-language time intake: a governed `setSendTime` tool that mirrors
`setSubject`/`draftBody`, parses the volunteered time with the CLIENT's clock+zone, and writes
`plan.sendAt` (or re-asks on ambiguous/past without writing). Surface the send time in the agent
context, thread the client tz/nowMs down the driver, teach the skill to use it, and prove §4.

Purpose: SC1 (optional NL slot) + SC2 (ambiguous re-asked, never guessed). The model must NOT
invent "now" or the timezone (§2-D trust boundary) — the client supplies them (RESEARCH Open
Question 1, recommendation (a)). The picker (Wave 3) remains the confirm-source-of-truth.
Output: `setSendTime` tool + context surfacing + SMOKE op + clientContext threading + skill section.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.5-deferred-send/03.5-CONTEXT.md
@.planning/phases/03.5-deferred-send/03.5-RESEARCH.md
@.planning/phases/03.5-deferred-send/03.5-VALIDATION.md
@docs/playbooks/cockpit.md
@.planning/phases/03.5-deferred-send/03.5-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — mirror these existing patterns exactly. -->

llm.ts setSubject tool (llm.ts:688-700) — the shape to mirror for setSendTime:
  setSubject: tool({ description, inputSchema: jsonSchema<{subject:string}>({...}),
    execute: async ({subject}) => { await ctx.runMutation(internal.plans.patchPlan,{planId,subject}); return "Subject set."; } })
draftBody tool (llm.ts:715-745) — mirror the scan/return-a-confirmation-string shape.

buildCockpitTools signature (llm.ts:509-513): buildCockpitTools(ctx, tenantId, planId).
  Add an optional 4th arg `clientContext?: { tz: string; nowMs: number }` (append-only — every
  existing caller keeps working). The setSendTime tool reads nowMs/ianaTz from it; absent (or the
  SMOKE path) → pin a deterministic value (mirror the attachment date-pinning trick, RESEARCH §5).

buildAgentContext (llm.ts:446-502) — the model-facing plan view (address-free §2-D). Add a
  `Send time:` line: resolved → the ABSOLUTE formatted time (Intl, user tz) so the model can
  confirm it; nothing set → "(immediate on approve)". Extend the param type with sendAt?: number.

runCockpitAgent (llm.ts:1120-1162) — args { tenantId, threadId, planId, text, model? }. Add an
  optional clientContext arg and pass it into buildCockpitTools(ctx, tenantId, planId, clientContext)
  at :1144. The SMOKE branch (:1147-1148) pins nowMs/ianaTz.

SMOKE grammar: AgentSmokeOp union (llm.ts:1015-1026), parseAgentSmoke (:1028-1075), runAgentSmokeOp
  (:1077-1108). Add a `sendTime=<expr>` op → invokeTool(tools,"setSendTime",{text:<expr>}). Pin a
  fixed nowMs/ianaTz on the SMOKE path (mirror the personalize=<i>:<intent> colon-split precedent).

cockpit.ts sendCockpitMessage (cockpit.ts:45-86) — args { threadId?, text }. Add optional
  clientContext: v.optional(v.object({ tz: v.string(), nowMs: v.number() })) and pass it into the
  internal.llm.runCockpitAgent call (:67-72). resolveRecipients (:104-146) also calls runCockpitAgent —
  thread clientContext there too if trivially available, else omit (the RESOLUTION_CONTINUE turn
  carries no time; a missing clientContext just means setSendTime isn't callable that turn — fine).

Skill 5-file mirror (RESEARCH Open Question 4 / cockpit.md "How to change safely"): the cockpit-agent
  skill is packages/contracts/skills/cockpit-agent.md (canonical) + packages/contracts/src/skills/
  cockpitAgent.ts (byte-identical derived cockpitAgentSkillBody + COCKPIT_AGENT_SKILL name const) +
  the seedSkills row + a drift test.each row in skills.test.ts. seedSkills publishes on change
  (version bump). This is the §5 path — NEVER hardcode the instruction in llm.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setSendTime tool + buildAgentContext surfacing + clientContext threading + SMOKE op</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpit.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - setSendTime({ text }) with clientContext present: parseSendTime(text, nowMs, ianaTz) →
      resolved → patchPlan{ sendAt: epochMs } + return an ABSOLUTE-time confirmation string (Intl,
      user tz) for the model to echo.
    - ambiguous → NO write, return a re-ask ("that time is ambiguous — which day/‌time did you mean?").
    - past → NO write, return a re-ask ("that time has passed — when did you mean?").
    - none → NO write, return a note that no time was detected (immediate send on approve).
    - buildAgentContext emits a "Send time:" line (resolved absolute time in user tz, or immediate).
    - SMOKE::agent::sendTime=<expr> drives the tool offline with a pinned nowMs/ianaTz.
  </behavior>
  <action>
    RESEARCH Pattern 5. In llm.ts:
    - Add `setSendTime` to buildCockpitTools mirroring setSubject/draftBody (llm.ts:688-745):
      inputSchema jsonSchema<{text:string}>; execute calls @pikar/core parseSendTime with nowMs +
      ianaTz from the new `clientContext` arg (add it as the optional 4th param on buildCockpitTools).
      resolved → patchPlan{sendAt}; ambiguous/past/none → NO patch, return the re-ask/none message.
      Format the confirmation via Intl.DateTimeFormat({timeZone: ianaTz}). NEVER take nowMs/tz from
      the model (§2-D). If clientContext is absent AND not SMOKE, return a graceful "I couldn't read
      your timezone — use the date picker on the plan card" (the picker is the fallback writer).
    - Extend buildAgentContext (:446-502) param with sendAt?: number and add the "Send time:" line.
    - Thread clientContext: add the optional arg to runCockpitAgent (:1120-1131), pass into
      buildCockpitTools at :1144; on the SMOKE branch (:1147) pass a pinned {tz:"UTC", nowMs:<fixed>}
      so sendTime= is deterministic offline (mirror the 1970-01-01 attachment pinning).
    - SMOKE: add `sendTime` to AgentSmokeOp (:1015), parseAgentSmoke (:1037 switch) and runAgentSmokeOp
      (:1081 switch) → invokeTool(tools,"setSendTime",{text:val}).
    In cockpit.ts: add optional clientContext to sendCockpitMessage args (:46) and pass it into the
    runCockpitAgent call (:67-72).
    Add cockpitTools.test.ts cases (VALIDATION Wave-0 gap): resolved → sendAt patched; ambiguous →
    no write + re-ask; past → no write + re-ask; buildAgentContext shows the absolute time. Drive
    via the existing __invokeCockpitTool shim with a pinned clientContext (build the tools with a
    fixed nowMs/ianaTz).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test cockpitTools</automated>
  </verify>
  <done>setSendTime writes sendAt only on resolved; ambiguous/past re-ask with no write; buildAgentContext surfaces the absolute time; SMOKE sendTime= drives the tool offline; clientContext threads sendCockpitMessage → runCockpitAgent → buildCockpitTools; backend source typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: §4 redaction static scan — scheduling fields never logged</name>
  <files>packages/backend/convex/llmRedaction.test.ts</files>
  <behavior>
    - Static scan proves the setSendTime tool surface scans/handles the send time on the content
      plane only: sendAt / scheduledFunctionId / the raw NL time text never appear in an audit,
      deadLetters, or telemetry payload construction (extend the existing scoped-scan pattern the
      file already uses for draftCockpit / recipientBodies).
  </behavior>
  <action>
    RESEARCH §4 / VALIDATION Wave-0 gap. Extend llmRedaction.test.ts with a scoped scan over the
    setSendTime tool code surface (mirror the existing recipientBodies-never-logged + redact-before-
    draft assertions): assert no `internal.audit.log` / `deadLetter` / telemetry write inside the
    setSendTime execute path carries sendAt / the NL text; assert the send time lives only in
    patchPlan (content plane). Keep it a static source-scan (no runtime), consistent with the file.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test llmRedaction</automated>
  </verify>
  <done>llmRedaction.test.ts asserts the scheduling fields stay content-plane; green.</done>
</task>

<task type="auto">
  <name>Task 3: cockpit-agent skill "## Scheduling" section via the 5-file mirror</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - The cockpit-agent skill teaches: when the user volunteers a send time ("send at 4pm tomorrow"),
      call setSendTime; CONFIRM the resolved absolute time back to the user; on an ambiguous/past
      answer, ASK one focused question (never guess); no time mentioned → say nothing (immediate on
      approve, the default). The skill body version bumps and seedSkills publishes on change.
  </behavior>
  <action>
    RESEARCH Open Question 4 (light "## Scheduling" section — setSendTime is reactive). Edit the
    canonical packages/contracts/skills/cockpit-agent.md adding a short "## Scheduling" section
    (SUGGEST/confirm-then-ask, reason about the send time by its confirmed absolute value, never
    invent a clock). Regenerate the byte-identical derived cockpitAgentSkillBody in
    packages/contracts/src/skills/cockpitAgent.ts. Bump the seedSkills row version in skills.ts
    (append-only edit to the shared seedSkills list — PARALLELIZATION.md §"three shared singletons").
    Update the drift test.each row in skills.test.ts so the canonical .md ↔ derived .ts drift check
    stays green. Do NOT hardcode the instruction in llm.ts (§5).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test skills</automated>
  </verify>
  <done>cockpit-agent skill carries a Scheduling section; canonical .md and derived .ts are byte-identical (drift test green); seedSkills version bumped; skills.test.ts green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test cockpitTools cockpit llmRedaction skills` green (the pre-existing audit.test.ts auditCounts red is NOT a regression — do not chase it, PARALLELIZATION.md).
- `pnpm --filter @pikar/core test emailIntent` still green (parser unchanged).
- The model never receives nowMs/ianaTz from its own output — only from clientContext (§2-D).
- No hardcoded scheduling prompt in llm.ts (§5); the instruction lives in the skill row.
</verification>

<success_criteria>
The agent can accept a natural-language send time on the fast path, write it as an absolute sendAt,
and re-ask (never guess) on ambiguous/past — with the clock+zone supplied by the trusted client, not
the model. Scheduling fields stay off the log plane (§4). No scheduling yet — Approve still sends
immediately until Wave 3 adds the executePlan branch.
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-deferred-send/03.5-02-SUMMARY.md`.
Update cockpit.md §9 Last verified + the skill-registry playbook Last verified in the same change.
</output>
