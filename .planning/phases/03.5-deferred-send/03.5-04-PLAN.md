---
phase: 03.5-deferred-send
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - apps/web/e2e/cockpit-schedule.spec.ts
  - docs/playbooks/cockpit.md
autonomous: false
requirements: [SCHD-01]

must_haves:
  truths:
    - "The offline E2E drives sendTime= → the PLAN card shows the resolved absolute time before the single Approve → Approve → ScheduledCard 'Scheduled for…' → Cancel → 'Canceled', with nothing sent (SC2/SC3/SC4)"
    - "smoke:fanout is unchanged and still proves the governed spine the scheduled fire reuses (SC3)"
    - "A real future send fires at the requested moment; a real Cancel halts it before fire (audited); a token dead at fire → awaiting_reauth + notification exactly like an immediate send (SC3/SC4 — the live-only proof)"
    - "cockpit.md records the deferred-send invariants and its Last verified is bumped to close the phase (§9)"
  artifacts:
    - path: "apps/web/e2e/cockpit-schedule.spec.ts"
      provides: "The SMOKE sendTime= schedule/cancel E2E — Playwright-discovered, type-loads"
      contains: "sendTime="
    - path: "docs/playbooks/cockpit.md"
      provides: "Deferred-send invariants + Last verified → 03.5-04 (phase close)"
      contains: "03.5-04"
  key_links:
    - from: "cockpit-schedule.spec.ts"
      to: "the picker + ScheduledCard + Cancel surface (Wave 3)"
      via: "SMOKE::agent::sendTime= → picker shows abs time → Approve → ScheduledCard → Cancel"
      pattern: "ScheduledCard|Scheduled for"
    - from: "the SCHD-01 human-verify"
      to: "a live Gmail-connected backend"
      via: "real future send fires / real cancel halts / real dead-token-at-fire → awaiting_reauth"
      pattern: "human-verify"
---

<objective>
Prove deferred send end-to-end and close the phase: an offline E2E for the schedule→cancel card
flow, the playbook update recording the new invariants, and the SCHD-01 human-verify — the sole
live-only proof that a real future send fires at the requested moment, a real cancel halts it, and a
dead token at fire degrades exactly like an immediate send.

Purpose: SC2/SC3/SC4 verification. The offline suite uses fake timers and cannot observe real inbox
arrival or a real token expiry at fire time (VALIDATION §"Manual-Only Verifications") — the
human-verify is mandatory for SCHD-01 sign-off.
Output: cockpit-schedule.spec.ts + cockpit.md phase close + the human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.5-deferred-send/03.5-CONTEXT.md
@.planning/phases/03.5-deferred-send/03.5-RESEARCH.md
@.planning/phases/03.5-deferred-send/03.5-VALIDATION.md
@docs/playbooks/cockpit.md
@.planning/phases/03.5-deferred-send/03.5-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — mirror the sibling specs. -->

E2E precedent: apps/web/e2e/cockpit-attachment.spec.ts and cockpit-personalize.spec.ts drive the
offline SMOKE::agent:: grammar through the composer, assert plan-row-derived CARDS (not agent prose),
sequence turns on the composer clearing, and defer the live green run to verify-work (needs
`next dev :3111` + a seeded E2E user via auth.setup.ts — the phase-wide convention). The Wave-2
SMOKE op is `SMOKE::agent::sendTime=<expr>` (e.g. sendTime=in 2 hours). The Wave-3 card surface:
the PLAN card shows the resolved absolute time + a datetime picker before Approve; after Approve a
ScheduledCard renders "Scheduled for <abs, tz>" + Cancel; a canceled plan shows "Canceled".

Phase-close precedent (cockpit.md "Last verified" line): each phase-close plan bumps it (03.4-04,
03.3-06) and adds the phase's invariants. watch.json ALREADY covers cockpit.ts/plans.ts/llm.ts/
cards.tsx/emailIntent.ts + the cockpit skills under cockpit.md — no watch.json edit needed (verify
check-playbooks exit 0). smoke:fanout (run-smoke-fanout.mjs) is UNCHANGED by this phase — note in
the playbook that it still proves the spine the scheduled fire reuses (SC3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cockpit-schedule.spec.ts — the offline schedule→cancel E2E</name>
  <files>apps/web/e2e/cockpit-schedule.spec.ts</files>
  <action>
    Mirror cockpit-personalize.spec.ts. Drive the composer: `add=alice@example.com` →
    `subject=Q3 sync` → `body=SMOKE::route=direct_llm::…` → `sendTime=in 2 hours` → assert the PLAN
    card shows a resolved ABSOLUTE send time (a concrete date/time string, user tz) + the datetime
    picker BEFORE the single Approve (SC2), and that nothing is sent. Then Approve → assert the
    ScheduledCard renders "Scheduled for …" with a Cancel control (status scheduled, NOT a REPORT).
    Then click Cancel → assert the card shows "Canceled" and NO REPORT/send occurred. Assert
    plan-row-derived cards (not agent prose); sequence turns on the composer clearing. Playwright-
    discovered + type-loads; the live green run is deferred to verify-work (phase-wide convention).
    Also add a second minimal path assertion: no sendTime given → Approve → REPORT (immediate default
    unchanged, SC1) — reuse the cockpit-report.spec.ts shape if trivial, else a short case here.
  </action>
  <verify>
    <automated>pnpm --filter web exec playwright test cockpit-schedule --list</automated>
  </verify>
  <done>cockpit-schedule.spec.ts type-loads and is Playwright-discovered (>=1 test); it drives sendTime= → picker abs time pre-Approve → Approve → ScheduledCard → Cancel → Canceled with nothing sent, plus the no-time immediate default.</done>
</task>

<task type="auto">
  <name>Task 2: Phase close — cockpit.md deferred-send invariants + Last verified</name>
  <files>docs/playbooks/cockpit.md</files>
  <action>
    Update cockpit.md (§9 definition-of-done): bump "Last verified" to 03.5-04 and add the deferred-
    send invariants under "Invariants — what must never break":
    - "Approve SCHEDULES on a future sendAt (SCHD-01)": executePlan seeds requests AT APPROVE (content
      frozen — approve once), sets status scheduled, arms ctx.scheduler.runAt(sendAt,
      startScheduledDelivery), stores scheduledFunctionId; nothing sends before fire. sendAt unset or
      <= now → the immediate synchronous startFanout (today's default, SC1).
    - "The single workflow.start call site holds via startFanout": both the immediate branch and
      startScheduledDelivery call the shared startFanout helper; git grep 'workflow.start(' stays at
      exactly one hit.
    - "Cancel is CAS-guarded and refs-only (SC4)": cancelScheduledPlan proceeds only on
      status==="scheduled" (scheduler.cancel throws on an already-fired id), flips to canceled, writes
      a refs-only plan.canceled audit (payload {planId} only, §4), idempotent + tenant-guarded.
    - "Fire-time failure is inherited, not re-implemented (SC4)": startScheduledDelivery starts the
      SAME deliverApprovedPlan, so a dead token at fire → awaiting_reauth/DLQ/telemetry are the
      identical immediate-send code.
    - Add a "How to verify" line for the fake-timer scheduler tests + cockpit-schedule.spec.ts, and
      note smoke:fanout is UNCHANGED (it still proves the spine the scheduled fire reuses). Add the
      SCHD-01 human-verify to the Manual-only section. Update Key files (cockpit.ts startFanout/
      startScheduledDelivery/cancelScheduledPlan, plans.ts setPlanSendTime, cards.tsx picker/
      ScheduledCard, emailIntent.ts parseSendTime).
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>cockpit.md carries the deferred-send invariants + Last verified → 03.5-04; check-playbooks exits 0 (no watch.json edit needed — paths already covered).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SCHD-01 human-verify — real future send fires / real cancel halts / dead token at fire</name>
  <what-built>
    Deferred send end-to-end on the governed spine: a natural-language send time (fast path) + a
    PLAN-card datetime picker (confirm), Approve SCHEDULES the existing deliverApprovedPlan via
    ctx.scheduler.runAt (nothing sent before fire), a ScheduledCard with Cancel (CAS-guarded, audited
    halt), and fire-time failure inherited from the immediate path (awaiting_reauth).
  </what-built>
  <action>
    This is a human-verification checkpoint (no code). Ensure the live Lane-A backend is running
    (own `npx convex dev` + `next dev :3111`, Gmail connected, `npx convex run skills:seedSkills`),
    then walk the operator through the how-to-verify steps below and record the outcome.
  </action>
  <how-to-verify>
    On the live Gmail-connected Lane-A worktree backend (own `npx convex dev` + `next dev :3111`,
    Gmail connected, skills seeded):
    1. Compose to a real inbox; give a subject + body; say "send this in 3 minutes" (or use the PLAN-
       card picker). Confirm the PLAN card shows the RESOLVED ABSOLUTE time in your timezone BEFORE
       Approve, and that an ambiguous time ("send at 4") is RE-ASKED, never guessed.
    2. Approve ONCE. Confirm the card becomes "Scheduled for <abs time> · Cancel" and that NOTHING
       arrives in the inbox before the scheduled minute.
    3. Let it fire. Confirm the email ARRIVES at the requested moment and the card transitions to the
       live REPORT. Inspect audit/deadLetters/telemetry for refs-only (no raw subject/body/recipient,
       no sendAt content — §4).
    4. Separately: schedule another send a few minutes out, then click Cancel BEFORE it fires. Confirm
       NOTHING is sent, the card shows "Canceled", and a refs-only plan.canceled audit row exists.
    5. (Dead-token-at-fire) With an expired/revoked Gmail token, let a scheduled send fire. Confirm it
       lands at awaiting_reauth + a reauth notification — exactly like an immediate send — never a
       silent loss.
  </how-to-verify>
  <resume-signal>Type "approved" once all five hold, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter web exec playwright test cockpit-schedule --list` discovers the spec (type-loads).
- `node scripts/check-playbooks.mjs` exits 0.
- SCHD-01 human-verify approved on the live backend (the sole live-only proof — fake timers cannot
  observe real inbox arrival or real token expiry at fire).
- smoke:fanout unchanged (git diff of run-smoke-fanout.mjs empty for this plan).
</verification>

<success_criteria>
Deferred send is proven: offline the card flow schedules and cancels with nothing sent; live a real
future send fires at the requested moment, a real cancel halts it (audited refs-only), and a dead
token at fire degrades to awaiting_reauth + notification exactly like an immediate send. The playbook
records the invariants and the phase is closed (SCHD-01 satisfied + human-verified).
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-deferred-send/03.5-04-SUMMARY.md`.
The orchestrator runs verify_phase_goal + marks Phase 3.5 complete after the human-verify sign-off.
</output>
