---
phase: 19-contacts-crm-follow-ups
plan: 06
type: execute
wave: 5
depends_on: ["19-02", "19-05"]
files_modified:
  - packages/core/src/actionType.ts
  - packages/core/src/actionType.test.ts
  - packages/core/src/contacts.ts
  - packages/core/src/contacts.test.ts
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/cockpitTools.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
  - docs/playbooks/contacts-crm.md
autonomous: true
requirements: [ACTN-05]

must_haves:
  truths:
    - "A CRM write reaches the user as its own plan card showing exactly what will be written"
    - "Approving that card applies every operation in the list, or none of them"
    - "Approving it sends no email — no requests rows, no fan-out, no Gmail token needed"
    - "Approving it twice applies nothing a second time"
    - "The stale Phase-19 prediction in actionType.ts is corrected in the same commit that falsifies it"
  artifacts:
    - path: "packages/core/src/actionType.ts"
      provides: "crm_write as the fifth ACTION_TYPES member on the inline arm, and a corrected doc comment"
      contains: "crm_write"
    - path: "packages/backend/convex/cockpit.ts"
      provides: "_ARM_TABLE entry + the inline arm's crm_write branch"
      contains: "crm_write"
    - path: "packages/backend/convex/schema.ts"
      provides: "plans.kind widened + the operation list on the plan row"
      contains: "crm_write"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "The CRM plan card branch, ahead of the email chrome"
  key_links:
    - from: "packages/backend/convex/schema.ts plans.kind"
      to: "packages/backend/convex/plans.ts patchPlan"
      via: "the hand-maintained union mirror, widened in the SAME edit"
      pattern: "crm_write"
    - from: "packages/backend/convex/cockpit.ts executePlan"
      to: "packages/backend/convex/contacts.ts"
      via: "the inline arm applying the operation list transactionally"
      pattern: "case \"inline\""
---

<objective>
`crm_write` becomes the fifth `ACTION_TYPES` member on the `inline` arm, landed across all eleven
registration sites in ONE commit, and the stale prediction that said this phase would use a
different arm is corrected by the commit that falsifies it.

Purpose: SC#1 — "a CRM write stages into the plan and executes only via the human Approve gate".
Adding the member is a DELIBERATE compile error at both `satisfies` binds; that is the guarantee
the machinery was built for. Do not route around it.

Output: the member, both arm tables, the widened `plans.kind` and its hand-maintained `patchPlan`
mirror, the inline arm's applier, the plan card, and the tests.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-02-SUMMARY.md
@.planning/phases/19-contacts-crm-follow-ups/19-05-SUMMARY.md
@.planning/phases/20-media-canvas/20-07-PLAN.md
@CLAUDE.md
@packages/core/src/actionType.ts
@packages/backend/convex/cockpit.ts

<interfaces>
<!-- Verified from source 2026-08-09. RE-VERIFY BY SYMBOL, never by number. -->

`packages/core/src/actionType.ts`:
```ts
export const ACTION_TYPES = ["email", "memo", "calendar_event", "media"] as const;   // :8
export type ActionType = (typeof ACTION_TYPES)[number];
export const actionTypeOf = (kind: "memo" | "calendar_event" | "media" | undefined): ActionType => …  // :13
export type Arm = "workflow" | "inline" | "externalAction";                          // :42
const ARMS = {                                                                        // :48
  email: "workflow", memo: "inline", calendar_event: "externalAction", media: "externalAction",
} as const satisfies Record<ActionType, Arm>;
export const armFor = (t: ActionType): Arm => ARMS[t];                                // :55
```
**The stale prediction is at lines 39-41** (CONTEXT.md's "36-38" is off by three; the file drifted):
> "Phases 18 (document creation) and 19 (CRM writes) are the same mechanism — one governed
> external side effect driven by the retrier, not a DB write and not a fan-out — so they reuse
> this arm rather than adding a fourth."

`packages/backend/convex/cockpit.ts`:
```
:565  const _ARM_TABLE = { … }            ← the SECOND satisfies bind, separate on purpose
:579  type ExternalActionType = { [K in ActionType]: (typeof _ARM_TABLE)[K] extends "externalAction" ? K : never }
:609  const EXTERNAL_TARGETS = { … }      ← DERIVED. An `inline` member needs NO entry.
:683  case "inline": {                    ← the memo arm, the exact template
```
The memo arm, verbatim (`cockpit.ts:683`):
```ts
case "inline": {
  // memo (12-05 BEVL-02): Approve means SAVE, not send — one transactional write, so a
  // workflow would add rows and latency for nothing. Nothing below (seed requests →
  // startFanout → gmail.send) is reachable from here; the double-approve CAS above already
  // makes it exactly-once.
  await ctx.db.patch(planId, { status: "done" });
  await persistNextStepMemo(ctx, plan);
  return { ok: true };
}
```
The arm runs AFTER the `plan.status !== "proposed"` CAS and the `escalated` guard, and BEFORE the
Gmail pre-check — deliberately, "because a memo must not require a connected Gmail". `crm_write`
inherits that for free.

`packages/backend/convex/schema.ts:288`:
```ts
kind: v.optional(v.union(v.literal("memo"), v.literal("calendar_event"), v.literal("media"))),
```
`packages/backend/convex/plans.ts:441` — the HAND-MAINTAINED mirror of that same union inside
`patchPlan`'s args (`plans.ts:20-21` explains why it is hand-maintained: the `PLAN_STATUS`
Pitfall-5 lesson).

`apps/web/.../workspace/cards.tsx`: the `memo` branch is around `:322`; `const hasDraft = …` is
around `:2288`.

`dispatchGuard.test.ts`'s "externalAction arm wires each occupant's OWN retrier action" scan
enumerates only `externalAction` occupants, so an `inline` member does not touch it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The fifth member, its arm, the corrected comment, and the operation-list contract</name>
  <files>packages/core/src/actionType.ts, packages/core/src/actionType.test.ts, packages/core/src/contacts.ts, packages/core/src/contacts.test.ts</files>
  <behavior>
    In `actionType.test.ts` (VALIDATION row 1) — extend, do not rewrite:
    - Test: `ACTION_TYPES` equals EXACTLY `["email","memo","calendar_event","media","crm_write"]`
      (an exact-array assertion, 20-07 Task 1's shape — not a `.includes`).
    - Test: `armFor("crm_write") === "inline"`.
    - Test: the four existing arm assertions are UNCHANGED and still pass.

    In `contacts.test.ts`, the pure operation-list contract:
    - Test: `parseCrmOperations` accepts a list of `{ op: "addContact", email, name?, origin }`,
      `{ op: "addFollowUp", email, title, dueAt }`, `{ op: "completeFollowUp", followUpRef }`,
      `{ op: "cancelFollowUp", followUpRef }` and normalizes every email through
      `normalizeAddress`.
    - Test: an EMPTY list is REFUSED (a CRM plan with nothing to apply is a bug, not a no-op).
    - Test: `addFollowUp` with no `dueAt` is REFUSED (due date is required).
    - Test: `addFollowUp` with NO contact reference is REFUSED — **the agent must ALWAYS name a
      contact.** Contactless follow-ups are a USER-only capability; this refusal is the structural
      brake against the CRM quietly becoming a general task generator.
    - Test: a list over a sane ceiling (pick one, e.g. 25 ops) is REFUSED.
  </behavior>
  <action>
    **`packages/core/src/actionType.ts` — four edits, all in this commit:**
    1. `ACTION_TYPES` += `"crm_write"`.
    2. `actionTypeOf`'s param type → `"memo" | "calendar_event" | "media" | "crm_write" | undefined`
       (otherwise the `plans.kind` widening breaks its call site in `cockpit.ts`).
    3. `ARMS` += `crm_write: "inline"` — a COMPILE error until you do it. That is the guarantee.
    4. **Correct the doc comment at lines 39-41** (verify by symbol first — the file drifts).
       The correction must state: the prediction FAILED for Phase 18 too (`ACTION_TYPES` was still
       four members and `plans.kind` was still `memo | calendar_event | media` when Phase 18
       shipped, so 18 did not take that path either), and Phase 19 builds OUR OWN `contacts` table
       — an add is *"a single transactional write"*, which is this same file's own definition of
       the `inline` arm. Keep the correction in the file's established "CORRECTED in Phase N"
       voice; do not delete the original prediction, supersede it in place as the file already
       does for the Phase-17 correction.

    **`packages/core/src/contacts.ts`** — add the `CrmOperation` discriminated union and a pure
    `parseCrmOperations(raw: unknown): CrmOperation[]` that validates and normalizes, throwing a
    named error per rule above. Pure TS, no Convex import (CLAUDE.md §1) — `followUpRef` and any
    contact reference are plain strings; the Convex adapter resolves them.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- actionType</automated>
    <automated>pnpm --filter @pikar/core test -- contacts</automated>
    <automated>pnpm --filter @pikar/core typecheck</automated>
  </verify>
  <done>VALIDATION row 1 is green. `ACTION_TYPES` has exactly five members; `armFor("crm_write") === "inline"`; the stale lines 39-41 prediction is corrected in the same file, in the same commit. `parseCrmOperations` refuses an empty list, a dateless follow-up and a contactless agent follow-up.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Both arm tables, the schema union and its mirror, and the transactional applier</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/schema.ts, packages/backend/convex/plans.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - Test (row 4): `patchPlan({ planId, kind: "crm_write" })` is ACCEPTED by the RUNTIME validator.
      This is Pitfall 1 — widen `schema.ts` but not `plans.ts:441` and every typecheck passes while
      the runtime validator rejects the new kind at the first real propose.
    - Test (row 5): approving a `crm_write` plan carrying 2 contact adds + 3 follow-up adds
      applies ALL FIVE, sets the plan to `done`, seeds ZERO `requests` rows, and never reaches
      `deliverApprovedPlan`/`gmail.send` (assert the send spy has zero calls — the
      `gapAction.test.ts` idiom).
    - Test: approving a `crm_write` plan works with NO Gmail token connected (the inline arm runs
      before the Gmail pre-check, deliberately).
    - Test (row 6): double-approve applies NOTHING a second time — after two approves there are
      still exactly 2 contacts and 3 follow-ups, and the second call is a no-op return.
    - Test (atomicity): a list whose THIRD operation is invalid applies NONE of the first two.
      A Convex mutation is one serializable transaction — assert that rather than assuming it.
    - Test: `crm_write` requires no `EXTERNAL_TARGETS` entry — `dispatchGuard.test.ts` stays green
      unmodified.
  </behavior>
  <action>
    Work TOP TO BOTTOM, ONE commit (20-07's rule — a partial landing is the Pitfall-1 trap):

    1. `cockpit.ts` `_ARM_TABLE` += `crm_write: "inline"` — the SECOND compile error.
       `EXTERNAL_TARGETS` needs NO entry: `ExternalActionType` is DERIVED from `_ARM_TABLE` and an
       `inline` member is excluded by construction. Say so in a comment so nobody adds one.
    2. `schema.ts` `plans.kind` → add `v.literal("crm_write")` to the union, still `v.optional`.
       ABSENT still means email ⇒ no migration, no backfill. Add
       `crmOperations: v.optional(v.array(v.any()))` on `plans` — content plane, validated by
       `parseCrmOperations` at BOTH the write and the apply boundary. Comment it as content-plane
       (CLAUDE.md §4: it holds names and note text and must never reach `audit.payload`).
    3. `plans.ts` `patchPlan`'s hand-maintained mirror of that union — widen it in the SAME edit,
       and widen the args to carry `crmOperations`.
    4. `cockpit.ts` `executePlan`'s `case "inline"` branches on kind, mirroring the memo arm:
       ```ts
       case "inline": {
         // Approve means APPLY, not send. A Convex mutation is serializable, so a multi-operation
         // CRM plan is approve-all-or-none for free — no saga, no compensation, no idempotency key
         // beyond the CAS above. Nothing below (seed requests → startFanout → gmail.send) is
         // reachable from here.
         if (actionTypeOf(plan.kind) === "crm_write") {
           await applyCrmOperations(ctx, plan);
           await ctx.db.patch(planId, { status: "done" });
           return { ok: true };
         }
         await ctx.db.patch(planId, { status: "done" });
         await persistNextStepMemo(ctx, plan);
         return { ok: true };
       }
       ```
       `applyCrmOperations` calls `parseCrmOperations` on `plan.crmOperations` (validate at the
       apply boundary too — the plan row is the content plane and could have been revised), then
       performs the writes through the same `ctx.db` upserts `contacts.ts` uses. Do NOT duplicate
       the normalize/upsert logic — extract a shared helper from `contacts.ts` and import it, or
       call the internal mutation. **Three copies of the identity rule is three chances to
       disagree** (CLAUDE.md §8, ponytail rung 2).

    Run `npx convex codegen` from `packages/backend` before quoting a typecheck number.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools</automated>
    <automated>pnpm --filter @pikar/backend test -- dispatchGuard</automated>
    <automated>pnpm --filter @pikar/backend test -- plans</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>VALIDATION rows 4, 5 and 6 are green. Both `satisfies` binds compile. Mutation check: revert the `plans.ts:441` mirror alone and confirm row 4 goes RED (proving the test actually exercises the runtime validator), then restore.</done>
</task>

<task type="auto">
  <name>Task 3: The CRM plan card, the duplicate-card exclusion, and the playbook bumps</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, docs/playbooks/cockpit.md, docs/playbooks/contacts-crm.md</files>
  <action>
    Read `docs/design/BRAND.md` first (CLAUDE.md §10).

    1. Add a `if (plan.kind === "crm_write") { … }` branch **BEFORE** the email chrome, mirroring
       the `memo` branch. Every line below that point (recipients, mode, the send-time picker,
       "Send to N recipients") is a LIE on a CRM plan — that is why the branch goes first, not
       inside.
       The card shows EXACTLY what will be written: one line per operation, plain language
       ("Add contact: bob@x.com (Bob)", "Follow up with bob@x.com by Fri 15 Aug — chase the
       quote"). ONE plan, ONE Approve, all-or-none. **No per-row opt-out UI** — deferred by
       CONTEXT. **No chat confirmation message** — the receipt IS the plan reaching `done` and
       Pipeline reflecting it; a second receipt is a second thing that can disagree with the
       database.
    2. Add `plan.kind !== "crm_write"` to the `hasDraft` conjunction, or a DRAFT card prints an
       email body beside the CRM card.
    3. Tokens only. Chips get teal in the FILL (`color-mix(in srgb, var(--teal-400) 30%,
       var(--card))`) with `--ink` for the label — BRAND §6 bans `--teal-600` as small text
       (~2.9:1). ZERO `--held` amber: it is reserved for the approval gate and is spent in exactly
       one place. No component library.

    Bump `cockpit.md` (`Plan 19-06`): the fifth action type, its arm and WHY (`inline` = one
    transactional write, per that file's own definition), the eleven registration sites as a
    checklist, and Pitfall 1 (the `plans.ts` hand-maintained mirror) with the test that pins it.
    Bump `contacts-crm.md` (`Plan 19-06`): the ACTOR rule — agent-proposed writes ALWAYS stage
    through the plan gate; direct user edits on the Pipeline page are ungated, because a human
    marking their own follow-up done is not an agent act and gating it is friction with no safety
    payoff. Both CRLF — normalize and assert anchors are unique.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>pnpm --filter @pikar/backend test -- traceParity</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>A `crm_write` plan renders its own card ahead of the email chrome with no recipients/mode/send-time controls and no duplicate draft card; web typecheck and production build green; both playbooks bumped with the registration checklist and the ACTOR rule.</done>
</task>

</tasks>

<verification>
- **`pnpm test` (full turbo) — the WHOLE suite (this plan edits `cockpit.ts` and `plans.ts`).**
- `pnpm typecheck` — delta 0 vs a freshly re-measured baseline
- `pnpm --filter @pikar/web build` — green
- `node scripts/check-playbooks.mjs` — exit 0
- All eleven registration sites landed in ONE commit (list them in the summary and tick each)
</verification>

<success_criteria>
- `ACTION_TYPES` has exactly five members and `crm_write` is on the `inline` arm
- `actionType.ts`'s stale Phase-19 prediction is corrected by the commit that falsifies it
- Both `satisfies` binds and both hand-maintained unions (`schema.ts`, `plans.ts`) are widened together
- Approving a CRM plan applies the whole list atomically, sets `done`, and sends nothing
- Double-approve is a no-op; the memo/calendar/media/email arms are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-06-SUMMARY.md`
</output>
