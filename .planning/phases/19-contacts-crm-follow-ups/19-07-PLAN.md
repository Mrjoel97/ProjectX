---
phase: 19-contacts-crm-follow-ups
plan: 07
type: execute
wave: 6
depends_on: ["19-02", "19-06"]
files_modified:
  - packages/backend/convex/contacts.ts
  - packages/backend/convex/contacts.test.ts
  - apps/web/app/(app)/dashboard/pipeline/page.tsx
  - apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx
  - apps/web/app/(app)/dashboard/pipeline/pipelineView.test.ts
  - apps/web/e2e/pipeline.spec.ts
  - docs/playbooks/contacts-crm.md
  - docs/playbooks/dashboard-pages.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "A user can open /dashboard/pipeline and see four counts, a contact table and their unassigned follow-ups"
    - "On a brand-new empty tenant all four tiles read 0 — never a dash, never Unknown"
    - "A contact with no name shows its address, not a blank row"
    - "A user can mark a contact suppressed from the table and the send guard honours it immediately"
    - "The page reads the Phase 19 substrate and nothing else — no second CRM store, no stage, no money"
  artifacts:
    - path: "packages/backend/convex/contacts.ts"
      provides: "pipelineTiles, listContacts (bounded), listUnassignedFollowUps"
      exports: ["pipelineTiles", "listContacts", "listUnassignedFollowUps"]
    - path: "apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx"
      provides: "The whole connected page: four tiles, five-column table, unassigned section, row actions"
      min_lines: 200
    - path: "apps/web/app/(app)/dashboard/pipeline/pipelineView.test.ts"
      provides: "The empty-state zero assertions — .test.ts, NOT .test.tsx"
    - path: "apps/web/e2e/pipeline.spec.ts"
      provides: "The authored browser gate, RUN by the owner at 19-10"
  key_links:
    - from: "apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx"
      to: "packages/backend/convex/contacts.ts"
      via: "one useQuery per section so a single failing read cannot erase the page"
      pattern: "useQuery\\(api\\.contacts\\."
    - from: "packages/backend/convex/contacts.ts"
      to: "packages/core/src/dashboard.ts"
      via: "createDashboardBound / dashboardCursorFor — the 26-01 bounded-read contract"
      pattern: "createDashboardBound"
---

<objective>
SC#8: a narrow connected Pipeline route over the ONE substrate — four tiles, five columns, an
unassigned-follow-ups section, three row actions — that invents no opportunity, no stage, no
monetary value and no second CRM data plane.

Purpose: PIPE-01's central worry is a second CRM store. This page has no state of its own: every
number on it is derived from `contacts`, `followUps`, `suppressions` and the existing `requests`
rows. The nav item stays `soon: true` — **26-18 flips it, not this phase.** Phase 19 ships the
connected route reachable by URL, exactly as 26-10 Task 1 shipped Finance.

Output: three read models, `page.tsx` + `PipelineView.tsx` + `pipelineView.test.ts`, the authored
e2e spec, and three playbook bumps.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-02-SUMMARY.md
@CLAUDE.md
@docs/design/BRAND.md
@packages/core/src/dashboard.ts
@apps/web/app/(app)/dashboard/finance/FinanceView.tsx
@apps/web/app/(app)/dashboard/finance/page.tsx
@docs/playbooks/dashboard-pages.md

<interfaces>
<!-- Verified from source 2026-08-09. -->

26-01 contracts (`packages/core/src/dashboard.ts`) — consume, do not re-implement:
```ts
createDashboardBound({ returned, limit, nextCursor, partial, partialReason })  // enforces nextCursor ⇒ partial, partial ⇔ partialReason
DashboardPartialReason = "row-cap" | "time-cap" | "legacy-window" | "coverage-gap" | "source-unavailable"   // CLOSED — do not invent one
compareDashboardOrder / dashboardCursorFor / parseDashboardCursor            // newest-first + a validated "v1:" cursor
DASHBOARD_STATE_COPY                                                         // code-owned page-state prose
resolveDashboardWindow / DashboardWindow                                     // throws on an inverted or uncovered window
DashboardMoney / createDashboardMoney                                        // ★ DO NOT IMPORT — PIPE-01 forbids monetary pipeline values
```

26-10's shape (`apps/web/app/(app)/dashboard/finance/`): `page.tsx` is ~5 lines returning
`<FinanceView />`; `FinanceView.tsx` is `"use client"` and holds the whole page; tests are
`financeView.test.ts`. **Component tests are `.test.ts`, NOT `.test.tsx` — `apps/web`'s vitest
includes `app/**/*.test.ts` ONLY and a `.tsx` is SILENTLY SKIPPED.** `apps/web/vitest.config.mts`
sets `esbuild: { jsx: "automatic" }`.

CSS reality: only `stat-grid`, `stat-tile`, `stat-head`, `stat-badge`, `stat-value` and
`caps-label` exist in `globals.css`. `.card`, `.meter`, `.pill`, `.btn`, `.sec`, `.bars` are
MOCKUP-ONLY. **`.ledger` IS defined but is the DARK marketing audit block from the landing page** —
the Pipeline mockup uses `<table class="ledger">`, which would render the table on a navy panel.
Use inline `CSSProperties` over `var(--card)` / `var(--rule)` / `var(--canvas)` / `var(--ink)` /
`var(--ink-soft)` — the ApprovalsView/FinanceView idiom.

`requests` indexes (`schema.ts`): `by_tenant_status`, `by_tenant_status_createdAt`,
`by_correlation`, `by_tenant_safeTextHash`, `by_plan`.

Nav (`apps/web/app/(app)/layout.tsx:49`): `{ label: "Sales Pipeline", icon: <TrendIcon />, soon: true }`
with the authoritative comment at `:43-46`: *"The branch below keys off `href`, not `soon`, so
adding the href IS the activation. Rollback is deleting the href."* **DO NOT ADD THE HREF.**

The corrected mockup (`docs/design/mockups/pending-pages.html:863-905`): four tiles —
*Contacts needing attention · Follow-ups due · Consent on record · Suppressed contacts* — and five
columns — *Contact · Origin · Last touch · Next step · Consent*. The two `<div class="note dead">`
gravestones carry the tie-breaker: **"The written requirement wins over an unbuilt mockup."**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The three read models, bounded by the 26-01 contracts</name>
  <files>packages/backend/convex/contacts.ts, packages/backend/convex/contacts.test.ts</files>
  <behavior>
    - Test (row 19 backend half): on an EMPTY tenant, `pipelineTiles` returns
      `{ needingAttention: 0, followUpsDue: 0, consentOnRecord: 0, suppressed: 0 }` — real
      numbers, never `null`, never `undefined`. Contacts and follow-ups have NO coverage-start
      concept, so `Unknown` is never the truth here.
    - Test: `needingAttention` counts contacts with NO OPEN follow-up (deliberately COMPLEMENTARY
      to `followUpsDue`, not a restatement of it). A contact whose only follow-up is `done` or
      `canceled` IS needing attention.
    - Test: `followUpsDue` counts CONTACTLESS follow-ups too — one honest total, no asterisk.
    - Test: `consentOnRecord` counts only contacts with a non-empty `consentAt`. Nothing is
      defaulted to consented.
    - Test (row 21): `listContacts` returns a `createDashboardBound`-shaped result;
      `nextCursor` implies `partial === true` and `partialReason` is a member of the CLOSED
      `DashboardPartialReason` vocabulary. Assert with more rows than the limit so the cursor
      actually appears — a bound test under the limit is vacuous.
    - Test: `listContacts` round-trips its cursor through `dashboardCursorFor`/`parseDashboardCursor`
      and page 2 contains no row from page 1.
    - Test: `lastTouchAt` is the newer of (the newest DELIVERED send to that address) and (the
      newest COMPLETED follow-up for that contact); a contact with neither returns `null`, which
      the component renders as an explicit "no contact yet", not `0` and not `Unknown`.
    - Test (isolation): all three read models return empty/zeroes for a foreign tenant (extend the
      existing `asA`/`asB` block; every new public function belongs in it).
  </behavior>
  <action>
    Append three `tenantQuery` read models to `packages/backend/convex/contacts.ts`:

    - `pipelineTiles()` → the four counts above.
    - `listContacts({ cursor?, limit? })` → newest-first bounded rows carrying
      `{ contactId, email, name, origin, lastTouchAt, nextStep, consent }` where `nextStep` is the
      soonest OPEN follow-up (title + dueAt) or `null`, and `consent` is
      `{ at, source } | null`. Bound it with `createDashboardBound`; order with
      `compareDashboardOrder`; page with `dashboardCursorFor`/`parseDashboardCursor`. Do NOT
      hand-roll a `limit`/`offset` pair — `nextCursor ⇒ partial` and `partial ⇔ partialReason` are
      enforced by the helper and drift if re-implemented.
    - `listUnassignedFollowUps({ cursor?, limit? })` → OPEN follow-ups with no `contactId`,
      same bound shape.

    **"Last touch" (Open Question 4 — RESOLVED HERE).** Read `requests` ONCE per page through
    `by_tenant_status_createdAt` (status `"sent"`) and fold in memory against the contact set,
    plus the newest `completedAt` from `by_tenant_contact`. No denormalized `contacts.lastTouchAt`
    field — that would be a write-path obligation this phase does not otherwise have.
    `// ponytail: one bounded requests scan folded in memory, beta scale. Upgrade path when the
    scan stops being cheap: a denormalized contacts.lastTouchAt written by recordDeliveryTerminal
    and setFollowUpStatus — a write-path obligation, so don't take it until the read hurts.`
    Bound that scan too (it is a read model; an unbounded scan is the thing 26-01 exists to
    prevent) and set `partialReason: "row-cap"` if it truncates.

    Return TYPED FACTS, never display prose (`DASHBOARD_STATE_COPY` is code-owned and lives on the
    component side). Do NOT import `DashboardMoney`/`createDashboardMoney` — importing it would be
    a signal in the wrong direction (PIPE-01). No `stage`, no `amountCents`, no opportunity concept:
    the structural scan from 19-02 covers this file and must stay green.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- contacts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>VALIDATION row 21 is green and the empty-tenant tile counts are real zeroes. The 19-02 structural scan still passes over the extended file. All three read models appear in the `asA`/`asB` isolation block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The connected Pipeline route</name>
  <files>apps/web/app/(app)/dashboard/pipeline/page.tsx, apps/web/app/(app)/dashboard/pipeline/PipelineView.tsx, apps/web/app/(app)/dashboard/pipeline/pipelineView.test.ts</files>
  <behavior>
    In `pipelineView.test.ts` (**`.test.ts`, NOT `.test.tsx` — a `.tsx` is silently skipped**):
    - Test (row 19): with all four counts at 0, the rendered tiles contain the string `"0"` four
      times and contain NEITHER `"—"` NOR `"Unknown"` anywhere in the tile region. Assert the
      ABSENCE explicitly — this is the 26-10 defect (commit `1a63992`) stated from the other side.
    - Test: with zero contacts the table is replaced by the empty-state explanation plus exactly
      ONE action ("Add your first contact") — and no seeded suggestions from recent mail.
    - Test: a contact with `name: undefined` renders its EMAIL in the Contact column, not a blank
      and not "Unknown".
    - Test: a contact with `consent: null` renders "none on record" — the truth, not "consented".
    - Test: `lastTouchAt: null` renders an explicit no-contact-yet string, distinct from `0`.
    - Test: contactless follow-ups render in their OWN section BENEATH the table, and the contact
      table contains no em-dash contact row (one row per person, exactly as the mockup has it).
    - Test: the un-suppress control is a two-step arm/commit and calls NO `window.confirm`.
  </behavior>
  <action>
    Read `docs/design/BRAND.md` first (CLAUDE.md §10). Copy 26-10's three-file shape exactly:
    `page.tsx` is ~5 lines returning `<PipelineView />`; `PipelineView.tsx` is `"use client"` and
    holds the page.

    **Four tiles** (mockup order): Contacts needing attention · Follow-ups due · Consent on record ·
    Suppressed contacts. Use the REAL classes — `stat-grid`, `stat-tile`, `stat-head`,
    `stat-value`, `caps-label`. **A real zero renders as `0`.** There is no `Unknown` state on this
    page and no coverage clamp: the substrate is user-created, so "we weren't watching" cannot
    apply. Do not import `resolveDashboardWindow` for the tiles — they are not windowed.

    **Five columns:** Contact · Origin · Last touch · Next step · Consent. Do NOT use
    `<table class="ledger">` — `.ledger` is the DARK marketing block and would render the table on
    a navy panel. Inline `CSSProperties` over `var(--card)`/`var(--rule)`/`var(--ink)`/
    `var(--ink-soft)`, the ApprovalsView/FinanceView idiom. BRAND §5's "executive report card":
    aligned columns, ruled sections, card-native whitespace, no gridlines. Origin and Consent are
    chips with teal in the FILL (`color-mix(in srgb, var(--teal-400) 30%, var(--card))`) and
    `--ink` for the label — BRAND §6 bans `--teal-600` as small text. **ZERO `--held` amber**: it
    is reserved for the approval gate.

    **Contactless follow-ups get their OWN section beneath the table**, not em-dash rows inside it.

    **Row actions: read + mark suppressed + add follow-up. That is all** — full inline contact
    editing is deferred. Marking suppressed calls `markSuppressed` and is honoured by the send
    guard immediately (19-05). Un-suppressing uses an in-component arm/commit two-step whose armed
    state states that re-subscribing without fresh consent is the user's responsibility.
    **NEVER `window.confirm`** — a browser modal blocks the Playwright spec.

    **Every section owns its own `useQuery`** so one failing read cannot erase the rest of the
    page (the 26-10 idiom). Take loading/empty/error prose from `DASHBOARD_STATE_COPY`, not from
    component-local strings — backends return typed facts, never display prose.

    **Do NOT touch `apps/web/app/(app)/layout.tsx`.** The nav stays `soon: true`; adding the
    `href` IS the activation and 26-18 owns it. The route is reachable by URL only.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web test -- pipelineView</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>node -e "const fs=require('fs'); const p='apps/web/app/(app)/layout.tsx'; if(fs.readFileSync(p,'utf8').includes('/dashboard/pipeline')) throw new Error('nav flipped — 26-18 owns that, not Phase 19'); console.log('ok')"</automated>
  </verify>
  <done>VALIDATION row 19 is green. The test file is `.test.ts` and vitest actually reports its cases (a zero-test run means the file was skipped — check the count, not just the exit code). No `.ledger`, no `window.confirm`, no `--held`, no nav href.</done>
</task>

<task type="auto">
  <name>Task 3: The authored browser gate and three playbook bumps</name>
  <files>apps/web/e2e/pipeline.spec.ts, docs/playbooks/contacts-crm.md, docs/playbooks/dashboard-pages.md, docs/playbooks/cockpit.md</files>
  <action>
    Author `apps/web/e2e/pipeline.spec.ts` (already registered in `watch.json` under
    `dashboard-pages.md`; do NOT add it to `contacts-crm.md`'s list). It is AUTHORED here and RUN
    by the owner at 19-10 — the 18-07 → 18-09 precedent. Put that fact in the spec's own header
    comment, including that `playwright.config.ts` pins `127.0.0.1:3111` with NO `webServer` block,
    so it needs a live `convex dev` (not `--once`), Next on :3111, and
    `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` an executor cannot mint. **A `--list` is NOT a run, and a
    blank result means NOT RUN, never that it passed** (26-05 and 26-10 both stopped at
    `auth.setup.ts` for want of these).

    The spec drives: `/dashboard/pipeline` loads; the four tiles are present with numeric values;
    the contact table renders; the unassigned-follow-ups section renders; mark-suppressed moves a
    contact into the suppressed count; the un-suppress arm/commit takes two clicks. Add a SECOND
    test pinning the EMPTY-tenant state, which is what makes the first non-vacuous. Use stable
    `data-testid`s added in Task 2.

    Playbook bumps (`Plan 19-07`):
    - `contacts-crm.md`: the page's read models and the invariant that all four tiles are
      ALWAYS-KNOWN counts — a real zero renders as `0`, never `—`, never `Unknown` — with the
      component test that pins it. Also record the "Last touch" ponytail ceiling and its upgrade
      path, and that the nav item is deliberately NOT flipped here (26-18 owns it).
    - `dashboard-pages.md`: the Pipeline route exists and is URL-reachable with the nav still
      `soon: true`; the e2e spec is authored and NOT YET RUN; `.ledger` is the dark marketing block
      and must not be used for a data table.
    - `cockpit.md`: it watches `apps/web/e2e/`, so bump its `Last verified` for the new spec —
      nothing more.
    All three are CRLF — normalize and assert anchors are unique before writing. Bump them
    regardless of `check-playbooks.mjs`'s exit code.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec playwright test --list e2e/pipeline.spec.ts</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>node -e "const s=require('fs').readFileSync('apps/web/e2e/pipeline.spec.ts','utf8'); if(!s.includes('NOT RUN')) throw new Error('spec header must state it has never executed'); console.log('ok')"</automated>
  </verify>
  <done>The spec is `--list`-discoverable with at least two tests and its header states plainly that it has never executed and what a blank result means. All three playbooks bumped.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- contacts` — green (rows 20, 21 still)
- `pnpm --filter @pikar/web test -- pipelineView` — green with a NON-ZERO test count (row 19)
- `pnpm --filter @pikar/web build` — green
- `pnpm test` (full turbo) + `pnpm typecheck` — no regression
- `node scripts/check-playbooks.mjs` — exit 0
- `grep -n "dashboard/pipeline" "apps/web/app/(app)/layout.tsx"` — NO hit
- `grep -rn "amountCents\|opportunit\|\bstage\b" "apps/web/app/(app)/dashboard/pipeline/"` — no hit
</verification>

<success_criteria>
- `/dashboard/pipeline` is connected, tenant-safe and bounded, reachable by URL with the nav unflipped
- All four tiles read `0` on an empty tenant and `Unknown` does not exist as a state on the page
- The page derives every number from the Phase 19 substrate — no second CRM store, no stage, no money
- The e2e spec is authored, discoverable, and honestly marked as never executed
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-07-SUMMARY.md`
</output>
