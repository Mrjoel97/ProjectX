---
phase: 19-contacts-crm-follow-ups
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - packages/backend/convex/tenantProfile.ts
  - packages/backend/convex/tenantProfile.test.ts
  - apps/web/app/(app)/dashboard/profile/ShapePanel.tsx
  - apps/web/app/(app)/dashboard/profile/page.tsx
  - docs/playbooks/onboarding.md
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "A user can set and see their physical postal address on /dashboard/profile"
    - "Saving an empty or whitespace-only postal address is refused at the write boundary, not silently stored"
    - "An existing tenant with no postal address is still a valid tenant — nothing forces re-onboarding"
  artifacts:
    - path: "packages/backend/convex/tenantProfile.ts"
      provides: "postalAddress read/write through the existing tenantQuery/tenantMutation surface"
      contains: "postalAddress"
    - path: "packages/backend/convex/tenantProfile.test.ts"
      provides: "Write-boundary validation + the not-required-for-onboarding assertion"
  key_links:
    - from: "apps/web/app/(app)/dashboard/profile/"
      to: "packages/backend/convex/tenantProfile.ts"
      via: "useMutation on the existing save path"
      pattern: "postalAddress"
---

<objective>
Give the CAN-SPAM postal address a place to be typed and a write boundary that refuses an empty
one — without making it a required onboarding field.

Purpose: SC#6 fails closed on a missing postal address (19-05 refuses the approve and throws at
`gmail.send`). That refusal is only humane if the field has a surface to point at. Phase 11
deliberately admits idea-stage users with almost nothing filled in and that decision is NOT
reopened — so this is an ENRICHMENT field on `/dashboard/profile`, exactly like the rest of the
tier facts, never an onboarding gate.

Output: `postalAddress` on the tenantProfile read/write surface, a field on the profile page,
and an `onboarding.md` bump.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-01-SUMMARY.md
@CLAUDE.md
@docs/design/BRAND.md
@packages/backend/convex/tenantProfile.ts
@apps/web/app/(app)/dashboard/profile/ShapePanel.tsx
@docs/playbooks/onboarding.md

<interfaces>
<!-- Verified from source 2026-08-09. Locate by symbol. -->

`packages/backend/convex/tenantProfile.ts`:
```ts
export const forTenant  = internalQuery({ … });   // tenantId arg, internal
export const get        = tenantQuery({ … });     // the surface the profile page reads
export const saveFacts  = tenantMutation({ … });  // the surface the profile page writes
```
`saveFacts` is where completeness is enforced (`missingSlots`/`canComplete`) — the table's own
comment (`schema.ts:1190-1196`) states that "the 'narrow' half of widen-migrate-narrow is
deliberately NEVER taken. Completeness is enforced at the WRITE boundary … not by the schema."

`packages/backend/convex/schema.ts` — `tenantProfiles.postalAddress: v.optional(v.string())`
was added by 19-01.

BRAND §6 (non-negotiable): `--teal-600` on white is ~2.9:1 — fills only, never small teal text.
Use `globals.css` variables (`var(--card)`, `var(--rule)`, `var(--ink)`, `var(--ink-soft)`),
never a hardcoded hex a token covers (CLAUDE.md §10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: postalAddress on the tenantProfile write boundary</name>
  <files>packages/backend/convex/tenantProfile.ts, packages/backend/convex/tenantProfile.test.ts</files>
  <behavior>
    - Test: `saveFacts({ postalAddress: "123 Main St, Springfield IL 62704, USA" })` then `get()`
      returns it verbatim (no reformatting, no parsing — CAN-SPAM requires a valid physical
      address, not a parsed one).
    - Test: `saveFacts({ postalAddress: "   " })` THROWS. An empty-after-trim address is refused
      at the write boundary — a footer rendering an empty address looks compliant and is not.
    - Test: `saveFacts({ postalAddress: "x".repeat(LIMIT + 1) })` THROWS (a length ceiling; pick
      a sane one such as 500 and name it as a const).
    - Test: `saveFacts` with `postalAddress` ABSENT does not clear an already-set value, and does
      not throw — the field is optional and other slots save independently.
    - Test: a tenant whose profile has NO `postalAddress` still satisfies whatever
      `missingSlots`/`canComplete` returns today — assert the EXISTING onboarding-completeness
      result is byte-unchanged with and without the field. This is the assertion that pins
      "this does NOT become a required onboarding field".
    - Test: tenant B's `get()` never returns tenant A's postal address (extend the existing
      isolation coverage in this file if present; add it if not).
  </behavior>
  <action>
    Widen `saveFacts`' args with `postalAddress: v.optional(v.string())` and store
    `postalAddress.trim()`. Validate at the boundary ONLY (that file, nowhere else): non-empty
    after trim, and under a named length ceiling. Surface `postalAddress` from `get()` (and from
    `forTenant` if `footerFor` in 19-02 reads through it — check which internal read 19-02 used
    and expose it there rather than adding a second reader).

    Do NOT add `postalAddress` to `missingSlots`, `canComplete`, or any onboarding gate. Add an
    in-file comment saying so and why: *"CAN-SPAM enrichment, not an onboarding slot. Phase 11
    deliberately admits idea-stage users with almost nothing filled in (19-CONTEXT); the send path
    fails closed on the missing field instead (19-05)."*

    Do NOT write a structured address object. `// ponytail: free-text block, not a parsed address.
    CAN-SPAM requires a valid physical postal address, not a structured one; a structured object
    invites a country/state enum this phase does not need. Upgrade path: parse at render time if a
    per-country format is ever required.`
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- tenantProfile</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>`postalAddress` round-trips through `saveFacts`/`get`; an empty-after-trim value throws; the onboarding-completeness result is provably unchanged whether or not the field is set.</done>
</task>

<task type="auto">
  <name>Task 2: The profile field, and the onboarding playbook bump</name>
  <files>apps/web/app/(app)/dashboard/profile/ShapePanel.tsx, apps/web/app/(app)/dashboard/profile/page.tsx, docs/playbooks/onboarding.md</files>
  <action>
    Add a "Postal address" input to the profile enrichment surface. Read `docs/design/BRAND.md`
    first (CLAUDE.md §10) and match the panel's EXISTING field pattern in `ShapePanel.tsx` — do
    not invent a new one, do not add a component library (BRAND §8). Use `globals.css` variables
    for every colour; hardcode no hex a token covers.

    Copy, verbatim intent: a multi-line `<textarea>` (a postal address is 2-3 lines), label
    "Postal address", and a one-line helper — *"Required by law in the footer of every email you
    send. Sending is blocked until this is set."* That sentence is the entire user-facing
    explanation of the 19-05 refusal; it is not decoration.

    Show the refusal from the write boundary inline (the panel's existing error affordance),
    never a `window.alert`. Never `window.confirm` anywhere in this tree — a browser modal blocks
    the Playwright specs (the 26-09/26-10 idiom).

    If `ShapePanel.tsx` is not the right panel once read, place it in whichever panel already
    owns free-text business facts and adjust `files_modified` in the summary — do not create a
    new panel file for one field.

    Bump `docs/playbooks/onboarding.md`: `Last verified` line + a note under invariants that
    `postalAddress` is an ENRICHMENT field, deliberately absent from `missingSlots`/`canComplete`,
    and that the enforcement of its presence lives in the SEND path (19-05), not here. Name the
    test that pins it. CRLF file — normalize and assert the anchor is unique before writing.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The profile page renders a postal-address field wired to `saveFacts`; web typecheck and production build are green; `onboarding.md` records that the field is enrichment-only and names where its absence is actually enforced.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- tenantProfile` — green
- `pnpm --filter @pikar/web typecheck` and `pnpm --filter @pikar/web build` — green
- `node scripts/check-playbooks.mjs` — exit 0
- `grep -rn "postalAddress" packages/backend/convex/onboarding.ts` — NO hit (it must not become an
  onboarding slot)
</verification>

<success_criteria>
- A postal address can be typed on `/dashboard/profile` and read back verbatim
- An empty-after-trim address is refused at the write boundary
- No onboarding gate changed: the completeness result is provably identical with and without it
- `onboarding.md` states the field is enrichment-only and names the send-path enforcement point
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-03-SUMMARY.md`
</output>
