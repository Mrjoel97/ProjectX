---
phase: 19-contacts-crm-follow-ups
plan: 13
type: execute
wave: 1
depends_on: ["19-10"]
gap_closure: true
files_modified:
  - packages/backend/convex/contacts.ts
  - packages/backend/convex/contacts.test.ts
  - docs/playbooks/contacts-crm.md
  - .planning/phases/19-contacts-crm-follow-ups/19-VERIFICATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "A signed-in tenant can reproduce one contact's exact consent wording, timestamp, source, and capture context on request"
    - "A foreign tenant and an unauthenticated caller cannot read the consent record"
    - "A contact with no consent returns null, while absent content-plane fields return explicit null fields"
    - "Reading the content-plane consent record writes no audit payload"
    - "Phase 19 verification records the code gap as closed while preserving the outstanding owner UAT/sign-off gate"
  artifacts:
    - path: "packages/backend/convex/contacts.ts"
      provides: "Bounded tenant-scoped consentRecord reader"
      exports: ["consentRecord"]
    - path: "packages/backend/convex/contacts.test.ts"
      provides: "Public-query proof for exact reproduction, tenant isolation, authentication, and audit safety"
    - path: "docs/playbooks/contacts-crm.md"
      provides: "Consent-record request-path invariant and verification commands"
    - path: ".planning/phases/19-contacts-crm-follow-ups/19-VERIFICATION.md"
      provides: "Re-verification evidence distinguishing the closed code gap from pending owner judgement"
  key_links:
    - from: "packages/backend/convex/contacts.ts"
      to: "packages/backend/convex/lib/functions.ts"
      via: "tenantQuery wrapper injects the authenticated tenant"
      pattern: "consentRecord = tenantQuery"
    - from: "packages/backend/convex/contacts.test.ts"
      to: "packages/backend/convex/contacts.ts"
      via: "convex-test calls the public api.contacts.consentRecord query"
      pattern: "api.contacts.consentRecord"
---

<objective>
Close the final code gap in Phase 19 verification: make the durable consent wording and capture
context reproducible through one bounded, tenant-scoped request path, prove it at the public query
boundary, and record honest re-verification evidence without self-approving the remaining owner UAT.

Purpose: `assertConsent` already stores the exact content-plane record, but the latest
`19-VERIFICATION.md` found that the wording and context were write-only. A point lookup is the
smallest request path that makes SC#4 true without adding bulk export scope or leaking content into
the refs-only audit plane.

Output: `consentRecord`, focused backend coverage, a contacts CRM playbook update, and refreshed
Phase 19 verification evidence.
</objective>

<context>
@.planning/phases/19-contacts-crm-follow-ups/19-VERIFICATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@packages/backend/convex/contacts.ts
@packages/backend/convex/contacts.test.ts
@docs/playbooks/contacts-crm.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bounded tenant-scoped consent record request path</name>
  <files>packages/backend/convex/contacts.ts, packages/backend/convex/contacts.test.ts</files>
  <action>
    Add `consentRecord({ contactId })` as a public `tenantQuery`. Read exactly one contact with
    `ctx.db.get`, reject missing and cross-tenant ids with the same `CONTACT_NOT_FOUND` message,
    return `null` when no consent exists, and otherwise return typed facts
    `{ at, source, wording, context }`, using explicit `null` for unrecorded content fields.

    Keep the query read-only: no audit write and no bulk/list endpoint. Test through
    `api.contacts.consentRecord` for exact byte-for-byte wording/context reproduction, timestamp and
    source, the no-consent null case, foreign-tenant refusal using a real foreign id,
    unauthenticated refusal, and audit payload absence after the read. Add the query to the public
    export/isolation surface pin.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test contacts</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>The public query returns the complete stored consent record only to its owning tenant, remains point-bounded and audit-free, and all focused backend tests/typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document the request path and re-verify the phase gap</name>
  <files>docs/playbooks/contacts-crm.md, .planning/phases/19-contacts-crm-follow-ups/19-VERIFICATION.md</files>
  <action>
    Bump the contacts CRM playbook for Plan 19-13. Document `consentRecord` beside the public read
    surface and strengthen the consent invariant with its point-lookup ceiling, tenant isolation,
    explicit-null semantics, exact-reproduction test, and audit-safety test.

    Re-run the latest Phase 19 verification conclusion against the committed request path. Change
    the SC#4 code gap from partial to verified with exact command evidence, update the must-have
    score/status accordingly, and preserve the separate remaining human owner UAT/sign-off verbatim
    as outstanding. Do not mark ACTN-05 or PIPE-01 complete in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>git diff --check -- packages/backend/convex/contacts.ts packages/backend/convex/contacts.test.ts docs/playbooks/contacts-crm.md .planning/phases/19-contacts-crm-follow-ups/19-VERIFICATION.md .planning/phases/19-contacts-crm-follow-ups/19-13-PLAN.md</automated>
  </verify>
  <done>The playbook names the shipped request path and tests, verification shows 8/8 code must-haves with the owner judgement gate still pending, and no requirement checkbox is changed.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test contacts`
- `pnpm --filter @pikar/backend typecheck`
- `node scripts/check-playbooks.mjs`
- `git diff --check` over Phase 19-owned paths
</verification>

<success_criteria>
- The latest SC#4 verification gap has a real public request path, not another stored-only field
- The reader is one-row bounded, authenticated, tenant-safe, exact, and audit-free
- The playbook documents the invariant and its ceiling
- Re-verification closes only the code gap and leaves owner browser UAT/sign-off pending
- No Finance, Graphify, Phase 25, Gmail, ROADMAP, STATE, or REQUIREMENTS path is modified by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-13-SUMMARY.md`.
</output>
