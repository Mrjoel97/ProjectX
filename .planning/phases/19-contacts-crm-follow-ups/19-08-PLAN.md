---
phase: 19-contacts-crm-follow-ups
plan: 08
type: execute
wave: 7
depends_on: ["19-06", "19-07"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/schema.ts
  - packages/backend/convex/smoke.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/traceParity.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [ACTN-05]

must_haves:
  truths:
    - "When the user names someone who is a saved contact, the agent uses the saved record instead of searching Gmail headers"
    - "A saved match brings that contact's open follow-ups into the turn without a second tool call"
    - "The agent can stage contact adds and follow-ups, and they arrive as a plan card the human approves"
    - "The agent cannot write a contact or a follow-up without the Approve gate"
    - "The new tool appears in the workspace trace — it has an agentSteps literal and a VERB entry, asserted both ways"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "contacts-first resolution in resolveContacts + the stageCrmWrite tool"
      contains: "stageCrmWrite"
    - path: "packages/backend/convex/schema.ts"
      provides: "the agentSteps.tool literal for the new tool key"
      contains: "stageCrmWrite"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "the VERB entry for the new tool key"
  key_links:
    - from: "packages/backend/convex/llm.ts buildCockpitTools"
      to: "packages/backend/convex/schema.ts agentSteps.tool"
      via: "one v.literal per tool key — a missing literal makes agentSteps:record throw an ArgumentValidationError the AI SDK SILENTLY swallows"
      pattern: "stageCrmWrite"
    - from: "packages/backend/convex/llm.ts resolveContacts"
      to: "packages/backend/convex/contacts.ts"
      via: "a saved-contact READ that never writes — the SC#7 invariant's enforcement point"
      pattern: "resolveContacts"
---

<objective>
SC#1's first half: the agent READS the person store in-loop (contacts first, Gmail headers as
fallback) and STAGES its writes through the plan gate — with all three registration surfaces
landed in one commit so the tool is actually visible in the trace.

Purpose: A saved contact is a deliberate human statement about who someone is; a Gmail header
match is an inference. Preferring the saved record is also the reason a user would bother saving
one — it visibly makes the agent faster.

Output: contacts-first precedence inside the existing `resolveContacts`, ONE new tool key with its
`agentSteps.tool` literal and `cards.tsx` VERB entry, the offline SMOKE driver, and two playbook
bumps.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-06-SUMMARY.md
@CLAUDE.md
@packages/backend/convex/llm.ts
@docs/playbooks/cockpit.md
@docs/playbooks/agent-runtime.md

<interfaces>
<!-- Verified from source 2026-08-09. Locate by symbol. -->

`packages/backend/convex/llm.ts`:
```
:1295  export function buildCockpitTools(…)     ← the tool record
:1672  resolveContacts: tool({ … })             ← the existing Gmail-header resolution tool
```
`llm.ts:1312` already notes the tool set is kept byte-identical across callers (the runner, the
test shims) — a new key must be added in that one place, not per-caller.

`packages/backend/convex/schema.ts:606-650` — `agentSteps.tool` is a CLOSED `v.union` of literals
(e.g. `v.literal("searchVault")` at `:632`). It is a §4 protection, not an oversight.
**`cockpitTools.test.ts` scans every `<name>: tool(` key and fails if any lacks a literal.**
**`traceParity.test.ts` asserts the key↔VERB relationship BOTH ways.**
`apps/web/.../workspace/cards.tsx:1477` — `const VERB: Record<string, [running, done]>`.

This has bitten the repo THREE times (`searchVault`, `recordScorecardAnswer`, `resetPlan`): a
missing literal makes `agentSteps:record` throw an `ArgumentValidationError` that the AI SDK
**silently swallows**, so the trace loses a step in production while the whole suite stays green.

`plans.candidates` / `pendingValid` / `recipientNames` (`schema.ts:203-235`) — the TRANSIENT
resolution store, wiped on pick by `clearCandidates`. **That behaviour is UNCHANGED by this plan.
`resolveContacts` must never call a contacts WRITE — that absence is the SC#7 invariant's
enforcement point and the playbook names it as such.**

From 19-06 (`@pikar/core`): `parseCrmOperations`, the `CrmOperation` union, and `crm_write` on the
`inline` arm. From 19-06 (`plans.ts`): `patchPlan` accepts `kind: "crm_write"` and `crmOperations`.

The SMOKE grammar precedent (`llm.ts:3422`): `brief=today|yesterday|week | create=<short|long>:<topic>`,
with the nested route prefix `SMOKE::agent::…::route=direct_llm::` required to keep a turn offline.
`smoke.ts` is watched by `agent-runtime.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contacts-first resolution, and the one staging tool</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - Test: with a SAVED contact matching the named person, `resolveContacts` returns that record
      and does NOT hit the Gmail-header search path (assert the header search spy has zero calls).
    - Test: with NO saved match, `resolveContacts` falls back to the existing candidates /
      resolution-card path, byte-unchanged.
    - Test: a saved match's return carries that contact's OPEN follow-ups, so "surface follow-up
      context" costs no second tool call.
    - Test (THE SC#7 pin): `resolveContacts` performs ZERO writes to `contacts` — assert the
      contacts row count is unchanged across a resolution that matched nothing, matched one, and
      matched several. Gmail header resolution NEVER writes a contact row.
    - Test: `plans.candidates`/`pendingValid` wipe-on-pick behaviour is UNCHANGED (extend the
      existing assertion rather than writing a parallel one).
    - Test: `stageCrmWrite` with a valid operation list PROPOSES a plan with `kind: "crm_write"`
      and `status: "proposed"` — and writes NOTHING to `contacts`/`followUps`. The gate is the
      only path.
    - Test: `stageCrmWrite` with a follow-up carrying no contact reference returns a refusal
      SENTENCE (never throws out of the governed loop — the 18-06 rule: `execute` always returns a
      sentence). The agent must always name a contact.
    - Test: `stageCrmWrite` with an empty list returns a refusal sentence.
  </behavior>
  <action>
    **(a) Contacts-first precedence**, inside the EXISTING `resolveContacts` tool — do not add a
    second resolution tool, which would be a second registration surface for the same job
    (ponytail rung 2). Look the name up against `contacts` first (tenant-scoped read via an
    internal query taking `tenantId` explicitly, since the tool plane passes `tenantId` — the
    `internal.vaultSources.latestCreated` precedent). On a hit, return the saved record plus its
    open follow-ups and SKIP the header search. On a miss, fall through to today's path unchanged.
    Comment the precedence and its reason: *a saved contact is a deliberate human statement; a
    header match is an inference.* And comment the write-absence: *this tool NEVER writes a
    contact row — that absence is the "no contacts cache at rest" invariant's enforcement point
    (SC#7).*

    **(b) ONE new tool key: `stageCrmWrite`.** It takes an operations array, runs
    `parseCrmOperations`, and PROPOSES a `crm_write` plan carrying the list. It applies nothing —
    `executePlan`'s inline arm does that after Approve (19-06). One tool, not two: CONTEXT locks
    "ONE plan carries a LIST of operations, applied atomically", so a `saveContact` tool and a
    `logFollowUp` tool would be two registration surfaces, two literals, two VERB entries and two
    fixtures for one governed act.

    Every refusal RETURNS a sentence. A PII refusal, a bad operation list and a contactless agent
    follow-up are all returned strings, never throws out of the governed loop (18-06's rule).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>`resolveContacts` prefers saved contacts, brings their open follow-ups, and provably writes nothing. `stageCrmWrite` proposes and never applies. Every refusal is a returned sentence.</done>
</task>

<task type="auto">
  <name>Task 2: The three registration surfaces, asserted both ways</name>
  <files>packages/backend/convex/schema.ts, apps/web/app/(app)/dashboard/workspace/cards.tsx, packages/backend/convex/traceParity.test.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <action>
    Land all three in THIS commit — a tool key with a missing `agentSteps.tool` literal works, but
    `agentSteps:record` throws an `ArgumentValidationError` the AI SDK silently swallows, so the
    trace loses a step in production while every test passes:

    1. `schema.ts` `agentSteps.tool` union += `v.literal("stageCrmWrite")`. Optional field, closed
       union — no migration.
    2. `cards.tsx` `VERB` += an entry for `stageCrmWrite`, e.g.
       `["Preparing a CRM update", "Prepared a CRM update"]`. Read `docs/design/BRAND.md` first;
       match the existing entries' voice exactly, tokens only, no new colour.
    3. Confirm `cockpitTools.test.ts`'s key-scan and `traceParity.test.ts`'s both-ways assertion
       cover the new key. If either scan enumerates keys from a hand-maintained list rather than
       from the tool record, extend that list — do not weaken the scan.

    **Mutation check (do it, then revert):** delete the `v.literal("stageCrmWrite")` and confirm
    `cockpitTools.test.ts` goes RED; delete the VERB entry and confirm `traceParity.test.ts` goes
    RED. A guard that does not go red is not a guard.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools</automated>
    <automated>pnpm --filter @pikar/backend test -- traceParity</automated>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
  </verify>
  <done>VALIDATION row 8 is green: the new tool key has an `agentSteps.tool` literal AND a `cards.tsx` VERB entry, asserted BOTH ways, and both guards were proven to go red when their half is removed.</done>
</task>

<task type="auto">
  <name>Task 3: The offline SMOKE driver and the playbook bumps</name>
  <files>packages/backend/convex/smoke.ts, packages/backend/convex/llm.ts, docs/playbooks/cockpit.md, docs/playbooks/agent-runtime.md</files>
  <action>
    Register a SMOKE op at ALL FOUR SMOKE sites (the `create=` precedent — find them by grepping
    the existing grammar, do not assume a count). Suggested grammar:
    `crm=<email>[:<followUpTitle>]`, driving `stageCrmWrite` with a deterministic operation list so
    a turn can exercise the tool with **no model call and $0**. Document the verbatim smoke string
    (including the nested `SMOKE::route=direct_llm::` prefix — without it the turn is NOT offline,
    the 18-06/18-07 lesson) in `agent-runtime.md`, because 19-09's eval fixture and 19-10's UAT
    both need it exactly.

    Bump `cockpit.md` (`Plan 19-08`): contacts-first resolution precedence and its reason; the ONE
    new tool key and its three registration surfaces with the guard that catches each omission;
    and the explicit statement that `resolveContacts` never calls a contacts write, naming it as
    the SC#7 invariant's enforcement point.
    Bump `agent-runtime.md` (`Plan 19-08`): the new SMOKE op, its verbatim string, and the fact
    that the eval fixture owed by the 18-08 override condition lands in 19-09.
    Both CRLF — normalize and assert anchors are unique. Bump regardless of the hook's exit code.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools</automated>
    <automated>pnpm --filter @pikar/backend test -- llm</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/playbooks/agent-runtime.md','utf8'); if(!s.includes('SMOKE::route=direct_llm')) throw new Error('verbatim smoke string not recorded'); console.log('ok')"</automated>
  </verify>
  <done>The SMOKE op is registered at every SMOKE site and drives `stageCrmWrite` offline at $0; both playbooks record what 19-09 and 19-10 need to run.</done>
</task>

</tasks>

<verification>
- **`pnpm test` (full turbo) — the WHOLE suite (this plan edits `llm.ts` and the trace spine).**
- `pnpm typecheck` — delta 0 vs a freshly re-measured baseline
- `pnpm --filter @pikar/web build` — green
- `node scripts/check-playbooks.mjs` — exit 0
- `grep -n "stageCrmWrite" packages/backend/convex/schema.ts "apps/web/app/(app)/dashboard/workspace/cards.tsx" packages/backend/convex/llm.ts` — a hit in all THREE
</verification>

<success_criteria>
- A saved contact beats a Gmail header match, and brings its open follow-ups with it
- `resolveContacts` provably writes nothing to `contacts` — the SC#7 invariant survives
- The agent can only STAGE a CRM write; the Approve gate is the only application path
- The new tool key exists at all three registration surfaces, with both guards proven red-able
- An offline $0 SMOKE op drives the tool end to end
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-08-SUMMARY.md`
</output>
