---
phase: 19-contacts-crm-follow-ups
plan: 05
type: execute
wave: 4
depends_on: ["19-02", "19-03"]
files_modified:
  - packages/backend/convex/cockpit.ts
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/plans.ts
  - packages/backend/convex/deliverApprovedPlan.ts
  - packages/backend/convex/cockpitTools.test.ts
  - packages/backend/convex/gmail.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "Approving a plan with one suppressed address among five sends to the other four and tells the user which address was withheld and why"
    - "A suppressed member of a GROUP send is dropped from the joined string before the send exists"
    - "When every recipient is suppressed the plan stays proposed with zero requests rows"
    - "A suppression created AFTER approve but BEFORE a scheduled fire is still refused"
    - "Every product email carries the tenant's postal address and an unsubscribe link in its bytes"
    - "A tenant with no postal address cannot approve and cannot send"
    - "The service notice sent to the user's own mailbox carries no footer"
  artifacts:
    - path: "packages/backend/convex/cockpit.ts"
      provides: "The pre-CAS postal-address refusal and the pre-join per-address suppression partition"
      contains: "all_recipients_suppressed"
    - path: "packages/backend/convex/gmail.ts"
      provides: "The unbypassable per-send suppression backstop and the footer at the buildMime call site"
      contains: "suppressed"
    - path: "packages/backend/convex/plans.ts"
      provides: "recordDeliveryTerminal widened so a post-approve suppression leaves honest counters"
  key_links:
    - from: "packages/backend/convex/cockpit.ts"
      to: "packages/backend/convex/contacts.ts"
      via: "suppressedAmong, before the group join and before the CAS patch"
      pattern: "suppressedAmong"
    - from: "packages/backend/convex/gmail.ts"
      to: "packages/backend/convex/contacts.ts"
      via: "isSuppressed + footerFor, between getForDelivery and buildMime"
      pattern: "internal\\.contacts\\.(isSuppressed|footerFor)"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      to: "executePlan's return union"
      via: "setNote on no_postal_address / all_recipients_suppressed / withheld"
      pattern: "no_postal_address"
---

<objective>
SC#5 and SC#6, at the two points where they are structurally unbypassable: the per-address
suppression drop before the group join in `executePlan`, and the per-send backstop plus the
CAN-SPAM footer inside `gmail.send`.

Purpose: The send path converges TWICE, not once. `executePlan` collapses group recipients into
ONE comma-joined string BEFORE the `requests` seed loop, so a per-address drop is only possible
before that join. And `startScheduledDelivery` re-fires a `requestIds` list frozen at approve
time — an approve-time filter alone CANNOT see a suppression created during the wait. Both are
needed and they are not redundant.

Output: two guarded call sites, one footer, an honest terminal for a post-approve suppression, the
user-facing refusals, and a `cockpit.md` bump.

**This plan touches the approve spine. The gate is the WHOLE suite, never a filtered run (20-07's
rule).**
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-02-SUMMARY.md
@.planning/phases/19-contacts-crm-follow-ups/19-03-SUMMARY.md
@CLAUDE.md
@packages/backend/convex/cockpit.ts
@packages/backend/convex/gmail.ts
@packages/backend/convex/deliverApprovedPlan.ts
@docs/playbooks/cockpit.md

<interfaces>
<!-- Verified from source 2026-08-09. RE-VERIFY BY SYMBOL — this tree moves daily. -->

`cockpit.ts` `executePlan` (a `tenantMutation`, pinned by `dispatchGuard.test.ts`), the workflow
arm in order:
```
:782  if (!tokens) return { ok: false, reason: "gmail_not_connected" };   ← the fail-before-mutate precedent
:788  send_time_too_far cap
:794  CAS patch → status "approved"                                       ← EVERYTHING BEFORE THIS IS SAFE TO REFUSE FROM
:796  const recipients = plan.recipients ?? [];
:800  const targets = mode === "group" ? [recipients.join(", ")] : recipients;   ★ THE JOIN
:806  recipientTotal: targets.length, queuedCount: …                      ← counters
:832-867  FOR EACH target: ctx.db.insert("requests", { … })
      if sendAt in future → scheduler.runAt(startScheduledDelivery) → return
      else startFanout(ctx, args)
:654  the return union already carries: "gmail_not_connected" | … governed refusals
```
`startScheduledDelivery` (`cockpit.ts:530`, an `internalMutation`) re-reads the plan, checks the
sendAt CAS and calls the SAME `startFanout` with `requestIds` frozen at approve time.

`gmail.ts`:
```
:81   export function buildMime(to, subject, body, parts, threading)   ← DO NOT PUT THE FOOTER INSIDE
:154  type SendResult = … { delivered: false, reason } arm ALREADY EXISTS
:158  export const send = internalAction({ … })      ← "use node"; ctx.runQuery is free here
:161  const req = await ctx.runQuery(internal.gmailAuth.getForDelivery, { requestId });
:186  throws `gmail.send: attachment blob ${a.storageId} missing`   ← the fail-closed precedent
:201  const raw = base64Url(buildMime(req.recipient, req.subject, req.body, parts, threading));  ★ THE FOOTER POINT
```
`getForDelivery` projects `body: r.editedBody ?? r.draft ?? ""` — every stage before `:201` is a
bypass, which is why the footer cannot live at the drafter, the plan row or the request row.
`notifyExternal.ts:53` is a SECOND `buildMime` caller sending a static service notice to the
user's OWN mailbox — it must carry NO footer. `gmail.test.ts` V4 pins `buildMime`'s zero-attachment
byte identity; appending OUTSIDE `buildMime` keeps it green.

`deliverApprovedPlan.ts:37` is the SOLE production caller of `internal.gmail.send`
(`grep -rn "internal.gmail.send"` = 1 hit) and already `continue`s on `!result.delivered`.

`plans.ts` `recordDeliveryTerminal`:
```ts
outcome: v.union(v.literal("sent"), v.literal("failed")),
…
if (request.status === "sent" || request.status === "failed") return { applied: false };
await ctx.db.patch(requestId, { status: outcome });
…
const total = Math.max(0, Math.floor(plan.recipientTotal));
const sentCount   = Math.min(total, plan.sentCount + (outcome === "sent" ? 1 : 0));
const failedCount = Math.min(total - sentCount, plan.failedCount + (outcome === "failed" ? 1 : 0));
await ctx.db.patch(planId, { sentCount, failedCount,
  queuedCount: Math.max(0, total - sentCount - failedCount) });
```
`requests.status` already includes `"blocked"` (`schema.ts:140`).

From 19-02 (`contacts.ts`): `suppressedAmong({ tenantId, addresses })`,
`isSuppressed({ tenantId, recipient })`, `footerFor({ tenantId, recipient })` → `{ text } | null`.
From `@pikar/core` (19-01): `normalizeAddress`, `renderFooter`.

`cards.tsx` already handles `res.reason === "gmail_not_connected"` with a `setNote(...)` — extend
that same record.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pre-CAS refusals and the pre-join per-address drop in executePlan</name>
  <files>packages/backend/convex/cockpit.ts, packages/backend/convex/cockpitTools.test.ts</files>
  <behavior>
    - Test (row 16a): a tenant with NO `postalAddress` approving an email plan gets
      `{ ok: false, reason: "no_postal_address" }`, the plan is STILL `proposed`, and there are
      ZERO `requests` rows. (A refusal after the CAS leaves a half-approved plan nothing can
      resume — 20-07's exact lesson.)
    - Test (row 11): 5 recipients, 1 suppressed ⇒ `{ ok: true, withheld: ["<the address>"] }`,
      FOUR `requests` rows, and `recipientTotal === 4` / `queuedCount === 4` (the counters are
      computed from the ALLOWED list, not the original list, or Approvals progress lies).
    - Test (row 12): `mode: "group"` with 3 recipients, 1 suppressed ⇒ ONE `requests` row whose
      `recipient` string contains the two allowed addresses and does NOT contain the suppressed
      one. **Use three recipients, not two** — a group test with one recipient passes vacuously.
    - Test (row 13): ALL recipients suppressed ⇒ `{ ok: false, reason: "all_recipients_suppressed" }`,
      plan STILL `proposed`, ZERO `requests` rows, nothing scheduled (assert the scheduler was
      not called).
    - Test: suppression matching is case- and whitespace-insensitive (`" Bob@X.com "` suppressed
      blocks `"bob@x.com"` in `plan.recipients`) — the `normalizeAddress` agreement, proven at
      the guard rather than assumed.
    - Regression (row 7): the memo, calendar_event and media arms are behaviourally UNCHANGED —
      extend 20-07 Task 3's existing regression rather than writing a parallel one.
  </behavior>
  <action>
    In `executePlan`'s workflow arm, BEFORE the CAS patch (locate the `status: "approved"` patch by
    symbol; everything above it is safe to refuse from):

    1. **Postal-address pre-check**, immediately beside the `gmail_not_connected` check. Read the
       tenant profile; if `postalAddress` is missing/empty, `return { ok: false, reason:
       "no_postal_address" }`. Add `"no_postal_address"` and `"all_recipients_suppressed"` to the
       explicit return union.

    2. **The per-address partition, BEFORE `targets` is computed** — this is the whole point;
       after the join a suppressed address is baked into one comma-joined string and is
       unremovable downstream:
       ```ts
       const all = plan.recipients ?? [];
       const suppressed = await suppressedAmong(ctx, ctx.tenantId, all);   // one index read
       const recipients = all.filter((r) => !suppressed.has(normalizeAddress(r)));
       if (recipients.length === 0) return { ok: false, reason: "all_recipients_suppressed" };
       const mode = plan.mode ?? "individual";
       const targets = mode === "group" ? [recipients.join(", ")] : recipients;
       ```
       Then set `recipientTotal`/`queuedCount` from `targets.length` as today — which now reflects
       the ALLOWED set by construction.

    3. **The withheld report.** `withheld.length > 0` is NOT a refusal — a partial send still
       returns `{ ok: true, withheld: string[] }`. Only bugs throw; a governed stop returns. Add
       `withheld?: string[]` to the ok arm of the return union.

    Comment the ordering explicitly: *"Every refusal here runs BEFORE the CAS patch. A refusal
    after it leaves the plan `approved` with zero `requests` rows and no workflow — a half-approved
    state nothing can resume (20-07)."* And on the filter: *"BEFORE the join. `mode === 'group'`
    collapses recipients into one comma-joined string, so a per-address drop is impossible
    afterwards."*

    Do NOT read `contacts` here. The guard reads `suppressions` only — that is what makes a
    contacts bug unable to un-suppress anyone (SC#5).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools</automated>
    <automated>pnpm --filter @pikar/backend test -- dispatchGuard</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>VALIDATION rows 11, 12, 13, 16a and 7 are green. Both new refusals return before the CAS. Mutation check: temporarily move the suppression filter to AFTER the `approved` patch and confirm the "plan stays proposed" assertions go RED, then revert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The unbypassable send-path backstop, the footer, and the honest terminal</name>
  <files>packages/backend/convex/gmail.ts, packages/backend/convex/plans.ts, packages/backend/convex/deliverApprovedPlan.ts, packages/backend/convex/gmail.test.ts</files>
  <behavior>
    - Test (row 14, THE test that proves the guard is in the SEND path): approve a plan for a
      FUTURE `sendAt` with a clean recipient; THEN create the suppression; THEN fire
      `startScheduledDelivery`. `gmail.send` returns `{ delivered: false, reason: "suppressed" }`
      and no Gmail POST happens. The approve-time filter cannot see this — only the backstop can.
    - Test (row 15): a normal send's MIME BYTES contain the tenant's postal address verbatim AND
      an `/unsubscribe/` URL. Assert on the bytes handed to the send endpoint, not on `req.body`.
    - Test (row 16b): a tenant whose `postalAddress` was cleared BETWEEN approve and fire makes
      `gmail.send` THROW, naming the missing field. Nothing is POSTed.
    - Test (row 17): `buildMime`'s V4 zero-attachment byte-identity assertions are UNCHANGED and
      green, and `notifyExternal`'s service notice contains NO postal address and NO
      `/unsubscribe/` URL.
    - Test (counter arithmetic, OQ3): after a post-approve suppression on a 3-recipient plan,
      the plan's counters BALANCE — `sentCount + failedCount + queuedCount === recipientTotal`
      and `queuedCount` reaches 0 once the other two land. The suppressed row is NOT left at
      `delivering` forever.
    - Test: the suppressed terminal is idempotent — recording it twice applies once.
  </behavior>
  <action>
    **(a) The backstop**, in `gmail.send` immediately after `getForDelivery` and its `!req` throw:
    ```ts
    // SC#5 trust boundary: this is the ONE place every product send converges
    // (deliverApprovedPlan.ts is the sole caller). The executePlan filter is better UX; THIS is
    // what makes it unbypassable — a suppression created after approve but before a scheduled
    // fire is caught only here.
    if (await ctx.runQuery(internal.contacts.isSuppressed, {
          tenantId: req.tenantId, recipient: req.recipient })) {
      return { delivered: false, reason: "suppressed" };
    }
    ```
    `SendResult`'s non-delivered arm already exists; add `"suppressed"` to its reason union.

    **(b) The honest terminal (Open Question 3 — RESOLVED HERE, do not re-open).**
    `deliverApprovedPlan` currently `continue`s on `!delivered`, which is correct for the RESUMABLE
    `awaiting_reauth` case and WRONG for a suppression, which is permanent — the row would sit at
    `delivering` forever and `queuedCount` would never reach 0.
    Resolution: widen `recordDeliveryTerminal`'s outcome union to
    `"sent" | "failed" | "suppressed"`. On `"suppressed"`:
      - patch `requests.status = "blocked"` (an EXISTING `requests.status` member — do not add one);
      - extend the idempotency early-return to include `"blocked"`;
      - patch `recipientTotal = Math.max(0, total - 1)` and recompute `queuedCount` from the new
        total. **Decrementing the total is the truthful statement**: the plan now has one fewer
        recipient, which is exactly the semantics of the `executePlan` filter (where a suppressed
        address never entered `recipientTotal` at all). `sentCount`/`failedCount` stay honest.
    In `deliverApprovedPlan`, branch on `result.reason === "suppressed"` and call
    `recordDeliveryTerminal` with the new outcome instead of a bare `continue`. Leave every other
    `!delivered` reason on the existing `continue` path — do not change `awaiting_reauth`.

    **(c) The footer**, at the `buildMime(...)` CALL SITE — **NOT inside `buildMime`**
    (`notifyExternal.ts` is a second caller sending a service notice to the user's own mailbox, and
    `gmail.test.ts` V4 pins `buildMime`'s bytes):
    ```ts
    const footer = await ctx.runQuery(internal.contacts.footerFor, {
      tenantId: req.tenantId, recipient: req.recipient,
    });
    if (!footer) throw new Error("gmail.send: tenant has no postal address (set it on /dashboard/profile)");
    const raw = base64Url(buildMime(req.recipient, req.subject, req.body + footer.text, parts, threading));
    ```
    The throw mirrors the existing `gmail.send: attachment blob … missing` fail-closed precedent:
    never silently send without the promised part. `req.body` is ALREADY the resolved per-recipient
    body, so `recipientBodies` is covered for free; and `buildMime` does the multipart branching
    itself, so attachments need no special handling.

    Comment why the footer is here and nowhere else: the model's output flows
    `plans.body → plans.recipientBodies → requests.draft`, and `getForDelivery` reads
    `r.editedBody ?? r.draft` — every earlier stage is a bypass, and a prompt instruction is
    exactly the "classifier being right" failure mode CONTEXT rejects (CLAUDE.md §5 forbids
    hardcoding it in a prompt anyway).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- gmail</automated>
    <automated>pnpm --filter @pikar/backend test -- plans</automated>
    <automated>pnpm --filter @pikar/backend test -- llmRedaction</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>VALIDATION rows 14, 15, 16b and 17 are green. `gmail.test.ts` V4 byte-identity is untouched and passing. `llmRedaction.test.ts`'s closed `SEND_ENDPOINT` set assertion still holds (still exactly two POST sites). The counters balance after a post-approve suppression.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the refusals to the user, and bump cockpit.md</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, docs/playbooks/cockpit.md</files>
  <action>
    Read `docs/design/BRAND.md` first (CLAUDE.md §10). Extend the EXISTING `setNote(...)` record in
    `cards.tsx` that already maps `res.reason === "gmail_not_connected"` — do not add a second
    error mechanism:
    - `"no_postal_address"` → name the missing field and link `/dashboard/profile`:
      *"Add your postal address before sending — the law requires it in every email's footer."*
    - `"all_recipients_suppressed"` → *"Nobody on this list can be emailed: every recipient has
      unsubscribed."*
    - `res.withheld` (a partial send, `ok: true`) → a NON-error note naming the withheld addresses
      and why: *"Sent to N. Withheld M who unsubscribed: <addresses>."* This is SC#5's "TELL the
      user which addresses were withheld"; render it in the neutral note style, never in `--held`
      amber (BRAND §2 reserves amber for the approval gate — spend it in exactly one place).

    Use `globals.css` variables only; hardcode no hex a token covers. Do not add a component
    library (BRAND §8). No `window.alert`, no `window.confirm`.

    Bump `docs/playbooks/cockpit.md` (`Last verified` + `Plan 19-05`). Record under invariants:
    (a) the send path converges TWICE and both points are guarded, with the reason each is
    necessary; (b) every refusal in `executePlan` runs BEFORE the CAS patch, and the test that
    pins it; (c) the footer lives at the `buildMime` CALL SITE and must never move inside
    `buildMime` (naming `notifyExternal` and the V4 byte-identity tests as the two reasons);
    (d) a post-approve suppression terminates as `blocked` via `recordDeliveryTerminal`'s
    `"suppressed"` outcome, which decrements `recipientTotal` so the counters stay balanced.
    CRLF file — normalize and assert the anchor is unique.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web typecheck</automated>
    <automated>pnpm --filter @pikar/web build</automated>
    <automated>pnpm --filter @pikar/backend test -- traceParity</automated>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>All three outcomes render through the existing note affordance with no new error mechanism and no amber; web typecheck and production build green; `cockpit.md` records the four invariants above.</done>
</task>

</tasks>

<verification>
- **`pnpm test` (full turbo) — the WHOLE suite is the gate for this plan, not a filtered run
  (20-07's rule: the approve spine has no safe partial).**
- `pnpm typecheck` — delta 0 vs a freshly re-measured baseline, zero non-test errors
- `pnpm --filter @pikar/web build` — green
- `node scripts/check-playbooks.mjs` — exit 0
- `grep -rn "internal.gmail.send" packages/backend/convex --include=*.ts | grep -v test` — still
  exactly ONE production hit (the convergence claim must remain true)
</verification>

<success_criteria>
- Rows 11-17 of `19-VALIDATION.md` are green
- A suppression created after approve and before a scheduled fire is refused, proven by test
- The MIME bytes of every product send carry the postal address and an unsubscribe URL
- `notifyExternal`'s service notice carries neither, and `buildMime`'s V4 byte identity is intact
- No refusal runs after the CAS patch (proven by a reverted mutation check)
- Plan counters balance after a post-approve suppression
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-05-SUMMARY.md`
</output>
