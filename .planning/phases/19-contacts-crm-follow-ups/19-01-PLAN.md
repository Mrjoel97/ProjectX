---
phase: 19-contacts-crm-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/contacts.ts
  - packages/core/src/contacts.test.ts
  - packages/core/src/index.ts
  - packages/backend/convex/schema.ts
  - docs/playbooks/contacts-crm.md
  - docs/playbooks/watch.json
autonomous: true
requirements: [ACTN-05, PIPE-01]

must_haves:
  truths:
    - "One address-normalization function exists and every later contacts/suppression/guard call site can import it"
    - "The three new tables exist in the schema with tenant-leading indexes and no monetary or stage column"
    - "tenantProfiles can hold a physical postal address without any tenant being forced to re-onboard"
    - "The 'no contacts cache at rest' reconciliation is stated in a committed playbook, not only in a plan summary"
  artifacts:
    - path: "packages/core/src/contacts.ts"
      provides: "normalizeAddress, needsAttention, followUpIsDue, renderFooter — the phase's pure predicates"
      exports: ["normalizeAddress", "needsAttention", "followUpIsDue", "renderFooter"]
    - path: "packages/core/src/contacts.test.ts"
      provides: "Boundary tests for all four pure functions"
    - path: "packages/backend/convex/schema.ts"
      provides: "contacts, followUps, suppressions tables + tenantProfiles.postalAddress"
      contains: "suppressions: defineTable"
    - path: "docs/playbooks/contacts-crm.md"
      provides: "SC#7's written reconciliation + the phase's invariant list"
      contains: "no contacts cache at rest"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/contacts.ts"
      via: "barrel re-export"
      pattern: "export \\* from \"./contacts\""
    - from: "docs/playbooks/watch.json"
      to: "docs/playbooks/contacts-crm.md"
      via: "watch registration of every new path prefix"
      pattern: "contacts-crm\\.md"
---

<objective>
Lay the substrate the whole phase stands on: the ONE address-identity function, the three
pure predicates, the three new tables, the `tenantProfiles` postal-address field, and the new
`contacts-crm.md` playbook with its `watch.json` registration.

Purpose: Every later plan imports `normalizeAddress` rather than writing its own `.toLowerCase()`
— that is what makes "the guard and the contact row agree by construction" true rather than
hoped for. And `check-playbooks.mjs` blocks any turn that adds `packages/backend/convex/contacts.ts`
or `apps/web/.../dashboard/pipeline/` while no playbook watches them, so the playbook must exist
before the code does.

Output: `packages/core/src/contacts.ts` (+ test), three tables and one optional profile field in
`schema.ts`, `docs/playbooks/contacts-crm.md`, and the `watch.json` entry.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@CLAUDE.md
@docs/playbooks/TEMPLATE.md
@packages/backend/convex/schema.ts
@packages/core/src/index.ts

<interfaces>
<!-- Contracts later plans consume. Do not explore the codebase for these — they are here. -->

Verified 2026-08-09 by symbol (line numbers rot; locate by symbol):

`packages/backend/convex/schema.ts:1251` — the tenantProfiles insertion point:
```ts
    blueprintConfirmedAt: v.optional(v.number()),
```
`schema.ts:1190-1196` is that table's own "ALL optional ⇒ no migration" blessing; `schema.ts:1189`
carries "New table ⇒ no migration (prior-phase discipline)". Copy both comment idioms.

`packages/backend/convex/schema.ts:208-212` — the invariant this phase must reconcile with:
> "TRANSIENT name-resolution store … wiped on pick (clearCandidates). The 'no contacts cache at
> rest' invariant: only the chosen address persists in `recipients`; candidates/pendingValid never
> survive the pick."

Existing index naming (observed): `by_tenant`, `by_tenant_status`, `by_tenant_status_createdAt`,
`by_tenant_kind`, `by_plan`.

`packages/core/src/index.ts` is a barrel of `export * from "./<module>"` lines.
`packages/core/package.json` exports both `.` (the barrel) and `./*`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pure contacts core — one identity function, three predicates</name>
  <files>packages/core/src/contacts.ts, packages/core/src/contacts.test.ts, packages/core/src/index.ts</files>
  <behavior>
    `normalizeAddress(s: string): string`
    - Test: `normalizeAddress("  Bob@Example.COM ") === "bob@example.com"`
    - Test: IDEMPOTENT — `normalizeAddress(normalizeAddress(x)) === normalizeAddress(x)` for a
      table of inputs including already-normal, mixed case, leading/trailing whitespace, tabs.
    - Test: an empty/whitespace-only string normalizes to `""` (callers reject; this function
      does not validate).

    `needsAttention(contactId: string, openFollowUpContactIds: ReadonlySet<string>): boolean`
    - Test: a contact with NO open follow-up ⇒ `true`.
    - Test: a contact WITH an open follow-up ⇒ `false`.
    - Test: a contact whose only follow-ups are `done`/`canceled` ⇒ `true` (the caller passes
      only OPEN ones; assert the boundary with an empty set).

    `followUpIsDue(dueAt: number, now: number): boolean`
    - Test: `dueAt < now` ⇒ true; `dueAt === now` ⇒ true (due AT the instant counts as due);
      `dueAt > now` ⇒ false. Pin the `===` boundary explicitly — it is the tile's arithmetic.

    `renderFooter(input: { postalAddress: string; unsubscribeUrl: string }): string`
    - Test: the returned string CONTAINS the postal address verbatim and the unsubscribe URL
      verbatim.
    - Test: it begins with a blank-line separator so appending it to any body cannot run into
      the last sentence.
    - Test: a postal address that is empty or whitespace-only THROWS (fail closed — a footer
      rendering an empty address looks compliant and is not).
  </behavior>
  <action>
    Create `packages/core/src/contacts.ts` with exactly the four exports above and nothing else.
    Framework-agnostic pure TS (CLAUDE.md §1) — no Convex import, no `Id<>` types; take plain
    `string` ids so both the Convex adapter and the web component can call it.

    `normalizeAddress` is `s.trim().toLowerCase()`. Do NOT add email validation, punycode,
    plus-address stripping or Gmail dot-folding — CONTEXT locks identity to "lowercased email
    address, one row per address" and person-level merging is explicitly deferred. Mark the
    ceiling: `// ponytail: trim+lowercase only. Plus-addressing and dot-folding are person-level
    merging (deferred, 19-CONTEXT Deferred Ideas); upgrade path is a second canonicalise() beside
    this, never a change to this one — the suppressions key must stay byte-stable.`

    `renderFooter` returns plain text (the body is `text/plain` in both `buildMime` branches).
    Keep the copy to two lines: the postal address, then "Unsubscribe: <url>". Do NOT add an
    "is this commercial?" branch — CONTEXT locks every send to carry it.

    Add `export * from "./contacts";` to `packages/core/src/index.ts`, keeping the file's
    existing alphabetical-ish grouping.

    Write `contacts.test.ts` FIRST (RED), then the implementation (GREEN). No new dependency.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- contacts</automated>
    <automated>pnpm --filter @pikar/core typecheck</automated>
  </verify>
  <done>VALIDATION rows 2 and 3 are green. All four functions exported from the barrel; `pnpm --filter @pikar/core test -- contacts` passes with the boundary cases above; `renderFooter` throws on an empty address.</done>
</task>

<task type="auto">
  <name>Task 2: Three tables and one optional profile field — no migration</name>
  <files>packages/backend/convex/schema.ts</files>
  <action>
    Add THREE new tables (NOT two — the ROADMAP prose is superseded by 19-CONTEXT and the third
    table is what makes SC#5 structurally clean) and ONE optional field.

    `contacts`:
      tenantId, email (ALREADY normalized by the write boundary — say so in the comment),
      name: v.optional(v.string()),
      origin: v.union(v.literal("mailbox-resolved"), v.literal("user-entered"), v.literal("inbound")),
      consentAt: v.optional(v.number()),
      consentSource: v.optional(v.union(v.literal("asserted-by-user"), v.literal("inbound-form"))),
      consentWording: v.optional(v.string()),   // the exact wording shown — content plane, NEVER audit
      consentContext: v.optional(v.string()),   // the user's free-text capture context
      unsubscribedAt: v.optional(v.number()),   // DISPLAY MIRROR ONLY — see the comment rule below
      createdAt: v.number(), updatedAt: v.number()
      indexes: `by_tenant_email` (["tenantId","email"]), `by_tenant_createdAt` (["tenantId","createdAt"])

    `followUps`:
      tenantId, contactId: v.optional(v.id("contacts")), title/note text field,
      dueAt: v.number()  (REQUIRED — no date, no follow-up),
      status: v.union(v.literal("open"), v.literal("done"), v.literal("canceled")),
      completedAt: v.optional(v.number()),
      sourcePlanId: v.optional(v.id("plans")),   // provenance ref — an id, refs-only by construction
      createdAt: v.number()
      indexes: `by_tenant_status_dueAt` (["tenantId","status","dueAt"]),
               `by_tenant_contact` (["tenantId","contactId"])

    `suppressions`:
      tenantId, address (normalized), suppressedAt: v.number(),
      source: v.union(v.literal("unsubscribe-link"), v.literal("user-marked")),
      index: `by_tenant_address` (["tenantId","address"])  — the ONE index the send guard reads.

    On `tenantProfiles`, immediately after `blueprintConfirmedAt` (verify by symbol), add
    `postalAddress: v.optional(v.string()),` — a single free-text block, NOT a structured object
    (CAN-SPAM requires "a valid physical postal address", not a parsed one; a structured object
    invites a country/state enum this phase does not need). Completeness is enforced at the WRITE
    boundary, per that table's own comment.

    Comment rules, all mandatory:
    - "New table ⇒ NO migration (prior-phase discipline, `schema.ts:1189`)" on each new table.
    - "ALL optional ⇒ NO migration (convex-migration-helper: Safe Changes → Adding Optional Field)"
      on `postalAddress`.
    - On `contacts.unsubscribedAt`: "DISPLAY MIRROR ONLY. The send-path guard reads
      `suppressions` and NEVER this field or this table — that is what makes a contacts bug
      unable to un-suppress anyone, and contact deletion a non-event for the guard (SC#5)."
    - On `contacts.consentWording`/`consentContext`: "Content plane. CLAUDE.md §4 — this text
      MUST NEVER reach `audit.payload`, which carries refs/ids/counts only."
    - On the tables as a set: "No `opportunities` table, no stage enum, no `amountCents` — PIPE-01
      and Phase 19 SC#8. Real money arrives with Phase 28's connector-backed Cash surface."

    DO NOT touch `plans.kind` here — widening it without `actionTypeOf`'s parameter type breaks
    the `actionTypeOf(plan.kind)` call in `cockpit.ts`. Plan 19-06 owns that edit in one commit.

    Run `npx convex codegen` from `packages/backend` before quoting any typecheck number
    (a new module reads as +1 error until `_generated/api` has an entry — RESEARCH Pitfall 11).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- schema</automated>
    <automated>pnpm --filter @pikar/backend typecheck</automated>
  </verify>
  <done>Three tables and `tenantProfiles.postalAddress` are in `schema.ts` with the comment idioms above. Backend typecheck delta is 0 against a FRESHLY re-measured baseline (do not quote 13 or 150 from a document — re-measure). No `amountCents`, no `stage`, no `opportunit*` token anywhere in the diff.</done>
</task>

<task type="auto">
  <name>Task 3: The contacts-crm playbook and its watch registration</name>
  <files>docs/playbooks/contacts-crm.md, docs/playbooks/watch.json</files>
  <action>
    Create `docs/playbooks/contacts-crm.md` from `docs/playbooks/TEMPLATE.md`. Header format copies
    `dashboard-pages.md:1-4` exactly:

    ```
    # Playbook: Contacts, CRM & follow-ups

    > Last verified: 2026-08-09 (Plan 19-01 — pure core, three tables, playbook created)
    > Build history: `.planning/phases/19-contacts-crm-follow-ups/` · Related ADRs: none
    ```

    Fill every TEMPLATE section. Three sections are LOAD-BEARING and are not boilerplate:

    (1) **Invariants — SC#7's written reconciliation, in these terms.** The `schema.ts:208-212`
    invariant forbids a CACHE — data that accretes as a side effect of reading the mailbox. A
    `contacts` row is not a cache: it exists ONLY because a human deliberately made it (typed it,
    or approved a staged add). Gmail header resolution still writes nothing; `clearCandidates`
    still wipes on pick; `plans.candidates`/`pendingValid` still never survive the pick. Nothing
    accretes. State the CHANGE-SAFETY rule explicitly: any future code path that writes a contact
    row without a human act re-opens the invariant and is forbidden, and the way to prove it stays
    true is that `resolveContacts` (`llm.ts`) never calls a contacts write.

    (2) **Invariants — the suppression split.** The send-path guard reads `suppressions` ONLY and
    never `contacts`. `contacts.unsubscribedAt` is a display mirror. Suppression OUTLIVES the
    contact. Un-suppressing is behind an explicit confirm + a refs-only audit row, never a toggle.

    (3) **Invariants — the four tiles are ALWAYS-KNOWN counts.** Contacts and follow-ups have NO
    coverage-start concept: the substrate is created by the user, so "we weren't watching" cannot
    apply. Therefore a real zero renders as `0` — never `—`, never `Unknown` (the 26-10 defect,
    commit `1a63992`, from the other side). `Unknown` must not exist as a state on this page.

    Also record, under "Known gaps & deferred work": person-level merging, full inline contact
    editing, per-row opt-out in a CRM plan card, contact deletion/GDPR erasure, seeded contact
    suggestions from recent mail, and RFC 8058 `List-Unsubscribe` headers.

    Register in `docs/playbooks/watch.json` under a new `"contacts-crm.md"` key with EXACTLY these
    prefixes — the hook cannot protect a path that is not listed (CLAUDE.md §9):
    ```json
    "contacts-crm.md": [
      "packages/core/src/contacts.ts",
      "packages/core/src/contacts.test.ts",
      "packages/backend/convex/contacts.ts",
      "packages/backend/convex/contacts.test.ts",
      "apps/web/app/(app)/dashboard/pipeline/"
    ]
    ```
    DO NOT add `apps/web/e2e/pipeline.spec.ts` — it is already registered under
    `dashboard-pages.md` and covered by `cockpit.md`'s `apps/web/e2e/` prefix. Note the choice in
    the playbook's "Key files" section: *the Pipeline e2e spec is owned by `dashboard-pages.md`.*

    `docs/playbooks/*.md` are CRLF — normalize and assert your edit anchor is unique before
    writing, or a multi-line `\n` anchor silently no-ops.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>node -e "const w=require('./docs/playbooks/watch.json'); const e=w['contacts-crm.md']; if(!e) throw new Error('no contacts-crm.md entry'); if(e.includes('apps/web/e2e/pipeline.spec.ts')) throw new Error('double-registered e2e spec'); if(!e.includes('apps/web/app/(app)/dashboard/pipeline/')) throw new Error('pipeline dir unwatched'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/playbooks/contacts-crm.md','utf8'); for(const p of ['no contacts cache at rest','resolveContacts','suppressions','Unknown']) if(!s.includes(p)) throw new Error('playbook missing: '+p); console.log('ok')"</automated>
  </verify>
  <done>VALIDATION row 22 is green. `node scripts/check-playbooks.mjs` exits 0. The playbook states the SC#7 reconciliation in writing, names `resolveContacts` as the enforcement point, and the `watch.json` entry covers every new path prefix without double-registering the e2e spec.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- contacts` — green (rows 2, 3)
- `pnpm --filter @pikar/backend typecheck` — delta 0 against a freshly re-measured baseline
- `node scripts/check-playbooks.mjs` — exit 0 (row 22)
- `pnpm test` (full turbo) — green, no regression from the schema addition
- Structural: `grep -rn "amountCents\|opportunit" packages/core/src/contacts.ts packages/backend/convex/schema.ts` returns nothing new
</verification>

<success_criteria>
- `normalizeAddress` is the single exported identity function and is idempotent under test
- Three tables exist with tenant-leading indexes; `tenantProfiles.postalAddress` is optional
- No table, field or index in the diff carries a stage, opportunity or monetary concept
- `contacts-crm.md` exists, is registered, and states the SC#7 reconciliation in writing
- `check-playbooks.mjs` exits 0 (bump the playbooks you touched regardless of its exit code —
  a foreign lane bumping them first is a FALSE NEGATIVE on your own obligation)
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-01-SUMMARY.md`
</output>
