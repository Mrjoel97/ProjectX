---
phase: 19-contacts-crm-follow-ups
plan: 10
type: execute
wave: 9
depends_on: ["19-05", "19-07", "19-09"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
  - docs/playbooks/contacts-crm.md
autonomous: false
requirements: [ACTN-05, PIPE-01]

must_haves:
  truths:
    - "The ROADMAP no longer says two new tables when three shipped"
    - "The playbook's Last verified line names the commit the whole phase was verified against"
    - "Every one of the 22 verification rows is marked green, red or explicitly NOT RUN — never blank"
    - "The owner has driven the page, the row actions and the unsubscribe landing page in a real browser"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "The corrected Phase 26 resolution note and the Phase 19 plan list"
      contains: "three new tables"
    - path: "docs/playbooks/contacts-crm.md"
      provides: "The phase-complete playbook with a verified-against sha"
  key_links:
    - from: ".planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md"
      to: "the executed test commands"
      via: "a status per row, with NOT RUN spelled out where it applies"
      pattern: "NOT RUN|✅|❌"
---

<objective>
Close the phase honestly: correct the two documents this phase falsified, mark every verification
row with what actually happened, and get the owner in front of a real browser.

Purpose: The ROADMAP's Phase-26 resolution note still says *"The schema that follows is two new
tables: `contacts` and `followUps`"* — three shipped, and the third is the one that makes SC#5
structurally clean. And 26-05 and 26-10 both proved the Playwright suite cannot run in an executor
shell: **a blank row means NOT RUN, never that it passed.**

Output: two corrected planning documents, a filled-in verification map, a phase-complete playbook,
and a blocking owner UAT.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-05-SUMMARY.md
@.planning/phases/19-contacts-crm-follow-ups/19-07-SUMMARY.md
@.planning/phases/19-contacts-crm-follow-ups/19-09-SUMMARY.md
@CLAUDE.md
@docs/playbooks/contacts-crm.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two falsified documents and fill in the verification map</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md</files>
  <action>
    **`.planning/ROADMAP.md`:**
    - In the Phase-26 header block's 2026-08-09 resolution note, correct the closing sentence
      *"The schema that follows is two new tables: `contacts` and `followUps`"* → **three** new
      tables: `contacts`, `followUps` and `suppressions`, with one clause on WHY the third exists
      (the send-path guard reads `suppressions` ONLY and never touches `contacts`, so a contacts
      bug cannot un-suppress anyone and contact deletion is a non-event for the guard). Do not
      delete the note — correct it in place, the way this file already supersedes.
    - Update the Phase 19 section: `**Plans**: TBD` → the real count and the plan list with
      checkboxes, and drop *"It is 0/TBD — no plans exist yet, so it needs `/gsd:plan-phase 19`"*.
    - Add one line under Phase 19 recording that 26-18 still owns the nav flip and that the route
      shipped URL-reachable with `soon: true` intact.
    - **The ROADMAP tracks Phase 19's status in THREE places, not one. Correct all three or the
      file contradicts itself.** Besides the Phase 19 section above: (a) the *"Amended 2026-08-07
      (owner)"* bullet (~line 1274) still reads *"Phase 19 is pulled forward... Needs planning
      first — 0/TBD"*; (b) the master **Phase Index** table row (~line 1318) still reads
      `| 19. Contacts, CRM & Follow-ups | 0/TBD | **Not started — PULLED FORWARD 2026-08-07**;
      needs /gsd:plan-phase 19 ... | - |`. Update both to the real plan count and status. Leaving a
      "Not started, needs planning" row a few hundred lines from a "10/10 complete" section is the
      same self-contradicting document state the 26-10 lesson exists to prevent.

    **`.planning/REQUIREMENTS.md` — TWO locations, same rule:**
    - The checkbox entries `- [ ] **ACTN-05**` (~line 147) and `- [ ] **PIPE-01**` (~line 174).
    - **AND the separate status table** — `| ACTN-05 | Phase 19 | Pending |` (~line 341) and
      `| PIPE-01 | Phase 19 (consumed by Phase 26 nav/integration gate) | Pending |` (~line 354).
      Ticking the checkbox and leaving the table on `Pending` is a half-corrected document.

    Tick/mark complete in BOTH locations ONLY if the owner checkpoint below passed. If it did not,
    leave them as-is and say why in one clause — a requirement is not met because the code exists,
    it is met because someone verified it.

    **`19-VALIDATION.md`:** fill the Status column for all 22 rows with the ACTUAL outcome from
    the plan summaries. Set `nyquist_compliant: true` and `wave_0_complete: true` in the
    frontmatter only if they are true. Any row whose command was never executed is marked
    **NOT RUN**, spelled out — never left blank and never marked ✅ by inference. Re-measure and
    record the backend typecheck baseline here rather than quoting the stale number.

    All three are CRLF — normalize and assert every edit anchor is unique before writing, or a
    multi-line `\n` anchor silently no-ops.

    `gsd-tools commit` breaks on Windows quoting — use plain `git commit -m "msg" -- <paths>`,
    never `git add -A`, and check `.git/MERGE_HEAD` first (lanes share one working tree).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); if(s.includes('two new tables: `contacts` and `followUps`')) throw new Error('ROADMAP still says two tables'); if(!s.includes('suppressions')) throw new Error('third table not named'); if(/Phase 19[\s\S]{0,4000}?\*\*Plans\*\*: TBD/.test(s)) throw new Error('Phase 19 plan count still TBD'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/19-contacts-crm-follow-ups/19-VALIDATION.md','utf8'); const pending=(s.match(/⬜ pending/g)||[]).length; if(pending) throw new Error(pending+' verification rows still pending'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); for(const bad of ['Not started — PULLED FORWARD 2026-08-07','needs `/gsd:plan-phase 19`','Needs planning first — 0/TBD']) if(s.includes(bad)) throw new Error('stale Phase 19 status still in ROADMAP: '+bad); console.log('ok')"</automated>
    <automated>node -e "const L=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8').split(/\r?\n/); for(const id of ['ACTN-05','PIPE-01']){ const box=L.find(l=>l.trim().startsWith('- [')&&l.includes('**'+id+'**')); const row=L.find(l=>l.trim().startsWith('| '+id+' |')); if(!box||!row) throw new Error('cannot locate '+id+' in both places'); const ticked=box.includes('- [x]'); const complete=/complete/i.test(row); if(ticked!==complete) throw new Error(id+': checkbox and status table DISAGREE — one was corrected and the other was not'); } console.log('ok')"</automated>
  </verify>
  <done>The ROADMAP names three tables and a real plan count in ALL THREE of its Phase 19 status locations; REQUIREMENTS' checkbox and status table agree with each other; every VALIDATION row carries an outcome; no row is blank or pending; requirements are marked complete only on a passed checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OWNER GATE — the connected browser UAT</name>
  <what-built>
    The whole phase: three tables, the tenant-scoped person store with its isolation assertion, the
    postal-address field on `/dashboard/profile`, the public unsubscribe landing page, the
    per-address suppression guard at BOTH `executePlan` and `gmail.send`, the un-omittable CAN-SPAM
    footer, `crm_write` as the fifth action type with its own plan card, the contacts-first agent
    tools, and the connected `/dashboard/pipeline` route.

    Everything offline is green. Three things NO automated test in this repo can see: BRAND
    conformance, the browser behaviour of the page, and how the unsubscribe landing page actually
    looks to a recipient.
  </what-built>
  <how-to-verify>
    Stack: a live `convex dev` (NOT `--once`), a PRODUCTION build of the web app on `:3111`
    (`next dev` OOMs on the workspace page — prod-build for UAT replays), and
    `UNSUBSCRIBE_SECRET` set on the DEPLOYMENT (`npx convex env set`, not `.env.local`).
    Seed evidence rows under a `uat-19:` correlation prefix so the run is re-checkable afterwards
    (the 26-10 discipline).

    1. **Empty state.** Open `http://127.0.0.1:3111/dashboard/pipeline` on a fresh tenant.
       All four tiles must read **`0`**. If any reads `—` or `Unknown`, that is the 26-10 defect
       and it is a BLOCKER, not a nit.
    2. **Nav is still `soon`.** "Sales Pipeline" in the left rail must still be greyed/`soon` —
       26-18 owns the flip. If it is clickable, that is a defect.
    3. **Populate.** Add a contact by hand. Add one follow-up bound to it and one free-standing
       ("chase the supplier quote"). Confirm: the contact table is one row per person, the
       free-standing follow-up appears in its OWN section BENEATH the table (not as an em-dash row
       inside it), the tiles moved, and a contact with no name shows its ADDRESS.
    4. **The agent path.** In the cockpit, ask it to save a contact and log a follow-up. Confirm a
       `crm_write` PLAN CARD appears in the right pane showing exactly what will be written — with
       NO recipients, NO mode and NO send-time picker — and a row in Approvals. Approve it. The
       card reaches `done` and Pipeline reflects it. There must be no chat message claiming it was
       saved before you approved.
    5. **Ask for a contactless follow-up in chat.** The agent must ASK who it is for, not invent
       one.
    6. **Suppression, end to end.** Mark a contact suppressed from the table. Compose an email to
       FIVE recipients including that address. Approve. Confirm: four are sent, and the UI TELLS
       you which address was withheld and why. Then suppress a second address and confirm the
       counters in Approvals still balance.
    7. **The footer, in a real inbox.** Open one of the delivered messages in Gmail. It must carry
       your postal address and an Unsubscribe link. Then check the service-notice mail Pikar sends
       to your OWN mailbox — it must carry NEITHER.
    8. **Fail closed.** Clear your postal address on `/dashboard/profile`, then try to approve an
       email plan. It must refuse, NAME the missing field, and point at the profile page. Restore it.
    9. **The unsubscribe page — this is the one flagged for your judgement.** Click the Unsubscribe
       link from the real message. Confirm: the page loads, echoes the right address, and NOTHING
       is suppressed yet (re-check the Pipeline suppressed tile before pressing anything). Then
       press Confirm and check the tile moves. **The page is styled with INLINE hex values mirroring
       the BRAND tokens because a Convex `httpAction` cannot import `globals.css`** — moving it into
       `apps/web` costs a default-deny middleware edit plus a bearer-secret hop back into Convex.
       Judge whether the current page clears the bar for the one screen a RECIPIENT (not a user)
       ever sees. If not, say so and it becomes a follow-up plan, not a silent debt.
    10. **Responsive.** Narrow the window to a phone width. The four tiles and the table must stay
        readable — BRAND §4 forbids dense tables-on-white.

    Optionally run the authored spec: `pnpm --filter @pikar/web test:e2e -- e2e/pipeline.spec.ts`
    with `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` set. **A `--list` is not a run. A blank result means
    NOT RUN, never that it passed.**
  </how-to-verify>
  <resume-signal>Type "approved", or list the defects with the step number that surfaced each.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the playbook against the verified commit</name>
  <files>docs/playbooks/contacts-crm.md</files>
  <action>
    Bump `Last verified` to the UAT date and the actual short sha the owner verified against
    (`git rev-parse --short HEAD`), with `(Plan 19-10 — phase-complete owner UAT)`.

    Fill the remaining TEMPLATE sections against what SHIPPED, not what was planned:
    - **How to verify:** the exact commands, split into unit / static scan / needs-a-live-deployment
      / manual-only — and say plainly which of them an executor CANNOT run and why.
    - **Known gaps & deferred work:** every `ponytail:` ceiling this phase left, each with its
      upgrade path — the unsubscribe page's inline styles, the in-memory "Last touch" fold, the
      trim+lowercase address identity, and `isSuppressed`'s whole-row refusal in group mode. Plus
      the deferred list from 19-01 and anything the owner raised at UAT.
    - **Operational notes:** `UNSUBSCRIBE_SECRET` is a deployment secret; an unset one 404s every
      unsubscribe link, which is correct fail-closed behaviour but is invisible without checking.

    If the owner reported defects that were NOT fixed in this plan, record them in the playbook as
    open gaps with their step number — a defect recorded is a defect; a defect omitted is a lie.
    CRLF file.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/playbooks/contacts-crm.md','utf8'); for(const p of ['19-10','ponytail','UNSUBSCRIBE_SECRET','no contacts cache at rest']) if(!s.includes(p)) throw new Error('missing: '+p); console.log('ok')"</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>`contacts-crm.md` is verified against a real sha, lists every ponytail ceiling with its upgrade path, and records any unfixed UAT defect as an open gap. Full suite and both typechecks green.</done>
</task>

</tasks>

<verification>
- `pnpm test` (full turbo) — green
- `pnpm typecheck` — green, zero non-test backend errors
- `pnpm --filter @pikar/web build` — green
- `node scripts/check-playbooks.mjs` — exit 0
- `19-VALIDATION.md` has no `⬜ pending` rows
- ROADMAP says three tables and carries a real Phase 19 plan list
- Owner UAT resolved
</verification>

<success_criteria>
- Both documents this phase falsified are corrected in place
- All 22 verification rows carry a real outcome, with NOT RUN spelled out where it applies
- The owner has driven the page, the agent path, the suppression drop, the real inbox footer and
  the unsubscribe landing page in a browser
- `ACTN-05` and `PIPE-01` are ticked only because someone verified them
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-10-SUMMARY.md`
</output>
