---
phase: 19-contacts-crm-follow-ups
plan: 09
type: execute
wave: 8
depends_on: ["19-08"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/scripts/eval-cases/36-crm-follow-up.json
  - packages/backend/scripts/run-eval-golden.mjs
  - packages/backend/convex/smoke.ts
  - docs/playbooks/skill-registry.md
  - docs/playbooks/agent-runtime.md
autonomous: false
requirements: [ACTN-05]

must_haves:
  truths:
    - "The active cockpit-agent body teaches the contacts tools — a tool the body does not teach is a tool that does not exist"
    - "A fixture exercises the new tool BEFORE any paid gate run, so the gate cannot certify instructions it never tested"
    - "The fixture floor rises with the fixture, so a quietly dropped case cannot quietly shrink the gate"
    - "The owner knows which skill version carries this body before a cent is spent"
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "The canonical human-editable body, teaching contacts-first resolution and stageCrmWrite"
      contains: "stageCrmWrite"
    - path: "packages/contracts/src/skills/cockpitAgent.ts"
      provides: "The byte-identical auto-derived bundler-safe mirror"
    - path: "packages/backend/scripts/eval-cases/36-crm-follow-up.json"
      provides: "The fixture owed by the binding 18-08 override condition"
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "The $0 crm observable + the 34 → 35 fixture floor bump"
      contains: "35"
  key_links:
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "packages/contracts/src/skills/cockpitAgent.ts"
      via: "the vitest byte-identity sync assertion (LF-normalized)"
      pattern: "cockpitAgentSkillBody"
    - from: "packages/backend/scripts/eval-cases/36-crm-follow-up.json"
      to: "packages/backend/convex/smoke.ts"
      via: "the $0 observable the fixture asserts on"
      pattern: "smoke:"
---

<objective>
Teach the active `cockpit-agent` body the contacts capability, and pay the debt that comes with it:
**teach a tool, owe a fixture.**

Purpose: `cockpit-agent` is in `GATED_SKILLS` with ONE candidate stream. The lane is currently FREE
— 18-08 landed (`cb48d11`) and was certified live 2026-08-08 by gate `14feb4b7`, 34/34, with
`cockpit-agent@17` ACTIVATED — but 20-12 and 20.1-01 are both queued behind Phase 16, which is
DEFERRED on billing with no date. Phase 19 takes the lane now and inherits 18-08's **binding**
override condition (`PARALLELIZATION.md:36-43`): *"the original rule's real objection was never
'two lanes in one file' — it was 'the next eval would certify instructions it never tested'. That
objection stands and is answered by FIXTURES."* Any lane taking this override owes a fixture that
exercises the taught tool BEFORE the gate run.

Output: the body edit, its byte-identical mirror, `eval-cases/36-*.json`, a `$0` observable, the
fixture floor bump 34 → 35, and a BLOCKING owner checkpoint for the paid gate run.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/PARALLELIZATION.md
@.planning/phases/19-contacts-crm-follow-ups/19-CONTEXT.md
@.planning/phases/19-contacts-crm-follow-ups/19-RESEARCH.md
@.planning/phases/19-contacts-crm-follow-ups/19-08-SUMMARY.md
@CLAUDE.md
@packages/backend/scripts/eval-cases/35-create-document.json
@docs/playbooks/skill-registry.md

<interfaces>
<!-- Verified from source 2026-08-09. -->

`packages/contracts/src/skills/cockpitAgent.ts` header, verbatim:
> "AUTO-DERIVED from packages/contracts/skills/cockpit-agent.md. The .md is the canonical,
> human-editable source. This .ts constant is the bundler-safe artifact that ships (the Convex
> runtime cannot fs.read repo files). A vitest sync assertion keeps the two byte-identical
> (LF-normalized), so this is registry-bound generated data, NOT a hardcoded prompt."

⇒ **Edit the `.md`, then regenerate the `.ts`.** Never hand-edit the `.ts` — the sync assertion
will catch it, but the point is the `.md` is the source (CLAUDE.md §5).

`packages/contracts/src/skill.ts:41,243` — `COCKPIT_AGENT_SKILL = "cockpit-agent"`, listed in
`GATED_SKILLS`. `isGatedSkill(name)` is the runtime check.

`packages/backend/scripts/run-eval-golden.mjs`:
```
:185-194  the `createdDocCount` observable, read from smoke:createdDocCountForThread
:272-279  the anti-vacuity rule: a zero floor is vacuous and is rejected
:549-551  assert.ok(fixtures.length >= 34, …)   ← "The floor is a deletion tripwire: a fixture
                                                   quietly dropped must not quietly shrink the gate."
```

Fixture shape (`35-create-document.json`): `{ id, description, turns[], expect{}, needles[] }`.
The `description` is load-bearing — 35's names what would make it VACUOUS and states why it does
not. Match that standard.

**THE VERSION-COLLISION GOTCHA, and it just bit someone.** `seedSkills` mints `maxVersion + 1`,
which is NOT necessarily `active + 1`; optimizer dry-run candidates occupy versions.
`candidatesForReview` was fixed 2026-08-09 (`75fe550`) because it offered DOWNGRADES
(`cockpit-agent v17->v16`). `skills.ts:213-219` now reads: *"ONLY candidates AHEAD of what is
live … optimizer dry-runs leave candidate rows behind at lower versions … Observed 2026-08-09."*
⇒ **Call `getActiveSkill` and check the arrow direction in any EVAL_GATE error BEFORE running
`eval:golden` on a named version. Running it on a stale candidate spends real money to bless a
rollback.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teach the body, regenerate the mirror</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts</files>
  <action>
    Edit `packages/contracts/skills/cockpit-agent.md` — the CANONICAL source — then regenerate
    `packages/contracts/src/skills/cockpitAgent.ts` with whatever generator the repo uses (find it
    by grepping for the derive step; the vitest sync assertion tells you when they diverge).
    **Never hand-edit the `.ts`.**

    Teach exactly three things, in the body's existing voice, as tightly as possible — every
    sentence added to a gated body is a sentence the gate must certify:
    1. **Contacts first.** When the user names a person, the saved contact record is authoritative;
       fall back to searching the mailbox only when there is no saved match.
    2. **`stageCrmWrite` STAGES, it does not apply.** Saving a contact or logging a follow-up
       becomes a plan the user approves — never claim a contact was saved or a follow-up recorded,
       exactly as the body already forbids claiming an email was sent.
    3. **Always name a contact on a follow-up.** The agent may not create a follow-up that belongs
       to nobody; if the user has not named the person, ASK one short question and wait. (The
       user can create contactless follow-ups on the Pipeline page; the agent cannot. This is the
       structural brake against the CRM becoming a general task generator.)

    Do not restate the tool's argument schema in prose — the tool description carries it. Do not
    add a "when in doubt, save a contact" instruction: contacts are created by EXPLICIT ACTS only,
    and an eager body is a soft version of the accretion SC#7 forbids.

    Run the sync assertion after regenerating. Do NOT seed or activate anything yet.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts test</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>The `.md` teaches the three rules; the `.ts` is byte-identical to it (LF-normalized) and the sync assertion is green. Nothing has been seeded or activated.</done>
</task>

<task type="auto">
  <name>Task 2: The owed fixture, the $0 observable, and the floor bump</name>
  <files>packages/backend/scripts/eval-cases/36-crm-follow-up.json, packages/backend/scripts/run-eval-golden.mjs, packages/backend/convex/smoke.ts</files>
  <action>
    **(a) The `$0` observable.** Add a `smoke:` op mirroring `smoke:createdDocCountForThread` —
    e.g. `smoke:crmOperationCountForThread`, counting the operations carried by `crm_write` plans
    proposed on the thread. Register it in `run-eval-golden.mjs`'s observable list beside
    `createdDocCount`, and give it the SAME anti-vacuity rule that governs `createdDocCount` and
    the hosted-search floor: a `0` expectation is vacuous and must be rejected by the loader.

    **(b) `eval-cases/36-crm-follow-up.json`.** Match 35's shape and its standard — the
    `description` must name what would make the fixture VACUOUS and state why it does not.
    Design notes, all binding:
    - Turn 1 names a real person and asks for a follow-up in plain language, with a benchmark
      needle (35 uses `benchmark-CD1`); mint a fresh one.
    - Turn 2 asks for something that must be REFUSED-BY-ASKING: a follow-up with NO person named.
      The teeth are that the agent asks rather than inventing a contactless follow-up.
    - `expect`: `crmOperationCount: 1` (NOT 2 — turn 2 must add nothing), plus
      `recipientCount: 0` and `statusAtMost` set so the fixture also proves a CRM turn never
      becomes an outbound plan (35's exact teeth, reused).
    - What would make it vacuous, and is therefore NOT done: naming the tool in the turn (that
      tests compliance, not routing), or asking for something the agent might reasonably turn into
      an email instead.

    **(c) The floor.** Bump `assert.ok(fixtures.length >= 34, …)` to `>= 35` and extend the
    in-file comment with one line naming 19-09's case, the way 18-08's line names 35.

    **Do NOT run `pnpm eval:golden`.** It costs real money and is the owner's call — Task 3.
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --list</automated>
    <automated>node -e "const f=require('./packages/backend/scripts/eval-cases/36-crm-follow-up.json'); if(f.expect.crmOperationCount!==1) throw new Error('expect.crmOperationCount must be 1'); if(!f.description || f.description.length<200) throw new Error('description must state what would make it vacuous'); if(!f.needles||!f.needles.length) throw new Error('no needles'); console.log('ok')"</automated>
    <automated>pnpm --filter @pikar/backend test -- smoke</automated>
  </verify>
  <done>The fixture loads, the floor is 35, the observable is registered with an anti-vacuity rule, and the SMOKE op returns the count offline at $0. No paid run has happened.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OWNER GATE — seed, check the arrow, run the paid eval, activate</name>
  <what-built>
    The `cockpit-agent` body now teaches contacts-first resolution and `stageCrmWrite`; the
    byte-identical mirror is regenerated; `eval-cases/36-crm-follow-up.json` exercises the new tool
    with a `$0` observable; the fixture floor is 35. Everything is authored and offline-green.
    Nothing has been seeded, evaluated or activated — that costs real money and is yours.
  </what-built>
  <how-to-verify>
    Run these IN ORDER. Stop at the first surprise.

    1. **Check what is live BEFORE spending anything.** Query `getActiveSkill` for `cockpit-agent`
       and note the active version. Expected from the last certification: **`cockpit-agent@17`**,
       activated 2026-08-08 by gate `14feb4b7` (34/34).
    2. **Seed the candidate:** `pnpm --filter @pikar/backend seed` (or the repo's seed command).
       `seedSkills` mints `maxVersion + 1`, which is NOT necessarily `active + 1` — optimizer
       dry-run rows occupy versions. **Note the version it actually minted and confirm your body
       is on it.**
    3. **Check the arrow.** If an `EVAL_GATE` error names a version, confirm it points FORWARD
       from the active one. `cockpit-agent v17->v16` is a ROLLBACK; running the gate on it spends
       real money to bless a downgrade (observed 2026-08-09, fixed in `75fe550`).
    4. **Run the gate:** `pnpm eval:golden --skill cockpit-agent@<the version from step 2>`.
       Expect **35/35**. Fixture 36 is NEW and has never executed — if it fails, read it as a real
       signal, not as a flaky test. (Fixture 35 failed its first live execution and was RIGHT to:
       it caught `createDocument` obeying a model-supplied `replace` on a first create, a defect no
       reply-text assertion could see.)
    5. **Activate** only on a clean 35/35, then re-check `getActiveSkill`.

    Environment: the eval path is Windows-fragile. If `convex dev` destabilizes into a retry storm
    you will see `InternalServerError` at $0 — kill all convex processes, start ONE clean `dev`,
    confirm it is stable, then re-run. Do not re-run the gate into an unstable deployment.

    **If the lane is no longer free** — if 20-12 or 20.1-01 landed a candidate while this was in
    flight — STOP and rebase this body on theirs before seeding. A candidate carrying two lanes'
    prose is exactly what the override condition exists to prevent.
  </how-to-verify>
  <resume-signal>Type "approved" with the gate result and the ACTIVE version, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the certification in the playbooks</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/agent-runtime.md</files>
  <action>
    After the checkpoint resolves, bump both playbooks (`Plan 19-09`) with the FACTS from the run,
    not the intent: the gate sha, the score (N/N), the version seeded, the version ACTIVATED, and
    the date. Record in `skill-registry.md` that Phase 19 took the `cockpit-agent` override lane
    and discharged the "teach a tool, owe a fixture" condition with `eval-cases/36-*.json` and the
    34 → 35 floor bump, and that **20-12 and 20.1-01 must now rebase on this body.** Record in
    `agent-runtime.md` the new observable and its anti-vacuity rule.

    If the checkpoint did NOT reach activation, say so in both playbooks in those words — an
    unrun gate is recorded as UNRUN, never as passed. Both files are CRLF.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
    <automated>node -e "const fs=require('fs'); for(const p of ['docs/playbooks/skill-registry.md','docs/playbooks/agent-runtime.md']) if(!fs.readFileSync(p,'utf8').includes('19-09')) throw new Error('not bumped: '+p); console.log('ok')"</automated>
  </verify>
  <done>Both playbooks carry the actual gate outcome (or an explicit UNRUN), the activated version, and the note that the two queued lanes must rebase.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/contracts test` — the body/mirror sync assertion green
- `node packages/backend/scripts/run-eval-golden.mjs --list` — 35 fixtures
- `pnpm test` (full turbo) + `pnpm typecheck` — green
- `node scripts/check-playbooks.mjs` — exit 0
- Owner checkpoint resolved with a recorded gate sha and score
</verification>

<success_criteria>
- The body teaches the capability and the `.ts` mirror is byte-identical to the `.md`
- The owed fixture exists, is non-vacuous, and the floor rose with it
- No paid run happened without the owner, and no version was activated without checking the arrow
- The playbooks record the real outcome, including an UNRUN if that is what happened
</success_criteria>

<output>
After completion, create `.planning/phases/19-contacts-crm-follow-ups/19-09-SUMMARY.md`
</output>
