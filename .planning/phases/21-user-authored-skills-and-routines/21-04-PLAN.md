---
phase: 21-user-authored-skills-and-routines
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/convex/importGuard.test.ts
  - apps/web/app/(app)/ops/page.tsx
  - apps/web/app/(app)/ops/tenantSkillReview.test.ts
  - docs/playbooks/skill-registry.md
  - docs/playbooks/authorization.md
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "A tenant user candidate activates only when exact passing evidence exists and the caller crosses the Phase 22 ownerMutation boundary; either gate alone is insufficient."
    - "Global and tenant candidate activation reuse one module-local transition helper; the existing global activateSkill/activateCandidate APIs and exact behavior remain compatible."
    - "Activation changes only the exact tenant/name scope, records the candidate as rollback-eligible, and never changes global or another tenant's rows."
    - "Rollback is owner-only, evidence-exempt, and can target only the server baseline or a row proven previously active—not an arbitrary pending candidate."
    - "The bounded owner review surface identifies tenant/user provenance, exact base/candidate diff, and eval state; ordinary users have no activation control or raw-body access."
  artifacts:
    - path: "packages/backend/convex/skills.ts"
      provides: "One shared global/tenant activation transition plus bounded owner tenant review and rollback"
      exports: ["tenantCandidatesForReview", "activateTenantCandidate", "rollbackTenantSkill"]
    - path: "packages/backend/convex/skills.test.ts"
      provides: "Owner/eval truth table, exact-scope status transitions, and rollback eligibility proof"
    - path: "apps/web/app/(app)/ops/page.tsx"
      provides: "Owner-only tenant candidate diff/evidence/activate/rollback controls"
    - path: "packages/backend/convex/importGuard.test.ts"
      provides: "Named wrapper guard that prevents tenant activation/review downgrading to tenant APIs"
  key_links:
    - from: "activateTenantCandidate"
      to: "shared global/tenant activation transition helper"
      via: "ownerMutation supplies authority; the shared scope-discriminated helper supplies exact evidence and one atomic status transition"
      pattern: "transitionSkillActivation"
    - from: "tenantCandidatesForReview"
      to: "tenantSkills.by_status_createdAt"
      via: "bounded newest candidate reads; no deployment-wide unbounded collect"
      pattern: "take("
    - from: "rollbackTenantSkill"
      to: "rollbackEligible"
      via: "status alone never grants the rollback exemption"
      pattern: "ROLLBACK_NOT_ELIGIBLE"
---

<objective>
Add the separate human-owner boundary that turns exact passing evidence into an active tenant skill,
plus a one-write rollback path and bounded owner review UI.

Purpose: evaluation answers whether a body passed; owner authorization answers who may change live
runtime. Phase 21 requires both and must preserve emergency rollback.

Output: owner-only review/activation/rollback, truth-table tests, static wrapper guards, and an
updated ops panel. No paid eval or live activation is performed in this plan.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/design/BRAND.md
@.planning/phases/21-user-authored-skills-and-routines/21-RESEARCH.md
@.planning/phases/21-user-authored-skills-and-routines/21-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-03-SUMMARY.md
@packages/backend/AGENTS.md
@packages/backend/convex/_generated/ai/guidelines.md
@packages/backend/convex/skills.ts
@packages/backend/convex/skills.test.ts
@packages/backend/convex/lib/functions.ts
@apps/web/app/(app)/ops/page.tsx
@docs/playbooks/authorization.md
@docs/playbooks/skill-registry.md

<interfaces>
Phase 22's invariant is unchanged:

```text
public owner operation = ownerQuery/ownerMutation
internal eval operation = identity-free internalQuery/internalMutation
owner authorization and EVAL_GATE are independent
```

The existing global `activateSkill({name,version})`, `activateCandidate({name,version})`,
`activateSkillVersion` compatibility wrapper, and global review queue remain supported with their
current errors/evidence exemptions/return values. Tenant operations use exact tenantSkills ids so
colliding local versions are never ambiguous. There must be one status-transition implementation,
not a copied tenant transaction beside the global helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the owner/eval activation truth table atomically</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts, packages/backend/convex/importGuard.test.ts</files>
  <behavior>
    - Non-owner + no evidence refuses; non-owner + valid exact evidence also refuses with all statuses unchanged.
    - Owner + no/stale/wrong-tenant evidence throws EVAL_GATE and writes no activation state.
    - Owner + valid exact evidence activates only that candidate, archives only the current active row
      in the same tenant/name scope, and marks both rows rollback-eligible appropriately.
    - Global active rows and another tenant's colliding rows remain unchanged.
    - Re-activating the already active exact candidate is idempotent.
    - One successful activation writes a refs-only `skill.user_candidate_activated` audit event;
      failed/idempotent attempts do not duplicate it.
    - Existing global candidate activation, internal activation, rollback-to-prior-version evidence
      exemption, errors, and return shape stay green through the same transition implementation.
  </behavior>
  <action>
Generalize the existing module-local `activateSkillVersion` implementation into ONE
scope-discriminated `transitionSkillActivation(ctx,target)` helper, where target is exactly either
`{scope:"global",name,version}` or `{scope:"tenant",candidateId,mode:"activate"|"rollback"}`. It
must own target resolution, scope-local current-active lookup, evidence/exemption decision, atomic
archive/activate status patches, and idempotence. Keep a thin `activateSkillVersion(ctx,name,version)`
compatibility wrapper and route BOTH existing global exports through it unchanged. Do not duplicate
the archive/activate patches in a tenant-only helper.

Add `activateTenantCandidate` as `ownerMutation({candidateId})` and route it through the same shared
transition with tenant mode `activate`. The tenant branch requires `author === "user"`,
`status === "candidate"`, and exact passing tenant evidence before any status transition; it queries
the active row by tenant/name/status, archives only that row, and marks the target rollback-eligible.
Patch no body, authoredBody, name, version, author, lineage, or evidence.

Do not put `requireOwner` inside the shared internal helper used by runner/seeding: the owner wrapper
is the public authority and the helper must stay testable/identity-free. Keep the existing global
activate export unchanged. Audit only ids, tenant/name/version, author enum, eval run id, and owner
user id; no body or prose.

Build the four-cell truth table with real owner/non-owner users and an existing candidate in every
case. The non-owner+evidence row is the anti-vacuity proof that owner auth—not eval—refused it.
Rerun the existing global `activateSkill`/`activateCandidate` suite unchanged and add a source
assertion that all three exports (`activateSkill`, `activateCandidate`, `activateTenantCandidate`)
reach `transitionSkillActivation`, with exactly one archive/activate patch block in the module.
Extend `importGuard.test.ts` to pin the named tenant review/activate/rollback exports to owner
wrappers. Mutation-bypass `transitionSkillActivation` with direct tenant status patches and expect
the shared-helper source assertion to turn red; restore. Mutation-downgrade activation to
`tenantMutation` and observe both static and behavioral tests turn red; restore and rerun green.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts convex/importGuard.test.ts --maxWorkers=1</automated></verify>
  <done>Only owner plus exact passing evidence can activate, and the transition is atomic and scope-exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit rollback eligibility and one-write owner rollback</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - The server-created baseline can replace the current tenant active row without evidence.
    - A previously active user row can be restored without new evidence.
    - A pending candidate, even if its status is changed to archived in a mutation fixture, refuses
      when `rollbackEligible !== true`.
    - Rollback is owner-only, exact-id, one transaction, and never changes global/foreign rows or bodies.
  </behavior>
  <action>
Add `rollbackTenantSkill` as `ownerMutation({targetId})` and call
`transitionSkillActivation(ctx,{scope:"tenant",candidateId:targetId,mode:"rollback"})`. The shared
tenant rollback branch requires `rollbackEligible === true` and an archived/rolled_back target. Do
not use status alone as proof of prior activation. It archives the current active row in the same
tenant/name scope, restores the target, and leaves evidence untouched. A broken eval harness must
never block this owner-authorized incident path.

Write `skill.user_skill_rolled_back` with refs/versions/counts only. Mutation-remove the
`rollbackEligible` predicate and prove a never-active candidate becomes activatable and the test
turns red; restore it and rerun the exact focused command green.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Rollback always works for genuine baseline/prior-active rows and never launders a pending candidate around the eval gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add bounded owner review and exact UI controls</name>
  <files>packages/backend/convex/skills.ts, apps/web/app/(app)/ops/page.tsx, apps/web/app/(app)/ops/tenantSkillReview.test.ts</files>
  <behavior>
    - `tenantCandidatesForReview` is ownerQuery, uses the status/createdAt index, orders newest-first,
      takes a fixed cap, and returns exact candidate id, tenant/user provenance, base/candidate bodies,
      status, evidence summary, and gatePassed.
    - A non-owner receives no row/body; the refusal happens before the handler reads candidates.
    - Ops clearly separates global optimizer candidates from user tenant candidates.
    - Activate is disabled until gatePassed and calls the exact candidate id. Rollback lists only
      eligible prior versions. Errors render inline and no fixture prompt is exposed.
  </behavior>
  <action>
Add a bounded owner query for newest tenant candidates. Never `.collect()` the deployment-wide
queue. Resolve the current effective/tenant active row for diff context and return only what the
owner needs; ordinary tenant APIs continue to hide full/base bodies and raw evidence.

Extend the existing owner-only Optimizer section with a `User-authored candidates` subsection using
the established card/diff/status-button idiom. Identify the tenant/user by safe refs, show
`Candidate — awaiting evaluation` or `Evaluation passed — ready for owner activation`, and call
the exact-id mutations. Do not add controls to the ordinary user panel.

Add source/static rendering contracts for bounded query use, exact-id calls, explicit state words,
and no fixture/prompt leakage. Reuse tokens and focus states; no new route or component library.
  </action>
  <verify><automated>pnpm --filter web exec vitest run 'app/(app)/ops/tenantSkillReview.test.ts' &amp;&amp; pnpm --filter web typecheck</automated></verify>
  <done>The owner can review and act on one exact tenant candidate while non-owners cannot retrieve bodies or controls.</done>
</task>

<task type="auto">
  <name>Task 4: Update authorization and registry operations</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/authorization.md</files>
  <action>
Document the two independent gates, exact-id owner transaction, bounded disclosure queue, audit key
sets, explicit rollback eligibility, and the difference between global and tenant activation.
State that Phase 22's internal identity-free eval path remains intact. Include operator commands/UI
steps but mark live evidence/activation as unpaid until Plan 06.
  </action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The owner boundary and incident rollback procedure are explicit without overclaiming a live run.</done>
</task>

</tasks>

<verification>
- Focused backend and ops tests pass.
- Owner/eval four-cell truth table is non-vacuous.
- Existing global activation APIs and rollback exemption tests stay unchanged and green through the
  one shared transition; a direct tenant patch bypass mutation turns the source contract red.
- Wrapper-downgrade and rollback-eligibility mutations turn red and are reverted.
- Backend/web typechecks and playbook gate pass.
- No live candidate is evaluated, activated, or rolled back in this plan.
</verification>

<success_criteria>
- Exact tenant activation requires both passing eval and owner authorization.
- Scope transitions are atomic and tenant/name-local.
- Rollback is always available only for genuine prior-active/baseline rows.
- Owner review is bounded and non-owner body disclosure is impossible server-side.
</success_criteria>

<output>
Create `.planning/phases/21-user-authored-skills-and-routines/21-04-SUMMARY.md`. Keep SKILL-01
pending until pinned prompts and the live integrated gate complete.
</output>
