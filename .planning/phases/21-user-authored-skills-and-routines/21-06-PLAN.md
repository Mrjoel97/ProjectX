---
phase: 21-user-authored-skills-and-routines
plan: 06
type: execute
wave: 5
depends_on: ["21-04", "21-05"]
files_modified:
  - apps/web/e2e/skill-authoring.spec.ts
  - .planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "Authenticated browser evidence proves candidate-only publication and pinned-prompt fresh-thread behavior before any paid evaluation or activation."
    - "The full fast and free automated gates pass before a live handoff is created."
    - "Exactly one browser-published candidate is inspected; no later task republishes or substitutes a newest name/version row."
    - "One refs-only immutable handoff pins deployment, exact candidate identity/provenance/lineage/hash/state, rollback baseline, prior effective active identity/hash, and foreign-tenant pre-state."
    - "Plan 21-07 can machine-compare every paid/post-live readback to the same preflight bytes without relying on shell memory or human transcription."
  artifacts:
    - path: "apps/web/e2e/skill-authoring.spec.ts"
      provides: "Authenticated candidate-only authoring and pinned-prompt fresh-thread browser proof"
    - path: ".planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json"
      provides: "Refs-only machine-readable immutable input to the separate paid/live closing plan"
      contains: "phase21-live-handoff.v1"
  key_links:
    - from: "browser publish response"
      to: "inspect-tenant-skill"
      via: "the exact returned tenantSkills id, never name/version/newest lookup"
      pattern: "candidateId"
    - from: "inspect-tenant-skill --json"
      to: "21-LIVE-HANDOFF.json"
      via: "schema-checked refs-only serialization after two identical read-only snapshots"
      pattern: "deploymentUrlHash"
    - from: "pinned prompt Run"
      to: "new cockpit thread"
      via: "browser observes a different thread id and ordinary activity/plan behavior"
      pattern: "useSendCockpitMessage"
---

<objective>
Finish every free/browser prerequisite and freeze the exact live candidate into a refs-only handoff
before requesting model spend.

Purpose: the live gate must evaluate the same immutable row that browser authoring and preflight
proved. Splitting here prevents an oversized plan from republishing or losing candidate identity
between free verification and the owner checkpoint.

Output: executed authenticated browser evidence, green focused/full free gates, and
`21-LIVE-HANDOFF.json`. No paid eval, evidence write, activation, rollback, requirement completion,
or roadmap completion occurs in this plan.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/design/BRAND.md
@.planning/phases/21-user-authored-skills-and-routines/21-RESEARCH.md
@.planning/phases/21-user-authored-skills-and-routines/21-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-04-SUMMARY.md
@.planning/phases/21-user-authored-skills-and-routines/21-05-SUMMARY.md
@apps/web/e2e/README.md
@apps/web/e2e/auth.setup.ts

<interfaces>
The exact read-only snapshot command is:

```powershell
pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill <candidateId> --foreign-tenant <foreignTenantId> --expect-status candidate --expect-evidence absent --expect-gate-passed false --expect-rollback-eligible false --json
```

It may query once and must exit before seed/model/evidence/status code. Its JSON includes a
deployment URL hash, exact candidate, stored lineage, rollback baseline, current effective row and
foreign effective row. It never prints either body.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute authenticated authoring and pinned-prompt browser proof once</name>
  <files>apps/web/e2e/skill-authoring.spec.ts</files>
  <behavior>
    - User A opens the workspace panel, selects one supported specialist, publishes one unique
      adaptation exactly once, and observes candidate/awaiting-eval while the active version is unchanged.
    - The publish response's exact `candidateId` is captured as a refs-only Playwright attachment;
      no later browser step publishes another candidate.
    - No Activate control or hidden base body is present on the user surface.
    - A real user message is pinned, reload survives, Run opens a distinct fresh thread through the
      ordinary activity/plan path, and deleting the pin preserves both chats.
    - A separately named, read-only `refs-only post-live readback` case is skipped unless
      `PHASE21_LIVE_RESULT_PATH` is set; when set by Plan 21-07 it loads only the closed refs artifact,
      freshly proves the pin is absent and both distinct thread ids remain, reruns all five privacy
      needle counts, and requires every fresh refs/count result to equal the artifact exactly.
    - Desktop/narrow keyboard focus and loading/empty/busy/error-safe selectors are exercised.
  </behavior>
  <action>
Create and run one narrowly named Playwright spec using the existing authenticated project. Use a
unique high-entropy adaptation/prompt needle per run so append-only history cannot satisfy a stale
assertion. Drive only real UI/public tenant mutations.

Capture the exact candidate id returned by the ONE publish mutation with `testInfo.attach` as
`phase21-candidate-ref.json`; the attachment contains only `{candidateId,publishedAt}` and no body,
prompt, output, email, token, tenant guess or version guess. Do not republish after capturing it and
do not recover it by querying newest candidate. The spec stops at pending eval and spends $0.

If auth/local stack is unavailable or the exact id cannot be captured, record the environmental
blocker and do not create the live handoff. An authored-but-unexecuted spec is not a pass.

Also implement the read-only post-live case in this same spec. It must authenticate as the same
workspace user, reject result files with content fields, perform no publish/pin/run/delete/write,
look up the exact pinnedPromptId/sourceThreadId/runThreadId from the refs-only result, and freshly
compare pin removal, both thread-existence booleans, distinct ids, and audit/evidence/log/DLQ/
telemetry needle counts. Plan 21-07 invokes this exact named case after the live checkpoint.
  </action>
  <verify><automated>pnpm --filter web exec playwright test e2e/skill-authoring.spec.ts --project=chromium --workers=1</automated></verify>
  <done>The browser published one exact parked candidate and proved routine-v0 fresh-thread behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run the fast focused smoke, mutation ledger, and complete free gate</name>
  <files>repository tests/build only; no source edits expected</files>
  <action>
Run the exact fast focused smoke from 21-VALIDATION first: contracts authoring; backend
skills/dispatch/savedPrompts/importGuard; three focused web source tests; golden `--self-check`.
Then execute every mutation-ledger row, observe the named positive-witness test red, restore only the
deliberate hunk, and rerun the owning command green.

Only after those pass, run full package tests, repository typecheck, runner self-check, production
web build, playbook watcher and diff check. Run the privacy/source scans for routines/cron/trigger,
raw browser cockpit action, non-owner activation, caller-supplied provenance/state, and authored/
pinned text in evidence/audit/DLQ/telemetry/errors. Any red, skip, un-restored mutation or unrun gate
blocks Task 3 and all spend.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts exec vitest run src/skillAuthoring.test.ts &amp;&amp; pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts convex/dispatch.test.ts convex/savedPrompts.test.ts convex/importGuard.test.ts --maxWorkers=1 &amp;&amp; pnpm --filter web exec vitest run 'app/(app)/dashboard/workspace/skillAuthoring.test.ts' 'app/(app)/dashboard/workspace/pinnedPrompts.test.ts' 'app/(app)/ops/tenantSkillReview.test.ts' &amp;&amp; pnpm --filter @pikar/backend eval:golden -- --self-check</automated>
    <automated>pnpm test &amp;&amp; pnpm typecheck &amp;&amp; pnpm --filter @pikar/backend eval:golden -- --self-check &amp;&amp; pnpm --filter web build &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>All free, structural, privacy, mutation, build and browser gates are green before handoff.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the exact candidate and baseline into a refs-only live handoff</name>
  <files>.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json</files>
  <action>
Read `candidateId` from Task 1's Playwright attachment and the independently supplied real user-B
tenant ref from `PHASE21_FOREIGN_TENANT_ID`. In ONE PowerShell block, call the exact JSON inspection
command twice. Require byte-equivalent canonical JSON and machine-assert all of these before writing:

- deployment URL hash is nonempty;
- candidate id equals the browser attachment exactly; tenantId/name/version/bodyHash are nonempty;
- `author === "user"`, authorUserId is nonempty, status is `candidate`, evidence is absent,
  gatePassed is false, rollbackEligible is false;
- lineage contains basedOnScope/name/version and exactly the appropriate global/tenant base id;
- rollback baseline has exact tenant skill id/name/version/bodyHash, status `archived`, author
  `system`, and rollbackEligible true;
- prior/current effective row has exact scope/id/name/version/bodyHash and is active;
- rollback-baseline hash equals the prior effective hash copied at first customization;
- foreign tenant id is the supplied B ref, candidateIdVisible is false, and its effective id/hash
  differ from A's candidate id/hash.

Write exactly one `phase21-live-handoff.v1` JSON object containing `deploymentUrlHash`, `capturedAt`,
the full refs-only candidate identity/provenance/lineage/state, rollback baseline, prior effective
active row, global active row, foreign tenant/effective pre-state, and browser candidate attachment ref
`{attachmentName,candidateId,publishedAt}`. Map the
inspection output's `currentEffective` to handoff `effectiveBefore`, `globalCurrent` to handoff
`globalBefore`, and `foreignCurrent` to handoff `foreignBefore`. Include no
bodies, authoredBody, prompt text, fixture/model output, raw evidence JSON, email, secret or token.
Validate exact allowed keys and high-entropy needle absence. This artifact is immutable input to
Plan 21-07: do not update it after paid work.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json',h=JSON.parse(fs.readFileSync(p,'utf8')),eq=(o,ks,n)=>{const a=Object.keys(o).sort(),b=[...ks].sort();if(JSON.stringify(a)!==JSON.stringify(b))throw Error(n+' keys '+a)},need=(o,ks,n)=>ks.forEach(k=>{if(o[k]===undefined||o[k]===null||o[k]==='')throw Error(n+' '+k)}); eq(h,['schema','deploymentUrlHash','capturedAt','candidate','rollbackBaseline','effectiveBefore','globalBefore','foreignBefore','browserRef'],'root'); need(h,['deploymentUrlHash','capturedAt'],'root'); if(h.schema!=='phase21-live-handoff.v1')throw Error('schema'); const c=h.candidate,l=c.lineage,b=h.rollbackBaseline,e=h.effectiveBefore,g=h.globalBefore,f=h.foreignBefore,br=h.browserRef; eq(c,['id','tenantId','name','version','bodyHash','author','authorUserId','status','rollbackEligible','lineage','evidenceState','gatePassed','evidenceTarget','evidenceSummary'],'candidate'); need(c,['id','tenantId','name','version','bodyHash','authorUserId','lineage'],'candidate'); const baseKey=l.basedOnScope==='global'?'basedOnGlobalSkillId':l.basedOnScope==='tenant'?'basedOnTenantSkillId':null;if(!baseKey)throw Error('lineage scope');eq(l,['basedOnScope','basedOnName','basedOnVersion',baseKey],'lineage');need(l,['basedOnScope','basedOnName','basedOnVersion',baseKey],'lineage'); if(c.author!=='user'||c.status!=='candidate'||c.gatePassed!==false||c.rollbackEligible!==false||c.evidenceState!=='absent'||c.evidenceTarget!==null||c.evidenceSummary!==null)throw Error('candidate state'); eq(b,['id','tenantId','name','version','bodyHash','author','status','rollbackEligible'],'baseline'); need(b,['id','tenantId','name','version','bodyHash','author','status'],'baseline'); if(b.tenantId!==c.tenantId||b.name!==c.name||b.status!=='archived'||b.author!=='system'||b.rollbackEligible!==true)throw Error('baseline'); for(const [x,n] of [[e,'effective'],[g,'global']]){eq(x,['scope','id','name','version','bodyHash','status'],n);need(x,['scope','id','name','version','bodyHash','status'],n)} if(e.name!==c.name||e.status!=='active'||b.bodyHash!==e.bodyHash||g.scope!=='global'||g.status!=='active')throw Error('effective'); eq(f,['tenantId','candidateIdVisible','effective'],'foreign'); eq(f.effective,['scope','id','name','version','bodyHash','status'],'foreign effective'); if(!f.tenantId||f.tenantId===c.tenantId||f.candidateIdVisible!==false||f.effective.id===c.id||f.effective.bodyHash===c.bodyHash)throw Error('foreign'); eq(br,['attachmentName','candidateId','publishedAt'],'browserRef'); if(br.attachmentName!=='phase21-candidate-ref.json'||br.candidateId!==c.id||!br.publishedAt)throw Error('browser ref'); const forbidden=new Set(['body','authoredBody','prompt','promptText','fixture','modelOutput','rawEvidence','email','secret','token']),walk=o=>{if(o&amp;&amp;typeof o==='object')for(const [k,v] of Object.entries(o)){if(forbidden.has(k))throw Error('content key '+k);walk(v)}}; walk(h); if(JSON.stringify(h).includes('PHASE21_PRIVATE_NEEDLE'))throw Error('content needle'); console.log('handoff ok')"</automated>
  </verify>
  <done>One immutable refs-only artifact pins the exact already-published candidate and every pre-live comparison value.</done>
</task>

</tasks>

<verification>
- Authenticated Playwright executed and published exactly one candidate.
- Fast focused smoke, all mutation reds/restores, full tests/typecheck/self-check/build/playbooks/diff are green.
- Two identical read-only snapshots produce one schema-valid refs-only handoff.
- Candidate, baseline, effective-before and foreign-before identities/hashes are machine asserted.
- No paid eval, activation, rollback or completion marker occurred.
</verification>

<success_criteria>
- Free/browser readiness is proven independently of the paid owner gate.
- The exact candidate id from the one browser publish—not a replacement or newest guess—is frozen.
- The handoff contains every value needed for exact pre/post/live comparisons and no content.
- Plan 21-07 can proceed without republishing or relying on an ephemeral shell variable.
</success_criteria>

<output>
Create `.planning/phases/21-user-authored-skills-and-routines/21-06-SUMMARY.md` recording browser,
focused/full free-gate and handoff validation results. Keep SKILL-01 and Phase 21 pending; the paid
eval/owner/runtime/rollback closure is exclusively Plan 21-07.
</output>
