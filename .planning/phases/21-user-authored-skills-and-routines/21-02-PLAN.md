---
phase: 21-user-authored-skills-and-routines
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/runCockpitAgent.test.ts
  - apps/web/app/(app)/dashboard/workspace/SkillAuthoringPanel.tsx
  - apps/web/app/(app)/dashboard/workspace/skillAuthoring.test.ts
  - apps/web/app/(app)/dashboard/workspace/page.tsx
  - docs/playbooks/skill-registry.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "A signed-in user can publish a bounded business adaptation only as an immutable tenant candidate with author=user; no caller controls tenant, author, status, version, evidence, base body, or rollback eligibility."
    - "The first customization creates one server-owned rollback baseline plus one user candidate atomically; repeated identical publication does not churn versions or audits."
    - "Tenant next-version and newest-candidate lookup use by_tenant_name_version in descending order with take(1); cost does not grow into an unbounded history collect."
    - "A tenant active specialist overrides the global specialist body only for that tenant, while a tenant with no active overlay still uses the existing global active row."
    - "The user authoring surface shows only labels, the user's adaptation, and honest status; it exposes neither hidden base prompts nor any Activate control."
  artifacts:
    - path: "packages/backend/convex/skills.ts"
      provides: "Shared immutable candidate insertion, tenant publisher/list, and effective loader"
      exports: ["publishUserCandidate", "myUserSkills", "getEffectiveSkill"]
    - path: "packages/backend/convex/llm.ts"
      provides: "Real specialist runtime consumption of tenant effective skill bodies"
    - path: "apps/web/app/(app)/dashboard/workspace/SkillAuthoringPanel.tsx"
      provides: "Bounded candidate-only authoring UI for the three supported business specialists"
    - path: "packages/backend/convex/skills.test.ts"
      provides: "Candidate-only, immutable baseline, bounded many-version allocation, idempotence, isolation, fallback, audit/privacy, and mutation proofs"
  key_links:
    - from: "skills.publishUserCandidate"
      to: "the existing insertCandidate seam"
      via: "one shared local immutable-version insertion helper; global insertCandidate behavior stays unchanged"
      pattern: "status: \"candidate\""
    - from: "llm.runSpecialistTurn"
      to: "skills.getEffectiveSkill"
      via: "trusted tenantId plus specialist skillName; no model-supplied tenant"
      pattern: "getEffectiveSkill"
    - from: "tenant next-version allocation"
      to: "tenantSkills.by_tenant_name_version"
      via: "order desc then take(1), reused for newest-candidate idempotence"
      pattern: "order(\"desc\").take(1)"
    - from: "SkillAuthoringPanel"
      to: "api.skills.publishUserCandidate"
      via: "name + authoredBody only"
      pattern: "publishUserCandidate"
---

<objective>
Build the tenant candidate write/read path, prove the real specialist runtime consumes an activated
tenant overlay, and add the user-facing candidate-only authoring surface.

Purpose: a tenant row that never reaches a real model call is not a skill capability. This plan
connects immutable publication to the shipped specialist loop while leaving evaluation and activation
for the later owner-gate plans.

Output: candidate-only publisher, effective loader, isolation/provenance tests, and an honest
workspace authoring panel. No eval evidence, owner activation, or paid run occurs here.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/design/BRAND.md
@.planning/phases/21-user-authored-skills-and-routines/21-RESEARCH.md
@.planning/phases/21-user-authored-skills-and-routines/21-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-01-SUMMARY.md
@packages/backend/AGENTS.md
@packages/backend/convex/_generated/ai/guidelines.md
@packages/backend/convex/skills.ts
@packages/backend/convex/skills.test.ts
@packages/backend/convex/llm.ts
@packages/core/src/specialists.ts
@apps/web/app/(app)/dashboard/workspace/page.tsx
@docs/playbooks/skill-registry.md
@docs/playbooks/cockpit.md

<interfaces>
Preserve the current global exports and semantics:

```ts
insertCandidate({name,body})        // internal SkillOpt write-back, global gated candidate only
getActiveSkill({name})              // global active read
getSkillVersion({name,version})     // global exact read
activateCandidate({name,version})   // owner global activation; unchanged in this plan
```

`runSpecialistTurn` already receives trusted `tenantId` and code-owned `skillName`; it is the
smallest real runtime seam for all three v0 authorable targets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor one immutable insertion core and publish tenant user candidates</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - `publishUserCandidate` accepts only `{name, authoredBody}` and derives tenant/user from the
      authenticated wrapper; an unknown or non-authorable name, blank body, and over-cap body refuse
      before any row or audit write.
    - First publication inserts an archived system baseline copied from the current effective global
      active row and a version-2 user candidate in one mutation. The baseline is rollback-eligible;
      the candidate is not.
    - Later publication bases on the tenant's active row. It never recursively appends a prior draft.
    - A byte-identical newest candidate with the same base returns `inserted:false`, keeps row counts
      fixed, and writes no second audit event.
    - After at least 200 tenant-local historical versions, publication reads only the newest indexed
      row, allocates the exact next version, and never calls `.collect()` for allocation/idempotence.
    - Existing global `insertCandidate` tests remain green and its public/internal signature and
      behavior do not change.
    - One new candidate writes one `skill.user_candidate_published` audit payload containing only
      skillName, candidate id/version, base scope/version/id, author enum, body hash and byte count.
  </behavior>
  <action>
Extract the current `insertCandidate` transaction into a module-local shared helper that allocates
the next immutable version for an explicit registry scope. For tenant scope, query
`by_tenant_name_version`, call `.order("desc").take(1)`, and reuse that single newest row for both
`nextVersion` and byte/lineage idempotence. Never `.collect()` tenant history. Keep the exported global
`insertCandidate({name,body})` wrapper byte-compatible for SkillOpt. Add
`publishUserCandidate` as a `tenantMutation`; never accept authorization/provenance/state fields.

Within the tenant transaction, validate through the Plan-01 contract, resolve the effective active
base server-side, and compose the complete body server-side. For the first tenant customization,
insert exactly one system baseline copied from the global active body, then the user candidate.
Populate exactly one of the global/tenant base-id fields and enforce the cross-field invariant in
the helper. Existing tenant rows are never patched except later sanctioned status/evidence fields.

Add `myUserSkills` as a bounded tenant query (`take`, never unbounded `collect`) returning labels,
authoredBody, version/status, base scope/version, `gatePassed` boolean, and createdAt. It returns no
base/full body, raw evidence, other tenant id, or owner data. Use the tenant index; foreign ids are
not accepted by this read.

Extend `skills.test.ts` with two authenticated users and unique adaptation needles. Pair every zero
assertion with a positive row witness. Seed at least 200 prior versions, publish once, and assert the
next exact version plus a source contract scoped to the tenant allocation helper that positively
finds `by_tenant_name_version` + descending `take(1)` and rejects `.collect()`.

Run and record these exact mutation checks, restoring each edit immediately and rerunning green:

1. change the publisher's inserted status from `candidate` to `active` -> the candidate-only/prior-
   active assertions in `skills.test.ts` turn red;
2. remove the `tenantId` equality from the newest-candidate query -> the two-tenant collision test
   returns the foreign row and turns red;
3. populate `authorUserId` from a fake argument instead of `ctx.userId` -> the provenance test turns red;
4. replace descending `take(1)` with `.collect()` -> the bounded-allocation source assertion turns red;
5. remove the initial system baseline insert -> the first-customization rollback-target test turns red;
6. add `authoredBody` or full `body` to `skill.user_candidate_published.payload` -> the exact-key/high-
   entropy audit scan turns red while the positive audit-row witness remains present.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Users can create only tenant-owned immutable candidates with exact provenance and one
  rollback baseline; global SkillOpt insertion remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve tenant active specialist first and global active as fallback</name>
  <files>packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts, packages/backend/convex/llm.ts, packages/backend/convex/runCockpitAgent.test.ts</files>
  <behavior>
    - `getEffectiveSkill({tenantId,name})` returns exactly one tenant active row when present.
    - With no tenant active row it returns the existing global active row and preserves fail-closed
      `NO_ACTIVE_SKILL` when neither exists.
    - Tenant A's active adaptation needle reaches A's specialist model system prompt; tenant B sees
      the global body and never A's needle.
    - Candidate rows never affect ordinary runtime loading.
    - Code-owned specialist tool grants are byte/identity unchanged by the body overlay.
  </behavior>
  <action>
Add a pure DB helper `loadEffectiveSkill(ctx, tenantId, name)` and an `internalQuery`
`getEffectiveSkill`. Return the resolved row identity and explicit registry scope alongside body and
version so Plan 03 can attribute the exact runtime use. Query the tenant overlay by the full
tenant/name/status index with `.unique()`; fall back to the existing `loadSkill` global helper, not a
duplicated global query.

In `llm.runSpecialistTurn`, replace only the ordinary active-body read with
`internal.skills.getEffectiveSkill({tenantId,name: skillName})`. Leave existing global exact-version
pin behavior intact until Plan 03 adds exact tenant pins. Do not thread tenant overlays into voice,
inbox, reply, extraction, or unrelated skill paths; those names are not authorable in v0.

Use the mock-model integration seam to inspect the actual system prompt for two tenants and assert
the unique adaptation needle reaches only the author tenant. Assert `SPECIALISTS[route].tools` is
unchanged: prompt rows advise behavior but never grant capabilities. Mutation-remove the active
loader's tenant predicate and expect the cross-tenant runtime needle test to turn red; restore it.
Mutation-remove global fallback and expect the no-overlay tenant case to turn red with
`NO_ACTIVE_SKILL`; restore it and rerun the focused command green.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts convex/runCockpitAgent.test.ts --maxWorkers=1</automated></verify>
  <done>All three supported specialist routes consume tenant active bodies in production while
  global fallback and code-owned tool grants remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the candidate-only workspace authoring panel</name>
  <files>apps/web/app/(app)/dashboard/workspace/SkillAuthoringPanel.tsx, apps/web/app/(app)/dashboard/workspace/skillAuthoring.test.ts, apps/web/app/(app)/dashboard/workspace/page.tsx</files>
  <behavior>
    - Chat options offers `Adapt a business skill`; opening it renders a white card/panel without a
      new route or navigation item.
    - The only choices and labels come from the shared three-name contract.
    - Publish sends `{name,authoredBody}` only and shows loading, validation, candidate-awaiting-eval,
      passing-awaiting-owner, active, and inline error states in words, not color alone.
    - No `Activate`, eval-fixture content, global base body, raw evidence, tool selector, schedule,
      trigger, or routine builder appears.
    - Keyboard focus and buttons are real controls using existing tokens; no hardcoded token-covered hex.
  </behavior>
  <action>
Create `SkillAuthoringPanel` beside the workspace and toggle it from the existing Chat options menu.
Use the existing card/menu idioms and CSS variables; do not add a component library or route. Render
a select/radio group for the closed metadata record, a bounded textarea whose copy names the byte
limit, and a `Publish candidate` button. Query `myUserSkills` for current states and call only
`publishUserCandidate`.

The user authors an adaptation, not a replacement: copy must explain that Pikar preserves the core
skill and that publishing changes nothing live until evaluation and owner review. Never render the
full composed/base body. Keep candidate text on the content plane and out of errors/logging.

Add a node/source contract test (the existing workspace test idiom) that positively finds the
publisher and three options, proves no activation API/import/copy exists in the panel, and proves
the UI does not use a raw tenant/user/status/version/evidence field. This repository has no general
client DOM harness; do not add one for this panel.
  </action>
  <verify><automated>pnpm --filter web exec vitest run 'app/(app)/dashboard/workspace/skillAuthoring.test.ts' &amp;&amp; pnpm --filter web typecheck</automated></verify>
  <done>A user can publish and inspect honest candidate states but has no activation, fixture, base
  prompt, capability, or automation control.</done>
</task>

<task type="auto">
  <name>Task 4: Update registry and cockpit playbooks for the live authoring seam</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/cockpit.md</files>
  <action>
Document the shared insertion helper, candidate-only public args, initial rollback baseline,
idempotence/audit payload, effective specialist load order, exact v0 target set, and user-panel
disclosure boundary. State that evaluation/activation are still unpaid and unimplemented until
Plans 03-04; publishing costs $0 and cannot affect active runtime. In `cockpit.md`, document the
workspace panel and explicitly distinguish it from Plan 05's pinned prompts.
  </action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>The playbooks describe the real candidate/runtime seam without claiming evaluation or
  activation has occurred.</done>
</task>

</tasks>

<verification>
- Focused backend and web tests pass.
- `pnpm --filter @pikar/backend typecheck` and `pnpm --filter web typecheck` pass.
- `node scripts/check-playbooks.mjs` and `git diff --check` pass.
- Deliberate status/tenant/provenance mutations turn the named tests red and are reverted.
- The 200-version allocation/no-collect mutation and baseline/fallback/privacy mutations turn their
  named positive-witness tests red, are restored, and the focused suite reruns green.
- No model/eval call, activation, schedule, cron, or outbound action runs in this plan.
</verification>

<success_criteria>
- Authenticated users can publish only immutable tenant candidates with author=user.
- First customization has a real immutable rollback baseline.
- Ordinary specialist runtime honors the author tenant's active overlay and isolates every other tenant.
- The user surface exposes candidate publication/status only.
</success_criteria>

<output>
Create `.planning/phases/21-user-authored-skills-and-routines/21-02-SUMMARY.md`. Keep SKILL-01
pending: tenant eval evidence and owner activation are Plans 03-04, and pinned prompts are Plan 05.
</output>
