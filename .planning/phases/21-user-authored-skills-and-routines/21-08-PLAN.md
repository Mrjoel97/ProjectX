---
phase: 21-user-authored-skills-and-routines
plan: 08
type: execute
wave: 7
depends_on: ["21-07"]
files_modified:
  - .planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json
  - docs/playbooks/skill-registry.md
  - docs/playbooks/cockpit.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SKILL-01]

must_haves:
  truths:
    - "Every step in this plan costs $0. Rollback requires no eval, activation requires no eval, and the runtime witnesses reuse the already-purchased evidence — so any step may be retried freely without re-buying the gate."
    - "The candidate handed over by 21-07 is `status: candidate`, `evidenceState: passing`, `gatePassed: true`, and its bodyHash still equals the immutable handoff."
    - "A non-owner is refused by the SERVER with state unchanged, and can read neither the candidate nor A's pinned prompt."
    - "Runtime attribution names the exact tenant candidate for A, while B is a mandatory NON-NULL positive witness: same specialist name, different skill id AND different body hash."
    - "Rollback activates the exact handoff baseline id/hash, preserves the candidate's immutable hash and evidence, and changes no global or foreign row."
    - "SKILL-01 and Phase 21 are marked complete only after the machine gate AND the human owner checkpoint both pass."
  artifacts:
    - path: ".planning/phases/21-user-authored-skills-and-routines/21-EVAL-EVIDENCE.json"
      provides: "The purchased gate from Plan 21-07 — read-only input, never re-bought"
    - path: ".planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json"
      provides: "Refs-only activation/runtime/foreign/rollback/prompt/privacy results bound to the handoff hash"
      contains: "phase21-live-result.v1"
    - path: "docs/playbooks/skill-registry.md"
      provides: "Executed owner/runtime/rollback evidence for the first tenant-scoped candidate"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Truthful SKILL-01 completion only after all gates"
  key_links:
    - from: "handoff candidate id/version/bodyHash"
      to: "subagent.completed"
      via: "bounded smoke:userSkillRuntimeAttribution correlation readback"
      pattern: "userSkillRuntimeAttribution"
    - from: "handoff rollbackBaseline id/bodyHash"
      to: "rollbackTenantSkill"
      via: "exact owner target then current-effective readback"
      pattern: "rollbackBaseline"
    - from: "every handoff-to-live comparison"
      to: "packages/backend/scripts/compare-refs.mjs"
      via: "one committed comparator, called; never re-inlined"
      pattern: "compare-refs"
---

<objective>
Take the candidate that Plan 21-07 already paid for, prove the owner boundary, the tenant runtime
attribution and the exact-baseline rollback, and close SKILL-01 only after machine and human
evidence agree.

Purpose: this plan holds everything in the Phase 21 live gate that is FREE and DETERMINISTIC. It was
split out of 21-07 because bundling it behind the same blocking checkpoint as the paid eval meant a
fixture flake discarded work that had already passed. Nothing here calls a model. Every step can be
re-run at no cost.

Output: refs-only live result artifact, final playbooks, truthful requirement/roadmap closure.
No new candidate publication, no prompt/runtime mechanism, no schema or capability change.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/21-user-authored-skills-and-routines/21-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-07-SUMMARY.md
@.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json
@.planning/phases/21-user-authored-skills-and-routines/21-EVAL-EVIDENCE.json
@docs/playbooks/skill-registry.md
@docs/playbooks/cockpit.md

<interfaces>
`21-LIVE-HANDOFF.json` and `21-EVAL-EVIDENCE.json` are both IMMUTABLE INPUTS. This plan reads their
literal ids and never republishes, re-freezes or re-evaluates. **`eval:golden` is not called with any
paid argument anywhere in this plan.** Rollback and activation both require no eval by design; if a
step here seems to need one, that is a signal to stop and replan, not to spend.

**THE COMPARATOR — the same one 21-07 uses, called, never re-inlined.** Full rationale is in
`21-07-PLAN.md` `<interfaces>`; the short version is that the previous cut of this plan hand-wrote
`ConvertTo-Json -Compress` string equality three times and every copy was broken two ways (a renamed
key compared against `$null`, and order/key-set-sensitive string compare of equal data).

```powershell
node packages/backend/scripts/compare-refs.mjs --self-check   # 14 assertions, $0, run it first
node packages/backend/scripts/compare-refs.mjs --expected $H --actual live.json --label post-live `
  --pair deploymentUrlHash=deploymentHash `
  --pair rollbackBaseline=rollbackBaseline --pair globalBefore=globalCurrent `
  --pair foreignBefore=foreignCurrent `
  --allow-extra rollbackBaseline.scope
```

Two allow-list facts, both VERIFIED rather than assumed, and both the only ones:

- `rollbackBaseline.scope` — `baselineRefs` (`convex/skills.ts:1123`) writes `"tenant"` as a
  hardcoded literal. It is never read from the row, so it cannot vary and cannot drift; 21-06's
  writer simply did not record it.
- `candidate.lineage.basedOnTenantSkillId` — live-only and `null`, which passes by rule with no
  flag. This candidate descends from a GLOBAL row, so there is no tenant parent.

**`--pair effectiveBefore=currentEffective` is NOT used after activation.** Current-effective is the
thing this plan deliberately moves — to the candidate, then back to the baseline. Compare it to the
expected post-state explicitly at each step instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the purchased gate is intact and still bound to the frozen handoff</name>
  <files>21-LIVE-HANDOFF.json, 21-EVAL-EVIDENCE.json, deployment state — all read-only</files>
  <action>
Verify `21-EVAL-EVIDENCE.json`'s `handoffSha256` against the unchanged handoff bytes, then inspect
the candidate id and require: `status` still `candidate`, `evidenceState` `passing`, `gatePassed`
true, `evidenceTarget` exactly the candidate's own id/tenant/name/version, and every immutable
identity/provenance/lineage/bodyHash field equal to the handoff via `compare-refs`. The baseline is
still archived/eligible, prior effective still active, foreign state unchanged.

**If the evidence is absent or the row is not `candidate`, STOP.** Absent evidence means the gate was
lost (a deployment reset, or someone re-seeded); that is 21-07's to re-buy under fresh authorization,
never this plan's to work around. A row already `active` means someone activated outside this plan —
diagnose before touching anything.
  </action>
  <verify><automated>node packages/backend/scripts/compare-refs.mjs --self-check</automated></verify>
  <verify><automated>$H='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json'; $E='.planning/phases/21-user-authored-skills-and-routines/21-EVAL-EVIDENCE.json'; $h=Get-Content -Raw $H | ConvertFrom-Json; $e=Get-Content -Raw $E | ConvertFrom-Json; $sha=(Get-FileHash $H -Algorithm SHA256).Hash.ToLower(); if($e.handoffSha256 -cne $sha){throw 'the handoff bytes changed — it is immutable'}; $tmp=New-TemporaryFile; pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill=$h.candidate.id --foreign-tenant=$h.foreignBefore.tenantId --expect-status=candidate --expect-evidence=passing --expect-gate-passed=true --json | Set-Content -Encoding utf8 $tmp; if($LASTEXITCODE -ne 0){throw 'the purchased gate is not on the row — do NOT re-buy it here'}; node packages/backend/scripts/compare-refs.mjs --expected $H --actual $tmp --label pre-activation --pair deploymentUrlHash=deploymentHash --pair rollbackBaseline=rollbackBaseline --pair effectiveBefore=currentEffective --pair globalBefore=globalCurrent --pair foreignBefore=foreignCurrent --allow-extra rollbackBaseline.scope; if($LASTEXITCODE -ne 0){throw 'handoff mismatch'}; Remove-Item $tmp</automated></verify>
  <done>The paid evidence sits on the frozen row, bound to unchanged handoff bytes, at $0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner boundary, activation, two-tenant runtime attribution, and exact-baseline rollback</name>
  <files>.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json, deployment state</files>
  <action>
**This checkpoint is blocking because it needs two real identities and an owner's hands, NOT because
it spends.** Every step below is `$0`. State it that way when requesting the gate: a red step here
costs a re-run, never a re-purchase. Re-run Task 1 in the same shell first and retain `$h`.

1. As non-owner user B, call activation on `$h.candidate.id` and require `OWNER_REQUIRED` with state
   unchanged. Authenticated B list/read APIs must reveal neither the candidate, nor the full or
   authored body, nor A's pinned prompt.
2. As owner, activate `$h.candidate.id`. Inspect: active, `gatePassed` true with passing evidence,
   same bodyHash and immutable fields; the baseline is now archived; global and foreign rows
   unchanged.
3. In A's workspace, execute the supported specialist, capture `rootRequestId`, and query
   `smoke:userSkillRuntimeAttribution`. Require scope `tenant` and the exact handoff candidate
   id/name/version/bodyHash.
4. Execute the corresponding B path, capture B's correlation, and require its attribution is
   **non-null** for the same specialist name with BOTH a different skill id and a different body
   hash. This positive-witness rule is deliberate and is the one allowed shape: **null is a
   FAILURE** (it proves nothing about isolation — an absent record is equally consistent with the
   query being broken), and a record carrying either A's candidate id or A's candidate hash is a
   failure.
5. Owner rollback targets `$h.rollbackBaseline.id` EXACTLY — not "the previous version", not a
   name/version lookup. Inspect: candidate archived and still passing/eligible with the same
   hash/evidence target; current effective is the handoff rollback baseline id/version/bodyHash with
   status active. Global and B rows unchanged. **Confirm and record that rollback required no eval** —
   the structural exemption is Success Criterion 2 of this phase, and the only way to observe it is
   to notice that nothing was purchased.
6. Reconfirm A's live pin/run/delete fresh-thread behaviour and the responsive/focus/honest states.
7. Scan the audit, evidence, log, DLQ and telemetry planes for the high-entropy needles: refs, hashes
   and counts only (CLAUDE.md §4).

Then write one closed `phase21-live-result.v1` object with exactly these root keys: `schema`,
`handoffSha256`, `evalEvidenceSha256`, `deploymentHash`, `recordedAt`, `nonOwner`, `activation`,
`authorRuntime`, `foreignRuntime`, `rollback`, `promptRun`, `privacy`, `ownerReviewedAt`.

- `evalEvidenceSha256` binds this result to the exact purchased gate, so a result can never be
  paired with a different run's evidence. `evalRun` itself does NOT reappear here — it lives in
  `21-EVAL-EVIDENCE.json` and duplicating it would create two sources of truth for one run.
- `nonOwner`: candidateId/bodyHash, `OWNER_REQUIRED`, stateUnchanged, unchanged global/foreign snapshots.
- `activation`: exact active candidate snapshot plus baseline, global and foreign snapshots.
- both runtime records: exact `{tenantId,correlationId}` queryArgs, correlationId, scope, skillId,
  skillName, skillVersion, skillBodyHash, candidateAbsent. A is the exact tenant candidate with
  `candidateAbsent` false. B is the mandatory non-null same-name / different-id / different-hash
  positive witness with `candidateAbsent` true.
- `rollback`: exact targetId, archived candidate snapshot, final currentEffective, unchanged global
  and foreign snapshots, and `requiredEval: false`.
- `promptRun`: pinnedPromptId, distinct sourceThreadId/runThreadId, pinRemoved, sourceThreadExists,
  runThreadExists.
- `privacy`: passed plus zero audit/evidence/log/DLQ/telemetry needle counts.

All timestamps ISO strings. Refs-only: no bodies, authoredBody, prompt/fixture/model output,
evidence JSON, message or content text, email, secret, token or provider response. Task 3 rejects
every extra and missing key recursively and every forbidden key or value rather than trusting this
checkpoint's narrative.
  </action>
  <verify>Owner records a complete PASS and the refs-only result file. Every machine comparison uses
  the immutable handoff values; user B remains absent from A's plane; no republish and no paid
  command occurred.</verify>
  <done>The frozen candidate went owner-activation -> attributed tenant runtime -> exact baseline
  rollback, with two-tenant and content-plane boundaries intact, at $0.</done>
  <resume-signal>Resume by re-running the failed step — it is free. Do not re-enter Plan 21-07 and do not re-buy the gate unless Task 1 proves the evidence itself is gone.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Machine-verify post-live identities, seal playbooks, and close SKILL-01</name>
  <files>21-LIVE-RESULT.json, docs/playbooks/skill-registry.md, docs/playbooks/cockpit.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
After—and only after—Task 2 PASS. Load the handoff, the eval evidence and the result; verify both
SHA-256 bindings against unchanged bytes; re-query the candidate with the handoff foreign tenant.

Machine-assert through `compare-refs`: every immutable candidate identity/provenance/lineage/bodyHash
field equals the handoff; evidence is passing, `gatePassed` true, `evidenceTarget` exact; the
candidate is archived and rollback-eligible; current effective is the handoff rollback baseline
id/name/version/bodyHash and active; global and foreign snapshots — including their body hashes —
equal the immutable handoff exactly; foreign visibility false; foreign effective id and hash both
differ from A's candidate.

Query A's stored correlation and require the exact tenant candidate id/name/version/hash. Query B's
and require the positive-witness rule from Task 2: non-null, same specialist name, different skill id
AND different body hash. Null fails; either candidate value fails.

Re-run the authenticated refs-only browser readback against the stored pinned-prompt/thread ids and
every privacy-plane count. Verify every result field against the fresh readbacks and the immutable
handoff. Recursively reject any missing or extra key, forbidden content key, private needle or
email-like string. Any mismatch leaves the requirement and roadmap PENDING.

Only then: append deployment-safe evidence to both playbooks and bump their `Last verified` lines,
mark SKILL-01 complete in checkbox + traceability, and update the Phase 21 roadmap entry to **eight
plans across seven waves** (the re-cut split the old Plan 07 into the paid gate and this one — say
so, so the count change reads as a structural split rather than as scope growth). Also update
`21-VALIDATION.md`: rows `21-07-02` and `21-07-03` moved to this plan and become `21-08-01` …
`21-08-03`.

Do not claim ISO approval, and do not claim any capability beyond candidate-only tenant adaptations
and manually re-runnable pinned prompts. Routine v0 remains a manual pinned-prompt rerun.
  </action>
  <verify><automated>node packages/backend/scripts/check-phase21-artifacts.mjs --self-check</automated></verify>
  <verify><automated>node packages/backend/scripts/check-phase21-artifacts.mjs --handoff .planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json --evidence .planning/phases/21-user-authored-skills-and-routines/21-EVAL-EVIDENCE.json --result .planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json</automated></verify>
  <verify><automated>$H='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json'; $h=Get-Content -Raw $H | ConvertFrom-Json; $r=Get-Content -Raw '.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json' | ConvertFrom-Json; $tmp=New-TemporaryFile; pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill=$h.candidate.id --foreign-tenant=$h.foreignBefore.tenantId --expect-status=archived --expect-evidence=passing --expect-gate-passed=true --expect-rollback-eligible=true --json | Set-Content -Encoding utf8 $tmp; if($LASTEXITCODE -ne 0){throw 'final inspection failed'}; node packages/backend/scripts/compare-refs.mjs --expected $H --actual $tmp --label post-live --pair deploymentUrlHash=deploymentHash --pair rollbackBaseline=rollbackBaseline --pair globalBefore=globalCurrent --pair foreignBefore=foreignCurrent --allow-extra rollbackBaseline.scope; if($LASTEXITCODE -ne 0){throw 'post-live handoff mismatch'}; $live=Get-Content -Raw $tmp | ConvertFrom-Json; if($live.currentEffective.id -cne $h.rollbackBaseline.id -or $live.currentEffective.bodyHash -cne $h.rollbackBaseline.bodyHash -or $live.currentEffective.status -cne 'active'){throw 'live current-effective is not the exact rollback baseline'}; $a=npx convex run smoke:userSkillRuntimeAttribution ($r.authorRuntime.queryArgs|ConvertTo-Json -Compress)|ConvertFrom-Json; if($LASTEXITCODE -ne 0 -or $null -eq $a){throw 'author readback'}; if($a.scope -cne 'tenant' -or $a.skillId -cne $h.candidate.id -or $a.skillName -cne $h.candidate.name -or [string]$a.skillVersion -cne [string]$h.candidate.version -or $a.skillBodyHash -cne $h.candidate.bodyHash){throw 'author attribution'}; $b=npx convex run smoke:userSkillRuntimeAttribution ($r.foreignRuntime.queryArgs|ConvertTo-Json -Compress)|ConvertFrom-Json; if($LASTEXITCODE -ne 0 -or $null -eq $b){throw 'foreign attribution must be non-null'}; if($b.skillName -cne $h.candidate.name -or $b.skillId -ceq $h.candidate.id -or $b.skillBodyHash -ceq $h.candidate.bodyHash){throw 'foreign positive-witness rule'}; Remove-Item $tmp</automated></verify>
  <verify><automated>$env:PHASE21_LIVE_RESULT_PATH='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json'; try { pnpm --filter web exec playwright test e2e/skill-authoring.spec.ts --project=chromium --workers=1 --grep "refs-only post-live readback"; if($LASTEXITCODE -ne 0){throw 'prompt/thread/privacy readback failed'} } finally { Remove-Item Env:PHASE21_LIVE_RESULT_PATH -ErrorAction SilentlyContinue }</automated></verify>
  <verify><automated>node scripts/check-playbooks.mjs; git diff --check</automated></verify>
  <done>Machine evidence and owner review agree, documentation matches reality, and SKILL-01 closes without identity drift.</done>
</task>

</tasks>

<verification>
- No paid command runs in this plan. No candidate is published, republished or re-evaluated.
- Every handoff-to-live comparison goes through `compare-refs.mjs`; zero inlined JSON string equality.
- Non-owner is refused SERVER-side with state unchanged and can read neither body nor pinned prompt.
- A's runtime correlation attributes the exact id/name/version/bodyHash; B is a mandatory non-null
  same-name positive witness with both a different id and a different body hash.
- Rollback restores the exact baseline id/hash, preserves the candidate's hash and evidence, and
  records that it required no eval.
- The result is refs-only and bound to BOTH the handoff hash and the purchased evidence hash.
- Playbooks, requirements and roadmap update only after PASS.
</verification>

<success_criteria>
- The evidence bought once in Plan 21-07 carries this plan end to end with no second purchase.
- Owner boundary, runtime attribution and exact-baseline rollback are machine-linked to the freeze.
- Foreign tenant and content-plane privacy remain intact before and after every state change.
- Rollback's structural eval-exemption is OBSERVED, not asserted.
- Routine v0 remains manual pinned-prompt rerun only.
- SKILL-01 and Phase 21 close truthfully after eight plans across seven waves.
</success_criteria>

<output>
Create `21-08-SUMMARY.md` with both SHA-256 bindings, the exact activation/runtime/foreign/rollback
refs, the automated commands, and the owner's approval verbatim.

Do not mark SKILL-01 or Phase 21 complete on a missing or mismatched handoff, an unbound evidence
file, an absent runtime attribution, a NULL B witness, a foreign leak, or a failed rollback.
</output>
