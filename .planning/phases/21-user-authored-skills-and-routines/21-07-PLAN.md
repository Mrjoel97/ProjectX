---
phase: 21-user-authored-skills-and-routines
plan: 07
type: execute
wave: 6
depends_on: ["21-06"]
files_modified:
  - .planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json
  - docs/playbooks/skill-registry.md
  - docs/playbooks/cockpit.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SKILL-01]

must_haves:
  truths:
    - "The paid run targets the exact candidate id frozen by Plan 21-06; Plan 21-07 never publishes or substitutes another candidate."
    - "A fresh read-only pre-check matches deployment, candidate identity/provenance/lineage/hash/state, rollback baseline, prior effective active row, and foreign pre-state byte-for-byte with the handoff."
    - "Exact passing evidence and gatePassed are machine-read on that same row before owner activation."
    - "Runtime subagent.completed attribution matches the same tenant skill id/name/version/body hash, while the foreign tenant cannot read or execute it."
    - "Rollback activates the exact handoff baseline id/hash, preserves immutable candidate hash/evidence, and changes no global or foreign row."
    - "SKILL-01 and Phase 21 are marked complete only after the post-live machine gate and human owner checkpoint pass."
  artifacts:
    - path: ".planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json"
      provides: "Immutable exact pre-live identity input from Plan 21-06"
    - path: ".planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json"
      provides: "Refs-only run/evidence/activation/runtime/foreign/rollback results bound to the handoff hash"
      contains: "phase21-live-result.v1"
    - path: "docs/playbooks/skill-registry.md"
      provides: "Executed exact tenant candidate eval/owner/runtime/rollback evidence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Truthful SKILL-01 completion only after all gates"
  key_links:
    - from: "21-LIVE-HANDOFF.json candidate.id"
      to: "eval:golden --tenant-skill"
      via: "literal exact id in the paid command; no publish/newest lookup"
      pattern: "tenant-skill"
    - from: "handoff candidate id/version/bodyHash"
      to: "subagent.completed"
      via: "bounded smoke:userSkillRuntimeAttribution correlation readback"
      pattern: "userSkillRuntimeAttribution"
    - from: "handoff rollbackBaseline id/bodyHash"
      to: "rollbackTenantSkill"
      via: "exact owner target then current-effective readback"
      pattern: "rollbackBaseline"
---

<objective>
Consume the immutable Phase 21 handoff, authorize one exact paid eval, prove owner activation and
real tenant runtime attribution, roll back to the exact baseline, and close SKILL-01 only after
machine and human evidence agree.

Purpose: this separate closing plan keeps paid state changes behind a blocking owner checkpoint and
eliminates the former republish/ephemeral-variable ambiguity.

Output: refs-only live result artifact, final playbooks, and truthful requirement/roadmap closure.
No new candidate publication, prompt/runtime mechanism, schema or capability is allowed.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/21-user-authored-skills-and-routines/21-RESEARCH.md
@.planning/phases/21-user-authored-skills-and-routines/21-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-06-SUMMARY.md
@.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json
@docs/playbooks/skill-registry.md
@docs/playbooks/cockpit.md

<interfaces>
`21-LIVE-HANDOFF.json` is immutable. Every command reads its literal `candidate.id`; no task calls
`publishUserCandidate`, drives the browser Publish control, or queries newest name/version.

Paid command after fresh authorization only:

```powershell
$h = Get-Content -Raw '.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json' | ConvertFrom-Json
pnpm --filter @pikar/backend eval:golden -- --tenant-skill $h.candidate.id
```

`--only` cannot record evidence. A prior approval does not authorize this run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate the immutable handoff against the live deployment at zero spend</name>
  <files>configured Convex deployment state and 21-LIVE-HANDOFF.json read-only</files>
  <action>
Load the handoff, hash its bytes, and call `--inspect-tenant-skill $h.candidate.id --foreign-tenant
$h.foreignBefore.tenantId --json` in the same PowerShell block. Do not publish anything.

Machine-compare deploymentUrlHash plus canonical JSON for candidate identity/provenance/lineage,
rollbackBaseline, live `currentEffective` against handoff `effectiveBefore`, and live
`foreignCurrent` against handoff `foreignBefore`. Require the
candidate still `candidate`, evidence absent, gatePassed false and rollbackEligible false; require
the baseline still archived/system/eligible; require prior effective still active; require baseline
hash equal prior-effective hash; require foreign candidate visibility false and foreign effective
id/hash unequal A's candidate. Also re-run every free Plan-06 summary command if its recorded commit/
tree no longer matches the current tree.

Any mismatch, missing field, changed deployment hash, later candidate, evidence already present,
foreign collision or failed free gate blocks the checkpoint. Do not “refresh” the handoff or
republish; diagnose/replan instead.
  </action>
  <verify><automated>$h=Get-Content -Raw '.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json' | ConvertFrom-Json; $live=pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill $h.candidate.id --foreign-tenant $h.foreignBefore.tenantId --expect-status candidate --expect-evidence absent --expect-gate-passed false --expect-rollback-eligible false --json | ConvertFrom-Json; if($LASTEXITCODE -ne 0){throw 'inspection failed'}; if($h.deploymentUrlHash -cne $live.deploymentUrlHash){throw 'deployment mismatch'}; foreach($pair in @(@('candidate','candidate'),@('rollbackBaseline','rollbackBaseline'),@('effectiveBefore','currentEffective'),@('globalBefore','globalCurrent'),@('foreignBefore','foreignCurrent'))){if(($h.($pair[0]) | ConvertTo-Json -Depth 12 -Compress) -cne ($live.($pair[1]) | ConvertTo-Json -Depth 12 -Compress)){throw "handoff mismatch $($pair[0])"}}</automated></verify>
  <done>The live deployment still matches every frozen pre-live identity and state at $0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Authorize and execute the exact paid eval, owner activation, runtime and rollback</name>
  <files>.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json, configured deployment state</files>
  <action>
PAUSE and request fresh explicit authorization for ONE full golden run, quoting current model/provider
and playbook cost estimate. Silence, generic “continue”, old approval or authorization for another
candidate/deployment is refusal. Before spending, rerun Task 1 in the same shell and retain `$h`.
Never call Publish or `publishUserCandidate` anywhere in this plan.

After authorization:

1. Run free `--self-check`, then exactly
   `eval:golden -- --tenant-skill $h.candidate.id`. No `--only`, fixture weakening, automatic command
   retry, evidence handwriting or name/version lookup. Red/flaky/interrupted/over-cap parks the exact
   candidate and blocks completion.
2. Inspect the same id. Machine-assert every immutable candidate identity/provenance/lineage/hash
   equals the handoff, status remains candidate, evidenceState is passing, gatePassed is true, and
   evidenceTarget exactly equals candidate id/tenant/name/version. Baseline/effective-before and
   foreign pre-state remain unchanged.
3. As non-owner user B, call exact activation and require `OWNER_REQUIRED` with state unchanged.
   Authenticated B list/read APIs reveal neither candidate/full/authored body nor A's pinned prompt.
4. As owner, activate `$h.candidate.id`. Inspect it active with gatePassed/passing evidence, same
   bodyHash and immutable fields; baseline is archived, global/foreign rows unchanged.
5. In A's workspace execute that supported specialist, capture rootRequestId, and query
   `smoke:userSkillRuntimeAttribution`. Require scope tenant and exact handoff candidate id/name/
   version/bodyHash. Execute the corresponding B path, capture B correlation, and require its
   attribution is non-null for the same specialist name but has both a different skill id and a
   different body hash. This positive B witness is the one allowed rule: null is a failure, and a
   record carrying either A's candidate id or A's candidate hash is a failure.
6. Owner rollback targets `$h.rollbackBaseline.id` exactly. Inspect candidate archived and still
   passing/eligible with the same hash/evidence target; require current effective is the handoff
   rollback baseline id/version/bodyHash with status active. Global and B rows remain unchanged and
   rollback required no eval.
7. Reconfirm A's live pin/run/delete fresh-thread behavior and responsive/focus/honest states.
   Scan audit/evidence/log planes for the high-entropy needles: refs/hashes/counts only.

After all steps pass, write one closed `phase21-live-result.v1` object with exactly these root keys:
`schema`, `handoffSha256`, `deploymentUrlHash`, `recordedAt`, `evalRun`, `nonOwner`, `activation`,
`authorRuntime`, `foreignRuntime`, `rollback`, `promptRun`, `privacy`, `ownerReviewedAt`.

- `evalRun`: runId, caseCount, retryCount, costUsd, model, exact candidate identity, exact
  evidenceTarget and gatePassed.
- `nonOwner`: candidateId/bodyHash, `OWNER_REQUIRED`, stateUnchanged, and unchanged global/foreign
  snapshots.
- `activation`: exact active candidate snapshot plus baseline, global and foreign snapshots.
- both runtime records: exact `{tenantId,correlationId}` queryArgs, correlationId, scope, skillId,
  skillName, skillVersion, skillBodyHash and candidateAbsent. A is exact tenant candidate with
  candidateAbsent false. B is the mandatory non-null same-name/different-id/different-hash positive
  witness with candidateAbsent true.
- `rollback`: exact targetId, archived candidate snapshot, final currentEffective, unchanged global
  and foreign snapshots.
- `promptRun`: pinnedPromptId, distinct sourceThreadId/runThreadId, pinRemoved,
  sourceThreadExists and runThreadExists.
- `privacy`: passed plus zero audit/evidence/log/DLQ/telemetry needle counts.

All timestamps are ISO strings. It is refs-only: no bodies, authoredBody, prompt/fixture/model
output, evidence JSON, message/content text, email, secret, token or provider response. Task 3 must
reject every extra/missing key recursively and every forbidden key/value rather than trusting the
checkpoint narrative.
  </action>
  <verify>Owner records a complete PASS and the refs-only result file. Every machine comparison uses
  the immutable handoff candidate/baseline values; user B remains absent; no republish occurred.</verify>
  <done>The exact frozen candidate completed eval -> owner activation -> attributed tenant runtime ->
  exact baseline rollback, with two-tenant and content-plane boundaries intact.</done>
  <resume-signal>Resume only after fresh spend authorization and complete recorded PASS evidence, or report the exact parked/red state without creating a passing result or marking SKILL-01 complete.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Machine-verify post-live identities, seal playbooks, and close SKILL-01</name>
  <files>.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json, docs/playbooks/skill-registry.md, docs/playbooks/cockpit.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
After—and only after—Task 2 PASS, load handoff + result, verify result.handoffSha256 against the
unchanged handoff bytes, and re-query the exact candidate with the handoff foreign tenant. Machine-
assert all immutable candidate identity/provenance/lineage/body-hash fields equal handoff; evidence
is passing, gatePassed true and evidenceTarget exact; candidate is archived/rollback-eligible;
current effective is the handoff rollback baseline id/name/version/bodyHash and active; global and
foreign snapshots—including their body hashes—equal the immutable handoff exactly, foreign
visibility is false, and foreign effective id/hash differ from A candidate.

Query A's result correlation and require exact tenant candidate id/name/version/hash. Query B's
result correlation and require the one exact positive-witness rule from Task 2: a non-null record
for the same specialist name with both a different skill id and different body hash; null or either
candidate value fails. Re-run the authenticated refs-only browser readback against the stored
pinned-prompt/thread ids and every privacy plane count. Verify every result field against the fresh
readbacks and immutable handoff. Recursively reject any missing/extra key, forbidden content key,
private needle, or email-like string. Any mismatch leaves requirement/roadmap pending.

Only then append deployment-safe evidence to both playbooks, update Last verified/commands, mark
SKILL-01 complete in checkbox+traceability, and update Phase 21 roadmap plan/completion status to
seven plans across six waves. Do not claim ISO approval or any capability beyond candidate-only
tenant adaptations and manually re-runnable pinned prompts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),crypto=require('crypto'),hp='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json',rp='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json',hb=fs.readFileSync(hp),h=JSON.parse(hb),r=JSON.parse(fs.readFileSync(rp)),stable=x=>x&amp;&amp;typeof x==='object'?(Array.isArray(x)?x.map(stable):Object.fromEntries(Object.keys(x).sort().map(k=>[k,stable(x[k])]))):x,same=(a,b,n)=>{if(JSON.stringify(stable(a))!==JSON.stringify(stable(b)))throw Error(n)},keys=(o,k,n)=>{if(!o||Array.isArray(o)||typeof o!=='object'||JSON.stringify(Object.keys(o).sort())!==JSON.stringify([...k].sort()))throw Error(n+' keys')},need=(v,n)=>{if(typeof v!=='string'||!v)throw Error(n)},iso=(v,n)=>{need(v,n);if(!Number.isFinite(Date.parse(v)))throw Error(n)},num=(v,n,min,int=false)=>{if(typeof v!=='number'||!Number.isFinite(v)||v&lt;min||(int&amp;&amp;!Number.isInteger(v)))throw Error(n)},eff=(o,n)=>{keys(o,['scope','id','name','version','bodyHash','status'],n);['scope','id','name','bodyHash','status'].forEach(k=>need(o[k],n+'.'+k));num(o.version,n+'.version',1,true)},foreign=(o,n)=>{keys(o,['tenantId','candidateIdVisible','effective'],n);need(o.tenantId,n+'.tenantId');if(o.candidateIdVisible!==false)throw Error(n+'.visible');eff(o.effective,n+'.effective')},target=(o,n)=>{keys(o,['candidateId','registryTenantId','name','version'],n);['candidateId','registryTenantId','name'].forEach(k=>need(o[k],n+'.'+k));num(o.version,n+'.version',1,true)},summary=(o,n)=>{keys(o,['runId','caseCount','retryCount','costUsd','model'],n);need(o.runId,n+'.runId');need(o.model,n+'.model');num(o.caseCount,n+'.caseCount',1,true);num(o.retryCount,n+'.retryCount',0,true);num(o.costUsd,n+'.costUsd',0)},lineage=(o,n)=>{const ref=o&amp;&amp;o.basedOnScope==='global'?'basedOnGlobalSkillId':o&amp;&amp;o.basedOnScope==='tenant'?'basedOnTenantSkillId':null;if(!ref)throw Error(n+'.scope');keys(o,['basedOnScope','basedOnName','basedOnVersion',ref],n);need(o.basedOnName,n+'.name');need(o[ref],n+'.ref');num(o.basedOnVersion,n+'.version',1,true)},candidate=(o,status,n)=>{keys(o,['id','tenantId','name','version','bodyHash','author','authorUserId','status','rollbackEligible','lineage','evidenceState','gatePassed','evidenceTarget','evidenceSummary'],n);for(const k of ['id','tenantId','name','bodyHash','author','authorUserId'])need(o[k],n+'.'+k);num(o.version,n+'.version',1,true);lineage(o.lineage,n+'.lineage');target(o.evidenceTarget,n+'.evidenceTarget');summary(o.evidenceSummary,n+'.evidenceSummary');if(o.status!==status||o.rollbackEligible!==true||o.evidenceState!=='passing'||o.gatePassed!==true)throw Error(n+'.state');for(const k of ['id','tenantId','name','version','bodyHash','author','authorUserId','lineage'])same(o[k],h.candidate[k],n+'.'+k)},baseline=(o,n)=>{keys(o,['id','tenantId','name','version','bodyHash','author','status','rollbackEligible'],n);same(o,h.rollbackBaseline,n)},runtime=(o,n)=>{keys(o,['queryArgs','correlationId','scope','skillId','skillName','skillVersion','skillBodyHash','candidateAbsent'],n);keys(o.queryArgs,['tenantId','correlationId'],n+'.queryArgs');['correlationId','scope','skillId','skillName','skillBodyHash'].forEach(k=>need(o[k],n+'.'+k));need(o.queryArgs.tenantId,n+'.tenantId');if(o.queryArgs.correlationId!==o.correlationId)throw Error(n+'.correlation');num(o.skillVersion,n+'.version',1,true)}; keys(r,['schema','handoffSha256','deploymentUrlHash','recordedAt','evalRun','nonOwner','activation','authorRuntime','foreignRuntime','rollback','promptRun','privacy','ownerReviewedAt'],'root');if(r.schema!=='phase21-live-result.v1'||r.handoffSha256!==crypto.createHash('sha256').update(hb).digest('hex')||r.deploymentUrlHash!==h.deploymentUrlHash)throw Error('binding');iso(r.recordedAt,'recordedAt');iso(r.ownerReviewedAt,'ownerReviewedAt');keys(r.evalRun,['runId','caseCount','retryCount','costUsd','model','candidate','evidenceTarget','gatePassed'],'evalRun');summary({runId:r.evalRun.runId,caseCount:r.evalRun.caseCount,retryCount:r.evalRun.retryCount,costUsd:r.evalRun.costUsd,model:r.evalRun.model},'evalRun.summary');keys(r.evalRun.candidate,['id','tenantId','name','version','bodyHash'],'evalRun.candidate');for(const k of ['id','tenantId','name','version','bodyHash'])same(r.evalRun.candidate[k],h.candidate[k],'evalRun.candidate.'+k);target(r.evalRun.evidenceTarget,'evalRun.evidenceTarget');if(r.evalRun.gatePassed!==true)throw Error('evalRun.gate');const es={runId:r.evalRun.runId,caseCount:r.evalRun.caseCount,retryCount:r.evalRun.retryCount,costUsd:r.evalRun.costUsd,model:r.evalRun.model},et={candidateId:h.candidate.id,registryTenantId:h.candidate.tenantId,name:h.candidate.name,version:h.candidate.version};same(r.evalRun.evidenceTarget,et,'evalRun.target');keys(r.nonOwner,['candidateId','candidateBodyHash','code','stateUnchanged','global','foreign'],'nonOwner');if(r.nonOwner.candidateId!==h.candidate.id||r.nonOwner.candidateBodyHash!==h.candidate.bodyHash||r.nonOwner.code!=='OWNER_REQUIRED'||r.nonOwner.stateUnchanged!==true)throw Error('nonOwner');eff(r.nonOwner.global,'nonOwner.global');foreign(r.nonOwner.foreign,'nonOwner.foreign');same(r.nonOwner.global,h.globalBefore,'nonOwner.global handoff');same(r.nonOwner.foreign,h.foreignBefore,'nonOwner.foreign handoff');keys(r.activation,['candidate','baseline','global','foreign'],'activation');candidate(r.activation.candidate,'active','activation.candidate');same(r.activation.candidate.evidenceTarget,et,'activation.target');same(r.activation.candidate.evidenceSummary,es,'activation.summary');baseline(r.activation.baseline,'activation.baseline');eff(r.activation.global,'activation.global');foreign(r.activation.foreign,'activation.foreign');same(r.activation.global,h.globalBefore,'activation.global handoff');same(r.activation.foreign,h.foreignBefore,'activation.foreign handoff');runtime(r.authorRuntime,'authorRuntime');if(r.authorRuntime.queryArgs.tenantId!==h.candidate.tenantId||r.authorRuntime.scope!=='tenant'||r.authorRuntime.skillId!==h.candidate.id||r.authorRuntime.skillName!==h.candidate.name||r.authorRuntime.skillVersion!==h.candidate.version||r.authorRuntime.skillBodyHash!==h.candidate.bodyHash||r.authorRuntime.candidateAbsent!==false)throw Error('author runtime');runtime(r.foreignRuntime,'foreignRuntime');if(r.foreignRuntime.queryArgs.tenantId!==h.foreignBefore.tenantId||r.foreignRuntime.skillName!==h.candidate.name||r.foreignRuntime.skillId===h.candidate.id||r.foreignRuntime.skillBodyHash===h.candidate.bodyHash||!['global','tenant'].includes(r.foreignRuntime.scope)||r.foreignRuntime.candidateAbsent!==true)throw Error('foreign runtime positive witness');keys(r.rollback,['targetId','candidate','currentEffective','global','foreign'],'rollback');if(r.rollback.targetId!==h.rollbackBaseline.id)throw Error('rollback target');candidate(r.rollback.candidate,'archived','rollback.candidate');same(r.rollback.candidate.evidenceTarget,et,'rollback.target');same(r.rollback.candidate.evidenceSummary,es,'rollback.summary');eff(r.rollback.currentEffective,'rollback.currentEffective');const restored={scope:'tenant',id:h.rollbackBaseline.id,name:h.rollbackBaseline.name,version:h.rollbackBaseline.version,bodyHash:h.rollbackBaseline.bodyHash,status:'active'};same(r.rollback.currentEffective,restored,'rollback restored');eff(r.rollback.global,'rollback.global');foreign(r.rollback.foreign,'rollback.foreign');same(r.rollback.global,h.globalBefore,'rollback.global handoff');same(r.rollback.foreign,h.foreignBefore,'rollback.foreign handoff');keys(r.promptRun,['pinnedPromptId','sourceThreadId','runThreadId','pinRemoved','sourceThreadExists','runThreadExists'],'promptRun');['pinnedPromptId','sourceThreadId','runThreadId'].forEach(k=>need(r.promptRun[k],'promptRun.'+k));if(r.promptRun.sourceThreadId===r.promptRun.runThreadId||r.promptRun.pinRemoved!==true||r.promptRun.sourceThreadExists!==true||r.promptRun.runThreadExists!==true)throw Error('promptRun');keys(r.privacy,['passed','auditNeedleCount','evidenceNeedleCount','logNeedleCount','dlqNeedleCount','telemetryNeedleCount'],'privacy');if(r.privacy.passed!==true)throw Error('privacy');for(const k of ['auditNeedleCount','evidenceNeedleCount','logNeedleCount','dlqNeedleCount','telemetryNeedleCount']){num(r.privacy[k],'privacy.'+k,0,true);if(r.privacy[k]!==0)throw Error('privacy '+k)}const forbidden=new Set(['body','authoredbody','prompt','prompttext','fixture','modeloutput','rawevidence','evidencejson','message','messages','content','text','email','secret','token','providerresponse','headers','authorization','response','output']),walk=(o,p='root')=>{if(o&amp;&amp;typeof o==='object')for(const [k,v] of Object.entries(o)){if(forbidden.has(k.toLowerCase()))throw Error('forbidden key '+p+'.'+k);walk(v,p+'.'+k)}else if(typeof o==='string'&amp;&amp;(o.includes('PHASE21_PRIVATE_NEEDLE')||/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i.test(o)))throw Error('forbidden value '+p)};walk(r);console.log('closed result ok')"</automated>
    <automated>$hp='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-HANDOFF.json'; $rp='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json'; $h=Get-Content -Raw $hp | ConvertFrom-Json; $r=Get-Content -Raw $rp | ConvertFrom-Json; function Same($a,$b,$n){$aj=$a|ConvertTo-Json -Depth 30 -Compress;$bj=$b|ConvertTo-Json -Depth 30 -Compress;if($aj -cne $bj){throw "$n mismatch"}}; $live=pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill $h.candidate.id --foreign-tenant $h.foreignBefore.tenantId --expect-status archived --expect-evidence passing --expect-gate-passed true --expect-rollback-eligible true --json | ConvertFrom-Json; if($LASTEXITCODE -ne 0){throw 'final inspection failed'}; Same $live.candidate $r.rollback.candidate 'final candidate'; Same $live.candidate.evidenceSummary ([pscustomobject]@{runId=$r.evalRun.runId;caseCount=$r.evalRun.caseCount;retryCount=$r.evalRun.retryCount;costUsd=$r.evalRun.costUsd;model=$r.evalRun.model}) 'eval summary'; Same $live.candidate.evidenceTarget $r.evalRun.evidenceTarget 'evidence target'; Same $live.currentEffective $r.rollback.currentEffective 'final effective'; Same $live.globalCurrent $h.globalBefore 'global handoff'; Same $live.globalCurrent $r.nonOwner.global 'nonowner global'; Same $live.globalCurrent $r.activation.global 'activation global'; Same $live.globalCurrent $r.rollback.global 'rollback global'; Same $live.foreignCurrent $h.foreignBefore 'foreign handoff'; Same $live.foreignCurrent $r.nonOwner.foreign 'nonowner foreign'; Same $live.foreignCurrent $r.activation.foreign 'activation foreign'; Same $live.foreignCurrent $r.rollback.foreign 'rollback foreign'; $active=$live.candidate.PSObject.Copy();$active.status='active';Same $active $r.activation.candidate 'activation candidate';Same $h.rollbackBaseline $r.activation.baseline 'activation baseline';$a=npx convex run smoke:userSkillRuntimeAttribution ($r.authorRuntime.queryArgs|ConvertTo-Json -Compress)|ConvertFrom-Json;if($LASTEXITCODE -ne 0 -or $null -eq $a){throw 'author readback'};$ar=[pscustomobject]@{queryArgs=$r.authorRuntime.queryArgs;correlationId=$r.authorRuntime.correlationId;scope=$a.scope;skillId=$a.skillId;skillName=$a.skillName;skillVersion=$a.skillVersion;skillBodyHash=$a.skillBodyHash;candidateAbsent=$false};Same $ar $r.authorRuntime 'author runtime';if($a.scope -cne 'tenant' -or $a.skillId -cne $h.candidate.id -or $a.skillName -cne $h.candidate.name -or [string]$a.skillVersion -cne [string]$h.candidate.version -or $a.skillBodyHash -cne $h.candidate.bodyHash){throw 'author attribution'};$b=npx convex run smoke:userSkillRuntimeAttribution ($r.foreignRuntime.queryArgs|ConvertTo-Json -Compress)|ConvertFrom-Json;if($LASTEXITCODE -ne 0 -or $null -eq $b){throw 'foreign attribution must be non-null'};$br=[pscustomobject]@{queryArgs=$r.foreignRuntime.queryArgs;correlationId=$r.foreignRuntime.correlationId;scope=$b.scope;skillId=$b.skillId;skillName=$b.skillName;skillVersion=$b.skillVersion;skillBodyHash=$b.skillBodyHash;candidateAbsent=$true};Same $br $r.foreignRuntime 'foreign runtime';if($b.skillName -cne $h.candidate.name -or $b.skillId -ceq $h.candidate.id -or $b.skillBodyHash -ceq $h.candidate.bodyHash){throw 'foreign positive-witness rule'}</automated>
    <automated>$env:PHASE21_LIVE_RESULT_PATH='.planning/phases/21-user-authored-skills-and-routines/21-LIVE-RESULT.json'; try { pnpm --filter web exec playwright test e2e/skill-authoring.spec.ts --project=chromium --workers=1 --grep "refs-only post-live readback"; if($LASTEXITCODE -ne 0){throw 'prompt/thread/privacy readback failed'} } finally { Remove-Item Env:PHASE21_LIVE_RESULT_PATH -ErrorAction SilentlyContinue }</automated>
    <automated>node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>Machine evidence and owner review agree, documentation matches reality, and SKILL-01 closes without identity drift.</done>
</task>

</tasks>

<verification>
- No candidate publication occurs in Plan 21-07; every operation uses handoff candidate.id.
- Pre-live snapshot matches handoff exactly on deployment, candidate, baseline, effective and foreign state.
- Exact paid evidence makes gatePassed true only on that row; non-owner still refuses.
- A runtime correlation attributes exact id/name/version/bodyHash; B is a mandatory non-null
  same-name positive witness with both a different id and different body hash.
- Rollback restores exact baseline id/hash and preserves candidate hash/evidence.
- Result is refs-only and handoff-hash-bound; playbooks/requirements/roadmap update only after PASS.
</verification>

<success_criteria>
- The one snapshotted candidate—not a republish—passes the full held-out gate and owner boundary.
- Exact evidence, activation, runtime attribution and rollback are machine-linked end to end.
- Foreign tenant and content-plane privacy remain intact before and after state changes.
- Routine v0 remains manual pinned-prompt rerun only.
- SKILL-01 and Phase 21 close truthfully after seven plans across six waves.
</success_criteria>

<output>
Create `.planning/phases/21-user-authored-skills-and-routines/21-07-SUMMARY.md` with handoff hash,
exact run/evidence/activation/runtime/foreign/rollback refs, automated commands and owner approval.
Do not mark SKILL-01 or Phase 21 complete on a missing/mismatched handoff, republished candidate,
parked/red eval, absent runtime attribution, foreign leak or failed rollback.
</output>
