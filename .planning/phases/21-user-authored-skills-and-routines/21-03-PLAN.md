---
phase: 21-user-authored-skills-and-routines
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - packages/contracts/src/skill.ts
  - packages/backend/convex/skills.ts
  - packages/backend/convex/skills.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/dispatch.ts
  - packages/backend/convex/evaluations.ts
  - packages/backend/convex/dispatch.test.ts
  - packages/backend/convex/smoke.ts
  - packages/backend/scripts/run-eval-golden.mjs
  - docs/playbooks/skill-registry.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "A tenant candidate is evaluated by exact tenantSkills row id while fixture plans/messages/data remain in the throwaway eval tenant."
    - "A full, nonempty, unfiltered all-green run records refs/counts-only evidence on that exact tenant row; filtered, failed, interrupted, or over-cap runs record none."
    - "The exact candidate body reaches the dispatched specialist that its held-out fixture exercises; the active or another tenant's same-name/same-version row cannot be certified accidentally."
    - "Every real specialist use emits refs-only exact registry scope/id/name/version/body-hash attribution on the existing subagent.completed audit lineage, with a bounded read-only correlation readback."
    - "Global --skill name@version pins and multi-pin behavior remain compatible for existing gated candidates."
  artifacts:
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "Exact tenant-candidate CLI pin, offline self-check, throwaway-data separation, and evidence write"
    - path: "packages/backend/convex/skills.ts"
      provides: "Exact tenant candidate read and evidence-only patch"
      exports: ["getTenantSkillVersion", "recordTenantEvalEvidence"]
    - path: "packages/backend/convex/dispatch.ts"
      provides: "Tenant candidate identity threaded across scheduled specialist dispatch and exact refs-only runtime attribution"
    - path: "packages/backend/convex/smoke.ts"
      provides: "Bounded read-only runtime-attribution lookup over the existing audit.by_correlation seam"
    - path: "packages/backend/convex/skills.test.ts"
      provides: "Cross-tenant collision and evidence-only mutation proofs"
  key_links:
    - from: "run-eval-golden --tenant-skill"
      to: "skills.getTenantSkillVersion"
      via: "resolve exact row before first paid turn and derive its name/version/registry tenant"
      pattern: "tenantSkillIds"
    - from: "evaluations.applyActOnGap"
      to: "dispatch.runSpecialist"
      via: "the exact tenant skill id rides the same scheduled lineage as existing skillVersions"
      pattern: "tenantSkillIds"
    - from: "runSpecialistTurn"
      to: "getTenantSkillVersion"
      via: "candidate id wins for that skill name; absent id uses ordinary effective loader"
      pattern: "getTenantSkillVersion"
    - from: "runSpecialistTurn resolved skill"
      to: "audit subagent.completed"
      via: "scope + exact row id + name + version + SHA-256 body hash, never body text"
      pattern: "skillBodyHash"
---

<objective>
Extend the shipped golden runner and dispatch pin plumbing so one full held-out run can certify an
exact tenant candidate without using the user's real data tenant.

Purpose: `<name>@<version>` is ambiguous once two tenants can both own version 2. Evidence must
follow the exact row whose body executed, while preserving every existing global eval behavior.

Output: exact tenant candidate pins, evidence-only writes, collision tests, and a zero-cost offline
self-check. This plan does not activate anything and must not run a paid/live eval.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/21-user-authored-skills-and-routines/21-RESEARCH.md
@.planning/phases/21-user-authored-skills-and-routines/21-VALIDATION.md
@.planning/phases/21-user-authored-skills-and-routines/21-02-SUMMARY.md
@packages/backend/AGENTS.md
@packages/backend/convex/_generated/ai/guidelines.md
@packages/backend/scripts/run-eval-golden.mjs
@packages/backend/convex/skills.ts
@packages/backend/convex/dispatch.ts
@packages/backend/convex/evaluations.ts
@packages/backend/convex/llm.ts
@docs/playbooks/agent-runtime.md
@docs/playbooks/skill-registry.md

<interfaces>
Keep the existing global CLI and transport intact:

```text
--skill <name>@<version>       existing global pin; repeatable multi-pin
skillVersions: Record<string,number>
```

Add an orthogonal exact tenant identity transport:

```text
--tenant-skill <tenantSkillsId>
tenantSkillIds: Record<string, Id<"tenantSkills">>
```

The runner resolves the id to name/version before any paid case. Reject a global and tenant pin for
the same name in one run; never let last-one-wins select an unintended body.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact tenant candidate reads and evidence semantics</name>
  <files>packages/contracts/src/skill.ts, packages/backend/convex/skills.ts, packages/backend/convex/skills.test.ts</files>
  <behavior>
    - `getTenantSkillVersion({candidateId})` returns body/version/skillId/name/tenantId for that exact
      row regardless of candidate status and throws the same non-oracle not-found error for absence.
    - `inspectTenantSkill({candidateId,foreignTenantId?})` is read-only and returns the exact
      candidate identity/status/lineage/evidence target plus refs-only evidence summary, SHA-256
      body hash, rollback baseline, `currentEffective`, `globalCurrent`, and optional
      `foreignCurrent`, never either body; it performs no write.
    - `recordTenantEvalEvidence({candidateId,evidence})` patches only evidence on that exact row.
    - Passing tenant evidence requires pass=true plus exact candidate id, registry tenant, name and
      version; evidence for another tenant's colliding name/version fails closed.
    - Unparseable, failed, missing-id, or stale evidence fails closed.
  </behavior>
  <action>
Extend `EvalEvidence` additively with an optional exact tenant target record while retaining
`skillVersions` for global rows. Add `hasPassingTenantEvidence` that checks every exact identity
field. Do not weaken `hasPassingEvidence` or add a freshness rule not required by Phase 21.

Add exact-id internal read/evidence mutations plus the refs-only `inspectTenantSkill` internal query.
Compute hashes with the existing `convex/lib/hash.ts` helper; return no body/authoredBody. The
inspection snapshot must include:

```text
candidate: id,tenantId,name,version,bodyHash,author,authorUserId,status,rollbackEligible,
           lineage(basedOnScope,basedOnName,basedOnVersion,basedOnGlobalSkillId?,
                   basedOnTenantSkillId?),evidenceState,gatePassed,
           evidenceTarget(candidateId,registryTenantId,name,version)?,
           evidenceSummary(runId,caseCount,retryCount,costUsd,model)?
rollbackBaseline: id,tenantId,name,version,bodyHash,author,status,rollbackEligible
currentEffective: scope,id,name,version,bodyHash,status
globalCurrent: scope(global),id,name,version,bodyHash,status
foreignCurrent?: tenantId,candidateIdVisible(false),
                 effective(scope,id,name,version,bodyHash,status)
```

Resolve the baseline only through the stored lineage/base id, not a newest-row guess. The initial
rollback baseline hash must equal the pre-activation effective global hash. A foreign tenant snapshot
must not return the candidate id/hash as effective and must carry no candidate/authored body.
Validate that the row is a user-authored candidate
before the runner treats it as an evaluable tenant target; the exact read itself may support active
status for diagnostics, but evidence recording must never patch body/name/version/provenance/status.

Build a two-tenant collision fixture with the same name and version. Record A evidence and prove B
remains evidence-free and cannot satisfy `hasPassingTenantEvidence`. Snapshot every non-evidence
field before/after the patch. Mutation-replace candidate-id selection with name/version selection;
the collision test must show the wrong row could certify and turn red. Mutation-copy A's passing
evidence onto B's same-name/version row while leaving A's id inside it; the exact-id evidence test
must remain refused and turn red if that predicate is weakened. Restore after each mutation and
rerun the focused suite green.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>Tenant evidence identifies one immutable candidate row and cannot certify a colliding row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread exact tenant candidate ids through the real scheduled specialist path</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/dispatch.ts, packages/backend/convex/evaluations.ts, packages/backend/convex/dispatch.test.ts, packages/backend/convex/smoke.ts</files>
  <behavior>
    - Existing global `skillVersions` args and behavior remain unchanged.
    - `tenantSkillIds` is validated as a name-to-tenantSkills-id record and follows
      eval turn -> applyActOnGap -> scheduled dispatch -> runSpecialistTurn.
    - `runSpecialistTurn` loads the exact tenant row when its skill name has a tenant id; otherwise
      it uses Plan 02's effective active loader.
    - A pin whose resolved row name differs from the requested specialist refuses before a model call.
    - The exact candidate's unique body needle reaches the mock model; active/global/colliding bodies do not.
    - `runSpecialistTurn` returns registry scope, exact row id, name, version and body hash; the
      existing `subagent.completed` audit payload records those refs and never body/authoredBody.
    - `smoke.userSkillRuntimeAttribution` reads `audit.by_correlation`, filters the exact tenant and
      `subagent.completed`, takes a fixed cap, and returns only the exact attribution fields.
  </behavior>
  <action>
Add optional `tenantSkillIds` beside `skillVersions` across `runCockpitAgent`/runner args only where
the eval path needs it, `evaluations.applyActOnGap`, the dispatch input validator/type, scheduled
dispatch arms, and `llm.runSpecialistTurn`. Preserve every existing caller by making the field
optional. Use `v.record(v.string(), v.id("tenantSkills"))`; do not accept a registry tenant or body
from the model.

At the final body load, an exact candidate id wins only for its matching skill name. Verify the row
name returned by `getTenantSkillVersion`; mismatch throws before `generateText`. Otherwise use
`getEffectiveSkill`. Tool names remain the code-owned `SPECIALISTS` record and are never read from
the tenant row. Hash the resolved body with `contentHash`, then return `{skillScope, skillId,
skillName, skillVersion, skillBodyHash}` alongside the existing result. In `governedDispatch`, add
those exact refs to the existing `subagent.completed` audit payload; this is attribution on the
existing lineage, not a second trace event or a new content store.

Add `smoke.userSkillRuntimeAttribution({tenantId,correlationId})` as a bounded read-only
`internalQuery`: query `audit.by_correlation`, take a fixed maximum, enforce tenant equality, select
the `subagent.completed` row, and return only scope/id/name/version/body-hash. No body, authoredBody,
prompt, output, source URL, mutation, or public tenant read.

Extend dispatch tests through the scheduled seam, not only a direct llm shim. Assert the audit row's
exact key/value attribution, its body hash against `contentHash(candidateBody)`, the bounded readback,
and absence of the high-entropy body needle. Mutation-drop `tenantSkillIds` at one handoff and observe
the unique candidate-body assertion turn red. Mutation-drop `skillId` or substitute the colliding
row id in `subagent.completed`; the attribution/readback assertion turns red. Mutation-add the body
to the audit payload; the exact-key/needle privacy assertion turns red. Restore each change and rerun.
  </action>
  <verify><automated>pnpm --filter @pikar/backend exec vitest run convex/dispatch.test.ts convex/skills.test.ts --maxWorkers=1</automated></verify>
  <done>The same exact tenant candidate selected by the runner is the body the dispatched specialist executes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the golden runner with an exact tenant target and no-evidence rules</name>
  <files>packages/backend/scripts/run-eval-golden.mjs</files>
  <behavior>
    - `--tenant-skill <id>` resolves the exact row before inbox/vault/blueprint seed and before the first paid turn.
    - Unknown/missing/non-candidate/non-user ids and duplicate same-name global/tenant pins abort at $0.
    - Fixture plans/messages remain under `eval-&lt;runId&gt;`; the registry tenant is used only to read the body/evidence row.
    - Full all-green unfiltered execution records one exact tenant evidence row; `--only`, zero case,
      failed, interrupted, or over-cap execution records none.
    - Existing global multi-pin parsing/evidence stays green.
    - `--inspect-tenant-skill <id>` is mutually exclusive with paid-run arguments, calls only the
      read-only inspection query, prints refs/status/lineage/evidence-summary/body-hash JSON, and exits.
    - Inspection-only expectation flags (`--expect-status`, `--expect-evidence`,
      `--expect-gate-passed`, `--expect-rollback-eligible`) compare the returned snapshot and exit
      nonzero on mismatch without performing any write.
    - JSON inspection output includes a SHA-256 hash of the configured Convex deployment URL so a
      later live gate cannot silently inspect or mutate a different deployment.
    - `--foreign-tenant <tenantId>` adds the bounded refs-only foreign-effective snapshot; it is
      inspection-only and rejects a foreign result equal to the candidate id/body hash.
    - `--self-check` covers all new parsing/merge/suppression branches with zero Convex calls.
  </behavior>
  <action>
Parse repeatable `--tenant-skill` ids separately from existing `--skill` pins. Resolve each via a
read-only Convex function before any fixture seed; build both `skillVersions` and `tenantSkillIds`
from the resolved set. Reject duplicate skill names across both scopes.

Thread tenant ids into every normal turn and `applyActOnGap` call, including background specialist
dispatch. On an all-green, nonempty, unfiltered run, serialize refs/counts-only evidence containing
runner/runId/pass/counts/retries/cost/model/timestamp, the full global pins, and the exact tenant
target `{candidateId,registryTenantId,name,version}`. Call `recordTenantEvalEvidence` by id. Candidate
body, authoredBody, fixture prompts, model replies, and user tenant data never enter evidence.

Extend `selfCheck()` with in-memory parser/merge/no-evidence cases. Do not run a live gate here.
The offline self-check must remain the only command in this task and must perform zero Convex/model
calls; unknown arguments should fail rather than fall through to a paid run. Add the read-only
`--inspect-tenant-skill <id>` mode for Plan 06 state snapshots; it may make one query and must exit
before fixture seed/model/evidence code. Add inspection-only expectation flags for exact status,
evidence state (`absent|passing|failing`), gatePassed, and rollbackEligible; reject them outside
inspection mode. Add `--json` for one machine-readable JSON object and optional
`--foreign-tenant <tenantId>`; cover matching/mismatch/foreign-collision branches in `selfCheck()`.
Hash the configured deployment URL into the JSON output without printing credentials or query
parameters; the Phase 21 handoff pins this hash across the pre/post live gate.
Mutation-remove the `filters.length === 0` evidence guard;
the self-check's positive filtered-execution witness plus absent-evidence assertion turns red.
Restore and rerun `--self-check` green.
  </action>
  <verify><automated>pnpm --filter @pikar/backend eval:golden -- --self-check</automated></verify>
  <done>The existing held-out corpus can certify one exact tenant candidate without touching the
  user's data tenant or recording partial/diagnostic evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Document exact tenant evaluation and its unpaid status</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/agent-runtime.md</files>
  <action>
Document the two pin scopes, exact candidate identity, registry-tenant versus throwaway-data-tenant
separation, every no-evidence condition, the dispatched handoff, and the offline command. State
prominently that no Phase 21 candidate has passed live yet and no paid eval is authorized by this
plan. Document both read-only commands exactly:

```powershell
pnpm --filter @pikar/backend eval:golden -- --inspect-tenant-skill <tenantSkillsId>
npx convex run smoke:userSkillRuntimeAttribution '{"tenantId":"<tenantId>","correlationId":"<rootRequestId>"}'
```

The first reports state/body hash without bodies; the second reads the existing
`subagent.completed` lineage. Preserve current warnings about `--only`, `--list`, cost accounting,
and live version lookup.
  </action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>Operators have one exact, non-ambiguous procedure and no documentation claims a live pass.</done>
</task>

</tasks>

<verification>
- Focused skills/dispatch suites pass with a deliberate dropped-handoff and name/version-collision mutation demonstrated red then reverted.
- Exact audit attribution, bounded correlation readback, and body-needle privacy mutations turn red,
  are restored, and the focused dispatch suite reruns green.
- `pnpm --filter @pikar/backend eval:golden -- --self-check` passes and makes zero Convex/model calls.
- Backend typecheck and playbook gate pass.
- No tenant candidate status changes and no paid eval runs.
</verification>

<success_criteria>
- Exact tenant candidate identity survives the complete held-out specialist path.
- Evidence can land only on the exact executed candidate after a full unfiltered green run.
- User data and eval fixture data remain separated.
- Existing global eval pins remain compatible.
</success_criteria>

<output>
Create `.planning/phases/21-user-authored-skills-and-routines/21-03-SUMMARY.md`. Keep SKILL-01
pending: owner activation/rollback and the live paid proof remain Plan 04/06.
</output>
