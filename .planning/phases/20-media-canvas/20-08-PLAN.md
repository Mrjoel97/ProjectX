---
phase: 20-media-canvas
plan: 08
type: execute
wave: 6
depends_on: ["20-03", "20-07"]
files_modified:
  - packages/core/src/specialists.ts
  - packages/core/src/specialists.test.ts
  - packages/backend/convex/llm.ts
  - packages/backend/convex/dispatch.ts
  - packages/backend/convex/plans.ts
  - docs/playbooks/growth-diagnostic.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [MEDIA-01]
lane_gate: "NOT gated on Phase 16. This plan adds the TOOL; plan 20-12 teaches `cockpit-agent` to reach for it, and 20-12 is the only parked plan."

must_haves:
  truths:
    - "The cockpit can dispatch a media specialist that produces an art direction and a shot deck and spends nothing but tokens."
    - "The media specialist's capability grant is exactly `searchVault` — there is NO code path from a dispatched specialist to a fal POST."
    - "`diagnose()` never prescribes `media` as a gap remedy."
    - "A dispatched media run stages a `media` plan without destroying an in-flight deck on the same thread."
  artifacts:
    - path: "packages/core/src/specialists.ts"
      provides: "media in SPECIALIST_ROUTES, the dispatchMedia stepTool literal, the SPECIALISTS.media spec reusing SPECIALIST_TOOLS verbatim"
      contains: "dispatchMedia"
    - path: "packages/backend/convex/dispatch.ts"
      provides: "runMedia — dispatchAndLand + persistStoryboard, the runResearch shape"
      contains: "runMedia"
  key_links:
    - from: "packages/backend/convex/dispatch.ts"
      to: "packages/core/src/storyboard.ts"
      via: "persistStoryboard parses the specialist body with parseShotDeck before writing plans.shots"
      pattern: "parseShotDeck"
    - from: "packages/core/src/specialists.ts"
      to: "packages/contracts/src/skill.ts"
      via: "specialists.test.ts reads skill.ts off disk and asserts a matching constant for every skillName"
      pattern: "media-director"
---

<objective>
`media` becomes a dispatchable route: the cockpit hands a media brief to a read-only specialist that
proposes an art direction and a shot deck, and the deck lands on the plan row as a `media` plan
awaiting one human click.

Purpose: D1 locked the dispatch surface as the way media is reached, and D2 locked the specialist as
**proposal-only**. This plan is where "plan-gated by construction" stops being a claim and becomes a
structural fact: the grant is `SPECIALIST_TOOLS` verbatim, and a static scan proves no member of it
reaches the fal adapter.

Output: the route, the tool, the runner, and the storyboard persistence.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-CONTEXT.md
@.planning/phases/20-media-canvas/20-RESEARCH.md
@.planning/phases/20-media-canvas/20-03-SUMMARY.md
@.planning/phases/20-media-canvas/20-07-SUMMARY.md
@packages/core/src/specialists.ts
@packages/core/src/specialists.test.ts
@packages/backend/convex/dispatch.ts
@packages/backend/convex/llm.ts
@packages/backend/convex/plans.ts
@docs/playbooks/growth-diagnostic.md

<interfaces>
<!-- Read from source 2026-08-01. -->

```ts
// packages/core/src/specialists.ts
export const SPECIALIST_ROUTES = ["offer-architect","money-model-designer","lead-engine","research"] as const;
export type SpecialistSpec = {
  readonly skillName: string;
  readonly tools: readonly string[];
  readonly stepTool: "dispatchOfferArchitect" | "dispatchMoneyModelDesigner" | "dispatchLeadEngine" | "dispatchResearch";
};
const SPECIALIST_TOOLS = ["searchVault"] as const;   // :58 — THE grant
const RESEARCH_TOOLS  = ["searchVault","webResearch","declareUnsupported"] as const;
export const SPECIALISTS: Readonly<Record<SpecialistRoute, SpecialistSpec>> = { … };
```
`specialists.ts:46-57` — the `SPECIALIST_TOOLS` comment records `evaluateBusiness` as **deliberately
refused** (a write and a re-entrancy hazard wearing a read's clothes). A media generate tool is
strictly worse: it spends real dollars with no human in the loop.

`specialists.test.ts` assertions this plan must grow:
- `:23-32` the exact `SPECIALIST_ROUTES` array
- `:74-83` the per-route grant assertion
- `:46-64` reads `packages/contracts/src/skill.ts` OFF DISK and asserts a matching constant for every
  `skillName` — so `MEDIA_DIRECTOR_SKILL` must already exist (plan 20-03)
- `:94-112` *"every granted tool is TAUGHT in its specialist's canonical skill body"* — the media body
  must teach `searchVault` or this goes RED
- `:202-266` the ADR-010 pattern: **NEW companion assertion — `diagnose()` emits no `"media"` under
  any input**

`llm.ts:811-859` — `dispatchResearchTool`: stage the plan row, schedule, return immediately.
`dispatch.ts:633-654` — `runResearch = dispatchAndLand(…) + persistFindings(…)`.
`plans.ts:103-166` — `stageResearchPlan`'s three-rule refusal, with its own `ponytail:` at `:119-121`
naming *"more than one plan row per thread"* as the real fix. **`plans.by_thread` is `.unique()`.**
That comment also FORBIDS generalizing it into a shared helper: *"a knob for two callers that
disagree is the abstraction §8 forbids."* **Copy the shape; do not extract a helper.**

From plan 20-03: `MEDIA_DIRECTOR_SKILL = "media-director"`, UNGATED.
From plan 20-01: `parseShotDeck(body) → ParsedDeck`, `isPaidShot`.
From plan 20-07: `plans.kind` accepts `"media"` in both schema and `patchPlan`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `media` joins `SPECIALIST_ROUTES` with the grant reused VERBATIM</name>
  <files>packages/core/src/specialists.ts</files>
  <action>
Four edits:
1. `SPECIALIST_ROUTES` += `"media"`.
2. `SpecialistSpec.stepTool` closed union += `"dispatchMedia"`.
3. `SPECIALISTS.media = { skillName: "media-director", tools: SPECIALIST_TOOLS, stepTool: "dispatchMedia" }`.
   `skillName` is the inlined string VALUE of `MEDIA_DIRECTOR_SKILL` — the same inlining the other
   four use, for the same reason (`@pikar/contracts` is not a dependency of `@pikar/core`, and
   `specialists.test.ts` reads that file off disk and asserts the copies match).
4. **`SPECIALIST_TOOLS` is reused VERBATIM. Do not mint a media grant. Do not add a generate tool.**
   Extend the `SPECIALIST_TOOLS` doc comment with the media refusal, in the `evaluateBusiness`
   paragraph's voice:
   > *"Also deliberately NOT granted: any media generation tool. A tool that called fal.ai from
   > inside a dispatch would be strictly worse than `evaluateBusiness` — it spends REAL DOLLARS with
   > no human in the loop, and it would falsify roadmap SC #3 ('an agent or injected content cannot
   > fire generation without human approval'). The media specialist emits prose plus a structured
   > shot deck and spins up a canvas; the paid call fires only from the post-`approved` arm
   > (`cockpit.ts` `EXTERNAL_TARGETS.media`) or from an explicit human click in the canvas. Do not
   > 'fix' this by adding a generate tool here."*

Also add the ADR-010 note: `media`, like `research`, is reachable by DISPATCH but is **not** a route
`diagnose()` emits — media is not a remedy for a business constraint.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- specialists</automated>
  </verify>
  <done>`SPECIALISTS.media` resolves with `tools === SPECIALIST_TOOLS` (identity, not a copy) and `stepTool === "dispatchMedia"`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the specialist assertions, including the `diagnose()`-emits-no-media companion and the SC3 no-path scan</name>
  <files>packages/core/src/specialists.test.ts, docs/playbooks/growth-diagnostic.md</files>
  <behavior>
    - `SPECIALIST_ROUTES` equals exactly `["offer-architect","money-model-designer","lead-engine","research","media"]`.
    - `SPECIALISTS.media.tools` equals exactly `["searchVault"]`.
    - Every `skillName` has a matching constant in `packages/contracts/src/skill.ts` — now including
      `media-director`.
    - Every tool granted to `media` is TAUGHT in `packages/contracts/skills/media-director.md`.
    - **`diagnose()` emits no `"media"` under any input** — the ADR-010 companion that exists today
      for `research`.
    - **SC3, static:** no member of `SPECIALIST_TOOLS` or `RESEARCH_TOOLS` names anything that reaches
      the fal adapter, and `packages/backend/convex/media.ts`'s submit entry point is referenced only
      from `cockpit.ts`'s external arm and from the canvas mutations — never from `llm.ts`'s specialist
      tool set.
  </behavior>
  <action>
Grow the four existing assertions and add the two new ones. The `diagnose()` companion is the one
that must be written even though it feels redundant: ADR-010 already decided that the routes
`diagnose()` emits are a strict SUBSET of `SPECIALIST_ROUTES`, and this is the second instance of
that decided pattern — the assertion is what keeps it decided.

The SC3 no-path scan is a source scan over comment-stripped code (the `llmRedaction` idiom). Put it
here rather than in `llmRedaction.test.ts` because it is a statement about the CAPABILITY GRANT, and
`specialists.test.ts` is where grants are policed.

Bump `docs/playbooks/growth-diagnostic.md` (`specialists.ts` and `specialists.test.ts` are its watched
paths): record `media` as a dispatch-only route, its `searchVault`-only grant, the deliberate refusal
of a generate tool, and the `diagnose()` exclusion.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/core test -- specialists</automated>
  </verify>
  <done>All six assertions pass; deleting `"media"` from the exact-array assertion or adding a generate tool to the grant each turns the suite RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the `dispatchMedia` tool, `runMedia`, `stageMediaPlan`, and `persistStoryboard`</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/dispatch.ts, packages/backend/convex/plans.ts, docs/playbooks/cockpit.md</files>
  <behavior>
    - The `dispatchMedia` tool stages a plan row, schedules the run, and **returns immediately** —
      the deck does not arrive this turn (the `dispatchResearch` contract).
    - `stageMediaPlan` REFUSES to recycle a thread's plan row when that would destroy an in-flight
      deck (a plan with `mediaJobs` rows not yet terminal), returning a governed reason.
    - `runMedia` writes `plans.artDirection` + `plans.shots` and sets `kind: "media"`,
      `status: "proposed"`.
    - A specialist body that produces no parseable SHOT DECK lands a memo-shaped refusal, **not an
      empty deck** — an empty canvas that looks like a successful proposal is the failure to avoid.
    - The dispatched run spends TOKENS only: no `mediaJobs` row is inserted and no media window moves.
  </behavior>
  <action>
1. `llm.ts`: `dispatchMedia` tool, the `dispatchResearchTool` shape **verbatim** (`llm.ts:811-859`) —
   stage the plan row, schedule the dispatch, return a short acknowledgement. The tool's description
   must say the deck arrives in the workspace, not in this turn, and that **generating costs money
   and needs a separate human click.**
2. `plans.ts`: `stageMediaPlan`, copying `stageResearchPlan`'s three-rule refusal shape
   (`plans.ts:103-166`). **Do NOT extract a shared helper** — that file's own `ponytail:` comment
   forbids it. The media-specific rule: refuse a recycle when the existing plan is `kind: "media"`
   and has non-terminal `mediaJobs` rows; a user who wants a second deck starts a new chat
   (`plans.by_thread` is `.unique()`). Carry a `ponytail:` naming the real fix (more than one plan row
   per thread) and why it is not this phase's.
3. `dispatch.ts`: `runMedia = dispatchAndLand(…) + persistStoryboard(…)`, the `runResearch` shape
   verbatim. `persistStoryboard` runs the specialist's body through `parseShotDeck` (and a
   9-field art-direction parse) and patches `plans.artDirection` / `plans.shots` /
   `kind: "media"` / `status: "proposed"` via direct `ctx.db.patch` — **not through `patchPlan`**,
   which deliberately has no shot args (plan 20-02).
   On a parse failure, land the specialist's prose as a memo-shaped plan with the failure reason,
   and audit refs-only. **Never write an empty `shots: []`.**
4. Bump `docs/playbooks/cockpit.md`: the new dispatch route, the tool's return-immediately contract,
   `stageMediaPlan`'s refusal rules, and the fact that a dispatched media run cannot spend money.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- dispatch</automated>
  </verify>
  <done>A dispatched media run stages a proposed `media` plan carrying parsed shots and zero `mediaJobs` rows; an unparseable body lands a refusal, not an empty deck; `node scripts/check-playbooks.mjs` exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/core test -- specialists`, `pnpm --filter @pikar/backend test -- dispatch`, and `pnpm test` full suite green.
- `pnpm typecheck` backend delta exactly 0 against the 150 baseline.
- `node scripts/check-playbooks.mjs` exits 0.
- **Mutation check:** add a fake `"generateMedia"` to `SPECIALIST_TOOLS` — the grant assertion AND the "every granted tool is taught" assertion must both go RED. Remove.
- **Mutation check:** make `persistStoryboard` write `shots: []` on a parse failure — the no-empty-deck assertion must go RED. Restore.
- **Cost: $0.** Dispatch tests use the offline SMOKE driver; no model call.
- Shared tree: `ls .git/MERGE_HEAD`; pathspec commits only. **Do not bump `media.md`** — plan 20-09 owns it this wave.
</verification>

<success_criteria>
- Media is dispatchable, the specialist is structurally incapable of spending, and a test proves the absence of the path rather than asserting the intention.
- The conversational entry point (`cockpit-agent` teaching the tool) is the ONLY thing still missing, and it is plan 20-12.
</success_criteria>

<output>
After completion, create `.planning/phases/20-media-canvas/20-08-SUMMARY.md`. Record the
`dispatchMedia` tool description verbatim — plan 20-12's `cockpit-agent` section must agree with it.
</output>
