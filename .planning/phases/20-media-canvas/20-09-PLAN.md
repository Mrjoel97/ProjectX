---
phase: 20-media-canvas
plan: 09
type: execute
wave: 9
depends_on: ["20-02", "20-07"]
files_modified:
  - packages/backend/convex/media.ts
  - packages/backend/convex/media.test.ts
  - docs/playbooks/media.md
autonomous: true
requirements: [MEDIA-01]
lane_gate: "NOT gated on Phase 16."
rescope: "REVISED 2026-08-01 for the finished-reel re-scope (delta §6.6). Three material changes: the read plane gains a REEL region (the finished mp4 and its sidecar's gate summary); per-block state becomes a PIPELINE of two independent job states (clip and voice) rather than one status; and `deckEstimate` must ITEMISE — D7's 'never spend without showing the estimate first' now means four lines, not one. Plus a fifth free editor affordance (edit a block's narration) which is the UI half of the 140-character guard, and a new truth: regenerating a block invalidates the reel. Moved Wave 6 → Wave 9."

must_haves:
  truths:
    - "The canvas can show the itemised job estimate — clips, voice, captions, render — the model and resolution it was priced at, and today's remaining media budget, BEFORE anything is spent."
    - "A human click generates: the reel, or one block, through the identical reserve-then-submit path."
    - "Each block reports TWO independent states — its clip and its voice — plus its narration and that narration's character count against the 140 ceiling."
    - "A finished reel is readable only through a tenant-guarded query, and only when its sidecar validated."
    - "Regenerating any block CLEARS the render, so the canvas can never show a stale reel beside a fresh block."
    - "A user can edit a block's prompt AND its narration, reorder blocks and delete a block, and none of those costs a cent."
    - "Another tenant gets an empty result from every read, cannot regenerate this tenant's block, and cannot read this tenant's reel."
    - "A signed asset URL is minted only inside a tenant-guarded query and is never logged."
  artifacts:
    - path: "packages/backend/convex/media.ts"
      provides: "byPlan / assetUrls / reel / jobEstimate / remainingCents tenantQueries; generateReel / regenerateBlock / editBlockPrompt / editBlockNarration / reorderBlocks / deleteBlock tenantMutations"
      contains: "tenantQuery"
  key_links:
    - from: "packages/backend/convex/media.ts assetUrls"
      to: "ctx.storage.getUrl"
      via: "a tenantQuery that guards the owning plan row before minting the bearer URL"
      pattern: "ctx.storage.getUrl"
    - from: "packages/backend/convex/media.ts generateReel"
      to: "reserveJobInner"
      via: "the identical money gate the approve arm uses — no second rail, no bypass"
      pattern: "reserveJobInner"
---

<objective>
The canvas's backend plane: what it reads, what it can spend, and what it can edit for free — with
the isolation assertion that ships alongside the surface.

Purpose: D7's binding rule — *"the editor must not offer a control that can spend money without
showing the estimate first"* — is a backend requirement before it is a UI one. The re-scope makes it
a harder one: the thing being bought is no longer "N clips" but a whole reel with four cost lines, so
the estimate query must itemise or the number on screen is an assertion rather than an explanation.
And BETA-05: an isolation assertion ships with each new surface, not after it.

Output: five tenant-guarded reads, six tenant-guarded writes, and the two-tenant assertions.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-CONTEXT.md
@.planning/phases/20-media-canvas/20-RESEARCH.md
@.planning/phases/20-media-canvas/20-RESEARCH-DELTA.md
@.planning/phases/20-media-canvas/20-04-SUMMARY.md
@.planning/phases/20-media-canvas/20-07-SUMMARY.md
@packages/backend/convex/media.ts
@packages/backend/convex/plans.ts
@packages/backend/convex/lib/functions.ts
@docs/playbooks/media.md

<interfaces>
<!-- THE template for assetUrls and reel, read from source. Copy it structurally. -->

```ts
// plans.ts:374-388 — and its header comment IS the rule:
//   "the URL is a bearer capability, so it is ONLY ever returned from this tenant-guarded query and
//    NEVER logged (CLAUDE.md §4). Guards on the plan row — a caller whose identity ≠ plan.tenantId
//    gets an empty array, never another tenant's signed URL."
export const attachmentUrls = tenantQuery({
  args: { planId: v.id("plans") },
  handler: async (ctx, { planId }) => {
    const plan = await ctx.db.get(planId);
    if (!plan || plan.tenantId !== ctx.tenantId) return [];
    return Promise.all((plan.attachments ?? []).map(async (a) => ({
      filename: a.filename, mimeType: a.mimeType, size: a.size,
      url: await ctx.storage.getUrl(a.storageId),
    })));
  },
});
```

CLAUDE.md §2: import `tenantQuery` / `tenantMutation` from `convex/lib/functions.ts`. Raw
`query`/`mutation` imports are BANNED here and the Biome rule enforces it.

`cockpit.ts:531` — the executePlan rule: *"no cross-tenant approve"* — `throw new Error("plan not found")`
when `plan.tenantId !== ctx.tenantId`. Reads return `[]`; writes throw.

From plan 20-01: `chooseMediaBatch`, `MEDIA_JOB_CAP_USD = 3.50`, `MEDIA_SANDBOX_USD_PER_RENDER`,
`MEDIA_DEFAULT_VIDEO/IMAGE/VOICE/STT`, `MAX_CHARS_PER_BLOCK = 140`, `isPaidBlock`.
From plan 20-02: the render plane on `plans` — `renderStatus`, `renderStorageId`, `sidecarStorageId`,
`sidecarHash`, `renderReason`, `renderedAt`; `plans.shots[].narration`; `plans.clipSeconds`.
From plan 20-04: `reserveJobInner(ctx, { tenantId, planId, blocks, clipSeconds, withCaptions })`, and
`mediaRemainingCents` (internalQuery on `guardrails.ts`).
From plan 20-05: `internal.media.submitBatch`.
From plan 20-13: `parseAssemblySidecar` — used here to render the sidecar's GATE SUMMARY in words.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the read plane — the block pipeline, the reel, and the ITEMISED estimate the button waits on</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - `byPlan({ planId })` returns **one entry per BLOCK** carrying `{ blockIndex, narration,
      narrationChars, overCharLimit, clip: { status, verdict, model, spec, estUsd, actualCents },
      voice: { status, verdict, estUsd, actualCents } }` — **two independent states per block**, and
      **no URL**. A block whose voice has landed but whose clip has not must be distinguishable from
      one where the reverse is true; a single merged status cannot express that.
    - `assetUrls({ planId })` returns `[{ blockIndex, kind, mimeType, status, verdict, url }]` where
      `url` is `ctx.storage.getUrl(assetStorageId)`; a line with no asset yet yields a null url, not
      an omitted row.
    - **`reel({ planId })`** returns `{ status, url, durationS, blockCount, gates, reason }` — the
      finished mp4's signed URL and the sidecar's gate summary IN WORDS. It returns a null `url`
      whenever `renderStatus !== "rendered"`, and it returns `reason` for `failed`, so the canvas can
      say *"the render finished but did not produce a valid assembly record, so it was not published"*
      rather than showing an empty box.
    - All three return `[]` / a null-shaped object for a caller whose identity ≠ the plan's tenant.
    - **`jobEstimate({ planId })`** returns
      `{ lines: [{ label, qty, unit, cents }], totalCents, capCents, remainingCents, refusal? }`
      with **four labelled lines** — clips, voice, captions, render — so the UI prints
      *"6 clips $3.00 · voice $0.02 · captions $0.01 · render $0.02 = $3.05. $6.95 of today's $10.00
      media budget remains."* A single total is not enough: the user must be able to see which line
      is the expensive one before deciding to cut a block.
    - `refusal` carries `over_job_cap` / `unknown_model` / `illegal_duration` / `narration_too_long`
      (with its block index and character count) so the UI can name the lever.
    - `jobEstimate` consumes NOTHING — it is a query and cannot.
    - Free block types contribute 0 to the clip line and are still listed, so the user sees why a
      10-block reel does not cost 10 clips.
  </behavior>
  <action>
Four `tenantQuery`s in `media.ts`, all guarding on the owning `plans` row exactly as
`plans.attachmentUrls` does. Carry that function's header comment across, adapted — the bearer-URL
rule is the reason the query exists and must be readable at the call site.

`jobEstimate` reuses `chooseMediaBatch` — the SAME function `reserveJobInner` uses — and builds the
SAME spec list `reserveJobInner` builds, including the 2× voice multiplier and the flat render
constant. It must not re-derive prices or re-derive the line set; a UI that computes its own estimate
and a rail that computes another is the drift this phase exists to prevent. **Assert equality with
`reserveJobInner`'s number directly**, not by eyeball. It also reads `mediaRemainingCents`.

`reel` parses the stored sidecar with `parseAssemblySidecar` and returns its `gates` and
`totalDurationS` **as data, not as prose** — the wording lives in the canvas (plan 20-10), because
BRAND copy is a UI concern. What lives here is the guarantee that a `url` is only ever non-null when
the sidecar validated.

`ponytail:` note: `byPlan`, `assetUrls` and `reel` are three queries rather than one because only two
of them mint bearer URLs, and a query that mints one should be the smallest possible surface. Do not
merge them.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>All four queries return tenant-guarded data; a block reports clip and voice independently; `reel` returns a null url for every non-`rendered` status and a reason for `failed`; `jobEstimate`'s four lines sum to `reserveJobInner`'s number for the same deck (asserted directly).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the paid write plane — generate the reel, regenerate one block, through the ONE money gate</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - `generateReel({ planId })` reserves the whole job via `reserveJobInner`, sets
      `renderStatus: "pending"` and schedules `internal.media.submitBatch`; on refusal it returns the
      governed reason and schedules nothing.
    - `regenerateBlock({ planId, blockIndex })` is a **job of one block** through the identical path —
      same cap, same window, same rows, no bypass — and it reserves its clip, its voice take AND a
      render, because a regenerated block requires a re-render.
    - **Regenerating any block CLEARS the render:** `renderStorageId`, `sidecarStorageId`,
      `sidecarHash` and `renderedAt` are unset and `renderStatus` returns to `pending`. Assert it.
      A canvas showing a stale `final.mp4` beside a freshly regenerated block is lying to the user,
      and it is the kind of lie they would only discover by watching the whole reel.
    - Regenerating a block that already has an asset creates NEW job rows; the previous rows and their
      stored assets are left alone (history, not mutation).
    - Neither can be called for another tenant's plan — a cross-tenant call throws `plan not found`.
    - Neither mutation calls fal directly; both only schedule.
  </behavior>
  <action>
Two `tenantMutation`s. Both:
1. `const plan = await ctx.db.get(planId); if (!plan || plan.tenantId !== ctx.tenantId) throw new Error("plan not found");`
   — the `cockpit.ts:531` no-cross-tenant rule verbatim. **Writes throw; reads return `[]`.**
2. Build the block list from `plan.shots`, mapping each to its specs with the model, resolution, voice
   and sample rate **explicitly pinned** from the `MEDIA_DEFAULT_*` constants — never left for fal to
   default.
3. `reserveJobInner(ctx, …)` → on `{ ok: false }` return the reason (a governed stop, never a throw).
4. Patch `renderStatus: "pending"` and clear the four render artifact fields **in the same mutation as
   the reservation** — so a regenerate can never leave a published reel that no longer matches its
   blocks, not even for a scheduler tick.
5. `await ctx.scheduler.runAfter(0, internal.media.submitBatch, { tenantId: ctx.tenantId, batchId })`.

**`regenerateBlock` is deliberately not a second code path.** One block, one `reserveJobInner` call,
one `submitBatch`. Comment it: *"a job of one block — clip, voice and render. No second rail, no
second cap, no bypass: the job cap and the daily window are the same two numbers whether the reel is
1 block or 6. And it reserves a RENDER, because a changed block means the published mp4 is stale and
the reel must be assembled again — a regenerate that skipped the render line would be cheaper and
wrong."*

Refs-only audit on both (the plan 20-06 allow-list; **bump that plan's pinned audit-site count**).
Never the prompt, never the narration, never a filename.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>Both paid entry points reserve before scheduling and refuse without scheduling; a regenerate clears every render artifact field in the same transaction; a cross-tenant call throws; the media window moves by exactly the estimate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the free editor mutations — now five — and the isolation assertion that ships with the surface</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts, docs/playbooks/media.md</files>
  <behavior>
    - `editBlockPrompt({ planId, blockIndex, prompt })` patches one element of `plans.shots`; costs nothing.
    - **`editBlockNarration({ planId, blockIndex, narration })`** patches the block's spoken line;
      costs nothing; **REFUSES a narration over `MAX_CHARS_PER_BLOCK`** with the same
      `narration_too_long` reason and the same character count the parser returns. This is not scope
      creep — it is the UI half of the pre-payment guard, and without it the refusal has no cure: a
      user told *"block 4's line is 186 characters"* with no way to shorten it is stuck.
    - `reorderBlocks({ planId, order })` permutes `plans.shots`, renumbers `index` and recomputes
      `windowStartMs`; costs nothing; rejects an `order` that is not a permutation of the existing
      indices.
    - `deleteBlock({ planId, blockIndex })` splices, renumbers and recomputes `windowStartMs`; costs nothing.
    - **None of the five touches `mediaJobs` or any budget window.** Existing jobs keep their own
      `blockIndex` snapshot so a reorder cannot re-point a landed asset at a different block.
    - **All four structural edits (reorder, delete, and either text edit) CLEAR the render** the same
      way a regenerate does — a reel assembled from a different block order is not the reel on screen.
      Assert it for each.
    - **Isolation:** tenant B gets `[]`/null from `assetUrls`, `byPlan` and `reel` on tenant A's plan;
      tenant B's `regenerateBlock`/`editBlockPrompt`/`editBlockNarration`/`reorderBlocks`/`deleteBlock`
      on tenant A's plan all throw; and the webhook, given tenant A's job id + digest, writes ONLY
      tenant A's row.
  </behavior>
  <action>
Five `tenantMutation`s, each the same guard then one array patch plus the render-clear. D7's scope
floor AND ceiling: edit prompt, edit narration, regenerate one block, reorder, delete. **Nothing
else.** No timeline, transitions, filters, layers, masking, music, or client-side rendering. If a
reviewer asks for one, it is a deferred idea, not a small addition.

**Factor the render-clear into ONE private helper** used by all six mutating paths (the five here plus
`regenerateBlock`). Six copies of a four-field unset is exactly how one of them ends up missing a
field, and this is the guarantee that a stale reel can never be shown.

**The reorder hazard, and it is real:** `mediaJobs.blockIndex` is a snapshot taken at reserve time. A
reorder after submit would otherwise re-point a landed asset at a different block's tile. Keep the
job's own `blockIndex` authoritative for rendering and `ponytail:` comment the ceiling: *"a reorder
after submit leaves in-flight jobs pointing at their original index. The canvas renders a job under
the block it was reserved for. Upgrade path if that ever confuses anyone: a stable per-block id
instead of an array index — which is a schema change, not a UI one."*

The isolation assertions go in `media.test.ts` using the two-tenant `convex-test` harness
(plan 20-04's, extended). This is BETA-05's obligation for this surface.

Bump `docs/playbooks/media.md`: the canvas contract (what is read, what spends, what is free), the
editor's floor and ceiling now at five affordances, the estimate-before-spend rule with its four
itemised lines, the render-invalidation rule and the single helper that enforces it, the reorder
ceiling, and the isolation assertion's location.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>The five editor mutations work and cost nothing; an over-length narration edit is refused with its count; every structural edit clears the render through one shared helper; all isolation assertions pass; `node scripts/check-playbooks.mjs` exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- media` and `pnpm test` full suite green.
- `pnpm typecheck` backend delta exactly 0 against the 150 baseline.
- `node scripts/check-playbooks.mjs` exits 0.
- **Mutation check:** drop the `plan.tenantId !== ctx.tenantId` guard from `assetUrls` — the two-tenant assertion must go RED. Restore.
- **Mutation check:** make `regenerateBlock` schedule `submitBatch` without calling `reserveJobInner` — the "window moved by exactly the estimate" assertion must go RED. Restore.
- **Mutation check:** remove the render-clear from `reorderBlocks` — the stale-reel assertion must go RED. Restore. **Observe it: this is the assertion that stops the canvas lying.**
- **Cost: $0.**
- Shared tree: `ls .git/MERGE_HEAD`; pathspec commits only.
</verification>

<success_criteria>
- SC4's isolation half is closed with a shipped assertion.
- Every paid control routes through the one reservation, every free control provably touches no window, and no control can leave a reel on screen that its blocks no longer produce.
- The user can see WHICH line of the job costs the money before they buy it.
</success_criteria>

<output>
After completion, create `.planning/phases/20-media-canvas/20-09-SUMMARY.md`. Record the exact
query/mutation names and return shapes — plan 20-10's canvas is written against them — and the
render-clear helper's name and the six call sites it covers.
</output>
</content>
