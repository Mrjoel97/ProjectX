---
phase: 20-media-canvas
plan: 09
type: execute
wave: 6
depends_on: ["20-07"]
files_modified:
  - packages/backend/convex/media.ts
  - packages/backend/convex/media.test.ts
  - docs/playbooks/media.md
autonomous: true
requirements: [MEDIA-01]
lane_gate: "NOT gated on Phase 16."

must_haves:
  truths:
    - "The canvas can show the per-shot estimate, the deck total, the model and resolution it was priced at, and today's remaining media budget — BEFORE anything is spent."
    - "A human click generates: the deck, or one shot, through the identical reserve-then-submit path."
    - "A user can edit one shot's prompt, reorder shots and delete a shot, and none of those costs a cent."
    - "Another tenant gets an empty array from assetUrls, cannot regenerate this tenant's shot, and cannot read this tenant's jobs."
    - "A signed asset URL is minted only inside a tenant-guarded query and is never logged."
  artifacts:
    - path: "packages/backend/convex/media.ts"
      provides: "byPlan / assetUrls / deckEstimate / remainingCents tenantQueries; generateBatch / regenerateShot / editShotPrompt / reorderShots / deleteShot tenantMutations"
      contains: "tenantQuery"
  key_links:
    - from: "packages/backend/convex/media.ts assetUrls"
      to: "ctx.storage.getUrl"
      via: "a tenantQuery that guards the owning plan row before minting the bearer URL"
      pattern: "ctx.storage.getUrl"
    - from: "packages/backend/convex/media.ts generateBatch"
      to: "reserveBatchInner"
      via: "the identical money gate the approve arm uses — no second rail, no bypass"
      pattern: "reserveBatchInner"
---

<objective>
The canvas's backend plane: what it reads, what it can spend, and what it can edit for free — with
the isolation assertion that ships alongside the surface.

Purpose: D7's binding rule — *"the editor must not offer a control that can spend money without
showing the estimate first"* — is a backend requirement before it is a UI one. The estimate query
exists so the button can be disabled until the number is on screen. And BETA-05: an isolation
assertion ships with each new surface, not after it.

Output: four tenant-guarded reads, five tenant-guarded writes, and the two-tenant assertions.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-CONTEXT.md
@.planning/phases/20-media-canvas/20-RESEARCH.md
@.planning/phases/20-media-canvas/20-04-SUMMARY.md
@.planning/phases/20-media-canvas/20-07-SUMMARY.md
@packages/backend/convex/media.ts
@packages/backend/convex/plans.ts
@packages/backend/convex/lib/functions.ts
@docs/playbooks/media.md

<interfaces>
<!-- THE template for assetUrls, read from source. Copy it structurally. -->

```ts
// plans.ts:374-388 — and its header comment IS the rule:
//   "the URL is a bearer capability, so it is ONLY ever returned from this tenant-guarded query and
//    NEVER logged (CLAUDE.md §4). Guards on the plan row — a caller whose identity ≠ plan.tenantId
//    gets an empty array, never another tenant's signed URL."
export const attachmentUrls = tenantQuery({
  args: { planId: v.id("plans") },
  handler: async (ctx, { planId }) => {
    const plan = await ctx.db.get(planId);
    if (!plan || plan.tenantId !== ctx.tenantId) return [];
    return Promise.all((plan.attachments ?? []).map(async (a) => ({
      filename: a.filename, mimeType: a.mimeType, size: a.size,
      url: await ctx.storage.getUrl(a.storageId),
    })));
  },
});
```

CLAUDE.md §2: import `tenantQuery` / `tenantMutation` from `convex/lib/functions.ts`. Raw
`query`/`mutation` imports are BANNED here and the Biome rule enforces it.

`cockpit.ts:531` — the executePlan rule: *"no cross-tenant approve"* — `throw new Error("plan not found")`
when `plan.tenantId !== ctx.tenantId`. Reads return `[]`; writes throw.

From plan 20-04: `reserveBatchInner(ctx, { tenantId, planId, shots })`, and `mediaRemainingCents`
(internalQuery on `guardrails.ts`).
From plan 20-01: `chooseMediaBatch`, `MEDIA_BUDGET_USD_PER_REQUEST`, `MEDIA_DEFAULT_VIDEO`,
`MEDIA_DEFAULT_IMAGE`, `isPaidShot`.
From plan 20-05: `internal.media.submitBatch`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the read plane — jobs, asset URLs, and the estimate the button waits on</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - `byPlan({ planId })` returns one entry per shot with `{ shotIndex, status, verdict, model, spec, estCents, actualCents }` — and **no URL**.
    - `assetUrls({ planId })` returns `[{ shotIndex, mimeType, status, verdict, url }]` where `url` is
      `ctx.storage.getUrl(assetStorageId)`; a shot with no asset yet yields a null url, not an omitted row.
    - Both return `[]` for a caller whose identity ≠ the plan's tenant.
    - `deckEstimate({ planId })` returns `{ perShot: [{ shotIndex, cents, model, resolution }], totalCents, remainingCents, refusal? }` —
      where `refusal` carries `over_batch_cap` / `unknown_model` / `illegal_duration` when the deck
      cannot be priced, so the UI can name the lever.
    - `deckEstimate` consumes NOTHING — it is a query and cannot.
    - Free shot types contribute 0 to `totalCents` and are still listed, so the user sees why a
      10-shot deck costs $0.33.
  </behavior>
  <action>
Three `tenantQuery`s in `media.ts`, all guarding on the owning `plans` row exactly as
`plans.attachmentUrls` does. Carry that function's header comment across, adapted — the bearer-URL
rule is the reason the query exists and must be readable at the call site.

`deckEstimate` reuses `chooseMediaBatch` — the SAME function `reserveBatchInner` uses. It must not
re-derive prices; a UI that computes its own estimate and a rail that computes another is the drift
this phase exists to prevent. It also reads `mediaRemainingCents` so the canvas can print *"$4.67 of
today's $5.00 media budget remains."*

Worked example the copy must be able to produce (put it in the doc comment):
*"9 images + 1 clip — 9 × $0.009 (FLUX schnell, 1080×1920) + 1 × $0.25 (Wan 2.5, 480p, 5 s) = $0.33."*

`ponytail:` note: `byPlan` and `assetUrls` are two queries rather than one because only `assetUrls`
mints bearer URLs, and a query that mints one should be the smallest possible surface. Do not merge
them.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>All three queries return tenant-guarded data; `deckEstimate` agrees with `reserveBatchInner`'s number for the same deck (assert equality directly, not by eyeball).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the paid write plane — generate the deck, regenerate one shot, through the ONE money gate</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - `generateBatch({ planId })` reserves the deck via `reserveBatchInner` and schedules
      `internal.media.submitBatch`; on refusal it returns the governed reason and schedules nothing.
    - `regenerateShot({ planId, shotIndex })` is a **batch of one** through the identical path — same
      cap, same window, same rows, no bypass.
    - Regenerating a shot that already has an asset creates a NEW job row; the previous row and its
      stored asset are left alone (history, not mutation).
    - Neither can be called for another tenant's plan — a cross-tenant call throws `plan not found`.
    - Neither mutation calls fal directly; both only schedule.
  </behavior>
  <action>
Two `tenantMutation`s. Both:
1. `const plan = await ctx.db.get(planId); if (!plan || plan.tenantId !== ctx.tenantId) throw new Error("plan not found");`
   — the `cockpit.ts:531` no-cross-tenant rule verbatim. **Writes throw; reads return `[]`.**
2. Build the shot list from `plan.shots`, paid shots only (`isPaidShot`), mapping each to its
   `MediaSpec` with the model and resolution **explicitly pinned** from `MEDIA_DEFAULT_VIDEO` /
   `MEDIA_DEFAULT_IMAGE` — never left for fal to default.
3. `reserveBatchInner(ctx, …)` → on `{ ok: false }` return the reason (a governed stop, never a throw).
4. `await ctx.scheduler.runAfter(0, internal.media.submitBatch, { tenantId: ctx.tenantId, batchId })`.

**`regenerateShot` is deliberately not a second code path.** One shot, one `reserveBatchInner` call,
one `submitBatch`. Comment it: *"a batch of one. No second rail, no second cap, no bypass — the
per-request cap and the daily window are the same two numbers whether the deck is 1 shot or 10."*

Refs-only audit on both (the plan 20-06 allow-list). Never the prompt, never a filename.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>Both paid entry points reserve before scheduling and refuse without scheduling; a cross-tenant call throws; the media window moves by exactly the estimate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the free editor mutations, and the isolation assertion that ships with the surface</name>
  <files>packages/backend/convex/media.ts, packages/backend/convex/media.test.ts, docs/playbooks/media.md</files>
  <behavior>
    - `editShotPrompt({ planId, shotIndex, prompt })` patches one element of `plans.shots`; costs nothing.
    - `reorderShots({ planId, order })` permutes `plans.shots` and renumbers `index`; costs nothing;
      rejects an `order` that is not a permutation of the existing indices.
    - `deleteShot({ planId, shotIndex })` splices and renumbers; costs nothing.
    - **None of the three touches `mediaJobs` or any budget window.** Existing jobs keep their own
      `shotIndex` snapshot so a reorder cannot re-point a landed asset at a different shot.
    - **Isolation:** tenant B gets `[]` from `assetUrls` and `byPlan` on tenant A's plan; tenant B's
      `regenerateShot`/`editShotPrompt`/`deleteShot` on tenant A's plan throw; and the webhook, given
      tenant A's job id + digest, writes ONLY tenant A's row.
  </behavior>
  <action>
Three `tenantMutation`s, each the same guard then one array patch. D7's scope floor AND ceiling: edit
prompt, regenerate one shot, reorder, delete. **Nothing else.** No timeline, transitions, filters,
layers, masking, audio, or client-side rendering — and no `/assemble`, no concatenation. If a
reviewer asks for one, it is a deferred idea, not a small addition.

**The reorder hazard, and it is real:** `mediaJobs.shotIndex` is a snapshot taken at reserve time. A
reorder after submit would otherwise re-point a landed asset at a different shot's tile. Keep the
job's own `shotIndex` authoritative for rendering and `ponytail:` comment the ceiling: *"a reorder
after submit leaves in-flight jobs pointing at their original index. The canvas renders a job under
the shot it was reserved for. Upgrade path if that ever confuses anyone: a stable per-shot id instead
of an array index — which is a schema change, not a UI one."*

The isolation assertions go in `media.test.ts` using the two-tenant `convex-test` harness
(plan 20-04's, extended). This is BETA-05's obligation for this surface.

Bump `docs/playbooks/media.md`: the canvas contract (what is read, what spends, what is free), the
editor's floor and ceiling, the estimate-before-spend rule, the reorder ceiling, and the isolation
assertion's location.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>The three editor mutations work and cost nothing; all four isolation assertions pass; `node scripts/check-playbooks.mjs` exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- media` and `pnpm test` full suite green.
- `pnpm typecheck` backend delta exactly 0 against the 150 baseline.
- `node scripts/check-playbooks.mjs` exits 0.
- **Mutation check:** drop the `plan.tenantId !== ctx.tenantId` guard from `assetUrls` — the two-tenant assertion must go RED. Restore.
- **Mutation check:** make `regenerateShot` schedule `submitBatch` without calling `reserveBatchInner` — the "window moved by exactly the estimate" assertion must go RED. Restore.
- **Cost: $0.**
- Shared tree: `ls .git/MERGE_HEAD`; pathspec commits only. **Do not bump `cockpit.md` or `growth-diagnostic.md`** — plan 20-08 owns them this wave.
</verification>

<success_criteria>
- SC4's isolation half is closed with a shipped assertion.
- Every paid control routes through the one reservation, and every free control provably touches no window.
</success_criteria>

<output>
After completion, create `.planning/phases/20-media-canvas/20-09-SUMMARY.md`. Record the exact
query/mutation names and return shapes — plan 20-10's canvas is written against them.
</output>
