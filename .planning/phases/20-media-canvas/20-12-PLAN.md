---
phase: 20-media-canvas
plan: 12
type: execute
wave: 15
depends_on: ["20-11", "20-20"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/scripts/eval-cases/38-media-dispatch.json
  - packages/backend/scripts/eval-cases/38b-media-not-a-document.json
  - packages/backend/scripts/run-eval-golden.mjs
  - docs/playbooks/agent-runtime.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/skill-registry.md
  - .planning/phases/20-media-canvas/20-12-SUMMARY.md
autonomous: false
requirements: [MEDIA-01]
scope: "Media body/fixture candidate cycle only. Finance clearance is plan 20-20."

must_haves:
  truths:
    - "The media edit starts from the exact active finance fingerprint evidenced by 20-20."
    - "A media-specific fixture proves ordinary video language calls dispatchMedia from agent-step/plan state, never reply prose."
    - "A NEGATIVE fixture proves a document deliverable (slide deck, one-pager, report) does NOT reach dispatchMedia: mediaDispatchCount 0 and createdDocCount 1 on the same turn."
    - "The body says what createDocument actually writes (markdown + PDF) and that asking for pptx/docx/slides is answered with what it does write, never agreed to."
    - "The body says a created document appears in the WORKSPACE as well as the vault, so the agent stops reporting its tool inventory as the product capability."
    - "All watched playbooks are updated during the offline edit before the first owner pause."
    - "Paid seed/eval and exact-version activation are separately authorized."
    - "Activation completes before a distinct no-spend conversational observation; the two checkpoints are not conflated."
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "Creating images and video guidance on the certified finance body"
      contains: "dispatchMedia"
    - path: "packages/backend/scripts/eval-cases/38-media-dispatch.json"
      provides: "Plain-language media dispatch fixture"
      contains: "mediaDispatchCount"
    - path: "packages/backend/scripts/eval-cases/38b-media-not-a-document.json"
      provides: "The inverse assertion — a slide-deck request routes to createDocument, not to the reel specialist"
      contains: "createdDocCount"
    - path: "docs/playbooks/agent-runtime.md"
      provides: "Fixture/runner observable and candidate-pinned media gate contract"
      contains: "mediaDispatchCount"
    - path: ".planning/phases/20-media-canvas/20-12-SUMMARY.md"
      provides: "Media candidate/eval/activation and later no-spend UAT evidence"
  key_links:
    - from: "packages/backend/scripts/eval-cases/38-media-dispatch.json"
      to: "packages/backend/scripts/run-eval-golden.mjs"
      via: "mediaDispatchCount reads the dispatchMedia tool record for the fixture plan"
      pattern: "mediaDispatchCount"
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "packages/contracts/src/skills/cockpitAgent.ts"
      via: "same-commit generated mirror"
      pattern: "cockpitAgentSkillBody"
---

<objective>
Teach the certified active `cockpit-agent` body to select and describe `dispatchMedia`, certify that
edit with an owed behavioural fixture, activate the exact evidenced version, and then observe a
separate real no-spend conversational turn.

Finance lifecycle debt is not part of this plan; plan 20-20 clears it first. This plan has one body
edit, one paid candidate gate, one activation authority and one later observation checkpoint.

**FOLDED IN 2026-08-14 — the routing half of a live cockpit defect.** A real conversation asked
for a slide deck and the agent invoked `dispatchMedia`, the short-form VIDEO specialist, on
three consecutive turns. The destructive half is already fixed in code (`dispatch_in_flight`
and `image_proposal_pending` in `stageMediaPlan`, commit `ceb21b6`) — a retry can no longer
discard a staged proposal. What code cannot fix is the agent reaching for the video tool for a
document in the first place; that is a body rule, and it is folded HERE rather than paid for in
its own gate. Full trace in `docs/playbooks/cockpit.md`, section
"The created-artifact surface".

The same transcript produced two more body-shaped defects, both mitigated in code and both
belonging in the body durably: the agent claimed it had written the deck **in PowerPoint
format** (no format argument exists; `DocFormat` is `pdf | html`), and told the user it
**cannot open files in the workspace** when the Output card was already rendering the document
there. Tool results now say both things; the body should too, because a tool result only
reaches the model AFTER it has already chosen the tool.

DRAFT WORDING — for the `Creating a document or a post` section. Not applied to
`packages/contracts/skills/cockpit-agent.md` here: editing the live body outside this plan
would create an uncertified candidate, which is the exact lifecycle debt 20-20 exists to clear.

> - **A slide deck, a one-pager, a report or a proposal is `createDocument`, never
>   `dispatchMedia`.** `dispatchMedia` proposes a short-form VIDEO reel and nothing else. If
>   they want a deck they can read, that is a document; only ask for a reel when they asked
>   for a video.
> - **It writes markdown, plus a PDF for `long`.** There is no PowerPoint, Word or slides
>   output. Asked for one, say what you can write instead — never report having produced a
>   format that does not exist.
> - **A created document appears in the workspace as well as the vault**, so the user can read
>   it without downloading anything. You have no tool that opens it, and you do not need one.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-media-canvas/20-08-SUMMARY.md
@.planning/phases/20-media-canvas/20-11-SUMMARY.md
@.planning/phases/20-media-canvas/20-20-SUMMARY.md
@packages/contracts/skills/cockpit-agent.md
@packages/contracts/src/skills/cockpitAgent.ts
@packages/backend/scripts/run-eval-golden.mjs
@docs/playbooks/agent-runtime.md
@docs/playbooks/cockpit.md
@docs/playbooks/skill-registry.md

<interfaces>
`dispatchMedia` is already registered in the live toolset. This plan improves selection reliability
and scope honesty; it does not create reachability. The candidate stream must be empty and the active
version/sha must match 20-20 before editing. Optimizer rows can consume version numbers, so every live
step reads the actual version and sha rather than predicting them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: media guidance, mirror, non-vacuous fixture and watched playbooks</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, packages/backend/scripts/eval-cases/38-media-dispatch.json, packages/backend/scripts/run-eval-golden.mjs, docs/playbooks/agent-runtime.md, docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md</files>
  <action>
First read the live active row and prove it matches 20-20's finance version/sha with no parked
candidate. Then make the offline edit:

1. Add `## Creating images and video`: ordinary social reel/ad/explainer asks use `dispatchMedia`;
   the director returns script, art direction and narrated blocks later; proposal is free; clips,
   voice, captions and assembly require a separate human Generate click and take minutes; the agent
   cannot click or claim generation happened. Relay budget/model/duration/narration refusals with a
   useful lever. Promise only the shipped 5/10-second-block reel, never music, sung tracks, re-cut
   footage or unsupported length.
2. Regenerate `cockpitAgent.ts` in the same commit and run mirror drift tests.
3. Add fixture 38 using ordinary video language without naming the tool. Add closed
   `mediaDispatchCount`, graded from `agentSteps`/plan state. Require exactly one call, zero
   recipients/attachments and status at most proposed. Add self-checks rejecting zero as vacuous and
   failing absent/duplicate calls; raise the fixture deletion floor by one and mutation-check reader.
4. Before the first pause, update `agent-runtime.md` for the new observable/fixture/gate mechanics,
   `cockpit.md` for user-visible media selection and the no-Generate authority boundary, and
   `skill-registry.md` for the owed-fixture and candidate lifecycle. Run playbook gate now.
  </action>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The offline body/mirror/fixture and every watched playbook are green; no candidate is seeded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: authorize media candidate seed and paid gate</name>
  <files>None — approval and live registry/eval rows only</files>
  <action>Present the offline diff/proofs, exact active predecessor, empty candidate stream and cost estimate; wait for a hard-maximum proceed signal.</action>
  <decision>May the executor seed the media candidate and run its complete paid gate?</decision>
  <context>The full gate historically costs about $0.35 plus the new case. Any paid single-case falsification is included in the same owner maximum. The gate never clicks Generate, so fal media spend remains zero.</context>
  <options>
    <option id="proceed"><name>Proceed with a named maximum</name><pros>Produces candidate-pinned media evidence.</pros><cons>Consumes one complete paid golden run.</cons></option>
    <option id="hold"><name>Hold</name><pros>No live mutation or spend.</pros><cons>Phase 20 and 20.1-02 remain blocked.</cons></option>
  </options>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The owner approved an explicit maximum, or execution stopped with the edit unseeded.</done>
  <resume-signal>Reply `proceed media gate, max $X` or `hold`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: seed, evaluate and evidence the media candidate</name>
  <files>None — live skill/eval rows only</files>
  <action>After approval, seed the edited body, read back actual candidate version/sha, optionally falsify fixture 38 inside the same cap, then run the complete gate pinned to that exact candidate. The run must include finance fixture 37 and media fixture 38. Record run/case/retry/pin/cost evidence, stay below the hard maximum, attach evidence and leave candidate parked.</action>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The exact media candidate has complete green evidence and remains unactivated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: owner activates the evidenced media candidate</name>
  <files>None — owner-gated live skill row only</files>
  <action>Present exact evidence and wait for exact-version authorization; activate only that version and read back active version/sha. Do not perform the conversational observation in this task.</action>
  <what-built>A media candidate pinned to a complete green run containing both finance and media fixtures.</what-built>
  <how-to-verify>Review version/sha, full case count, retries and cost. After authorization, activate that exact version and prove live active version/sha equals the evidence pin. Rejection leaves it parked.</how-to-verify>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The exact evidenced media candidate is active, or the plan stopped parked.</done>
  <resume-signal>Reply `activate media candidate <actual-version>` or describe the rejection.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: later no-spend conversational observation</name>
  <files>None — live conversation and observed rows only</files>
  <action>Only after Task 4 active-row readback, ask for a short reel in ordinary language and observe proposal behavior without clicking Generate.</action>
  <what-built>An active media-bearing cockpit body whose selection behavior now needs a real conversational observation.</what-built>
  <how-to-verify>Ask for a short social reel without naming `dispatchMedia`. Confirm one proposal/card arrives with script, art direction and narrated blocks; the agent says generation requires a separate click and time. Do not click Generate. Confirm mediaJobs/provider actual spend remains zero. Record thread/plan refs and timestamps only.</how-to-verify>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>Ordinary language stages a proposal on the active body and fal media spend remains zero.</done>
  <resume-signal>Reply `approved` with the no-spend observation, or describe the mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: record media-cycle and observation evidence</name>
  <files>docs/playbooks/agent-runtime.md, docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md, .planning/phases/20-media-canvas/20-12-SUMMARY.md</files>
  <action>Append evidence-only updates: actual candidate/active version/sha, run id/count/retries/cost, activation authorization/readback and later thread/plan refs plus zero media spend. Create summary, run playbook gate and diff check. Mark MEDIA-01 complete only if 20-11 evidence is complete and Phase 20 has no open validation row.</action>
  <verify><automated>node scripts/check-playbooks.mjs</automated></verify>
  <done>The exact media lifecycle and distinct later observation are reproducibly recorded.</done>
</task>

</tasks>

<verification>
- 20-20's active finance fingerprint is the predecessor to the media candidate.
- Fixture 38 and its runner observable are documented in agent-runtime before the first pause.
- One complete media gate ran within its separately approved maximum and included fixtures 37/38.
- Activation and later no-spend observation are distinct checkpoints.
- Contracts tests, runner self-check, playbook gate and diff check pass.
</verification>

<success_criteria>
- The active body reliably selects `dispatchMedia` and describes its human-gated workflow honestly.
- Finance remains certified; media is the only instruction delta in this candidate.
- A later real turn stages a proposal with zero fal media spend.
</success_criteria>

<output>
Create `20-12-SUMMARY.md` only after the later observation. Do not fabricate live identifiers or cost.
</output>
