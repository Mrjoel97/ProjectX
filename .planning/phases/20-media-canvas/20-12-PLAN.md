---
phase: 20-media-canvas
plan: 12
type: execute
wave: 14
depends_on: ["20-11"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/scripts/eval-cases/38-media-dispatch.json
  - packages/backend/scripts/run-eval-golden.mjs
  - docs/playbooks/cockpit.md
  - docs/playbooks/skill-registry.md
autonomous: false
requirements: [MEDIA-01]
resume: "REFRESHED 2026-08-10. The source body contains merged finance edits (5f3f45e and fixes) that are not yet seeded/evaluated/activated. Clear that candidate before creating the media candidate."

must_haves:
  truths:
    - "The finance-bearing source fingerprint is seeded, fully evaluated, evidenced and owner-activated before the media body is edited or read as live."
    - "The media edit becomes a later candidate, never an accidental finance+media aggregate candidate."
    - "A new media-specific golden fixture proves a plain-language video request calls dispatchMedia from plan/tool state, not from reply prose."
    - "Every paid eval and every activation has separate explicit owner approval; no plan-authored version number is trusted over the live row."
    - "The activated media body says proposal is free, generation/rendering need a separate human click, and the system makes a reel without promising unsupported music, re-cutting or length."
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "The Creating images and video section on top of the already-certified finance body"
      contains: "dispatchMedia"
    - path: "packages/backend/scripts/eval-cases/38-media-dispatch.json"
      provides: "Non-vacuous behavioural evidence that ordinary video language reaches dispatchMedia"
      contains: "mediaDispatchCount"
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "Closed mediaDispatchCount observable, fixture validation, mutation/self-check and raised deletion floor"
      contains: "mediaDispatchCount"
  key_links:
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "packages/contracts/src/skills/cockpitAgent.ts"
      via: "the generated LF-normalized mirror committed with the markdown body"
      pattern: "cockpitAgentSkillBody"
    - from: "packages/backend/scripts/eval-cases/38-media-dispatch.json"
      to: "packages/backend/scripts/run-eval-golden.mjs"
      via: "mediaDispatchCount reads dispatchMedia agent-step records for the fixture plan"
      pattern: "mediaDispatchCount"
---

<objective>
Close Phase 20’s conversational reliability gap without crossing the shared candidate stream.

The old plan assumed Phase 16 still held `cockpit-agent@16`. That is stale: Phase 19 activated v18,
then the finance work merged a newer body plus fixture 37 into source (`5f3f45e`, followed by
`4e09e64`, `d97dabc`, `06e2ff1`, `f278606` in its history). Those finance edits are code-complete but
not seeded, evaluated or active. They must clear their own gate first. Only then may this plan add
media guidance, seed a second candidate, evaluate it with an owed media fixture, and request owner
activation.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-08-SUMMARY.md
@.planning/phases/20-media-canvas/20-11-SUMMARY.md
@packages/contracts/skills/cockpit-agent.md
@packages/contracts/src/skills/cockpitAgent.ts
@packages/backend/scripts/eval-cases/37-finance-update.json
@packages/backend/scripts/run-eval-golden.mjs
@packages/backend/convex/skills.ts
@docs/playbooks/skill-registry.md

<interfaces>
`cockpit-agent` is gated and has one candidate stream. `seedSkills` publishes a changed source body as
`maxVersion + 1` candidate; it does not make the body active. `recordEvalEvidence` and owner-gated
activation are distinct live mutations. Optimizer candidates can consume version numbers, so every
gate reads back the actual candidate version and sha256 after seeding.

`dispatchMedia` is already registered in the live toolset. This plan improves selection reliability
and scope honesty; it does not create reachability. The golden fixture must observe the tool call or
its staged plan state, never infer success from an agreeable sentence.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: authorize the finance candidate cycle that source already owes</name>
  <files>None — live registry/eval state only</files>
  <action>Run the stated $0 preflight, present the exact source and live fingerprints, and wait for the owner to choose proceed with a hard maximum or hold.</action>
  <decision>May the executor read and mutate the live skill registry, run the paid finance gate, and later ask for owner activation?</decision>
  <context>
The current source body contains the finance section and fixture 37, but no evidence proves that exact
fingerprint is active. Adding media first would combine two unevaluated instruction changes in one
candidate. The candidate stream must be cleared before this plan edits the body.

Run `$0` preflight first: mirror drift tests, contracts tests, and
`node packages/backend/scripts/run-eval-golden.mjs --self-check` (currently 36 fixtures). Then report
the source sha256, active live version/sha, and any parked candidates. Do not seed or spend yet.

The finance full gate historically costs about **$0.35**, but approval must name a hard maximum. Seed,
evidence recording and activation are live mutations; the gate is paid. No step is implied by approval
to edit files.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with a named maximum</name>
      <pros>Clears the already-merged finance body and gives media a clean candidate boundary.</pros>
      <cons>Consumes one full golden run before the later media candidate has its own run.</cons>
    </option>
    <option id="hold">
      <name>Hold the candidate stream</name>
      <pros>No live mutation or model spend occurs.</pros>
      <cons>20-12 and 20.1 stay blocked.</cons>
    </option>
  </options>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The owner explicitly approved a finance-gate maximum, or the plan stopped without live mutation/spend.</done>
  <resume-signal>Reply `proceed finance gate, max $X` or `hold`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: seed and evaluate the exact finance-bearing source body</name>
  <files>None — live skill-registry rows and eval evidence only</files>
  <action>
After Task 1 approval only:

1. Run the offline self-check again immediately before the live mutation.
2. Seed the current source body. Read it back and record actual candidate version, sha256, status and
   its predecessor active version. Stop if the candidate fingerprint differs from local source.
3. Run the complete gate pinned to that exact candidate. Fixture 37 must pass for
   `financeClaimCount: 1`; partial `--only` runs are diagnostic and cannot record evidence.
4. Stay within the owner’s hard maximum. Record run id, case count, retries, total cost and all pins.
5. Record eval evidence on the exact candidate. Do **not** activate in this task.
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The exact finance-bearing candidate has a complete green evidence row and remains unactivated pending Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: owner activates the evidenced finance candidate</name>
  <files>None — owner-gated live skill row only</files>
  <action>Present the complete evidence, wait for the exact-version activation signal, activate only that candidate, and read back the active version/sha.</action>
  <what-built>A finance-bearing candidate pinned to a complete golden run; no media prose has been added yet.</what-built>
  <how-to-verify>
Review the candidate version/sha, run id, complete case count, failures/retries and actual cost. If and
only if the gate is fully green, the owner authorizes activation. Activate that exact version, then
read back `getActiveSkill` and prove active version and sha match the evidence pin. If not approved,
leave it parked and stop; do not add media on top of an unactivated body.
  </how-to-verify>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The evidenced finance candidate is the live active fingerprint, or remains parked after an explicit rejection.</done>
  <resume-signal>Reply `activate finance candidate <actual-version>` or describe the rejection.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: add media guidance, its mirror, and a non-vacuous fixture</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, packages/backend/scripts/eval-cases/38-media-dispatch.json, packages/backend/scripts/run-eval-golden.mjs, docs/playbooks/cockpit.md, docs/playbooks/skill-registry.md</files>
  <action>
Begin only after Task 3 proves the finance fingerprint is active.

1. Add `## Creating images and video`, mirroring the register of `## Researching the outside world`:
   ordinary asks for a social reel/ad/explainer use `dispatchMedia`; the director returns a script,
   art direction and narrated block deck later; the proposal is free; clips, voice, captions and
   assembly take minutes and start only after a separate human Generate click; the agent cannot click
   it or claim generation happened. Relay budget/model/duration/narration refusals with their lever.
2. State the supported boundary: one finished reel assembled from fixed 5/10-second blocks with
   voiceover and captions. Do not promise music, sung tracks, re-cut user footage, or a length the
   budget refuses. Remove any surviving “assets, not a reel” wording.
3. Regenerate `cockpitAgent.ts` in the same commit and run the mirror drift tests.
4. Add `38-media-dispatch.json`. Its turn asks for a short launch/social reel in ordinary language
   without naming `dispatchMedia`. Add closed observable `mediaDispatchCount`, graded from the
   fixture plan’s `agentSteps` tool records (exactly one `dispatchMedia` call), plus anti-drift zeros
   for recipients/attachments and `statusAtMost: proposed`. Reply text is never evidence.
5. Add self-checks proving zero is rejected as vacuous, one real dispatch passes, absent or duplicate
   calls fail, and the fixture deletion floor rises 36 -> 37. Mutation-check the tool-count reader.
6. Update cockpit/skill-registry playbooks with the new section, fixture and two-cycle serialization.
7. Run contracts tests, runner self-check, `node scripts/check-playbooks.mjs`, and `git diff --check`.
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The offline media body/mirror/fixture contract is green and no live candidate has yet been seeded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: authorize the media candidate seed and paid gate</name>
  <files>None — approval and live registry/eval state only</files>
  <action>Present the offline body/fixture evidence and cost estimate, then wait for a proceed signal with a hard maximum before any seed or paid run.</action>
  <decision>May the executor publish the media candidate and run its complete golden gate?</decision>
  <context>
Show the body diff, mirror proof, fixture 38 self-check and current active finance version/sha first.
The full gate is expected to be roughly **$0.35** plus the new case; the owner must name a hard maximum.
If the fixture is falsified with a paid `--only 38` diagnostic first, include that diagnostic in the
same approved maximum. No fal media generation occurs: this gate may stage a free proposal and spend
LLM/specialist budget, but it never clicks Generate.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with a named maximum</name>
      <pros>Produces evidence for the exact media candidate.</pros>
      <cons>Consumes another paid golden run after the finance-cycle gate.</cons>
    </option>
    <option id="hold">
      <name>Hold the media candidate</name>
      <pros>The offline edit remains reviewable with no live mutation or spend.</pros>
      <cons>Phase 20 and 20.1 remain blocked.</cons>
    </option>
  </options>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The owner explicitly approved a media-gate maximum, or the plan stopped with the offline candidate unseeded.</done>
  <resume-signal>Reply `proceed media gate, max $X` or `hold`.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: seed, evaluate and evidence the media candidate without activating it</name>
  <files>None — live skill-registry rows and eval evidence only</files>
  <action>
After Task 5 approval, seed the edited body, read back actual candidate version/sha, run any approved
single-case falsification, then run the complete 37-fixture gate pinned to the exact candidate. The
complete run must include fixture 37 (finance remains intact) and fixture 38 (media dispatch). Record
the evidence on that candidate and stay under the approved maximum. Do not activate it.
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The exact media candidate carries complete green evidence and is parked for owner review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: owner activation and live no-spend conversational proof</name>
  <files>.planning/phases/20-media-canvas/20-12-SUMMARY.md</files>
  <action>Present the exact evidence pin, wait for exact-version activation approval, activate and read back that row, then perform the no-Generate conversational proof and record the supplied observations.</action>
  <what-built>An evidenced cockpit-agent candidate containing both the already-active finance guidance and the new media guidance.</what-built>
  <how-to-verify>
1. Review candidate version/sha, run id, 37-case result and actual cost; owner explicitly authorizes
   activation of that exact version.
2. Activate and read back the active row; version and sha must equal the evidence pin.
3. In a real cockpit turn ask for a short social reel without naming the tool. Confirm one media
   proposal/card arrives with script, art direction and narrated blocks, and the agent says generation
   needs a separate human click and time.
4. **Do not click Generate.** Confirm fal/media actual spend remains $0 for this conversational proof.
5. Create `20-12-SUMMARY.md` with both candidate cycles, actual versions/shas, run ids, case counts,
   costs, activation approvals and the live no-spend result. Mark Phase 20/MEDIA-01 complete only if
   20-11 is already evidenced and no Phase-20 validation row remains open.
  </how-to-verify>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated>
  </verify>
  <done>The media-bearing candidate is active, ordinary language stages a real proposal, fal media spend stays zero, and the summary records both gate cycles.</done>
  <resume-signal>Reply `activate media candidate <actual-version>` and then `approved` with the live no-spend observation, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- Finance and media are two distinct candidate/evidence/activation cycles; their versions and shas are recorded.
- `node packages/backend/scripts/run-eval-golden.mjs --self-check` reports at least 37 valid fixtures.
- The complete media run includes fixture 37 and fixture 38; a partial run never records gate evidence.
- Every paid run and activation is preceded by explicit owner approval and stays under its named maximum.
- Contracts tests, playbook check and `git diff --check` exit 0.
</verification>

<success_criteria>
- The live active body reliably reaches `dispatchMedia` for ordinary video language and describes the
  asynchronous, human-gated, paid workflow honestly.
- Finance guidance remains certified and active; media did not overwrite or silently bundle with an
  unevaluated finance candidate.
- The final live conversational proof stages a proposal and spends no fal media budget.
</success_criteria>

<output>
After both owner-approved cycles and the live no-spend proof, create
`.planning/phases/20-media-canvas/20-12-SUMMARY.md` with the exact evidence. Do not fabricate a live
version, sha, run id, cost or activation.
</output>
