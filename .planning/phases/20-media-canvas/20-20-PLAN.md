---
phase: 20-media-canvas
plan: 20
type: execute
wave: 14
depends_on: ["20-11"]
files_modified:
  - .planning/phases/20-media-canvas/20-20-PLAN.md
  - docs/playbooks/agent-runtime.md
  - docs/playbooks/skill-registry.md
  - .planning/phases/20-media-canvas/20-20-SUMMARY.md
autonomous: false
requirements: [MEDIA-01]
scope: "Zero-spend reconciliation of the already-cleared cloud-dev finance fingerprint. Production clearance and owner ratification remain separate; no seed, deploy, paid eval or registry mutation is authorized by this plan."

must_haves:
  truths:
    - "Cloud dev and production are named separately; evidence from one is never described as activation of the other."
    - "Cloud dev woozy-wren-368 has cockpit-agent v1 ACTIVE with the LF-normalized local source fingerprint and exact passing evidence from run 107ee875."
    - "The cloud-dev v1 row was bootstrapped active on an empty registry before the gate; it was not a candidate activated after an owner signal."
    - "seedSkills is idempotent against the newest identical body, so rerunning it cannot mint the candidate demanded by the stale plan."
    - "Production opulent-octopus-494 remains unseeded and unactivated, and no production claim follows from the cloud-dev evidence."
    - "Evaluation evidence and owner authority remain independent; the existing cloud-dev state needs explicit owner ratification before it may be used as 20-12's development predecessor."
  artifacts:
    - path: ".planning/phases/20-media-canvas/20-20-PLAN.md"
      provides: "Truthful deployment-scoped checkpoint and executable zero-spend next action"
    - path: "docs/playbooks/skill-registry.md"
      provides: "Live-read cloud-dev row/evidence, seed idempotence and production boundary"
    - path: "docs/playbooks/agent-runtime.md"
      provides: "Superseding runtime evidence for fixture 37 and the exact source fingerprint"
    - path: ".planning/phases/20-media-canvas/20-20-SUMMARY.md"
      provides: "Conditional closure record, created only after the owner ratifies an honestly satisfied deployment scope"
  key_links:
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "skills row cockpit-agent@1 on woozy-wren-368"
      via: "SHA-256 after the repository's required CRLF-to-LF normalization"
      pattern: "b765d7422d5e0d5d0d6beaa58b1310fbba02ced028a613cdc38aef01c3fec7e7"
    - from: "packages/backend/convex/skills.ts"
      to: ".planning/phases/20-media-canvas/20-20-PLAN.md"
      via: "seedSkills compares the current body with newest.body and inserts nothing when equal"
      pattern: "newest.body === body"
---

<objective>
Reconcile Phase 20's finance-bearing `cockpit-agent` lifecycle without spending or mutating a live
registry, then stop for the owner to choose the deployment scope that Phase 20-12 may rely on.

The old premise was false: the exact finance fingerprint is already active and fully evaluated in
cloud dev. It is not present in production, and cloud dev reached ACTIVE through empty-registry
bootstrap rather than a candidate activation. The plan therefore must not demand a candidate that
idempotent `seedSkills` cannot mint or silently turn cloud-dev proof into a production claim.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
@C:/Users/expert/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/STATE.md
@packages/contracts/skills/cockpit-agent.md
@packages/contracts/src/skills/cockpitAgent.ts
@packages/backend/scripts/eval-cases/37-finance-update.json
@packages/backend/scripts/run-eval-golden.mjs
@packages/backend/convex/skills.ts
@docs/playbooks/agent-runtime.md
@docs/playbooks/skill-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile the finance fingerprint at zero spend</name>
  <files>.planning/phases/20-media-canvas/20-20-PLAN.md, docs/playbooks/agent-runtime.md, docs/playbooks/skill-registry.md</files>
  <action>
Run the contracts body/mirror test and the golden runner's offline `--self-check`. Compute the local
fingerprint after LF normalization. Read, but do not mutate, the cloud-dev and production registries.
Record the exact deployment, version, status, hash and evidence refs. Inspect `seedSkills` and state
the consequence of its newest-body idempotence. Spend $0; do not seed, deploy, evaluate, attach new
evidence or activate anything.
  </action>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check &amp;&amp; pnpm --filter @pikar/contracts test -- src/skills/skillBodies.test.ts &amp;&amp; node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>
The playbooks distinguish cloud dev from production and supersede every claim that fixture 37 has
never run. The plan no longer asks `seedSkills` to mint an identical candidate.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: owner ratifies the deployment boundary</name>
  <files>None — decision only; no live mutation</files>
  <decision>May Phase 20-12 treat the evidenced cloud-dev v1 row as its certified development predecessor, or must finance clearance first be established in production?</decision>
  <context>
The exact local LF-normalized sha is active in cloud dev and has one complete 36/36 gate including
fixture 37. Production has no skill rows. The cloud-dev row was bootstrap-active before evaluation,
so ratification does not rewrite history as an owner-triggered candidate activation. A green run
alone grants no production authority.
  </context>
  <options>
    <option id="ratify-cloud-dev"><name>Ratify cloud dev</name><pros>Uses existing complete evidence at $0 and lets 20-12 start its separately governed media edit in cloud dev.</pros><cons>Certifies only the development predecessor; production remains unseeded and blocked behind its own authorization.</cons></option>
    <option id="require-production"><name>Require production first</name><pros>Keeps the predecessor and eventual runtime deployment identical.</pros><cons>20-12 stays blocked; a separate authorized production deploy/bootstrap plan must address fresh-registry auto-activation and evidence provenance.</cons></option>
    <option id="hold"><name>Hold</name><pros>Makes no authority decision and preserves $0/no mutation.</pros><cons>20-12 remains blocked.</cons></option>
  </options>
  <verify><automated>node packages/backend/scripts/run-eval-golden.mjs --self-check</automated></verify>
  <done>The owner names one scope. No deployment, seed, paid run, evidence write or activation occurs at this checkpoint.</done>
  <resume-signal>Reply `ratify cloud-dev finance predecessor`, `require production finance clearance`, or `hold`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: close only the ratified boundary</name>
  <files>docs/playbooks/agent-runtime.md, docs/playbooks/skill-registry.md, .planning/phases/20-media-canvas/20-20-SUMMARY.md</files>
  <action>
Only after `ratify-cloud-dev`, record the owner's decision verbatim and create the summary with an
explicit cloud-dev-only scope; do not claim candidate activation or production activation. If the
owner selects `require-production` or `hold`, do not create a summary: leave this plan open at the
precise production/authority checkpoint and prepare no live command implicitly.
  </action>
  <verify><automated>node scripts/check-playbooks.mjs &amp;&amp; git diff --check</automated></verify>
  <done>Either a cloud-dev-only summary records the explicit ratification, or the plan remains open without a misleading completion artifact.</done>
</task>

</tasks>

<verification>
- Local LF-normalized sha equals the cloud-dev active-row sha.
- Cloud-dev evidence pins `cockpit-agent: 1`, run `107ee875`, 36/36 and fixture 37's complete run.
- Production registry remains empty; no production activation is claimed.
- No body, mirror, fixture, runner or live registry row is changed.
- No paid command runs; reconciliation cost is $0.00.
- `git diff --check` passes. The repository-wide playbook check must pass once unrelated concurrent
  work no longer leaves watched files without their owning playbook update; do not edit a foreign
  playbook merely to hide that obstruction.
</verification>

<success_criteria>
- The stale unseeded/unevaluated/inactive premise is removed and replaced with deployment-scoped facts.
- The plan does not request an impossible duplicate candidate from idempotent `seedSkills`.
- A separately explicit owner decision controls whether cloud-dev evidence is sufficient for 20-12.
- Production remains unclaimed and requires its own authorization if selected.
- No summary exists until the selected scope honestly satisfies the revised boundary.
</success_criteria>

<output>
Stop at Task 2. Create `.planning/phases/20-media-canvas/20-20-SUMMARY.md` only after explicit
`ratify-cloud-dev` authorization. A production-first or hold decision leaves the plan open and
creates no summary. Do not mark MEDIA-01 complete; 20-12 remains.
</output>
