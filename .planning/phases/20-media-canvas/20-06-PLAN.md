---
phase: 20-media-canvas
plan: 06
type: execute
wave: 4
depends_on: ["20-05"]
files_modified:
  - packages/backend/convex/http.ts
  - packages/backend/convex/mediaComplete.ts
  - packages/backend/convex/media.test.ts
  - packages/backend/convex/llmRedaction.test.ts
  - docs/playbooks/media.md
  - docs/playbooks/cockpit.md
autonomous: true
requirements: [MEDIA-01]
lane_gate: "NOT gated on Phase 16."

must_haves:
  truths:
    - "A callback with a bad or absent HMAC segment gets 401 and writes NOTHING."
    - "Asset bytes land in _storage; the row holds a storage id and a content hash; no field anywhere holds a fal URL."
    - "A video that came back with no moderation field is recorded as `none_reported` — never as clear, passed or safe."
    - "Actual spend is reconciled against the estimate from the SAME price table, and only a positive delta is consumed."
    - "Every media audit payload is refs-only: ids, hashes, counts, a verdict enum — and no url/href/http substring anywhere."
    - "The callback is idempotent: a re-delivered webhook does not double-store or double-consume."
  artifacts:
    - path: "packages/backend/convex/http.ts"
      provides: "the POST /fal/callback/* route — HMAC path segment, fail-closed 401, ±300s timestamp"
      contains: "/fal/callback"
    - path: "packages/backend/convex/mediaComplete.ts"
      provides: "landResult — the terminal mutation: verdict derivation, delta reconciliation, refs-only audit"
      contains: "none_reported"
  key_links:
    - from: "packages/backend/convex/http.ts"
      to: "packages/backend/convex/mediaComplete.ts"
      via: "the httpAction stores the bytes then runs the terminal mutation"
      pattern: "internal\\.mediaComplete\\.landResult"
    - from: "packages/backend/convex/http.ts"
      to: "packages/backend/convex/gmailAuth.ts"
      via: "hmacHex re-derived from the path segment, compared to the digest"
      pattern: "hmacHex"
---

<objective>
The authenticated callback and the landing plane: prove the caller knows a secret we minted for this
job, download the asset bytes inside the webhook, store them under the tenant, and record an
honest moderation verdict and the real cost.

Purpose: two things at once. An unguarded `POST /fal/callback` is a write endpoint that flips job
status, stores attacker-supplied bytes as a tenant's asset and drives spend reconciliation. And a
signed fal URL persisted anywhere is **both a content leak and a live credential** — downloading the
bytes here is what makes CLAUDE.md §4 structural instead of a promise: there is nowhere in the schema
for a fal URL to live.

Output: the route, `mediaComplete.landResult`, and the static scans that pin the audit surface.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-CONTEXT.md
@.planning/phases/20-media-canvas/20-RESEARCH.md
@.planning/phases/20-media-canvas/20-05-SUMMARY.md
@packages/backend/convex/http.ts
@packages/backend/convex/gmailAuth.ts
@packages/backend/convex/calendarComplete.ts
@packages/backend/convex/llmRedaction.test.ts
@packages/contracts/src/audit.ts
@docs/playbooks/media.md

<interfaces>
<!-- The three existing http.ts route patterns, read from source. /skillopt/* is the closest analogue:
     an unauthenticated-by-default WRITE endpoint guarded by a shared secret with a fail-closed 401. -->

`http.ts:84-97` — `/skillopt/export`:
```ts
const expected = process.env.SKILLOPT_TOKEN;
const auth = req.headers.get("Authorization");
// Fail-closed: reject when the token is unset OR the header is missing/mismatched.
if (!expected || auth !== `Bearer ${expected}`) return new Response("unauthorized", { status: 401 });
```
`http.ts:123-129` — the SECURITY rule on `/skillopt/writeback`: **the tenant comes from trusted
server config, never from the request body.** Copy that rule verbatim for the callback.

`gmailAuth.ts:65-72` — `verifyState`: `lastIndexOf(".")`, split, re-derive `hmacHex`, compare.

<!-- fal callback payload, research §1.2 (MEDIUM-HIGH). -->
```json
{ "request_id": "<uuid>", "gateway_request_id": "<uuid>",
  "status": "OK" | "ERROR", "payload": { }, "error": "<string, ERROR only>" }
```
video success payload → `{ video: { url, content_type, file_name, file_size } }`
image success payload → `{ images: [{ url, content_type, file_name, file_size, width, height }],
                           seed, has_nsfw_concepts: [bool] }`
Signature headers fal also sends (NOT used here — see the ponytail ceiling):
`X-Fal-Webhook-Signature`, `x-fal-webhook-request-id`, `x-fal-webhook-user-id`,
`x-fal-webhook-timestamp`. Timestamp tolerance: **300 seconds**.

<!-- The four-value verdict, research §3. DO NOT INVENT A VERDICT. -->
| Value | Set when | Means |
|---|---|---|
| `provider_blocked` | 422 `content_policy_violation` at submit | the INPUT was refused; no asset exists |
| `checker_flagged` | response carried `has_nsfw_concepts` and it was `true` | the provider's checker flagged this output |
| `checker_clear` | response carried `has_nsfw_concepts` and it was `false` | the provider's checker passed this output |
| `none_reported` | an asset came back and the response carried **no** moderation field | **the provider reported nothing. This is NOT "clean."** |

**Every Wan 2.5 video lands as `none_reported`** — the model publishes no per-output moderation
field. Labelling it passed/clear/safe would be a compliance claim the provider never made.

`llm.ts:947-949` / `smoke.ts:239` — `ctx.storage.store(new Blob([bytes], { type }))` → `Id<"_storage">`.
`lib/hash.ts` — `contentHash(bytes)`.
`calendarComplete.ts` — the rule this file copies: a terminal lives in a sibling module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `POST /fal/callback/*` — fail-closed 401, and nothing security-relevant from the body</name>
  <files>packages/backend/convex/http.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - Absent segment, malformed segment, wrong digest, or unset `FAL_WEBHOOK_SECRET` → **401**, and
      the target row is byte-unchanged (assert the row, not just the status code).
    - A path id that does not `normalizeId` to a `mediaJobs` row → 401/404, zero writes.
    - `x-fal-webhook-timestamp` outside ±300 s → rejected.
    - A correct segment for a job already at a terminal status → 200, **no second store and no second
      spend consumption** (idempotent; fal delivery is assumed at-least-once).
    - A correct segment → the bytes are fetched, stored, and `landResult` runs for exactly that row.
  </behavior>
  <action>
```ts
http.route({ path: "/fal/callback/*", method: "POST", handler: httpAction(async (ctx, req) => { … }) });
```
The handler, in order:
1. Take the last path segment; `lastIndexOf(".")`; left half = the raw job id, right half = the
   digest. **`gmailAuth.verifyState`'s shape verbatim.**
2. `const secret = process.env.FAL_WEBHOOK_SECRET; if (!secret) return new Response("unauthorized", { status: 401 });`
   — **fail closed when the env is unset**, the `/skillopt/*` rule.
3. `const jobId = ctx.db`-free: resolve via `ctx.runQuery(internal.mediaComplete.resolveJob, { raw, digest })`,
   which does `normalizeId("mediaJobs", raw)` (null → refuse: a malformed or foreign-table id must
   never reach `ctx.db.get`), re-derives `await hmacHex(raw, secret)` and compares. Return 401 on any
   mismatch. **Everything security-relevant — `tenantId`, `planId`, `model`, `estCents`, `spec` — is
   read from the ROW, never from the payload.** The only things taken from the body are the asset URL
   to fetch and the moderation field, and both are validated.
4. Timestamp guard: `x-fal-webhook-timestamp` within ±300 s, else 401. Three lines, and it kills
   replay of a captured URL+body. Take it.
5. Terminal-status short-circuit: if the row is already `succeeded`/`failed`/`blocked`, return 200
   and do nothing. Idempotency, because fal's retry policy is undocumented.
6. On `status: "ERROR"`: `landResult` with a failure CODE derived from the body — never fal's
   `error` string verbatim (§4, the `reasonCode` idiom).
7. On `status: "OK"`: read the asset URL out of the payload (`payload.video.url` or
   `payload.images[0].url`), `fetch` it, `arrayBuffer()`, `ctx.storage.store(new Blob([bytes], { type }))`.
   **Then discard the URL.** It is not passed to `landResult`, not logged, not stored. `httpAction`
   has `ctx.storage`, so no extra hop is needed.
8. `ctx.runMutation(internal.mediaComplete.landResult, { jobId, storageId, bytes: byteLength, mimeType, moderation, actualSpecFromPayload })`.

**The `ponytail:` ceiling comment at the route, verbatim intent:**
> *"HMAC path segment, not Ed25519/JWKS. The segment proves the caller knows a secret we minted for
> THIS job; it does not prove fal sent it. Upgrade path when that matters: verify
> `X-Fal-Webhook-Signature` (Ed25519 over `request_id\nuser_id\ntimestamp\nsha256(body)`) against
> fal's JWKS at `https://rest.fal.ai/.well-known/jwks.json`, cached ≤24 h. FIRST confirm the Convex
> default runtime's `crypto.subtle` supports Ed25519 — **UNVERIFIED, research Open Question 3,
> deliberately deferred out of this phase** — and note that a file holding an `http.route` cannot be
> `"use node"`, so `node:crypto` is only reachable via an extra `runAction` hop."*

Test it with `convex-test`'s http harness plus the `FAL_FIXTURE` seam: synthesize the three callback
bodies (image OK with `has_nsfw_concepts`, video OK with no moderation field, and the ERROR case),
sign the segment correctly, and POST to the real route. **$0.**
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>Every rejection path returns 401 with a byte-unchanged row; the happy path stores bytes and runs the terminal once; a replayed callback changes nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `mediaComplete.landResult` — honest verdict, delta reconciliation, refs-only audit</name>
  <files>packages/backend/convex/mediaComplete.ts, packages/backend/convex/media.test.ts</files>
  <behavior>
    - A video response with no moderation field → `verdict: "none_reported"`. **Never** `checker_clear`.
    - An image response with `has_nsfw_concepts: [false]` → `checker_clear`; `[true]` → `checker_flagged`.
    - The row gains `assetStorageId`, `assetHash`, `mimeType`, `bytes`, `status: "succeeded"`.
    - `actualCents` is re-priced from the SAME `@pikar/cost/media` table using what fal actually
      produced (the payload's duration / dimensions), not from the estimate.
    - `actual > est` → exactly `actual - est` is consumed on BOTH media windows with `reserve: true`.
    - `actual <= est` → **nothing is consumed and nothing is refunded.**
    - Exactly one audit row per landing, whose payload keys are exactly the allow-list.
  </behavior>
  <action>
New module `packages/backend/convex/mediaComplete.ts` — a sibling terminal, the `calendarComplete.ts`
rule. Holds `resolveJob` (internalQuery, used by the route) and `landResult` (internalMutation).

**Verdict derivation — the honest four (research §3):** derive ONLY from what the provider actually
said. Comment it hard:
> *"`none_reported` means the provider reported NOTHING. It is not 'clean'. Every Wan 2.5 video lands
> here — the model publishes no per-output moderation field. Rendering it as a pass would be a
> compliance claim fal never made. Do not add a fifth value that means 'probably fine'."*

**Delta reconciliation:** re-price from the same table using the payload's actual duration /
dimensions, then
`if (actual > est) await rateLimiter.limit(ctx, "mediaSpendCents", { key: tenantId, count: actual - est, reserve: true })`
and the same for `deploymentMediaSpendCents`. **No refund on an over-reservation** — plan 20-04's
`ponytail:` rule; repeat the pointer here rather than the argument.

**Audit (CLAUDE.md §4), and this is the allow-list task 3 pins:** exactly
`{ jobId, batchId, planId, falRequestId, model, resolution, promptHash, assetHash, verdict,
estCents, actualCents, shotCount }` — ids, hashes, counts and one enum. **No URL. No prompt text. No
filename.** `packages/contracts/src/audit.ts` permits any string in its flat map, so the TYPE is not
the guard — the guard is task 3's static scan.

If the payload's actual spec disagrees with the submitted spec (e.g. fal returned a different
resolution), record the ACTUAL in `actualCents` and add the `resolution` ref to the audit payload so
the drift is visible in the log plane. Do not silently trust the estimate.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- media</automated>
  </verify>
  <done>All four verdict values are reachable and `none_reported` is what a moderation-free video yields; the delta consumption is asserted in both directions; exactly one audit row lands per job.</done>
</task>

<task type="auto">
  <name>Task 3: the static scans — the §4 guard that the type system cannot give us</name>
  <files>packages/backend/convex/llmRedaction.test.ts, docs/playbooks/media.md, docs/playbooks/cockpit.md</files>
  <action>
`llmRedaction.test.ts` already holds 45 scans of exactly this shape (e.g. `:110` *"gmail.ts
mailbox.searched audit payload is refs-only"*, `:876` *"voiceDoc.ts: EVERY payload object is free of
report content"*). Add:

1. **Media audit payloads are refs-only.** Every audit payload literal in `media.ts` /
   `mediaComplete.ts` uses only keys from the task-2 allow-list. Any other key fails.
2. **No `url` / `href` / `http` substring** appears in any media audit payload literal. A signed fal
   URL in an audit row is both a content leak and a live credential.
3. **No prompt text ever reaches a log/audit/DLQ write** from `media.ts` or `mediaComplete.ts` —
   `promptHash` is the only representation. Scan for the `prompt` identifier inside payload objects.
4. **The media audit-site count is PINNED to an exact number** (the `voiceDoc.ts:915` idiom), so a
   later plan cannot quietly add a fifth write.
5. **Who may write a terminal status.** Pin the set of modules that write
   `succeeded`/`failed`/`blocked` to exactly `{media.ts, mediaComplete.ts}` and assert no other module
   does.
   **DELIBERATE CORRECTION to research's SC2 line, and record it in the summary:** research says *"the
   webhook is the ONLY writer of succeeded/failed/blocked"*. That is not achievable and not desirable —
   a **422 `content_policy_violation` is SYNCHRONOUS at submit and produces no webhook at all**, so
   `media.ts` must be able to write `blocked`/`failed`. The honest pin is the two-module set, with
   `succeeded` reachable ONLY from `mediaComplete.ts`. Assert both halves.
6. **Nothing calls `storage.getUrl` outside a `tenantQuery`** — the pattern that would leak a bearer
   capability into a log or a non-guarded return.

All scans strip comments before matching against CODE (the 15.2-07 lesson: a scan that matches inside
a comment passes on the wrong evidence, and these files deliberately spell out what is forbidden).

Then bump `docs/playbooks/media.md` (the webhook auth mechanism + its ceiling, the four-value verdict
and what `none_reported` does and does not mean, the audit allow-list, the delta-reconciliation and
no-refund rules) and `docs/playbooks/cockpit.md` (`http.ts` is a watched path — one line naming the
new route and its guard).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- llmRedaction</automated>
  </verify>
  <done>All six scans pass; the audit-site count is pinned to a literal; both playbooks bumped; `node scripts/check-playbooks.mjs` exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- media` and `… -- llmRedaction` green; `pnpm test` full suite green.
- `pnpm typecheck` backend delta exactly 0 against the 150 baseline.
- `node scripts/check-playbooks.mjs` exits 0.
- **Mutation check:** add `url: falUrl` to a media audit payload — scan 2 must go RED. Remove.
- **Mutation check:** make the no-moderation-field video yield `checker_clear` — the verdict test must go RED. Restore.
- **Mutation check:** drop the `!secret` guard — the unset-env 401 test must go RED. Restore.
- **Cost: $0.** Every callback body in these tests is synthesized locally.
- Shared tree: `ls .git/MERGE_HEAD`; pathspec commits only.
</verification>

<success_criteria>
- SC4 is closed offline: bytes in `_storage`, a hash and a storage id on the row, no URL anywhere, an honest verdict, and a refs-only audit proven by scan.
- SC1's webhook-auth half is closed.
- Only the live-egress question remains, and it is plan 20-11's owner-run gate.
</success_criteria>

<output>
After completion, create `.planning/phases/20-media-canvas/20-06-SUMMARY.md`. Record the corrected
terminal-writer pin (two modules, not one) and its reason, and the exact audit allow-list as shipped.
</output>
