---
phase: 20-media-canvas
plan: 11
type: execute
wave: 8
depends_on: ["20-10"]
files_modified:
  - docs/decisions/012-media-route-and-corrected-fal-pricing.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-07-16-port-koda-stack-content-prompts-into-skills-registry.md
  - docs/playbooks/media.md
  - docs/playbooks/cockpit.md
  - docs/playbooks/growth-diagnostic.md
  - docs/playbooks/skill-registry.md
autonomous: false
requirements: [MEDIA-01]
lane_gate: "NOT gated on Phase 16. This plan closes the phase for everything except the conversational entry point, which is plan 20-12."

must_haves:
  truths:
    - "The corrected fal arithmetic is recorded in an ADR, without editing ADR-011 (ADRs are immutable after acceptance)."
    - "No requirement or roadmap line still states a premise this phase refuted."
    - "All four affected playbooks carry a real, dated verification of the diff that made them stale."
    - "fal's live queue has ACCEPTED our submit body, our webhook URL was reachable from fal's egress, and the billed amount was compared against the price table."
    - "That comparison is recorded as D5's first reconciliation run."
  artifacts:
    - path: "docs/decisions/012-media-route-and-corrected-fal-pricing.md"
      provides: "ADR-012 — the dispatchable route whose product costs money but whose specialist cannot spend, plus the corrected ADR-011 arithmetic"
      contains: "supersedes"
    - path: "docs/playbooks/media.md"
      provides: "the completed playbook incl. the executed reconciliation observation with real numbers"
      contains: "Reconciliation"
  key_links:
    - from: "docs/decisions/012-media-route-and-corrected-fal-pricing.md"
      to: "docs/decisions/011-media-provider-fal-wan25.md"
      via: "an amending ADR, because 011 is Accepted and CLAUDE.md §9 forbids editing it"
      pattern: "ADR-011"
---

<objective>
Close the phase honestly: record the decisions, correct every line that still states a refuted
premise, discharge all four playbook obligations, and run the one thing offline testing cannot prove.

Purpose: a phase that ships against a requirement stating a refuted premise cannot be marked
Complete. `REQUIREMENTS.md:139` still says *"video (≤3 min) via the connected Pikar-Ai service"* —
both halves refuted by the spike. And `ROADMAP.md`'s Phase 20 block plus SC #1 still carry ADR-011's
"≤15 s" and "~$0.05/s", which are wrong for the model actually chosen.

Output: ADR-012, three corrected documents, four bumped playbooks, and one owner-run $0.26 live gate.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-CONTEXT.md
@.planning/phases/20-media-canvas/20-RESEARCH.md
@.planning/phases/20-media-canvas/20-VALIDATION.md
@.planning/phases/20-media-canvas/20-01-SUMMARY.md
@.planning/phases/20-media-canvas/20-05-SUMMARY.md
@docs/decisions/011-media-provider-fal-wan25.md
@docs/decisions/010-specialist-routes-superset.md
@docs/decisions/007-toolset-is-a-code-owned-capability-grant.md
@docs/playbooks/media.md

**CRLF WARNING:** `.planning/*.md` and `docs/playbooks/*.md` are CRLF files. Multi-line `\n` edit
anchors silently no-op on them. Normalize and assert the anchor is unique before writing, and
`git diff` every one of these files after editing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-012 — the route, and the corrected arithmetic</name>
  <files>docs/decisions/012-media-route-and-corrected-fal-pricing.md</files>
  <action>
D1 requires a new ADR for widening `SPECIALIST_ROUTES`. Research §11.4 verified what it must and must
NOT contain — write the part that is genuinely owed, and cite rather than re-litigate the rest.

**Do NOT re-decide:**
- **ADR-010 already decided the superset question** — *"`SPECIALIST_ROUTES` is the set of routes the
  SYSTEM can dispatch; the routes `diagnose()` emits are a strict SUBSET of it."* Adding `media` as a
  non-`diagnose()` route is the SECOND instance of a decided pattern. Cite ADR-010 and point at the
  companion assertion (plan 20-08's `diagnose()` emits no `"media"`).
- **ADR-007** already establishes that a tool-set is a code-owned capability grant. Cite it.

**What ADR-012 actually decides — a dispatchable route whose downstream product costs real money,
where the specialist is structurally incapable of spending it:**
1. The D2 three-stage split: propose → canvas → human-initiated paid generation.
2. *"Plan-gated by construction"* is **STRUCTURAL**, not procedural: there is no code path from a
   dispatched specialist to a fal POST. Name the two paid entry points (the post-`approved`
   `EXTERNAL_TARGETS.media` arm and the canvas mutations) and the test that proves the absence.
3. `SPECIALIST_TOOLS` is reused **verbatim** and a generate tool is **deliberately refused** — the
   `evaluateBusiness` precedent at `specialists.ts:50-56`.
4. The batch, not the shot, is the priced and reserved unit, and why a per-shot cap bounds nothing.
5. A second budget rail with its own kill switch and a keyless deployment ceiling
   (`deploymentMediaSpendCents`, research Open Question 2 → **added**), mirroring 22.1-02's argument.

**And the ADR-011 CORRECTION, stated plainly — this is the second reason the ADR exists.**
ADR-011 is **Accepted**, and CLAUDE.md §9 says ADRs are never edited after acceptance. So the wrong
row is corrected HERE, with a `supersedes`/`amends` note at the top of ADR-012 and a one-line pointer
added to nothing in ADR-011 (leave that file byte-unchanged).

| ADR-011 claim | Reality (re-confirmed at plan 20-01's preflight — use THOSE numbers) |
|---|---|
| "Wan 2.5 ~$0.05/s" | True **only at 480p**. 720p is 2×, 1080p is 3×, and **1080p is the endpoint's DEFAULT**. |
| "a 15 s Wan 2.5 clip is ~$0.75 → passes with headroom" | **Wan 2.5 cannot generate 15 s.** `duration` ∈ {5, 10}. At the default 1080p a 10 s clip is $1.50 and is REFUSED by the locked $1.00 cap. |
| "Veo 3 (~$6.00), Seedance 1080p (~$10.23) fail closed" | Unchanged, still the right conclusion. |
| "≤15 s is a MODEL ceiling across 28 models" | Still true as a class statement; Wan 2.5's own ceiling is tighter (10 s). The UI must never offer 15 s for Wan 2.5. |

State the consequence that makes this worth an ADR rather than a footnote: **an estimate built on
"~$0.05/s" under-reports a default-resolution 10 s clip by 3×**, and the containment is that the
price table is keyed by `(model, resolution)` and the submit body is built from the same spec object
the estimate consumed.

Also record as **deferred, deliberately**: Wan 2.5's native audio (pinned OFF on every submit, not
requested, separate moderation and rights questions); Ed25519 webhook signature verification
(Convex default-runtime `crypto.subtle` support UNVERIFIED — research Open Question 3); koda
`/assemble` and multi-clip concatenation; a premium model default.

Status: Accepted, dated, owner decision.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>ADR-012 exists, cites ADR-007 and ADR-010 rather than re-deciding them, records the D2 split and the corrected arithmetic with the LIVE numbers from plan 20-01, and lists the four deferrals. `docs/decisions/011-*.md` is byte-unchanged (`git diff` proves it).</done>
</task>

<task type="auto">
  <name>Task 2: the corrections and the four playbooks</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/todos/pending/2026-07-16-port-koda-stack-content-prompts-into-skills-registry.md, docs/playbooks/media.md, docs/playbooks/cockpit.md, docs/playbooks/growth-diagnostic.md, docs/playbooks/skill-registry.md</files>
  <action>
The 18-09 precedent: one plan owns the contradictions.

1. **`REQUIREMENTS.md` MEDIA-01** currently reads *"produces images and video (≤3 min) via the
   connected Pikar-Ai service as async governed jobs with a separate cost cap"*. Both premises were
   refuted by the spike and re-scoped in the ROADMAP on 2026-08-01. Rewrite to match what shipped:
   images and **short-form video (5 or 10 s on the default model)** via a **server-to-server provider
   API (fal.ai) behind a deployment secret**, as async governed jobs with a separate capped budget.
   **Leave MEDIA-01 itself Pending** until plan 20-12 lands and the phase is genuinely complete —
   `gsd-tools requirements mark-complete` flipping it early is the 17.1-01 trap.
2. **`ROADMAP.md` Phase 20:** correct the RE-SCOPED block's *"Video is <=15 SECONDS"* to name the
   chosen model's real ceiling (Wan 2.5: 5 or 10 s; ≤15 s remains true only as a class statement
   about the 28-model survey — keep both sentences, do not delete the survey finding). Correct SC #1's
   *"Wan 2.5 (~$0.05/s)"* to the resolution-tiered rate and note it is $0.05/s **at 480p**. Add a
   pointer to ADR-012.
   **After editing, `git diff .planning/ROADMAP.md` and confirm every changed line belongs to Phase
   20** — `roadmap update-plan-progress` has been observed writing one phase's counts over another's.
3. **The koda todo** (`.planning/todos/pending/2026-07-16-…`): it explicitly SKIPPED the media stages
   as "heavy". D6 reversed that for the media stages ONLY. Update it — `/art-direction`,
   `/storyboard` and `/generate` are DONE (ported into `media-director`, plan 20-03); `/assemble`,
   `/trends`, `/brief`, `/concept`, `/script`, `/publish`, `/repurpose` remain pending for a later
   content phase. Leaving it contradictory is what D6 forbids.
4. **All four playbooks** — `media.md`, `cockpit.md`, `growth-diagnostic.md`, `skill-registry.md` —
   get a final `Last verified` bump with a one-line statement of WHAT was verified. **Do not bump a
   playbook whose diff you did not read** (shared-tree discipline); every one of these four was
   touched by this phase's own plans, so the claim is honest here.
   `media.md` additionally gets: the ADR-012 pointer, the completed dependency section (the
   `-preview` endpoint risk, Replicate as the ADR-011 fallback, the two deployment secrets), and the
   **reconciliation slot for task 3's numbers**.
5. Refresh the knowledge graph: `graphify update .` then `node scripts/extract-convex-edges.mjs`
   (the graph fixup — a rebuild drops the injected Convex cross-module edges and the per-table nodes).
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>No line in `REQUIREMENTS.md`, `ROADMAP.md` or the koda todo states a refuted premise; all four playbooks bumped with real content; `git diff` on each `.planning` file shows only Phase-20 lines changed; the graph is refreshed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: THE LIVE GATE — owner-run, budgeted ≈$0.26, and it doubles as D5's first reconciliation run</name>
  <what-built>
Everything above this line is proven offline at $0: the price table, the batch reservation and its
concurrency guarantee, the submit body, the webhook auth, the byte download, the verdict derivation,
the audit shape, tenant isolation, the dispatch route, and the canvas build.

**Three things offline testing structurally cannot prove**, and they are exactly the ones that fail
in production: that fal's live queue ACCEPTS our submit body, that our webhook URL is reachable from
fal's egress, and that the amount actually billed matches the price table. This is the 15.2 / 17.1-10
owner-run live-gate precedent.
  </what-built>
  <how-to-verify>
**Preflight**
1. Set the two deployment secrets (deployment env, **NOT `.env.local`** — the Phase-2 lesson):
   `npx convex env set FAL_KEY <key>` and `npx convex env set FAL_WEBHOOK_SECRET <a fresh random secret>`.
   Run these from `packages/backend`.
2. Confirm `CONVEX_SITE_URL` is the deployment's public site origin and that
   `POST {CONVEX_SITE_URL}/fal/callback/bogus.bogus` returns **401** from outside the app. If it
   returns anything else, STOP — the guard is not live.
3. Record fal's dashboard balance/credit figure and the timestamp. This is the "before" number.

**The run — budget ≈ $0.26 total**
4. Dispatch a media request in the workspace (or, until plan 20-12 lands, invoke the `dispatchMedia`
   tool through the offline SMOKE driver / directly call `api.media.generateBatch`). Confirm the
   canvas spins up with an art direction and a shot deck, and that **nothing has been spent yet**.
5. Confirm the canvas shows the deck total, the model+resolution, and the remaining media budget
   **before** the Generate button is clickable.
6. Reduce the deck to exactly two shots: **one 480p × 5 s Wan 2.5 clip ($0.25)** and **one FLUX
   schnell image ($0.009)**. Click Generate.
7. Observe, in order: both tiles show a status **before any asset exists** (SC2 — no synchronous
   hang); the image lands within seconds; the clip lands in roughly 1–3 minutes.
8. Exercise all four editor affordances: edit a shot's prompt, regenerate that one shot (confirm it
   shows its own estimate FIRST), reorder, delete.

**The evidence to record — this is the deliverable, not the vibe**
9. `sum(mediaJobs.actualCents)` for the run, per job, with each job's `model`, `spec`, `estCents`,
   `actualCents` and `verdict`.
10. **Confirm the video job's verdict is `none_reported`** and that the canvas renders it as
    "Not checked", not as a pass.
11. Confirm **no `mediaJobs` row and no audit payload contains a fal URL** — read the rows.
12. fal's dashboard balance AFTER, and the delta. **Compare the delta to
    `sum(mediaJobs.actualCents)`.** Any difference is price-table drift — record the number even if
    it is zero, because "we compared and it matched" is the finding.
13. Write 9–12 into `docs/playbooks/media.md`'s `## Reconciliation` section with the date, and state
    in that section that **this observation IS D5's first reconciliation run**.

**Stop and report instead of proceeding if:** fal rejects the submit body (a field name or an allowed
value has moved — that is a plan 20-05 fix, not a gate failure to work around); the webhook never
arrives (egress/URL problem — check `CONVEX_SITE_URL` and the 401 preflight); or the billed delta
differs from the table by more than a rounding cent (price-table drift — correct the table and
re-record, do not paper over it).
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded numbers pasted, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` full suite green; `pnpm typecheck` backend delta exactly 0 against the 150 baseline.
- `node scripts/check-playbooks.mjs` exits 0.
- `git diff docs/decisions/011-media-provider-fal-wan25.md` is EMPTY — ADR-011 is immutable.
- The live gate's numbers are in `media.md`, dated.
- **Total live cost for this plan: ≈$0.26.** No other plan in this phase spends anything.
- Shared tree: `ls .git/MERGE_HEAD`; pathspec commits only.
</verification>

<success_criteria>
- SC1 is fully closed (live queue accepted, webhook reachable, rates re-confirmed and recorded).
- SC5 is fully closed (the ceiling is named in code AND the reconciliation procedure has been RUN once with real numbers).
- No document in the repo still states a premise this phase refuted.
</success_criteria>

<output>
After completion, create `.planning/phases/20-media-canvas/20-11-SUMMARY.md` with the live-gate
numbers recorded verbatim (the fal delta, `sum(mediaJobs.actualCents)`, and the difference), and
fill `20-VALIDATION.md`'s two Manual-Only rows with their observed results.

**Do NOT mark Phase 20 complete or flip MEDIA-01 here** — plan 20-12 is still outstanding.
</output>
