---
phase: 20-media-canvas
plan: 11
type: execute
wave: 13
depends_on: ["20-10", "20-17"]
files_modified:
  - docs/playbooks/media.md
  - .planning/phases/20-media-canvas/20-11-SUMMARY.md
  - .planning/phases/20-media-canvas/20-VALIDATION.md
autonomous: false
requirements: [MEDIA-01]
resume: "REFRESHED 2026-08-10. Tasks 1-3 landed in 1db8a03. Resume at the owner-approved Run-A-only live gate; do not replay the ADR/document sweep."

must_haves:
  truths:
    - "Commit 1db8a03 remains the evidence for ADR-012, ADR-013, the corrected requirements/roadmap/todo text, and the four playbook updates."
    - "The only paid run uses the shipped governed application path: proposal -> canvas -> human Generate click -> fal queue/webhook -> render worker -> retained final reel."
    - "The run is one 5 s Wan 2.5 480p clip plus one FLUX schnell image, with voice, captions and one render; owner approval names a hard maximum before any live/deploy mutation or spend."
    - "LongCat Runs B/C and the 30-second/fps-divisor probe do not run: the repository cannot price, submit or storyboard them through the governed path."
    - "Observed cost, reservation, webhook, render, retention and refs-only evidence is recorded verbatim rather than inferred from modelled prices."
  artifacts:
    - path: "docs/playbooks/media.md"
      provides: "Dated Run A reconciliation with observed numbers and residual LongCat deferral"
      contains: "Reconciliation"
    - path: ".planning/phases/20-media-canvas/20-11-SUMMARY.md"
      provides: "Closure evidence for the landed documentation work and the single owner-run live gate"
    - path: ".planning/phases/20-media-canvas/20-VALIDATION.md"
      provides: "Observed results for every manual-only Run A row"
  key_links:
    - from: "docs/playbooks/media.md"
      to: ".planning/phases/20-media-canvas/20-VALIDATION.md"
      via: "the same run id, timestamps and observed cents appear in both"
      pattern: "Run A"
---

<objective>
Close the executable remainder of 20-11 without replaying landed work or inventing support that the
repository does not have.

Tasks 1-3 are complete in commit `1db8a03`. This resume plan owns only the smallest live proof the
shipped system can execute honestly: one governed Run A through the real canvas, queue/webhook and
Vercel Sandbox renderer, followed by evidence capture. No direct provider call is acceptable because
it bypasses the price table, reservation, audit and human gate this phase exists to prove.
</objective>

<execution_context>
@C:/Users/expert/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-media-canvas/20-VALIDATION.md
@docs/decisions/012-media-route-and-the-reel.md
@docs/decisions/013-the-render-worker.md
@docs/playbooks/media.md
@packages/cost/src/media.ts
@packages/core/src/storyboard.ts

<completed_work>
- `1db8a03` completed original Tasks 1-3: both ADRs, the contradiction cleanup, the koda deferral
  reversal and the four playbooks. Do not edit or recommit those artifacts merely to replay the plan.
- The media implementation supports Wan 2.5 and FLUX through the governed adapter/price-table path.
- `MEDIA_VIDEO_PRICING` has no LongCat row, the adapter has no LongCat submit arm, and the storyboard
  contract accepts only 5 or 10 seconds. Therefore the old Runs B/C are not executable verification.
</completed_work>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: owner-approved governed Run A only</name>
  <files>None — deployed configuration, live rows and owner evidence only</files>
  <action>Present the exact supported shape and hard maximum, wait for the resume signal, then execute the approved steps in &lt;how-to-verify&gt;. Stop on any listed production mismatch.</action>
  <what-built>
The offline suite proves pricing, whole-job reservation, cents-flooring, webhook authentication,
sidecar validation, tenant isolation and retention logic. It cannot prove that the deployed secrets,
public callback, fal queue and real Vercel Sandbox work together.
  </what-built>
  <how-to-verify>
**Approval boundary.** Do not set/change deployment secrets, bake a snapshot, deploy, submit to fal,
or click Generate until the owner explicitly approves this exact shape and a hard maximum. Expected
external cost is about **$0.29**; default hard maximum is **$0.35** unless the owner names a lower one.

**Supported shape — exactly one run:**
1. One Wan 2.5 clip, `480p`, `5 s` (`$0.25` modelled).
2. One FLUX schnell image at the shipped default size (`~$0.009` modelled).
3. Narration for those two blocks, captions STT and one sandbox render (`~$0.03` modelled together).
4. No regeneration and no second Generate click. Stop before the approved maximum can be exceeded.

**After approval:**
1. Confirm the deployment (not `.env.local`) has `FAL_KEY`, `FAL_WEBHOOK_SECRET`,
   `MEDIA_RENDER_SECRET`, and `MEDIA_RENDER_URL`; confirm Vercel has `MEDIA_RENDER_SECRET` and a
   working `MEDIA_SANDBOX_SNAPSHOT_ID`. Any write/change here is itself owner/deployer-authorized.
2. From outside the app, unauthenticated POSTs to the fal callback and media-render route must both
   return 401. Stop on any other result.
3. Record fal balance/credits and UTC timestamp before the run.
4. In the real cockpit request a short reel. Use the registered `dispatchMedia` conversational path
   (name the tool in the user message only if model selection needs the hint), wait for the real plan
   card, then edit it to the exact supported shape above. Do not invoke fal directly.
5. Before the click, record the itemized estimate, model/resolution, whole-job reserved cents and
   remaining tenant/deployment media budget. Confirm no provider job exists yet.
6. The owner clicks Generate once. Observe clip and voice pending states, webhook landing, automatic
   assembly, final mp4 publication, captions and audible narration. Record timestamps and render
   wall-clock. Stop on rejected submit, missing webhook, timeout or invalid sidecar; do not work
   around a production mismatch inside the gate.
7. Read the resulting rows/audit/dead-letter surfaces. Record per-job `kind`, model/spec, estimate,
   actual cents and verdict; confirm missing provider moderation is `none_reported`; confirm no URL,
   filename, prompt, narration or ffmpeg output appears in refs-only log planes.
8. Confirm intermediates were deleted/null after publication while final mp4 and sidecar remain.
9. Record fal balance/credits after the run and compare its delta with summed actual cents. Any
   difference over one rounding cent is price-table drift and keeps the plan open.

**Explicit deferral:** do not run LongCat A/B, a 30-second block, or an fps-divisor probe. Those need
a separately planned adapter/price-table/storyboard implementation and their own cost approval.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @pikar/cost test</automated>
  </verify>
  <done>The owner-approved Run A completed within its hard maximum and supplied every observation required by Task 5.</done>
  <resume-signal>Reply `approved Run A, max $0.35` (or a lower maximum), then provide the recorded evidence after the run; otherwise describe the failed precondition.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: write the observed evidence and close 20-11</name>
  <files>docs/playbooks/media.md, .planning/phases/20-media-canvas/20-11-SUMMARY.md, .planning/phases/20-media-canvas/20-VALIDATION.md</files>
  <action>
Only after Task 4 supplies real observations:

1. Fill `docs/playbooks/media.md`'s reconciliation table with the date, run/reference ids, before and
   after provider balance, provider delta, summed actual cents, difference, reserved job cents versus
   the sum of independently floored line items, render wall-clock, submit/webhook results, verdicts,
   retention result and refs-only scan result.
2. State that this is the first D5 reconciliation run **for the shipped Wan/FLUX path only**.
3. Add a residual-gate note: LongCat and 30-second/fps-divisor claims remain unverified and unsupported
   by current code; no finding about their quality, price or divisor was made.
4. Create `20-11-SUMMARY.md` referencing `1db8a03` for Tasks 1-3 and copying the live numbers verbatim.
5. Fill only the matching manual-only rows in `20-VALIDATION.md`; do not mark an unobserved row green.
6. Run `node scripts/check-playbooks.mjs` and `git diff --check`.
  </action>
  <verify>
    <automated>node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>The supported Run A is evidenced in all three artifacts with matching real numbers, while LongCat/30-second claims remain explicitly deferred.</done>
</task>

</tasks>

<verification>
- Tasks 1-3 are evidenced by `git show --stat 1db8a03`; they are not rerun.
- The only live submit came through the application’s governed media path after explicit approval.
- Actual spend stayed at or below the approved hard maximum.
- `node scripts/check-playbooks.mjs` and `git diff --check` exit 0.
- `MEDIA-01` and Phase 20 remain open until 20-12’s candidate/eval/activation cycle completes.
</verification>

<success_criteria>
- fal accepted the shipped Run A bodies, the public webhook landed, the real renderer returned a valid
  sidecar within its ceiling, and the final reel retained only allowed outputs.
- Reservation, actual cents and provider balance delta are reconciled with real observations.
- Unsupported LongCat/30-second work is deferred instead of being simulated through direct fal calls.
</success_criteria>

<output>
Create `.planning/phases/20-media-canvas/20-11-SUMMARY.md` only after the live evidence exists.
Do not mark Phase 20 or MEDIA-01 complete; 20-12 remains.
</output>
