---
phase: 15-sub-agent-dispatch-action-executor
plan: 06
type: execute
wave: 5
depends_on: ["15-04"]
files_modified:
  - packages/contracts/skills/offer-architect.md
  - packages/contracts/skills/money-model-designer.md
  - packages/contracts/skills/lead-engine.md
  - packages/contracts/src/skills/offerArchitect.ts
  - packages/contracts/src/skills/moneyModelDesigner.ts
  - packages/contracts/src/skills/leadEngine.ts
  - packages/backend/scripts/run-eval-golden.mjs
  - packages/backend/scripts/eval-cases/
  - docs/playbooks/skill-registry.md
  - docs/playbooks/agent-runtime.md
autonomous: true
requirements: [DISP-01]

must_haves:
  truths:
    - "Each specialist body is written to RUN — it instructs its own grounding through searchVault instead of promising a later build"
    - "One eval run can pin all three specialists at once; SKILL_NAMES no longer excludes seven of the eleven gated skills"
    - "Three golden fixtures exercise gap -> tap -> dispatch -> staged specialist output end to end"
    - "A specialist body change cannot activate without recorded passing eval evidence"
    - "If the gate does not go green in-phase the framework still ships — candidates park, active v1 bodies stay live"
  artifacts:
    - path: "packages/contracts/skills/offer-architect.md"
      provides: "runnable specialist body with an explicit searchVault grounding instruction"
      contains: "searchVault"
    - path: "packages/backend/scripts/run-eval-golden.mjs"
      provides: "multi-pin --skill; SKILL_NAMES derived from GATED_SKILLS"
      contains: "GATED_SKILLS"
    - path: "packages/backend/scripts/eval-cases/"
      provides: "3 new fixtures, one per specialist, gap -> dispatch -> staged output"
  key_links:
    - from: "packages/contracts/skills/offer-architect.md"
      to: "packages/contracts/src/skills/offerArchitect.ts"
      via: "byte-identity enforced by skillBodies.test.ts"
      pattern: "offerArchitectSkillBody"
    - from: "packages/backend/scripts/run-eval-golden.mjs"
      to: "packages/contracts/src/skill.ts"
      via: "SKILL_NAMES derived from GATED_SKILLS instead of a hardcoded three"
      pattern: "GATED_SKILLS"
---

<objective>
Make the three specialists' BODIES worth running, and make the eval gate able to certify them in one
sitting.

Purpose: CONTEXT's "the three specialist bodies get rewritten to run" plus the eval-gate consequence
that follows from it. This is deliberately the LAST wave: Phase 15's five success criteria are all
satisfied by 15-01..15-05, so a red or unaffordable eval gate here cannot block the phase.

**RESOLVED (RESEARCH Open Question 3): yes, each rewritten body gets an explicit `searchVault`
grounding instruction, and the gate is budgeted as ONE run.** The bodies today already end with
"Cite the user's own material for every claim" — but the specialist has NEVER had a retrieval tool
until this phase, so that instruction was unsatisfiable. Now it has exactly one tool and the body
must say how to use it. **If the gate does not go green inside Phase 15, SHIP DARK** (CONTEXT):
the framework is already landed and fully tested offline; the candidates park unactivated and the
active v1 bodies stay live.

Output: three rewritten bodies (5-file mirror), a multi-pin eval runner, three golden fixtures, one
recorded eval run.

**Lane A.** `convex/skills.ts` seeding is Lane-A-only this phase (PARALLELIZATION singleton #8) —
`seedSkills` writes `maxVersion + 1` and optimizer dry-run candidates already occupy versions, so
two lanes seeding concurrently cross versions SILENTLY.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sub-agent-dispatch-action-executor/15-CONTEXT.md
@.planning/phases/15-sub-agent-dispatch-action-executor/15-RESEARCH.md
@.planning/phases/15-sub-agent-dispatch-action-executor/15-04-SUMMARY.md
@docs/playbooks/skill-registry.md
@docs/playbooks/agent-runtime.md

<interfaces>
<!-- Extracted seams. Do NOT re-explore the codebase for these. -->

The 5-file mirror already EXISTS for all three specialists — this plan changes only two files each:
```
packages/contracts/skills/offer-architect.md         (canonical)   <-- edit
packages/contracts/src/skills/offerArchitect.ts      (derived)     <-- edit, byte-identical
packages/contracts/src/skill.ts   OFFER_ARCHITECT_SKILL            (name const — already exists)
packages/backend/convex/skills.ts :277-279  seedSkills rows        (already exist)
packages/contracts/src/skills/skillBodies.test.ts    (md <-> ts byte-identity — must stay green)
```
Same for `money-model-designer.md` / `moneyModelDesigner.ts` and `lead-engine.md` / `leadEngine.ts`.

The placeholder framing CONTEXT says to DROP, present in all three bodies verbatim:
```
"Registered now; a full build runs later. For now you produce the method and the next step …"
"## What you produce now"
```

packages/contracts/src/skill.ts — GATED_SKILLS (11 entries) already includes all three specialists:
```ts
export const GATED_SKILLS: readonly string[] = [
  COCKPIT_AGENT_SKILL, DOCUMENT_DRAFTER_SKILL, INBOX_DIGEST_SKILL, REPLY_DRAFTER_SKILL,
  GROWTH_OS_DIAGNOSTIC_SKILL, SWOT_SKILL, LEAN_CANVAS_SKILL, BMC_SKILL,
  OFFER_ARCHITECT_SKILL, MONEY_MODEL_DESIGNER_SKILL, LEAD_ENGINE_SKILL,
];
```

packages/backend/scripts/run-eval-golden.mjs — the SINGLE-PIN runner, and every line that changes:
```js
:38-39   const SKILL_NAMES = ["cockpit-agent","document-drafter","inbox-digest"];  // excludes 8 of 11
:113-120 parseSkillPin(spec)  ->  throws on a name not in SKILL_NAMES
:376     ...(pin && { skillVersions: { [pin.name]: pin.version } })
:519     skillVersions: { [pin.name]: pin.version }   (the recorded evidence row)
:537-538 const skillIdx = argv.indexOf("--skill"); const pin = skillIdx >= 0 ? parseSkillPin(argv[skillIdx+1]) : null;
:335-343 selfCheck() assertions — must keep passing
:36      const COST_CAP_USD = 1.0;   // cumulative cost beyond this ABORTS the run (exit 2)
```
`eval-cases/` currently holds 27 fixtures; `27-grounded-assessment` and `28-healthy-no-gaps` are the
Phase-12 precedent to copy. The runner rejects any fixture using a term outside its CLOSED `expect`
vocabulary BEFORE the first spawn — a bad fixture must never cost a cent.

Last recorded full run: **27 cases, $0.1686, run `ed251c29`, cockpit-agent@15** (STATE.md, Phase 12).

packages/backend/convex/skills.ts — the gate:
```ts
seedSkills            // writes maxVersion + 1; a gated skill's FIRST seed lands v1 ACTIVE via the
                      // rows.length === 0 bootstrap path — so a FRESH lane deployment will LIE to
                      // you about whether the gate applies (Pitfall 6)
activateSkillVersion  // throws unless hasPassingEvidence(target.evidence, name, version)
skills:recordEvalEvidence  // what the runner calls on a green run
```

packages/core/src/specialists.ts (15-02) — the specialist's ENTIRE tool-set:
```ts
tools: ["searchVault"]     // no evaluateBusiness (it WRITES), no plan writes, no send
```
The evaluation snapshot (framework, the gap's label/reason/proofMetric/playbook, grounded findings +
citation titles) is injected into the PROMPT by `dispatch.ts` (15-03), not fetched by a tool.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the three specialist bodies to run — grounded, tool-aware, no placeholder framing</name>
  <files>packages/contracts/skills/offer-architect.md, packages/contracts/skills/money-model-designer.md, packages/contracts/skills/lead-engine.md, packages/contracts/src/skills/offerArchitect.ts, packages/contracts/src/skills/moneyModelDesigner.ts, packages/contracts/src/skills/leadEngine.ts</files>
  <action>
Rewrite each `.md` and mirror it byte-identically into its derived `.ts` constant.
`skillBodies.test.ts` enforces the byte-identity — run it after every edit, not once at the end.

For each of the three bodies:

1. **Drop the placeholder framing.** Remove "Registered now; a full build runs later. For now you
   produce the method and the next step…" and retitle "## What you produce now" to a working
   deliverable section. CONTEXT: "write them as working specialists producing grounded output."

2. **Add an explicit grounding instruction naming the ONE tool the specialist has.** This is the
   material change and the reason the eval gate fires. Something in the shape of:

   > ## How to ground this
   > You have exactly one tool: `searchVault`. Before you assert anything about this business,
   > search the vault for the material that would support it. Cite the document title beside every
   > claim you make. If the search returns nothing that supports a claim, say what is missing and
   > what to gather — do not fill the gap from general knowledge. You have no way to send anything,
   > save anything, or change the plan; you produce findings, and the human approves what happens
   > next.

   Two properties must survive: (a) the specialist is told it is READ-ONLY, which reinforces at the
   prompt layer what the tool record already enforces structurally; (b) the not-enough-data state is
   an affirmative, honest answer — the Phase-12 posture, never dressed up as a finding.

3. **Keep the method.** The value equation / financial spine / gate ordering content is the tested
   part; do not rewrite the method itself. CONTEXT defers "specialist output quality beyond the
   first rewrite" to a later pass — this is ONE honest rewrite, not a method redesign.

4. **Keep the deferral to the shared financial spine.** Each body already says it does not redefine
   the diagnostic's metrics; that must stay true or the memo could assert a figure the evaluation
   did not ground.

5. **Original wording only.** No Hormozi (or any) book text — the 12-02 constraint stands.

6. Do NOT touch `packages/contracts/src/skill.ts` (the name consts already exist) and do NOT touch
   `packages/backend/convex/skills.ts` seed rows (they already point at these constants). Adding a
   version number by hand anywhere is wrong — `seedSkills` computes `maxVersion + 1`.
  </action>
  <verify>
    <automated>pnpm --filter @pikar/contracts exec vitest run src/skills/skillBodies.test.ts && pnpm --filter @pikar/contracts exec vitest run</automated>
  </verify>
  <done>All three bodies name `searchVault`, state the read-only posture and the honest not-enough-data state, and contain no "runs later" placeholder framing; `skillBodies.test.ts` green (md ↔ ts byte-identical); the contracts suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-pin the eval runner and add the three end-to-end specialist fixtures</name>
  <files>packages/backend/scripts/run-eval-golden.mjs, packages/backend/scripts/eval-cases/</files>
  <action>
1. **`SKILL_NAMES` derived, not re-listed.** Replace the hardcoded
   `["cockpit-agent","document-drafter","inbox-digest"]` at `:38-39` with a derivation from
   `GATED_SKILLS` (`packages/contracts/src/skill.ts`). Today's list excludes `reply-drafter` AND all
   seven Phase-12 skills — including the three this plan must pin, so `--skill offer-architect@N`
   currently throws at `:119-120`. Deriving it means a newly gated skill is pinnable the day it is
   gated, with no second list to drift. If the runner cannot import from the workspace package,
   read the constant from source rather than duplicating the array, and add a `--self-check`
   assertion that the derived list matches `GATED_SKILLS` exactly.

2. **Multi-pin `--skill`.** `--skill` is single-pin today: `argv.indexOf("--skill")` → one
   `parseSkillPin` (`:537-538`), threaded as `skillVersions: { [pin.name]: pin.version }` at `:376`
   and recorded at `:519`. Change to collect EVERY `--skill` occurrence into a
   `pins: Array<{name, version}>`, and thread `skillVersions` as the merged record
   `Object.fromEntries(pins.map(p => [p.name, p.version]))`. Reject a duplicate name with a clear
   error (two versions of one skill is a bug, not a request). Evidence recording at `:519` already
   works per `(name, version)` — record one evidence row PER pin off the SAME run, so one run, one
   cost, one sitting certifies all three (CONTEXT).

3. **Keep the guardrails.** `COST_CAP_USD = 1.0` stays as-is (three extra cases on a 27-case,
   $0.1686 run leaves ample headroom). The `--self-check` assertions at `:335-343` must keep
   passing — extend them, do not weaken them. The CLOSED `expect` vocabulary check must still run
   BEFORE the first spawn: a bad fixture must never cost a cent.

4. **Three new fixtures in `packages/backend/scripts/eval-cases/`**, one per specialist, numbered
   after the existing 27 (copy the `27-grounded-assessment` / `28-healthy-no-gaps` shape). Each one
   is end to end: a seeded business whose grounded evaluation produces a gap routing to THAT
   specialist → "Act on this" → dispatch → the staged plan body. Assert on:
   - the plan reaches `status: "proposed"` with `kind: "memo"` (i.e. the `collecting` → `proposed`
     flip happened, so dispatch actually completed)
   - the body carries the attribution line naming that specialist
   - the body cites at least one vault document title (grounding actually happened through
     `searchVault`)
   - `findingsPresent`-style pairing where the fixture asserts absence: the 12-06 lesson is that a
     zero-count assertion can pass VACUOUSLY, so pair every "no X" with a "and Y is present"
   Use only terms already in the runner's closed `expect` vocabulary; if a new term is genuinely
   needed, add it to the vocabulary in the same commit and cover it in `--self-check`.
   Bump the fixture-floor constant if the runner carries one (the 12-06 precedent bumped 18 → 27).
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check && pnpm --filter @pikar/backend exec vitest run</automated>
  </verify>
  <done>`--self-check` passes with the derived `SKILL_NAMES`, multi-pin parsing, and 30 fixtures registered; `--skill offer-architect@N --skill money-model-designer@N --skill lead-engine@N` parses without throwing; no fixture uses a term outside the closed vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3: Run the eval gate once — activate on green, ship dark on red — and update the playbooks</name>
  <files>docs/playbooks/skill-registry.md, docs/playbooks/agent-runtime.md</files>
  <action>
This task SPENDS money against the owner's API keys (budget ≈ $0.19, extrapolated from the last
recorded run: 27 cases / $0.1686 / run `ed251c29`) and needs a live deployment. Run it ONCE. Per
CONTEXT, **automated green is sufficient — there is no owner sign-off gate inside this phase**; the
owner's single live human-verify happens on integrated `main` after all lanes land (PARALLELIZATION
Stage 3).

1. **Seed and find the real versions FIRST.** `npx convex dev` (codegen + this worktree's own dev
   deployment), then `pnpm dev` / `npm run seed` so `skills:seedSkills` runs — `convex dev` ALONE
   does not seed (the 12-06 correction). Then **read back which version carries your body** for each
   of the three skills before pinning anything. `seedSkills` writes `maxVersion + 1` and optimizer
   dry-run candidates already occupy versions, so a version number guessed from the plan will be
   wrong against the live DB (Pitfall 5). Record the three `(name, version)` pairs in the SUMMARY.

   Beware the bootstrap lie (Pitfall 6): on a FRESH lane deployment these three have never been
   seeded, so their first seed takes the `rows.length === 0` path and lands **v1 ACTIVE** — the gate
   appears not to apply. That is a property of the empty deployment, not of the gate. Never conclude
   gate behaviour from a lane deployment; `activateSkillVersion` still throws without
   `hasPassingEvidence`.

2. **Run the gate, once, all three pinned:**
   `pnpm eval:golden --skill offer-architect@N --skill money-model-designer@N --skill lead-engine@N`

3. **Branch on the result — both branches are pre-decided, neither needs the owner:**
   - **GREEN** → the runner records evidence per `(name, version)`. Activate each candidate through
     the normal path and VERIFY LIVE with `getActiveSkill` that the active row is the version you
     pinned (12-06 did exactly this). Record the run id, case count and cost in the SUMMARY.
   - **RED, flaky, or over `COST_CAP_USD`** → **SHIP DARK.** Do not retry more than the runner's own
     retry allows, do not weaken a fixture to get green, and do not hand-activate. The candidates
     park unactivated, the active v1 bodies stay live, and Phase 15's five success criteria are
     already met by 15-01..15-05 — none of them requires a rewritten body. Record in the SUMMARY:
     the failing case(s), the cost spent, the parked `(name, version)` pairs, and what a later pass
     must do. Add the carry-forward to STATE.md's Pending Todos.

4. `docs/playbooks/skill-registry.md` (watches `packages/contracts/skills/`,
   `packages/contracts/src/skills/`, `convex/skills.ts`) — record: the three specialist bodies are
   now RUNNABLE and name `searchVault`; a specialist's tool-set is NOT in the registry and never will
   be (ADR-007 — the prompt is registry-owned, the capability is code-owned); `seedSkills` seeding is
   Lane-A-only this phase and the live version must be read back before any pin or activate; the
   bootstrap-lie trap. State the outcome of the gate run explicitly (activated at v_N, or parked).
   Bump `Last verified`.

5. `docs/playbooks/agent-runtime.md` (watches `scripts/run-eval-golden.mjs`, `scripts/eval-cases/`)
   — record: `--skill` is now MULTI-pin and `SKILL_NAMES` derives from `GATED_SKILLS` (so a newly
   gated skill is pinnable immediately and there is no second list to drift); one run records one
   evidence row per pin; the three new end-to-end specialist fixtures and what each asserts; the
   pair-every-zero-count-assertion rule (12-06's vacuous-pass lesson). Bump `Last verified`.
  </action>
  <verify>
    <automated>node packages/backend/scripts/run-eval-golden.mjs --self-check && node scripts/check-playbooks.mjs check && pnpm test</automated>
  </verify>
  <done>The gate ran ONCE with all three pinned; either all three are ACTIVE on recorded passing evidence (verified live via `getActiveSkill`) or all three are parked as candidates with the failure, the cost and the carry-forward recorded; both playbooks updated with bumped `Last verified`; the full offline suite is green either way.</done>
</task>

</tasks>

<verification>
- `pnpm test` green (sole permitted red: the documented `audit.test.ts` `auditCounts` row)
- `pnpm typecheck` green INCLUDING `apps/web`
- `node scripts/check-playbooks.mjs check` exit 0
- `node packages/backend/scripts/run-eval-golden.mjs --self-check` exit 0
- `git diff --exit-code -- packages/backend/convex/cockpit.ts packages/backend/convex/deliverApprovedPlan.ts packages/core/src/actionType.ts apps/` — no Lane-B file, no web file
- Phase 15's SC #1-#5 remain green regardless of the gate outcome (they are proven by 15-01..15-05)
</verification>

<success_criteria>
- Each specialist body is written to RUN and instructs its own grounding through the one tool it has
- `--skill` pins all three in ONE run, with `SKILL_NAMES` derived from `GATED_SKILLS`
- Three end-to-end fixtures exercise gap → tap → dispatch → staged specialist output
- The gate outcome is RECORDED — activated with evidence, or parked with the reason and the cost
- A red gate does not block the phase: the framework shipped in 15-01..15-05 and is fully tested offline
</success_criteria>

<output>
After completion, create `.planning/phases/15-sub-agent-dispatch-action-executor/15-06-SUMMARY.md`
</output>
