---
phase: 03.10-cockpit-conversation-repair
plan: 01
subsystem: api
tags: [convex, gmail, fixtures, eval-harness, vitest, tdd]

# Dependency graph
requires:
  - phase: 03.7-inbox-briefing
    provides: "the inboxFixtures table + smoke.seedInboxFixture/getInboxFixture seam and listInbox's fixture-before-token pattern (gmail.ts:394-408), mirrored here"
  - phase: 03.2.1-cockpit-agent
    provides: "gmail.search + resolveContacts/rankCandidates (the never-substitute rule this seam now feeds on the eval tenant)"
provides:
  - "gmail.search fixture-before-token branch: a seeded inboxFixtures row serves HeaderRecords (from/subject/date) with NO Gmail token"
  - "refs-only mailbox.searched audit on the fixture branch (queryHash+resultCount only, mutation-checked)"
  - "eval fixture 09 description updated: Alonzeva now exercises rankCandidates' no-match branch, not not_connected"
affects: [03.10-02, 03.10-03, eval-harness, resolveContacts]

# Tech tracking
tech-stack:
  added: []
  patterns: ["fixture-before-token seam extended to search (third gmail.ts action on the same inboxFixtures seam)"]

key-files:
  created: []
  modified:
    - packages/backend/convex/gmail.ts
    - packages/backend/convex/gmail.test.ts
    - packages/backend/scripts/eval-cases/09-resolve-degradation.json
    - docs/playbooks/cockpit.md
    - docs/playbooks/agent-runtime.md

key-decisions:
  - "Fixture branch sits AFTER the SMOKE:: name-sentinel and BEFORE freshAccessToken — sentinel ordering pinned by test so a SMOKE:: search on a fixture tenant still routes through the sentinel"
  - "date mapped via new Date(internalDate).toUTCString() so rankCandidates' Date.parse round-trips the fixture epoch exactly"
  - "Reused the search handler's existing audit closure — the fixture branch writes the identical refs-only {queryHash, resultCount} payload, no new audit shape"

patterns-established:
  - "Deliberate-leak mutation check: temporarily leaked records[0].from into the audit payload, confirmed the refs-only test goes RED, reverted (house style)"

requirements-completed: [UAT-B]

# Metrics
duration: 12min
completed: 2026-07-19
---

# Phase 03.10 Plan 01: gmail.search Fixture Seam Summary

**gmail.search gained the same inboxFixtures fixture-before-token seam as listInbox, so resolveContacts can park candidates on the tokenless eval tenant from a plain-NL turn (the UAT-B eval prerequisite)**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-07-18T23:35:32Z
- **Completed:** 2026-07-18T23:47:00Z
- **Tasks:** 1 (TDD: RED + GREEN commits)
- **Files modified:** 5

## Accomplishments
- Fixture-first branch in `gmail.search` (after the `SMOKE::` sentinel, before `freshAccessToken`), mirroring `listInbox`'s seam byte-for-byte in argument: fixture rows only exist for smoke/eval tenants, so the seam cannot shadow a live mailbox
- 4 new gmail.test.ts behaviors (28/28 green): fixture-first HeaderRecords, refs-only `mailbox.searched` audit (whole-payload key assertion + needle scan, mutation-checked with a deliberate `from` leak), no-fixture fall-through to the unchanged `not_connected` path, sentinel-stays-first ordering
- Eval fixture 09's description now names the branch it actually exercises post-seam (rankCandidates no-match — returns `[]`, never substitutes); turns/expect/needles untouched
- cockpit.md (new top entry) + agent-runtime.md (Last verified bump) updated in the same commit as the code (§9)

## Task Commits

1. **Task 1 RED: failing tests for the search fixture seam** - `cc7c164` (test)
2. **Task 1 GREEN: fixture-before-token branch + fixture 09 + playbooks** - `c517bba` (feat)

_No refactor commit — the GREEN diff was already minimal (15 lines in gmail.ts)._

## Files Created/Modified
- `packages/backend/convex/gmail.ts` - fixture-first branch in the `search` handler (~15 lines, reuses the existing `audit` closure and `internal.smoke.getInboxFixture`)
- `packages/backend/convex/gmail.test.ts` - new `search (fixture seam + refs-only audit)` describe block (4 tests)
- `packages/backend/scripts/eval-cases/09-resolve-degradation.json` - description-only update (no-match branch, not not_connected)
- `docs/playbooks/cockpit.md` - new Last verified entry (03.10-01) documenting the seam + safety argument
- `docs/playbooks/agent-runtime.md` - Last verified bumped to 03.10-01 (fixture 09's degradation-branch change; eval-cases/ is agent-runtime-watched)

## Decisions Made
- `toUTCString()` for the date field (fixture epochs are whole-second, so `Date.parse` round-trips exactly — asserted in test)
- No new exports/tables/mutations, per the plan's checker constraint

## Deviations from Plan

None - plan executed exactly as written. (One execution mishap, not a deviation: the post-mutation-check `git checkout` reverted the then-uncommitted GREEN edit; it was re-applied identically and re-verified green before commit.)

## Issues Encountered
- **Pre-existing red discovered (out of scope, logged to `deferred-items.md`):** `llmRedaction.test.ts`'s static scan pins cockpit.ts to exactly 1 `audit.log` call site, but 03.5-05's `reschedulePlan` added a second (`plan.rescheduled`) without bumping the count. Fails identically pre-change; not touched here (scope boundary).
- Full-suite reds otherwise match the documented baseline: `audit.test.ts` auditCounts red; `vaultGround`/`runCockpitAgent` 5000ms machine-load timeouts appeared under parallel load once and passed in the post-GREEN full run.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Wave 1 complete: plan 03's stalled-resolution eval fixture can now park candidates on the tokenless eval tenant via plain NL ("email Sarah Chen…" → fixture HeaderRecords → rankCandidates match)
- Verification chain green: gmail.test.ts 28/28, `run-eval-golden.mjs --self-check` exit 0 (18 fixtures, fixture 09 revalidates), `check-playbooks.mjs` exit 0
- Carry-forward: the llmRedaction cockpit.ts audit-count scan needs a 1→2 bump (deferred item) — worth folding into whichever 03.10 plan next touches that file

## Self-Check: PASSED

---
*Phase: 03.10-cockpit-conversation-repair*
*Completed: 2026-07-19*
