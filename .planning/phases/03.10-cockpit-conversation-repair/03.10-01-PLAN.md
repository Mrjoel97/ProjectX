---
phase: 03.10-cockpit-conversation-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/convex/gmail.ts
  - packages/backend/convex/gmail.test.ts
  - packages/backend/scripts/eval-cases/09-resolve-degradation.json
  - docs/playbooks/cockpit.md
autonomous: true
requirements: ["UAT-B"]

must_haves:
  truths:
    - "On a tenant with a seeded inboxFixtures row, gmail.search returns ok:true HeaderRecords mapped from the fixture messages WITHOUT any Gmail token — so resolveContacts can park candidates on the tokenless eval tenant"
    - "On a real tenant (no fixture row) search behavior is byte-for-byte unchanged: SMOKE:: sentinel first, then freshAccessToken"
    - "The fixture branch writes the same refs-only mailbox.searched audit (counts only — never a from/subject/address)"
  artifacts:
    - path: "packages/backend/convex/gmail.ts"
      provides: "fixture-first branch in the search handler, mirroring listInbox's (gmail.ts:394-408)"
      contains: "getInboxFixture"
    - path: "packages/backend/convex/gmail.test.ts"
      provides: "unit coverage: fixture-first records, refs-only audit, no-fixture fall-through, not_connected degradation still covered"
  key_links:
    - from: "packages/backend/convex/gmail.ts"
      to: "internal.smoke.getInboxFixture"
      via: "search handler fixture-before-token branch"
      pattern: "getInboxFixture"
---

<objective>
Wave 0: give `gmail.search` the same `inboxFixtures` seam `listInbox`/`fetchInboxBodies` already have, so a plain-NL "email Sarah Chen…" turn can park candidates on the tokenless eval tenant. Without this, the Defect-B eval fixture (plan 03) degrades to "couldn't read the mailbox" and measures nothing (RESEARCH Pitfall 3).

Purpose: hard prerequisite for the stalled-resolution eval fixture — the phase's UAT-B gate evidence.
Output: fixture-first `search` branch + unit tests; fixture 09's description updated to the branch it now exercises.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-cockpit-conversation-repair/03.10-CONTEXT.md
@.planning/phases/03.10-cockpit-conversation-repair/03.10-RESEARCH.md
@packages/backend/convex/gmail.ts
@packages/backend/convex/gmail.test.ts

<interfaces>
The pattern to mirror is already in the same file — listInbox's fixture-before-token branch
(gmail.ts:394-408): `const fixture = await ctx.runQuery(internal.smoke.getInboxFixture, { tenantId }); if (fixture) { …map fixture.messages…, audit, return }`. Fixture rows only exist for
smoke/eval tenants ("the seam cannot shadow a live mailbox" — the safety argument, verbatim
from listInbox's comments).

Target shape for search (RESEARCH Finding 5 — verified sketch):
```ts
// gmail.ts search handler, AFTER the SMOKE:: name-sentinel branch, BEFORE freshAccessToken:
const fixture = await ctx.runQuery(internal.smoke.getInboxFixture, { tenantId });
if (fixture) {
  const records: HeaderRecord[] = fixture.messages.map((m) => ({
    from: m.from,                                  // "Sarah Chen <sarah.chen@example.com>"
    subject: m.subject,
    date: new Date(m.internalDate).toUTCString(),  // rankCandidates Date.parse-es rec.date
  }));
  // same refs-only mailbox.searched audit the token path writes (count only)
  return { ok: true, records };
}
```
Downstream: `rankCandidates` (packages/core/src/emailIntent.ts:144-190) parses each `from`,
scores name-match (full-name substring = 2, token = 1, no match = [] — the 03.3-06
never-substitute rule). "Sarah Chen" matches the seeded fixture row; "Alonzeva" (fixture 09)
matches nothing → still `candidatesPending: false`, now via the no-match branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fixture-first branch in gmail.search + tests + fixture-09 description + playbook bump</name>
  <files>packages/backend/convex/gmail.ts, packages/backend/convex/gmail.test.ts, packages/backend/scripts/eval-cases/09-resolve-degradation.json, docs/playbooks/cockpit.md</files>
  <behavior>
    - Test 1 (fixture-first): tenant with a seeded inboxFixtures row → search returns ok:true with one HeaderRecord per fixture message (from/subject/date mapped as sketched), WITHOUT touching freshAccessToken.
    - Test 2 (refs-only audit): the fixture branch writes the same mailbox.searched audit shape as the token path — resultCount/refs only; assert no `from`/`subject`/address string lands in the payload (deliberate-leak mutation check, house style).
    - Test 3 (live tenants unaffected): no fixture row → falls through to the existing token path; not_connected degradation (`ok:false`) still covered where it already lives in gmail.test.ts.
    - Test 4 (ordering): the SMOKE:: name-sentinel branch stays FIRST — a SMOKE:: search on a fixture-seeded tenant still routes through the sentinel, not the fixture.
  </behavior>
  <action>
    In gmail.ts's `search` handler (lines ~242-308), insert the fixture-first branch AFTER the
    SMOKE:: sentinel branch and BEFORE `freshAccessToken`, exactly mirroring listInbox's
    (gmail.ts:394-408) — reuse `internal.smoke.getInboxFixture` and the same "fixture rows only
    exist for smoke/eval tenants" comment/argument. Map `fixture.messages` → `HeaderRecord[]`
    per the interfaces sketch (date via `new Date(m.internalDate).toUTCString()` — rankCandidates
    Date.parse-es rec.date). Write the SAME refs-only mailbox.searched audit the token path
    writes (count only), then return `{ ok: true, records }`. No new exports, no new tables, no
    new mutations (checker flags any).

    gmail.test.ts: add the behavior cases above, following the file's existing convex-test setup.

    09-resolve-degradation.json: update ONLY the `description` string — post-seam, "Alonzeva"
    exercises the no-match branch of rankCandidates (returns [], never substitutes), not the
    not_connected degradation; turns/expect/needles unchanged (its state assertions still hold —
    RESEARCH Finding 5).

    docs/playbooks/cockpit.md: gmail.ts is a watched path — add a short "Last verified: 2026-XX-XX
    (03.10-01)" entry documenting the search seam (fixture-before-token, refs-only audit, cannot
    shadow a live mailbox), same commit (§9 Stop hook).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- gmail && node packages/backend/scripts/run-eval-golden.mjs --self-check && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>gmail vitest green with the 4 new behaviors; self-check exit 0 (fixture 09 still validates); check-playbooks exit 0; no change to live-tenant search behavior (fall-through test proves it).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- gmail` green (new cases + existing 24+).
- `node packages/backend/scripts/run-eval-golden.mjs --self-check` exit 0 — zero convex calls, fixture 09 revalidates.
- `node scripts/check-playbooks.mjs` exit 0 (cockpit.md bumped in the same commit).
- Documented non-regressions unchanged (audit.test.ts auditCounts red; runCockpitAgent machine-load timeout).
</verification>

<success_criteria>
A tokenless tenant with a seeded inbox fixture gets real HeaderRecords from `gmail.search`, so plan 03's fixture 19 can park candidates via plain NL; live tenants are provably untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/03.10-cockpit-conversation-repair/03.10-01-SUMMARY.md`
</output>
