---
phase: 03.10-cockpit-conversation-repair
plan: 07
subsystem: agent-runtime
tags: [cockpit, convex, skills, eval-gate, schema, redaction, briefing]

# Dependency graph
requires:
  - phase: 03.10-06
    provides: history-injected prompt (incl. the fail-open hotfix) + skill v10 + the 21-fixture golden set
  - phase: 03.10-05
    provides: resetPlan explicit-clear contract (this plan extends its clear list with recipientNames)
  - phase: 03.7
    provides: briefInbox tool + @pikar/core briefing view (the count-parity fix spans both)
  - phase: 03.6
    provides: eval gate (seedSkills publish → eval:golden evidence → activateSkill)
provides:
  - "recipientNames optional map on the plans row (lowercased address → picked displayName): a completed pick is VISIBLE to the model — post-pick context shows `#1: Brett J. Fox`, never `#1 (no name)` for a picked contact"
  - "Structural recipient-tool withholding: omitRecipientEdits flag threaded cockpit.ts → runCockpitAgent → RunLoopArgs → the loop's buildCockpitTools — setRecipients/addRecipients/removeRecipient are ABSENT on the RESOLUTION_CONTINUE turn only, so a fabricated-recipient overwrite is impossible, not discouraged"
  - "Reworded RESOLUTION_CONTINUE stating the fold happened (recipients shown by #index are the picks; do not change; ask for unset slots)"
  - "isNeedsYou exported from @pikar/core: ONE shared needs-you predicate — briefInbox's return states listedCount + needsYou + summarized count, identical numbers to the card masthead"
  - "cockpit-agent v11 active via the gate: 'After a pick completes' section + the distrust clause scoped to exclude panel-picked recipients"
affects: [cockpit, inbox-reply, briefing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-turn tool-set variation: the first entry-point-conditional tool set (an append-only omit flag withholds tools on one synthetic turn) — structural safety over skill wording"
    - "One shared predicate for cross-surface count parity: the agent's sentence and the card masthead count with the same exported function over the same array"

key-files:
  created: []
  modified:
    - packages/backend/convex/schema.ts
    - packages/backend/convex/plans.ts
    - packages/backend/convex/cockpit.ts
    - packages/backend/convex/llm.ts
    - packages/core/src/briefing.ts
    - packages/backend/convex/cockpitTools.test.ts
    - packages/contracts/skills/cockpit-agent.md
    - packages/contracts/src/skills/cockpitAgent.ts
    - docs/playbooks/cockpit.md
    - docs/playbooks/agent-runtime.md
    - docs/playbooks/skill-registry.md

key-decisions:
  - "UAT-F2 fixed STRUCTURALLY (tools absent on the continue turn), not by skill wording — v10's wording had failed live against a fabricated setRecipients overwrite"
  - "recipientNames is optional (no migration) and explicitly cleared by resetPlan — the explicit-write reset contract holds"
  - "Fixture 22 explicitly SKIPPED: the runner never drives resolveRecipients (authenticated tenantAction) and seedCockpitPlan seeds a bare plan — covering the continue turn needs harness surgery (seed folded state + runner omit-flag grammar); the blocking human-verify covered it live. Upgrade path recorded."
  - "Briefing counts: code owns the numbers on BOTH surfaces via one exported predicate (isNeedsYou) — the agent can no longer contradict its own panel"

requirements-completed: [UAT-F]

# Metrics
duration: not individually recorded (executed 2026-07-19; closed at the phase-wide sign-off)
completed: 2026-07-19
---

# Phase 03.10 Plan 07: Post-Pick Trust Repair Summary

**The trust collapse is structurally gone: picked display names persist on the plan row and render in the model's context (no more "#1 (no name)" for a picked contact), the recipient-mutating tools are physically absent on the post-pick continue turn (a fabricated overwrite like the live brett@example.com incident is impossible), RESOLUTION_CONTINUE states the fold happened, and the briefing sentence carries the card masthead's exact numbers via one shared predicate — UAT-F closed with skill v11 gate-activated and the full transcript approved 2026-07-19.**

## Performance

- **Completed:** 2026-07-19
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify, closed by the phase-wide final replay)
- **Files modified:** 11 (commit c10c012)

## Accomplishments
- **UAT-F1 (pick visibility):** optional `recipientNames` map (schema.ts, lowercased address → picked displayName; no migration) + patchPlan arg + resetPlan explicit clear; the resolveRecipients fold writes it from picks[].displayName; buildAgentContext renders it via buildRecipientView — the post-pick context shows `#1: Brett J. Fox`, so the model can structurally distinguish a folded pick from an unresolved placeholder and the distrust clause has nothing to fire on.
- **UAT-F2 (structural safety):** `omitRecipientEdits` flag threaded cockpit.ts → runCockpitAgent → RunLoopArgs → the loop's own buildCockpitTools; when true, setRecipients/addRecipients/removeRecipient are simply not in the tool record. ONLY the resolveRecipients re-invoke (the RESOLUTION_CONTINUE turn) passes it — normal turns and every eval fixture keep the full set byte-identically.
- **UAT-F2 (wording):** RESOLUTION_CONTINUE reworded to state the fact — the picks are already the plan's recipients (by #index), do not change recipients, ask for unset subject/body rather than inventing.
- **UAT-F3 (count parity):** `isNeedsYou` exported from @pikar/core (the predicate composeLede/the card Kpis already use); briefInbox's return now states listedCount + isNeedsYou-counted needsYou + summarized count — the same numbers as the card masthead by construction. No more 50-vs-25 / 6-vs-2 contradictions.
- cockpitTools.test.ts: briefing-return regex updates + new unit checks (named-recipient render, withheld tool set) — **78/78 green with the skills suite**.
- Skill **v11** mirror (cockpit-agent.md + byte-identical cockpitAgent.ts): new "After a pick completes" section (the continue message means the fold happened; "continue composing" is NOT license to fabricate recipients/subject/body) + the distrust clause SCOPED to exclude panel-picked recipients (the unscoped clause had scripted the live "didn't properly register" defect verbatim).
- 3 playbook bumps (cockpit.md, agent-runtime.md, skill-registry.md — Last verified 03.10-07), same commit per §9.

## Task Commits

1. **Task 1: persist+render picked names, structural tool withholding, honest RESOLUTION_CONTINUE, one briefing predicate + skill v11 mirror + playbooks** - `c10c012` (feat)
2. **Task 2: live gate cycle** - runtime procedure, no code commit (see below)
3. **Task 3: human-verify** - closed by the 2026-07-19 phase-wide sign-off (see below)

## Gate Cycle (Task 2)

- `seedSkills` minted cockpit-agent candidate **v11** (read back at runtime).
- `activateSkill` v11 REFUSED pre-evidence (EVAL_GATE) — gate proven.
- Pinned run **39d0c4e2** — **21/21 green, $0.0871** (cap $1.00). The withholding never applies on the runner (no flag passed) and recipientNames is never runner-seeded, so the full-set green run is the skill-wording regression proof.
- `activateSkill` v11 succeeded; **v11 confirmed active** (verified post-session-cutoff by the orchestrator).

## Human Verify (Task 3)

**APPROVED in the final 2026-07-19 live replay** ("I'm satisfied. I've tested everything the way you asked me to test. Everything is good. I approve."): briefing counts matched the card masthead, picks registered visibly by NAME immediately after the pick, no fabricated recipients/subject/body appeared on the continue turn, reset carried nothing over, and zero unintended sends occurred.

**Context note:** mid-execution, a fabricated proposed plan from the live UAT session (invented brett@example.com / devpost@example.com recipients — the very UAT-F2 defect) was still parked on the thread; the orchestrator defused it via `plans:resetPlan` before this plan's fixes landed. No send occurred.

## Files Created/Modified
- `packages/backend/convex/schema.ts` - optional recipientNames map on plans.
- `packages/backend/convex/plans.ts` - patchPlan accepts recipientNames; resetPlan explicitly clears it.
- `packages/backend/convex/cockpit.ts` - fold persists picked names; reworded RESOLUTION_CONTINUE; re-invoke passes omitRecipientEdits.
- `packages/backend/convex/llm.ts` - buildAgentContext renders picked names; omitRecipientEdits threading; briefInbox count-parity return.
- `packages/core/src/briefing.ts` - isNeedsYou exported.
- `packages/backend/convex/cockpitTools.test.ts` - regex updates + named-render + withheld-tool-set checks (78/78 with skills).
- `packages/contracts/skills/cockpit-agent.md` / `packages/contracts/src/skills/cockpitAgent.ts` - v11 body, byte-identical mirror.
- `docs/playbooks/cockpit.md`, `docs/playbooks/agent-runtime.md`, `docs/playbooks/skill-registry.md` - §9 bumps.

## Decisions Made
- Structural absence over wording for the safety fix (UAT-F2) — the tool set now varies by entry point for the first time, recorded in agent-runtime.md.
- Fixture 22 SKIPPED with a recorded upgrade path (harness surgery: seed folded recipientNames state + teach the runner the omit-flag/continue-turn grammar); the human-verify covered the continue turn live.
- Deferred: listedCount saturates at INBOX_LIST_CAP=50 — a "50+" display treatment is future polish (both surfaces are consistent today).

## Deviations from Plan

None in-scope — the plan executed as written, including the explicitly planned fixture-22 skip.

## Issues Encountered
- The parked fabricated plan from the UAT session (defused via plans:resetPlan by the orchestrator — see Context note). Not a defect in this plan's changes; it was the pre-fix state this plan exists to make impossible.

## User Setup Required
None.

## Next Phase Readiness
- UAT-F closed; all three legs of the trust collapse structurally gone. Phase 03.10 execution complete (7/7 plans) — awaiting `/gsd:verify-work` phase-goal verification, then Phase 3.11 (Inbox Reply) builds on the repaired flow.

---

## Self-Check: PASSED

- Commit `c10c012` FOUND on main (11 files, matches the file list above).
- recipientNames present in schema.ts/plans.ts/cockpit.ts; omitRecipientEdits present in llm.ts; isNeedsYou exported from packages/core/src/briefing.ts (per commit diff stats).
- Human sign-off recorded 2026-07-19; v11 active confirmed by the orchestrator.

---
*Phase: 03.10-cockpit-conversation-repair*
*Completed: 2026-07-19*
