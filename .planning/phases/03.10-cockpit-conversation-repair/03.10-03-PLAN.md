---
phase: 03.10-cockpit-conversation-repair
plan: 03
type: execute
wave: 3
depends_on: ["03.10-01", "03.10-02"]
files_modified:
  - packages/contracts/skills/cockpit-agent.md
  - packages/contracts/src/skills/cockpitAgent.ts
  - packages/backend/scripts/eval-cases/19-stalled-resolution-recovery.json
  - docs/playbooks/skill-registry.md
  - packages/backend/convex/llm.ts
autonomous: false
requirements: ["UAT-B"]

must_haves:
  truths:
    - "While a contact pick is pending, the agent still acts on subject/body intent in the user's message: fixture 19 ends with candidatesPending:true + subjectPresent:true + bodyPresent:true + recipientCount:0 + statusAtMost:collecting under the pinned candidate skill"
    - "The edited cockpit-agent skill is ACTIVE in production only after an all-green version-pinned eval run (EVAL_GATE refused pre-evidence, succeeded post-evidence)"
    - "The recipient guard is untouched: the eval run proves zero recipients were guessed/forced (recipientCount:0) and no premature propose (statusAtMost:collecting)"
    - "The UAT transcript replayed live: picker visible on top without scrolling, subject+body absorbed during the stall, panel pointed at most once more, pick completes to a PLAN with a NAMED recipient (never '#1 (no name)')"
  artifacts:
    - path: "packages/contracts/skills/cockpit-agent.md"
      provides: "new '## While a pick is pending' section (stalled-resolution recovery contract)"
      contains: "While a pick is pending"
    - path: "packages/contracts/src/skills/cockpitAgent.ts"
      provides: "byte-identical (LF-normalized) regenerated skill body — drift test.each stays green"
    - path: "packages/backend/scripts/eval-cases/19-stalled-resolution-recovery.json"
      provides: "stalled-resolution fixture asserting plan state (never reply text)"
      contains: "candidatesPending"
  key_links:
    - from: "packages/backend/scripts/eval-cases/19-stalled-resolution-recovery.json"
      to: "gmail.search fixture seam (03.10-01)"
      via: "turn 1 'Draft an email to Sarah Chen.' parks the seeded sarah.chen@example.com candidate via plain NL"
      pattern: "Sarah Chen"
    - from: "packages/contracts/skills/cockpit-agent.md"
      to: "skills registry (seedSkills → eval:golden → activateSkill)"
      via: "the 3.6 gate cycle — an unactivated candidate fixes nothing live"
      pattern: "cockpit-agent"
---

<objective>
Fix Defect B (AGENT STALLED-RESOLUTION RECOVERY) through the full registry gate: teach the cockpit-agent skill what to DO while a pick is pending (absorb subject/body intent via setSubject/draftBody, point at the panel at most once more with new words, never re-ask a given slot), prove it with fixture 19 under a pinned eval run, activate only on green, then human-verify the whole phase by replaying the UAT transcript live.

Purpose: the transcript's turns 3-5 — re-announce loop, re-asked subject, ignored "tell him i will attend the workshop", final "#1 (no name)" plan.
Output: cockpit-agent vN active (gate-proven), 19-fixture golden set, blocking human sign-off.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-cockpit-conversation-repair/03.10-CONTEXT.md
@.planning/phases/03.10-cockpit-conversation-repair/03.10-RESEARCH.md
@packages/contracts/skills/cockpit-agent.md
@packages/backend/scripts/eval-cases/09-resolve-degradation.json
@.planning/phases/03.7-inbox-briefing/03.7-05-PLAN.md
@docs/playbooks/skill-registry.md

<interfaces>
Mirror edit is a 2-FILE change for an existing skill (RESEARCH Finding 4): the canonical
`packages/contracts/skills/cockpit-agent.md` + the regenerated `cockpitAgentSkillBody` string in
`packages/contracts/src/skills/cockpitAgent.ts` (LF-normalized byte-identity; the drift test.each
row in skills.test.ts already exists). GATED_SKILLS and the runner's SKILL_NAMES already contain
cockpit-agent — no runner surgery, no new expect keys, self-check floor is >=18 so fixture 19
passes without a floor bump.

What the model already sees every stalled turn (buildAgentContext, llm.ts:498-519):
```
Awaiting the user's contact pick (NOT yet recipients — tell the user to pick from the card, do NOT claim you added them):
  Brett J. Fox: 2 contact(s) found
Subject: (not set)
Body drafted: no
```
draftBody/setSubject have NO recipient/candidate precondition — they work with candidates parked.

Fixture 19 (RESEARCH Finding 6 — drafted, uses only the existing closed expect vocabulary):
```json
{
  "id": "19-stalled-resolution-recovery",
  "description": "UAT 2026-07-19: pick pending — the agent must still absorb subject/body intent (setSubject/draftBody with candidates parked), never re-ask, never force recipients",
  "turns": [
    "Draft an email to Sarah Chen.",
    "Subject: Workshop. Tell her I will attend the workshop."
  ],
  "expect": {
    "candidatesPending": true,
    "subjectPresent": true,
    "bodyPresent": true,
    "recipientCount": 0,
    "statusAtMost": "collecting"
  },
  "needles": ["sarah.chen@example.com", "attend the workshop"]
}
```
This fixture SHOULD fail against the current active skill (it reproduces the defect) and pass only
under the `--skill cockpit-agent@N` pin — that is how the gate run works.

Gate cycle (03.7-05 Task 2 shape, proven live twice): persistent `npx convex dev` →
`npx convex run skills:seedSkills` publishes candidate vN (read N back — runtime fact, never
hardcode) → `activateSkill` vN REFUSES (EVAL_GATE, no evidence) → `pnpm eval:golden --skill
cockpit-agent@N` all-green records EvalEvidence → `activateSkill` vN succeeds. Cost cap $1.00,
expected ~$0.05-0.15. Exit 2 = environment (governed stop/cap), never a case failure. After a
network blip: kill ALL convex processes, restart ONE clean dev before re-running (eval-env-recovery).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skill mirror edit ("While a pick is pending") + fixture 19 + playbook</name>
  <files>packages/contracts/skills/cockpit-agent.md, packages/contracts/src/skills/cockpitAgent.ts, packages/backend/scripts/eval-cases/19-stalled-resolution-recovery.json, docs/playbooks/skill-registry.md</files>
  <action>
    cockpit-agent.md: add a "## While a pick is pending" section after "## Working a message"
    (03.5-02/03.3-04 wording style: teach the state, give trigger phrases, restate the rails).
    Content targets (RESEARCH Finding 7 + CONTEXT locked decisions):
    - Your context's "Awaiting the user's contact pick" block means the panel pick is STILL OPEN —
      you cannot complete it and must not try (never guess an address; panel-only resolution stays).
    - That reminder is STATE, not an instruction to repeat yourself: mention the panel at most ONCE
      more after the first announcement, with fresh words locating it concretely — post-Defect-A-fix
      the picker is the TOP card of the workspace panel, so say so ("the contact list is at the top
      of the workspace panel"); do NOT use CONTEXT.md's pre-fix "below the briefing" example.
    - STILL act on everything else the user gives while the pick is open: subject → setSubject,
      what to say → draftBody, attachment → generateAttachment, time → setSendTime. They all work
      with candidates parked and are kept when the pick completes.
    - If the user seems lost ("where is the list"), say plainly the pick is still pending and where
      the card is — once — then keep working with what they give.
    - Never re-ask for a slot the context shows as set (the Subject:/Body drafted: lines).
    - Discretionary (#1-no-name probe): before proposePlan, if the only recipients are unresolved
      placeholders, confirm with the user instead of proposing — wording only, NO new code guard.
    cockpitAgent.ts: regenerate `cockpitAgentSkillBody` as the byte-identical LF-normalized string
    of the .md (the existing drift test.each row must stay green).
    19-stalled-resolution-recovery.json: exactly the interfaces-block JSON (closed vocabulary,
    plain-NL turns, no SMOKE::, both needles). One fixture — the optional 3-turn variant is skipped
    (RESEARCH OQ2: the human-verify replay covers the full arc).
    docs/playbooks/skill-registry.md: note the edit + gate cycle, bump `Last verified` (03.10-03),
    same commit (§9).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- skills && node packages/backend/scripts/run-eval-golden.mjs --self-check && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Drift test green (byte-identity); self-check exit 0 with 19 validated fixtures; playbook bumped; no runner/code changes.</done>
</task>

<task type="auto">
  <name>Task 2: live gate cycle — publish, prove refusal, pinned eval, activate</name>
  <files>packages/backend/convex/llm.ts</files>
  <action>
    PRECONDITION: ONE persistent `npx convex dev` running for the whole task (per-command cold
    starts contaminate the runner — 03.6-05; after any network blip kill all convex processes and
    restart one clean dev before re-running, or the run storms InternalServerErrors at $0).
    1. `npx convex run skills:seedSkills` — publish-on-change mints cockpit-agent candidate vN.
       Read N back (03.7-05 Task 2 step 1 procedure) — runtime fact, never hardcoded.
    2. Prove the gate: `npx convex run skills:activateSkill '{"name":"cockpit-agent","version":N}'`
       must REFUSE with EVAL_GATE (no evidence yet).
    3. `pnpm eval:golden --skill cockpit-agent@N` — all 19 fixtures live. Cap $1.00, expected
       ~$0.05-0.15. Exit 2 = environment, not failure; one-retry flake policy is the runner's.
    4. If fixture 19 reds because the per-turn context imperative beats the skill wording
       (RESEARCH Pitfall 5), the NAMED FALLBACK is softening llm.ts:516's context string from
       "tell the user to pick from the card, do NOT claim you added them" to a neutral state
       description ("the user has not picked yet — do not claim you added them") — a CONTEXT
       string, not a prompt (no §5 issue; llmRedaction scans unaffected); then re-run. Text-level
       re-announce looping is cosmetic and acceptable UNLESS it suppresses the tool calls fixture
       19 asserts. Do NOT activate on a red run; fix forward via skill wording first, llm.ts:516
       second. (llm.ts appears in files_modified ONLY for this fallback — if untouched, say so
       in the SUMMARY.)
    5. All green → evidence recorded → `activateSkill` vN succeeds. Confirm the active version.
  </action>
  <verify>
    <automated>pnpm eval:golden --skill cockpit-agent@N</automated>
  </verify>
  <done>All-green pinned run (run-id + cost recorded); activateSkill refused pre-evidence and succeeded post-evidence; cockpit-agent vN is the live active skill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live UAT transcript replay + owed E2Es + phase close</name>
  <files>.planning/phases/03.10-cockpit-conversation-repair/03.10-VALIDATION.md</files>
  <action>Present the verification steps below and PAUSE. On approval: mark the two Manual-Only rows in 03.10-VALIDATION.md verified and record the sign-off in the plan SUMMARY. Do not proceed without the resume signal.</action>
  <what-built>Both UAT defects fixed: composition-aware BriefingCard demotion (plan 02) and the gate-activated stalled-resolution recovery skill (Tasks 1-2), plus the gmail.search fixture seam powering fixture 19.</what-built>
  <how-to-verify>
    With `npx convex dev` + `pnpm dev` running and your real Gmail connected:
    1. Fresh thread → "What happened in my inbox?" → briefing renders (brief is the top card — nothing composed yet).
    2. "Draft a reply to Brett J. Fox." (the exact name — fuzzy matching is deferred) → the ResolutionCard must now be the FIRST card in the workspace panel, visible WITHOUT scrolling; the brief sits demoted below it, still readable.
    3. WITHOUT picking, type: "Subject: Workshop. Tell him I will attend the workshop." → the agent must set the subject AND draft the body while the pick stays open, and mention the panel at most once, with concrete words — no "ready to pick" loop, no re-asking for the subject.
    4. Complete the pick in the panel → the PLAN card must show the picked NAMED recipient — never "#1 (no name)". Stop at Approve (approve/send optional, your call).
    5. Same session (connected user satisfies the standing E2E blockers): run the owed live specs — `pnpm --filter @pikar/web test:e2e -- cockpit-briefing cockpit-activity cockpit-resolve` (plus cockpit-demote if plan 02 created it).
    6. Confirm zero unintended sends and no mailbox writes beyond your own actions.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>User replies "approved" after walking the steps on the live deployment</verify>
  <done>Human sign-off recorded; manual-only VALIDATION rows checked; owed live E2Es run in the connected session; phase ready for /gsd:verify-work</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- skills` green (drift byte-identity); self-check exit 0 with 19 fixtures; check-playbooks exit 0.
- Live: all-green `pnpm eval:golden --skill cockpit-agent@N`; refused-then-succeeded activateSkill sequence observed; cockpit-agent vN active.
- Human-verify approved: picker-on-top, stall absorbed (subject+body set pre-pick), single panel pointer, named recipient on the final PLAN.
- No new mutations, no proposePlan/executePlan seam changes (checker flags any).
</verification>

<success_criteria>
The stalled-resolution conversation is fixed live and gate-proven: fixture 19 green under the pinned run, the candidate activated only through the gate, and the verbatim UAT transcript replays cleanly end to end — phase 3.10 complete, unblocking 3.11.
</success_criteria>

<output>
After completion, create `.planning/phases/03.10-cockpit-conversation-repair/03.10-03-SUMMARY.md`
</output>
