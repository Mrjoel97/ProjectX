---
phase: 03.10-cockpit-conversation-repair
verified: 2026-07-19T15:31:45Z
status: passed
score: 7/7 plans verified (33/33 must-have truths across UAT-A through UAT-F)
---

# Phase 03.10: Cockpit Conversation Repair Verification Report

**Phase Goal:** Repair the cockpit conversation defects surfaced by live UAT (2026-07-19) — UAT-A
(panel desync), UAT-B (stalled-resolution recovery), UAT-C (propose-while-pending deadlock +
collapsible brief), UAT-D (missing reset tool / confabulated cancel / re-announce loop), UAT-E
(conversational amnesia), UAT-F (post-pick distrust + fabricated-recipient safety hole + briefing
count inconsistency).
**Verified:** 2026-07-19T15:31:45Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Defect | Truth | Status | Evidence |
|---|--------|-------|--------|----------|
| 1 | UAT-B prereq | `gmail.search` returns fixture-derived HeaderRecords tokenlessly on a seeded eval tenant; live tenants byte-unchanged | ✓ VERIFIED | `gmail.ts:267-280` fixture-first branch (after SMOKE::, before freshAccessToken), reuses `internal.smoke.getInboxFixture`, same refs-only audit shape. `gmail.test.ts` 28/28 green |
| 2 | UAT-A | ResolutionCard renders ABOVE the demoted BriefingCard the moment composition is active; brief never destroyed | ✓ VERIFIED | `cards.tsx:1111-1129` `composing` boolean; `{!composing && briefing}` first, `{composing && briefing}` (demoted) last |
| 3 | UAT-A | Briefing-only flow (collecting, nothing set) keeps brief primary, SC-4 zero-button/a intact | ✓ VERIFIED | `cockpit-briefing.spec.ts` byte-untouched (per 03.10-02-SUMMARY); `briefing-body` testid scoping in `cockpit-resolve.spec.ts:179-180` |
| 4 | UAT-B | While a pick is pending, agent still absorbs subject/body via setSubject/draftBody (fixture 19: candidatesPending+subjectPresent+bodyPresent, recipientCount:0, statusAtMost:collecting) | ✓ VERIFIED | `19-stalled-resolution-recovery.json` on disk, matches spec exactly; skill section "## While a pick is pending" (`cockpit-agent.md:120-150`) |
| 5 | UAT-C | Backend refuses `proposePlan` while a pick is pending (refuse-and-instruct, no proposeEmailPlan call) | ✓ VERIFIED | `llm.ts:1087-1092` `if (plan.candidates?.length) return "Can't propose yet…"` — before the attachment gate |
| 6 | UAT-C | Frontend never suppresses the picker even at status `proposed`; unpickable "#1 (no name)" PlanCard withheld while candidates parked | ✓ VERIFIED | `cards.tsx:1105` `resolving = Boolean(plan.candidates?.length) && !reporting` (no status clause); `cards.tsx:1123` PlanCard gated on `!plan.candidates?.length` |
| 7 | UAT-C | BriefingCard collapses to its masthead when composing, re-expands via toggle; SC-4 holds on `briefing-body` content | ✓ VERIFIED | `cards.tsx:832-901` `useState(demoted)`, `data-testid="briefing-body"`, aria-expanded toggle; E2E `cockpit-resolve.spec.ts:166-180` |
| 8 | UAT-D | `resetPlan` mutation clears every composition slot explicitly (patchPlan can't — drops undefined) | ✓ VERIFIED | `plans.ts:178-198` explicit patch of all 12 slot fields including `recipientNames` (added later by plan 07) |
| 9 | UAT-D | `resetPlan` tool in buildCockpitTools calls the mutation, refuses past-proposed | ✓ VERIFIED | `llm.ts:884-903` (tool wired; status guard present) |
| 10 | UAT-D | Pending-pick context is a NEUTRAL statement, not an imperative | ✓ VERIFIED | `llm.ts:534` "A contact pick is still open (…the user has not picked from the card yet):" |
| 11 | UAT-D | Skill v9 active only after gate cycle (refuse pre-evidence, succeed post-evidence); fixture 20 proves reset clears an accreted subject/body | ✓ VERIFIED | `20-reset-and-honesty.json` on disk; gate cycle logged in 03.10-05-SUMMARY (v9, run 7940a9a0, 20/20, $0.0791) |
| 12 | UAT-E | Model prompt is no longer turn-one-only: both drivers fetch history, loop renders bounded "Conversation so far" block above plan context | ✓ VERIFIED | `llm.ts:566` `buildHistoryBlock`; `llm.ts:1722` prompt assembly; `cockpit.ts:148-165` `fetchRecentHistory` called at `:70` (sendCockpitMessage, before save) and `:247` (resolveRecipients) |
| 13 | UAT-E | Bare fragment answer absorbed into the right slot (fixture 21: "Meeting reminder" → subjectPresent:true, bodyPresent kept, recipientCount:0) | ✓ VERIFIED | `21-fragment-answer-absorbed.json` on disk, matches spec exactly |
| 14 | UAT-E | Eval-runner accumulates history for production parity (runner calls `runCockpitAgent` directly, never `sendCockpitMessage`) | ✓ VERIFIED | `run-eval-golden.mjs:295-321` history accumulation from turn text + `res.reply` |
| 15 | UAT-E | All 21 fixtures green under the pinned run (history breaks nothing) | ✓ VERIFIED | 03.10-06-SUMMARY: run 27efb84d, 21/21, $0.0973 (after documented 361d5769 storm, never activated on red) |
| 16 | UAT-E | Skill v10 active only through the gate; new transcript-reading section | ✓ VERIFIED | `cockpit-agent.md:30-43` "## Read the conversation, not just the last line" |
| 17 | UAT-F1 | Completed picks visible to model: `recipientNames` persisted + rendered (`#1: Brett J. Fox`, never "#1 (no name)" for a picked contact) | ✓ VERIFIED | `schema.ts`/`plans.ts:87` `recipientNames` field; `cockpit.ts:221-227` fold merges displayNames; `llm.ts:450,485,502` buildAgentContext renders via `buildRecipientView` |
| 18 | UAT-F2 | Fabricated-recipient overwrite structurally impossible: recipient-mutating tools absent on the continue turn | ✓ VERIFIED | `llm.ts:610,810,817` conditional spread omits `addRecipients`/`setRecipients`/`removeRecipient` under `omitRecipientEdits`; `cockpit.ts:261` `omitRecipientEdits: true` passed only by `resolveRecipients` |
| 19 | UAT-F2 | `RESOLUTION_CONTINUE` states the fold happened, forbids invented subject/body | ✓ VERIFIED | `cockpit.ts:138-141` reworded string |
| 20 | UAT-F | Skill v11 active only through the gate; "After a pick completes" section + scoped distrust clause | ✓ VERIFIED | `cockpit-agent.md:152-167`; live `npx convex run skills:getActiveSkill` confirms `"version": 11` with byte-matching body |
| 21 | UAT-F3 | Briefing sentence and card masthead share ONE predicate (`isNeedsYou`), eliminating count contradictions | ✓ VERIFIED | `packages/core/src/briefing.ts:191` `export function isNeedsYou`; `llm.ts:1265-1268` `items.filter(isNeedsYou)` feeding the same numbers the card reads |
| 22 | UAT-F | All 21 fixtures still green post-change (withholding/recipientNames never fire on runner-driven turns) | ✓ VERIFIED | 03.10-07-SUMMARY: run 39d0c4e2, 21/21, $0.0871 |
| 23 | All | Human sign-off recorded for the full live-replay transcript covering UAT-A through UAT-F | ✓ VERIFIED | 03.10-VALIDATION.md status: resolved, human_signoff: 2026-07-19; STATE.md corroborates verbatim approval quote |

**Score:** 23/23 observable truths verified (spanning all 7 plans' must_haves)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/backend/convex/gmail.ts` | fixture-first `search` branch | ✓ VERIFIED | Lines 267-280, wired to `internal.smoke.getInboxFixture` |
| `apps/web/app/(app)/dashboard/workspace/cards.tsx` | composing demotion + status-independent `resolving` + collapsible brief | ✓ VERIFIED | Lines 832-901 (collapse), 1105-1129 (guards) |
| `packages/contracts/skills/cockpit-agent.md` | v11 body (11 sections incl. "While a pick is pending" + "After a pick completes") | ✓ VERIFIED | 12 `##` sections present; content matches all seams |
| `packages/contracts/src/skills/cockpitAgent.ts` | byte-identical LF-normalized mirror | ✓ VERIFIED | `skills.test.ts` drift test.each green (31/31 tests) |
| `packages/backend/convex/llm.ts` | propose-guard, resetPlan tool, neutral statement, buildHistoryBlock, omitRecipientEdits, briefInbox parity | ✓ VERIFIED | All greps confirm presence and correct placement (redaction-scan bounds respected — llmRedaction green except the documented pre-existing tally red) |
| `packages/backend/convex/plans.ts` | resetPlan mutation clearing all slots incl. recipientNames | ✓ VERIFIED | Lines 178-198 |
| `packages/backend/convex/cockpit.ts` | fetchRecentHistory (fail-open), reworded RESOLUTION_CONTINUE, fold persists recipientNames, omitRecipientEdits passed | ✓ VERIFIED | Lines 70, 107(catch), 138-165, 221-227, 261 |
| `packages/core/src/briefing.ts` | `isNeedsYou` exported | ✓ VERIFIED | Line 191 |
| `packages/backend/scripts/eval-cases/{19,20,21}.json` | UAT-B/D/E proof fixtures | ✓ VERIFIED | All three on disk, content matches plan specs exactly |
| `packages/backend/scripts/run-eval-golden.mjs` | history accumulation (production parity) | ✓ VERIFIED | Lines 295-321 |
| `apps/web/e2e/cockpit-resolve.spec.ts` | demotion-order + proposed-parked + collapse/SC-4 assertions | ✓ VERIFIED | 3 Playwright cases discovered incl. `UAT-A` and `UAT-C` tagged tests |
| `docs/playbooks/{cockpit,agent-runtime,skill-registry}.md` | Last verified bumped to 03.10-07, same-commit discipline (§9) | ✓ VERIFIED | `check-playbooks.mjs` exit 0; all three carry "Last verified: 2026-07-19 (03.10-07)" |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `gmail.ts` search handler | `internal.smoke.getInboxFixture` | fixture-before-token branch | ✓ WIRED | Confirmed in source, ordered after SMOKE:: sentinel |
| `cards.tsx` PlanCards | `plan.candidates`/`subject`/`body`/`recipients`/`status` | render-side `composing`/`resolving` derivation | ✓ WIRED | No new field/mutation/query — pure render logic |
| `19-stalled-resolution-recovery.json` | `gmail.search` fixture seam | turn 1 "Draft an email to Sarah Chen." | ✓ WIRED | Fixture content matches; seam confirmed live (03.10-05/06/07 gate runs all green) |
| `cockpit-agent.md` | skills registry (seedSkills→eval:golden→activateSkill) | 3.6 gate cycle | ✓ WIRED | Live `getActiveSkill` query confirms version 11 active, body byte-identical to file |
| `llm.ts proposePlan.execute` | parked candidates on the plan row | name-only `PlanRow` widening | ✓ WIRED | `candidates?.length` check at line 1091, before attachment gate |
| `cards.tsx resolving` | `ResolutionCard` | status-independent boolean | ✓ WIRED | Confirmed no `status !== "proposed"` clause remains |
| `cards.tsx BriefingCard` | local `useState(demoted)` | masthead toggle button | ✓ WIRED | No plan field added — pure local UI state |
| `llm.ts` (resetPlan tool) | `plans.ts` (resetPlan mutation) | `ctx.runMutation(internal.plans.resetPlan, …)` | ✓ WIRED | Confirmed at `llm.ts:900` |
| `cockpit.ts` (sendCockpitMessage + resolveRecipients) | `llm.ts` (runCockpitAgent) | optional `history` arg | ✓ WIRED | Both call sites pass `history` (`:70`/`:93` and `:247`/`:254`) |
| `cockpit.ts` (fetchRecentHistory) | `@convex-dev/agent` listMessages | reused UI-pagination read | ✓ WIRED | Fail-open `catch` confirmed (hotfix b9da09b) |
| `run-eval-golden.mjs` (attemptCase) | `llm:runCockpitAgent` | accumulated history arg | ✓ WIRED | Confirmed lines 295-321 |
| `cockpit.ts` (resolveRecipients fold) | `plans.ts` (patchPlan) | `recipientNames` map from `picks[].displayName` | ✓ WIRED | `cockpit.ts:221-227` |
| `llm.ts` (buildAgentContext) | `@pikar/core buildRecipientView` | displayName lookup by lowercased address | ✓ WIRED | `llm.ts:502` |
| `cockpit.ts` (resolveRecipients runAction) | `llm.ts` (runCockpitAgent→runAgentLoop→buildCockpitTools) | `omitRecipientEdits` append-only flag | ✓ WIRED | Threaded through `:610`, `:1350`, `:1363/1365`, `:1623/1637/1676/1728`; only `cockpit.ts:261` sets it true |
| `llm.ts` (briefInbox return) | `@pikar/core isNeedsYou` + `cards.tsx` masthead | one exported predicate over the same items array | ✓ WIRED | `llm.ts:1265` `items.filter(isNeedsYou)`; `briefing.ts:191` export |

### Requirements Coverage

No REQUIREMENTS.md IDs are minted for this phase (confirmed by grep — zero UAT-A..F hits in `.planning/REQUIREMENTS.md`). Per the phase brief, UAT-A through UAT-F are phase-local gate IDs tracked in the plan frontmatters and `03.10-VALIDATION.md`. All six are accounted for:

| Gate ID | Plan(s) | Status | Evidence |
|---------|---------|--------|----------|
| UAT-A | 03.10-02 | ✓ SATISFIED | Panel demotion in `cards.tsx`; offline E2E + human-verify approved |
| UAT-B | 03.10-01, 03.10-03 | ✓ SATISFIED | gmail.search seam + fixture 19 + skill "While a pick is pending" + gate-activated |
| UAT-C | 03.10-04 | ✓ SATISFIED | proposePlan guard + status-independent picker + collapsible brief |
| UAT-D | 03.10-05 | ✓ SATISFIED | resetPlan mutation+tool + neutral statement + fixture 20 + v9 gate |
| UAT-E | 03.10-06 | ✓ SATISFIED | buildHistoryBlock + both drivers + eval parity + fixture 21 + v10 gate |
| UAT-F | 03.10-07 | ✓ SATISFIED | recipientNames + omitRecipientEdits + reworded RESOLUTION_CONTINUE + isNeedsYou + v11 gate |

No orphaned requirements found.

### Anti-Patterns Found

None blocking. Two pre-existing, documented, out-of-scope test reds were re-confirmed at HEAD and are explicitly excluded from this phase's scope per the verification brief:

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `packages/backend/convex/audit.test.ts` | pre-existing `auditCounts` red | ℹ️ Info (documented non-regression) | Unrelated to phase 03.10; long-standing per STATE.md |
| `packages/backend/convex/llmRedaction.test.ts` | `cockpit.ts audit-tally 1-vs-2` (03.5-05's `reschedulePlan` added a 2nd `audit.log` call site, scan never bumped) | ℹ️ Info (documented deferred item) | Logged in `deferred-items.md` items 1 (found) and 1 (close-out); fix owed to whichever plan next touches `llmRedaction.test.ts`, explicitly deferred by this phase's plans |

No stub patterns (`return null`, empty handlers, TODO/FIXME, hardcoded skill prompts) found in any of the phase's touched files. No raw `query`/`mutation`/`action` imports introduced. No audit-mutating functions added. No new pinned-component version bumps.

### Human Verification Required

None outstanding — human sign-off is RECORDED (2026-07-19, `03.10-VALIDATION.md` status: `resolved`, `human_signoff: 2026-07-19`). The owner replayed the full UAT transcript live across four successive rounds (initial + gap-closure replays that surfaced UAT-C/D/E/F) and approved verbatim: *"I'm satisfied. I've tested everything the way you asked me to test. Everything is good. I approve."* Zero unintended sends confirmed across all replays. This verification additionally confirmed the code-level truth of every must-have and independently queried the live deployment (`npx convex run skills:getActiveSkill`) to reconfirm cockpit-agent v11 is the active skill with a body byte-identical to `cockpit-agent.md`.

### Gaps Summary

No gaps found. All 7 plans' must_haves (truths, artifacts, key_links) verified against the actual codebase:

- Automated test suites match documented expectations: `skills.test.ts` 31/31, `cockpitTools.test.ts` 47/47, `packages/core` 125/125, all green; the only two backend test reds are the pre-existing, phase-external, explicitly-documented non-regressions (audit.test.ts, llmRedaction cockpit.ts tally).
- `node scripts/run-eval-golden.mjs --self-check` passes with all 21 fixtures valid.
- `node scripts/check-playbooks.mjs` exits 0 — all three watched playbooks (cockpit.md, agent-runtime.md, skill-registry.md) carry "Last verified: 2026-07-19 (03.10-07)".
- `pnpm --filter @pikar/web exec tsc --noEmit` exits 0; Playwright discovers all three cockpit-resolve.spec.ts cases including the UAT-A demotion and UAT-C proposed-parked regression tests.
- Live production skill registry independently queried and confirmed: cockpit-agent v11 active, body byte-identical to the committed `.md`.
- One minor documentation-completeness note (not a goal-achievement gap): plan `03.10-03-PLAN.md` has no corresponding `03.10-03-SUMMARY.md` on disk — its work (skill mirror edit + fixture 19 + gate cycle, commit `509925b`) shipped and is fully accounted for in `03.10-VALIDATION.md`'s task table and cross-referenced by every subsequent plan's SUMMARY, so this does not affect goal achievement; flagging only for phase-closure bookkeeping.
- Known documented non-regressions (per the verification brief) were not flagged as gaps: `audit.test.ts` pre-existing red; `llmRedaction` cockpit.ts audit-tally 1-vs-2; eval fixture 22 deliberately skipped; reply-grounding rebuild owner-deferred.

---

*Verified: 2026-07-19T15:31:45Z*
*Verifier: Claude (gsd-verifier)*
