---
phase: 03.10-cockpit-conversation-repair
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: ["UAT-C"]
files_modified:
  - packages/backend/convex/llm.ts
  - packages/backend/convex/cockpitTools.test.ts
  - apps/web/app/(app)/dashboard/workspace/cards.tsx
  - apps/web/e2e/cockpit-resolve.spec.ts
  - docs/playbooks/cockpit.md

must_haves:
  truths:
    - "Backend refuses to propose while a pick is pending: with parked candidates, proposePlan returns a refuse-and-instruct string and does NOT flip status→proposed (cockpitTools.test.ts asserts the refusal + that proposeEmailPlan was never called)"
    - "Frontend never suppresses the picker while candidates are parked: even at status 'proposed', resolving is true and the ResolutionCard renders, and the unpickable '#1 (no name)' PlanCard is NOT shown while candidates exist"
    - "The BriefingCard collapses to its teal masthead strip when composition is active and re-expands via a header toggle; collapse is local UI (useState) — NO new plan field / mutation / query"
    - "SC-4 holds: the briefing CONTENT region (data-testid briefing-body) contains zero email-actionable controls (button/link/onClick); the collapse toggle is masthead chrome OUTSIDE briefing-body — the E2E asserts zero buttons/links within briefing-body"
    - "cockpit.md documents BOTH the propose-pending-pick guard invariant AND the SC-4 collapse-chrome exception, same commit (§9 — llm.ts and cards.tsx are cockpit.md-watched)"
  artifacts:
    - path: "packages/backend/convex/llm.ts"
      provides: "proposePlan pending-pick precondition (refuses while plan.candidates?.length)"
      contains: "pick is still pending"
    - path: "apps/web/app/(app)/dashboard/workspace/cards.tsx"
      provides: "status-independent resolving guard + BriefingCard collapse (demoted prop, useState toggle, briefing-body testid)"
      contains: "briefing-body"
    - path: "apps/web/e2e/cockpit-resolve.spec.ts"
      provides: "proposed+parked picker-visible regression + collapse/expand + SC-4 briefing-body assertions"
      contains: "briefing-body"
    - path: "docs/playbooks/cockpit.md"
      provides: "propose-guard invariant + collapse-chrome SC-4 exception, Last verified bumped 03.10-04"
      contains: "03.10-04"
  key_links:
    - from: "packages/backend/convex/llm.ts proposePlan.execute"
      to: "the parked candidates on the plan row"
      via: "a name-only PlanRow widening (candidates?: { name: string }[]) so the count read type-checks WITHOUT re-adding the matches/address shape the header-hint redaction scan rejects"
      pattern: "candidates\\?\\.length"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx resolving"
      to: "ResolutionCard"
      via: "resolving = Boolean(plan.candidates?.length) && !reporting (drops the status !== 'proposed' clause)"
      pattern: "plan\\.candidates\\?\\.length"
    - from: "apps/web/app/(app)/dashboard/workspace/cards.tsx BriefingCard"
      to: "local collapsed UI state"
      via: "useState(demoted) toggled by a masthead chrome button — no plan field"
      pattern: "useState"
---

<objective>
Close UAT-C (live 2026-07-19): the propose-while-pending DEADLOCK and the tall-brief CLUTTER. Two
defects, defense-in-depth. Backend: proposePlan REFUSES while a contact pick is still parked, so the
"proposed plan + unpicked candidate ⇒ picker vanishes" state is unreachable for every reader. Frontend:
the picker is never suppressed while candidates exist (even at status 'proposed'), and the demoted
BriefingCard is upgraded from "moved below" to "collapsed to its masthead with a manual expand toggle".

Purpose: the transcript's dead-end — a "#1 (no name)" plan proposed over a still-open pick, with the
picker gone and the tall brief burying everything.
Output: the propose guard + its refusal test, the status-independent render guards + collapsible brief,
the owed E2E assertions, and the two documented cockpit.md invariants.
</objective>

<execution_context>
@C:/Users/expert/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/expert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-cockpit-conversation-repair/03.10-CONTEXT.md
@.planning/phases/03.10-cockpit-conversation-repair/03.10-RESEARCH.md
@packages/backend/convex/llm.ts
@apps/web/app/(app)/dashboard/workspace/cards.tsx
@apps/web/e2e/cockpit-resolve.spec.ts
@docs/playbooks/cockpit.md

<interfaces>
BACKEND — the propose gate (llm.ts ~976-1019). `proposePlan.execute` already refuses on
zero-recipients / no-subject / no-body / attachmentError / over-cap / personalization⊗group, each
returning a refuse-and-instruct string BEFORE the `internal.cockpit.proposeEmailPlan` write. It NEVER
checks the parked pick. Add ONE sibling precondition after the body check (~995) and before the
attachment gate. `const plan = await readPlan()` returns the `PlanRow` type — which (llm.ts:450-453)
deliberately does NOT declare `candidates`, because declaring the `matches`/address shape in that span
trips the draftCockpit header-hint redaction scan. `getById` returns the FULL row at runtime, so the
count IS present; widen `PlanRow` with a NAME-ONLY field `candidates?: { name: string }[]` (no `matches`,
no address/displayName — that keeps the scan green) so `plan.candidates?.length` type-checks. Structural
facts come from the ROW, never model args (DECISION #2).

FRONTEND — PlanCards (cards.tsx 1067-1095). Today:
```
const resolving = Boolean(plan.candidates?.length) && plan.status !== "proposed" && !reporting;
const composing = Boolean(plan.candidates?.length || plan.subject || plan.body || plan.recipients?.length) || plan.status !== "collecting";
...
{!composing && brief}
{resolving && <ResolutionCard plan={plan} threadId={threadId} />}
{plan.status === "proposed" && <PlanCard plan={plan} threadId={threadId} />}
...
{composing && brief /* demoted below the work */}
```
Two deadlock guards: (1) drop the `plan.status !== "proposed"` clause from `resolving` so the picker
survives a plan that got proposed with a pick still open; (2) gate the PlanCard on
`plan.status === "proposed" && !plan.candidates?.length` so the unpickable "#1 (no name)" placeholder is
never shown while candidates are parked. Post-guard exactly one card — the picker — renders in that state.
`Plan` (cards.tsx:37) is `NonNullable<FunctionReturnType<typeof api.plans.byThread>>`, so `candidates`,
`pendingValid`, and `status` are all already on it — no type work frontend-side.

BRIEFING COLLAPSE — BriefingCard (cards.tsx 833-926). The masthead is the self-contained teal-900 band
(lines 850-871): label "Inbox Briefing", the `scope` line, three `Kpi` counts. The content lives in ONE
padded div starting line 873. Add `demoted?: boolean`; `const [collapsed, setCollapsed] = useState(demoted ?? false)`.
When `collapsed`, render the masthead band ONLY (skip the content div). Put a toggle button ON the masthead
(aria-expanded + aria-label, BRAND §6 not colour-alone), reusing a brand token — it is VIEW CHROME, not an
action on email content, so SC-4 holds. Tag the content div `data-testid="briefing-body"` so the E2E can
assert "zero actionable controls inside the briefing CONTENT" while allowing the masthead toggle. The
demoted BriefingCard only mounts when `composing` is already true (the bottom slot), so `useState(demoted)`
is correct at mount with no effect — a fresh mount per slot. Thread it by giving PlanCards the `briefing`
data (not a pre-built `brief` ReactNode) so it can render `<BriefingCard briefing={briefing} demoted />` in
the composing slot and `<BriefingCard briefing={briefing} />` (expanded, primary) in the non-composing slot;
CardList's `plan === null` branch renders `<BriefingCard briefing={briefing} />` directly. NO new plan
field/mutation/query — ADR-004 dumb renderer; collapse is per-session local UI only. BRAND §10: existing
tokens, match the masthead.

E2E — cockpit-resolve.spec.ts. The existing demotion test (line 122) and its SC-4 re-assert
(`briefCard.locator("button").toHaveCount(0)`, lines 166-167) will BREAK once the masthead toggle exists —
rescope those to `briefCard.getByTestId("briefing-body").locator("button"/"a")` (zero inside the content;
the toggle lives outside it). These specs drive the OFFLINE `SMOKE::agent::` sentinels but the composer is
gated on `gmailAuth.status.connected` (the standing phase blocker) — the live RUN is owed to a connected
session (03.10-03's human-verify runs cockpit-resolve); the automated gate here is web typecheck +
Playwright discovery + the backend refusal test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backend proposePlan pending-pick guard + refusal test + playbook</name>
  <files>packages/backend/convex/llm.ts, packages/backend/convex/cockpitTools.test.ts, docs/playbooks/cockpit.md</files>
  <behavior>
    - With plan.candidates parked (a pick still open) AND recipients+subject+body otherwise set,
      proposePlan returns a refuse-and-instruct string and does NOT call internal.cockpit.proposeEmailPlan
      (plan stays out of 'proposed').
    - With NO candidates parked and recipients+subject+body set, proposePlan still proposes (control — the
      guard is scoped to the pending pick, no regression of the happy path).
  </behavior>
  <action>
    llm.ts: widen the PlanRow type with a NAME-ONLY `candidates?: { name: string }[]` (NOT the matches/
    address shape — that trips the draftCockpit header-hint redaction scan; the note at ~450-453 explains
    why). In proposePlan.execute, add a sibling precondition AFTER the `!plan.body` check (~995) and BEFORE
    the attachmentError gate:
      `if (plan.candidates?.length) return "Can't propose yet — a contact pick is still pending; tell the
       user to pick the contact from the card first.";`
    Mirrors the existing refuse-and-instruct pattern; refuses BEFORE the proposeEmailPlan write so the bad
    state is unreachable for every reader (backend owns the invariant; the frontend guard in Task 2 is
    defense-in-depth). No new mutation, no proposeEmailPlan/executePlan seam change.
    cockpitTools.test.ts: add ONE case to the existing proposePlan-gate suite (the home of the
    attachmentError/zero-recipient refusal tests) asserting the pending-pick refusal + that
    proposeEmailPlan was never invoked, plus the no-candidates control still proposes. Reuse the existing
    convex-test tool-build harness — no new fixtures.
    docs/playbooks/cockpit.md: add a Last verified (03.10-04) note documenting the propose-pending-pick
    invariant (propose refuses while candidates are parked; backend owns it, name-only widening avoids the
    redaction scan), same commit (§9 — llm.ts is cockpit.md-watched).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/backend test -- cockpitTools && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Refusal test + control green; proposeEmailPlan not called on a parked pick; header-hint redaction scan still green (name-only widening); playbook bumped; no new mutation/seam change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: frontend deadlock render guards + collapsible brief + E2E + playbook</name>
  <files>apps/web/app/(app)/dashboard/workspace/cards.tsx, apps/web/e2e/cockpit-resolve.spec.ts, docs/playbooks/cockpit.md</files>
  <behavior>
    - Proposed plan WITH parked candidates → the ResolutionCard (picker) is still visible and the
      unpickable "#1 (no name)" PlanCard is NOT rendered (exactly one card: the picker).
    - Composition active → the BriefingCard renders collapsed to its masthead (content hidden); clicking
      the masthead toggle expands it to full content and back.
    - SC-4: the briefing CONTENT region (data-testid briefing-body) has zero buttons/links; the masthead
      toggle (outside briefing-body) is allowed chrome.
  </behavior>
  <action>
    cards.tsx — PlanCards (1067-1095): (1) `resolving = Boolean(plan.candidates?.length) && !reporting`
    (drop the `plan.status !== "proposed"` clause); (2) gate the PlanCard render on
    `plan.status === "proposed" && !plan.candidates?.length`. Change PlanCards to take `briefing` (data)
    instead of the pre-built `brief` ReactNode so it can pass the demoted flag: render
    `<BriefingCard briefing={briefing} />` (expanded/primary) in the `!composing` slot and
    `<BriefingCard briefing={briefing} demoted />` in the `composing` slot; update CardList (1053-1055) to
    pass `briefing` and render `<BriefingCard briefing={briefing} />` directly in the `plan === null`
    branch (1054). BriefingCard (833): add `demoted?: boolean`, `const [collapsed, setCollapsed] =
    useState(demoted ?? false)`; when collapsed render ONLY the masthead band, else the full card; add a
    masthead toggle button (aria-expanded, aria-label "Collapse/Expand briefing", existing token, no
    amber — BRAND §2/§6) and tag the content div (873) `data-testid="briefing-body"`. NO new plan
    field/mutation/query (ADR-004). Do NOT touch pane scrolling — the backend guard is the real fix; the
    ActivityCard "Latest trace" still renders above the PlanCards grid by design (CardList) and is out of
    scope here.
    cockpit-resolve.spec.ts: (a) extend the demotion test — after candidates park, assert the demoted brief
    is COLLAPSED (briefing-body absent/hidden) and the masthead toggle expands it (briefing-body visible)
    then collapses again; (b) rescope the SC-4 assertions (166-167) to
    `briefCard.getByTestId("briefing-body").locator("button"/"a").toHaveCount(0)` (content stays inert; the
    toggle is allowed); (c) add a proposed+parked-candidates regression: drive the plan to `proposed` with a
    candidate still parked and assert the picker ("PICK A CONTACT") stays visible and no "no name" placeholder
    PlanCard shows. Assertions ride plan-row-derived cards, never reply prose (file convention).
    docs/playbooks/cockpit.md: add a Last verified (03.10-04) note — the status-independent picker guard,
    the collapse upgrade (demotion → masthead collapse, local useState, no new field), and the SC-4
    collapse-chrome EXCEPTION (toggle is masthead chrome; SC-4 now asserted on briefing-body), same commit
    (§9 — cards.tsx is cockpit.md-watched).
  </action>
  <verify>
    <automated>pnpm --filter @pikar/web exec tsc --noEmit && pnpm --filter @pikar/web test:e2e -- cockpit-resolve --list && node scripts/check-playbooks.mjs</automated>
  </verify>
  <done>Web typecheck exit 0; Playwright discovers the updated/new cockpit-resolve cases; cards.tsx Biome baseline unchanged; playbook bumped. Live run of the specs is owed to the connected human-verify session (standing composer-gate blocker).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @pikar/backend test -- cockpitTools` green: propose refuses on a parked pick (proposeEmailPlan not called) + no-candidates control still proposes; header-hint redaction scan still green.
- `pnpm --filter @pikar/web exec tsc --noEmit` exit 0; Playwright discovers the cockpit-resolve cases; SC-4 assertions rescoped to briefing-body.
- `node scripts/check-playbooks.mjs` exit 0 — cockpit.md carries both new invariants, Last verified 03.10-04, same commit.
- No new mutation/query/plan field; proposePlan/executePlan seam untouched (checker flags any).
</verification>

<success_criteria>
The propose-while-pending deadlock is closed at the backend invariant AND the frontend render, and the
demoted brief collapses to its masthead with a manual expand toggle while SC-4 holds on its content —
UAT-C fixed with the smallest correct diffs (one backend guard, two frontend guards, one collapse toggle),
both invariants documented. Live paint folds into the connected human-verify session.
</success_criteria>

<output>
After completion, create `.planning/phases/03.10-cockpit-conversation-repair/03.10-04-SUMMARY.md`
</output>
