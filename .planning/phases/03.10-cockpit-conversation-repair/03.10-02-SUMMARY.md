---
phase: 03.10-cockpit-conversation-repair
plan: 02
subsystem: ui
tags: [react, cockpit, cards, playwright, e2e, uat]

# Dependency graph
requires:
  - phase: 03.10-cockpit-conversation-repair
    provides: "wave 1 (03.10-01) — phase sequencing only; this plan touches no backend file"
  - phase: 03.7-inbox-briefing
    provides: "BriefingCard + the SC-4 zero-button/a structural guarantee + cockpit-briefing.spec.ts's seeding convention (JWT-sub tenantId + no-shell convexRun)"
  - phase: 03.2.1-cockpit-agent
    provides: "ResolutionCard + the SMOKE::agent::resolve offline grammar the demotion E2E drives"
provides:
  - "PlanCards composing boolean (candidates OR subject OR body OR recipients OR status past collecting) — render-side only, no new field/mutation/query"
  - "BriefingCard demotion: {!composing && brief} first, {composing && brief} LAST in the same grid — the picker is the top card of the PlanCards grid the moment candidates exist"
  - "offline DOM-order regression lock in cockpit-resolve.spec.ts (picker precedes the demoted brief; brief attached; SC-4 re-asserted on the demoted card)"
affects: [03.10-03, cockpit-ui]

# Tech tracking
tech-stack:
  added: []
  patterns: ["compareDocumentPosition DOM-order assertion via element handles (order, not layout — viewport-independent)"]

key-files:
  created: []
  modified:
    - apps/web/app/(app)/dashboard/workspace/cards.tsx
    - apps/web/e2e/cockpit-resolve.spec.ts
    - docs/playbooks/cockpit.md

key-decisions:
  - "Reorder only, no collapse/<details> — the laziest mechanic that meets the owner's done-condition (RESEARCH OQ3); revisit only if 03.10-03's human-verify says the demoted brief still crowds the panel"
  - "Extended cockpit-resolve.spec.ts rather than minting cockpit-demote.spec.ts — the seed plumbing coexists cleanly with the existing test (fresh thread per page.goto isolates them)"
  - "DOM-order asserted via compareDocumentPosition on element handles, not bounding boxes — order is the invariant, layout is not"

patterns-established: []

requirements-completed: [UAT-A]

# Metrics
duration: 6min
completed: 2026-07-19
---

# Phase 03.10 Plan 02: Cockpit Panel Demotion Summary

**PlanCards demotes the BriefingCard below the plan cards the moment composition is active (one derived `composing` boolean, pure reorder), so the ResolutionCard is the top workspace card and a stalled pick is visible without scrolling — locked by an offline DOM-order E2E**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-07-18T23:46:11Z
- **Completed:** 2026-07-18T23:52:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Defect A (PANEL DESYNC, UAT 2026-07-19 "where is the list") fixed with 9 inserted lines in `PlanCards` only: `composing` derives from fields already on the `plan` prop (candidates / subject / body / recipients / status past `collecting`); `{!composing && brief}` stays first, `{composing && brief}` renders LAST in the same grid — demotion, never destruction
- Briefing-only flow untouched: plan at `collecting` with nothing set keeps the brief primary and fully rendered; `cockpit-briefing.spec.ts` byte-untouched (`git diff --stat` empty), SC-4's zero-button/a assertions structurally intact (nothing added inside `data-testid="briefing-card"`)
- Dispatch traps (Pitfall 2) verified dodged post-edit: `useQuery(api.agentSteps.latestTurn)` no-args at cards.tsx:1035, still above `!threadId` (:1050), still above `plan === null` (:1054); `<ActivityCard>` renders in CardList, outside PlanCards; the only cards.tsx diff is inside PlanCards
- New offline demotion-order test in `cockpit-resolve.spec.ts`: seed inbox fixture (`offlineDigest:true, baseMs:1577880000000`) → `SMOKE::agent::brief=today` (brief primary, visible) → `SMOKE::agent::resolve=SMOKE::Sarah` (candidates park) → picker PRECEDES the demoted brief in DOM order (`compareDocumentPosition`), brief still attached, zero `button`/`a` inside it (SC-4 re-assert)
- `cockpit.md`: new workspace-card-arbitration invariant bullet + Last verified entry (03.10-02), same commit as the spec

## Task Commits

1. **Task 1: composing-aware demotion in PlanCards** - `c4c3c15` (fix)
2. **Task 2: offline DOM-order E2E + playbook** - `c9ccce6` (test)

## Files Created/Modified
- `apps/web/app/(app)/dashboard/workspace/cards.tsx` - `composing` boolean + `{brief}` reorder in PlanCards (9 insertions, 1 deletion — the whole diff)
- `apps/web/e2e/cockpit-resolve.spec.ts` - demotion-order test + the briefing spec's seeding helpers (convexRun/resolveTenantId/SMOKE_NOW_MS, copied per convention)
- `docs/playbooks/cockpit.md` - arbitration invariant + Last verified (03.10-02)

## Decisions Made
- Reorder only — no collapse component, no new CSS, no `<details>` (deferred to the human-verify's taste call per RESEARCH OQ3)
- Extended the existing resolve spec instead of a new file; each test gets a fresh thread via `page.goto`, so the two scenarios cannot leak plan state into each other

## Deviations from Plan

None - plan executed exactly as written.

## Verification
- `pnpm --filter @pikar/web typecheck` exit 0 (both tasks)
- Biome stash-diff on cards.tsx: NO new findings — baseline unchanged (sole pre-existing `useHookAtTopLevel` at ResolutionCard:572)
- Playwright discovery: `cockpit-resolve.spec.ts:122 › demotion: brief primary → candidates park → picker precedes the demoted brief (UAT-A)` listed
- `cockpit-briefing.spec.ts` diff empty; `node scripts/check-playbooks.mjs` exit 0
- LIVE RUN stays blocked as planned (E2E_USER_EMAIL/PASSWORD absent; composer gated on `gmailAuth.status.connected` — gate NOT weakened): this plan proves type-load + discovery; the real paint is 03.10-03's human-verify

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Wave 2 complete. 03.10-03 (skill gate cycle + human-verify) can word the panel pointer correctly: post-fix the picker is the TOP card of the workspace panel (the CONTEXT's "below the briefing" example is now WRONG — RESEARCH Finding 7's reconciliation note)
- The human-verify session (connected user by construction) should run `pnpm --filter @pikar/web test:e2e -- cockpit-resolve` to land the live paint of both this demotion test and the pre-existing resolve flow
- Carry-forward unchanged from 03.10-01: the llmRedaction cockpit.ts audit-count scan 1→2 bump (deferred item) — this plan touched no backend file

## Self-Check: PASSED

---
*Phase: 03.10-cockpit-conversation-repair*
*Completed: 2026-07-19*
