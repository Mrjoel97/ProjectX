---
phase: 03.10-cockpit-conversation-repair
plan: 05
subsystem: agent-runtime
tags: [cockpit, convex, skills, eval-gate, redaction]

# Dependency graph
requires:
  - phase: 03.10-04
    provides: proposePlan pending-pick refusal + picker-survival render guards (the plan this one shares llm.ts/cockpit.md with)
  - phase: 03.10-01
    provides: gmail.search fixture seam (tokenless eval tenant can park candidates — fixture 20's turn resolves seeded rows)
  - phase: 03.6
    provides: eval gate (seedSkills publish → eval:golden evidence → activateSkill)
provides:
  - "resetPlan internal mutation (plans.ts): explicit slot-clear to a clean collecting state — patchPlan drops undefined and can never clear"
  - "resetPlan tool in buildCockpitTools: 'cancel and start fresh' actually mutates the row instead of being confabulated"
  - "Neutral pending-pick context statement (imperative → state fact): the skill, not the code, owns what to say while a pick is parked"
  - "cockpit-agent v9 active via the gate: reset flow + reply-vs-compose no-invent-subject + never-claim-unperformed + attach-only-on-ask"
  - "Fixture 20-reset-and-honesty (20-fixture golden set): plan-state proof that reset clears the accreted subject/body"
affects: [03.10-06, 03.10-07, cockpit, inbox-reply]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Explicit-write reset contract: a reset writer sets every slot field explicitly (clearCandidates/recordAttachments precedent) because patchPlan drops undefined keys"
    - "Context strings state facts, prompts own behavior: the pending-pick block became a neutral state statement so skill wording controls the reply"

key-files:
  created:
    - packages/backend/scripts/eval-cases/20-reset-and-honesty.json
  modified:
    - packages/backend/convex/plans.ts
    - packages/backend/convex/llm.ts
    - packages/backend/convex/cockpitTools.test.ts
    - packages/contracts/skills/cockpit-agent.md
    - packages/contracts/src/skills/cockpitAgent.ts
    - docs/playbooks/cockpit.md
    - docs/playbooks/agent-runtime.md
    - docs/playbooks/skill-registry.md

key-decisions:
  - "resetPlan is a COMPOSITION reset (collecting/proposed only) — scheduled-cancel stays cockpit.cancelScheduledPlan; the tool refuses past proposed"
  - "The pending-pick header changed from an imperative to a neutral state statement — the single highest-leverage re-announce-loop fix"
  - "The architectural reply-grounding rebuild stays DEFERRED (owner decision) — documented, escalatable ceiling"

requirements-completed: [UAT-D]

# Metrics
duration: not individually recorded (executed 2026-07-19; closed at the phase-wide sign-off)
completed: 2026-07-19
---

# Phase 03.10 Plan 05: Cockpit Reset + Honesty Summary

**A real resetPlan mutation+tool replaces the confabulated "I've canceled the plan", the pending-pick context block became a neutral state statement (killing the re-announce loop's code-side driver), and cockpit-agent v9 activated through the gate with fixture 20 proving a reset genuinely clears the accreted subject/body — UAT-D closed under the 2026-07-19 human sign-off.**

## Performance

- **Completed:** 2026-07-19
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify, closed by the phase-wide final replay)
- **Files modified:** 9 (commit 86e5a5f)

## Accomplishments
- `resetPlan` internal mutation (plans.ts, beside clearCandidates): explicit slot-clear — recipients [], status collecting, mode/subject/bodyIntent/body/attachments/attachmentError/candidates/pendingValid/greetingName/recipientBodies/sendAt undefined. Explicit because patchPlan drops undefined keys and can never clear a filled slot.
- `resetPlan` tool in buildCockpitTools (llm.ts): empty inputSchema, status guard (refuses scheduled/delivering/done — scheduled cancel stays the card's path), refs/counts-only return.
- Neutral pending-pick context statement (llm.ts): the header changed from an imperative ("tell the user to pick from the card") to a state fact ("A contact pick is still open…") — the skill now owns what to say each stalled turn.
- Skill mirror edit (cockpit-agent.md + byte-identical cockpitAgent.ts regeneration): reset section, reply-vs-compose (subject unknown → ASK, never invent), never-claim-an-unperformed-action, attach-only-on-explicit-request.
- Fixture `20-reset-and-honesty.json`: turn 1 sets subject+body for Sarah Chen; turn 2 cancels and drafts to Tom Alvarez — subjectPresent:false + bodyPresent:false after a turn that SET them is only possible if resetPlan actually cleared them; candidatesPending:true + recipientCount:0 + statusAtMost:collecting prove the fresh resolve and the intact recipient guard.
- 3 playbook bumps (cockpit.md, agent-runtime.md, skill-registry.md — Last verified 03.10-05), same commit per §9.

## Task Commits

1. **Task 1: resetPlan mutation + tool + neutral context + skill mirror + fixture 20 + playbooks** - `86e5a5f` (feat)
2. **Task 2: live gate cycle** - runtime procedure, no code commit (see below)
3. **Task 3: human-verify** - closed by the 2026-07-19 phase-wide sign-off (see below)

## Gate Cycle (Task 2)

- `seedSkills` minted cockpit-agent candidate **v9** (version read back at runtime, never hardcoded).
- `activateSkill` v9 REFUSED pre-evidence (EVAL_GATE) — gate proven.
- Pinned run **7940a9a0** — **20/20 green, $0.0791** (cap $1.00). Evidence recorded.
- `activateSkill` v9 succeeded; v9 confirmed active.
- Environment note: the FIRST eval attempt hit the documented retry-storm (InternalServerError at $0 — a destabilized `convex dev` after a network blip). Recovered per the eval-env-recovery procedure: kill ALL convex processes, restart ONE clean dev, verify stable, re-run. Exit-2 environment stops were never counted as case failures.

## Human Verify (Task 3)

The plan's blocking checkpoint was **superseded by subsequent live replays**, which surfaced two NEW defects — UAT-E (conversational amnesia, closed by plan 06) and UAT-F (post-pick trust collapse, closed by plan 07). The standing plan-05 checks — reset actually resets, no invented subject, no claimed-but-unperformed actions — were re-verified in every replay and **APPROVED in the final 2026-07-19 live replay** ("I'm satisfied. I've tested everything the way you asked me to test. Everything is good. I approve."). Zero unintended sends.

## Files Created/Modified
- `packages/backend/convex/plans.ts` - resetPlan internal mutation (explicit slot-clear).
- `packages/backend/convex/llm.ts` - resetPlan tool in buildCockpitTools; neutral pending-pick state statement.
- `packages/backend/convex/cockpitTools.test.ts` - adjusted for the new tool surface.
- `packages/contracts/skills/cockpit-agent.md` / `packages/contracts/src/skills/cockpitAgent.ts` - skill tightenings, byte-identical mirror (drift test green).
- `packages/backend/scripts/eval-cases/20-reset-and-honesty.json` - the reset-flow plan-state fixture.
- `docs/playbooks/cockpit.md`, `docs/playbooks/agent-runtime.md`, `docs/playbooks/skill-registry.md` - §9 bumps.

## Decisions Made
- resetPlan resets COMPOSITION only; the tool refuses past `proposed` — scheduled-send cancel remains `cockpit.cancelScheduledPlan`.
- The reply-grounding rebuild (structured/reconciled reply narration) stays deferred per the owner's decision — recorded as an escalatable ceiling (see deferred-items.md).

## Deviations from Plan

None in-scope — the plan executed as written. The reply-grounding rebuild remains the documented deferred ceiling (owner decision), not a deviation.

## Issues Encountered
- The documented eval retry-storm on the first pinned-run attempt (environment, recovered per procedure — see Gate Cycle above).

## User Setup Required
None.

## Next Phase Readiness
- UAT-D closed and gate-proven. Plans 06 (UAT-E) and 07 (UAT-F) built directly on this plan's resetPlan/neutral-statement seams.

---

## Self-Check: PASSED

- Commit `86e5a5f` FOUND on main (9 files, matches the file list above).
- `packages/backend/scripts/eval-cases/20-reset-and-honesty.json` FOUND on disk.
- resetPlan present in plans.ts and llm.ts (subsequent plan 07 extended its clear list with recipientNames — expected).
- Human sign-off recorded 2026-07-19.

---
*Phase: 03.10-cockpit-conversation-repair*
*Completed: 2026-07-19*
