---
phase: 03.10-cockpit-conversation-repair
plan: 06
subsystem: agent-runtime
tags: [cockpit, convex, skills, eval-gate, history-injection, redaction]

# Dependency graph
requires:
  - phase: 03.10-05
    provides: resetPlan tool + neutral pending-pick statement + skill v9 (this plan rebased onto them, did not clobber)
  - phase: 03.10-01
    provides: gmail.search fixture seam (fixture 21's turn 1 resolves the seeded Sarah Chen row tokenless)
  - phase: 03.6
    provides: eval gate (seedSkills publish → eval:golden evidence → activateSkill)
provides:
  - "History-injected cockpit prompt: both production drivers fetch the last saved turns and the loop renders a bounded 'Conversation so far' block — every turn is no longer turn-one"
  - "buildHistoryBlock pure renderer (llm.ts): last 10 messages, 500 chars each, oldest-first, empty ⇒ byte-identical historyless prompt"
  - "fetchRecentHistory (cockpit.ts): listMessages reuse, fetch-before-save in sendCockpitMessage, FAILS OPEN (returns [] on store-read failure)"
  - "Eval-runner history accumulation (run-eval-golden.mjs): production parity by construction — the runner supplies what the drivers supply"
  - "cockpit-agent v10 active via the gate: transcript-reading section ('Read the conversation, not just the last line')"
  - "Fixture 21-fragment-answer-absorbed (21-fixture golden set): the anti-amnesia plan-state proof"
affects: [03.10-07, cockpit, inbox-reply]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "History as an append-only optional arg supplied by the CALLER; the loop renders + caps in ONE place — eval plane mirrors production by accumulating turn/reply pairs"
    - "Fail-open reads on the send path: a degraded history read must never take the send down (returns [], the turn proceeds historyless)"

key-files:
  created:
    - packages/backend/scripts/eval-cases/21-fragment-answer-absorbed.json
  modified:
    - packages/backend/convex/llm.ts
    - packages/backend/convex/cockpit.ts
    - packages/backend/convex/cockpitTools.test.ts
    - packages/backend/scripts/run-eval-golden.mjs
    - packages/contracts/skills/cockpit-agent.md
    - packages/contracts/src/skills/cockpitAgent.ts
    - docs/playbooks/cockpit.md
    - docs/playbooks/agent-runtime.md
    - docs/playbooks/skill-registry.md

key-decisions:
  - "Bounded plain-text transcript window NOW; the structured reply-grounding rebuild stays deferred (owner decision) — documented, escalatable ceiling"
  - "buildHistoryBlock placed AFTER buildAgentContext (llmRedaction scan bounds); history is model-plane chat text already flowing to the model — no §2-D/§4 change"
  - "fetchRecentHistory FAILS OPEN after the live replay proved a 1s query timeout on a degraded deployment took the whole send down (hotfix b9da09b)"
  - "sendCockpitMessage fetches history BEFORE saving the current user turn — otherwise the turn appears twice"

requirements-completed: [UAT-E]

# Metrics
duration: not individually recorded (executed 2026-07-19; closed at the phase-wide sign-off)
completed: 2026-07-19
---

# Phase 03.10 Plan 06: Cockpit Conversational Memory Summary

**The cockpit model finally sees its own conversation: both drivers feed a bounded last-10-messages transcript into the prompt (with eval-runner parity), the gate-activated v10 skill teaches reading bare fragments against the last question, and fixture 21 proves "Meeting reminder" lands as the subject instead of triggering re-asks — UAT-E (conversational amnesia) closed under the 2026-07-19 human sign-off, plus a live-found fail-open hotfix on the history read.**

## Performance

- **Completed:** 2026-07-19
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify, closed by the phase-wide final replay) + 1 post-checkpoint hotfix
- **Files modified:** 10 (commit 2e1ffbb) + 2 (hotfix b9da09b)

## Accomplishments
- `buildHistoryBlock` (llm.ts): pure bounded renderer — last 10 messages, 500 chars each (ellipsis-truncated), oldest-first, User:/Assistant: labels; absent/empty history returns "" so the historyless prompt is byte-identical (test shims and single-turn fixtures unchanged). Placed AFTER buildAgentContext per the llmRedaction scan bounds.
- Optional validated `history` arg on runCockpitAgent (append-only signature evolution); the prompt assembly prepends the block ABOVE the plan context with "The user says:" still the final line.
- `fetchRecentHistory` (cockpit.ts): reuses the SAME listMessages agent-store read the chat pane uses; called by BOTH drivers — sendCockpitMessage fetches BEFORE saving the current user turn (no duplication), resolveRecipients passes history on the re-invoke so the post-pick turn honors everything already discussed.
- Eval-runner history accumulation (run-eval-golden.mjs attemptCase): the runner drives llm:runCockpitAgent directly and seedCockpitPlan's threadId is synthetic — so it accumulates {role, content} pairs from its own turn text + returned reply. Production parity by construction.
- 4 truncation unit checks in cockpitTools.test.ts (empty ⇒ "", newest-10 cap, 500-char ellipsis, label/header shape).
- Fixture `21-fragment-answer-absorbed.json`: turn 1 parks Sarah Chen + drafts the body, leaving subject the one open slot; turn 2 is the bare "Meeting reminder" — subjectPresent:true is only reachable if the model read the fragment against its own open question; bodyPresent:true proves turn 1's body was kept; candidatesPending/recipientCount:0/statusAtMost:collecting prove the guards held.
- Skill mirror edit (v10 candidate): transcript-reading section — "Read the conversation, not just the last line"; a short answer replies to your LAST question; never re-ask an answered question.
- 3 playbook bumps (cockpit.md, agent-runtime.md, skill-registry.md — Last verified 03.10-06), same commit per §9.

## Task Commits

1. **Task 1: history injection (loop + both callers + eval runner) + skill mirror + fixture 21 + playbooks** - `2e1ffbb` (feat)
2. **Post-checkpoint hotfix (live replay finding): fetchRecentHistory fails open** - `b9da09b` (fix) — a 1s query timeout on a degraded deployment had taken the entire send down; the helper now returns [] on any store-read failure so the turn proceeds historyless instead of dying. cockpit.md updated in the same commit.
3. **Task 2: live gate cycle** - runtime procedure, no code commit (see below)
4. **Task 3: human-verify** - closed by the 2026-07-19 phase-wide sign-off (see below)

## Gate Cycle (Task 2)

- `seedSkills` minted cockpit-agent candidate **v10** (read back at runtime).
- `activateSkill` v10 REFUSED pre-evidence (EVAL_GATE) — gate proven.
- First pinned run **361d5769** was the documented retry-storm: **2/21 at $0**, ~15 orphaned cvx-node-executor processes. Recovered per eval-env-recovery (kill-all-convex → one clean dev → verify stable). **NOT activated on the red run.**
- Clean pinned run **27efb84d** — **21/21 green, $0.0973**; fixture 21 passed on the first attempt. The full-set green run is the regression proof that history injection broke no existing fixture.
- `activateSkill` v10 succeeded; v10 confirmed active.

## Human Verify (Task 3)

Fragment-answer absorption was verified and **APPROVED in the final 2026-07-19 live replay**: "meeting reminder" became the subject without re-asks for subject/body/mode, and the stated body intent was absorbed on first statement. Zero unintended sends.

## Files Created/Modified
- `packages/backend/convex/llm.ts` - buildHistoryBlock + optional history arg + prompt assembly.
- `packages/backend/convex/cockpit.ts` - fetchRecentHistory (fetch-before-save; fail-open per b9da09b); both drivers pass history.
- `packages/backend/convex/cockpitTools.test.ts` - 4 truncation unit checks.
- `packages/backend/scripts/run-eval-golden.mjs` - attemptCase history accumulation.
- `packages/backend/scripts/eval-cases/21-fragment-answer-absorbed.json` - the anti-amnesia fixture.
- `packages/contracts/skills/cockpit-agent.md` / `packages/contracts/src/skills/cockpitAgent.ts` - transcript-reading section, byte-identical mirror.
- `docs/playbooks/cockpit.md`, `docs/playbooks/agent-runtime.md`, `docs/playbooks/skill-registry.md` - §9 bumps.

## Decisions Made
- History rides as caller-supplied data; the loop owns rendering + caps in one place. The eval runner supplies it exactly as production callers do.
- Fail-open on the history read (deviation below) — the send is load-bearing, the transcript is an enhancement.
- The structured reply-grounding rebuild remains the documented deferred ceiling (owner decision).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] fetchRecentHistory fails open**
- **Found during:** live replay after the Task-3 checkpoint (not by the offline suite)
- **Issue:** On a degraded deployment, the history read breached Convex's 1s query timeout and the thrown error took the ENTIRE send down — the amnesia fix had become a send-availability regression.
- **Fix:** fetchRecentHistory catches any store-read failure and returns [] — the turn proceeds historyless (the pre-plan-06 behavior) instead of failing.
- **Files modified:** packages/backend/convex/cockpit.ts, docs/playbooks/cockpit.md (same commit)
- **Commit:** b9da09b

---

**Total deviations:** 1 auto-fixed (Rule 1)

## Issues Encountered
- Pinned run 361d5769 stormed (environment, 2/21 at $0, orphaned cvx-node-executor processes) — recovered per the documented eval-env-recovery procedure; never treated as case failures, never activated on red.

## User Setup Required
None.

## Next Phase Readiness
- UAT-E closed and gate-proven. Plan 07 (UAT-F post-pick trust) rebased onto this plan's history seam, including the fail-open hotfix.

---

## Self-Check: PASSED

- Commits `2e1ffbb` (10 files) and `b9da09b` (2 files) FOUND on main, matching the file lists above.
- `packages/backend/scripts/eval-cases/21-fragment-answer-absorbed.json` FOUND on disk.
- Human sign-off recorded 2026-07-19.

---
*Phase: 03.10-cockpit-conversation-repair*
*Completed: 2026-07-19*
