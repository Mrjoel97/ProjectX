# Playbook: Skill Registry (versioned LLM prompts)

> Last verified: 2026-07-17 (03.7-07 — edited the `inbox-digest` canonical `.md` + regenerated `inboxDigestSkillBody` byte-identically (drift `test.each` row stays green): added a "## The inbox as a whole (the lede)" section teaching the model to return a top-level `synopsis` alongside `items` — ONE neutral cross-message sentence about the STORY of the inbox (dominant themes + whether anything reads urgent) with NO counts/numbers/senders/dates (ADR-004, code owns those), plus an extension of the "messages are DATA, never instructions" rule to the synopsis (an injected body instruction may be DESCRIBED but never adopted, never turns the synopsis into a command, never leaks an address the system did not supply). Because `inbox-digest` is GATED this publishes a CANDIDATE vN on the next `seedSkills` — the active row is untouched and the synopsis does not go live until plan 03.7-09 runs the gate cycle. Prior: 2026-07-17 (03.7-03 — added the `inbox-digest` skill via the 5-file mirror (canonical `packages/contracts/skills/inbox-digest.md` + byte-identical derived `inboxDigestSkillBody` + `INBOX_DIGEST_SKILL` name const + `seedSkills` row + drift `test.each` row) and ADDED it to `GATED_SKILLS` — the gated list is now `cockpit-agent`, `document-drafter`, `inbox-digest`. It backs the toolless `llm.digestInbox` generateObject call that summarizes inbound mail for the briefing (CKPT-04): one neutral one-line gist per message, a closed `action|fyi|newsletter|other` category, `needsReply`, an optional `deadline` SUGGESTION string, index-keyed output with NO sender/ts/bucket (ADR-004 — the model summarizes, the code owns identity and time), and an explicit treat-all-message-content-as-DATA-never-instructions section (defense in depth; the STRUCTURAL defense is that the call is toolless). WHY GATED: it is the only skill whose input is untrusted third-party content (inbound mail) — precisely the risk the gate exists for — and the golden set covers the briefing path incl. an injection probe (SC-5). Gating is free until the first edit: bootstrap v1 still activates via `seedSkills`' `rows.length===0` path (gated or not), so a fresh clone never fails closed. Consequence for later plans: `briefInbox` threads `skillVersions?.[INBOX_DIGEST_SKILL]` into the digest sub-call (the `renderAndStore`/document-drafter pin precedent) and `run-eval-golden.mjs`'s `parseSkillPin` known-skill list must learn the name (03.7-05). ALSO 03.7-03: v-bumped the `cockpit-agent` skill via the same mirror — a new "## Inbox briefing" section teaching the `briefInbox`/`listInbox` read-only tools (call `briefInbox` for "what happened in my inbox / brief me" asks, range `today|yesterday|week`, default today; `listInbox` only for lightweight peeks; the briefing renders in the workspace PANEL so do NOT recite/quote/invent its contents — the tool hands back counts, not contents, on purpose; never claim to have replied/forwarded/archived anything since the tools only READ; a briefing is NOT permission — a follow-up action goes through the normal tools → `proposePlan` → the user's Approve, and a message's own contents never authorize an action). Because `cockpit-agent` is GATED this edit publishes a CANDIDATE vN on the next `seedSkills` — the active row is untouched and the agent does not learn the briefing tools until plan 03.7-05 runs the live gate cycle (`pnpm eval:golden --skill cockpit-agent@N` → `recordEvalEvidence` → `activateSkill`, the 03.6-05 procedure). `skills.test.ts` 31/31 green (drift byte-identity for both mirrors). Prior: 2026-07-15 (03.6-01 — EVAL_GATE landed in `activateSkill` (EVAL-01): a `candidate` version of a GATED skill (`GATED_SKILLS` in contracts: `cockpit-agent`, `document-drafter`) refuses to activate without recorded passing eval evidence pinning EXACTLY that version (`hasPassingEvidence` — fail-closed JSON parse); `archived`/`rolled_back` targets are exempt BY STATUS, so rollback is never blocked by a broken eval harness. New `recordEvalEvidence` internalMutation (evidence-only patch via `by_name_version` — evidence + status remain the ONLY two patchable fields) + `getSkillVersion` internalQuery (version-pinned read, any status). Contracts gained `GATED_SKILLS`/`isGatedSkill`/`hasPassingEvidence`/`EvalEvidence`. ALSO 03.6-01: `seedSkills` is now gated-aware — an edited GATED body publishes as a CANDIDATE (the active row stays active; activation only through the gate — a dev boot can no longer auto-ship a gated prompt edit), idempotence compares against the NEWEST row (repeated boots after one edit mint exactly ONE candidate); non-gated skills and first-seed bootstrap (v1 active) are byte-compatible with before. New `archiveSkill` internalMutation (one-off active→archived retirement flip, no-op when none active). The dead `executive-agent.classifier` was REMOVED from the seeds array (a re-seed cannot resurrect it; the live row's archival runs in plan 05) — its contracts constant + canonical `.md` stay as historical record. Prior: 04-02 — added the `attachment-extractor` skill via the 5-file mirror (canonical `packages/contracts/skills/attachment-extractor.md` + byte-identical derived `attachmentExtractorSkillBody` + `ATTACHMENT_EXTRACTOR_SKILL` name const + `seedSkills` row + drift `test.each` row): an OCR/extraction system prompt — extract all text VERBATIM preserving structure, or factually describe non-text visual content, plain text output only — for the inbound image/PDF intake OCR call (INTK-02). No new skill code, an append to the existing registry rows. `skills.test.ts` 13/13 green (drift byte-identity + active-seed). Prior: 03.5-02 — v-bumped the `cockpit-agent` skill via the 5-file mirror (canonical `.md` edited, derived `cockpitAgentSkillBody` regenerated byte-identically; `seedSkills` publish-on-change ships it as v-next; the drift `test.each` row stays green): added a "## Scheduling" section teaching the new `setSendTime` tool — when the user volunteers a send time ("send at 4pm tomorrow"), pass the phrase to `setSendTime` (the app resolves it against the user's own clock+zone — the agent NEVER supplies "now" or the timezone), CONFIRM the resolved absolute time back to the user, and on an ambiguous/past answer ASK one focused question (never guess); no time mentioned → say nothing (immediate on approve is the default). Safety voice unchanged (address-by-index/label only, no self-send, redaction). No new skill row — an edit to the existing one; the `setSendTime` tool + §2-D clock threading half lives under cockpit.md. Prior: 03.4-02 — v-bumped the `cockpit-agent` skill via the 5-file mirror (canonical `.md` edited, derived `cockpitAgentSkillBody` regenerated byte-identically; `seedSkills` publish-on-change ships it as v-next; the drift `test.each` row stays green): added a "## Personalization" section teaching the new `personalizeRecipient` tool — tailor ONE recipient's wording by `#index` (never an address), SUGGEST-then-confirm before tailoring (never personalize unasked), and personalization REQUIRES individual mode (a group send is one combined email, so `proposePlan` refuses a group plan that carries tailoring — set `individual` first). Safety voice unchanged (address-by-index/label only, no self-send, redaction). No new skill row — an edit to the existing one. Prior: 03.3-06 gap-closure — reworked the `document-drafter` prompt after CKPT-02 human-verify found the generated PDF shallow and full of raw `**`/`#`: the output contract now instructs structured, professional markdown the upgraded renderer supports — `##`/`###` sections, `**bold**` labels, `- ` bullets, `1.` numbered steps, and GitHub pipe tables — with a framing intro + a closing section, no title repetition, and still no invented facts. Shipped via the 5-file mirror + `seedSkills` publish-on-change (live-verified active). The renderer half lives under cockpit.md. Prior: 03.3-06 gap-closure — reworked the `cockpit-agent` prompt after CKPT-02 human-verify caught the agent dropping an explicitly-requested attachment: replaced the rigid ordered "Making progress" pipeline (which walked recipients→subject→body→mode and never revisited a bundled attachment request) with a requirement-driven "Working a message" + a "Serve the user, not a script" contract — read the WHOLE message, act on EVERY intent it contains in the same turn (including `generateAttachment` for an explicitly-asked attachment, which is now the go-ahead, not a suggest-then-confirm gate), never re-ask for what the user already gave, and ASK one focused question only for what is genuinely missing rather than inventing it. Safety rails unchanged (address-by-index/label only, resolve-through-panel STOP, no self-send, redaction). Shipped via the 5-file mirror + `seedSkills` publish-on-change (live-verified active). Prior: 03.3-04 — v-bumped the `cockpit-agent` skill via the 5-file mirror (canonical `.md` edited, derived `cockpitAgentSkillBody` regenerated byte-identically; `seedSkills` publish-on-change ships it as v-next; drift `test.each` row stays green): the agent now SUGGESTS an attachment in one question and calls `generateAttachment` only after the user confirms, reasons about attachments by filename/`#index`, and still never claims a send. No new skill row — an edit to the existing one. Prior: 03.3-01 — added the `document-drafter` skill (5-file mirror: canonical `.md` + derived `documentDrafterSkillBody` + `DOCUMENT_DRAFTER_SKILL` name const + `seedSkills` row + drift `test.each` row). It backs `draftDocument` for attachment generation (CKPT-02). Prior: 03.2.1 gap-closure — `seedSkills` gained a PUBLISH-ON-CHANGE path: an edited skill body is published as a new immutable version + activated, closing the old "no production write path for v2+" gap. Used it to ship a `cockpit-agent` prompt edit (contact resolution is now panel-driven: after `resolveContacts` the agent STOPS and the user picks in the panel; never set a resolved contact via `addRecipients`/`setRecipients`) as v2. Prior: 03.2.1-01) Merged (Lane C, Phase 5): — added the `graph-extractor` skill via the 5-file mirror (canonical `.md` + byte-identical derived `graphExtractorSkillBody` + `GRAPH_EXTRACTOR_SKILL` name const + `seedSkills` row + drift `test.each` row). It backs the GraphRAG entity/relationship extraction at vault ingest (VALT-02): given a document's already-redacted text, emit structured `{ nodes:[{type,name}], edges:[{from,to,rel}] }` over a fixed type set (person/org/project/place/topic/other), extract-only-never-invent, no raw addresses/secrets echoed. Fails closed unseeded, no hardcoded prompt (§5).
> Build history: `.planning/phases/01-foundation-governance-substrate/01-04-*.md` · Related ADRs: [003](../decisions/003-skill-registry-for-prompts.md)

## Purpose

All agent/LLM prompts live as versioned rows in the `skills` table (`name`, `version`,
`body`, `status`) and are loaded at runtime — never hardcoded in source (CLAUDE.md §5).
A prompt change is a NEW version row plus an atomic status flip, never an in-place
edit. This gives versioning, rollback, and audit-able `{name, version}` telemetry, and
it is the substrate the Phase 8 SkillOpt optimization loop will operate on.

## Key files

- `packages/backend/convex/schema.ts` — `skills` table + `by_name_status`, `by_name_version` indexes
- `packages/backend/convex/skills.ts` — the module (the SOLE status-writing module): `loadSkill`, `getActiveSkill` (internalQuery), `getSkillVersion` (internalQuery — version-pinned read, any status), `activateSkill` (internalMutation — the sole ACTIVATION path and the EVAL_GATE choke point), `recordEvalEvidence` (internalMutation — evidence-only patch), `seedSkills` (internalMutation — idempotent vs the newest row; publishes an edited body as CANDIDATE for gated skills, publish-and-activate for non-gated), `archiveSkill` (internalMutation — one-off retirement flip)
- `packages/contracts/src/skill.ts` — loader contract, error prefixes, skill-name constants, `GATED_SKILLS`/`isGatedSkill`/`hasPassingEvidence`/`EvalEvidence` (the gate's pure logic)
- `packages/contracts/skills/*.md` — canonical human-editable prompt bodies; `packages/contracts/src/skills/*.ts` — derived bundler-safe constants (drift between the two is test-enforced)
- `packages/backend/convex/llm.ts` — the runtime callers (`route`, `draft`, `draftCockpit` → `getActiveSkill`)
- `packages/backend/scripts/run-seed.mjs` — post-deploy seed runner
- `packages/backend/convex/skills.test.ts` — loader/activation/immutability/no-hardcoded-prompt tests

## Dependencies & blast radius

`graphify query "skills registry"`. Every LLM call site depends on an active skill row
existing — **a fresh deployment without seeding dead-letters every request**
(`NO_ACTIVE_SKILL: executive-router`). Current seeded skills: `executive-router`,
`email-drafter`, `cockpit-agent`, `document-drafter`, `attachment-extractor`,
`graph-extractor`, `inbox-digest` (the dead `executive-agent.classifier` was de-seeded and
archived in 03.6-01; its constant + `.md` remain as historical record). GATED (candidate
activation requires eval evidence): `cockpit-agent`, `document-drafter`, `inbox-digest`
(03.7-03 — the one skill whose INPUT is untrusted third-party mail). The active row's `version`
is part of the LLM action-cache key, so activation/rollback automatically invalidates
cached outputs.

## Data flow

1. **Seed + publish**: `seedSkills` inserts each skill v1/`active` on first run (bootstrap — gated or not, a fresh clone never fails closed). On re-run it is idempotent when the body is UNCHANGED vs the NEWEST row (so an already-published candidate is not re-minted). When a `.md` edit has changed the derived constant it publishes a NEW version (`maxVersion+1`) — never mutating a prior row (immutable-per-version): **non-gated** skills archive the old active row and activate the new one (unchanged behavior); **gated** skills insert the new version as `candidate` and leave the active row untouched — activation only through the gate (step 4). Local dev auto-seeds (`convex dev --run skills:seedSkills`); production requires `npm run seed` after `npx convex deploy`.
2. **Load**: `loadSkill(ctx, name)` selects the single row with `status == "active"` for the name (`.unique()` — throws on 0 or >1). Selection is active-row-wins, NOT max-version. `getSkillVersion(name, version)` is the version-pinned read (any status) the eval runner threads into the agent loop so a candidate evaluates as itself.
3. **Missing/ambiguous** → throws `NO_ACTIVE_SKILL` → the pipeline dead-letters the request. Fail-closed by design: an agent can never silently run without a governed prompt.
4. **Gated publish → eval → activate** (EVAL-01): edit the `.md` → dev boot/`seedSkills` publishes candidate vN (active untouched) → `pnpm eval:golden --skill <name>@<N>` (plan 04) → a green run records evidence on the row via `recordEvalEvidence` (refs/counts-only JSON: `{runner, runId, pass, casesPassed, casesTotal, retriedCases, costUsd, model, skillVersions, ts}` — never raw content, §4) → `npx convex run skills:activateSkill '{"name":"<name>","version":<N>}'` passes the gate (`hasPassingEvidence` requires `pass === true` AND `skillVersions[name] === N` — stale/mismatched/unparseable evidence fails closed). Non-gated candidates activate without evidence.
5. **Rollback**: `activateSkill(name, priorVersion)` — same atomic flip in reverse, and STRUCTURALLY exempt from the gate: an `archived`/`rolled_back` target was active before and activates on status alone, no evidence needed. A broken eval harness can never block a mid-incident rollback.

## Invariants — what must never break

- **No hardcoded prompts in source**: enforced by `skills.test.ts`, which scans `convex/*.ts` and fails on any string literal >200 chars. Also enforces `.md` ↔ derived `.ts` body no-drift.
- **Bodies immutable per version**: only `status` and `evidence` are EVER patched — never `body`/`name`/`version`. Tested (v1 body unchanged after activating v2; `recordEvalEvidence` patches evidence only).
- **Exactly one active row per name**: `.unique()` on `by_name_status`; loader throws otherwise.
- **Fail-closed loader**: `NO_ACTIVE_SKILL` throw, never a fallback default prompt. Tested in `skills.test.ts` and `cockpitDraft.test.ts` (unseeded `email-drafter`).
- **`skills.ts` is the sole status-WRITING module** (restated 03.6-01; it uses the raw internal builder and sits on the plan-02 allow-list — do not add writers elsewhere). Within it: `activateSkill` is the sole ACTIVATION path (and the EVAL_GATE choke point), `seedSkills` writes `candidate`/`active` on publish, `archiveSkill` is the one-off retirement flip.
- **Gated candidates only activate through a recorded green eval** (EVAL-01, 03.6-01): gated list = `cockpit-agent` + `document-drafter` + `inbox-digest` (03.7-03) (`GATED_SKILLS` in `@pikar/contracts`); a `candidate` target without version-pinned passing evidence → `EVAL_GATE` refusal. `seedSkills` never auto-activates a gated edit — the gate would be decorative otherwise.
- **The eval gate never blocks rollback** (EVAL-01, 03.6-01): only a `candidate` version of a gated skill needs passing evidence; `archived`/`rolled_back` targets were active before and are exempt purely by status — a broken eval harness must never stop a mid-incident rollback.
- **Skills are GLOBAL** — the table has no `tenantId` (deliberate; the LLM cache is tenant-namespaced, the prompt body is shared). Don't "fix" this by adding tenancy without an ADR.

## How to change safely

- **Editing a NON-gated prompt**: edit the canonical `.md` in `packages/contracts/skills/`, regenerate/update the derived `.ts` constant, re-seed — publish-and-activate is automatic. Never edit an existing row's body — the immutability test will not catch a direct DB edit, only discipline does.
- **Editing a GATED prompt** (`cockpit-agent`, `document-drafter`, `inbox-digest`): same edit + re-seed, which lands a CANDIDATE only; then run the eval (`pnpm eval:golden --skill <name>@<N>`) and `activateSkill` once evidence is green. There is no bypass switch — if the harness is broken, fix the harness (rollback of an already-active version still works regardless).
- **Adding a skill**: add the `.md` + derived constant + name constant in `contracts/src/skill.ts`, extend `seedSkills`, seed the deployment. Decide gating: add to `GATED_SKILLS` if its output leaves the building.
- **Retiring a skill**: remove its `seedSkills` entry FIRST (or the next boot resurrects it), then `npx convex run skills:archiveSkill '{"name":"<name>"}'` on the deployment. Keep the contracts constant + `.md` as historical record.
- **Rollback of a bad prompt**: `npx convex run skills:activateSkill '{"name":"<name>","version":<prior>}'` — cache invalidation is automatic via the version-keyed cache.
- **Do not bump** the pinned agent/workflow component versions as part of skill work (CLAUDE.md §6).

## How to verify

- `pnpm --filter @pikar/backend test` (or `vitest run skills`) — seed+load, fail-closed throw, idempotent seed, atomic flip + immutability, gate refuse/exempt/stale-evidence/fail-closed-parse, gated candidate-publish + idempotence, classifier non-resurrection, md/ts no-drift, no long inline literals
- Seed check: `npm run seed` in `packages/backend` (calls `seedSkills` then asserts `executive-router` is active)

## Operational notes

- **Fresh deploy checklist**: `npx convex deploy` → `npm run seed`. Skipping the seed dead-letters everything.
- Skill names are hyphenated (`executive-router`); `executive_router` silently fails to match.
- `run-seed.mjs` judges success by CLI *output*, not exit code — the convex CLI returns a bogus non-zero exit on Windows/Node 24.
- Rollback is CLI-only today; there is no admin UI.

## Known gaps & deferred work

- **Source prompt edits publish via `seedSkills`** (a changed body → new version; gated → candidate, non-gated → activate; 03.2.1, gated split 03.6-01). Phase 8 (SkillOpt) remains the designated owner of the *automated* candidate→eval→activate loop; `seedSkills` covers the manual source-edit→publish path (edit `.md` → regenerate `.ts` → re-seed).
- The `rolled_back` status literal is still never WRITTEN (rollback produces `archived`) but since 03.6-01 it is READ: the gate's structural exemption checks for it. Keep the literal.
- The live `executive-agent.classifier` row's archival (`npx convex run skills:archiveSkill`) runs in plan 03.6-05's checkpoint — until then the deployed row is active but unreachable (nothing loads it).
