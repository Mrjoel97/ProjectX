# Evaluation evidence release — 2026-09-11

This continues the owner-authorized audit implementation and production promotion.
Baseline: `a0996f5d7656ab7b4961161570d6ea5a409fee98`. Target: the existing Convex/Vercel
application at `https://www.pikar-ai.com`. This file describes the candidate; successful
CI and production workflow receipts for its exact commit establish deployment.

## Changes

- Native Phase 30 case start, observed outcome, authenticated semantic review, immutable
  issuance and recovery records bind exact candidates, source bytes, corpus and evaluator.
  Generic audit/evidence submission cannot manufacture vertical qualification.
- Fixture cleanup preserves pending human reviews and authoritative spend records.
- One golden evaluation budget covers seed embeddings, model steps, nested drafting,
  dispatch, research, ingestion and Tavily. Unknown costs or billing retain reservations;
  ambiguous paid requests cannot automatically retry. Closure checks unfinished child work.
- Golden suite revisions now include an evaluator-source hash. Earlier evidence does not
  qualify the changed evaluator. Existing active skill rows are not changed by this release.
- Tool-step completion uses an exact tenant/turn/step index, including steps beyond the
  former twelve-row scan. Existing duplicate keys keep first-recorded behavior.
- Production-targeted authoring preparation, offline operator checks and CI regressions
  preserve exact-version evidence requirements. Marketing research remains preparation.
- Research uses structured claim references and checks quoted excerpts against actual tool
  results. Code labels excerpt access, never semantic support or independent corroboration;
  legacy/malformed answers remain explicitly unverified without another paid request.
- QuickBooks smoke operations select the explicit Convex deployment without shell parsing.
  Their legacy count is described as entity-read coverage, not a measured HTTP polling cost.
- Failed or refused media submissions now resolve render/caption failure through the existing
  terminal logic. Late callbacks cannot overwrite terminal jobs; bounded current-batch checks
  prevent historical failures from terminating a newer attempt. Reconciliation can only record
  a failure, never schedule a render or buy another asset. The verbatim voice gate is retained.
- The workspace keeps the media canvas visible across plan states and suppresses email
  delivery labels for media. Render failures and finished assets no longer disappear into
  a recipient report after Generate.
- Backend declares the already-used TypeScript 7.0.2 compiler directly so the Convex CLI
  can find it for strict local dry-run checks; its lockfile peer bindings follow that declaration.

## Qualification

Native Convex code generation, the production web build, all twenty free gates, source
provenance, twelve workspace typechecks and the non-backend package test tasks passed.
The eleven isolated operator-control Node tests and the 1007-file Biome check passed.
The production dry-run passed with no deleted indexes and the intended additive step index.
The first full backend run exposed three integration checks, which were repaired and passed
individually; its worker timeout was not treated as success. A complete lower-concurrency rerun
passed on 2026-09-12: 4,358 tests across 163 files, with no worker timeout. Backend
TypeScript, Biome, planning, playbooks, source pins and whitespace checks also passed.
The subsequent research repair passed 392 affected tests and backend TypeScript; refreshed
golden and vertical evaluator source pins and source provenance passed their offline checks.
The media terminal repair passed 324 media/reliability tests, including six new regressions,
and backend TypeScript. The exact production voice refusals remain failed acceptance, not a
completed video; deployment of honest failure handling does not manufacture that outcome.
The UI repair passed 142 tests, including eight rendered lifecycle/report regressions, and web
TypeScript. The remaining eleven workspace typechecks passed after the research changes.

The bounded graph refresh extracted 2,910 files but timed out before completing the
rebuild. The existing graph remains available; the Convex fixup succeeded and added
three edges. This is partial graph maintenance, not a verified complete refresh.

Fresh ordinary production sign-in was observed in the acceptance browser. Authenticated
desktop dashboard rendering had no horizontal overflow. Further live acceptance is recorded
in [the continuation audit](../../.planning/audits/2026-09-10-live-acceptance-continuation.md);
offline checks and deployment do not stand in for real workflow outcomes.

At `2026-09-12T07:45:15.922Z`, a status-only administrative readback confirmed the isolated
two-note acceptance folder is complete, its digest is built, and both source documents and
the generated summary are ready with RAG entries. This corroborates persistence/indexing;
the separate browser record establishes what the user interface actually showed.

## Rollout and recovery

Use the existing `[deploy]` main-branch pipeline: exact successful CI SHA, staged web build,
Convex dry-run/generated-file check, backend deploy, seed/verification, staged probe,
promotion and durable production URL probe. No direct promotion bypass is part of this release.

Schema changes are additive: `agentSteps.by_turn_step` and optional
`vaultDocuments.evalBudgetId`. Existing rows need no backfill. Once evaluated documents exist,
retain that field and budget-aware ingestion/recovery during repair. Older ingestion code
could ignore it and resume paid work without its envelope; use a forward repair preserving
these controls instead of redeploying the old backend wholesale.

## Remaining acceptance gates

Phase 23 still needs two fresh controlled identities, ordinary authored-candidate handoff,
verified provider billing, fresh approval of the displayed exact-candidate paid evaluation,
and observed activation/rollback. Phase 30 still needs real model outcomes, human semantic
reviews, authenticated all-six workflow acceptance and lifecycle drills. No candidate is
activated, no paid qualification is run and no evidence is invented by this release.

Phase 31 application work awaits the explicit semantic contract in plan 31-00. Recurrence
remains deferred until its DST/OAuth evidence gates pass. WORM remains off.
